# adaptive-dsp/memory.md 定向反扫 · **候选清单**(2026-08-05,第 7 实例自查)

```
⛔⛔ 本件是【候选清单】,**不是**「已查干净」的结论 —— 那个结论只能由独立 critic 出(lead 2026-08-05 裁定)
用途:把 critic 下一轮的工作量从「通读 781 行」变成「复核 N 条候选」
范围:agents/adaptive-dsp/memory.md(781 行);串源 = 常设反扫表 8 条 + 各轮撤回记录
```

## ⛔ 先写这次扫描**扫不到什么**(照我自己那条:落盘一张表前问「它丢掉了什么」)

```
① ⛔⛔ **只能命中我列得出的串** —— 而"我不知道它已经过期"的结论**根本不在表上**
   ⇒ 这正是 `:102` 那五项当初的处境:它们没被漏扫,是**从来没有人把它们列进任何表**
   ⇒ ∴ 本扫描对【未被登记的撤回】**零覆盖**
② **语义型过期**:结论不含数字(如"某机制成立"后被实测证伪)⇒ **无串可扫**
③ **跨件型过期**:memory 里的字**没变**,而它引用的**源件变了** —— `:102` 五项正是这种
   ⇒ 扫 memory 本身**发现不了**,必须逐条回源核(我这次对那五项是回源核的,对其余不是)
④ **粒度之下**:整条成立而其中一个从句过期 ⇒ 串在,判定不在
⑤ **假阳性率高**:本次 17 处命中里 **13 处是假阳性(76%)** ⇒ ⛔ 裸串计数不能判定,必须看上下文
```

---

## A. 真候选(**4 条**,我判为需处置;⛔ 我未自行修改,交 critic 复核)

| # | 行 | 现文 | 我判 | 理由 |
|---|---|---|---|---|
| A-1 | `:272` | 「**WORD32 档 20.16KB 仍越界**,保留为未排除的下行场景」 | **真过期** | 同文件 `:286` 自述本轮反扫「抓到 1 处真残留 —— §9-⑮ 仍写 m-4 修正前的 WORD32 旧值(**20.16 → 20.53KB**)」⇒ **正确值 20.53**,而 `:272` 仍是 20.16。⚠ 结论(越界)不变,错的是数 —— 与 `:102` 的 ② 同形态 |
| A-2 | `:112` | 「等代价平坦衰减 6/6 赢过陷波(7.80–9.31 vs **5.67**)」 | **真候选** | `5.67` 是 D6-x 限定串表点名要求**同段出现限定**的那个数(带限 8k / [L2/宿主仿真] / 上界);此行**无任何限定**。⚠ 且「上界」这个名字后被 F65.3 收窄为**只在 `bw_oct=1/5` 上成立** |
| A-3 | `:671` | 「B-1 的 **1.00–2.50 dB** 须标注"在 src_rms=−60 上测得"」 | **真候选(限定过弱)** | 该行的处置("补一句限定")**已被更强的定案取代**:B-1 是「**有效但不适用**」—— 需要**标称电平上的新测量**,不是给旧数补限定(见本文件后段 lead 更正)。⚠ 而"补限定即可"正是那条被明令禁止的读法 |
| A-4 | `:677` | 「`nhs.py` 加 `prefer_unnotched`(默认 False)」+ 哈希链 | **真候选(缺后续)** | 只记了"加了开关",**没记它后来的裁决**:F60 **实测证伪,判不提交**。⇒ 读到此行的新实例会以为它是个在用的修法 |

---

## B. 假阳性(**13 处**,我判无需处置;逐条给理由以便复核)

| 行 | 命中串 | 为什么是假阳性 |
|---|---|---|
| `:36–42` | 全部 8 串 | **是常设反扫表本身**(待收编) |
| `:149–167` | Q2.30 / 35.7 / 7.3 / 11.5% / CCES | **是我 2026-08-05 加的撤回警示块本身** |
| `:187` | 同上五串 | **是被加标的那一行原文**(E-2:不删,已由上方警示块覆盖) |
| `:103` | 「陷波的干净收益」 | 原文已带 `~~删除线~~` + **撤回** |
| `:122` / `:270` | `7.3` | **是章节号 `§7.3`**,不是 cyc/MAC 的 7.3 |
| `:221` | `30-50` | 实为「可配 **30-500**」的频率范围,不是"30-50 cyc/MAC" |
| `:255` | `1.1-1.2s` | 原文即写「v1.1 的 1.1-1.2s **偏乐观已改**」(就地已更正为 1.24s) |
| `:286` | `20.16` | **是"抓到该残留并更正为 20.53"的记述本身** |
| `:528` `:540` `:544` | `R=0.32` | 三处均在「**是我造的,不得引用** / 伪影 / 当场改名」语境内 |

---

## C. ⚠ 两条给复核者的提醒

```
① **A-1 与 `:102` 的 ② 是同一形态**:「结论对、数错」
   ⇒ 而这个形态**最不容易被抓**,因为读者核结论时会点头,不会去核那个数
⭐⭐ 而 A-1 有一处更硬的证据,lead 点名要写出来:
   **同一份文件里,【更正的记述】在(`:286`),而【被更正的那一处】还在(`:272`)**
   ⇒ 即:作者当时**知道**这个数错了、**写下了**更正、**却没有回去改被更正的那处**
   ⇒ ⇒ 两个"结论对、数错"的实例都在同一份文件里 ⇒ **这不是偶然**,
      是**这份 memory 的写法本身允许它发生**(只增不改 ⇒ 更正以"新增一条"的形式落地,
      而新增不触发对旧处的回改)
   ⇒ ⇒ ∴ 修法不能只修这两处 —— 与 `:102` 的结论一致:**要改的是格式,不是条目**
② 本清单只覆盖 **memory.md 一份**;而同族问题在 `W1B/W1A/合同/收口件` 里是否存在,**本轮未扫**
   ⇒ ⛔ 不得由"memory 已出候选清单"推出"其他件干净"
```

```
⛔ 本件未过门。⛔ 未 commit。⇒ 结论权归独立 critic。
```

## D. 处置状态(2026-08-05,lead 裁定)

| 候选 | 状态 | 依据 |
|---|---|---|
| **A-4** `:677` | ✅ **已加标**(⛔ 原行未动) | lead 判"立刻修" —— **危害类别不同**:另三条是"数错/限定弱"⇒ 读错;A-4 会让新实例**动手去做一件已被证伪的事** |
| A-1 / A-2 / A-3 | ⏸ **挂着,等 critic 复核后一并处置** | 处置权在复核之后 |

## E. ⚠ 那个 76% 假阳性,**请勿优化掉**(lead 点名保留)

```
它是「命中 ≠ 违规」的**量化证据**。
⇒ 一个 76% 假阳的工具若被当成"违规清单"用,会**训练所有人忽略它** ——
  而那正好是它最该被看的时候失效。
⇒ 与 critic 那条同源:**一个在正常情况下就误报的闸门,会训练所有人不看它。**
```
