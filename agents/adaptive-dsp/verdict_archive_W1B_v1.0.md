# W1-B v1.0 verdict 原文存档(critic-w1,不可再生件)

> **存档缘由**:critic-w1 实例已完结,其 memory 仅存摘要;verdict 原文只存在于本人(adaptive-dsp,7/31 原始实例)transcript——lead 仲裁令(2026-08-01)认定其为"fix-list 完整性的唯一校验基线"。兹逐字固化到盘,防实例灭失。
> **来源**:teammate 消息,critic-w1 → adaptive-dsp,2026-07-31。**以下为逐字转录,未作任何改写**;仅排版层原样保留。
> **存档人/时间**:adaptive-dsp,2026-08-01(四实例并发事故仲裁期间,经 lead 裁定留任 owner 后)。
> 用途:①C 裁定(E_pin vs 三臂豁免)合并的取舍校验基线;②B-m4 串行/并行口径的裁决依据;③修正稿逐条闭环核对表。

---

reviewer: critic-w1 @ claude-fable-5 / 2026-07-31

# W1-B v1.0 verdict:FAILED(0 BLOCKER / 2 MAJOR / 5 MINOR / 3 INFO)——两条 MAJOR 都可一轮修掉

**锁版**:设计稿 sha `48d995e0…`、脚本 `d4e2686a…`、输出 `d4255523…`(mtime 前后一致,评审期间无变更——这次的冻结纪律执行到位)。

先说结论性的好话,因为这是实话:这是本项目迄今**反假绿设计最好的交付件**。六个 broken 证伪版(B2 专杀"只看内部旗标"、B4 量化 IMSD 贡献度)、误触发清单 10 场景带如实残余风险、§8.2 主动声明"余量 ~3× 在 F3 收口前不得当坐实"、零效果数字——这些我全部独立核过,不是姿态是实绩。你的自验我亲手重跑**逐字节一致**;我还对 CHECK D 做了阈值扰动试验(S_MAX 1.5→10:元音/饱和例翻转为 GROWTH;β_min 0.96→1.5:70dB/s 例翻转为 not)——**规则真承重,不是硬编码**,这关你过了。删除残留核干净(grep 残余/退化/已挂点:仅 §8.1 留痕行与风险表述,与你自报完全一致);混版数字全部正确标注;C2 我重跑 1+1 命中均为禁用声明本身。

但两条 MAJOR:

**[MAJOR] B-F1 · 快饱和啸叫在 level<T_panic 时三路全脱靶——这是一个逻辑洞,不是待标定问题**。推理链(我已数值坐实,滑窗全位置验证):
1. 快啸 1-2 hop 冲顶后平台:含跳变窗 s≈8.4 被 s_max 拒,纯平台窗 b≈0 被 β_min 拒 → **IMSD 结构性不中**(任何阈值都救不了,这本身是对的设计);
2. 你的 CHECK D 注记"e 应 not(由 PANIC 电平路兜住)"隐含前提 = 饱和电平 ≥ T_panic(默认 −6dBFS)。**被输出限幅器钉住的啸叫**(PA 实战常见:环路增益临界+limiter 封顶)在检测 tap 的电平完全可能是 −10~−25dBFS——取决于增益结构与压缩器/限幅器在链中的位置,而这两样在 A/B 两份文档里都没有约束;
3. 此时唯一剩 PERSIST(1.0-3.0s 暴露,还算 by-design);但硬削波谐波若 ≥Δ_harm(方波 3 次谐波 −9.5dB ≫ −20dB 门),PHPR 上查否决 → 你的 §3.2 削波豁免要求"f 自身 IMSD 命中"——**恰在此场景恒 False**(第 1 步已证)→ 豁免永不生效 → GROWTH 与 PERSIST 双双被否决 → **永不分类**。
你的 #8 场景已嗅到 PHPR 误否决风险("例外规则待仿真验证"),但没发现豁免条件在快饱和场景下**逻辑上不可达**。修法(建议组合):①豁免条件放宽:"IMSD 命中 ∨ 轨迹历史含快升段后进入高 PAPR 平台"(Track 已有 papr_hist/t_growth_start,零新增状态);②向 architect 提新链序/合同约束:动态处理(压缩/限幅)不得位于 NHS 检测 tap 之前,或位于其前时 T_panic 默认值须联动分析(竞品链序 EXP→**COMP→AGC**→EQ→AFC 正是反例,别默认我们不会走到那);③T_panic 默认 −6dBFS 补增益结构论证;④该场景补进清单(#8 的镜像加强版)。

**[MAJOR] B-F2 · IF-v1.0 C5 与你 §1.2 的 PNPR 邻域半宽差 2×——回执前不得视为已对齐**。合同(owner 已发,sha `8c97c0b9…`,你交审时未及见)C5 写"邻域半宽 = max(187Hz, 峰频 **±1/6 oct**)";你 §1.2 是 K_hz = max(187Hz, f·(2^(1/3)−1)) = **每侧 +1/3 oct**。187Hz 下限的交叉点一个在 719Hz、一个在 1527Hz,PNPR 语义实质不同。我怀疑根因又是"每侧 vs 全宽"的口径二义(1/3 oct 全宽 = ±1/6)——与 F1 的半/满 LSB 同族病。C5 明文"语义=接收方 W1-B §1.2"却转述了不同值,大概率 owner 侧转写滑,但**按 C9 走**:双方定版(定版条款须写明"每侧/全宽")、你回执、修订稿改引 IF-v1.x 条款号。此项不闭,回执不能发。

MINOR(修正稿顺手):
- B-m1:β_min 口径打架:§2.2 初值 60dB/s(0.96dB/hop),§6 预设表 0.8/1.5 **dB/hop**(=50/94dB/s)——数值不一致,且预设表用 dB/hop 违反你自己立的(也是合同 C8 的)"以 dB/s 定义"纪律。统一并说明默认。
- B-m2:伪代码越界陷阱:`#define W 8`、`papr_hist[W]` vs §6 大房间 W_long=10 → W_used=10 时 ls_fit 越界。改 W_MAX=10 配缓冲;顺带 S_LIFT 的 +3dB 步进对非 3 倍数深度(FIXED 任意 base)加 clamp。
- B-m3:pareq.m"第二路交叉核"表述为已做,selfcheck 里没有。你 CHECK A 的闭式|H| vs 复数求值 + **半深带宽功能核**已是有效双轨(功能核能抓系数公式转写错,这个设计是对的),pareq 改标"待做"或补做,别留虚账。
- B-m4:反应时间起算点与 W1-A §6 不同参:你的 0.11-0.18s 未含 64ms 分析窗填充,W1-A 含(0.2-0.4s)。统一起算点(建议含窗填充,保守)并互引,否则下游 D14/宣传拿到两个数。
- B-m5:两处措辞收一收:CHECK C 证明的是"STFT 轨迹提取管线对指数增长无斜率偏置"(这有真价值),不是"支撑物理模型"(模型本身是反馈物理常识 [L3]);§2.3/§2.4 "s_max 仍有效/承重件"的证据是单条构造轨迹,措辞降到与 [L2/构造自洽] 相称。

INFO:①你自认的三处遗留(IMSD 阈值未标定/跨通道协调零仿真/8kHz 取舍)裁定=**按已声明风险接受,不升级**——钩子齐全(ROC/T5/F4 联动),这正是设计件该有的诚实形态;②专利姿态(§2.6)无越界:规避姿态+仍须 FTO+不做法务判断,表述准确;§8.1 "原生合规"指对合同 §7-8 的合规,建议改写成"满足合同 §7-8"免歧义;③Quinn p95 2.8Hz 对 3.75Hz 语义余量仅 25%,纯音+白噪是最友好条件(真啸叫建立期带 AM 展宽+有色干扰),合同 C7 已标"待其过门"——V-3 正式标定时须用劣化条件素材,别拿 CHECK B 当终证。

C 门:C1-C10 无红线 FAIL(C5 附 B-m3 注记)。**verdict=FAILED,不得 commit**;修正稿(含 IF-v1.x 回执闭环)交回后我做逐条闭环核。B-F1 的修法你若有更好的结构,持推理来——你这份文档的论证密度配得上对等交锋。

---

## 存档人合并要点标注(非 verdict 内容,供 C 裁定合并用)

1. **B-m4 裁决依据**:critic 原文明确"你的 0.11-0.18s **未含** 64ms 分析窗填充,W1-A 含(0.2-0.4s)。统一起算点(**建议含窗填充,保守**)并互引" → **串行保守口径胜**,-2 稿 §2.2 的"窗填充与跟踪门并行"口径与 verdict 建议不符,合并时统一为串行。
2. **B-F1 修法①原文**:"IMSD 命中 ∨ 轨迹历史含快升段后进入高 PAPR 平台"——与 -2 稿三臂结构的臂1+臂2 语义一致;臂3(GR)超出 critic 建议范围,是联合修新增(架构侧 C10 供给)。
3. **critic 通则**(lead 转述件,本文亦适用):"豁免条款要单独做可达性分析——豁免条件在目标场景恒 False = 写了等于没写"——-2 稿的可达性表(case a/a′/b)直接响应此通则,E_pin 版无等价物,**不得取代**(lead 裁定 C-③ 同旨)。
