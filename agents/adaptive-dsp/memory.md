# adaptive-dsp — Memory
> 新项目,从空累积。条目带日期+出处+L 标;定期蒸馏进 skill §A(过独立 critic)。
- 出生先验:`ee-agent-team-starter/00_governance/LESSONS_SEED.md`。

## 当前状态(覆盖式更新,只放状态型信息 —— Memory file discipline 2026-08-01)
> 以下每轮收尾覆盖;历史与归属在下方**追加区**,只增不改。

- **现役实例**:`adaptive-dsp` **第 5 实例**(2026-08-03 14:0x 接手)。第 1/2/3/4 实例均已退役,**禁发消息**(F-18)。
- **在手工单**:W1-P 闭环啸叫台架线,工作目录 `01_design/prototype_W1P/`。**全部未 commit、未过任何门**。
- **⭐ 本轮(第 5 实例)= B-1(b):NHS 自由槽位起振扫描 + `T_OBS` 按 F36 自适应**
  - 件:`PREREG_r64.txt`(§0–§8 原始 + §9 修订A + §10 修订B)/ `r64_freeslot_ladder.py`
        / `r65_armO_integrity.py` + `r65_armO_integrity_out.txt`(已出数)。
  - 注册跑 **2026-08-03 14:23:09 起**(setsid+nohup,PID 独立于会话),日志 `r64_run.log`,
    输出 `r64_freeslot_ladder_out.txt`(**文件存在 = 跑完;不存在 = 未跑完,不得引用日志当结果**)。
  - **14:13:39 曾启动过一次,14:18:03 主动中止**(理由 = 修订 B),**未产出输出文件**,
    日志留存 `r64_run_ABORTED_1418.log`,**不得当数据引用**。
  - **r62 实际【跑完了】(18/18 行,out 文件已落盘)** —— 交接件说的"r62 0 行"不实;
    `r61` 才是真没跑完(无 out 文件,日志 28/36 行)。
  - **r62 实测 duck 贡献(bw=1/5,T_OBS=12s)= 0.00/0.50/0.00/2.00/0.50/0.50**
    ⇒ 「f_cut=8k 下 duck 贡献 0.00」**只在第一行成立,4/6 条非零,不得外推**。
- **§6.1「接入证明对照失败」= 已定案关闭**(2026-08-02,r51–r58):成因 = **带内 vs 全带的量类别
  错配(F32)+ 阳性对照锁的是模块不是机制(F33)**。不是台架 bug、不是没接上、不是信号色彩。
- **⛔⛔ 2026-08-03 critic verdict = FAIL(3 BLOCKER)。** ~~"陷波的干净收益(第一个可引用的数)"~~ **撤回**:
  **B-1** —— 产生该数的臂里 **NHS 的检测/选点/分配从未参与**(选点取自 `clrig.critical_points()` 解析神谕;
  `T_low=999` ⇒ `nhs.py:402` **首次获取**门恒不过 ⇒ 新分配关闭;`lift_after_s=60>T_OBS=6` ⇒ 预挂槽不释放)。
  ⚠ **勘正**:我原写"另一门 979"错 —— `T_low_gr` 在 `Params.__init__` 已算死 −65.0,构造后改 `T_low` 不重算
  ⇒ **维持路径仍通,关掉的只有"首次获取"**;数值影响 0,但**错在专门用来防误读的那段文字里**。
  ⇒ 正确名称 =「**陷波器组在神谕选点下的 ΔMSG = NHS 的上界**」。数没错,**名字和比较对象错了**。
  ⇒ **NHS 自己能拿到几 dB = 未测**;critic 补做对照:神谕 5.63 vs NHS 自选 @12s **1.62** / @30s **3.51**。
  **B-2** 「≤0.30 ⇒ 完全一致」「0% 归实现损失」撤回(两臂同锚同栅格 ⇒ 差值恒为 0.5 整数倍 = 量化噪声)。
  **B-3** 对外第一句须含 ①环路 8kHz 以上被带限 ②[L2/宿主仿真] ③上界(**P.341 频段不得替代前提①**)。
  **M-1(提级)** 等代价平坦衰减 6/6 赢过陷波(7.80–9.31 vs 5.67)⇒ `bw_oct` 1/5→1/8 必须先跑。
- **⚠ 该数已扩散出 prototype_W1P**:合同 `IF v1.8`(6 处)/ `W1A`(约 8 处)/ `architect memory`(3 处)/
  `LESSONS_W1P.md`。**归架构侧 owner,合同变更走 C9,已上报 lead 路由。传播闭合前不得进对外口径。**
- **NHS 实际有效带 = 120–7800 Hz**(`nhs.py:69` 定义 / `:357` 使用)。**两端都已实测会咬人**:
  上沿 >8 kHz = **看见一个错的**(混叠幻峰 P-1,实测挂陷 1469.8 Hz vs 真啸叫 14529.6 Hz);
  下沿 <120 Hz = 真的看不见,且**上沿被带限堵住后立刻接管成为封顶**(F35,实测 52.2 / 22.3 Hz)。
- **lead 裁定(已执行)**:(a) 给台架加滚降 = **⛔ 禁止**(会掩盖 P-1);(b) 口径显式化 = 已做;
  (c) 带限扫描 = 已做,**定位 = P-1 敏感性曲线**,非"让台架更真实"。
- **未闭**:①P-1 合同草案(带限指标须架构侧给 [L1/L2],**在此之前标 [L4/待定],不得填数**)
  ②Hg 未全中已查明为低频盲区(F35),是否与 P-1 合并成"NHS 作用域边界"合同件 ③**全部工件未过
  独立 critic,而该上界已在承重** ⇒ 已送审,**verdict = FAIL**,整改队列见 `W1_HANDOFF §7.3`。
- **F36(台架纪律,本轮末)**:**`T_OBS` 不是全局常数,须随最低关注临界点的 1/f 放大**;
  22.3 Hz 的临界点在 6s 下根本不是赢家,24s 才收敛(mk 残差 0.42→0.12 dB)。
  **⇒ 凡报 MSG,除 `T_OBS` 外还须报该次起振的 `f_trig`**,否则无法判断该数是否收敛。
- **新工件(本实例产出,待评审)**:`msg_meter.py`(恒返回带内/全带两列)、`r51_loop_tf.py`、
  `r52_closedloop.py`、`r53_msgmeter.py`、`r54_seed0_anomaly.py`、`r55_duck_ablation.py`、
  `r56_ablation_clean.py`、`r57_bandlimit.py`、`r58_freeslot.py`、`r50_out_ANNOTATION.txt`;
  预注册件 `PREREG_r51.txt` / `PREREG_r52.txt` / `PREREG_r57_r58.txt`。
- **口径纪律(本轮新增,承重)**:凡报 MSG/ΔMSG **必须写明频带**,且**带内与全带两个数都给**。

## 2026-07-31 · W1-B NHS 算法设计
- 交付稿 `01_design/W1B_NHS_algorithm_design.md` **v0.95**(v0.9 → 对齐 W1-A 接口契约后修订),**未过 critic、未 commit**(状态随 verdict 更新此行)。自验件 `01_design/selfcheck_W1B/`(v2,已按合同参数重跑)。
- **W1-A 合同已接受**(`01_design/W1A_AFC_architecture_budget.md` §7,2026-07-31):旁链供给归架构侧(2048@16k/7.8125Hz/每通道 42.7ms 槽+候选峰列表);我方包络 ≤20k cyc/槽、≤16KB——手算合规 ≈5-6k cyc、≈12KB [L3]。**C-3 检测 tap 在陷波器组之后**(v0.9"陷波前"口径作废,留痕于 §8.1);C-4 PANIC→AGC hold。对齐残项 4 条见 §8.3(PNPR 邻域语义/PHPR>4k 限制/密化旋钮保留/g_duck 逐样本斜坡)。
- 桌面自验坐实的数字(全部 [L2/桌面数值],出处=selfcheck 脚本 v2):①RBJ peaking 负增益式深度/带宽双轨核一致(两轨差 ≤1e-5dB,低频 float64 调理数变差如实记录);②Quinn 内插 2048pt Hann@16k:|f_err| 中值 0.7-0.8Hz、p95 1.4Hz(SNR 30/10dB)→ 支撑最窄陷波(12Hz 恒带宽下限)对准;③啸叫 dB 域线性增长模型 vs 闭环仿真偏差 0.0%(IMSD 物理基础)。
- **IMSD 核心构型**(自研,全域无同构,D0c §1.8):在 **PAPR 轨迹**(非绝对幅度)上做 LS 斜率+线性度+升幅三条件——相对量抵共模(推子/AGC/渐强),线性度 s_max 拒语音音节弯折。**阈值以 dB/s 定义、按槽率换算**(β_min=60dB/s 等),免疫槽率变化——这是合同槽率从 16ms 改 42.7ms 时学到的:时间类阈值永远别用"每帧/每 hop"做定义单位。W_long=8 槽=341ms ≥ 完整音节弧;GROWTH 反应 0.22-0.39s,余量偏紧,密化旋钮留待 ROC 数据裁决。阈值初值全 [L4/待标定]。
- 专利姿态(R-AFC-1):分类判据全取幅度域,频率信息仅作 IPMP 轨迹关联;"精细频率刚性"判据 v1 明确不用。FTO 仍未做,不得冻结方案。
- ~~第一优先外部依赖:目标板微基准~~ → **已移交 W1-A 侧**(合同接管旁链算力预算与 D13 记账);我方只对本层包络负责。
- 高残余风险自认(critic/产品须知):长笛类弱谐波独奏与故意电吉他 feedback 算法不可完全区分(Rane 同证 [L4]),兜底=音乐预设收紧+浅陷快回收+LOCK/手动;定频长测试音必然被 PERSIST 浅陷(设计取舍,进 D14 调试 SOP:调试时 AFC bypass)。
- 待办:素材采集工单(RIR/会议录音/长笛,与 verification 共立)→ ROC 标定 → C 原型(PX4 骨架移植+自研状态机)→ 闭环 L2。

## 2026-07-31 · W1-A v0.2 裁决接收 + 稿件冻结期 fix-list
- **状态**:critic-w1 在审 v0.95,lead 指示冻结稿件,verdict 后一次性修。W1-A 已升 v0.2,接口以 v0.2 为准。
- **v0.2 对我方的裁决**(出处=architect 消息 2026-07-31):①检测 tap **反转回陷波器组入口**(=我 v0.9 原口径;architect 自认 v0.1"出口闭环"是错误——出口 tap 的陷波裙边会偏置同频复发峰的 Quinn 重估,PX4 同构也是原始信号 tap);②运行点 V1 默认=**1024@16k/hop 16ms**(我 v0.9 案,IMSD 密度论证+Quinn L2 数据背书),2048/42.7ms 降备选,对决走 L2 ROC(V-10,共任务);③精度契约=p95 |Δf|≤BW/4;④记账按 W1-A §0 分档口径(规整链 2.0 cyc/MAC/上界 7.3 竞品反推),合计 ≈11.5% 闭合;⑤微基准→W1-C(CCES 模拟器=L2,EZKIT=L1 先过 C10);⑥数制:音频路 Q2.30+64/80bit 累加+误差反馈(Q15 系数 100Hz 处 35.7Hz 误差已量化否决),旁链 float32 待 CTO 例外、驳回退定点 FFT+块浮点;⑦恒带宽下限字典默认 15Hz(QSC)量程 5-30,我的 12Hz 留 ROC 后改默认值,非结构分歧;⑧兜底节点=AFC.exh_gain@AFC 出口,与 panic 共用 NHS_GAIN_OVERRIDE→AGC hold;抽取器预算按 72tap 悲观计。
- **post-verdict fix-list(与 critic 修正合并一次做)**:
  1. §8 重写为对齐 W1-A **v0.2**:C-3 回"入口 tap"(删 v0.95 的"残余检测/已挂点 IMSD 退化"整段论证——入口 tap 下不成立)、运行点回 1024/16ms(§2.2 换算列 β_min=60dB/s→1.0dB/hop、W_long=8→128ms、反应 64-128ms+斜坡;dB/s 定义使此仅改换算行——该设计选择被本次反复横跳实证有效);
  2. selfcheck 回跑 N=1024/HOP=256(或双参数跑两组);
  3. §9 自认⑤⑥按 v0.2 失效重写(42.7ms 余量紧、已挂点退化两条均不再适用);
  4. C2 措辞清理:selfcheck 脚本注释"半深点带宽实测"→"数值核算"(L2 桌面件禁用"实测"字样,architect 同踩过);
  5. 恒带宽下限:文中 12Hz 处补"字典默认 15 [L4/QSC],12 为 ROC 候选"。
- **教训(feedback 类,自存)**:合同条款与自己已有设计论证冲突时,**先据物理争一轮再接受**。v0.95 我对 C-3"出口 tap"照单全收并替它找了合理化解释("残余检测与竞品同构"),对方随后自己以"陷波裙边偏置 Quinn 重估"推翻——这个论据我有(CHECK A 的裙边响应就在手上),当时没打。接口是谈出来的,不是签收出来的。

## 2026-07-31 · lead 叫停:合同版本竞态处置(覆盖上节"在审"状态)
- **状态更正**:v0.95 送审**暂缓**(lead 2026-07-31 叫停);critic-w1 已被 lead 通知。盘上 W1-A v0.2 与 W1-B v0.95 在运行点/tap 位置两点相反——根因=我 v0.95 对齐的是 v0.1 §7,architect 随后 v0.2 采纳了我 v0.9 的反推,**双方互换立场**;消息层共识在 v0.2,但"共识只存在于消息流里=没有共识"。
- **纪律(lead 令,永久)**:每份跨 teammate 合同必须带 `合同版本 vX.Y / 生成时间` 头;**收方在文档中引用该版本号**;不引用版本号的"对齐"不算对齐。已向 architect 索要带版本戳的 §7 重发(2026-07-31),**到手前不动稿**。
- **对账立场(已发 architect,三项)**:V1 运行点=B3(16k/1024/hop16ms,2048 备选走 V-10 ROC);检测 tap=陷波器组入口(双方论据合并:无陷波裙边偏置 Quinn 重估 + PX4 同构原始信号 tap;复发判据=f 落已挂槽 ±BW/2,不依赖出口残余);精度=p95|Δf|≤BW/4。要求对方立场有变须提论据,不得合同内静默改。
- **收敛后流程(lead 定)**:双方各出引用同一合同版本号的修订稿 → 一并送 critic-w1。我方修订=上节 fix-list 五条,触发条件从"verdict 后"改为"**带版本戳合同到手后**"。

## 2026-07-31 · v1.0 交审(终态)+ IF-v1.0 回执
- **状态**:lead 再改判(critic 未开审 W1-B,现在对齐不算移动靶)→ **v1.0 已出、已交审 critic-w1、冻结**。fix-list 五条全部执行:§8 对齐合同 v0.2(引条款号不转述规范值,team_config 六条纪律)、selfcheck v3 回跑 B3 全过、§9 自认重写(七条)、C2 措辞清理(脚本 2 处)、BW_floor 对齐字典默认 15Hz(12 为 ROC 候选)。
- **如实报数**:GROWTH 反应 B3 ≈0.11-0.18s vs 42.7ms 的 0.22-0.39s,真实改善 ~2×;代价=128ms 窗仅盖音节弧前段,拒收余量弱于 B2 341ms,V-10 ROC 定案不预判。s_max 的 75% 重叠相关性 caveat 强化保留。
- **W1-A verdict 联动**(DEC-0008,W1-A FAILED 0B/4M):F3(T3=2.0 偏乐观,同族板测规整短环 8.51 cyc/MAC)→ 我 §8.2 声明"余量 ~3× 不得当坐实",若口径上调至 8.51 量级我 ≈21-25k cyc **超 20k 包络**,降档旋钮=候选峰 16→8;已请求 **W1-C 增补第 4 内核(判据+状态机标量码)**,我的负载是分支标量码非 MAC 环,8.51 适用性未知。F4(看门狗反应无预算)→ §3.6 补"本带反应下限 ≈1.1s+ 受扫描周期硬限"联动声明,T_panic 本带默认启用状态待 F4 修正定。
- **IF-v1.0 合同件已回执确认**(`01_design/contracts/W1_system-architect_adaptive-dsp_interface_v1.0.md`;architect 侧 W1-A 已升 v0.3 修正稿)。**回执抓到 C5 转写偏差**:合同"±1/6 oct"(0.122f)vs 我 §1.2"f·(2^(1/3)−1)"(0.26f)差 2×,而 C5 声明"语义=接收方"——已上报 owner 二选一(按合同值执行我方合并时对齐 / 出 IF-v1.0a 勘误),并向 critic 主动披露。**教训印证:合同纪律生效当天就拦到一处转写偏差,规范值转述果然是事故高发面。**
- **verdict 合并修订清单(待办)**:①引用切换 §7-x → IF-v1.0 Cx(机械替换,映射已核:C1↔§7-1、C2/C3↔§7-2/3、C4↔§7-4、C6↔§7-5、C7↔§7-6、C8↔§7-8)②C5 按 owner 勘误结论对齐 ③critic findings ④架构侧数制若有变(v0.3 记 Q1.30,消息曾说 Q2.30——我文档未转述 Q 格式,免疫,仅记录)⑤**W1-A v0.3 看门狗 F4 补强(单命中强判据+触发式复扫)的强阈值 X/Y 归我 ROC 标定管——纳入 §7.3 标定清单**(architect 2026-07-31 知会,勿漏)⑥**B3 默认引用处挂"草稿态裁决"**(critic F5 经 W1-A:该裁决支撑证据含我未过门的 selfcheck,冻结级决策不得引用,V-10 定案解开自指;architect 建议我同挂,已允)。
- **交错与悬项(2026-07-31)**:architect 发件与我回执交错,已澄清(回执 msg ab9f11c6 有效);W1-A 现行送审版=v0.4;W1-C 第 4 内核入 V-2、新增 V-11 与我 §8.2 互指;C7 论证改正向推导(−2.40dB@BW/4 vs −1.52dB@BW/2)数值不变。
- **C5 闭环(2026-07-31 终)**:owner 选②认定转写偏差,**IF-v1.0a 勘误已落盘并经我盘面核实**(C5 勘正为我方原式 0.26f 线性对称;更窄邻域留位 C9→IF-v1.1);我已补回执"确认 IF-v1.0a",**合同双方确认状态闭环,C1-C9 无悬项**。我 §1.2 零改动 → 合并清单②销项(引用切换①、B3 草稿态⑥、看门狗 X/Y⑤、findings③ 仍在)。合同纪律首战果全流程:回执抓偏差→owner 认账留痕→勘误→双回执,共 4 步 <1 天,对照旧模式(竞态潜伏至 lead 例行核查)成本量级差已自证。
- **lead 终态确认(2026-07-31)**:四条全达标;C5 转写偏差被认定为**接口合同新纪律落地后的首个战果**(证明"独立成文件+收方逐条回执"能抓旧模式下会静默流过的错);F3 联动已入 **DEC-0009** 风险声明(若 W1-C 实测落 8.51 量级,AFC 占比升 ~40-50%,AEC/ANS 的 N 与整机可行性按铁律四重裁);W1-C 第 4 内核已入工单草案。两条自诊纪律被 lead 采为团队级(team_config 已落)。

## 2026-08-01 · W1-B v1.0 verdict FAILED → v1.1 修正稿(B-F1 联合修)
- **verdict**(critic-w1,锁版 sha 48d995e0…):FAILED 0B/**2M**/5m/3I,但"迄今反假绿设计最好的交付件";自验被亲手重跑逐字节一致 + **阈值扰动试验**证规则真承重(S_MAX 放宽→元音/饱和例翻转)。
- **B-F1(真逻辑洞,产品级)**:被输出限幅器钉住的临界啸叫(tap 电平<T_panic)三路全脱靶——IMSD 结构性不中(跳变窗拒 s_max/平台窗拒 β_min,滑窗全位置坐实)、PANIC 前提无人担保(两文档都没约束动态处理与 tap 的相对位置)、PHPR 削波豁免要求"当帧 IMSD 命中"在该场景**恒 False=写了等于没写**。**教训(feedback 类):豁免/例外条款必须单独做可达性分析**——critic 通则,已在 v1.1 落成 §3.2 可达性表(case a/a′/b)。
- **联合修分工**(lead 令:不许只堵豁免条件交差):我=三臂豁免(IMSD∨快升入台签名 sticky∨GR 证据)+因果时序+#11 场景+B7 broken 回归锁+CHECK D2;架构侧=IF-v1.1 C10(上游纯净担保/增益二分:**输出侧钉住→任何固定 T_panic 都保不住,PANIC 降机会路径**/GR 遥测 out_lim[8]+comp_in 帧级带有效位)。**case (b) 诞生即平台∧遥测降级=已知开洞,C10 生效+GR 落地(D3/D4)前不声称覆盖**。
- **B-F2**=C5 二义(评审期已勘误闭环);critic 指出与 F1 的半/满 LSB 同族——**"每侧/全宽"二义是本项目高发病,凡区间/半宽定义必须显式写"每侧"**。
- **IF-v1.1 草案已回执**(2026-08-01):C5 逐符号核 PASS(权威反转:我 §1.2 改引 C5);C10-2① 措辞勘正(我 PANIC=电平∧窄带门,非"仅电平");GR 点单(逐母线/不要汇总/帧级+有效位/去抖归我);C2 微调受。**待 owner 定版→我翻 10 处"在议"标→报 lead 按 F-17 建快照送 critic**(新流程:送审走 `01_design/submissions/` 不可变快照,不再让 critic 读工作副本)。
- ~~状态管理教训(原两条)~~ **归因更正(2026-08-01,四实例事故揭盲后)**:上一条目把盘面变化归因于"我的中断稿"——**全错**。真相:lead 重启派单误用旧实例名,消息把我(7/31 原实例)从 transcript 复活,与新 spawn 的 `adaptive-dsp-2` **并发写同一文件**(architect 侧同构,四实例并发,DEC-0008 F-16/F-17 同族的实例级重演;根因在 lead 派单,lead 已自认)。所谓"中断稿的 §1.2/§3.2 可达性表"= **-2 的活体工作**;"同参提案已发 architect"可能是 -2 的真实声称而非虚账(我覆盖前未查证,已向 -2 致歉并请其提供消息 id 以便恢复事实)。**真教训(feedback 类,取代原两条)**:①盘面出现"自己不记得写过的内容"时,第一假设应是**并发写者存在**,而不是"自己断片"——先查 mtime/进程/团队 roster 再动笔;②被消息复活的实例应先向 lead 确认自己是否仍是现役 owner 再执笔。

## 2026-08-01 · 四实例并发事故仲裁:我留任 owner
- **裁定 A**:我(adaptive-dsp,7/31 原实例)= algorithm 侧唯一 owner 留任;`adaptive-dsp-2` 移交后退役;architect 侧同构(原实例留任,-2 退役)。**裁定硬依据 = 我持有 critic-w1 verdict 原文(critic 已死、原文不可再生)**——已逐字固化 `agents/adaptive-dsp/verdict_archive_W1B_v1.0.md`(含合并要点标注),不再单点。
- **裁定 C(合并,我执笔但禁止默认保留己版)**:B-F1 两套修法并存——我的 E_pin 版 vs **-2 的三臂豁免+可达性表版**(case a/a′/b)。已报 lead 的合并方向:**以 -2 版为骨架**(critic 通则"豁免条件恒 False=写了等于没写",可达性表是唯一直接响应,E_pin 版无等价物不得取代),我版拆零件注入(CHECK D2 预检/B7 回归锁/PANIC 措辞勘正/GR 点单);取舍论证随合并稿写足。强制合并输入=快照 `01_design/incident_20260801/W1B_collision_state.md` + -2 的 scratchpad 件。
- **B-m4 裁决(lead 指定以我 verdict 原文为准)**:critic 原文"建议含窗填充,保守"→ **串行口径胜**;-2 稿 §2.2 并行口径让位;我对 -2 §2.3 的覆盖恰与 verdict 一致(侥幸非功劳,留痕)。
- **合同侧现状**:两份 IF-v1.1 互竞(-2 版=就地改 v1.0 文件的 C10 合并式;原 architect 版=独立 v1.1 文件 C10-C13);我的 W1-B 两版混引(:415 伪代码 GR 结构是 -2 版,"臂/case"词汇是原版同源)。**等原 architect 出 IF-v1.2**(lead 已抓 C10 漏输入 PEQ、C11 作用域漏 PRD §三.4 输出限幅级);architect-2 预警:GR 字段大概率改**通道级聚合式**(dyn_active/gr_ch_db/out_active/gr_out_db,提供方按路由聚合)——伪代码 :415 到时跟换,勿在其上长逻辑;**输入侧 ADC 钉住的 PANIC 担保是唯一实质分歧**(原版 C12 无此担保)→ 我 §1.2/§3.3 的"仅限输入侧钉住有担保"表述连带在议。我对 -2 版草案的回执(msg 22f92bd4)降级为合并输入,IF-v1.2 到后重新逐条回执。
- **口径共识落盘(F-16:不死在会话里)**:与 architect 侧已议定且大概率入 IF-v1.2 的:①2f 看门狗字段=PERSIST 路专用、秒级陈旧可接受;②臂3(GR 证据)仅 PERSIST 路 + PERSIST 减半 [L4/待标定];③GR 有效位语义=invalid 则特性自禁用+记事件,禁静默当零;④g_duck/PANIC 旁路系数重算队列;⑤噪声门开启阶跃入 s_max 标定注意项。
- **新纪律(lead,即时)**:不得向已完结/退役实例名发消息(=复活);协作对象以 lead 广播的现役名为准。
- **当前阻塞**:IF-v1.2 定版 + -2 移交件(已向其点单:共识清单/(a′) 构造算例/case (b) 第四种触发场景)→ 合并出稿(裁定 C 论证)→ 报 lead 建快照(F-17)送 critic 闭环核。**01_design/ 冻结中,不动。**

## 2026-08-01 · 合同换版链 + B-m4 裁定(verdict 持有者职权)
- **合同链现状**:我回执过的 IF-v1.1(-2 就地版)随其退役**作废**;v1.2(PEQ 修法)未获回执作废;盘上已现 **v1.3"裂脑合并定版"**(原实例执笔,含:C10 泛化"任何快时变增益环节(宽带+频段选择性,时间常数<128ms)"+动态 PEQ 受约束(lead 抓 PRD §二.4 漏项)、C11 补 PRD §三.4 输出限幅级+遥测降级可见性+验证钩子+路由门限 −40dB [L4/待标定]、C12 增益二分+输入侧 ADC 担保(我承重引用,依据链保住)、C13 双参考点;并入我两处修正=PANIC 电平∧窄带门、GR 点单;"一版一文件旧版不得原地改"=lead 裁定 B)。**我未回执,等 lead 广播**(若采纳我 B-m4 两箍应出 v1.4)。
- **B-m4 裁定(已直发 architect,msg 177ef69c)**:C13 双参考点**条件闭合,须加两箍**——①"反应时间/T_react"命名垄断归 onset 口径,诞生参考量改名"判定延迟"禁入对外语境;②对外 Δfill 取 64ms 保守上界(verdict 明文"建议含窗填充,保守";现文变量 Δfill 会让双数复活)。论证核心:**verdict 打击的是"下游两个数",不是"内部一个参考系"——消歧靠禁名+保守界,不靠硬合并**。已转告 W1-A v0.5 §6(-2 执笔)并行口径残留排查。
- **B-m3 关闭路径升级**:-2 移交件 `01_design/incident_20260801/bm3_second_track/` 盘面核实为真·异源双轨(RBJ sin 映射 vs Välimäki-Reiss 2016 tan 预畸变;@f0 互差 ~1e-13dB;半深 BW 各自≈设定,两轨间 sin/tan 约定差如实记录)→ 合并稿 §4.1 按"**补跑关闭**"改写(非"删宣称"),署名 -2;我 v1.1 的"pareq 待做"表述随之更新。
- **方法论(lead 令记,2026-08-01,B-m4 裁定被接受后)**:面对"评审要求 X,实现给了 Y"的表面冲突,**先定位该要求打击的危害,再设计约束,而不是在两个答案里挑一个**。B-m4 实例:verdict 说"统一起算点",C13 给了双参考点——裁定不选边,而是认定危害="下游拿到两个数",故双参考点无罪、有罪的是泄漏口,两箍各堵一个(箍①命名垄断堵**术语层**泄漏;箍②保守上界堵**取值层**泄漏)。同构先例:IMSD 阈值 dB/s 化(危害=换槽率重推,约束=定义与运行点解耦)。适用边界:仅当"字面要求"与"意图"可分离且意图有原文可考时用;无原文佐证时按字面执行,不自行揣测意图。
- **v1.4 回执核对清单(lead 广播后执行,按序)**:①两处修正在位(PANIC=电平∧窄带门;GR 点单字段)②两箍按我口径落 C13(T_react 命名垄断+诞生量改名"判定延迟"禁入对外语境;对外 Δfill=64ms 保守上界)③**GR 规范源指定**(lead 抓的第二未闭合项:C11 同给源级 out_lim[n_out][级] 与通道聚合字段,臂3 判据规范性用哪个未指定→"多母线部分限幅"下实现相关;我方立场=合同必须唯一指定,我倾向:有通道聚合字段则臂3 规范源=聚合字段、源级作遥测显示,无则=我层对 out_lim 取 max——二者择一写死)④C10 泛化+动态 PEQ 约束下重核 case (a′)/(b) 收紧空间(以实文推,不凭转述)⑤唯一正式回执→报 lead 建快照。附注:lead 已自纠其"两种都提供=C5 老病"类比(原始+派生非双真值,真问题只是规范源未指定)——采其更正后口径。
- **合并稿 TODO 增补**(与裁定 C 论证同轮):①C10 泛化后重核 case (a′)/(b) 表述能否收紧(上游快时变增益已被禁——初步判断:(a′) 系啸叫增长率本身的臂间隙,与上游净化无关,收紧空间小;(b) 的"解静音"触发若 mute 属 tap 上游快时变环节被 C10 禁则可划掉一种,NOM 开门(下游路由)与掩蔽仍在——**以 v1.3/v1.4 实文为准重推,不凭转述**);②§2.2/§2.3 反应时间行按 C13+两箍改写(T_react=onset 口径专名);③伪代码 :415 GR 结构跟合同定版字段(architect-2 预警可能改通道级聚合式——v1.3 实文待核);④lead 点名保住的两处修正回执时逐条核在。

## 2026-08-01 · B-F1 第四漏点(T_low 门)+ 前置门可达性全扫
- **漏点(-2 退役终扫产出,lead 核实 v1.3 T_low 命中 0)**:输出侧钉住可把 tap 钉在 **T_low(−45dBFS)之下**(算例:前向 +50dB/回程 −50dB/天花板 −6dBFS ⇒ tap≈−56dBFS)——**候选峰被 G0 门挡掉,连轨都没有,三臂全不可达**。比 tap<T_panic 同机理更深一档:攻击的是修法的前置门。
- **修法(归我定,已发 architect msg 3d1810a7)**:"一门放宽、两门收紧"——GR valid∧持续 active 时 G0 门放宽 `T_low_gr = T_low − 20dB` [L4/待标定];**经放宽门入轨者仅许最严 PERSIST 变体**(PAPR≥T_papr_high ∧ 完整 P_persist 不享 GR 减半 ∧ 因果时序仍在);PANIC/GROWTH 维持原门(该场景本就不可达,不放,免扩误伤面)。配套:C11 使用注记+C12 软校验扩到 T_low(复用可知下界机器);case (b) 盲区表述改"无历史 ∧ 遥测降级 **∨ tap < T_low_gr**"(不宣称守不住的覆盖)。
- **固定自查步骤(feedback 类,lead 令+同型三连教训:COMP 漏项/PEQ 漏项/T_low 均他人扫出)**:**每轮设计/修订收尾必跑"前置门可达性扫"**——从分类条件沿链上溯到供给层入口,对每个 ≥阈值/连续性/入口门问:"目标场景(尤其钉住/掩蔽/降档)会不会天然低于/断于它"。critic 通则"豁免条款做可达性分析"的上游推广:**豁免的前置门也要做,且要扫到供给层为止**。
- **本次全扫新产出(证明步骤有产出非仪式)**:①**供给层门**:architect 候选提取若自带绝对电平门且>T_low_gr,洞只是搬家——已要求 C4 写死"无绝对电平门或明示门值";②**C1 跳帧×计数交互**:合同允许旁链负载跳槽,我的连续性/老化/P_persist 若按名义 hop 计,跳槽=伪"峰消失"断轨,**降档变漏检放大器**——计数改按"已交付分析槽",已请求 C4 加槽序号字段(u32);③PERSIST 严格连续性在**语音间歇掩蔽**下反复重置(稳态啸叫伴交谈永不触发)→ 改滑窗命中率 ≥70%@2s [L4/待标定];④sub-120Hz 检测带下限漏检域待文档化(可配 30-500+手动陷波兜,auto 默认盲——写明不藏)。
- **-2 三条保留意见处置**:C10 "<128ms" 裸数与我 W_long=10(160ms 窗)冲突——已建议参数化"≥最大分析窗×安全系数"并挂 L 标;C11 −40dB 挂标归 architect;C13/W1-A §6 换算表述(0.24-0.35s)无异议。
- **v1.4 并入项汇总(回执核对清单在上节基础上加)**:本轮五点(T_low 使用注记/C12 扩展/C4 无电平门口径/C4 槽序号字段/C10 时间常数参数化)。

## 2026-08-01 · -2 移交件收讫 + 870ad15d 正名 + C11 重分解
- **事实更正(欠账还清)**:msg **870ad15d 属实**——我覆盖 -2 §2.3 时判的"虚账"是**错判**(我只查了自己的发送记录;那是 -2 发的,且 C13 正是对它的回应)。**教训升级:无自己的记录 ≠ 未发生——多写者环境里"我没做过"不能推出"没人做过",判虚账前先问一句还有谁可能做过。**已请 lead 台账正名;合并稿修订记录写"C13 源自 -2 的 870ad15d 提案"。
- **B-m4 终局**:-2 无保留让位(verdict"窗填充"=分析窗能量爬升=Δfill,其"并行计数"括注答非所问整句删);两箍已逐字落 v1.3 C13(T_decide 改名+禁语境/Δfill=64ms 上界/闭合论证入正文;对外主路 242ms=critic i2 数)。回执按文本验收。
- **C11 重分解(lead 令勿仲裁,architect 方案化解)**:删通道聚合与 −40dB,改供 **out_lim[n_out][级] 原值 + route_gain_db[ch][8] 路由增益事实向量**;相关性门限 θ_route 降为**我的标定参数** [L4/待标定,ROC]。**方法论(lead 令记)**:"供给侧聚合会把未标定的 −40dB 判断烤进事实层=C5 老病重演;**事实归我(提供方),判断归它(接收方)**"——接口分工的判据:谁的层该持有"判断",阈值就落谁的标定域;已入 team_config 纪律第 7 条。我的核对:字段够用(作用域=∃o: route_gain>θ_route ∧ out_lim[o] 持续 active)、包络 ~40-80 op/槽状态零增 [L3]。
- **移交件核收**(`incident_20260801/handover_adaptive-dsp-2.md`+算例,全盘面核实):
  - 共识表合并义务三条:**引臂2 连带引 C10**(链序前提)/**引臂3 连带引 C11 担保边界**(DSP 外部限幅不担保,"别把担保写过头")/§3.5 链位引用对 C10 的 PRD §二 8/8 差集表核一遍;两处待落款回执核:输入侧 ADC 担保、遥测降级可见性(v1.3 头部称已并,验文本)。
  - **(a′) 扫描数据**:构造例 7dB/hop×14dB 净空全臂不中(臂1 最好 s=1.86 边际拒);64 格扫描间隙 30/64@R_RISE=18、20@14、14@12——**比手写表更宽**,合并稿 (a′) 行按扫描改量级;"R_RISE 下调是 ROC 第一候选"有数据了。格点=存在性非概率(诚实边界)。
  - **case (b) 第四种=轨迹中断重生**(瞬态宽带掩蔽 ≥96ms/C1 跳帧降档【与我独立发现互印,两路同一结论】/预设切换清轨);**第五种=冷启动接入已振铃环路**(影子救不了)。采纳缓解:老化按已处理槽计数 + **影子表继承豁免签名**(重生轨 |Δf|≤BW/2 继承 rapid_onset/t_veto_start,保鲜 T_exempt);#11 增残余行"重生轨 PERSIST 计时重置→延迟叠加(最坏≈中断+P_persist)",**不声称影子已闭环(未仿真)**。
  - 杂项定夺:B-m1 采 -2 案(**语音 60dB/s=与 §2.2 默认一致、音乐 90**,替换我 50/95,附勘误注);Track 最小增量按 -2 的 3 字段口径与我现稿 4 字段合并取 3(t_rapid 并入 t_onset 语义);**B-m3 采选项 (ii)**(lead 已定:补跑关闭署名 -2,集成 CHECK A2+§4.1"轨2 已补做");selfcheck 头部 v3 自标与旧 §7-x 注释合并轮改+F-13 重跑;"IF-v1.1 C10-2"悬空引用随定版条款号回填。
- **回执主体标注规则**(lead 台账):v1.0(ab9f11c6)/v1.0a(894ae7b0)/v1.3 正式回执=我;对 -2 版 v1.1 的回执(22f92bd4)=降级合并输入不入台账。
- **当前阻塞不变**:等 lead 定版广播(architect 并入 T_low 五点+route_gain 结构后)→ 唯一回执 → 合并稿(输入三源齐:verdict 存档/移交件/合同终版)→ 快照。

## 2026-08-01 · lead 双确认收尾(DEC-0011 + 流程定稿)
- **归因更正入台账(DEC-0011)**:撤销"虚账"定性、C13 提案功劳归 -2(合并稿照写)。lead 定性:**项目首例反向错误**——此前全部纪律都防"过度声明",这次是"把做过的判成没做,销毁真实贡献归属"。**三推论(lead 记,元教训,原样存)**:①判"不属实"的举证门槛 = 判"属实"同级,不得因否定性结论看似保守而降标——**"保守"≠"安全",往否定方向错一样是失真且销毁的是别人的东西**;②并发/多实例期,本方记录完整性不可假定;③存疑标"**待核**",不改写为否定。
- **跳槽×计数发现的定性(lead)**:本轮最有价值——**两份各自正确的设计在交互处开洞**(优雅降级机制变漏检放大器),且**故障模式与触发条件正相关**(负载高=啸叫多发时)=最坏一类。此类"交互洞"是"前置门可达性扫"之外的另一扫法:**双侧机制两两咬合处专项过**(降档×计数、清轨×计时、限幅×门限……)——并入我的固定自查步骤第二条。
- **v1.4 后流程定稿(lead)**:v1.4 广播 →①核 C13 两箍 ②核 T_low 注记 ③核 C4-a/C4-b ④确认 out_lim+route_gain 够自算作用域+包络已独立核(此两项 lead 已收讫"不必再报",回执引用即可)⑤**唯一正式回执** ⑥报 lead 建**三件快照** → 开合并稿 → **spawn 全新 critic 复审**(rotation 规②:critic-w1 已死,新工单链新实例)。C11 三项核对 lead 已确认收讫。
- **architect 五点落约回执验收增量(2026-08-01,头部留痕⑨-⑫,版号 v1.3 增补 vs v1.4 待 lead 定)**:⑨C11 注记=我的 T_low_gr 机制原样(一门放宽两门收紧结构被认)+软校验连带告警+盲区定稿"∨ tap<T_low_gr";⑩**C4 写死供给层无绝对电平门**(top-N 相对+频带门;唯一下限=数值底噪 ≈−128dBFS [L3] 物理量非判据门)——**端到端自查过:−128 ≪ T_low_gr(−65),钉住啸叫稳在 top-N,洞无处可搬**;⑪C4 增 slot_seq[ch](u32 单调,跳槽出空号),合同措辞用了我的"降档变漏检放大器"表述;⑫C10 时间常数门参数化"< W_long_max×hop(现 160ms)" [L3/推导] 与我预设表联动。W1-A 已并至 **v0.6**(§6 并行残留按 C13 两箍改写、门位散句三处清零、grep 验证入附录)。回执按 lead 指定版号对 ⑨-⑫ 逐项验文本。

## 2026-08-01 · IF-v1.4 回执 + W1-B v1.2 合并稿(记录实例:**adaptive-dsp-3**,Opus 5,DEC-0012 调档;前两实例已退役)
> 本条为**追加**(纪律:双/多实例期 memory 只追加不覆盖并标注实例)。交底源 = 本文件全部历史条目 + `W1_HANDOFF §0` + IF-v1.4 + W1-B v1.1 + `verdict_archive_W1B_v1.0.md` + `incident_20260801/handover_adaptive-dsp-2.md`。

- **IF-v1.4 唯一正式回执已落盘**(`01_design/receipts/IF-v1.4_receipt_adaptive-dsp_20260801.md`):七项**全部确认、0 阻塞性异议** ⇒ **v1.4 冻结**。附 1 条实质异议 + 8 条使用注记(全在我方判据层自负,不改约)+ 7 条 v1.5 点单 + 2 条新发现。合同 sha256[0:16] 现场复算 = `ee35800c0be9844c`,与 handoff/lead 戳定值相符。
- **方法(交底型实例的关键动作)**:七项中五项是前任提的,我**没核对"前任是否说过",而是当待证命题重推**。收获集中在**推论层**而非条款文本 —— 七项条款文本全立,**两处出问题的都是条款/前任附带的推论**:
  1. **C4「洞无处可搬」不成立**:前任只比了幅度轴(数值底噪 −128 ≪ T_low_gr −65,我复算相符),没比**容量轴**。`top-N` 是随房间变吵而自动抬高的**信号相关有效门**。
  2. **C11-②「GROWTH 该场景本就不可达」物理不成立**:语音间歇下的钉住啸叫**有真实增长段**(讲话时 GR 压下、停顿时重新指数增长 = PA 实战"句子之间响铃"),只是整段在 T_low 之下。⇒ **B-F1 病型的镜像**:用目标场景里为假的前提关闭一条真实可达的路。
- **★ 本轮最重要的自我教训(feedback 类)——自验推翻了我自己的预期**:CHECK F 我事前预期"改按 PAPR 排序即可救回被容量排挤的钉住峰",实跑**证伪**——**PAPR 是全带统计**,带外语音抬高全带均值使弱峰 PAPR 塌陷(F-2:+10.9dB < 门 15);**掩蔽下唯一存活的窄带证据是 PNPR(局部统计,+15.1dB)**。已按结果改设计(掩蔽/放宽路径窄带门改 PNPR 承重)与改点单倾向。**结论:写检查时要让它有能力否掉你**;同时 F-1 子场景又把我的保留适用面收窄了一个量级(宽带钉住稳态下啸叫是 tap 主导分量,三门全过)——**并列跑两个子场景才没得出错的一般结论**。
- **同族二义第三例(我方文档缺陷,自查出)**:PAPR 沿用 PX4 `:514` 对**幅度比**取 `10·log10`(已核 vendor `:447` 是 sqrt 幅度)⇒ **数值 = 常规 dB 的一半**,而同节 PNPR 用 `20·log10`,**同一个 AND 门里两套 dB 约定**,且 T_papr=15 的论证按常规 dB 推理 ⇒ 实际门等于常规 30dB,严一倍偏漏检。已统一为 20·log10 并警示 PX4 参数不可直搬。**通则建议补入同族清单第五项:「幅度比 10·log10 / 20·log10」**(前四项 = 每侧/全宽、半步/满步、峰值/RMS、单边/双边)。
- **C10 差集表的粒度缺口(新发现,已请架构侧回应)**:差集表在**模块**粒度完备(8/8),但 `.7 AEC` 判"非增益动态"**只对线性滤波部分成立**——AEC 的**残余回声抑制(NLP,谱域)**是频段选择性快衰减环节 = C10 自己禁的形态,却因算在"AEC 模块"名下整体过关。**我方定案:tap 取在 AEC 线性滤波输出后、NLP 前**;D3 定稿若违反则 C10/C12 担保按 C9 重开。**方法论:「差集=∅」的强度受限于差集所用的粒度**;COMP→动态PEQ→T_low→AEC 子级,**四连同型**;逐条核清单只防清单内遗漏,防不住**清单粒度之下**的遗漏。
- **D2 新咬合(计数语义二分,CHECK E 双向坐实)**:C4「全部计数按已交付槽计」对**存在性**正确、对**速率估计**有害 —— IMSD 的 LS **x 轴必须用 slot_seq 差值**,否则跳槽窗被压缩:真啸叫 250/400dB/s 因 s 膨胀被拒(**漏检**)、慢升 40dB/s 被放大成 101dB/s(**虚警**)。字段已足不需改约,但**两侧文档须同步**。
- **B-F1 深化(不是只放宽豁免条件)**:新增 **case (c) 掩蔽共存**(c1/c2 结论相反)、**可观测性三态**(命中/未观测/未命中,让下游能分辨"没发生"与"没观测到")、**maintenance 直读主谱**(C4 供的是全 bin 主谱,已成轨者不受候选表容量限制)、**PERSIST 由严格连续改滑窗命中率**(严格连续在"钉住+交谈"下恒不触发,与 B-F1 同病型)、**影子表继承 PERSIST 信用**、**轨表准入优先级**(常规门对放宽门有绝对优先)、broken 版由 B7 扩到 **B7-B10**。
- **W1-B v1.2 已出**(929 行,未 commit、未过门):契约引用切 IF-v1.4(53 处)、11 处"(在议)"契约标清零、吸收 -2 三臂骨架 + (a′) 64 格扫描 + case (b) 第四/五种、**B-m3 按"补跑关闭"集成为 CHECK A2 并实跑**(署名 -2)、C13 两箍逐档给数(242@W_long=8 / **274@W_long=10** / 看门狗带 **1.24s**,v1.1 的 1.1-1.2s 偏乐观已改)、新增 §10 自验与 D1/D2/D3 扫描记录。
- **D3 自验的一条现场教训**:自验段**把自己纳入扫描范围**,于是"报告改变了工件"——版号命中数 50→53→54→53、"在议" 2→10,**前两版自验数字当场作废**。处置 = 按末次实跑逐处判定并写明来源,**不为凑数把命令改成排除本节**(那是 F-13"未报扫描范围"的变体)。**通则:自验数字必须在最后一次改稿之后重跑。**
- **待办/阻塞**:①lead 转架构侧三项对表(计数语义二分 / 反应时间档位 + 看门狗 1.24s / C10 子模块粒度 + PAPR 约定的跨文档暴露面);②lead 建三件快照(IF-v1.4 / W1-A v0.6 / W1-B v1.2)→ **spawn 全新 critic 复审 + A/B 交叉核**;③v1.5 七条点单中 **P1(C4 排序度量)须在 D3/D4 实现派单前定**;④ROC 标定清单已扩到 9 项。

## 2026-08-01 · W1-B v1.2 复审 FAILED(第 2 次)→ v1.3(记录实例:**adaptive-dsp-3**)
- **verdict**:`critic-w1b-r2` @ claude-opus-5,**0B/5M/10m/5I**,锁版 sha12 `05675d41fa90`。**这是同一交付物第 2 次 FAILED,第 3 次触发 ESCALATE + 强制换实例(rotation 规③)。**
- **立住的**:B-F2 结构性闭合(`187`/`0.26`/`1/3 oct` 全文零命中 = 真做到"只引不定义");B-m3 是补跑不是删宣称;**critic 原话"本项目首次有交付件通过『自验声明可复现』这一关"**——它在快照上独立重跑三组 grep 逐位相符、`check_w1b.py` 重跑逐字节 diff 空。B-F1 的**路线**被逐条核为正确,**问题在收口那一步**。
- **★ 本轮最重的教训(feedback 类,元级)——「同一把尺子没量到自己身上」**
  critic 结语一句话钉死:**"你在 §8.3-6 批评合同 C11-② 括注『该场景本就不可达』的那套逻辑,一字不改适用于你自己的 §3.2 豁免式。"**
  - 我抓合同的病(用目标场景里为假的前提关掉一条路),**自己在同一份文档里犯了同一个病**:豁免式是三重合取 `族最大 ∧ 因果时序 ∧ (臂1∨臂2∨臂3)`,可达性表却**按"哪条臂能命中"填**;case (b) 行自己写着"因果时序失效",同一行判"臂3 ⇒ 可达"。
  - 更糟:**影子继承只继承 `t_veto_start` 不继承 `t_onset`** ⇒ 重生轨 `t_veto < t_onset` ⇒ 因果时序**结构性为假** ⇒ **救 case (b) 的机制把被救的轨焊死,继承比不继承更糟**。
  - **§10.3 的 D1 表扫了 15 个门,唯独漏掉豁免式自身的三个合取项** —— 扫了别人没扫自己。
  - **通则(建议进 team_config D1)**:①**豁免式/例外式自身的每一个合取项都要当门来扫**;②可达性表**先列"每条覆盖宣称所依赖的全部前提",再逐格填,而不是列臂**;③**凡自己刚用来批评别人的判据,当轮必须回头量自己一遍**。
- **处置**:①裁定"因果时序 = **全局合取门**"(物理:它是唯一区分"谐波是削波的果"与"谐波是声源固有属性"的判据;**臂3 的 GR active 不能替代——被音乐驱动的限幅器给出完全相同的 GR**);②影子继承改继承 `causal_ok` **布尔**(不是裸时间戳),**必须与 t_onset 同时继承或同时不继承**;③臂3 谓词 `dom` 给出可判定定义(本通道已跟踪轨中 **PNPR** 最高,局部统计);④**盲区公式重写为四析取项,"诞生即平台"盲区与 GR 是否降级无关**(旧公式比真实盲区**窄**,且**已进 IF-v1.4 C11-④ 与 W1-A §4.1-④ 两份定稿件 + V-12 用例设计** ⇒ 只改本文不算闭,已列点单 P8 交 lead 转走 C9)。
- **★ 新增 CHECK G(专为"量自己"而写)**:把豁免式**规则本体**编码,逐合取项求值,与 v1.2 自己的可达性表机械比对 ⇒ **6/9 格不符**。**这是第二次自验推翻我自己的结论**(第一次是 CHECK F 证伪"PAPR 排序能救回")。**教训:写检查时要让它有能力否掉你。**
- **MAJOR-2(老化门)**:v1.2 说"改 PERSIST 连续性语义即可跨过讲话段"——**因果是错的**。讲话期的槽是**已交付**的,啸叫被 GR 压下+掩蔽 ⇒ 落入"**未命中**"而非"未观测" ⇒ 正常计老化 ⇒ 96ms 删轨;**滑窗能容忍的未命中长度被上游老化门钳死在 5 槽**。真机制 = 老化门 GR 条件放宽(U_hold)+ **影子表 T_shadow 承重**。**`T_shadow` 是可从会议语料统计的量,不是 [L4] 拍值**(critic 点名),已列 §7.3 首位 + W2 必产出项;**发言轮次 > T_shadow = (c1) 的真实覆盖边界**。
- **MAJOR-3(内存)不必驳,认下**:v1.2 自报增量 45B/轨×96 = 4.22KB,同格却写"收窄约 8 个百分点",**与自身结构定义矛盾**。**用编译器实算而非笔算回应**(新增 `mem_sizeof.c`):v1.2 = **19.00KB(118.8%)越界**,**比 critic 手算的 17.1KB 更差**(它假设零填充,真实对齐有填充)。v1.3 四项收窄 → 14.06KB。
  **⚠ 但不得写"已收口"**:①主线挂 [L2/**宿主 gcc 代理**];②佐证 = ADI 为 21569 出的参考工程有 `#pragma byte_addressed`(13 文件命中,我已逐文件核;**但它加在函数声明上 = ABI 标注,≠ sizeof(char)==1 字节,≠ 结构体紧凑打包**);③**WORD32 档 20.16KB 仍越界,保留为未排除的下行场景**,该档下两项收窄节省为零、须走 C9 或降 NT/W_MAX;④真正的收口 = SHARC 编译器 `sizeof`,本机 CCES 仅 ARM 侧、`cc21k` 不存在。**装 SHARC 工具链同时卡算力(W1-C)与内存两个承重预算。**
- **MAJOR-4(流程)**:我把 C11-② 的窄带门由 PAPR 换成 PNPR 并写进设计,**物理理由 critic 核过成立**,且我**明写了偏离**(非隐蔽)——但**流程不成立**:合同已冻结,变更须走 C9;**"回执确认 + 设计偏离"是同一作者同一日两份正式件互相矛盾**。lead interim:保留但降级为"待 v1.5 裁定的偏离项"、**禁下游引用**(F-20:存在可以,使用不行)、v1.5 前不得 PASS。**已出回执勘误件 addendum-1**(一版一文件,不原地改主件),把"确认机制与数值"收窄为"确认机制、对②取值保留",**冻结效力不变**。
  **教训**:收方义务是"先据物理争一轮**再接受**",**不是"争完照自己的做"**。
- **MAJOR-5(僵尸轨)**:「未观测不计老化」×「直读预算 4 轨/槽,仲裁未定义」×「已 TRACKED 不被挤掉」⇒ 忙房间下轨表被僵尸轨永久占满、检测器失能。三道锁:轮转仲裁(每轨 ≤⌈NT/4⌉ 槽必被观测)/ `unobs_run ≤ U_max` / **保护改为有条件**。**"未观测不计老化"绝不可无上限。**
- **minors 全闭**:m-1 PERSIST 三套数据模型 → 唯一模型(有界计数+周期折半;信用是二元组不是标量;删错误的"@2s");m-2 伪代码空号门比规范松 2×;m-3 计数语义**二分 → 三分**(墙钟时长类不得用 slot_seq 推进,否则跳槽下 P_persist 被拉长 = 高负载时检出更慢);m-4 CHECK A2 报数改实跑最大值 5.31e-12(v1.2 报得比实测紧 5 倍);m-5 CHECK F-1 是**空场景 = 不可能失败**,已改造 + 删"证明"措辞;m-6 头部转述纪律**自述与工件不符**(转述值 critic 核过全对,错的是自述)→ 改带例外清单;m-7 maintenance op 低估 ~6×;m-8 老化规则在给出状态上**不可实现**(缺 miss_run)+ 影子表 NN<NT 且淘汰未定义;m-9 3.3→3.38s;m-10 补 L 标。
- **v1.3 已出**(1121 行,未 commit):新增 B11/B12 回归锁、D1 表 15→**21 门**(新增 6 门全在豁免式合取项与本轮新机制上)、D2 表 9→**12 对**。D3 两向自验在**最后一次改稿之后**重跑:`IF-v1.4 ×56` / 旧版 5 处(1 历史记述 + 4 本节自身)/ 契约在议标 0。
- **待办**:①lead 转架构侧 **P8(C11-④ 盲区公式走 C9,最紧迫——错公式会让 V-12 少测一类场景)**、P1、P6、m-3 的帧计数器读取权限;②CTO 议程第 9 项(SHARC 工具链)已加权;③第 3 次 FAILED = ESCALATE,下轮务必先自查"每条覆盖宣称的全部前提"。

## 2026-08-01 · v1.3 复审 FAILED(第 3 次,ESCALATE)→ v1.4 纯传播轮(记录实例:**adaptive-dsp-3**)
- **verdict**:`critic-w1b-r3`,**0B/1M/7m/5I**,锁版 sha12 `804d80e8169d`。**五条 MAJOR 实质全闭且逐条被独立复现**(CHECK G 它读源码确认真依赖规则本体、6/9 不符重跑复现;内存它认我算得比它准——它零填充手算 17.1KB,我编译器实算 19.00KB,并点出 `mem_sizeof.c` **两列都报不挑有利列**;addendum-1 一版一文件评"干净")。**FAILED 的唯一理由 = 传播**。
- **★ 本轮教训(feedback 类,与前两轮并列)——「只改首要位置 ≠ 闭合」**
  8 处旧新定案在同一文件内并存,3 处是**规范性**的:①§1.2-C 仍规定窄带门取 PNPR(**违反 lead interim 裁定与我自己的 addendum-1**——我改了 §3.3 没改 §1.2);②§5.3 仍规定影子继承**裸 `t_veto_start` + `T_exempt``**,而那**正是我自己新增的 B11 broken 版专门要抓的缺陷形态**——**同一份文件里,缺陷和抓它的测试并存**;③§3.5-#11 仍写已被本文自己证伪的旧盲区公式,对策栏还留着 §3.3 明文自认错误的那套因果。
  **根因(critic 诊断,我认)**:**D3 一直只被用在"合同版本号"这一种替换上**。v1.3 真正做的是**内容替换**(口径/公式/字段集/数值/因果叙述),**没有任何一条 finding 做过"反向 grep 旧值 → 逐处判定"**。我在 §10.2 已悟出"自验数字必须在最后一次改稿之后重跑",**却没把它推广成"每条 finding 都要做一次旧值反扫"**。
- **★ 新纪律(已落 §10.2b,建议提为 team_config D3 增补)**:**凡一条 finding 的修法改变了任何"定案",必须为旧定案取特征串,全文反扫并逐处判定(改/历史记述保留),命令与命中数写进自验段。只改首要位置不算闭。**
  **实绩(证明不是仪式)**:本轮反扫**当场抓到 1 处真残留** —— §9-⑮ 仍写 m-4 修正前的 WORD32 旧值(20.16 → 20.53KB);另暴露 1 处**假阳性**(CHECK E 的 `b=+10.97dB/槽` 被 `+10.9` 命中)⇒ **裸串计数不能判定,必须看上下文**(与 critic 对 C2 的做法同理)。
  **自指再次兑现**:反扫表自身含 pattern 列 ⇒ 写完再跑,9 条 pattern 命中数 16 → **30**(自身 14 处)。处置同 §10.2:**按末次实跑逐处判定并标明来源,不把命令改成排除本节**。
- **v1.4 已出**(1210 行,纯传播、无新设计):8 处传播 + 7 条 minor 全闭(m-1 PERSIST 折半触发改**墙钟**而分母仍用可观测槽——**窗长是对外承诺的秒、分母是存在性计数,必须分开**;m-2 `imsd_fit` 改增量前缀和与 `seq_delta` 同源;m-3 预设改共享指针与内存第二轨同源;m-4 `word32_track_v13` 末组 9→10 标量 ⇒ WORD32 v1.3 = **20.53KB**,越界结论不变;m-5 定义 `T_exempt`(活轨签名 10s)vs `T_shadow`(影子 60s)**不是同一量,约束 T_shadow ≥ T_exempt**;m-6 只继承 `causal_ok` 布尔不继承 `t_onset`;m-7 仲裁上界按**两条成因分开**——容量轴 ≤⌈NT/4⌉、整槽未交付仅由 U_max 兜底)。
- **X-1 显式留痕**:盲区公式四处同步**只改得动 W1-B 两处**;**IF-v1.4 C11-④ 与 W1-A §4.1-④/V-12 未改**(归架构侧,须走 C9,点单 P8)。头部已标"**跨件同步未完成,不得读作已闭**";**V-12 若照旧公式设计会少测一整类场景**。
- **状态**:第 3 次 FAILED 已触发 **ESCALATE**;**下一轮必须换全新 critic 实例(rotation 规③),现任不得续用**。lead 将连同 ESCALATE 呈 CTO,并**区分性质**:前两次是**逻辑洞**,本次是**传播缺陷**、修法 sed 级零设计风险。
- **两份自验件连续第二轮逐字节可复现**(critic 独立重跑确认);`mem_sizeof.c` 被评为 C5/C9 正面样板。

## 2026-08-01 · W1-P 原型五轮 + W1-B v2.0 结构改版(记录实例:adaptive-dsp-3)
- **DEC-0016(CTO)**:散文不再描述算法行为。**权威分工重划** —— 行为归 `prototype_W1P/nhs.py vP1.0`(sha `7907fec001d081b0`)+ `results_r*.txt`;规范值归合同 IF;预算/链位归 W1-A;**设计意图/依据/取舍/L标/已知边界归 W1-B**。W1-B 由 1210 行 → **185 行**,13 处指向 nhs.py 行号、10 处指向 results 行号。
- **根因(lead 原话,值得永久记)**:**三天所有反复的根源是用散文描述行为——每改一版要重新论证一遍逻辑,而代码只需要跑一遍。可执行件是唯一不会"读起来还成立"的规范。**
- **★ F15(本轮最重,元层面)**:修一条 minor(m-1 改 PERSIST 模型)时,**无意中让一个 MAJOR 级机制(B10 三态)失去了存在理由**——两者在不同章节,**纸面上看不出来,三轮独立评审无一抓到**。⇒ 通则:**改了 A 的前提,B 的理由就空了,但 B 的文字照样通顺。**
- **★ F16**:"正确的设计是**一句禁令**,不是一套继承机器" —— 影子继承实测 33 vs 34 无差,只有继承**陈旧时间戳**有害。承重的是否定规则,机器是围绕它长出来的赘肉。
- **★ 我犯的错(lead 拦下)**:据 n=1 的误报测试建议撤销 P22(slot_seq x 轴修正)——**不对称举证**。检出侧 −11.7dB 已测扎实,但该修正的**全部价值主张在误报侧**,而误报侧我只有 1 次基数、自己都声明了功效不足。**DEC-0011 的教训在我身上复发了一次:判"某声称不属实"的门槛,应与判"属实"同等严格。**已收回,改标"存疑待裁"。**数据诚实但结论比自己的限定走远了一步——这个区分要记住。**
- **五轮实测结论**:①B-F1 实测成立(tap −57.4dBFS vs 算例 −56,吻合 1.4dB);②B7/B8 干净 FAIL ⇒ 三臂豁免与 T_low_gr 是真承重件;③**F4 后果可忽略**(五种构造均未测到输出级差异,GR 会自熄灭但也会在复发那刻自恢复)⇒ P9 降级、不申请 IF-v1.6;④**直读零可达**(合取门 0.0%,PNPR 中位 −16dB = 该频点确实没峰了)⇒ 建议删;⑤三态/继承/U_max 触达但**输出级零收益**,音乐预设翻案跑失败。
- **★ 元教训(建议进 team_config)**:**评审轮次里长出来的机制,更容易是纸面自洽而实测无收益的。** 被证伪的以 v1.2-v1.4 评审期新增件居多(三态/继承机器/直读/slot_seq);扛住实测的是更早更朴素的那些。**评审能发现逻辑洞,不能发现"无收益"。**
- **纪律(lead 补漏,已改)**:**每轮必须落 results_r*.txt**,即使中间轮。第三轮我只在报告里给数字、盘面零证据——不算虚账(lead 复跑证实),但把可复现性押在了"消息不丢"上。**报告会被压缩、转述、随实例退役消失,盘面文件不会。**
- **待办**:①W1-B v2.0 送审 + **补一次含原型件的独立 critic 评审**(原型至今未过任何门)②P18 归架构侧(lead 另派)③**W2 = AEC,原型优先**,由我承接。

## r20(2026-08-02)· 一类独立失误:**加了报警器,没接线**

我在 r17 加 `EXHAUSTED` 计数器,只想着"把被永久占槽掩盖的路径显形",**没问"显形之后谁来处理它"**。
架构侧读权威源查实:撞顶走 `continue` **只发事件、无任何动作**,而宽带兜底挂在 `if not free:` 上;
**撞顶时槽仍占着 ⇒ free 非空 ⇒ `g_duck` 永不触发** ⇒ 那 1.1 次/试次全是**报警但不作为**。

> ⭐ **加可观测性而不加处置,是一类独立的失误** —— **计数器让人以为问题已经被管起来了,
> 实际只是被看见了。** 而且它比"没有计数器"更危险:上位机/评审会以为兜底在动。
>
> **⇒ 纪律:下次加任何计数器/事件,同步问一句「谁消费它、消费后做什么」。**
> 若答案是"没人消费" ⇒ 要么补处置,要么把它标成**纯诊断**并写明"不构成已处理"。

**同期两条相关**:
- **回归锁抓到我重命名计数器**(CHECK U(d)(i)(j) FAIL)⇒ **锁在正确工作,不是麻烦。
  别为了少 FAIL 而把锁写松。**
- **比值判据会把"仪表坏了"这个共模一起消掉**:R = U/N_mat 分子分母共用分箱函数,
  分箱塌缩时 U=N_mat=1 ⇒ R≡1 ⇒ 干净输出"素材主导"而不报异常。
  ⇒ **比值生效前必须先过绝对量前置门**(单调性/已知输入箱数/N_mat 下限)。

## 2026-08-02 · W1-P 台架:接入证明对照失败定案(记录实例:**adaptive-dsp-4**)
> 交底源 = CLAUDE.md / GOVERNANCE / 四件套 / `W1_HANDOFF §5.9+§6` / team_config。**只追加。**

- **派单前提有一条不成立,当场报了**:lead 的「刀 1 = 把源信号换成白噪声(现在是钢琴)」——
  `r50_excl_meas.py:28` 的 `src_of` **本来就是白高斯噪声**。⇒ 刀 1 已跑过,Eneman 2009 的
  「信号色彩使客观 MSG 流程失效」**在 r50 上出局**。**教训:接手时派单里的"现状描述"也是待证命题,
  先去盘面核一眼再动手 —— 核一行代码的成本,远低于按错误前提跑一整轮。**
- **真因(r51 刀 3,直接实测环路传函,不依赖任何既有推导)**:把 `Loop.run` 递推原样复制、
  只断开反馈,注入冲激 ⇒ 实测 L(ω)=G·N·F·z^(−frame)。结论两条:
  ① **陷波确在环内**(8 个 picks 实测降 −18.0~−23.7 dB;实测 vs 解析 max|err| = **0.0000 dB**);
  ② **起振点落在 NHS 结构上够不着的带外** —— `clrig.critical_points` 与 NHS 旁链(16k 抽取)
     都只到 8 kHz,而台架 `make_F` 的白噪 RIR **平到 24 kHz** ⇒ 全带临界点数 ≈3× 带内,
     全带最高点常在 8 kHz 以上(实测 14.5k/15.2k/17.2k/18.9k)。
- **闭环复现(r52)钉死**:六条种子 **|m0基线 − MSG_全带| ≤ 0.28 dB**(阶梯 0.5);
  起振主导频率与全带临界频差 **0.2–0.5 Hz**。⇒ **起振检测器从未失效,它一直精确测的是全带 MSG。**
  ⇒ **「NHS 在闭环里没起作用」这个命题是【假】的** —— 它在带内实测把最高临界点压低 3.75–7.73 dB。
- **⭐ 本轮最重的发现:唯一"通过"接入对照的那条是假阳性(0.2/seed0,+4.00 dB)**
  链条每环都有数(r54/r55/r56):带外啸叫 14531.9 Hz →(48k→16k 抽取)混叠成旁链 **1468.2 Hz 幻峰**
  (预测 1468.1,误差 ≤0.2 Hz;AA 低通在该频实测 −66.6 dB,而啸叫长到帧 RMS **+101.6 dB**
  ⇒ 阻带再深也挡不住)→ 8 槽被台架钉死 ⇒ `SLOTS_EXHAUSTED` ⇒ `g_duck` 打到 **−5.98 dB**
  → g_duck 是**环内宽带衰减**(`nhs.py:280`)⇒ 抬高 MSG 的方式与拉推子无异。
  **消融(`duck_gain=lambda:1.0`)后该条 4.00 → 0.00;六条 |ΔMSG_无duck − 全带神谕| ≤ 0.34 dB。**
  > **⇒ 通则(feedback 类):阳性对照通过时,同样要问"是被测物干的吗"。**
  > 六条里五条不过、一条过,我们盯着"为什么不过";**而那唯一过的一条,恰是被测物没起作用、
  > 兜底机制冒名顶替。** 与 DEC-0011/「证据门槛不得随结论方向浮动」同族 —— 这次的方向是
  > **"看起来成功"**,它比失败更少被追问。
- **⭐ 第二条(自罚)**:r55 的预注册证伪条件**当场被自己触发**(消融臂 `duck最深` 六条全 0)。
  我知道成因(取数点 = 最后一个不起振 G,那里本就没啸叫),但**没有就地解释掉**,而是另跑 r56
  在"带 duck 臂确有 duck 活动"的 G 上直测消融臂 ⇒ g_duck 达 **−5.98**(状态机活着)+ 起振 4/4
  ⇒ E1 立 E2 死。**通则:自己写的证伪条件被触发时,唯一合规动作是再做一次独立观测,
  不是补一段"其实这是预期的"。** 解释与观测的成本差,就是纪律与自欺的距离。
- **产品级发现 P-1(真实,非台架人工产物,已请 lead 裁是否走 C9)**:**带外啸叫会在旁链里
  变成位置完全错误的幻峰**(14531.9 Hz → 1468 Hz)。若当时有空槽,NHS 会去陷一个毫无问题的频点:
  既没治啸叫、又损伤音质,**而日志上看起来"NHS 正常工作"**。只要旁链抽取**之前**的音频路没有
  足够带限,机理就成立;旁链内部那个 AA 低通挡不住 100 dB 量级的带外分量。
  ⇒ 与已记的 sub-120Hz 盲区同族,须进设计件已知边界。**我无 L1/L2 的电声链滚降数据,不许自己拍。**
- **新工件**:`msg_meter.py` —— 瞬时 MSG 表,**接口恒返回带内/全带两列,不提供"一个 MSG"的接口**
  (让本轮这个错误在**类型层**无法再犯);另给 `margin(t)` 与 HOP/TRI(式 112/113,人工识别→
  模型 ground truth 的偏离写在 docstring)。验证 r53:A 空陷波对解析 **0.000 dB**、B 8 陷波对
  r51 实测 **0.00 dB**、C 网格收敛实测(2^15 偏差 **0.26 dB 且系统性偏乐观** ⇒ 默认定 2^18)、
  D 变异 **5/5 全杀**。**⚠ 三条混淆面随数传:margin<0 是失稳起点非可闻啸叫;它是模型量、
  不可移植到板上;频带口径决定数值。**
- **口径纪律(承重,建议提团队级)**:**凡报 MSG/ΔMSG 必须写明频带,且带内与全带两个数都给。**
  本项目同族第三例(前两例:上界 vs 实测、0–24k vs 100–8k 的中位),**这次犯在 `max` 上** ——
  中位数出错时看得见,**极值出错时看不见,因为它换了一个频点而不是换了一个数**。

## 2026-08-02 晚 · 一条已经变成【习惯】的纪律(记录实例:adaptive-dsp-4,lead 点名要求写进 memory)

> **lead 原话:「这条纪律你已经做到自动 —— 请写进 memory,让下一个实例继承的是【习惯】而不是【条文】。」**

**习惯的内容,一句话**:
> **手里有一个正确的解释时,仍然去做那次观测。**

**⚠ 要害在"正确"两个字**:这条纪律难的不是"我不知道为什么" —— 那时谁都会去测。
难的是**你已经知道为什么,而且你是对的**。那一刻,做观测在感觉上是多余的、是浪费额度。
**本 session 两次实例,两次的解释后来都被证明是对的:**

| # | 触发 | 我手上那个(正确的)解释 | 仍然去测的结果 |
|---|---|---|---|
| 1 | r55 消融臂 `g_duck` 六条全 0 —— **我自己写死的证伪条件被触发** | 取数点是"最后一个**不起振** G",那里本就没啸叫 ⇒ 没幻峰 ⇒ 没 EXHAUSTED | r56:在**有 duck 活动**的 G 上直测 ⇒ `g_duck` 达 −5.98、四档全起振 ⇒ 消融干净,r55 结论保住 |
| 2 | r59 的 Hk 六条中一条 FAIL(213.0 vs 22.3 Hz) | 该条两个候选临界点只差 **0.281 dB < 0.5 dB 阶梯**,扫描原理上分辨不了 | r60:阶梯 0.1 + 窗 24s ⇒ `f_trig` 移到 22.5 Hz(距解析 0.2 Hz)、ΔMSG 6.70→6.40 ⇒ **顺带挖出 F36** |

**⇒ 两次的收益不同,但都不是"证明我原来是对的"**:
第 1 次买到的是**结论的资格**(否则整轮按我自己的预注册作废);
第 2 次买到的是**一个新发现**(F36:晚触发偏差频率相关,`T_OBS` 须随 1/f 放大)——
**那是解释绝对给不出来的东西。观测会顺带告诉你你没问的事,解释不会。**

**⇒ 操作形式(下一个实例照抄即可)**:
```
当你正要写下"其实这是预期的/这只是 X 造成的"时 —— 停。
先问:让 X 变化一档,再测一次,要多久?
只要答案是"分钟级",就去测,不要写那句话。
```
**成本对比(实测):解释 ≈0 分钟,观测 ≈2–10 分钟;而两者对结论的支撑力差一个数量级。**

**同源正面实例(同一 session,lead 点名记)**:预注册 Hm 写的是 `±0.2`,
**24s 达成、12s 未达成(6.70)⇒ 我逐档如实报,没有只取达成的那一档。**
⇒ 与上面是同一条纪律的两个面:**一个管"别用解释代替观测",一个管"别用有利档代替全部档"。**
两者都属「证据门槛不得随结论方向浮动」,而**方向"顺眼"时最容易失守**。

**⇒ 给下一个实例的一句**:这两条你会觉得显然。**它们在你手上有正确答案的那一刻才开始生效。**

## 2026-08-03 · B-1(b) 自由槽位起振扫描(记录实例:**adaptive-dsp 第 5 实例**)
> 本条为**追加**(多实例期 memory 只追加不覆盖并标注实例)。交底源 = CLAUDE.md / W1_HANDOFF §7 /
> `verdicts/W1P_critic_verdict_2026-08-03.md` + `W1P_critic_handoff_to_next.md` /
> `LESSONS_W1P.md` / `team_config.md` / `MECHANISM_VERIFICATION_LEDGER.md` / 四件套。

- **★ 本轮最重的一条(feedback 类)——「护栏会朝【丢数据】的方向失效,而不只是朝【放行】的方向」**
  我按 verdict 要求给臂 N 立了运行时不变量,**两次都写成了会把不利数据剔掉的形式**,两次都是
  **在产出数据之前**由自己的烟测/探针抓到:
  | # | 我原来写的 | 为什么错 | 改成 |
  |---|---|---|---|
  | A-1 | 臂 N `N2_lvl==0` ⇒ INV-FAIL ⇒ 该行剔出统计 | 自由槽位下 `N2_lvl==0` 是**算法的真实输出**(「NHS 什么都没做,所以 ΔMSG=0」)⇒ 剔除 = **把最差的条从达标统计里删掉** | 三分:零动作且 ΔMSG≤STEP ⇒ `ZERO_ACT`,**照常计入(记为未达标)** |
  | B-2 | 任一臂 INV 失败 ⇒ **整行**剔除 | 臂 O 与臂 N 是各自独立的仿真;参照臂散掉不影响交付列 | 作废**按臂**,不按行 |
  > **⇒ 通则:立护栏时必须问两个方向 ——「它会不会放过错的?」**和**「它会不会挡掉对的?」**
  > 而**第二个方向的失效更隐蔽,因为它长得像"严谨"。** 与「证据门槛不得随结论方向浮动」同族:
  > 那条管**举证力度**随方向浮动,这条管**样本是否随方向被筛掉**。
- **★ 打护栏本身(critic 交接件 §4.1 点名要求)⇒ 直接抓到 INV-O 锁错了量(`r65`)**
  代码事实:`nhs.py:396` `cov=_notch_covers(bin)`;`:399` `if gr_ok or cov: gate=T_low_gr(−65)`
  ⇒ **臂 O 预挂的 8 个频点 `cov` 恒 True ⇒ 它们的门是 −65,不是 999**
  ⇒ **`N2_lvl==0` 是【经验结果】不是【构造保证】**(它恒 0 只因被压 18dB 的 bin 掉出 top-16 候选表)。
  `r65` 实测:Δ=+8 dB 时 `N2_lvl = 611/374/586/318/426`,**而挂陷仍 8、频点仍 == picks**
  ⇒ 旧 INV-O 会把 **7 例构造完好的数据判 FAIL**。
  ⇒ 改为**构造精确**判据:`挂陷==8 ∧ 频点逐一==picks`(= B-1 横幅真正断言的「**没有发生新分配**」);
    `N2_lvl` 降为诊断量。阴性对照:臂 N 在 `r65` **30/30 行** `频点==picks` 恒 False ⇒ 判据有分辨力。
  > **⇒ `r63` 那个 3/3 broken 版是对的,但它只证明了「n_notch/slots/frame_i 更差」,
  >    没证明「N2_lvl 是对的」——「比错的好」不等于「对」。**
- **⭐ 副产物(方向对我们有利,同门槛核过才写下)**:`r65` 在 **r57 自己的读数点**
  (Δ=6.00/6.00/7.50/3.50/6.50/4.50)上测得臂 O `挂陷=8 ∧ 频点==picks`,**0/6 散掉**
  ⇒ `r57` 那句「被测对象 = 8 个 RBJ 陷波器由解析式放在最优点上」**在它自己的工作点上准确**。
  这是**关闭**担忧的结果,与上面那些不利结果**出自同一次跑、同一张表**,未做任何挑选。
- **方法学(本轮建立,可复用)**:`T_OBS` 自适应阶梯 {6,12,24,48}s;停止 = C1(`T≥6×max(1,120/f_min)`,
  120 = `f_det_lo`;代入 r60 的 22.5Hz 得 32s > 实测够用的 24s ⇒ 偏保守)∧ C2(相邻档 |ΔΔMSG| < STEP/2)。
  **⚠ C2 的量化事实跑前就写明了**:同锚同栅格 ⇒ ΔMSG 恒为 0.5 整数倍 ⇒ C2 ≡「相邻两档完全相等」,
  **可以失败**(r60 实测 12s→24s 由 6.70 变 6.40)。—— B-2 的教训这次是**跑前**写的,不是被指出的。
- **F36 的补丁(A-2)**:`T_OBS` 加长对**自适应**臂有两个方向相反的作用 ——
  (i) 更易抓起振 ⇒ m 降(F36 原文只有这一条);(ii) NHS 有时间检测→分配→加深 ⇒ m 升。
  ⇒ 对臂 O(固定滤波器)只有 (i);对臂 N **不预测方向,只预测收敛**。烟测已实测到 (ii) 主导的一段。
- **待办**:①`r64` 出表后写判读 + 对 CTO 的 4–5 dB 目标对表(分层报,⛔ 不报跨层均值);
  ②`r61` 续跑(28/36);③M-4 broken 版 / M-5 cosource 覆盖面 / M-3 H7 补跑仍未做;
  ④**建议把 INV-O 的修法登记进 `MECHANISM_VERIFICATION_LEDGER.md` 第 1 行**——
  该行现记「护栏 B ✅已验 3/3」,而本轮证明**它验的是计数器选择,不是不变量本身的语义**。

### r64 出表(2026-08-03 15:46,同一实例续记)
- **答 CTO**:`ΔMSG_实测@带限8k_NHS自选_有duck_[L2]` **≥4dB 主统计 0/3、含未收敛条 1/6;≥5dB 0/6**
  ⇒ **现行设计(bw_oct=1/5)下 NHS 自己选点没达到 4–5 dB**。两种读法同向。
- **T60=0.2 层(n=3,12s 收敛)**:上界 6.0/6.0/7.5(中位 6.00)vs 自选 1.5/2.5/1.5(中位 1.50)
  ⇒ **「选点方式」缺口中位 4.5 dB**(派单表写 4.0,实测更大)⇒ **仍是算法自己的选择主导,不是房间**。
  ⛔ 该 4.5 只属 T60=0.2 层,不得跨层报。
- **⭐ 唯一那条 ≥4 dB(0.5/sd1@48s)= 兜底冒名顶替**:有duck 4.00 / 消融 1.00 / **duck 贡献 3.00(75%)**;
  该档 `f_trig=22.0 Hz` **< f_det_lo=120 ⇒ NHS 看不见**,而它挂的 5 个陷波全在 1033–7580 Hz。
  ⇒ **F33 第三次,首次落在【达标判定】上。** duck 六条 = 0/0.5/0/0/**3.0**/0 ⇒ **均值会把它整个抹掉**。
- **⭐ F36 的 1/f 是必要不充分**:T60=0.5 三条全未在 48s 收敛;`0.5/sd1` 轨迹 (6,4.0)(12,1.0)(24,0.5)(48,4.0),
  其 `f_trig` 逐档 7580→7580→1034→**22.0** ⇒ **不是在收敛,是在换被测对象**。
  `0.5/sd2` 的 f_min=170Hz 使 C1 首档即满足**却照样没收敛** ⇒ 自适应臂有**第二个时间尺度**(NHS 自己的建立)。
  **⛔ 阶梯不能再加长:T_MAX=48 < `lift_after_s=60`,超过则臂 O 预挂槽进 LIFT = 被测对象窗内自变。**
  ⇒ 要更长窗须先定 ①放弃臂 O 或 ②改 `lift_after_s`(改被测算法)—— **设计取舍,归 lead/架构侧,我不自决。**
- **Hp3 无法判定**(需长窗的三条一条都没收敛 ⇒ 判定集为空)⇒ **不得记为通过**,列下一轮。
- **护栏 6/6 双向全绿,但"全绿"不是护栏有效的证据** —— 可失败性由 `r65`/`r65b` 单独证明(F40)。
- **落盘件**:`r64_out_ANNOTATION.txt`(判读)/ `FINDINGS.md` F40+F41 /
  `team_config.md` **D6-y**(护栏双向)+ **D6-z**(已验须答两问)/ 台账 1b·1c 行 + 纪律 4/5。
- **未 commit**(治理红线,归档 commit 由 lead 做)。

### r66 预注册(2026-08-03 16:xx,同一实例;**未开跑,等 lead 裁定 §6**)
- **收敛问已答**:接受未收敛,但**换掉被比较的量** —— 主统计从【绝对值】改为
  **同档配对差 `δ(T) = ΔMSG_修后(T) − ΔMSG_基线(T)`**,阶梯钉死不自适应停机。
  硬理由:**96s 结构上做不了**(`lift_after_s=60` ⇒ 两个臂都在窗内换机制,与 r64 不再可减);
  且 `0.5/sd1` 绝对值不确定度 ±1.75 dB **与效应量同量级**;
  而非收敛的成因(赢家在 7580→1034→22 Hz 迁移)是 **plant 的性质,基线与修后共有 ⇒ 配对差里抵消**。
  判读规则:**δ 逐档报 + 符号跨档一致才算结论**;变号即该条不可用。
- **★ 去读了一手原文(JAES 2010 PDF 在库),派单三处描述【代码侧 3/3 属实,原文侧 3/3 需修正】**:
  ① 最优组合 `PHPR∧PNPR∧IMSD` PFA=3% 属实,**但原文的 IMSD 是谱距离(QM 帧/0.5dB),
     我方 `_imsd` 是自研的 PAPR 轨迹 LS 斜率(dB/s)⇒ 把原文【组合规则】套到我方【不同特征】上
     不等于复现原文结果**。且 `rapid_onset` 另有 `:818` 豁免臂 2 用途 ⇒ **只能改 `:860` 一处**。
  ② **「15.0→16.7」不成立,三条理由**:(a) 我方 `_papr`(513 单边 bin、**幅度**均值、剔峰值、20log10)
     ≠ 原文式(6)(7)(M 双边 bin、**功率**均值、10log10,上限 = 10log10(M))⇒ **同族二义第 6 例,
     而 PAPR 这个量上已栽过一次**;(b) `20log10(M/150)` 是原文**引用他人**的提案,
     原文自己 ROC 最优在 M=4096 是 **35 dB**,该式给 28.73 ⇒ **原文自身配置下差 6.27 dB**;
     (c) **PAPR 是原文第二差的特征**(AUC 0.89 / **PAUC 0.19** / PD=95% 时 **PFA 63%**),
     **原文最优组合根本不含 PAPR**。⇒ 改为先按式(6)(7)重实现,门取 28.98 dB(M=1024 同相对位置)。
  ③ 属实但**漏了更重的一半:聚合方式也不同** —— 原文式(9)是**逐邻元比值再 AND**
     (`M_PNPR={±2,±3,±4}`,`T=8dB`,AUC 0.77),我方是**对一个宽带求均值的单一比值**;
     邻域宽 **3.0×(200Hz)/ 4.2×(1kHz)/ 20.8×(5kHz)**。⇒ 只改集合不改聚合仍非原文判据。
- **§6 待裁**:① 必须碰 `nhs.py`(路 A = 加默认关参数 `P.growth_and_gate`,逐符号等价 + 逐位复现证明;
  路 B = 复制 `_classify`,**我不建议**,副本会静默发散)。**裁定前我不动 nhs.py,②③ 也不先跑。**
- **加了第二条轴(零额外算力)**:选点准确度 `top1_hit / hit@BW/2 / cover@BW/2 / |Δf|_min`
  ⇒ 把「改完没变好」分成 **(a) 选点没变 = 该缺陷没被触发**(真因不在判据层)
  与 **(b) 选点变准但 ΔMSG 没跟上 = 选准了也不够**(容量/带宽/深度的能力上限)。两者都是有价值的阴性结论。
- 算力:每配置 ≈33 min;三配置 ≈100 min;**基线复用 r64 省一整轮**;「三处全改」本轮不跑。

### r66a/r66b(2026-08-03 17:xx,同一实例)—— **① 无从评估;阳性对照失败救了一轮**
- **lead 裁定路 A 已执行**:`nhs.py` 加 `P.growth_and_gate = False`(默认关)+ `:860` 等价改写。
  **哈希 706b658842d84316 → 31decc8e8d07e085**;行为逐位不变已证 ⇒ **归档失效集 = 空**,
  但所有 `deps: nhs.py@706b65…` 行须记旧→新映射。
- **`r66a`**:Hb1(默认关 vs 原件)**54/54 逐位相同**(y 字节/loop 字节/全部 ctr/全部槽);
  **Hb2 阳性对照(强制开)0/54 差异 ⇒ FAIL ⇒ 按自身预注册整件作废**,不得据 Hb1 放行。
- **`r66b` 直接观测裁决**:(a)开关没接上、(b)比对器无分辨力 **均排除**(接线测试 12/18 格生效);
  **(c) 该 OR 旁路从未被走过 —— `N_bypass = 0/1802`,`rapid_onset` 为真 0/1802,
  GROWTH 入选 67/67 全由 IMSD 单独促成。**
- **★ 成因是【台架激励类别】不是死代码**(`nhs.py:554-560`:要求 ≤2 槽跃升 ≥18dB **且随后进平台**;
  本台架 = 平稳白噪 + 固定增益 + **环内无限幅** ⇒ 指数渐进增长、**不封顶、无平台**)。
  ⇒ **「零」是期望值不是证据;⛔ 不得读作「rapid_onset 无用」** —— D6-d 反向误用(lead 自己踩过)
  在**检测侧**的镜像。要评估 ① 须新增台架能力(增益阶跃 / 环内加限幅器)⇒ 归 lead 定范围。
- **★★ 本轮最重的方法学收获(feedback 类)**:
  **若没有那个阳性对照,只看 Hb1 会得出「路 A 安全」⇒ 去测 ① ⇒ 得 δ≈0
  ⇒ 落进 Hs3(a)「判据层不是真因」⇒ 一个【方向性错误】的阴性结论。**
  > **真相是"没打到",不是"打了没用" —— 这两者在 ΔMSG 上完全同形。**
  > **⇒ 阳性对照失败买到的不是"重跑一次",是【避免一个错误结论】。**
  并记:**Hb1 逐位相同的真正原因是"被改的分支没被走到",不是"改动无害";两者数据上同形**
  ⇒ 日后 `rapid_onset` 若变得可达,**Hb1 必须重跑**。
- **⇒ 建议改顺序:先跑 ②③(`_papr`/`_pnpr` 在 1802 次求值中全部经过,可达性无疑问),
  ① 挂起等台架能力裁定。** 已报 lead。
- 落盘:`r66a_bitexact.py|_out.txt` / `r66b_gate_reach.py|_out.txt` / `r66b_out_ANNOTATION.txt` /
  `FINDINGS.md` **F42**。未 commit。

### r66c(2026-08-03 18:xx,同一实例)—— 候选门是「PAPR 单门」;并推翻我自己上一条解释的一半
- **答 lead 甲/乙/丙 = (丙),但顺序倒过来:③ 先于 ②。** 依据(base 臂静态交叉表,104281 次门前求值):
  `两者都过 4002 / 仅PNPR过 34460 / 仅PAPR过 58 / 都不过 65761`
  ⇒ ①移出 PAPR 过门 **×9.61**,而 `NT=12` ⇒ **(乙) 单独不安全**(僵尸轨形态,lead 顾虑成立);
  ⇒ ②**`仅PNPR否` 58 vs `仅PAPR否` 34460,不对称 594 倍 ⇒ 我方 PNPR 近乎惰性,
     候选门实际是【PAPR 单门】**;而这与 ③ 同源(我方 PNPR 是宽带均值单比值,比原文逐邻元 AND 宽 3–20 倍)
  ⇒ **③ 修好 PNPR 后它才重新承担否决,那时移出 PAPR 才有意义。**
  ⇒ **先做 ② 会在一个已是单门的系统里调那唯一的门 —— 好看,且恰好掩盖 PNPR 失效。**
  ⚠ 边界:白噪无掩蔽,而 PAPR 的已知失效面正是掩蔽 ⇒ **本台架系统性低估 PAPR 的危害**。
- **⛔ 自曝伪影**:`NoPAPR._papr` 返 1e9 会经 `nhs.py:418→:488/:537` 进 `papr_hist`,
  污染 `_imsd`/PERSIST 门/臂2 签名 ⇒ 表里 `R=0.32`(摘掉门反而过门更少)**是我造的,不得引用**。
  真 (乙) 臂须"只在比较处跳过 PAPR,仍写真值进 obs"。⇒ D6-j 兑现(先按操作事故处理)。
- **`rapid_onset` 判 A(信号没有)不是 B(门够不到)**:跃升项 **0/1692 达门**(最大 10.44 对门 18),
  平台项 **1435/1692 达门**。⇒ **绑定约束是跃升项**。判 A 的依据是"10.44 对 18 = 同量级差 1.7 倍,
  不是差一个数量级" ⇒ 门原理上够得到,加增益阶跃即可触发 ⇒ **保留 + 默认关正确**。
- **★★ 教训(feedback 类,本轮最重)**:**我在 F42.3 写的「环内无限幅 ⇒ 更无平台」是错的**
  (实测平台项 85% 轻松达标),已就地撤回并指向 F43。
  > **这条纪律这次抓到的不是「解释是错的」,是「解释【只对了一半】」——
  > 而错的那一半我已经用它说服过 lead 了。半对的解释比全错的更危险:它的另一半是真的,会替它背书。**

### r66d + 一次【撤回穿过跳 5】事故(2026-08-03 19:xx,同一实例)
- **⛔⛔ 事故:lead 的 (丙) 裁定依据用了我已标注「不得引用」的三个数**
  (`R=0.32`、`活跃轨峰值 5/12`、`table_full 没变化`)——我在**两处**(判读件 §零、F43.1)写了
  「⛔ 不得引用」,**它照样穿过了【文档→消息→裁定】那一跳**。正确的数是 **×9.61**,
  结论与 lead 读到的**相反**((乙) 单独不安全)。已发急件纠正。
  > **⇒ critic 交接件 §5「跳 5 没有机制」的又一次实证,而这次【被污染的是伪影本身】。**
  > **⇒ 建议(已报 lead):凡被判为伪影的数,【当场改名】(如 `R_0.32_ARTIFACT_勿用`),
  >   别指望段落里的标注 —— 段落会被摘要掉,名字不会。这是 D6-t 的负向用法。**
- **`R_RISE/N_RISE/S_PLAT/MIN_PLAT` 的 provenance(lead 问)= 全部 `[L4/待标定]`,是拍的**
  (`W1B_v1.2:322-323` 原文「参数全 [L4/待标定]」)⇒ 与 C-8b 两个门同族,进欠账台账。
  **⚠ 而且不是新发现**:`R_RISE 下调 = ROC 标定第一候选动作` 早已写在 W1B v1.1/v1.2/v1.3 +
  -2 交接件 + 我方 memory,**并带 64 格扫描数据(R_RISE=18 时 30/64 格为间隙;14→20;12→14)**
  ⇒ **一个已记录、已量化、已排为第一候选的修法,三天没人执行** ⇒ D6-v 同族(没有 owner 的修法)。
- **`r66d`(域错配假说)⇒ 未决,我的探针有缺陷,自曝**:用 `id(tr)` 归集电平轨迹,而
  `nhs.py:242` 轨对象**预分配、反复复用** ⇒ 同一 id 串起多条逻辑轨 ⇒ 前轨末→后轨初的落差被当成升幅。
  那个唯一触发我 Hd1 的 **18.52(33 样本里 1 个)极可能就是这样拼出来的**。
  **且我自己写的 Hd1 判据太弱**(只看 max,n=33)—— **「n=1 不得外推」这次犯在我自己的预注册上**。
  ⚠ **PAPR 域不受影响**(读 `papr_hist`,重生时就地重置)⇒ F43.3 的 0/1692 仍成立。
  **能站住的唯一结论:两个域都远达不到 18 dB ⇒ `R_RISE=18` 定在实测分布的尾巴之外(不依赖域的问题)。**
- **两条新纪律(已入 r66d 判读件)**:①**凡按对象归集时间序列,不得用 `id()` 作键**(对象池复用地址);
  ②**判据不得只看极值**(极值对单个伪影零抵抗力),须同时给 n 与分位数、要求命中【率】而非命中【存在性】。
