# channel-dsp 交接件(实例 #2)

```
⛔ 门禁状态:本文件是交接/状态记录,不是交付件,不构成任何放行依据。
   本文件描述的所有产出均为【未过门】(未经独立 critic 评审)。
实例    : channel-dsp 实例 #2 @ claude-opus-5[1m]
上岗    : 2026-08-04 ~13:53
前任    : 实例 #1(2026-08-04 约 13:50 退役,无交接件)
```

## §0 当前在做什么(⭐ 恢复工作先读这一节)

**状态:⛔ 检查点 r10 —— 本轮到此。⚠ 但请先读 §0.1:critic 对 02_impl 有两份 verdict 我一直没收到。**

**基线(实跑,exit 0)**:`run_all.sh` 201 秒 —— 元检查 **21/21** ｜ 模块自验 **47/47** ｜
负编译 **11/11** ｜ 变异自证 **40/40**(含名单对表)｜ 杀伤矩阵 **21/21 全杀** ｜ 第二轨 ｜ 强类型中立性。
`run_r3.sh` exit 0。

### §0.1 ⛔⛔ 我一直没收到的两份 verdict(**接手的人先看这个**)

`01_design/verdicts/` 里有 **`critic_02impl_INTERIM_20260804.md`(14:27)** 与
**`critic_02impl_r1_verdict_20260804.md`(14:46)**,以及 **`critic_02impl_r2_verdict_20260804.md`(15:28)**。
**我在 15:40 左右自己在 verdicts 目录里发现它们** —— 此前 lead 转给我的是**设计侧**那份
(`critic_D34_FIXEDPOINT_...`),我一直以为那就是全部。
⇒ **∴ r1 开的 1 BLOCKER + 5 MAJOR,我在 r2 之前一条都没动 —— 不是不做,是不知道。**
⇒ r2 判定 = **FAIL(维持)**,理由原文:「新增了大量高质量工作,而我提的六条一条没关」。

| r1/r2 条目 | 现状 |
|---|---|
| **BLOCKER-1** `expect_red` 分不清「闸门响了」与「命令跑不起来」 | ✅ **已闭** —— 见 §2⑫。⚠ **闭于 r2 tag(`d8fced2`)之后**,故 r2 verdict 里仍记为「逐字节未动」 |
| **MAJOR-3** 自证覆盖面与杀伤矩阵名单无对表 | ✅ **已闭(本轮)** —— 见 §2⑫ |
| **MAJOR-1** `ref_modules.py` 是同式转写却称「第二轨/独立重写」 | ⛔ **未闭** |
| **MAJOR-2** docstring 承诺 3 条对表项只实现 2 条;T2 无阳性对照 | ⛔ **未闭** |
| **MAJOR-4** 6 条已知零分辨力的旧检查未加标注 | ⛔ **未闭** |
| **MAJOR-5** 21/40 检查无证伪证据(成本已涨到补 14 个变异) | ⛔ **未闭** |

### ⚠ 评审锁(2026-08-04 起,lead 定)
- ⛔ **锁着,改前须报 lead**:`01_design/D3D4_CHANNEL_CHAIN_v0.1.md`、`01_design/d34_chain/`
- ✅ 未锁:`02_impl/`(但**改完立刻报 lead**,他要钉 commit SHA 快照)、
  `01_design/D3D4_CONSTANT_LEDGER_v0.1.md`、`01_design/fixedpoint/`
- 新建 `01_design/d34_gain_structure/`(因 `d34_chain/` 锁着,解锁后建议并入,⛔ 我不自行搬)

---

## §1 ⚠ 上岗时校正了 lead 派单里的两处事实

| lead 派单说 | 实际 |
|---|---|
| ②「前任只加了描述问题的注释,没修」 | **`02_impl/test/check_negcompile.sh` 是前任重写的新件,三道修法全在,实跑 10/10**。⛔ 但 critic 点名的那一份 —— `01_design/fixedpoint/check_negcompile.sh` —— **原封未动**,且 `run_r3.sh:87` 仍在调它 ⇒ BLOCKER 只闭了一半 |
| ③「把 check_gates_fire.sh 接到 run_all.sh 上」 | 该文件**已存在且 5/5 PASS**,但**未接进 run_all.sh**;且只覆盖 6 环中的 4 环 |
| ⑤「同一模型可能还传到了 `02_impl/test/check_modules.c`」 | **否定** —— 实测 `check_modules.c` / `ref_modules.py` 里**没有**噪声模型 |

---

## §2 已完成什么(带路径)

### ① `S>1` 残留(critic MAJOR-4)—— 5 处 + **反扫另查出 6 处**
- 改:`01_design/fixedpoint/chdsp_fixed.h` §12、`D34_FIXEDPOINT_CONVENTION_v0.1.md` §0/§3.3/§3.2.2、
  `D34_CONSTANT_LEDGER_v0.1.md` A3/C 段引言/C3/F 汇总
- ⭐ **反扫另查出 6 处 critic 清单外的**:`18.089 / 18.09` 这个**由旧值推出的数**传到了
  `D34_CONSTANT_LEDGER` §F(那句"建议写进 D2 参数字典并锁死")、`D3D4_CONSTANT_LEDGER` B14、
  `D3D4_CHANNEL_CHAIN` §0 表 + §3⑥。
  **实证**:`18.089` 是**二分求出的越界点**,在该值处实扫 max|b| = **16.0002 ⇒ 已越界**;
  可锁的是解析包络 **18.0618**。⇒ 全部改为 18.0618。
- 归档:`01_design/fixedpoint/check_envelope_r4.py` + `results_envelope_r4.txt`

### ② 负编译 BLOCKER + `run_r3.sh`(critic BLOCKER-1 / MAJOR-1)
- 重写 `01_design/fixedpoint/check_negcompile.sh`:(A) 命中预期错误正则 + 排除
  `file not found`/`implicit declaration`;(B) 前置存活自检(16 个符号);(C) `STRICT_TYPES=0` 对照臂
- 三条实证:改名 ⇒ FAIL ✓;拿走头文件 ⇒ FAIL ✓;**把 `apply_gain` 改收 dB(真打开量纲缺口,符号仍在)⇒ FAIL** ✓
- `run_r3.sh`:存活报警改显式计数 + 硬中止;`GOOD/NEG/REF` 全部消费;`PIPESTATUS` 取回块退出码
- 并同步修 **m-2**(含端点错一格):下界 −109.816 → **−109.8125**(−109.816 是**首个越界格**,|err| 0.010003 > 0.01)

### ③ 元检查扩充 + 接线
- `02_impl/test/check_gates_fire.sh`:5 → **13 条**,新增 G3b/G6/G7/G8/G9/G10
- 接进 `run_all.sh` 作**第 0 环前置**(`CHDSP_GATES_META=1` 防递归)

### ④ 变异自证(杀伤矩阵前置)
- 新增 `02_impl/test/probe_mutants.c`(15 条行为探针)+ `check_mutants_valid.sh`
- **Phase A 结构探针**:`gcc -E` 读预处理后的**真实调用顺序**,断言位置关系按声称方向翻转
  ⇒ 这才抓得住前任那个「挪了但没挪到声称的地方」
- **Phase B 行为探针**:每个变异只比它自己那一行读数
- G10 阳性对照:**复现前任的原始错误形态** ⇒ 自证确实变红 ✓

### ⑤ §6.4 全链噪声底(critic MAJOR-2)
- `D34_FIXEDPOINT_CONVENTION` §6.4 拆成 6.4.1(各节增益=1)+ **6.4.2(最坏合法增益)**
- 独立复算:① −164.32(模型精确)② −164.49 ③ −160.12 ④ **−91.64**(+72.68 dB)
  ④′ **D3 输入链 −76.97**(+85.59 dB)⇒ ⛔ 突破 PRD −106 达 14.36 dB
- 传播已改:`d34_chain/d34_analysis.py` EXP-5、`D3D4_CHANNEL_CHAIN` §0/§6、`D3D4_CONSTANT_LEDGER` E8
- 归档:`d34_chain/check_noise_chain_r5.py` + `results_noise_chain_r5.txt`
- ⛔ **未动格式**(Q4.27 / EF / DF1 全不变)

### ⑥ C 第二批:分频补全(r8)
- 预注册 `d34_chain/PREREG_D34_r8_xover.txt`(⛔ 写于跑批前)
- `d34_chain/xover_r8.py` + `results_xover_r8.txt`:EXP-9/9b/10/11 全过
- 实现 `02_impl/src/chdsp_biquad.{h,c}`:`chdsp_bq_design_first_order()`、
  `chdsp_bq_design_xover2()`(BW/LR/Bessel × 1..8 阶)、Bessel 归一化极点表
- 测试 `check_modules.c` CHK-X1…X6;新增 2 个变异 + 自证探针
- 文档:`D3D4_CHANNEL_CHAIN` §8 的 **Y7/Y8 已闭**、§4③ 加 Bessel 求和警告;
  `D3D4_CONSTANT_LEDGER` 新增 §G(G1–G6)

---

### ⑦ 急件:「阶数 mod 4」单位自相矛盾(lead 插队件)
**实证(改动前)**:`chdsp_xover_needs_polarity_flip(1, 12/24/36/48)` = **0,0,0,0**;
正确答案(按 n=2/4/6/8)= **1,0,1,0** ⇒ LR2/LR6 会被判同相 ⇒ 分频点深谷。
- ⭐ **关键观察**:设计函数早就有 `order > 8 ⇒ ERR_ORDER` ⇒ 喂 dB/oct 会**当场硬失败**,是安全的。
  **只有极性函数没有量程守卫,而它恰恰返回一个"看起来合理"的布尔值。**
  ⇒ **返回 bool 的函数最危险 —— 它没有"非法输入"这个取值。**
- 修法(D-1,复用本项目已有的强类型机制):
  ① `chdsp_xo_order_t`(n)/ `chdsp_xo_slope_t`(dB/oct)两个类型 ⇒ 传错**编译不过**(负编译 **N8**,对照臂证明确由强类型造成)
  ② 极性规则按 **n**;③ 运行期量程守卫:非法 ⇒ 返回 **−1**(⛔ 不是 0)
  ④ **CHK-B3s 会拦人**:四档各跑一次,规则极性求和 **0.000 dB**;
     阳性对照(反着来)**−120…−133 dB 深谷** ⇒ 该检查有分辨力,不是恒真
  ⑤ 新增变异 `CHDSP_BROKEN_XO_UNIT`(把 mod 4 算在 dB/oct 上)⇒ 被 CHK-B3/B3s 杀死

### ⑧ 急件:变异自证补【断言②】
原来只断言「声称要改的确实变了」(形态⑤)。现补「**没声称要改的,逐位没变**」——
即 critic 在设计侧抓到的**镜像**形态:变异**实现了它没声称的**缺陷 ⇒ 杀伤归错因。
- 实测 16 个变异:**15 个只动自己那一条探针**;唯一例外 `CHDSP_BROKEN_TRUNC` 也动 `P_NOEF`
  ⇒ 判为**物理上不可分割**(舍入模式就是量化器的一部分,P_NOEF 测的正是量化误差)
  ⇒ 进 `ALSO[]` **显式声明 + 写明理由**。⛔ 声明是留痕不是豁免:未声明的连带一律 FAIL。
- **G11 阳性对照**:让一个变异顺手注入第二个缺陷 ⇒ 断言② 确实变红 ✓

### ⑨ 两条数(lead 转 critic)
- **E1「31.14 dB 余量 10×」是错的说法**:31.14 是**两种链序之间的差值**,没有对标物 ⇒ 除以判据无物理含义。
  真正的临界配置 = 满刻度 + 2 段 +15 dB @40 Hz ⇒ PEQ 在前 **+30.00 dBFS(越 24.08 余量)**。结论不变,依据换掉。已改台账 E1。
- **`comp_makeup` 噪声**:我复算后**与转述的数对不上,而差别有原因** ——
  −143.72 对应「增益在**所有** 9 个量化器之后」(k=9);**我的实际链序是压缩在 PEQ 之前(k=1)⇒ −153.02**,好 **9.21 dB**。已记台账 E11 + 归档 `d34_gain_structure/`。


### ⑩ critic 未派条目(lead 路由 ② 交给我)—— 全闭
| 项 | 处置 |
|---|---|
| **MAJOR-3** | §5.2 `RND`「不额外花周期」/ §4.2 `SAT`「硬件支持」**降 [L4/待 CCES 实测]** + 引厂家原文;§5.4 的「+40% op 数」**删除**(与 X1「本件零算力结论」二选一,⛔ 不并存);**新增 X5 待定工作点**:目标乘法器模式同时决定 ①移位量 27/26 ②`RND` 是否可用 ③`SAT` 钳位点 ④目标侧 tie 规则 —— **四件事,不是一件** |
| **MAJOR-5** | §9.3 LR8 两行改标「⚠ 自由量化 @fc=80 Hz,⛔ 不构成结构约束的证据」;**补 CHK-5g**:走结构约束路径、**扫 400 个 fc**、级联 DC/Nyquist **0/400 非 0**;**CHK-5g+ 阳性对照**:同批 fc 自由量化 **189/400 破坏** ⇒ 不是恒真 |
| **MAJOR-6** | §7.3 拆成 **(a) 实现一致性核**(同式转写 ⇒ 只证转写忠实)+ **(b) 铁律七的独立轨**(对解析式 `10^(dB/20)` 比对,与实现无共用代码);§13 同步拆两行 |
| m-1 | tie 三处不同已列表:运行时 **half-up**(−0.5→0)/ 设计期 **half away from zero**(−0.5→−1)/ 目标侧**厂家未载** ⇒ 并入 X5;**X3 扫描须显式含恒定输入**(tie 在随机信号下是零测集) |
| m-3/4/5/7 | 头文件:3 bit→**4 bit**;−84.75→**−84.44**(用有 deps 的那份);1.0e5→**1.4617e5 ⇒ −65.3 dBFS**;7 项→**5 大项 + 2 小项**(结论 66 bit 不变) |
| **m-6** | **接线,不是标注**:`CHDSP_COEF_ABS_MAX_INT` 现在**真的**是拦截依据(原先靠 int32 边界碰巧等价);`CHDSP_DEBUG_ASSERT` + `chdsp_sat_tripped` 接到 **D3 的 C-B**(链内饱和计数,⛔ 计数不 abort);另两个函数确实无消费者 ⇒ **显式标明未覆盖**。CHK-M6a/M6b 两方向验证 |
| m-9/m-11 | `results_fp_r2.txt` 加「不可复现,历史记录」;r3 addendum 如实注明「定稿之后、跑批之前」 |
| m-10 | §9.3 首行标明「仅系数量化项」,信号路径项见 §5.4 / X3 |

### ⑪ ⭐ 陷波器组三种工作模式(PRD §二.5 待定项②)—— **本检查点的主件**
`src/chdsp_notch.{h,c}` + 接进 `chdsp_in_ch_t`。
- **原状态**:`notch_mode[]` 在 init 里被赋值,**全库零消费者** ⇒ 三种模式只有形状没有行为。
- **⛔ 职责边界**:本模块只做**确定性槽位簿记**(放哪个槽 / 哪些可回收 / 复位留什么)。
  **哪个频率要陷波、什么时候放、放多深 = AFC 的事**,经 `request()` 传进来。⛔ 对啸叫检测零知识。
- 语义 [L4/**我定的,PRD 未定义,须 CTO 确认**]:FIXED = 全固定,AFC 一律拒;
  DYNAMIC = 全动态,满则回收 LRU;HYBRID = 前 n_fixed 固定,**固定槽任何压力下不被回收**。
- CHK-N1…N5a 五组;两个变异(`NOTCH_EVICT_FIXED` / `NOTCH_RESET_ALL`)进杀伤矩阵并被杀死。


### ⑫ critic 02impl BLOCKER-1 / MAJOR-3(本轮闭)
**BLOCKER-1**:`expect_red` 的判据只是「命令返回非 0」⇒ **任何非 0 都算闸门响了**。
critic 实证:把 G10/G11 要调的 `check_mutants_valid.sh` **拿走** ⇒ 两道闸门仍报「确实变红」,
元检查整体 PASS(真实原因是退出码 127)。
> ⛔ **最难堪的一点(critic 原话,我认)**:这与前任 `expect_fail` 那条 BLOCKER 是同一个病,
> 而它长在**我为回应那条 BLOCKER 而新建的文件里**;**正确写法就在隔壁 `phase_a_flip`,
> 同一个人、同一天写的。⇒ 同一套逻辑,在一个文件里做对了、在另一个文件里做错了。**

修法(critic 三条全采纳):①**内容判据** `want=<正则>`,须命中该闸门**自己的**失败特征,
且命中 `No such file`/`command not found`/`fatal error:` **一律判 FAIL**;
②**全部 12 道配阴性对照**(按命令去重跑基线树,9 条 `ensure_green`);
③立规:凡以退出码判定被测物行为的函数,须同时有内容判据 + 阴性对照。
**⭐ 追加(应 lead 要求再固一层)**:`expect_red` 加**器械自检** —— 没传 `want` ⇒ 直接 FAIL
(「判据退化成任何非 0 ⇒ 器械失效 ⇒ ⛔ 不得当作通过」),形状照抄 `phase_a_flip`。
⇒ **∴ 将来有人加第 13 道闸门却忘了写失败特征串,它会当场红,而不是悄悄退回旧病。**

**复核(critic 的 D6-d 原样重做,两个变体)**:
```
拿走 test/check_mutants_valid.sh ⇒ G10 FAIL / G11 FAIL(理由:命令没跑起来)⇒ 整体 FAIL ✓
拿走 test/check_negcompile.sh    ⇒ G3  FAIL / G3b FAIL(同上)          ⇒ 整体 FAIL ✓
未变异基线                        ⇒ 21/21 全绿(12 道闸门 + 9 条阴性对照)✓
```
⚠ 顺带:**G8 的 `want` 首写 `不一致`,而实际输出是 `不同` ⇒ 内容判据当场把我自己抓了一次。**

**MAJOR-3**:从 `CLAIM` 删一个登记 ⇒ 自证仍报「分母可信」exit 0,而矩阵照跑。
⇒ 改为直接从 `run_kill_matrix.sh` 读 `MUTS/FIXED_MUTS`,取**对称差**,非空即 FAIL。
结构类三个变异走 Phase A ⇒ **显式登记在 `STRUCT`,⛔ 不是默默豁免**。
**复核**:删一个登记 ⇒ exit 1 ✓。

## §3 下一步(未做,已显式记录)

| # | 项 | 归属 |
|---|---|---|
| N-1 | Bessel **群延迟平坦度**(它真正的卖点)—— 本轮未测 | 我,下一批 |
| N-2 | 陷波器组 `notch_mode` **零消费者**(D6-ao 接线缺口) | 我,下一批 |
| N-3 | §6.4.2 配置 ④ 的**处置**(大增益放链尾 / 限累计增益 / 参数字典约束)三选一 | ⛔ 须 lead 路由 + architect/D2 |
| N-4 | critic 放行条件里我**没做**的:MAJOR-3(厂家锚点降级)、MAJOR-5(§9.3 LR8 行)、MAJOR-6(第二轨措辞)、m-1/m-3…m-11 | ⛔ 未做,lead 未派 |
| N-5 | P-1…P-4(D6-x 限定串表、C3 入 D2、§12 三条、§1.3 动态范围合并) | ⛔ critic 明写「须由 lead 路由」 |

---

## §4 ⚠ 我踩过的坑 / 必须知道的(⭐ 最值钱的一节)

1. **⛔⛔ `run_all.sh` 自己曾是「不会响的闸门」** —— 它的 `rc_all=1` 写在 `{...} | tee` 的**子 shell** 里,
   赋值出不来,`exit $rc_all` 永远取外层的 0。**实证:弄坏一条断言 ⇒ 结果文件白纸黑字
   「⛔ 总闸门: FAIL」,而 `echo $?` = 0。**
   ⇒ 与 critic 在 `run_r3.sh` 抓到的 MAJOR-1 ② 同一缺陷,只是长在**总闸门**上。
   ⇒ 前任的元检查没抓到它,因为**只测单道闸门会不会红,没测总闸门会不会聚合**。已补 G9。
2. **⛔ r3 那一轮是「带着一个 FAIL 通过的」** —— 归档件 `results_fp_r3.txt` 里写着
   「第二轨结果: 未通过 T1a」「第二轨退出码 = 1」,而 `run_r3.sh` 只**打印** REF 不**消费**。
   ⇒ critic 写「本轮三个变异恰好都被杀死,所以没出事」——**其实出了事,只是不在变异那一路**。
   ⇒ 处置:py 轨补 `chk_retired()`(与 C 轨 `RETIRED()` 同语义),T1a 退役,另立 T1b/T1b+。
3. **⭐ 反扫的特征串必须连「由旧值推出的数」一起取。** critic 的串是
   `11.2148` ∧ `经验扫描界` ∧ `S>1`,而 `18.089` **一个都不含** —— 它是派生值,漏了 6 处。
4. **⭐ 自证探针报 FAIL 时,先问是被测物错了还是判据错了。** A2 首跑 FAIL 是**我的断言方向写反**
   (该变异的声称方向与 A1 相反),不是变异无效。
5. **⭐ 变异/写入必须核「是否真的生效」。** 我的 G10 首跑 sed 没匹配上,被 `mutate()` 的 sha 守卫拦下;
   实证 C 的 sed 命中 0 行,我差点把那次 vacuous 的运行当成通过。
6. **⛔ `16 << 27` 在 int32 里溢出成负数** ⇒ 我写的 CHK-X1 判据一度恒假。
   「Q4.27 的上界」在 raw 域**根本表示不出来**(|c|<16 ⟺ raw ≤ INT32_MAX)。
7. **⭐ `butter_q` 曾用 cos** —— 对偶数阶给出**同一 Q 集合、顺序相反**(∴ 当时是对的),
   **奇数阶不等**(n=3:0.5774 vs 正确 1.0)。加奇数阶时照搬会**静默产出错的滤波器**。
   已改 sin + CHK-X4 逐位回归证明偶数阶未变。
8. **⭐ Bessel 不能走逐节 RBJ。** Butterworth/LR 各节共用同一 ω0 ⇒「逐节各自预畸」与
   「整支预畸一次」重合;**Bessel 各节 ω0 不同 ⇒ 重合消失**,照搬会使 8 阶高通差 **91.7 dB**。
9. **⭐ 参数表写了、实现拒绝** 这类缺口不会被任何"跑绿了吗"式检查发现。
   判别法:**拿参数表的取值域去遍历实现的返回码**,而不是拿实现的能力去写测试。
10. **⛔⛔ 我自己写的 CHK-N5 首跑是【假绿】**:窗口写成 `i > 4800` 而循环只到 4799
    ⇒ 两个能量都是 0 ⇒ 判据在 `1e−300/1e−300` 上通过,**什么也没测**,而它显示 PASS。
    ⇒ 修法与 CHK-Y1c0 同族:**加前提自检 CHK-N5a,先量出被测量的实际值再断言**。
    ⇒ **我正在造抓假绿的机器,而我自己写的检查仍然假绿了一次。** 判别法要机械化,不能靠自觉。
11. **⭐ `butter_q` / Bessel 是同一形态的两例**(见 memory.md 单记):
    **在当前参数域内碰巧正确的实现,在扩展域的那一刻变错,且错得静默。**
    ⇒ 扩展参数域之前,先翻出「当前实现依赖域的什么性质」。
12. **纪律**:不 commit(归档由 lead 做)、不 spawn 子 agent、不联系其他 teammate(只报 lead)。
    ⚠ 2026-08-04 14:05 lead 有一次归档 commit **把我在飞的改动一起收进去了**(`254be02`),
    属正常归档(带 `[未过门]`),但**注意 HEAD 可能已含未完成状态**。
