# VERIFICATION 交接件(活文件 · 随进展更新)

> **门禁状态:本文件不是 verdict,不构成任何放行。本文件是交接件,不得被下游引用为结论。**
> owner: `verification` 第 1 实例 @ claude-opus-5[1m] / 2026-08-04
> 首次上岗 2026-08-04 ~15:1x。全部产出 [L2/宿主仿真复核],⛔ 无一条 L1。

## 0. 当前断点(⛔ 恢复工作先读这行)
- **在手工单**:`r76`(收口件头条表出处)与 `F71`(§4 天花板论证依据)的**原始数据独立复核**。
  派单人 = lead。缘由 = 独立 critic 在 W1P 收口件 verdict 明确声明「未回原始数据,不为这两处背书」。
- **第一件工单(复核)已收口**:① r76(§3)② F71(§5),两件都已报 lead 并由 lead 归档推送。
- **⇒ 当前在手 = 第二件工单:D14(lead 2026-08-04 派)**,顺序 = ①素材库规格 → ②指标验收表 → ③bring-up 清单。
  - ①**已出件**:`01_design/D14_MATERIAL_SPEC_v0.1.md`(未过门)。已报 lead。
    ⚠ 该件引入**外部输入 7 条(X1–X7)**,全 [L4/网页转述],**一件原文未取到**
    ⇒ 铁律六入库未完成,已在件内 §7 列台账并上报 lead 路由。
  - ②**已出件**:`01_design/D14_ACCEPTANCE_TABLE_v0.1.md`(未过门)。已报 lead。
  - ③**已出件**:`01_design/D14_BRINGUP_TRIAGE_v0.1.md`(未过门)。已报 lead。
    ⛔ 它**不是**上板清单 —— 上板清单已存在(ITC 项目搬来),本件是它的**门禁核对**,结论 = 闸门未解除。
- **第三件工单(lead 派):回审 W1-C 微基准 —— 已出件两份**
  - ①`01_design/W1C_REAUDIT_v0.1.md`  ②`01_design/W1C_MEASUREMENT_PLAN_v0.2.md`(均未过门)
  - ⛔ **最重的一条**:`t1a_coeffs` 从未初始化 ⇒ 全零 ⇒ T1a 全 36 行按其**自身**判读规则不可采信;
    而 T1a 正是 DEC-0009/0014 的主口径点。**该件是本角色前一实例(08-01)自己写的。**
  - ⛔ **第二重**:T1a 调的是 `<filter.h>` 的 **float** `biquad()` ⇒ 在 DEC-0023 的新性质
    (取得第一个**定点**锚点)下**测的是另一条通路** ⇒ 已改判为「尺子标定」,并新增 T1a-fx。
  - **增补件 A1**(2026-08-05):`01_design/W1C_ADDENDUM_A1_datatype_census.md`(未过门)
    ⛔⛔ **我自己上一轮那句「T1b/T2/T3 本来就是定点/标量」错了 —— T3 是【浮点】内核**
    (struct 成员 float / 谱数组 float / 门限全 f 字面量 / `sqrtf`×1 `log10f`×3 / `math.h`)
    ⇒ 映到 DEC-0009 三档(⚠ **该行已被 2026-08-05 V-9 裁决翻转,现状见下方 A3 行**):
    ~~T1 档(经 T1b)与 T2 档拿得到定点锚点;T3 档拿不到~~
    ⇒ 而 T3 档正是 adaptive-dsp 的 **20k cyc/槽 包络**所在
    ⚠ **2026-08-05 按 critic MAJOR-1 更正**:~~唯一被声明"可能超"的资源约束~~ —— 那是**我自己的推论**。
    正确:算法侧在规划口径(2.0)上自报**合规**(余量 ~2.4–3×),并另行声明「若口径升到 8.51 量级则超包络」
    (`adaptive-dsp/memory.md:92` 与 `:185`);**属于我的只有那个越线点数值,且⛔ 未经算法侧确认**。
    ⚠ 而该越线点我自查出**用了已陈旧的两点**:原报 7.4 ⇒ **改 ≈5.2(区间 4.9–5.7)**,详见 A1 §1 修正框。
    ⚠ v0.1/v0.2 **未改**(冻结在 tag `review/w1c-reaudit-r1`=`32d0304` 等 critic),更正清单在 A1 §6
  - **增补件 A2**(2026-08-05):`01_design/W1C_ADDENDUM_A2_v9branch.md`(未过门)
    `V9_BRANCH` 列规格(**由类型推出,不由人填**)+ 跨档禁止推断条款 + T3 档现状登记。
    ⛔ A1 已冻在 `review/w1c-addendum-a1`=`5c10279` ⇒ A2 只增不改。
    ⚠ **A2 §3 是自审**:V9 列本身**什么也不阻止** ⇒ 只加列 = 复刻我自己抓到的 B-2 缺陷
    ⇒ 必须与【消费动作】一起交付(V-9 裁决时逐行扫 CSV + 标 VOID + 铁律五反扫)⇒ 归 lead 路由。
  - **增补件 A3**(2026-08-05):`01_design/W1C_ADDENDUM_A3_v9_consumption.md`(未过门)
    **V-9 已裁 = CTO 批准**(旁链准用 float32)⇒ **A2 那列的第一次被消费**。
    ⭐⭐ **裁决没有闭合缺口,它把缺口从 T3 档搬到了 T2 档**:
    ```
    T1 档 ✅ 不变(音频路,V-9 不管) ｜ T3 档 ✅ 获得(旁链既已合法浮点,T3 现状即活支锚点)
    T2 档 ⛔ 失去(其 Q31 定点 FFT 成落败支;活支 = float FFT,无件在测)
    ```
    ⇒ 新缺口**可以立刻排件**(不再是押注);旧缺口不能 —— 这才是裁决带来的进展。
    ⇒ A1/A2 的相关句**已在原处加撤回标注,原文一字未改**(LESSONS E-2)。
    ⚠ A1/A2 因此与其 review tag 不再逐字节相同(差异仅为纯增补的标注框),**已知会 lead**。
  - **增补件 A4**(2026-08-05):`01_design/W1C_ADDENDUM_A4_float_fft_kernel.md`(未过门)
    浮点 FFT 内核(T2 档活支)设计。**两个角色分开**:A=活支锚点(⛔ 无达标线)/ B=V-9 ②号理由实验臂(有判据)。
    **三臂**:A 现有 T2(定点+定标)/ B float+保留定标(只差数制)/ C float 无定标(将来要造的那个)。
    ⚠⚠ **G6 原生 MAC 闸**:现有 T2 的 Q31 乘是**可移植 C 的 64 位中间积**,未必映射到 SHARC 原生定点 MAC
    ⇒ 若被仿真拖慢,②号理由会**看起来被证实**,而那正是**对既有决定有利**的方向 ⇒ 前置反汇编闸,不过则②不出判定。
    ⚠ 「+10–20%」那个估计**本方案测不了**(现有 T2 不是块浮点)⇒ **lead 已批:永久 [L4],
    本项目【决定】不予验证(非"尚未验证")**,措辞见 A5 §1.3。
  - **增补件 A5**(2026-08-05):`01_design/W1C_ADDENDUM_A5_AB_protocol.md`(未过门)
    臂 A/B 执行协议 + **G6 的具体做法**(⛔ 不认指令名,改比两臂内层蝶形的**指令条数**,阈值 1.5)。
    ⭐ 跑序第 5 步的分岔:**G6 不过只废掉②的判定,⛔ 不废掉周期数本身** —— 那是角色 A 的数。
    ⚠⚠ **A5 §1.2 新发现**:`W1A:207` 已把 +10–20% **乘成了具体数**(+4,019~8,037 cyc/帧
    ⇒ 合计 11.99%~12.29%),而 V-9 已批 ⇒ **那一行是落败支行**。
    ⇒ 必须同时做两件:①标"落败支,不得代入" ②标"决定不验证"。**只做②比留白更危险。⇒ 归 lead。**
    ⭐ **A5 §0.5(2026-08-05 增补,lead 点名)**:写死「本微基准的精度**已不是**"越线点落在哪一侧"
    的限制因素」。⇒ 而我把该规则**回头用在本设计自己身上,命中三处不是一处**:
    ①越线点估计只知到 ±0.4(归算法侧)②G6 不过时 ② 号理由无论多精都不可判 ③G6 阈值 1.5 是我拍的。
    ⇒ ⛔ 三条**没有一条**是"测得不够准"。⚠ 而 v0.2 §5c 的 R=8 与半宽约束**仍要执行**
    ——它服务的是**锚点值自身的可信区间**(回填 D13),只是不再服务"落在哪一侧"。
  - **冻结点**:A1/A2/A3 已由 lead 重打 tag `review/w1c-addenda-r2` = `facdbd1`(旧 `5c10279` 保留为留痕)。
    ⭐ lead 立规矩:**冻结件在评审窗口内可加撤回标注 —— 条件:①只增不改原文 ②当场知会 lead ③由 lead 重打 tag。**
  - ⭐ **软处登记表**(2026-08-05):`01_design/W1C_SOFTSPOT_REGISTER.md`(未过门)
    横跨 v0.1/v0.2/A1–A5 的**全部自标软处**,共 16 条开口 + 3 条已闭,按**谁能解**分组
    (算法侧/架构侧/首跑/CCES手册/决定不解/板级空槽)⇒ 它同时是一张**路由单**。
    ⭐ 补了原件没有的一列:**「它掉了会怎样」** —— 因为限定之所以会掉,是下游看不出后果。
    ⚠ **本表固有盲区**:它只抓「我当初标过的」;「该标而未标的」归 critic。⛔ 不得读作"软处只有这些"。
  - ⛔⛔ **增补件 A6**(2026-08-05):`01_design/W1C_ADDENDUM_A6_T3_direct.md`(未过门)
    **T3 档改为直接量 `cyc/槽`,与 20k 一步比较** —— 算法侧拆墙,拆法 = **换测量对象**。
    ```
    我的"两点内插"算术上**等价于 20000/N_op** —— 是【除法】不是【内插】(两端点同在一条过原点射线上)
    ±0.4 = 手算 op 区间 ±8% 被除法搬过来,**不是测量不确定度**
    ⭐ 而 cyc/MAC 对 T3 **是错的单位** ⇒ 套用 8.51 **正是 A2 §4 我自己写下的禁令所禁的事**
    ⇒ 形态 =「规则在库里,而没被接到动作上」—— 今天第二次由我自己复现
    ```
    ⇒ 新判据 + **重做的两极值代入**;⭐ 第三次代入这次**有对象**:T3 内核少建模三个生产函数
    ⇒ 实测**天然偏低 ⇒ 天然偏向"合规"** ⇒ 立 **G7 覆盖声明闸**(判定一律写「合规(仅对子集)」)。
    ⇒ 旧的 `min(半档距, 越线点不确定度)` 约束**整条作废**(已就地标);R=8 重复**保留**。
    ⇒ 「精度不是限制因素」那句**已收窄**:新问法下若实测落 20k 邻域,精度**重新**是限制因素。
    ✅ 三条撤回已按 `RETRACTED_STRINGS.txt` 规矩③(每次撤回当场追加)**登记入常设反扫表**。
- **撤回传播(2026-08-05)**:「卡在仿真器连不上」= lead 自认捏造的无证据句。
  全库反扫已做,结果记 `D14_BRINGUP_TRIAGE_v0.1.md` §8。
  ⛔ **真传播 1 处在台账**:`decisions_log.md:527`(DEC-0023 风险声明②)⇒ **归 lead,我不改台账**。
  ✅ 我自己那处(D14-② 仪器表)已改:拆成**两行两阻塞**(有没有板 / 有没有编译器),解法不同不得合写。

## ⛔⛔ 0b. 本实例名号存疑(2026-08-04,我自己查出,待 lead 定名)
```
派单说「verification 首次上岗、从未 spawn 过」⇒ **与盘面不符**
`04_platform/W1C_microbench/PROVENANCE.md` 首行:「产出方:verification teammate(首次上岗)· 2026-08-01」
⇒ 2026-08-01 已有过一个 verification 实例,产出 W1-C 微基准 CCES 工程 + README_WINDOWS + PROVENANCE
⇒ 而 `W1_HANDOFF §0` 实例名单表里【没有 verification 这一行】(既非现役也非退役)
⇒ ∴ 本文件里所有「第 1 实例」字样**可能有误**,已上报 lead,待定名后统一改
⚠ 对接手者:**先读 `04_platform/W1C_microbench/PROVENANCE.md`**,那是本角色的前序产出
```

## 1. 纪律(本实例自持)
- ⛔ 不 commit / 不 spawn 子 agent / 不联系其他 teammate(只报 lead)
- ✅ 预注册 `01_design/verification/PREREG_V1_r76.txt`(**跑前落盘,§1–§6 未改**)
- ✅ 每条证伪条件做双向代入自查;✅ 每个数挂 L 标;⛔ 不替任何人圆

## 2. 已完成
| 件 | 路径 | 说明 |
|---|---|---|
| 预注册 V1 | `01_design/verification/PREREG_V1_r76.txt` | 跑前落盘,12 条检查 V-A…V-L |
| 复核脚本 A | scratchpad `v1_recheck_A.py` | V-A/B/C/D/E:只读 json/txt,不 import 分析层 |
| 复核脚本 B | scratchpad `v1_recheck_B.py` | V-C 修正容差 / V-E 精确 / **V-F 臂O 频域第二路径** / f_trig 归属 |
| 复核脚本 C | scratchpad `v1_recheck_C.py` | **直接观测**:picks 复现 / 臂O 量化关系 / (0.5,1) 单点观测 / V-G |
| 输出 | scratchpad `v1_C_out.txt` | 脚本 C 的原始输出 |

scratchpad = `/tmp/claude-1000/-home-it1234-processor/530be877-5ec0-4df7-ae7b-ed9cade0a0b7/scratchpad/`

## 3. r76 复核结论(2026-08-04 ~16:0x)

### 3.1 ✅ 数本身:全部相符,无一格对不上
- **V-A** 从 `r76_cell_s20f0.json` 原始 `m` 字段自减,逐格重算 dN/dNa/dO ⇒ **与收口件 §2 逐格相同**(6 行 × 4 列)
- **V-B** 中位 / ≥4 / ≥5 / 分层中位 / 分层计数 **16 项全符**
- **V-C** json ↔ out.txt 双路,逐字段 == `round(json,2)`,**不一致字段 = 0**
- **V-D** 四臂 `status` 全 `ok`;INV-N / INV-Na / INV-O **6/6 全 OK**;无量程截断
- **P-7** 收口件脚注「r87 独立重跑 6/6 四项全符」= **属实**,m0/ΔMSG/挂陷/终点 RMS **逐位相同**
- **C1** picks 我独立重算,6/6 逐点相同;`anchor` 逐格 **差 0.00e+00**

### 3.2 ⛔ MAJOR-1 ·「≥」这个限定的论证不成立(**数没错,限定错**)
```
收口件 §1/§2 给每一格加「≥」,依据 = F76.3/r83「m0 被高估 ⇒ 差值被低估,量级恰好一格 0.50」
```
三条独立证据反对该推理:
1. **m0 ≡ anchor,逐位相同(差 = 0.000e+00),6/6 种子 × 两个 T_OBS 全部**。
   `anchor` = 解析临界增益(MSGMeter 全带)。m0 臂 `proc=None` ⇒ 严格 LTI ⇒ **真值 = anchor**。
   ⇒ **m0 的偏差经解析基准量得 = 0**。在 G=anchor 上「仍在增长」正是极点落在单位圆上的
   预期签名,**不是**「读高了」的证据。
2. **r83/F76.3 那把尺子的可能输出集 = {0.50, 无法给数}** —— 其下限 = 一个扫描步长。
   m0 ≡ anchor ⇒ 该处必增长;anchor−0.5 处 |GF|=0.944 ⇒ 多数格 12 s 内趋稳
   ⇒ **该程序只能返回 0.50**。⇒「恰好一个扫描步长」不是巧合,是**它返回了自己的下限**
   ⇒ 分辨力之下的不等号(LESSONS C-3)。
3. **同一份数据里有反向实证**:臂 O 是 LTI(神谕 8 陷波钉死、T_low=999、INV-O 6/6 OK)
   ⇒ 有解析真值。**实测 dO 在 2/6 格【高于】解析真值**:0.5/1 高 **+0.169 dB**、0.5/2 高 **+0.019 dB**
   ⇒ 「≥」在那两格**被实测违反**。
   **直接观测**(0.5,1):G=anchor+6.5 解析已失稳 0.169 dB、末−首 RMS **+7.25 dB**,
   而 `is_howling = False`;到 +7.0 才响(末−首 +80.08)⇒ **检测器会漏掉小超量失稳**
   ⇒ 偏置方向是**把陷波臂读高**,即 ΔMSG 可能被**高估**。
```
⇒ 建议改写:「±0.50 dB(一格),方向未定」 ⛔ 不是「≥」
⇒ 为什么要紧:CTO 要判的是「4–5 dB 够不够得着」。「≥」把 2.00–6.00 读成"真值只会更好";
  而实际是"真值可能更差"。r76 自己的预注册 Hr1 立的规矩(证据门槛不得随结论方向浮动)在此未被执行。
```
⚠ **我预注册的 V-G(对称性检验)【未触发】,如实报**:F76.3 的方法原样施于陷波臂 Na,
   Na 在其自身终点上末−首 = **−3.02 … −36.74 dB**(强衰减)⇒ 该法对 Na 返回 0.00。
   **但两臂的这个量不是同一个量**:Na 是时变臂,末−首量的是 **NHS 收敛**不是极点半径
   (−36.74 dB 不可能来自稳定裕度)⇒ 属 LESSONS B-3「跨来源对账先核量的类别」。
   ⇒ ∴ 该对照**不能**用来支持「只有 m0 有偏置」。

### 3.3 ⚠ minor · 四条(不翻结论,但改读法)
```
m-1 m0 不是一个独立测量:它逐位 == 解析 anchor ⇒ ΔMSG 实为
    「**实测陷波臂终点 − 解析无陷波 MSG**」的混合量,不是收口件 §1 写的「两臂实测之差」。
    ⇒ 正面后果:该恒等式是一次**没被报过的台架验证**(时域检测器 vs 频域解析,6/6 逐位符)
    ⇒ 负面后果:「无陷波臂」在数值上不贡献信息,§1 的措辞会让人以为它是独立读数
m-2 仪器底 0.354 = STEP/2×√2 **算术对**(=0.35355),而**推导式与实际量化结构不符**:
    该式设两臂各有 ±半步独立误差;而 m0 侧量化误差**恒为 0**(m-1)⇒ 只有陷波臂量化
    ⇒ 真实分辨力 = **整格 0.50 dB(单边)**,不是 0.354。
    ✅ 对头条表**不承重**:六格值与缺口列全部是 0.5 的整数倍 ⇒ 门限取 (0,0.5) 内任意值判定不变
m-3 §2「起振频率 f_trig」列**未标是哪个臂**,而四臂差异极大
    (0.2/2:m0=5379 / N=5380 / **Na=3111** / O=503 Hz)。实为 **Na 臂**(6/6 匹配)
    ⚠ 另附一条值得看的:**臂 O 的 f_trig 全部落在 22–503 Hz**(186/365/503/52/22/170)
      ⇒ 神谕臂把前 8 峰压掉后,失稳点整体退到 NHS 检测带下沿 120 Hz 附近或之下(F35 同族)
m-4 §2 **未标频带**。实测量本身是全带量(检测器是宽带 RMS),而 `anchor` 取 `['full']` ⇒ 全带。
    但同一算例的**带内**解析 ΔMSG 在 0.5/0 上是 **4.691 vs 全带 3.198(差 1.49 dB)**
    ⇒ 与该件 §3 与作者 memory 自设的「凡报 MSG/ΔMSG 必须写明频带、两列都给」纪律相抵
```

### 3.4 ✅ 一条新的正面证据(此前没人做过)
**V-F:臂 O(上界列)的频域解析交叉核** —— 臂 O 是 LTI ⇒ 可解析预测。
```
解析 dO(全带)  6.026 / 6.033 / 7.731 / 3.198 / 6.331 / 4.481
实测 dO         6.00  / 6.00  / 7.50  / 3.00  / 6.50  / 4.50
|差| 中位 = 0.101 dB,最大 0.231 dB(< 半格 0.25)
4/6 格 实测 == 0.5×floor(解析/0.5),= 完美检测器 + 栅格向下取整的预期形态
```
⇒ **头条表的「臂 O」一列通过了一条与原路径完全不同的验证**(时域闭环扫描 vs 频域临界点解析)。
⇒ 这是铁律七要求的第二独立工具交叉核,此前 r76 线**没有做过**。

### 3.5 被测件代码状态(V-I)
```
✅ clrig.py / nhs.py 在 r76 跑完后都改过,而**默认路径逐位无影响**,且这是**实测确认的**不是读 diff:
   r87(13:40,改后的 nhs.py)重跑 r76 的主列 ⇒ 与 r76(00:32,改前)**逐位相同**(P-7)
⚠ PREREG_r76 钉的 `clrig.py@8ad47ce8d260dd18` 与盘上现值 `ed46450303b0423c` **不同**
   (00:43 那次 `delay_ms`→`prop_delay_ms` 改名,数值恒等;git diff 已核)
   ⇒ 哈希已失效但行为未变 ⇒ **建议把失效留痕补进 PREREG_r76 的跑后追加节,别静默**
⚠ `nhs.py` 用 `__version__="P1.0"` 做版本钉 ⇒ **改了不换号** ⇒ 版本钉不具备识别力
```

### 3.6 登记不追(LESSONS B-1:不改变任何决定)
```
F76.3 把 2/6「未找到」归因于「主峰未被压 ⇒ 时间常数极长」。
我逐位复现了那两格(0.2/1 +3.22、0.2/2 +3.16 dB),⛔ 但**未验证该归因**;
而退 6 dB 时环路增益已降到 0.50(衰减很快),与"时间常数极长"不直觉相符。
⇒ 登记为**未查明**;那两格已被 F76.3 自己判为"无分辨力",不改变任何决定 ⇒ 不追。
```

## 5. F71 复核结论(2026-08-04 ~16:5x)
预注册 `01_design/verification/PREREG_V2_F71.txt`(跑前落盘);脚本 scratchpad `v2_f71{,b,c}.py`

### 5.1 ✅ 复现的(我从零重建了 F71 的计算 —— 因为它没有配套件)
```
临界点数 234/233/223/465/475/454   ← h_eff(band_limit(h0,8k)) + 带 100–8000,逐格精确
top1−top9 = 2.11/3.15/2.49/1.97/2.94/1.92                        逐格精确
Q-6 三档 1.92–3.15 / 2.51–4.18 / 3.21–5.26                        三档区间精确
假说B 误差中位 1.57 dB                                             精确
dB 域 σ 5.33–5.71 / 均值 5.58(vs Rayleigh 5.57 差 0.01)           精确(全频点 @120–7800)
实测列 = r79 `dO` @ NN=8                                          逐格精确
⭐ 核心结论(ΔMSG 是顶部峰间的序统计量、不是峰-均比;加 NN 会饱和)—— **成立**,
   且被我的第二条独立路径(频域解析真值)与 r79 实测臂O **双双支持**
```

### 5.2 ⛔ MAJOR-2 ·「top」是**裸临界点序统计量,不去重** —— 与它自己的定义句不符
```
定义句:「top1 − top(NN+1) ← 完美陷掉前 NN 个峰后,第 NN+1 个【峰】接管」
实际  :`mdb` 降序排序取第 1 与第 9 个 —— **临界点**,不是**峰**,无排除窗
⇒ 223–475 个临界点里,第 9 大极可能是前 8 个峰的邻点 ⇒ 它本身就在陷波带内、已被压掉
⇒ 它不可能"接管" ⇒ 该量测的不是它命名的那件事
```
换成物理正确的排除窗构造(排除窗 = 陷波带宽,与分配同源):
```
              NN=8    NN=16   NN=24   (等预算 bw=1/NN,= F69/r79 口径,中位)
裸序统计(F71) 2.30 →  3.45 →  4.19    ← 仍在涨,**看不出饱和**
排除窗        3.86 →  4.74 →  5.22
解析真值      4.33 →  5.02 →  4.94    ← 16→24 **−0.07 = 真的饱和/回落**
r79 实测臂O   4.00 →  5.00 →  4.75    ← 与解析真值符合到 0.2–0.3 dB
|构造−解析真值| 中位:裸序统计 1.89 / 1.48 / 1.04  ⇔  排除窗 0.18 / 0.31 / 0.55
```
```
⇒ ① 饱和结论【成立,而且更正后的构造把它做实了】—— F71 自己那条反而看不出饱和
⇒ ② 而 §4 引的三档数【系统性偏低 1.0–1.9 dB】,方向 = 把天花板画得比真实更低
⇒ ③ 且与同一份件 §2.3 自己报的神谕臂中位(4.00/5.00/4.75)对不上 1.5–2 dB
     —— 而 §4 明写那三档是用来解析地解释 §2.3 那条饱和曲线的 ⇒ 更正后两者才对上
⇒ ④ **神谕臂 NN=16 的天花板 ≈5.0 dB 中位,落在 4–5 dB 目标带【内】,不在其下**
     ⇒ §4 结论句的准确形式:差距不在"数学形状把天花板压在 2–3 dB",
       而在**选点**(神谕 5.00 vs 自选 2.75)—— 这与 §4 自己的「剩下的唯一杠杆:选点」一致,
       **但与它引的那三档数不一致**
```

### 5.3 ⚠ 其余五条
```
m-5 **F71 没有 `件:` 行** —— 邻近 F69/F70/F72/F73/F74/F75/F76 全都有。
    收口件 §4 的全部依据在库内无可溯原始件(我已重建,除峰-均比外全部复现)
m-6 **峰-均比 9.70–11.58 / 中位 10.39 复现不出来**:我试了 2 带 × 4 nfft × 3 个 h 口径
    × 2 种均值约定(dB 域均值 / 线性均值)× 2 种取样集(全频点 / 仅临界点)= 48 组,
    六格最大偏差全部 >0.6 dB;最接近的(全频点@120–7800)给 10.73–12.53。
    ⇒ 该列与由它算出的 **6.88** 无法复核。⚠ **不改变结论**(假说A 被拒的裕度 6–8 dB)
    ✅ 而同段的 σ(5.33–5.71/5.58)与「复现到 0.01 dB」那句 **是可复现的、成立的**
m-7 **判别的工作点 = bw 1/8**(r79 N08),而 §4 印在 bw=1/5 的 §2 头条表之下,**未声明**。
    换到 §2 自己的工作点重做:假说B 误差 1.57 → **3.20**,判别裕度 4.4× → **1.9×**
m-8 **同一小节两个频带**:σ 那行只在 120–7800 复现,临界点数/top1−top9 只在 100–8000 复现,
    而小节标题只写了「120–7800 Hz」(LESSONS B-4)
m-9 **2/6 条种子上,8 个陷波后真正接管的临界点在 52.2 / 22.3 Hz**,落在序统计量的取值带
    (100–8000)之外 ⇒ 那两条上算的根本不是接管者。
    ⚠ 该缺陷方向:它让真值更低,**与 §4 结论同向**(不是有利于我方的错)
```

### 5.4 ⚠ 我自己的一处错(如实留痕)
第一遍我把 V2-C / V2-H 跑在 120–7800 上,据此把 Q-6 三档判为 ⛔ 不符。
**复现 F71 的带是 100–8000**;改带后三档逐格精确相符。⇒ 那次 ⛔ 是我的带口径错,不是它的。

## 6. 下一步(接手者从这里做)
- [ ] §3.5 的两条代码状态建议(PREREG_r76 哈希失效留痕 / nhs.py 版本钉无识别力),归 lead 路由
- [ ] §5.2 的构造更正若被采纳,§4 那三档数与 §2.3 的对账需要作者侧回填
- [ ] 本角色本职未动:D14 测试方案 / 指标验收表 / 素材库 / bring-up 清单 —— 一件未起

## 4. 乙类件索引盘点(2026-08-05,本实例最后一件)
件:`01_design/W1C_CLASSB_INDEX_AUDIT.md`(未过门)
```
⛔ lead 预设的结论「绝大多数乙类件没有索引」= **数据不支持**:86 份里无甲类指向者 **12 份 = 14%**
⚠ 而我第一版扫出 33 份(38%),差点那么报 —— 33→12 的两次收敛**都是我自己的方法错**:
   ① 分类按目录做 ⇒ 误扫 CRITIC_HANDOFF_current.md(它自述"活文件"= 甲类)⇒ 判别式是语义的
   ② 只按文件名/哈希匹配 ⇒ 漏掉人读形式的指向;抽查 3 份"零引用",**2 份是假阳性**
⇒ ⭐ 本轮第二产出比第一个重要:**机械扫描在"缺失类"问题上假阳性率高(抽 3 中 2)**
   ⇒ 与常设反扫表头同一条:机械信号负责检出,打开来看负责归因 ⇒ 本表出的是**候选**不是结论
⛔ lead 猜的盲区(「抓不到索引本身也是乙类」)**证伪** —— 我显式分了这一档,12 份里有 4 份
✅ 真盲区三条:①缺失类断言不可靠(12 是上界)②有指向≠是个能写撤回的地方 ③甲乙分类只能语义判
⭐ §4 一条更值的观察:高分量只有 3 份,而机制是**「数被搬走了,而件被留下了」**
   ⇒ 静默失效的确切形态不是"整份件失效没人知道",是**"件里的某个数失效了,而件本身看不出来"**
   ⇒ 而今天实际发生的六次全是后者
```

## 5. 软处表维护(2026-08-05,本表触发器首次被触发)
```
本表 §3③ 写着「每次有 DEC 落盘 / 架构侧或算法侧回话 ⇒ 回扫本表」
⇒ 算法侧 08-05 回话(拆掉越线点那堵墙)⇒ 我回扫,三处变更:
  S-1 越线点 5.24 ⇒ ⛔ **作废,转 S-C4** —— 不是"确认了",是【该量不该被求】
  S-17(新) `N_op` 从未实测 ⇒ ⭐ **新做法是【绕过】它不是【闭合】它**
            ⛔ 否则日后有人以为 N_op 已解决(lead 点名要求登记)
  S-18(新) G7 只挡措辞,挡不住"少算多少" ⇒ 代表性缺口在此升为判据的一部分,**但未消除**
⇒ ⭐ S-C4 印证了 §3③ 的必要性:它不是被做完的,是被【别处的回话】作废的 —— 不会自己通知我
```

## 6. A2–A6 自查(2026-08-05,拿 critic A1 verdict 的 MAJOR 形态回查自己)
```
Q1「承重最重的那句,出处标准是不是全件最松的一档?」⇒ **命中 1 处**
  ⛔ A2 §5:「它是全项目唯一被声明"可能超"的资源约束」= critic A1 MAJOR-1 判的**同一句**
     ⇒ 我在 A1 与 HANDOFF 改了,**A2 漏改** ⇒ 已就地补三分标注(原文保留)
  ⚠ 而同处第二个缺陷更值得记:A2 §5 整节**早该标"已由 V-9 消解"** ——
     我在 `A3 §4.2` **写下了**这条处置,**而没有在 A2 上执行它**
     ⇒ 形态 =「处置写下了,而没被接到动作上」;⭐ **两端都是我自己的件**
     ⇒ 它由本次自查抓出,⛔ 不是我做 A3 传播时抓出的

Q2「从别处搬来的每个数,写没写来源件?」⇒ **5 份中 2 份漏**
  ⛔ A6:3.5–4.1k / 7.0–8.2k / 30–35k **只给数不给来源** ⇒ 已补 submissions/W1B_v1.4_* §8.2.1
  ⛔ A4:+10–20% 只写"架构侧驳回支原文" ⇒ 已补 submissions/W1A_v0.6_* §3.2
  ⇒ ⭐ 即:我昨天提、lead 今天立的那条纪律(搬数须写明来源件),**在我自己五份件上 2/5 不合格**
```
**⇒ 对 critic「作者查内部 / critic 查关系」那条理论的实测结论,见给 lead 的回复(§本轮消息)。**

## 7. critic r6 FAIL 的整改(2026-08-05)
```
verdict: 01_design/verdicts/critic_W1C_ADDENDA_r6_verdict_20260805.md = FAIL(0B/2M/1m)
⭐ 三条同一形状:【一个数在它被发现的地方撤回了,而在它已经传到的地方还活着】

MAJOR-1「7.4 作判据」⇒ **全族反扫命中 3 处,而 critic 只列了 2**
  ✅ PLAN:163   决策规则 —— 最急(5.24 < 7.4 ⇒ 降档**更早**触发
                ⇒ 按原文执行者会【晚于实际】才认为越线 = 对我方不利的那一侧,今日第六次)
  ✅ REAUDIT:207 **同一条决策规则的另一份拷贝 —— critic 未列,我反扫抓到**
  ✅ REAUDIT:303 L 标汇总行
  ✅ 作用域:REAUDIT 作废框由「本节全部」改为「**全文件 + 全族**」(critic 判它写窄了)
MAJOR-2「唯一被声明"可能超"」⇒ 命中 1 处:A2:198 **句子本体**已划
  ⚠ 此前只有节首框,句子本身未划 ⇒ 框在块外、句在块内,读者读到的是句子
minor  A1 §6 的 E-1/E-4 未并入 ⇒ critic 判**球在 lead**(路由未完成),我不动
新增   软处表 §1b **全族反扫台账**(串 | 反扫日期 | 命中数 | 残留处置)—— critic r6 立的规矩
✅ 修法后复扫:两串在甲类件内**均无未划线的活实例**(仅剩台账自身的记述,合法)
```
