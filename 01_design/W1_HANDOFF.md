# W1 工单交接件(AFC/NHS 架构+算法)— 2026-07-31 会话中断点

> ## ⭐ 当前入口(**2026-08-05 更新**)
> **① 现役实例名单 ⇒ `## 0.` 节内的「⭐⭐ 现役实例名单 —— 唯一权威表」。**
> **⛔ ⚠ 而 `## 5.9` / `:641` / `:663` / `:799` 那几张表全部是【沿革记录】,⛔ 不得据以判断现役。**
> **⇒ ⚠⚠ 本行本身就是那个病的实例:2026-08-05 之前,本入口指的是 `## 5.9`,而权威表当天已移到 `## 0.` —— 【改了新的,没回来改指针】。⇒ 已改。**
>
> **② 撤回过的结论 ⇒ 先读 `00_governance/RETRACTED_STRINGS.txt`(常设反扫表,唯一权威)。**
> **⛔ 而它【不是门】:它只抓已被登记的撤回;一条从未被登记的过期结论,扫一万遍也不会命中。**
>
> **③ 各线断点(按线读,⛔ 不必全读):**
> ```
> W1-P / NHS 收口   → 01_design/W1P_CLOSEOUT/  (r5 = PASS-w-c 0B/0M/3m,verdict 在 verdicts/)
> W1-C / 微基准      → 01_design/W1C_*.md 全族 + W1C_SOFTSPOT_REGISTER.md(17 开口 + 4 已闭)
> D3/D4 设计 + C 实现 → 01_design/D3D4_CHANNEL_CHAIN_v0.1.md + 02_impl/(见 CHANNEL_DSP_HANDOFF.md)
> 各角色自己的交接件  → VERIFICATION_HANDOFF.md · verdicts/CRITIC_HANDOFF_current.md · CHANNEL_DSP_HANDOFF.md
> ```
> **④ 文末 `## 8.`(2026-08-03 晚)仍可作背景读 —— ⚠ 而它是 8-03 的状态,其后两天已大幅推进。**
>
> **⚠⚠ 本文件存在【重复编号】:`## 6.` 与 `## 7.` 各出现两次**(旧结构的 §0–§8 与后续追加的断点节
> 并存)。**⇒ 定位一律用【标题文字 + 日期】,不要用编号。** 按编号跳转会读到 7 月 31 日的旧节。
> (未重排编号:重排会使既有消息/memory 里"见 §6.2/§6.3"这类引用全部失效,风险大于收益。
>  ⇒ 记录该缺陷并给导航,不静默留坑。)

> **为什么有这份文件**:teammate **不跨会话持久**(团队法);三个实例(system-architect / adaptive-dsp / critic-w1)于 2026-07-31 08:50-08:52 因会话额度上限同时中止。本件是恢复工作的**唯一交底入口**——重启后按 §5 顺序读,不必回溯会话记录。
> **权威源**:各角色 `agents/<role>/memory.md`(fix-list 与教训在内)+ `decisions_log.md` DEC-0007~0010 + 本件。

---
## 0. ⭐ 当前断点(2026-08-01 11:07,第二次额度中断)—— 以本节为准,§1-§5 为 7/31 断点时状态,已大幅推进

**两个 Fable 5 实例(`system-architect`、`adaptive-dsp`)于 11:03-11:07 同时撞 Fable 5 额度上限而中止。** 二者均为 **7/31 原实例**(`-2` 实例已完成移交并退役)。

> ⚠ **恢复前必读 F-18**:额度中断 **≠** 实例死亡。额度恢复后 **`SendMessage` 向原名发送即可复活,且带完整上下文**。**不得凭"中断"推断实例已死而擅自新建**——上次正是此误判导致四实例并发裂脑(DEC-0011)。

### ⭐⭐ 现役实例名单 —— **唯一权威表**(2026-08-05 01:0x,lead 维护)
> **⛔ 本文件后文(§5.9 · :641 · :663 · :799)的一切名单表均为【沿革记录】,⛔ 不得据以判断现役。**
> **⇒ 起因:critic `ENTRY_FILES` verdict MAJOR-2 —— 同一文件曾并存 ≥3 张名单,而 CLAUDE.md 把本节定为唯一权威,照做的新实例拿到的是三天前那张(且它连 `channel-dsp` 一行都没有)。**
> **⇒ ⭐ 而本节自己在下面写过「过时的它就是 F-18 的复发入口」—— 它写下了这句话,然后成为了那个入口。**

| 实例 | 状态 | 在做什么 |
|---|---|---|
| **`channel-dsp-2`** | ✅ **现役** | `02_impl` C 实现 + critic verdict 整改(DISQ 判定顺序 → 14 条杀伤矩阵欠账) |
| **`adaptive-dsp-7`** | ✅ **现役** | W1-P 收口件 r4 整改 + 补测 Na 臂漏检幅度 |
| **`critic-w1c-2`** | ✅ **现役独立守门** | 入口文件/交接件评审已出(FAIL);下一轮 `agents/*/memory.md` |
| **`verification`**(2026-08-04 那个) | ✅ **现役** | W1C 回审 + 测量方案 v0.2;下一轮四内核定点/浮点逐字核 |
| `system-architect`(`a238950bec22a905b`) | ⏸ **休眠可唤醒** | 延迟配平 / 动态范围 / 移频器插入点已交 |

**⛔ 已退役,永不发消息(发消息 = 从 transcript 复活 = 重演 DEC-0011 四实例裂脑):**
`adaptive-dsp`(7/31 原) · `adaptive-dsp-2` · `adaptive-dsp-3` · `adaptive-dsp-4` · `adaptive-dsp-5` · `adaptive-dsp-6` · `system-architect`(7/31 原) · `system-architect-2` · `critic-w1` · `critic-w1p` · `critic-w1c` · `channel-dsp`(第 1 实例) · `verification`(2026-08-01 那个) · `sd-tool` · `rir-fixer`

> **⚠ 维护规矩(三条,前两条是血的教训):**
> 1. **实例变更后【就地改本表】,⛔ 不得在文件别处新增一张** —— 新增表不会与旧表矛盾,只会并存,而读者按"以本节为准"只读本节。
> 2. **漏一行与写错一行危害相同,而漏行更难发现** —— 它不会与任何东西矛盾(`verification` 曾漏三天,现役实例差点重造 W1-C 微基准)。
> 3. **退役是【被发现的】,不是【被计划的】** ⇒ 凡"该动而未动"超过一轮,发一条消息探活;⛔ 不从文件系统推断。

> **⚠ 三条硬规矩**
> 1. **凡标"已退役/已死"的名字,永不发消息**——发消息 = 从 transcript 复活 = 重演 DEC-0011 四实例裂脑。
> 2. **算法侧与架构侧不得直连**,一律经 lead 转。
> 3. **⚠ 名称复用陷阱(2026-08-01 由 `system-architect-2` 自己指出)**:`system-architect-2` 这个名字**曾属裂脑事故中退役的 Fable 实例**,其 `memory.md` 追加区白纸黑字写着"移交退役"——**先读 memory 的零上下文实例会误判现役架构侧已死**。现役身份以本表 + 各 memory 顶部 `## 当前状态` 块为准。**已立纪律:退役实例名不得复用**(见 team_config)。

> ⚠ **本表由 lead 维护,任何实例变更后必须当场更新。** 2026-08-01 曾出现本表写"`system-architect` 可复活、仍是现役"而台账 DEC-0015 已记其退役的矛盾,**由架构侧交底时抓获**——handoff 自称"以本节为准",过时的它就是 F-18 的复发入口。

### 盘面事实(lead 已逐条 grep 核验,非采信声明)
| 件 | 状态 | 标识 |
|---|---|---|
| **合同 IF-v1.4** | **已落盘、已核验、待接收方回执** | `contracts/…_v1.4.md`,sha256[0:16] `ee35800c0be9844c` |
| 合同 v1.0/v1.1/v1.2/v1.3 | 已加废止横幅,仅过程证据 | 不得引用其条款 |
| **W1-A** | v0.6 终合并完成,契约引用已全量切 `IF-v1.4`(×24,`IF-v1.3` ×0) | 盘面工作副本,**未快照** |
| **W1-B** | **仍是 v1.1,未做合并轮**:引用 `IF-v1.0 ×15 / v1.0a ×10 / v1.1 ×17`,C10-C13 十一处挂"(在议)" | **不可送审** |
| 回执 | **无**(算法侧在响应放行消息途中中断) | — |

### 恢复后执行序(严格按序,前一步未闭不得进下一步)
1. **复活 `adaptive-dsp`** → 出**唯一正式回执**(七项逐项:C13 两箍 / C11 T_low 注记 / **C4 无绝对电平门** / **C4 slot_seq** / C11 逐母线+`route_gain_db` / C12 ADC 担保双条件 / C11 遥测降级可见性)。**回执即冻结 v1.4**;收方义务仍在——有异议先据理争一轮,回执不是盖章。
2. **lead** 快照 `IF-v1.4`(哈希现场戳定)。合同链台账已提前记齐(DEC-0010)。
3. **`adaptive-dsp`** 出 **W1-B 合并稿**:全文切 `IF-v1.4 条款Cx`、清 11 处"(在议)"、吸收 -2 三臂骨架 + 自身增量、B-m3 作 CHECK A2;切版自验按 **D3 两向**报。
4. **lead** 快照 W1-A v0.6 + W1-B。
5. **spawn 全新 critic**(rotation 规②③;`critic-w1` 已死,不得复用)→ 复审双件 + **正式 A/B 交叉核**(剩余开口见 §3)。
6. 双件 PASS → commit → CTO 第三关(§6 议程,现 8 项)。

### 恢复时的额度形态提醒
roster 把 `critic` / `system-architect` / `adaptive-dsp` **三个角色全锁在 Fable 5**(team_config LOCKED)。第 5 步要起的**全新 critic 也是 Fable 5** ⇒ **W1 复审整体受同一额度池约束**,两次中断均源于此。是否临时调档 = **改 LOCKED 表,须 CTO 批 + 台账留痕**,lead 不得自决。

---
## 1. 一句话状态
**W1-A(架构+预算)与 W1-B(NHS 算法)双双 FAILED,修法全部明确,两件均已完成一轮或多轮修订但未过复审;所有送审件已冻结为不可变快照。**

> ⚠ **本行原写「无任何设计件 commit(放行纪律)」—— 该表述不实,2026-08-01 lead 自查更正(F-20)**:W1-A/W1-B 早在 `8d6e6c7`(标题为"DEC-0008 W1-A verdict **FAILED**…")即已入 git,即**在 FAILED 状态下被 commit**,此后多次随附带 commit 前进(2674b28 / 32142dd / 221c83d)。详见 `decisions_log.md` F-20。**零上下文实例请以 git 实况为准,勿采信本节旧表述。**

## 2. 送审快照(评审基线,不可变)
```
01_design/submissions/W1A_v0.4_6b8de7559dfe.md    ← W1-A 送审件
01_design/submissions/W1B_v1.0_48d995e0936a.md    ← W1-B 送审件
01_design/submissions/IF-v1.0a_e032f01c7bf5.md    ← 接口合同送审件
```
盘面工作副本(`01_design/*.md`、`contracts/`)可能已前进,**评审一律对快照**(F-17 机制)。

## 3. 未闭合的 findings
### W1-A(critic-w1 首审 FAILED,0B/5M/7m/3INFO)
F1 Q15 灵敏度算错 2 倍 + "双轨"实为同式重跑 ｜ F2 "上界 ≈17%" 未按档全量重定价(应 ≈22.7%,T2 无界)｜ F3 口径分档偏离 DEC-0006 缺留痕(**已由 lead 补 DEC-0009**)｜ F4 高频盲区反应时间无预算 ｜ **F5 A/B 合同版本竞态** ｜ m1-m7。
**作者已出 v0.4 声称全闭环,待 critic 复审逐条核。**

### W1-B(critic-w1 首审 FAILED,0B/2M/5m/3INFO;critic 评为"项目迄今质量最高")
- **★ B-F1 快饱和啸叫三路全脱靶(本工单最重要发现,未修)**:被输出限幅器钉住的啸叫,若检测 tap 电平 < T_panic —— IMSD 因无增长而结构性不中(critic 滑窗全位置数值坐实)→ PANIC 前提(冲顶电平)**无人担保**(压缩/限幅与 tap 的相对位置**两份文档均未约束**)→ 硬削波谐波触发 PHPR 否决,而 §3.2 削波豁免要求"f 自身 IMSD 命中"**在此场景恒 False** → GROWTH/PERSIST 双否,**永不分类**。
  **修法三方向**(①归 B,②③跨 A/B 须联合):①豁免条件放宽(改用已有轨迹历史,不要求当帧 IMSD 命中);②**链序约束**(触及 A9)——architect 已拟 **C-7:压缩器/限幅器不得位于 NHS 检测 tap 之前**,并**自查发现推定链位漏放 COMP(PRD §二.3 必备),A9 定稿时补**;③T_panic 的增益结构论证。
  ⚠ **纪律**:不得只修①交差——只堵豁免条件而不解决"限幅器把啸叫钉平"的物理场景,是把洞挪个位置。
- B-F2 IF-v1.0 C5 分叉 2×(±1/6 oct vs 每侧 1/3 oct)——**已勘误为 IF-v1.0a**(值取 0.26f),但 critic 要求定版条款**写明"每侧/全宽"**;结构修法见 §4。
- B-m3 **宣称的 pareq.m 第二路实际没做**(声称的双轨未跑)｜ B-m1/m4/m5 及 3 INFO 见 critic 完整 verdict(已发 adaptive-dsp)。

## 4. 已裁定但未落盘的修法(合并轮执行)
1. **IF-v1.1 结构修法**:C5 **自足化**(定义全文内联 + 补"(每侧)"字样 + 删"语义=接收方"指针),**权威方向反转**——W1-B §1.2 改为引用 C5。通则:**接口合同每一条款必须自足,受方文档只引用、不定义**(合同是唯一权威源,不得把语义外包给受方文档)。
2. W1-A 头部第 3 行状态栏 v0.3 → v0.4(版本标记自相矛盾);其余 5 处 "v0.3" 为历史记述,核对后保留或注明。
3. W1-A §3.4 写明**陷波模型与 BW 定义约定**(RBJ peaking 负增益式,BW 定义在半增益 dB 点)——lead 第三轨 −2.21/−1.25 vs 作者 −2.40/−1.52 的 0.2-0.3dB 差即此约定差,两套约定下"BW/4 过 ≥2dB、BW/2 不过"结论同向。
4. 两侧文档契约引用切至 IF 现行版。

## 5. 恢复工作顺序(重启后照此做)
1. **lead** 读本件 + `decisions_log.md` DEC-0007~0010 + 三份 `agents/*/memory.md`。
2. **spawn critic**:同工单评审链按 rotation **规①可续用逻辑**,但**实例已死** ⇒ 起新实例并交底(本件 §3 + 两份完整 verdict 在 critic memory 的 R4/R5 条目)。
3. **spawn system-architect + adaptive-dsp**,各自读己方 memory 的 fix-list。
4. **首要任务 = B-F1 联合修**(优先级高于其余所有 fix),按 §3 三方向;涉链序 ⇒ 先落 **IF-v1.1**、再改各自文档。
5. 修完 → lead 建快照 → critic 复审 W1-A + W1-B + **正式 A/B 交叉核**(剩余开口:B-F2 收敛、B-m4 反应时间起算点两文档不同参、B-m1 β_min 预设值 vs 初值、接收方补充回执)。
6. 双件 PASS → commit → 上 CTO 第三关(议程见 §6)。

## 6. CTO 第三关议程(累积,待 CTO) ⚠(**7/31 旧节**,与文中 `## 6. ⭐ 断点` 编号重复)
| # | 事项 | 性质 |
|---|---|---|
| 1 | **旁链 float32 对 DEC-0006(锁定点)的例外申请**(V-9) | 例外批准 |
| 2 | **DEC-0009 口径变更追认**(30-50 全局包络 → 分档制) | 变更追认 |
| 3 | **F4 高频残余风险**:0.6-2.5s 反应窗口,依赖 D4 输出保护限幅兜底 | 风险接受 |
| 4 | §5 预设持久化 UX 口径 | 口径裁定 |
| 5 | **EZKIT 实板是否存在**(决定 W1-C 能做到 L1 还是止于 CCES 模拟器 L2) | 事实确认 |
| 6 | R-AFC-1 **GN Hearing US9794695B2** 是否走法务 FTO(有效至 2032) | 法务决策 |
| 7 | D0b/D0c **补审工单**(F-14:标"待审"却已 commit,lead 偏差) | 排期 |
| 9 | **CCES 装的是 ARM-only,SHARC 工具链缺失**(2026-08-01 lead 实查):`/opt/analog/cces/2.12.1` 存在(2.1G),但**全树无 `cc21k`/`easm21k`、无 SHARC 目录、无 21569 支持包、未见 license**;`ARM/` 下仅 aarch64/arm-none-eabi/gcc-arm-embedded/openocd/qemu。⇒ **W1-C 的 CCES 模拟器分支(L2)当前不可跑**,阻塞项 = 装 SHARC 编译器 + 21569 支持包 + license,**与"EZKIT 实板是否存在"(第 5 项)相互独立** | 事实确认 + 采购/安装 |
| 8 | **sub-120Hz 自动检测盲域**:自动通路对约 120Hz 以下不保证(盲在判据侧,非供给侧——字典 `AFC.det_hpf` = OFF∪30-500Hz/默认 100,IF-v1.4 C4 主谱自 bin 15.625Hz 起无带宽下限截断);兜底 = 手动陷波。**姿态 = 明示不藏** | 明示限制/风险接受 |

## 7. 阻塞与关键路径 ⚠(**7/31 旧节** —— 最新断点是文末同名编号的 `## 7. ⭐ 断点(2026-08-02 晚)`)
**W1-C 微基准是唯一关键路径**:T1 取 2.0 cyc/MAC 是规划值,库内唯一同族板测规整短环是 **8.51 cyc/MAC(敞口 ×4.3)**,T2(FFT)**无板证上界**。两个下游挂在它身上——架构侧预算口径、算法侧包络余量(adaptive-dsp 已声明"若口径上调至 8.5 量级则超包络")。**若实测落 8.51 量级,AFC 占比升至 ~40-50%,AEC/ANS 的 N 值与整机可行性须按铁律四重裁**(DEC-0009)。
**W1-C 内核清单**:1024/2048pt 实 FFT + 8 级 biquad×64 样本 + 多相抽取 + **判据/状态机标量码(第 4 内核,adaptive-dsp 请求,其 20k cyc/槽 包络暴露于 T3 口径)**。~~CCES 模拟器分支 = L2,可立即开~~ ⛔ **该句已作废(2026-08-05)**;EZKIT 实板分支 = L1,**须先过 C10 三件套**(CTO 出稿清单 + 丝印 REV 肉眼确认 + 安全硬规矩)。
> **⛔⛔ 同文件内自相矛盾,已裁(由 `adaptive-dsp-7` 去原文核第 5/9 项时撞出,2026-08-05)**:
> 本行原写「**CCES 模拟器分支 = L2,可立即开**」,而**本文件第 9 项(2026-08-01 lead 实查)**写:CCES 2.12.1 在盘(2.1G)但**全树无 `cc21k`/`easm21k`、无 SHARC 目录、无 21569 支持包、未见 license ⇒ W1-C 的 CCES 模拟器分支(L2)当前不可跑**。
> **⇒ 以第 9 项为准(它是实查,本行是 7/31 的计划性表述)。⇒ ⛔ 不得据"可立即开"做任何安排。**
> **⚠ 而危险处在于本行【在前】,更容易被先读到** —— 与 `CLAUDE.md:9`(纠正以"新增一条"的形式存在,而被纠正的旧句一字未动)是**同一个形状**。⇒ ∴ 本次按正确做法处理:**在旧句原地划掉并标撤回**,⛔ 不只在别处新增一条。

## 8. 本工单产生的团队级纪律(已入 `.claude/team_config.md` / decisions_log)
- **Interface contract discipline 六条**(F-16/F5 修复):合同独立成文件 + 两侧只引版本号禁转述规范值 + 变更台账 + 先落合同后改文档 + 收方据理争辩义务 + critic 契约段逐条对表。
- **送审快照机制**(F-17):送审即入 `01_design/submissions/<件>_<版本>_<sha12>.md`,由构造保证不可变。教训:**口头纪律管不住并发写,要机制不要自觉**。
- **同族二义性通则**:规范值必须带明确度量约定(**每侧/全宽、半步/满步、峰值/RMS、单边/双边**)——C5 与 F1 均因此产生恰好 2× 偏差,2 小时内两次。
- **接口合同条款须自足**:受方文档只引用、不定义。

---
## 5.9 ⚠ 实例花名册更新(2026-08-02 晚,orchestrator)

**按 F-18 / DEC-0011 实例纪律,现役名单以本表为准,不得凭 memory 或旧消息推断。**

| 角色 | 实例 | 状态 | 说明 |
|---|---|---|---|
| adaptive-dsp | **第 3 实例** | ⛔ **已退役** | 上下文耗尽(~99.4 万 token),交接件 §6 由它写就 |
| adaptive-dsp | **第 4 实例** | ⛔ **已退役** | 2026-08-03 不可达;停止时把两个跑批子进程一并带走(r61 26/36 行、r62 0 行) |
| adaptive-dsp | **第 5 实例** | ⛔ **已退役** | 2026-08-03 晚上下文用尽;定位 `_allocate` 瓶颈 + `T_low` 死区 + 参考电平缺口 |
| adaptive-dsp | **第 6 实例** | ✅ **现役** | 2026-08-03 夜接手,任务 = 跑完源电平扫描(含标称 −20 dBFS 主档) |
| critic-w1p | — | ⛔ **已下岗** | verdict FAIL + 四份补件 + 交接件已交;复审须【全新实例】(自审禁令:整改稿引用了它自己产出的测量) |
| rir-fixer | — | ✅ 任务完成 | V-34 分析脚本自测已通过 |
| system-architect | 第 2 实例 | ✅ 现役 | |
| orchestrator | — | ✅ 常驻 | |

**⛔ 严禁向 adaptive-dsp 第 3 实例发消息** —— 会复活已退役实例 ⇒ 裂脑。

## 6. ⭐ 断点(2026-08-02,adaptive-dsp-3 上下文将尽)—— W1-P 闭环台架线

> **本节自成一体**,不依赖 §0-§5。接手者只读本节即可续跑闭环台架线。
> **工作目录**:`01_design/prototype_W1P/`。**未 commit**。

### 6.1 ~~⛔⛔ 唯一未解的技术问题:**接入证明对照失败**~~ → **✅ 已收口(2026-08-02,adaptive-dsp-4),定案见 §7**

> **⛔ 本节保留作历史记述,结论已被 §7 取代。**
> 定案:**不是台架 bug、不是没接上、不是信号色彩** —— 成因 = ①**带内 vs 全带的口径错配**
> (预测算带内 max,台架测全带 max)+ ②**阳性对照锁的是"NHS 模块"而不是"陷波机制"**
> (唯一"通过"的那条实为宽带兜底 `g_duck` 冒名顶替)。**下面 §6.1 的"下一轮对象/下一步"全部作废。**

### 6.1(历史记述)接入证明对照失败(2026-08-02 晚更正,orchestrator)

> **⚠ 本节对象已更正两次,接手者请读到底再动手。**
> 原对象「seed1 的 4 dB 缺口」——**作废**。r50 六条种子实测证明 seed1 不特殊。
> 第二版对象「六条系统性 4–6 dB 缺口」——**也浅了一层**。见下。

**r50 实测(排他区选法,六条种子,边界命中 0 ⇒ 无窗口截断):**

| T60 | seed | 神谕预测 | 实测 ΔMSG | 差 | 宽带@100–8000Hz | **接入对照** |
|---|---|---|---|---|---|---|
| 0.2 | 0 | 3.75 | **4.00** | +0.25 | −11.68 | ✓ |
| 0.2 | 1 | 6.03 | **0.00** | −6.03 | −9.65 | **✗** |
| 0.2 | 2 | 7.74 | **1.50** | −6.24 | −12.86 | ✓ |
| 0.5 | 0 | 4.67 | **1.00** | −3.67 | −9.31 | **✗** |
| 0.5 | 1 | 6.58 | **0.50** | −6.08 | −10.10 | **✗** |
| 0.5 | 2 | 4.49 | **0.50** | −3.99 | −12.44 | **✗** |

**⛔ 预注册的第三条阳性对照(接入证明)是 `|ΔMSG| > 2 × 阶梯步长 = 1.0 dB`,
否则判【台架 bug】。理由:「NHS 不在环内」与「NHS 在环内但完全无效」在数据上一模一样。**

**⇒ 六条里【四条】不过。⇒ 在该对照通过之前,这批数【不得】读作"实现损失"。**

**两条支持"没接上/无效"而非"损失"的形态证据:**
1. 实测值 `{0.00, 0.50, 0.50, 1.00, 1.50, 4.00}` = 阶梯的 `{0,1,1,2,3,8}` 步
   ⇒ 五条挤在 0–3 步,**不像"损失了一部分",像"根本没动"**;
2. 实测值**几乎与神谕预测无关**(预测 3.75→7.74 跨 4 dB,实测除孤例外恒在 0–1.5)
   ⇒ **因变量与自变量无关,通常意味着自变量没接上。**

**⇒ 下一轮的对象:为什么接入证明对照失败。** 不是"为什么掉 4–6 dB"。

**已排除(勿重复):**
- 陷波没挂上 —— 实测 `|H| = −18 ~ −35 dB`、状态全 HOLD、`g_duck = 0`
- 6 dB 门被环路着色误触发 —— 符号反了,该机制会让 ΔMSG 读【高】
- 相位扰动生成更高的新临界点 —— 实测新最大 3.77 dB < 原最大 7.75 dB
- 窗口截断 —— r50 边界命中 0

**下一步(已设计未执行,原五项仍适用,但对象改为"任一失败种子"而非 seed1):**
测 `G = 解析MSG−2 … +6` 逐步的 ①环路电平 ②检测器触发点 ③触发时主导频率
④**触发频率是否落在任一已挂陷波带宽内** ⑤触发时刻该频率处的 `|F·N₁…N₈|` 实测值。
判读:落带宽内 ⇒ 陷波作用点可能不在环内(尽管 `Loop.run` 取点已修过一次);
落带宽外且非预测的新临界点 ⇒ 预测式漏了机制。

**⚠ 建议优先做的一条零成本检查**:把 `k` 从 8 改到 1,
若 `ΔMSG` 仍 ≈ 0,说明问题与陷波数量无关,直接指向"作用点/环路接入"。


<details><summary>(存档)原 6.1 内容</summary>

### 原 6.1 seed1 的 4 dB 缺口

```
配置:T60=0.2 / seed=1 / 8 个陷波(批量选 top-8 临界点)/ depth −18dB / bw_oct=1/5
神谕预测 ΔMSG = 3.98 dB      实测 ΔMSG ≈ 0 dB(< 0.5,阶梯分辨力)
                              ⇒ 差 4 dB,**未解释**
```

**已排除的三条(各有实测,不必重查)**:
| 假说 | 排除依据 |
|---|---|
| 陷波没挂上 | 实测 proc 传递函数在 8 个目标频点 `|H| = −18 ~ −35 dB`;槽状态全 HOLD;深度全 −18.0;`g_duck = 0` |
| 6dB 门被环路着色误触发 | **符号反了**:挂陷后 `max\|GF\|` 变小 ⇒ 同 G 下着色更少 ⇒ 该机制会让 ΔMSG 读**高**不是低。第二条独立反证:基线实测与解析精确吻合 |
| 相位扰动生成更高的新临界点 | 实测挂陷后临界集 max = **3.77 dB** @527.7Hz < 原始 max **7.75 dB** @5506.7Hz ⇒ 是降的 |

**下一步(已设计,未执行)**:对 seed1 加陷波,测 `G = −7.25 / −6 / −5 / −4` 各点:
1. 环路电平(求和节点)
2. 检测器在哪一点触发
3. 触发时的主导频率
4. **【关键】触发频率是否落在任一已挂陷波的带宽内**
5. 触发时刻该频率处的 `|F·N₁···N₈|` 实测值

**判读(预写)**:
- **落在陷波带宽内** ⇒ 与已实测的 `|H| = −18~−35 dB` 矛盾 ⇒ **查陷波作用点是否真在环内**;
- **落在带宽外且不是 527.7 Hz** ⇒ **预测式漏了一个使 MSG 更低的机制**;
- 若该频率处 `|F·N|` 显著高于 3.77 dB ⇒ **预测式的问题,不是实现损失**。

</details>

### 6.2 本轮已确定、可直接引用的结论

> **⚠ 口径标注(2026-08-02,adaptive-dsp-4,lead 裁定 2 批准"加标注不撤回")**
> **下表两轴均为【带内 100–8000 Hz】口径**,`ΔMSG` 是带内神谕、`宽带` 是带内中位 |N|。
> **已机械核实同口径**:`r49_exclusion.py:25` 定义 `fm = f0[(f0>=100)&(f0<=8000)]`,
> `:26-:29` 的 `Hn_tot`、`_crit_from_H`、`median(|Hn_tot|)` **全部建在同一个 `fm` 上**
> ⇒ **两轴同口径 ⇒ 三臂比较是内部比较 ⇒ 结论存活,无需重判。**
> **⚠ 但下表的 `ΔMSG` 不等于闭环能测到的 MSG 提升**(闭环测的是**全带**;详见 §6.1 定案与
> `r50_out_ANNOTATION.txt`)。本表只可用于**三臂之间的相对比较**,不可作为绝对收益引用。
> 附:`r49_exclusion.py` 原把该轴标为"全带中位|N|" = **过时标签**(值一直是带内算的),
> 已勘正;勘正后逐位复跑,数字零变化。

**三臂选点策略(神谕层,6 条种子,双轴,**带内 100–8000 Hz 口径**)** — 证据 `r49_exclusion.py`:

| T60/seed | 批量 ΔMSG | 排他区 | 顺序挂 | 批量宽带 | 排他区宽带 | 顺序挂宽带 |
|---|---|---|---|---|---|---|
| 0.2/0 | 4.13 | **3.75** | 6.89 | −13.02 | −11.68 | −10.03 |
| 0.2/1 | 3.98 | 6.03 | 6.05 | −11.65 | −9.65 | −10.00 |
| 0.2/2 | 5.04 | 7.74 | 7.80 | −18.37 | −12.86 | −12.52 |
| 0.5/0 | 2.16 | 4.67 | 7.47 | −16.31 | −9.31 | −9.20 |
| 0.5/1 | 4.04 | 6.58 | 7.81 | −15.25 | −10.10 | −10.01 |
| 0.5/2 | 3.88 | 4.49 | 6.81 | −16.38 | −12.44 | −8.93 |
| **均值** | **3.87** | **5.54** | **7.14** | **−15.16** | **−11.01** | **−10.12** |

- **排他区在两个轴上都不劣于批量**(ΔMSG +1.67 dB、宽带衰减少 4.15 dB),**实现代价为零**(选点时剔除 ±1 带宽内的候选);
- **但排他区不能替代顺序挂**:回收率双峰(见 §6.3);
- **宽带衰减方向与直觉相反**:批量把多个陷波堆在同一簇 ⇒ **skirt 叠加** ⇒ 塌陷更宽更深。

**~~67%~~ → 55.1% 覆盖率(检测带线性频率轴)** ⛔ **2026-08-03 勘正(r61 实测,lead 批准,铁律五)**
> **旧值 67% 有两处口径错,方向相同(高估 11.9 个百分点)**:
> ① **求和 ≠ 并集** —— `1516+…+22 = 5288/7900 = 66.9%` 是**求和**,**重叠被重复计**;
>    正确应取区间**并集** ⇒ **55.1%**(同一组选点、同一脚本实测)。
> ② 它用的是**批量**选点,而现行方案是**排他区**选点 ⇒ 排他区并集 = **52.7%**。
> **⇒ 结论方向不变**(覆盖率仍 >50% ⇒ 中位落在陷波内 ⇒ 代价大),**但依据数须用 55.1 / 52.7。**
> **⇒ r61 六条实测(排他区、f_cut=8k)**:`bw=1/5` 平均 **57.0%** / `1/8` **42.7%** / `1/12` **30.8%**。
> ⚠ 并注意:`bw=1/8` 时覆盖率跌破 50%,但**中位插损并未塌到 0**(实测 −8.10 dB)——
> RBJ 裙边远超标称带宽,陷波之间仍被多裙边叠加压着。**「覆盖率<50% ⇒ 中位≈0」的直觉是错的。**

~~`1516+1101+765+655+518+504+207+22 ≈ 5288 Hz / 7900 Hz = 67%`~~(求和口径,已作废)
⇒ 中位数必然落在陷波内,这就是宽带衰减 −11~−18 dB 的来源。
⇒ **`bw_oct` 须从 1/5 改为 1/8 或更窄**(原文工程区间 1/10–1/60;总带宽预算 0.8–1.2 倍频程,我们 8×0.2 = **1.6**,超 1.3–2 倍)。

**MSG 定义已得一手确认**(van Waterschoot & Moonen, EUSIPCO 2009, 式 4, p.1998):
`MSG(t)[dB] = −20log₁₀[ max_{ω∈𝒫} |J(ω,t)[F(ω,t) − F̂(ω,t)]| ]`,
`𝒫` = **loop phase 为 2π 整数倍的频点集** ⇒ 与我方"临界点集"一致,相位条件勘正获一手来源支持。

### 6.3 ⛔ 已撤回、不得再引用的数

| 撤回的数 | 原因 | 正确值/处置 |
|---|---|---|
| **宽带衰减 −0.65 dB** | 按 **0–24 kHz** 算,中位落在 8–24 kHz 未处理区 ⇒ 无意义 | **检测带 100–8000 Hz 内中位 = −11.65 dB**(seed1 批量) |
| **排他区回收 51%** | 均值掩盖**双峰**:0.2/1 与 0.2/2 达 **99%**,0.2/0 为 **−14%(比批量还差)** | **不得用均值**;须逐条报 |
| **我们超过同行基线** | **上界 vs 实测的类别错配**,架构侧已撤 | — |
| ΔMSG 一次式(1.8–4.4 dB) | 结构假设错:默认挂 k 个陷波后临界点集不变,**而陷波自身有相位** | 用**迭代式**(相乘重算,非删点) |
| 「稳态挂陷/稳态占用」标签 | 100s 窗内截尾率 100%,从未达稳态 | 已全库改为「窗末累积占用@Ns」(41 处) |

### 6.4 待办队列(按序)

1. **四轴仪表化** — 定义已从一手源抄录(van Waterschoot & Moonen 2011 Proc.IEEE 综述,`research/sources/papers/vanWaterschoot_Moonen_2011_ProcIEEE_fifty_years_acoustic_feedback_control.pdf`):
   - **SD 式(111)**:`SD(t)=sqrt(∫₀^(fs/2) w_ERB(f)·[10log10(S_d/S_v)]² df)`;`S_d`=补偿后信号短时 PSD,`S_v`=**源信号** PSD(台架天然有);`M=4096 @48kHz`;50% 重叠;**均值与最大值都要报**。⚠ `w_ERB` 需 ANSI S3.5-1997 Table 2(未持有)⇒ 用公开临界带边界并**显式标为偏离**。
   - **HOP 式(112)**:`(Σtᵢ)/T`;**TRI 式(113)**:`(Σtᵢ)/N_HO`。
   - ⚠ 原文的啸叫区间识别①听②目视是**人工**的;**我方用台架 ground truth 替掉①②,保留③④(PAPR 峰两侧 −3dB)** ⇒ **严格更客观,但须显式记为偏离**。
2. **PD 的同种子双臂反事实**:关臂提供正类标签(实际失稳的频点与时刻),开臂提供检测结果。⚠ **不得用开臂自己的输出当 ground truth**(NHS 会阻止啸叫发生)。
3. **全通测试**(纯相位,`|H|≡1` 须实测验证)—— 验相位通路;平坦扫描只验了幅度通路。
4. **判据组合修改**(**须等四轴基线**):现状 `(PAPR ∧ PNPR) → (IMSD ∨ rapid_onset) → PHPR 否决`;原文最优是 `PHPR ∧ PNPR ∧ IMSD` 三项 AND、不含 PAPR;`T_papr` 硬编码 15.0 而原文式 `20log10(M/150)` 在 M=1024 时应为 16.7。
5. **ROC 立项**:六项判据各画 ROC,报 AUC + PAUC(PFA≤5%)+ PD=95% 处的 PFA。⚠ **在此之前,所有"某改动降低了误报"的结论都不得读作"变好"**(无配对检出率,无法排除拿漏检换的)。

### 6.5 ⚠ 接手者必须先知道的三条纪律(本轮血泪)

1. **跨来源对账先核【量的类别】**(上界 / 实测 / 期望 / 最坏)。**类别错不会自己暴露,只会表现为一个长期解不掉的"冲突"** —— 我们为此烧了 15 轮。
2. **任何"频带内的中位/均值"必须写明【频带范围】** —— `−0.65` 与 `−11.65` 是同一个量的两个范围,差 11 dB。
3. **分布双峰时不得报均值** —— 排他区那六条,均值 51% 是一个**不存在的中间态**(实际是两条 99%、一条为负)。

### 6.6 台架现状(可直接跑)

| 文件 | 作用 | 状态 |
|---|---|---|
| `clrig.py` | 噪声 RIR `F(z)` + 临界点 + 解析 MSG + 迭代式预测 + `h_eff` + `Loop` | 可用 |
| `howl_detect.py` | 起振判据(**零 import nhs**,AST 断言) | 可用 |
| `cosource_check.py` | 共源核查(**无条件执行**,结构检查非数值阈值) | 通过 |
| `r45_flat.py` | 平坦扫描(按臂缩窗) | 未跑完 |
| `r49_exclusion.py` | 三臂选点 | 已出数(§6.2) |
| `r32_clrig_controls.py` | 模态时代遗留 | **已标死,不可运行** |

**已通过的台架有效性检查**:平坦扫描斜率 **1.0000**(逐点精确,A=1/2/3/6/10)、`T` 敏感性(6s→12s 全 0.00,5/6 条;**第 6 条未测,原则上可推翻**)、共源核查交集为空、`N_crit(step)` 门(迭代 8 步内 ±10%)。
**未通过**:对照④(解析一致性)剩 2 条残差 2.0–2.3 dB,`h_eff` 修好另 2 条;**继续挂待查**。

---
## 7. ⭐ 断点(2026-08-02 晚,adaptive-dsp **第 4 实例**)—— 接入证明对照失败已定案收口

> **本节自成一体,取代 §6.1。** 工作目录 `01_design/prototype_W1P/`。**未 commit,未过任何门。**
> 一句话:**闭环台架与起振检测器从未失效;失效的是我们对它的【读法】。**

### 7.1 本轮的干净结论(会被引用的那些)

> # ⛔⛔ 本节 ① 已被独立 critic 判 **BLOCKER B-1**(2026-08-03)。**改名前的旧表述作废。**
> **旧名「陷波在其作用域内的实测收益」= 错。** 产生该数的臂里,**NHS 的检测/选点/分配从未参与**:
> `r57_bandlimit.py:51-61` 的 8 个陷波频点取自 `clrig.critical_points()`(**解析神谕**),
> `:64-76` 直接写 `st=HOLD / depth=max_depth` 并设 `a.P.T_low = 999.`;
> `nhs.py:402` `if lv < gate: continue`,**首次获取**门 = `T_low` = 999 ⇒ 0 dBFS 上限下不可能过
> ⇒ **新分配恒不发生**;`nhs.py:94` `lift_after_s=60 > T_OBS=6` ⇒ 预挂槽整轮不释放。
> ⚠ **勘正(2026-08-03,critic 抓获,我原写"另一条门 =999−20=979"是错的)**:
> `T_low_gr` 在 `Params.__init__`(`nhs.py:73`)已按 `−45−20` **算死为 −65.0**,
> 构造后改 `P.T_low` **不重算它** ⇒ **`nhs.py:399-401` 已覆盖 bin 的【维持】路径仍通(门 −65 dBFS)。
> 被关掉的只有「首次获取」。** 数值影响 = 0(预挂槽已在 max_depth),**但描述必须准确。****同一构造复用于 `r51:57-68` / `r53:45-55` / `r59`(直接 import `mk_alg`)。**
> **⇒ 被测对象 = 「8 个 RBJ 陷波器,由解析式放在最优点上」。这是 NHS 的【上界】,不是 NHS 的性能。**

**① 陷波器组在【神谕选点】下的 ΔMSG —— 即 NHS 的【上界】**(`r57_bandlimit_out.txt` §2)
```
逐条(六条种子):6.00 / 6.00 / 7.50 / 3.50 / 6.50 / 4.50 dB      ← 原始值,优先引用逐条
均值 5.67 dB(分布单峰 3.5–7.5;报均值必须同时给逐条)
duck 贡献 6/6 全 0.00
工作点:f_cut=8k / T_OBS=6.0s / STEP=0.5dB / FRAME=64 / bw_oct=1/5 / depth=−18dB / 8 槽
★ 最重的那一维(旧版漏列):**选点来源 = 解析神谕,非 NHS 检测输出;T_low=999 ⇒ 分配全程关闭**
```
> **⚠ 两条前提必须与数字同时出现,缺一即误导:**
> **(i) 【上界】** —— 选点由神谕给出,NHS 的检测/分类/分配未参与;
> **(ii) 【`f_cut = 8k` 的台架带限】** —— 该前提在真机上是否成立,**没有 [L1/L2] 依据**。
> **⇒ 禁止表述**:「NHS 实测 …」「五条达标」「NHS 的性能是 5.67 dB」。
>
> **⭐ B-3 对外第一句的合规模板(三要素缺一不可,原样用)**:
> > 「**在环路于 8 kHz 以上被带限**的条件下,**陷波器组在神谕选点下**取得 ΔMSG
> > 3.50–7.50 dB(均值 5.67,六条种子)—— 这是 **NHS 的上界**,`[L2/宿主仿真]`。
> > NHS 自身选点下的性能**尚未测量**。」
>
> **⚠ P.341 频段(100–8000 Hz)只能作【补充论据】,不得【替代】要素①。两者不等价:**
> P.341 说的是"**标准只要求测到 8 kHz**";要素① 说的是"**被控对象本身在 8 kHz 以上被带限**"。
> **前者是测量口径,后者是对物理系统的假设。用前者冒充后者 = 把"我们没测"说成"那里不存在"。**
> **⇒ NHS 自己能拿到几 dB = 未测**(整改队列 1(b),自由槽位起振扫描)。
> ⚠ critic 补做的缺失对照 [L2/单一工作点、未做起振扫描]:槽全空 + `T_low` 默认 + `G=MSG_全带+3dB`
> ⇒ 神谕 5.63 / **NHS 自选 @12s 1.62 / @30s 3.51**,4/6 起振;最差 `0.5/seed0` 30s 仍只 1 个陷波、
> **0.44 dB** —— 而它正是上表报 3.50 的那条。**该数不得直接替换上表;它证的是「两者不是一个量」。**

**⭐ 台架管路一致性的【确切表述】(B-2 改写后,原样引用,不要改回)**
```
闭环时域与同系数频域在 0.5 dB 扫描分辨力内一致
⇒ 台架管路与滤波施加无 >0.5 dB 额外损失
⇒ 陷波【设计环节】的损失未测(神谕取自已实现系数)
```
> **⛔ 已删除的旧表述(不得复活)**:「六条 |差| ≤ 0.30 dB ⇒ 完全一致」「残差 100% 归…,**0% 归实现损失**」。
> **原因(critic 指出,我核过并做了定量)**:两臂扫描**同锚同栅格** ⇒ `d = mk − m0`
> **构造上恒为 0.5 的整数倍**,而神谕是连续量。
> **⭐ 定量到底(比"落在量化噪声里"更硬)**:纯量化零假设下 `m = θ − u`,`u ~ U(0, STEP]`
> ⇒ `d − 神谕 = u₀ − u_k` ⇒ **`E|d − 神谕| = STEP/3 = 0.1667 dB`**(解析式与 2e6 次 MC 一致)。
> **实测六条 0.03/0.03/0.23/0.30/0.17/0.02,均值 `0.130 dB` —— 【低于】零假设期望 0.167。**
> **⇒ 我们观察到的"吻合",比【什么都没测】时期望看到的吻合还要好一点点(n=6 的抽样波动)。**
> **⇒ 该判据的信息量 ≈ 0,不构成任何证据**,不是"证据弱"。
> **⚠ 与另一个口径的关系**:拿**带内**神谕对时 `0.5/seed0` 差 **1.19 dB**,该差值来自"挂陷后最高
> 临界点掉到 **52.2 Hz**(< `f_det_lo=120`)",`r59` 直接实测到 `f_trig = 52.0 Hz`(距解析 0.2 Hz)。
> **⚠ 但"其余部分归实现损失为 0"这一推论已随上句一并撤回** —— 分辨力不足以支撑该分解。

**①b ⭐ `f_trig` 直接实测表 —— 「带外主导」从【推论】变成【直接观测】的那一步**
(`r59_ftrig8k_out.txt`;f_cut=8k,挂 8 陷 + duck 已消融)

| T60/sd | 首起振 G | **实测 f_trig** | 解析(挂陷**后**)全带峰 | \|差\| |
|---|---|---|---|---|
| 0.2/0 | −5.35 | **186.0 Hz** | 185.9 | **0.1 Hz** |
| 0.2/1 | −6.43 | **365.0 Hz** | 364.5 | **0.5 Hz** |
| 0.2/2 | −5.08 | **503.0 Hz** | 503.1 | **0.1 Hz** |
| 0.5/0 | −8.49 | **52.0 Hz** | 52.2 | **0.2 Hz** |
| 0.5/1 | −6.75 | **213.0 Hz** | 22.3 | **190.7 Hz** ⛔ 见 F36 |
| 0.5/2 | −7.52 | **170.0 Hz** | 169.6 | **0.4 Hz** |

> **⇒ 六条全部 ≤ 8000 Hz。对照:`f_cut = 24k` 时四条落在 14531 / 15182 / 17244 / 18895 Hz(带外)。**
> **⇒ 这一步之前,"带外主导"是我们的推论;这一步之后,它是一次直接观测。**
> ⛔ 唯一偏离条 `0.5/seed1` 已实测查清(**分辨力 + 低频建立时间**,非漏机制):该条两个候选
> 临界点只差 **0.281 dB < 0.5 dB 阶梯**;把阶梯降到 0.1 dB、窗拉到 24 s 后
> **`f_trig` 移到 22.5 Hz(距解析 0.2 Hz)、ΔMSG 6.70 → 6.40(距全带神谕 0.07)**(`r60`)。

**② NHS 的实际有效带 = 120 – 7800 Hz**
下沿 = `nhs.py:69` 定义 `f_det_lo`(`:357` 使用);上沿 = `f_det_hi` 7800 Hz,**并另受旁链
16 kHz 抽取的结构性限制 ⇒ >8 kHz 结构上不可见**。

**③ 带外啸叫时的实测行为**(`r58_freeslot_out.txt`,**产品实际工况:8 槽全空可自由分配**;
T60=0.2/seed0,真啸叫 14529.6 Hz;**逐行报,不合并成一句话**)

| margin(全带) | 行为 | 起振? |
|---|---|---|
| **+2.0 dB** | 无动作(正确,本就不该动) | no |
| **+0.5 dB** | 无动作 | no |
| **−0.5 dB** | **陷波挂到 1469.8 Hz(= 混叠像,预测 1470.4,差 0.6 Hz)** + `g_duck` −6 dB | no(**duck 兜住**) |
| **−2.0 dB** | 无陷波 + `g_duck` −6 dB | **YES**(兜不住) |
| **−4.0 dB** | 陷波挂到 2559.9 Hz(**无关的带内临界点**) + `g_duck` −6 dB | **YES** |

> **⇒ 失效形态 = 【静默 + 自我肯定】**:陷了个没问题的频点、啸叫照旧、**日志显示 NHS 正常工作**。
> 现场排障会被引向完全错误的方向。`g_duck` 是唯一真正起作用的,但**上限只有 −6 dB**。

**④ ⭐ `duck` 贡献六条全 0.00,是 P-1 因果链的【反向验证】**
带限堵住带外逃逸 ⇒ 没有带外啸叫 ⇒ 没有混叠幻峰 ⇒ 没有 `SLOTS_EXHAUSTED` ⇒ 没有 duck。
> **去掉源头,整条链一起消失 —— 这是因果链的最强证据形式,强于"我们解释了每一环"。**

**⑤ 台架本身的精度(六条种子)**:`|m0基线 − MSG_全带| ≤ 0.28 dB`;
起振主导频率与全带临界频差 **0.2–0.5 Hz**;实测环路传函对解析 `max|err| = 0.0000 dB`。

### 7.2 ⛔ 已知弱点(2026-08-03 按 critic verdict FAIL 更新;原六条 + 新增五条)

> **⛔ 0.(BLOCKER B-1,压过下面全部)承重数的产生臂里 NHS 的检测/选点/分配没有参与。**
> 详见 §7.1 顶部横幅。**该数是【上界】。NHS 自己能拿到几 dB = 未测。**
> critic 补做的缺失对照(槽全空/`T_low` 默认):神谕 5.63 vs **NHS 自选 @12s 1.62 / @30s 3.51**。

> **⛔ 0b.(BLOCKER B-2)**「≤0.30 dB ⇒ 完全一致」「0% 归实现损失」**已撤回** ——
> 两臂同锚同栅格 ⇒ 差值构造上恒为 0.5 整数倍 ⇒ 该判据落在量化噪声里。

> **⛔ 0c.(BLOCKER B-3)** 对外第一句必须同时含 ①「环路在 8 kHz 以上被带限」(**对被控对象的前提**)
> ②`[L2/宿主仿真]` ③「**上界**」。**⚠ P.341 频段只能作补充论据,不得【替代】该前提 —— 两者不等价**
> (P.341 说的是"标准只要求测到 8k",与"环路本身在 8k 以上被带限"是两回事)。

> **⛔ 0d.(M-1,lead 提级)等代价平坦衰减器在 6/6 条上赢过陷波**(7.80–9.31 vs 5.67)。
> **⇒ F33「duck 冒名顶替」的升级版:我们消融了 duck 以锁定机制,却没问
> 「这个机制在【等代价】下打不打得过最笨的那个办法」。**
> ⇒ 叠加 SD 未仪表化 ⇒ **目前不存在任何一条指标显示陷波优于平坦衰减,而唯一能算的那条它输了。**
> ⇒ `bw_oct` 1/5→1/8 必须在该数出门之前跑(67% 覆盖率正来自 1/5,改后可能翻转)。

> **⛔ 0e.(M-3)`PREREG_r52` 的 H7(k=8→k=1 零成本检查)从未执行,且从未记录未执行 ——
> 属【静默丢弃预注册项】。** 现予显式留痕:**未执行;原因 = 本实例把算力全部投向 f_cut 扫描与
> seed0 归因,遗漏回填,非有意取舍。** 补跑列入队列。

1. **`Hg` 未过(1.49 dB)** —— 成因已定位为**低频侧接管**(挂陷后最高临界点掉到 52.2 / 22.3 Hz,
   低于 `f_det_lo=120`),**但未修**。
2. **`f_cut=8k` 是台架构造,不是产品实测** —— 该上界成立于"环路被带限到 8k"这个前提,
   **该前提在真机上是否成立,无 [L1/L2] 依据。**
3. **六条种子、单一 `bw_oct=1/5`、单一深度 −18 dB** —— 而 `bw_oct=1/5` 已知**超文献总带宽预算
   1.3–2 倍**(8×0.2=1.6 vs 0.8–1.2 倍频程),**须改 1/8 而未改**。
4. **音质轴(SD)未仪表化** —— 该上界**没有配对的音质代价**,不构成"净收益"(与 0d 合看更重)。
5. **4σ 过度分散真因未知**,三个机制假说全被证伪。*(继承自前轮,本实例未复核。)*
6. **`T` 敏感性第 6 条未测**,原则上可推翻依赖它的结论。*(继承自 §6.6,本实例未复核。)*

### 7.3 待办队列(按序)

> **⛔ 队列已按 critic verdict FAIL(2026-08-03)重排。放行条件 = 全做完 + 换新实例复审。**
```
0  1(a) 全库改名「陷波器组在神谕选点下的 ΔMSG(= NHS 上界)」+ 删净"NHS 实测/五条达标"
        ⇒ 我方文件已做(本节);**合同 IF v1.8 / W1A / architect memory 归架构侧,须 lead 路由 + C9**
1  B-2 措辞改写 / B-3 对外第一句三要素                          —— 已做(§7.1)
2  ⭐ bw_oct 1/5 → 1/8 重测 + M-1 等代价平坦衰减对照            —— 提到最前(M-1 提级)
3  M-6 0.5/seed0 按 F36 重测(f_trig=52.0 Hz 却用 T_OBS=6.0,违反我们自己立的 F36)
4  ⭐ 1(b) 自由槽位起振扫描:六条 / f_cut=8k / T_low 默认 / T_OBS 按 F36 随 f_trig 放大
        ⇒ **这才是"NHS 自己能拿到几 dB"。依赖第 3 步先把 T_OBS 定法摸清。**
5  M-4 is_howling 与 set_coef 各一个 broken 版,证明 r57 §2 会 FAIL
        ⚠ `nhs.py:229` 的 B1..B14 开关框架本轮完全没用
6  M-5 cosource_check.py 覆盖面(:60 只覆盖 r45/r38,承重件 r51/r52/r55/r57/r59 一件没进过)
        + 加结构断言:**神谕的 N(ω) 不得取自闭环正在用的 slot 对象**
7  M-2 分层报:T60=0.2 → 6.50(n=3) / T60=0.5 → 4.83(n=3);达标率写 ≥4dB 五条 / ≥5dB 四条
8  M-3 H7(k=8→k=1)补跑
9  F35+P-1 合并为「NHS 作用域边界」合同件(带限指标位留 [L4/待定] 空着)
10 SD 仪表化(式 111;w_ERB 缺 ANSI S3.5 表,须显式记为偏离)
11 判据组合修 OR 旁路 + PAPR 门(须先有 ROC 基线)/ 低频侧 F35 处置
```
**⚠ 第 2 项为什么必须两端一起谈(lead 裁定 A)**:F35 与 P-1 **不是两个盲区,是同一个作用域缺陷
的两端**;`r57` 已直接证明 —— **堵住高频那端,低频那端立刻接管成为封顶**。
⇒ 分两份合同会出现"高频签了、低频还在评估"的状态,**而那个状态在物理上等于什么都没做**。

### 7.4 ⚠ 接手者最容易踩的三个坑(本轮实证,每条都真踩过)

```
① 任何 dB 数先问【频带口径】
   带内 vs 全带差 4–6 dB,曾被误读成"实现损失"好几轮。
   ⇒ 中位数错了看得见;极值错了看不见 —— 因为它换的是一个【频点】,不是一个【数】。
② 任何 ΔMSG 先问【duck 消融了没有】
   未消融时兜底会冒充陷波收益:实测 4.00 dB 里 3.9 是兜底,陷波只有 0.16。
   ⇒ 消融手法:alg.duck_gain = lambda: 1.0(只切音频施加,状态机照跑)。
③ msg_meter 的 nfft 不要调小
   2^15 偏 +0.26 dB 且【方向偏乐观】⇒ 会把数值误差读成算法收益。默认 2^18 是实测收敛点。
④ T_OBS 不是全局常数,须随【最低关注临界点】的 1/f 放大(F36)
   ⇒ 凡报 MSG,除 T_OBS 外还须报该次起振的 f_trig —— 否则无法判断这个数收敛没有。
   实证:带内点(数百 Hz–7.6 kHz)6s 就收敛到 ≤0.28 dB;
         22.3 Hz 的点 6s 下【根本不是赢家】,要 24s(mk 残差 0.42 → 0.12 dB)。
   物理直白:建立速度 ∝ 每秒往返次数,低频点每秒能长的 dB 少。
```
> **⭐ F36 的形态(比 D6 原来的形式深一层,请连这段一起继承)**:
> **`T_OBS` 早就在工作点向量里,但"它够不够"取决于 `f_trig`,而 `f_trig` 不在向量里。**
> **⇒ 向量的完备性不只是"有没有列全参数",还包括
> "【每个参数的充分性判据】所依赖的量有没有列全"。**
> ⇒ D6 原形只管"改这个参数,这个数会不会变";本条追加一问:
> **"这个参数取的值够不够?判断它够不够,又要看哪个量?那个量在向量里吗?"**

### 7.5 件清单(全部 `01_design/prototype_W1P/`,sha256[0:12])

| 件 | sha12 | 回答了什么 |
|---|---|---|
| `msg_meter.py` | `a0c16fd22b29` | 瞬时 MSG 表(**接口恒返回带内/全带两列**,取代起振检测器);含 `margin(t)` 与 HOP/TRI |
| `clrig.py` | `8ad47ce8d260` | 闭环台架(未改动) |
| `howl_detect.py` | `fd63e901f2d8` | 起振判据(未改动) |
| `nhs.py` | `706b658842d8` | NHS 算法权威源(未改动) |
| `r51_loop_tf.py` | `b8e616f15003` | **刀 3**:实测环路传函 ⇒ 陷波在环内(−18~−23.7dB);实测对解析 0.0000 dB |
| `r52_closedloop.py` | `b0174b6d29d2` | r50 扫描复现 + 起振仪表 ⇒ 检测器测的是**全带** MSG(≤0.28dB) |
| `r53_msgmeter.py` | (见 `_out`) | MSG 表验证:A 0.000 / B 0.00 / C 网格收敛 / D 变异 5/5 |
| `r54_seed0_anomaly.py` | `6291db6f431f` | seed0 例外归因 ⇒ 混叠幻峰 1468.2Hz + `g_duck` −5.98dB |
| `r55_duck_ablation.py` | `97da5a9d43b1` | duck 消融 ⇒ 4.00→0.00;六条 |Δ−全带神谕| ≤0.34 |
| `r56_ablation_clean.py` | `7f68ea9ab86b` | **消融洁净度独立复核**(我自己的证伪条件被触发后补测) |
| `r57_bandlimit.py` | `f45cf2a1d48f` | 带限扫描 = **P-1 敏感性曲线**;BL-1 前置自检 24/24;§2 给出 5.67 dB |
| `r58_freeslot.py` | `89c9baf20690` | **空槽工况**(产品实际) ⇒ 错误动作表(7.1③) |
| `r59_ftrig8k.py` | `5504e3bf53a1` | f_cut=8k 的**实测** f_trig ⇒ 六条全 ≤8kHz,五条距解析 0.1–0.5Hz |
| `r60_seed51_finestep.py` | `96f383a14132` | Hk 唯一失败条实测查清 = 分辨力 + 低频建立时间(F36) |
| `r49_exclusion.py` | `c4d62a048e6a` | 三臂选点(**标签已勘正**,数字零变化) |
| `r50_out_ANNOTATION.txt` | — | r50 表的标注件(**一版一文件,不原地改 r50_out.txt**) |
| 预注册 | — | `PREREG_r51/r52/r57_r58/r59` + `r54/r55/r56/r60` 各自文件头的预注册段 |
| 结论件 | — | `FINDINGS.md` 的 **F32–F36 + P-1** 段;本节 §7 |

**每个 `_out.txt` 都带 `deps:` 行。**  全部 [L2/宿主仿真]。

---
## 8. ⭐ 断点(2026-08-03 晚,adaptive-dsp **第 5 实例**)—— B-1 已答;瓶颈已定位到 `_allocate`

> **本节自成一体。** 工作目录 `01_design/prototype_W1P/`。**未 commit,未过任何门。**
> 一句话:**B-1 答了(0/6 达标),而追因把瓶颈一路推到了 `nhs.py:891` 的一行硬截断上。**

### 8.1 ⛔ 唯一未决:**B-1 需在标称电平上【重测】—— 它是「有效但不适用」**

```
B-1 实测(NHS 自选,兜底消融)  1.00 – 2.50 dB    0/6 达标(目标 4–5)
上界(神谕选点)                3.00 – 7.50 dB
⇒ 差 1.5 – 6.0 dB,全部在【选点】
```
**⚠ 而该数是在 `src_rms = −60 dBFS` 上测得,标称输入为 `−20 dBFS`(低 40 dB)。**
```
B-1 那个数,在它自己的 regime 里【有效】        ← 测量本身没问题
但产品工作在 −20 dBFS ⇒ 【另一个 regime】       ← 实测:−40 以上噪声底开始越门(49 → 359)
⇒ 正确表述:**有效但不适用**
⇒ ⛔ 不是"补一句限定就完事",是**需要一个【标称电平上的新数】**
⇒ 而那个新数**不是旧数的修正,是另一个工作点上的另一个测量**
```
> **⇒ ⛔ 接手者注意:B-1【没有】收口。`r75` 正在跑的就是这一步。**

**另一层(须与上一层分开呈报,否则修完实现会有人以为"应该全达标")**:
**上界那一列本身就有 1/6 低于 4 dB(`0.5/seed0` = 3.00)、2/6 低于 5 dB**
⇒ **即使选点完美,也不是每个房间都达标** ⇒ 这一层**修实现解决不了**,是
「8 槽 × 该带宽 × 该房间」的能力上限。

### 8.2 已确定、可引用的(带限定)

| # | 结论 | 出处 |
|---|---|---|
| 1 | **修法就在 `nhs.py:891` 的排序键**:`out.sort(key=(不是PANIC, −b))` **没有"是否已挂陷"项** ⇒ 已挂峰的复检只要斜率高就挤掉未挂的新峰。修法 = 插入 `_notch_covers(f)` 布尔秩,**零新阈值**(谓词是既有的 `:711`) | F55 |
| 2 | **供给恒为 1**:`return out[:1] if ... else out[:2]` 是**硬截断** ⇒ 每槽只有一个 howl 名额 ⇒ 一次对已挂峰的复检就吃掉它 | F52/F55 |
| 3 | **`_allocate` 开环下令**:加深 = 每 howl 一步 −3 dB **无速率限制**,而执行以 60 dB/s 爬(一步 50 ms)、分析槽 16 ms ⇒ **过驱 3.1×**,下令时上一步平均还差 **4.08 dB** 未执行 | F51 |
| 4 | **饿死机理**:`g_duck` 提前介入 ⇒ 神谕 top2/top3 的旁链电平**超 `T_low` 的比例 9.9%/4.5% → 0.0%/0.0%** ⇒ **不是"新分配被拒",是"没有对象"** | F52.3 |
| 5 | **⭐ `T_low` 死区**:`−70/−60/−50` 三档过门数**完全相同(49)**,到 −40 才跳到 359 ⇒ **`T_low=−45` 既不筛啸叫峰也不放噪声底,落在【什么都不做】的区间** | F57 |
| 6 | **三个绝对门全是拍的**:`T_low`(字典 −50 vs 实现 −45,分叉已由架构侧裁定为 **−45**)、`T_low_gr=−65`(明写 `[L4/待 ROC 标定]`)、`T_panic=−6`(**且出厂默认 OFF**) | F56 |
| 7 | **标称输入 = −20 dBFS**(`+4 dBu` + 20 dB headroom)`[L4/行业惯例,待 CTO 追认]`;`T_low=−45` 与它是**绑定对**,CTO 改标称则须同步重判 | 架构侧裁定 |
| 8 | **`rapid_onset` 死门**:门 18 dB vs 实测 max 10.44 ⇒ **0/1692**;下调到 3.0 触发 165 次而 **ΔMSG 48/48 行逐格不变**,一条种子上反而变差 | F42/F43/F46 |

> **⇒ 5 的形态请记入"惰性机制"族第三种**:`rapid_onset` 与 PHPR 是【从不动作】;
> **`T_low` 是【动作了但不产生区分】** —— 更隐蔽,因为它的计数器不是 0。
> **⇒ 而我们花了整整一轮去查它的来源、闭它的分叉、定它的值,而它在当前台架上根本没在做任何选择。**

### 8.3 ⛔ 已撤回 / 已更正(**不得继承旧版**)

| 撤回项 | 提出者 | 更正 |
|---|---|---|
| 「惰性机制是系统性形态」 | lead | **不成立**:清点做全后只剩 `rapid_onset` 一条真·门太严(F54) |
| 「R_RISE 呈双峰 + 存在平台」 | lead | **不成立**:18 个事件全在**一条种子、一个增益点**,`[6,10)` 空档是 n=1 产物 |
| 「M-1 已由 r61 结算」 | **我** | **错**:r61 三口径全是**功率侧**,而 M-1 的对齐口径应是**固定 ΔMSG 比 SD**(F47.1) |
| F54 判 PANIC 为「台架标度产物」 | **我** | **判轻了**:还有第二条独立成因 —— **出厂默认 OFF**(F56.2) |
| 清点表的 `eval` 分母 | **我** | **错 5/7 行**:`eval` 必须是【该分支判定点被到达的次数】,不是外层调用次数(F54.1) |
| 「B-1 补一句限定即可,不必重测」 | **我** | **错**:标称在另一个 regime ⇒ **有效但不适用 ⇒ 必须重测**(见 8.1) |
| `R = 0.32`(摘掉 PAPR 过门反而变少) | **我的探针伪影** | ⛔ **不得引用**;正确值 **×9.61**(F43.1) |
| 「加深与新分配不竞争」 | **我** | **错**:确实竞争,资源是**每槽唯一的 howl 名额**(F52.1) |

### 8.4 待办队列(按序)

```
0  ⭐ r75 出数 → B-1 在 −20 dBFS(标称)上的新数     ← 正在跑,唯一未决项
1  _allocate 修法的诊断性反事实(prefer_unnotched)  ← r75 同批已含,标「非提交修法」
2  ② _papr_jaes:重实现可,但【门必须在我方素材上做 ROC 标定】——文献门值不可移植
   ③ _pnpr_jaes:可达(翻转 19477/68281,严 2.2×),但**修完 _allocate 再测才可迁移**
3  M-1 重做:判据改为【固定 ΔMSG,比 SD】(r61 的功率口径回答不了它)
4  bw_oct 1/5→1/8:⚠ **一次改两个变量**(滤波形状 + 分配匹配窗 `BW=max(f·bw_oct,15Hz)`),须拆开
5  SD 跨带宽比较须先过 `sd_meter.cross_bw_comparable` 闸门;`1/12@200Hz` 属不可比档
6  ROC 素材缺口(语音/音乐)—— 卡住 ②的门标定 / SD 真实信号验证 / CTO 试听,lead 已合并上报
```

### 8.5 ⚠ 三个坑(每条都真踩过)

```
① **文件存在 = 跑完**。r61/r74 都在中途被中止,**只有日志没有 out 文件**
   ⇒ `r61_run.log`(28/36 行)与 `r74_run_ABORTED_range_superseded.log`(未产出 out)
     **均不得当结果引用**。r61 头部已加事后标注横幅。
② **跨档比较混着两个效应**:源电平扫描里,「三个绝对门同时平移」与「扫描终点 G 平移」并存。
   实证:源 −70 时 ΔMSG 反而比 −60 高(5.00 vs 1.50)—— **不是 bug,是终点效应**
   (固定 G 下过门率单调不减 0.82→8.88%)。**⇒ 跨档比较必须用【固定 G】的表来拆。**
③ **`src_rms_dbfs` 与 `cal_offset_db` 从未进过任何一轮的工作点向量。**
   架构侧原话:`src_rms_dbfs` **「正是护栏 A 该拦而没拦到的一维」**;
   `cal_offset_db`(`nhs.py:236`)默认 0.0 **从未被用过** —— 它是"端子 dBu → 内部 dBFS"的映射位,
   已进 D14 bring-up 清单。**⇒ 两维现已入向量,此后不得再漏。**
```

### 8.6 件清单(全部 `01_design/prototype_W1P/`)

```
预注册  PREREG_r64 / r66 / r67 / r75            (r64 含 §9/§10/§11 三次更正,§0–§8 未改)
测量    r64(B-1 主表)/ r65,r65b(护栏)/ r66a,r66b,r66c,r66d / r67(R_RISE)
        r70(门控+下游)/ r71(耦合量化)/ r72(分配规则)/ r73(惰性清点)/ r75a(逐位等价)
判读件  r64_out_ANNOTATION / r65b_out_ANNOTATION / r66b_out_ANNOTATION / r66c_out_ANNOTATION
        r66d_out_ANNOTATION / r67_out_ANNOTATION(+_bottleneck)/ r73_out_ANNOTATION
结论    FINDINGS.md **F40–F57**
纪律    team_config.md **D6-y / D6-z / D6-aa / D6-ab / D6-ac**
台账    MECHANISM_VERIFICATION_LEDGER.md 1b/1c 行 + 纪律 4/5
代码    nhs.py 两处**默认关**开关:`growth_and_gate`(r66a 证逐位等价)、
        `prefer_unnotched`(r75a 证:Hq5 24/24,阳性对照 9/24)
        哈希链 706b658842d84316 → 31decc8e8d07e085 → **a6b467df127e741f**
        ⇒ **行为逐位不变已证 ⇒ 既有归档失效集 = 空**,但所有 `deps: nhs.py@…` 行须更新
```
**全部 [L2/宿主仿真],未经 critic 评审,未 commit。**

---

## ⚠⚠ 实例名单变更(2026-08-04,lead 维护)

**⇒ 本节为最新,覆盖 §5.9 与 §24 的一切旧表述。**

| 实例 | 状态 | 备注 |
|---|---|---|
| `adaptive-dsp-6` | ⛔ **已退役**(2026-08-04 约 13:20 发现不可达) | 交付至 F78 / r86;**⛔ 不得再向此名发消息**(F-18 / DEC-0011:会复活 ⇒ 裂脑) |
| **`adaptive-dsp-7`** | ✅ **现役算法侧** | 窄任务上岗:只跑 `recheck_free` 的 ΔMSG 验证扫描 |
| **`channel-dsp`**(第 1 实例) | ✅ **现役** | D3/D4 设计 + C 化 + critic verdict 整改 |
| **`critic-w1c`** | ✅ **现役独立守门** | 已出 D34 定点约定 verdict(**FAIL**);正评 D3/D4 |
| `system-architect`(`a238950bec22a905b`) | ⏸ 休眠可唤醒 | 延迟配平 / 动态范围 / 移频器插入点已交 |
| `adaptive-dsp-3/4/5` · `critic-w1p` · `sd-tool` · `rir-fixer` | ⛔ 已退役 | ⛔ 不得发消息 |

**⚠ 退役是【发现】的,不是【计划】的**:`adaptive-dsp-6` 连续四次消息交错、最后一次批准的扫描始终未起跑,lead 起初判为"空等",**实为实例已结束**。
> **⇒ 教训:「agent 空闲」与「agent 已死」在 lead 这一侧看起来一样 —— 都是"没有新文件、没有进程"。**
> **⇒ 判别法只有一个:发一条消息,看它是否可达。⇒ 而不是从文件系统推断。**
> **⇒ ⇒ 因此:凡"该动而未动"超过一轮,先发消息探活,⛔ 不要继续重发指令。**

### ⚠⚠ 实例名单再变更(2026-08-04 约 13:50)—— **一轮探活查出两个已死**

| 实例 | 状态 |
|---|---|
| `channel-dsp`(第 1 实例) | ⛔ **已退役**。交付至 D34 定点约定 / D3D4 链序 / `02_impl` C 第一批 / `check_gates_fire.sh`;**critic verdict 的整改未完成** |
| `critic-w1c` | ⛔ **已退役**。交付 D34 verdict(FAIL);**D3/D4 那份未出** |
| **`channel-dsp-2`** | ✅ 现役 —— 接整改 + C 第二批 |
| **`critic-w1c-2`** | ✅ 现役独立守门 —— 接 D3/D4 + `02_impl` 评审 |
| `adaptive-dsp-7` | ✅ 现役(探活可达) |

> **⭐ 探活规则第一次生效:一轮三条探活,查出两个已死。⇒ 若按旧做法继续重发指令,会再损失一到两小时。**
> **⚠ 而三个实例(`adaptive-dsp-6` / `channel-dsp` / `critic-w1c`)**都没有留交接件**。**
> **⇒ ⇒ 立为常规:**每个实例上岗时即建交接件,并随进展更新**;⛔ 不是"退役前写",因为退役是【被发现的】不是【被计划的】。**

---

## 9. ⭐ 断点(2026-08-04 午后,adaptive-dsp **第 7 实例**)—— W1-P 收口件在写

> **⛔ 门禁状态:全部未过 critic、未 commit。全部 [L2/宿主仿真]。**
> **本节在动笔前落盘,每完成一件即更新** —— 2026-08-04 已死三个实例、无一留下交接。

### 9.0 本实例已完成(可直接引用其件)
```
r87  `recheck_free` 的 ΔMSG 验证扫描  ⇒ FINDINGS **F79** / `r87_dmsg_out.txt`
     结论 = **深度全部达标,而 ΔMSG 一格未涨**(主列 0/6 变好、1/6 变差 −0.50)
r86a `recheck_free` 逐位等价 + 阳性对照 ⇒ FINDINGS **F80** / `r86a_bitexact_out.txt`
     ① 30/30 逐位相同 ② 阳性对照 17/30 出现差异 ⇒ `nhs.py@b77a0524669386af` 已入 VERSIONS 链
     ⚠ 缺的是**三环**(c2eb9bef→57ced119→47a57d21→b77a0524);中间两版**未单测**
```

### 9.1 在写的件(lead 派:W1-P 收口件,⛔ 只做合成不做新测量)
```
目录 `01_design/W1P_CLOSEOUT/`
  ① 01_NHS_numbers.md        数值报告(核心结论 + 限定 + 逐种子表 + 天花板来源 + 120–280 Hz)
  ② 02_self_set_constants.md 自定常数台账(值 / 出处 / ⭐ 改了会不会改变已报结论)
  ③ 03_open_questions.md     未答清单(明写这一阶段不答什么)
状态:见下方【进度】行,每完成一件就改
进度:① ② ③ **三件全部已落盘**(2026-08-04 午后)⇒ 本派单完成,已报 lead
```

### 9.2 ⛔ 写这三件时必须守的(lead 写死)
```
(a) ⛔ 不得出现任何未被实测支持的乐观表述
    尤其:「深度全部达标」为真,而它换不来 ΔMSG —— **这两句必须挨在一起写**
(b) 必须把【天花板的来源】写清楚(CTO 路线决定的依据):
    序统计量 top1−top(NN+1) / 加陷波数 16 饱和 / 开窄陷波撞低频物理墙 / 压更深公式里没这一项
    ⇒ 剩下唯一杠杆 = 选点,且只在部分种子上有空间
(c) `120–280 Hz` 单列一节:五条互相独立的限制指向同一频段
    ⇒ 并写入 lead 的**待验推论**(低频模态少/尖/稳 ⇒ 装机时一次性固定陷波,PRD §五.2 已有该功能)
    ⛔ 标明它是待验推论不是结论
⇒ 有任何一句写不出实测支撑的,就写「未测」。
```

### 9.3 收口件已交(三件全部落盘,未过门)
```
01_design/W1P_CLOSEOUT/01_NHS_numbers.md         数值报告(含 §4 天花板来源 / §5 120–280 Hz 单列)
01_design/W1P_CLOSEOUT/02_self_set_constants.md  自定常数台账 **26 项**,9 项会翻结论,其中 6 项无来源
01_design/W1P_CLOSEOUT/03_open_questions.md      未答清单 A/B/C/D/E 五类
```
**头条(逐种子,⛔ 不得只引中位)**:`src=−20 dBFS(标称)` / `T_OBS=12 s` / `bw_oct=1/5` / `NN=8` /
**环路 8 kHz 带限** / duck 消融 ⇒ **ΔMSG ≥ 6.00 / 2.00 / 4.00 / 2.50 / 2.00 / 2.50 dB**(中位 ≥2.50)。
**≥4 dB 2/6;≥5 dB 1/6。** 写「≥」是因为 m0 臂在读数点上仍在长(偏高约一个步长 0.50 dB)。

### 9.4 ⭐ 下一个实例最该先读的三件事
```
① 天花板不是实现缺陷:ΔMSG ≈ top1 − top(NN+1) 是**序统计量,公式里没有深度项**(F71/F79)
   ⇒ 四条判据/分配层修法**全部零收益**(growth_and_gate / R_RISE / prefer_unnotched / recheck_free)
   ⇒ ⛔ 不要再修判据层,除非先证明它改的是【选点】
② 剩下唯一杠杆 = **选点**,缺口逐种子 0.00–4.50、中位 2.75 dB,且**只在部分种子上**
   ⚠ 而"上界"这个名字只在 bw_oct=1/5(超预算 1.3–2×)上成立;1/8 及以下有 5/30 格倒挂
③ **设计实验前先核结论库**(memory.md 顶部第四件)—— r87 的负结果本可由 F71 预见
```

### 9.5 收口件 r2 修订(2026-08-04 15:xx,同实例;critic MAJOR-3 + lead 追加)
```
① Schroeder 常数定案依据 = **一手原文逐字 + 原文自带算例自校验**(booth 14 m³/0.34 s → "about 600 cps";
   k=4000 给 623 ✅,k=2000 给 312 ⛔)⇒ 常数 4000、V 单位 m³
   ⛔ 而【不裁定】架构侧的 k=2000 —— 库内无那一版原文,不得据记忆定性(DEC-0011)⇒ 立为未答项 E-6
② 「少数性」冲突:架构侧 W1A:1642(且已进**冻结合同 IF-v1.8:366**)判「少数性不成立」——
   我方用同一 Weyl 式独立复核**支持它**(120–280 Hz 内 126/210/630 个模态,重叠 M 1.2–14.8)
   ⇒ 建议**不撤销、换频段**:真正孤立模态区 ≈54–110 Hz,整段在 NHS 盲区(<120 Hz)之内
③ §5 标题改为 `f_low … f_c(V,T60)` + V 敏感度表(k=4000 跨 103–365 Hz)
④ 限定焊死四处:§4 结论句 / §6 汇总表 / ②N-8 / §4「压得更深」行
⑤ ⭐ 新增 §5.3 收尾(lead 追加):该段**不是"待验",是【本台架原理上验不了】** ——
   台架按构造即统计模型,在它上面数临界点只会复现其自身假设 = 循环论证
   ⇒ 三种手段作用域已分清(Weyl 解析可用 / 台架数临界点不可用 / 真实房间实测 = 唯一手段)
   ⇒ 同步改 ③B-3 与 ③C-2①
```
**⇒ 下一个实例注意**:`120–280 Hz` 这个旧标题**已作废**,凡见到一律按 §5.0 的 `f_c(V,T60)` 读。

### 9.6 r88(非统计 plant 对照)—— **闸门未过 2/6,主对比没有启动**(2026-08-04 15:43)
```
件:PREREG_r88.txt / modal_rig.py / r88_gate.py|_out.txt|.json / r88_cell.py / r88_merge.py / r88_launch.sh
G2 配置断言 8/8 过;**G1 器械能力空测 2/6(门 ≥4/6)⇒ exit(1) ⇒ 六个 cell 一个都没起**
拆开:密度判据单独 4/6 过 ｜ σ_dB 判据单独 3/6 过 ｜ **两者 AND 只剩 2/6**
⛔ 两种读法我分不开,已如实并报 lead(a3d7c2a7),等裁:
  A 真实物理:T60=0.2 ⇒ 模态带宽 11 Hz ⇒ 连 14 m³ booth 在 120–280 Hz 都已重叠成统计场
  B 器械噪声:统计 plant 自己的 σ_dB 跨六种子 4.99–6.41(极差 1.42 dB)> 我的门 1.0 dB
⛔ **不得回去把门放宽再跑** —— 那是"停止规则只在舒服的时候执行"
⇒ 会议室 100 m³ 的同组统计(**非闸门,是结果**):Δσ 与密度比**均变号** ⇒ 无一致方向差别
  ⚠ 但阳性对照没过 ⇒ 「无差异」与「判别量看不见差异」**当前不可区分** ⇒ ⛔ 不得下 B-1 结论
```

### 9.7 收口件 r3 修订(critic MAJOR-2 / m-1 / m-2 / m-4 + E-6 改写)
```
m-1 §1 headline 补「duck 消融」并说明它为什么是句子的一部分
MAJOR-2 「≥」贴到**每一个 ΔMSG 列**(三列共用同一 m0 ⇒ 同向低估);
        而「选点缺口」列是两个 ΔMSG 之差 ⇒ m0 代数抵消 ⇒ **不带「≥」**
m-2 ②§0 改为「8 行(9 个数)/ 其中 6 行(7 个数)无来源」—— 行与数分开写
m-4 「54–110 Hz」⇒ **31–110 Hz(V 全网格 60–300)**;并补适用边界:
    上沿 110.3 Hz 距 120 只剩 9.7 Hz;**V ≲ 51 m³ 时两段重叠 ⇒ 干净结论失效** ⇒ 适用域 V ≳ 51 m³
E-6 **定案条件改写**:M = 4π·k²·2.2/c³ **只依赖 k**(k=2000⇒M=2.74;k=4000⇒M=10.96)
    ⇒ 两常数是同一曲线上两个阈值,不矛盾 ⇒ 要裁的是**我们设计需要的 M** ⇒ ⛔ 不需要 1996 那篇
    ⇒ 归架构侧 + C9(已复核 critic 的推导,成立)
```

### 9.8 r88b(换判别量后重做)—— **闸门过了,而结果是伪影;已作废**(2026-08-04 16:2x)
```
闸门(解析先验 M(f))✅ 过:阳性对照 V=9.01 m³ 全频点 M<1;主对比臂 M(120)>1
主对比跑完 ⇒ ⛔⛔ **ΔMSG 全列作废** —— 模态臂 9/9 格 f_trig 挤在 f_cross=600 Hz 接缝下方(471–595)
成因实测:低/高两段各自归一化 ⇒ 低段谱密度高 **16.66 dB** ⇒ 最高临界点被搬到接缝
⭐ 而这条措施正是我在 PREREG_r88 §1-③ 为**防止** f_cross 变成隐藏增益旋钮而加的
⇒ FINDINGS **F81** / 判读件 `r88b_out_ANNOTATION.txt`;数已改名 `DMSG_r88b_ARTIFACT_勿用`
⇒ 按 lead 硬边界:**不修不追**,Q2 并入「须实测真实房间」(收口件 ③B-3)
✅ 唯一站得住:M(f) 解析结论 —— 整段 M<1 需 V<3.7/9.3/18.6 m³ ⇒ 现实房间无一满足
   ⇒ 那一段永远是过渡区 ⇒ **Schroeder 常数之争没有物理内容**(E-6 已按此改写)
```

### 9.9 ⛔ 撤回「≥」+ modal_rig 改名(2026-08-04 16:3x,同实例)
```
① `modal_rig.py` → **`modal_rig_BROKEN_seam16dB.py`**(lead 令:警告进**文件名**,不能只在文件头)
   旧名留作 **raise ImportError 的桩**(import 即响,指向 F81);r88/r88b 四个证据件已改引用、仍可复现伪影
   ⇒ lead 原话:文件头就是段落,而下一个实例 import 时不会读文件头
② ⛔⛔ **「ΔMSG 应写 ≥」全线撤回** —— 前提被证伪(FINDINGS **F82**):
   m0 ≡ 解析 anchor 六格差 **0.0000**;「偏高 0.50」是 r83 那把尺子的**最小非零输出**
   ⇒ 现行口径:ΔMSG 是**栅格点估计**,⛔ 既非下界也非上界
   ⇒ 传播:F76.3 / F77.4 / F79.5 三处已加标(数不删);收口件 §1/§1.1/§2 已改
   ⚠ 与 critic 判「F79.5 方向正确」**不冲突**:critic 核的是推论,verification 打掉的是前提
```

### ⚠⚠ 花名册补漏(2026-08-04):`verification` 一直不在表内 —— 而它有过一个前任

**⇒ 这正是 F-18 / DEC-0011 防的形态,而它在本表上存在了三天。**

| 实例 | 状态 | 备注 |
|---|---|---|
| `verification`(2026-08-01 那个) | ⛔ **已退役** | 产出 `04_platform/W1C_microbench/` CCES 工程(4 内核 + 内存自查)+ `README_WINDOWS` + `PROVENANCE.md`。**⛔ 不得再向该名发消息** |
| **`verification`(2026-08-04,现役)** | ✅ **现役** | r76 / F71 独立复核 + D14 三件;**⛔ 其自称"第 1 实例"有误,已标存疑** |

> **⚠ 而 lead 派单时写「本角色首次上岗」—— 错的。⇒ 现役实例差一点重造了 W1-C 微基准那个轮子。**
> **⇒ 是它自己去读 `PROVENANCE.md` 首行才发现的,⛔ 不是 lead 告诉它的。**
> **⇒ ∴ 教训:名单表【漏一行】与【写错一行】危害相同 —— 而漏行更难发现,因为它不会与任何东西矛盾。**

**⚠ 另记:`knowledge_base/adsp21569/platform_lessons/bringup/ezkit_bringup_checklist.md` 的产出方写的是 `hardware-design` teammate —— roster 八角色里没有它。结合 `INVENTORY.md` 首行「源:ITC 项目」⇒ 那份清单是从另一个项目搬进来的,⛔ 不是本项目产出。**
