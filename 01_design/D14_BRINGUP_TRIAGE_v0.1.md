# D14-③ · bring-up 阻塞分诊 + C10 三件套现状 v0.1

```
⛔ 门禁状态:未过门 —— 未经独立 critic 评审、未 commit、⛔ 不得据本件指导任何物理动作
作者:verification / 2026-08-04   上游派单:lead
⛔⛔ 本件【不是】上板清单。上板清单已存在(见 §2),本件是它的**分诊与门禁核对**。
    ⇒ 我不重写它(LESSONS A-2:先确认它不是已知的)——我核它**够不够解闸**,答案是【不够】。
```

---

## §1 ⛔ 先更正三条事实(⛔ 都与派单转述不符,如实报)

### 1.1 ⚠ 本角色**不是首次上岗** —— 盘上已有 `verification` 的产出

```
`04_platform/W1C_microbench/PROVENANCE.md` 开头逐字:
   「产出方:`verification` teammate(首次上岗)· 2026-08-01」
⇒ 该实例产出了:W1-C 微基准 CCES 工程(4 内核 + 内存自查)、`README_WINDOWS.md`(给 CTO 的操作手册)、
  以及 `PROVENANCE.md`(三类来源逐类交代,含逐字节 diff 校验记录)
⇒ ∴ 派单说的「roster 里一直有它,而从未 spawn 过」**与盘面不符**
```
**⇒ 而更要紧的是:`W1_HANDOFF.md §0` 的实例名单表里【根本没有 verification 这一行】**
——既没列现役,也没列退役。而 `CLAUDE.md` 明写「**现役名单只以 `W1_HANDOFF.md` §0 表为准**」。
```
⇒ ⇒ 这正是 F-18 / DEC-0011(裂脑)防的那个形态:
   ① 后来者读名单 ⇒ 不知道有过 verification 实例 ⇒ 重复造轮子(我差点就是)
   ② 有人向 `verification` 发消息 ⇒ 可能复活一个已退役实例
⇒ **请 lead 补该行,并注明 2026-08-01 那个实例的状态(现役?已退役?)。**
⇒ ⚠ 我把自己的交接件写成了「第 1 实例」—— **那是我照派单写的,现已知有误,待 lead 定名后我改。**
```

### 1.2 ⚠ 上板清单**已存在**,而它的产出方是一个**不在本项目 roster 里的角色**

```
件:`knowledge_base/adsp21569/platform_lessons/bringup/ezkit_bringup_checklist.md`(284 行)
头部逐字:「产出方:hardware-design teammate ｜ 任务等级:P0」
⇒ `hardware-design` **不在 CLAUDE.md §2 的 roster 里**(roster 八角色无此项)
⇒ 结合 `INVENTORY.md` 首行「源:**ITC 项目** knowledge_base/ezkit/bsp/」
   ⇒ ∴ 该清单是**从另一个项目搬进来的**,不是本项目产出
```

### 1.3 ⛔ 「bring-up 卡在仿真器连不上」—— **盘面没有任何证据支持这句**

我全库检索 `仿真器 / ICE / JTAG / 连不上 / USBi`,**在本项目文件里的唯一命中是我自己昨天写的那一行**
(已更正)。盘面**实际**记录的是**两个互相独立**的阻塞,而**都不是"连不上"**:

| # | 盘面原文 | 性质 |
|---|---|---|
| 阻塞 A | `W1_HANDOFF §0` 第 5 项:**「EZKIT 实板是否存在」= 事实确认** | **我们不知道有没有板** |
| 阻塞 B | `W1_HANDOFF §0` 第 9 项(2026-08-01 lead 实查):`/opt/analog/cces/2.12.1` 存在(2.1G),但**全树无 `cc21k`/`easm21k`、无 SHARC 目录、无 21569 支持包、未见 license** ⇒ **CCES 模拟器分支(L2)当前不可跑**;**与第 5 项相互独立** | **沙箱内没有 SHARC 编译器** |

```
⇒ 「连不上」是一个**比这两条都靠后**的状态:要先有板(A)、先有工具链(B),才谈得上连
⇒ ∴ 除非 CTO 在会话里说过而未落盘,否则**当前阻塞不是"连不上",而是"有没有板 + 有没有编译器"**
⇒ ⛔ 这个区别不是措辞:两者的**解法完全不同**(A=问/买硬件;B=装软件+许可证)
```
**⚠ 而这与 DEC-0006 有一处需要 lead 裁的张力**:
```
DEC-0006(2026-07-30,LOCKED,CTO 拍板)写:「工具链 **CCES 2.12.1 + SAM BSP 已在手**」
而 2026-08-01 lead 实查:SHARC 工具链缺失
⇒ 两句**都可以是真的**(CCES 装了,只是没装 SHARC 部分)—— 但 DEC-0006 那句是**为选型背书**的,
  而它现在读起来像"能给 21569 编译",实际不能
⇒ ⇒ 这是「**限定不够**」而不是「限定漏了」—— 与 critic 在动态范围上抓到的同族(v0.61 ①)
⇒ 建议:DEC-0006 那行加一句现状注,归 lead 路由(⛔ 我不改台账)
```

---

## §2 C10 三件套现状核对 —— **结论:闸门【未解除】,且不止差一件**

> `GOVERNANCE 铁律九 / C10`:评估板上电/接调试器/焊接/跳线/烧熔丝前,须
> ①**CTO 确认出稿的操作清单** ②**物理版本肉眼确认**(丝印 REV,禁预设单一版本接线)
> ③**安全硬规矩写入清单** —— **缺一不得指导执行**。

| 件 | 现状 | 判 |
|---|---|---|
| ① CTO 确认出稿的操作清单 | 清单**存在**(284 行,6 项 + 三版差异表 + 逐步可打勾流程),⛔ 而**未见 CTO 确认记录** | ⛔ **未满足** |
| ② 物理版本肉眼确认 | 清单末尾「CTO 版本确认后回填位」**五个空位全是 `________`** | ⛔ **未满足** |
| ③ 安全硬规矩写入清单 | **满足且质量高**:顶部「🔒 安全闸门」明写三个厂商已警告的致命陷阱(Boot 模式抢 JTAG / **JTAG 热插拔烧硬件** / JP1 供电跳线接错),并写死解闸两条件 | ✅ **满足** |

### 2.1 ⛔⛔ 而我核出**第四条,它比上面三条更硬**:清单的**证据基础在本仓不存在**

清单自带「资料出处索引」,逐项挂 文档0/1/2 + 三版原理图。**我逐条查了,本仓一份都没有**:

```
清单引用路径          knowledge_base/ezkit/vendor_docs/tutorials/   ⇒ ⛔ `knowledge_base/ezkit` 目录不存在
                     knowledge_base/ezkit/vendor_docs/schematics/  ⇒ ⛔ 同上
全库 find 三份 tutorials PDF(硬件更新说明 / 初始状态说明 / 在线调试说明)⇒ **0 命中**
全库 find 三份底板原理图(V1.0 / V1.2 / V2.1)                      ⇒ **0 命中**
成因(INVENTORY.md 首行):knowledge_base 是从 **ITC 项目** 复制的,
  而**只复制了 `bsp/` 子树**(278 文件 / 93M),**`vendor_docs/` 没跟过来**
```
```
⇒ ∴ 清单里每一条安全事实(三个致命陷阱、三版差异、跳线位、LED 判据)
  **在本仓都【无法回溯到出处】**
⇒ ⛔ 这不等于"清单是错的" —— 它在 ITC 项目里可能完全正确
⇒ ✅ 它等于:**在本仓,它不满足"可核"这一条** ⇒ 而 C10 的第①件要的正是"CTO 确认出稿"
  ——CTO 拿什么确认?
⇒ ⇒ 且按 **C8 / 铁律六**:外部输入须 ≤24h 入库并标来源 ⇒ **那六份 vendor 文档必须先入库**
```

### 2.2 ⛔⛔ 而还有一条最危险的:**清单描述的板,可能不是我们要上的板**

```
清单标题:「**EV-21569-EZKIT** 物理上板」,正文说的是「**ADSP-21569EVB 开发板**」,
        有**底板 + 核心板**、三版(V1.0 老款无 USBi / V1.2 USBi 单独引出 / V2.1 双A2B 改版)、
        **JP1 供电跳线**、**板载电源开关有无** ⇒ 形态 = 第三方开发板
而本仓 `knowledge_base/adsp21569/bsp/reference_design/` 里放的是:
        `ev-21569-som-design-database.zip` + `ev-somcrr-ezkit-design-database.zip`
        ⇒ 形态 = **ADI 官方 SOM(EV-21569-SOM)+ 载板(EV-SOMCRR-EZKIT)**
⇒ ⇒ **这是两种不同的硬件形态**:SOM+载板没有"底板 JP1 跳线""三版底板差异"这些东西
⇒ ⇒ ∴ 若我们手上是 ADI SOM 套件,**清单的跳线/JTAG/电源事实一条都不适用**
⇒ ⇒ 而按清单自己的 PF-9 防御:**版本误判 = 找错跳线 = 厂商预警丢失** —— 这正是它要防的
⇒ ∴ **在确认"我们到底有哪块板"之前,⛔ 任何物理动作都不得开始**(这与阻塞 A 是同一件事)
```

---

## §3 阻塞分诊(⛔ 按依赖排序,前一条不闭不得进下一条)

| 序 | 阻塞 | 性质 | 解法 | owner | 现状 |
|---|---|---|---|---|---|
| **0** | **我们有没有板?是哪一块?**(ADI SOM+载板 / 第三方 EVB / 都没有) | **事实确认** | CTO 一句话 + 拍丝印照片 | CTO | ⛔ 未确认 |
| **1** | 那块板的 **vendor 文档入库**(原理图 + 上电/调试说明) | 外部输入 | 铁律六 ≤24h 入库 | CTO 提供 → lead 入库 | ⛔ 未入库 |
| **2** | **SHARC 工具链**:`cc21k`/`easm21k` + 21569 支持包 + license | 软件+许可 | 安装/采购 | CTO | ⛔ 缺 |
| **3** | C10 三件套解闸:①清单按**实际板型**重出稿并经 CTO 确认 ②丝印 REV 回填 ③安全规矩(已有) | **门禁** | 依赖 0/1 | lead + CTO | ⛔ 未解 |
| **4** | 上电 → ICE 连接 → Session 建立 | 物理动作 | 依赖 3 | CTO 执行 | 未开始 |
| **5** | 跑 W1-C 微基准,取回周期数 | 测量 | 依赖 2+4 | CTO 执行 → V 分析 | 未开始 |

```
⭐ 而 2 与 0/1 **相互独立**(W1_HANDOFF 第 9 项已明写)
⇒ ∴ **阻塞 2 可以现在就并行推进,不必等板** —— 它一闭,CCES 模拟器分支(L2)立刻能跑
⇒ ⇒ 而按 DEC-0023,现在缺的是【任何一个定点锚点】⇒ **L2 模拟器锚点也是锚点**
⇒ ⇒ ∴ **最短路径不是"等板",是"装工具链"**
```

---

## §4 ⭐ 第一目标改写的后果(DEC-0023)

```
旧:W1-C = 「验证厂家锚点(EE-408 的 2.5 cyc/biquad)在我方代码形态下是否成立」
新:W1-C = 【**取得第一个定点锚点**】
   理由(DEC-0023):EE-408 那个 2.5 cyc/biquad **是浮点数**,而本项目锁定点口径(DEC-0006)
        `MRF = RND MRF 不额外花周期` 亦降 [L4/待 CCES 实测]
```
**⇒ 这对 bring-up 顺序的影响,是本节唯一的实质产出**:

| | 旧目标下 | **新目标下** |
|---|---|---|
| 必须上板吗 | 是(要"我方代码形态下的板上值") | **否** —— 模拟器上的定点周期数**已经是我们现在没有的东西** |
| 最短路径 | 板 → 上电 → 测 | **装 SHARC 工具链 → CCES 模拟器跑 T1a → 得第一个定点数** |
| 该数的等级 | — | **[L2/工具]**,⛔ 非 L1;⛔ 不得进选型/承诺(C9) |
| 它能解锁什么 | — | 让 D13 的算力数**第一次有一手依据**(现在是零) |

```
⚠ 而必须同段写明,⛔ 不得读得太乐观(同族前车之鉴已在册):
  `decisions_log:273` 记「桌面 33×/17× vs 板上 1.32×,**差 ~25× 就栽在这**」
  ⇒ **模拟器值 ≠ 板上值**。它解的是"我们一个定点数都没有",⛔ 不解"预算够不够"
```

---

## §5 待办清单(可打勾;⛔ 每条标前置)

```
[ ] T-0  CTO 答:我们有没有板?是 ADI EV-21569-SOM + EV-SOMCRR-EZKIT,
         还是第三方 ADSP-21569EVB(底板+核心板),还是都没有?          前置:无
[ ] T-1  CTO 提供该板 vendor 文档(原理图 + 上电/调试说明)→ lead 入库    前置:T-0
[ ] T-2  CTO 装 SHARC 工具链(cc21k/easm21k + 21569 支持包 + license)  前置:无 ⭐ 可并行
[ ] T-3  按实际板型**重出稿**上板清单(⛔ 不得直接用 ITC 那份)          前置:T-0,T-1
[ ] T-4  CTO 肉眼确认丝印 REV + 拍照留档 + 回填五个空位                 前置:T-3
[ ] T-5  CTO 确认清单(C10 第①件)                                     前置:T-4
[ ] T-6  CCES 模拟器跑 W1-C(T1a biquad 起)⇒ **第一个定点锚点 [L2]**   前置:T-2
[ ] T-7  物理上电 → ICE Test → 建 Session                              前置:T-5
[ ] T-8  板上跑 W1-C 四内核 ⇒ **第一个 [L1] 锚点**                      前置:T-6,T-7
[ ] T-9  lead 补 `W1_HANDOFF §0` 的 verification 行(§1.1)             前置:无
[ ] T-10 lead 给 DEC-0006「工具链已在手」加现状注(§1.3)                前置:无
```
**⇒ 关键路径上只有 T-2 是现在就能推的。⇒ 其余全部悬在 T-0 那一句话上。**

---

## §6 双极值代入自查(lead 纪律③)

| 判据 | 真该过 ⇒ | 真该不过 ⇒ | 相反? |
|---|---|---|---|
| **G-1 板型闸**:清单描述的板型 == 实物板型 | 实物 = 第三方 EVB,用 EVB 清单 ⇒ **过** | 实物 = ADI SOM,却用 EVB 清单 ⇒ **不过,中止** | ✅ |
| **G-2 出处闸**:清单每条安全事实可回溯到本仓已入库文档 | 文档已入库,页码可查 ⇒ **过** | 出处指向不存在的目录 ⇒ **不过**(= 当前状态) | ✅ |
| **G-3 REV 闸**:回填位无 `________` 残留 | 五位全填 ⇒ **过** | 任一位仍空 ⇒ **不过** | ✅ |
| **G-4 锚点等级闸**:模拟器数标 [L2],⛔ 不得当 L1 | 标 [L2/CCES 模拟器] ⇒ **过** | 标"实测/measured" ⇒ **不过**(C2 红线词 BLOCKER) | ✅ |
| **G-5 顺序闸**:T-7 之前 T-5 必须已闭 | T-5 已闭再上电 ⇒ **过** | 工具链装好了就想先上电试试 ⇒ **不过** | ✅ |

**⇒ 「这个检查失败时会阻止什么?」**
```
G-1 阻止:按另一块板的跳线图接线 —— 清单自己的 PF-9 说这一条会丢掉厂商预警
G-2 阻止:CTO 在一份无法核对的清单上签字(C10 第①件形同虚设)
G-3 阻止:预设单一版本接线(铁律九明文禁止的那件事)
G-4 阻止:模拟器数被当成板上数进 D13/选型(C9 BLOCKER)
G-5 阻止:因为"软件都好了"就跳过物理门 —— 而三个致命陷阱全在物理侧
```
**⇒ G-1/G-2/G-3/G-5 都是【中止条件】。⛔ 失败即停,不得"记下来继续"。**

---

## §7 边界

```
⛔ 本件不指导任何物理动作;⛔ 不替代上板清单;⛔ 未经独立 critic 评审
⛔ 我未接触任何硬件、未跑任何工具链 ⇒ 本件全部为**盘面核对**,无一条 L1
⚠ §1.3 的判断基于「全库检索无证据」—— 若 CTO 在会话里说过"仿真器连不上"而未落盘,
  那么阻塞状态以 CTO 为准,而**那句话需要落盘**(否则下一个实例还会像我一样查不到)
  **⇒ 2026-08-05 已结案:lead 自认该句系其捏造,盘面从无证据。⇒ §1.3 的判断成立,无须改。**

---

## §8 ⛔ 撤回传播(铁律五:声明 + 全库反扫 + 逐处加标/删,三步缺一不生效)· 2026-08-05

**撤回对象**:「W1-C / bring-up 卡在仿真器连不上」——**无证据句,由 lead 于 2026-08-05 自认捏造**。

### 8.1 全库反扫结果(检索式:`连不上 | 仿真器连 | 连接不上 | 接不上 | 仿真器`)

| # | 命中位置 | 判 | 处置 |
|---|---|---|---|
| ⛔ 1 | **`decisions_log.md:527` = DEC-0023 风险声明②**:「而 W1-C **卡在仿真器连接上** ⇒ 该阻塞现在挡的不是"验证",是"我们有没有任何算力依据"」 | ⛔ **真传播,且在台账里** | **归 lead —— 我不改台账**(§8.2) |
| ✅ 2 | `D14_ACCEPTANCE_TABLE_v0.1.md` 仪器表 | 我的初版污染 | **已改**(拆两行 + 更正横幅) |
| ✅ 3 | 本件 §1.3 / §7 | 是**撤回文本本身**(引用该句以否定它) | **保留**,不得删 |
| ○ 4 | `04_platform/W1C_microbench/README_WINDOWS.md:206`「如果第 0 步硬件 bring-up 过程中有任何异常(连不上仿真器、板子灯不对等)」 | **假阳性** —— 是**假设式应急指引**,不是"现在连不上"的断言 | 不动 |
| ○ 5 | `ezkit_bringup_checklist.md:18/22/133/184`(Boot 模式抢 JTAG ⇒ 连不上 DSP) | **假阳性** —— 描述的是**厂商警告的陷阱机理** | 不动 |
| ○ 6 | `GOVERNANCE:41` / `PLAN v0.2:331`(「接仿真器」作为动作名) | **假阳性** | 不动 |

**⇒ 真传播 1 处,已纠 1 处,假阳性 3 类。**

### 8.2 ⛔⛔ 而反扫查出一处 lead 也不知道的:**那句已经进了台账**

```
`decisions_log.md:527` DEC-0023 风险声明② 里写着「W1-C 卡在仿真器连接上」
⇒ 措辞略异(「连接上」vs「连不上」),而**是同一个无证据断言**
⇒ ⚠ 而它的位置比我那行严重得多:**台账是 DEC 级引用源**,仅次于 CLAUDE.md
⇒ 而 DEC-0023 正是本轮全部 W1-C 工作的性质依据 —— 那条风险声明会随 DEC-0023 被反复引用
⛔ 我不改台账(不是我的件)⇒ **已上报 lead 路由**
⇒ 建议改法(供 lead 参考,⛔ 非我决定):
   「②而 W1-C 卡在两条**相互独立**的阻塞上:①实板是否存在(CTO 议程第 5 项,事实确认)
     ②SHARC 工具链缺失(第 9 项,采购/安装)⇒ 该阻塞现在挡的不是"验证",是"我们有没有任何算力依据"」
```

### 8.3 我从这次学到的一条(落在本节,⛔ 不另存别处)

```
我在 r76 回审里批评的是「限定词没回原始数据核」。
⇒ 而我隔了约两小时,就把派单里的一句无证据转述**原样写进了自己的交付件**。
⇒ ∴ 「知道这条规则」与「在写字时用上它」之间还有一步,而那一步不会自己发生。
⇒ 已立操作形式:**凡把上游的话写进我的件,先 grep 一次盘面**;
  grep 不到 ⇒ 要么不写,要么写成「上游口述,盘面无证据,待落盘」。
⚠ §2.2 的两种板型之争,我只能证「本仓两种线索并存」,⛔ 证不了"我们实际有哪块"——那是 T-0
```
