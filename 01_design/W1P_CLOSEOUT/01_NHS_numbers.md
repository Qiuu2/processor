# W1-P 收口件 ① · NHS(陷波式反馈抑制)数值报告

```
⛔ 门禁状态:未过门 —— 未经独立 critic 评审、未 commit、不得对外承诺、不得作为其他工件的依据
作者:adaptive-dsp(第 7 实例)   日期:2026-08-04   配套:02_self_set_constants.md / 03_open_questions.md
数据等级:全部 [L2/宿主仿真](numpy 闭环台架),⛔ 无一条是板上实测 [L1],⛔ 无一条用过真实房间冲激响应
用途:供 CTO 做**路线决定**。⛔ 不是验收数据。
```

---

## §1 一句话结论

> **在标称输入电平上,NHS 自己选点、8 个陷波、12 秒观测窗、环路 8 kHz 带限、
> 且【兜底 `g_duck` 已消融】的条件下,实测反馈余量改善 `ΔMSG = 2.00 – 6.00 dB`
> (六条种子,中位 2.50 dB)。六条中 2 条 ≥4 dB、1 条 ≥5 dB。**
>
> ⚠ **「duck 消融」是这句话的一部分,不是脚注**:不消融时兜底会以**环内宽带衰减**冒名顶替
> 陷波的成绩(本线已四次),而那与把推子拉下来无异(§2.1)。

> **而这条线的杠杆已经用尽:天花板不是实现缺陷,是 `ΔMSG ≈ top1 − top(NN+1)` 这个序统计量的数学形状(§4)。**

### ⛔⛔ 1.1 撤回:此前的「≥」限定 —— **它的前提被证伪了**(2026-08-04;verification 复核,我方独立验算)

```
旧写法(⛔ 已撤回):「ΔMSG **≥** 2.00–6.00 dB」
旧理由            :m0 臂在其读数点上仍在增长 ⇒ m0 被高估 ⇒ 差值被低估约一个步长 0.50 dB(F76.3 / F77.4)
```
**⇒ 三条实证推翻它。⚠ 前两条我在自己已落盘的数据上独立复核过;**
**   第三条我第一次改写时【漏了】,而它把结论的方向掉了个个儿(见 ③ 与 F82.5)**:
```
① **m0 ≡ 解析 anchor,六格逐格 差 = 0.0000 dB**
   两条**完全独立**的路径:闭环扫描取"最后一个不起振的 G"  vs  `MSGMeter` 解析算 MSG
   m0 = −11.8484 / −12.9325 / −13.0786 / −12.4907 / −13.7509 / −12.5187,与 anchor **逐位相同**
   ⇒ ⇒ **m0 没有系统性偏高 —— 它就等于解析临界点**
② **「恰好一个步长 0.50 dB」是仪器的底,不是测量值**
   `r83_m0bias.py` 的做法 = 从已报 m0 **逐 0.5 dB 下退**,找首个"末秒−首秒 RMS ≤0"的 G
   ⇒ 可能输出只有 {0.50, 1.00, …} 或「给不出数」⇒ **最小非零答案在构造上就是 0.50**
   ⇒ ⇒ 「4/6 条恰好 0.50」是它**返回了自己的下限**,⛔ 不构成"偏高 0.50 dB"的证据
```
**⇒ ⭐ 而「m0 臂在 anchor 上仍在增长」这个【观测】没错,错的是它的【解读】**:
> 在 `G = MSG` 上环路极点**恰在单位圆上** ⇒ 既不衰减也不快速发散
> ⇒ **缓慢增长正是临界点的预期签名**,不是"读高了"的证据。
> **⇒ ⛔ 同一个观测,支持不了那个推论。**

**⇒ ⚠ 而这与 `critic_r87_F76-F80_verdict_20260804.md` 判「F79.5 方向正确」【不冲突】**:
critic 核的是**推论**(若 m0 偏高,则 ΔMSG 被低估 —— 逻辑成立);verification 打掉的是**前提**(m0 并未偏高)。
**⇒ 有效推论 + 假前提 = 结论不成立。两份评审各自都对,⛔ 不必选边。**

**③ ⭐⭐ 而同一份数据里有【反向实证】—— 这一条我第一次改写时漏了,它把方向掉了个个儿**
```
臂 O 是 LTI(神谕 8 陷波钉死、T_low=999、INV-O 6/6 OK)⇒ **有解析真值**
⇒ 实测 dO 在 **2/6 格【高于】解析真值**:0.5/1 高 **+0.169 dB**、0.5/2 高 **+0.019 dB**
⇒ ⇒ 「≥」在那两格**被实测直接违反**
直接观测(0.5/1):G = anchor+6.5 处**解析已失稳 0.169 dB**、末−首 RMS **+7.25 dB**,
   而 `is_howling = **False**`;到 +7.0 才响(末−首 +80.08)
⇒ ⇒ **检测器会漏掉【小超量失稳】** ⇒ 偏置方向是把**陷波臂读高** ⇒ **ΔMSG 可能被【高估】**
```

**⇒ ⇒ 现行口径(照 verification 的结论写死)**:
> **`ΔMSG` 的不确定度 = **±0.50 dB(一格),方向未定**。**
> **⛔ 不是「≥」;⛔ 也不是"无偏的点估计"(我 16:3x 第一版改写就是这么写的,**同样不对**);**
> **⚠ 而现有的**唯一方向性证据**指向【高估】—— 即真值可能**更差**,不是更好。**

**⇒ 为什么这一条对 CTO 要紧**:他要判的是「4–5 dB 够不够得着」。
「≥」把 `2.00–6.00` 读成**"真值只会更好"**;而实测证据说**"真值可能更差"** —— **方向正好相反**。

> **⭐ 形态记一笔(与同日 F81 同型)**:`m0 偏高 0.50` 与 `r88b 的 16.7 dB 倾斜` 是同一件事的两面 ——
> **一个我们用来支持自己的量,其实是仪器自身的底 / 自身的伪影。**
> **⇒ 而我这次的二次失误是第三面:我核了【被转述给我的那两条】,却没问【是不是只有两条】。**
> **验证你拿到的部分,不能告诉你有没有拿全。**

### 1.2 ⚠ 随之而来的四条读法更正(verification minors)

```
m-1 **`m0` 不是一个独立测量**:它逐位 == 解析 anchor
    ⇒ ΔMSG 实为「**实测陷波臂终点 − 解析无陷波 MSG**」的**混合量**,
      ⛔ 不是 §1 原措辞暗示的"两臂实测之差"
    ✅ 正面后果:该恒等式本身是一次**从没被报过的台架验证**(时域检测器 vs 频域解析,6/6 逐位符)
m-2 **仪器底 0.354 的【推导式】与实际量化结构不符**:该式设两臂各有 ±半步独立误差,
    而 `m0` 侧量化误差**恒为 0**(见 m-1)⇒ 只有陷波臂在量化
    ⇒ **真实分辨力 = 整格 0.50 dB(单边)**,不是 0.354
    ✅ 对头条表**不承重**:六格值与缺口列**全部是 0.5 的整数倍** ⇒ 门限取 (0, 0.5) 内任意值,判定不变
m-3 §2 的 `f_trig` 列**未标是哪个臂** —— 实为 **Na 臂**(6/6 匹配);四臂差异极大
    (0.2/2:m0=5379 / N=5380 / **Na=3111** / O=503 Hz)
    ⚠ 另附一条值得看的:**臂 O 的 f_trig 全部落在 22–503 Hz**(186/365/503/52/22/170)
      ⇒ 神谕臂把前 8 峰压掉后,**失稳点整体退到检测带下沿 120 Hz 附近或之下**(F35 同族)
m-4 §2 **未标频带**:实测量是**全带**量(检测器 = 宽带 RMS;`anchor` 取 `['full']`)
    ⚠ 而同一算例的**带内**解析 ΔMSG 在 0.5/0 上是 **4.691 vs 全带 3.198,差 1.49 dB**
    ⇒ ⛔ 这与本件 §3 和我自己 memory 里那条「凡报 MSG/ΔMSG 必须写明频带、两列都给」**相抵**
    ⇒ 已在 §2 表头补「**全带**」;带内列**本轮未补**(需重算,归下一轮),⚠ 缺口如实标出
```

### 1.3 ✅ 而 verification 顺带给了一条【正面】的(此前没人做过)

```
V-F:臂 O(神谕选点列)是 LTI ⇒ 可解析预测 ⇒ 与实测交叉核
   解析 dO(全带) 6.026 / 6.033 / 7.731 / 3.198 / 6.331 / 4.481
   实测 dO        6.00  / 6.00  / 7.50  / 3.00  / 6.50  / 4.50
   **|差| 中位 0.101 dB,最大 0.231 dB(< 半格 0.25)**;4/6 格 实测 == 0.5×floor(解析/0.5)
   = **完美检测器 + 栅格向下取整**的预期形态
```
**⇒ 头条表的「臂 O」一列通过了一条**与原路径完全不同**的验证(时域闭环扫描 vs 频域临界点解析)。**
**⇒ 这是铁律七要求的【第二独立工具交叉核】,r76 线此前从未做过 ⇒ 该列可以更硬地引用。**

---

## §2 头条表(逐种子,⛔ 不得只引中位)

**工作点**:`src_rms = −20.0 dBFS`(标称 = `+4 dBu` + 20 dB headroom,[L4/行业惯例,待 CTO 追认])·
`T_OBS = 12 s` · `bw_oct = 1/5` · `NN = 8` 槽全空 · `T_low = −45` · `n_cand = 16` · `STEP = 0.5 dB` ·
`fs = 48 k` · **环路经 `band_limit(h, 8000)`,8 kHz 以上被带限** · 噪声源(白高斯)· 噪声 RIR plant。

⚠⚠ **本表原带的「≥」已全部撤回**(见 §1.1)。**不确定度 = ±0.50 dB(一格),方向未定**;
⚠ 而唯一的方向性证据指向**高估**(检测器漏掉小超量失稳)⇒ ⛔ 不得读作下界,**真值可能更差**。
⚠ **本表全部为【全带】量**(检测器 = 宽带 RMS,`anchor` 取 `full`);⛔ **带内列本轮未给**(见 §1.2 m-4)。
⚠ `f_trig` 列 = **Na 臂**(自选 + duck 消融);四臂差异极大,⛔ 不得当作"该格的起振频率"泛指。
⚠ 而 critic MAJOR-2 提的那一点**仍然成立且已执行**:三列共用同一个 m0 ⇒ **任何限定必须三列同标,不能只标主列**。

⛔⛔ **而「选点缺口」列的旧理由已作废(critic r4 MAJOR-1)—— 它是把【旧故事的理由】搬进了新故事**:
```
旧写法:「缺口是两个 ΔMSG 之差 ⇒ m0 代数抵消 ⇒ 不确定度与三列不同源(D6-ai **反面边界**)」
⛔ 三处都不再成立:
 ① 「m0 代数抵消」现在是**抵消一个零** —— §1.1① 已证 m0 ≡ 解析 anchor,其误差**本来就是 0**
 ② 「与三列不同源」**反了** —— 缺口的不确定度**恰恰与三列同源**(都来自 `m_臂` 的检测器偏差),
    只是它含**两份**,而三列各含一份
 ③ 「D6-ai 反面边界」**引反了**:判别法问的是「两臂用的是【同一个数】还是【各自测出的两个数】」——
    而 `m_O` 与 `m_Na` 是**各自测出的两个数** ⇒ 这是 **D6-ai 的【主体】**,不是反面边界
```
**⇒ ⇒ 现行口径**:`缺口 = m_O − m_Na` = **两个独立测量之差** ⇒ **⛔ 不得假定偏差抵消**;
其不确定度**至少与三列同量级,且含两份**。⚠ 而两端**没有理由相等**:
臂 O 是 **LTI**(神谕陷波钉死、`T_low=999`),主列 Na 是**自适应**(陷波随时间挂/移/加深)
⇒ 临界点谱形状不同 ⇒ "漏掉小超量失稳"的程度不同 ⇒ **偏差之差不为零,且目前没有任何东西把它界在 ±0.50 内**。

> **⭐ 而这次失效的形状值得记(第 7 实例自认)**:那句话**在旧前提下是对的**;
> 我改前提时改了**结论**(「≥」撤了),**没改跟着它的【理由】**。
> **⇒ 与"同一把尺子只用了一半"同族 —— 这次没用上的那一半是【理由那一半】。**

| T60 / seed | **NHS 自选 @duck 消融**(主列) | NHS 自选 @有 duck(附列) | 臂 O:神谕选点 @固定 −18 dB | 选点缺口(臂O − 主列) | `f_trig`(**Na 臂**) |
|---|---|---|---|---|---|
| 0.2 / 0 | **6.00** | 4.50 | 6.00 | **0.00(不可判)** | 2559 Hz |
| 0.2 / 1 | **2.00** | 6.50 | 6.00 | 4.00 | 1034 Hz |
| 0.2 / 2 | **4.00** | 8.00 | 7.50 | 3.50 | 3111 Hz |
| 0.5 / 0 | **2.50** | 3.00 | 3.00 | 0.50 | 2258 Hz |
| 0.5 / 1 | **2.00** | 3.00 | 6.50 | 4.50 | 7580 Hz |
| 0.5 / 2 | **2.50** | 1.50 | 4.50 | 2.00 | 716 Hz |
| **中位** | **2.50** | 3.75 | 6.00 | **2.75** | — |
| **分层中位** | 0.2 层 4.00 / 0.5 层 2.50 | — | 0.2 层 6.00 / 0.5 层 4.50 | — | — |

出处:`r76_cell_s20f0`(主列 = `dA`、附列 = `dN`、臂 O = `dO`);主列由 **r87 独立重跑,6/6 四项全符**
(m0 / ΔMSG / 挂陷数 / 终点 RMS 逐项相同)。

### 2.1 ⛔ 附列(有 duck)为什么不能当作陷波的成绩

`g_duck` 是**环内宽带衰减**(`nhs.py:280`),实测最深 −4.00 … −6.00 dB。
**它抬高 MSG 的方式与把推子拉下来无异 —— 代价是整机少放大同样的量。**
本线已有**四次**"兜底冒名顶替陷波"的实证(F33 / r54 幻峰 / r64 落在达标判定上 / F79.4)。
⇒ **附列只作产品形态参考,⛔ 不得单独引用,须与 `g_duck最深` 同出。**

### 2.2 与「4–5 dB 目标」对表

| 判据 | NHS 自选 @消融 | 臂 O(神谕选点) |
|---|---|---|
| **≥4 dB** | **2 / 6** | 5 / 6 |
| **≥5 dB** | **1 / 6** | 4 / 6 |

> **⇒ 现行设计(`NN=8`,`bw_oct=1/5`)下,NHS 自己选点在六条种子中只有 1 条到 5 dB。**
> **⇒ 而「臂 O」不是"努力就能到"的目标 —— 它是把峰位置直接告诉算法(解析神谕)的构造臂。**

⚠ **「上界」这个词只在 `bw_oct = 1/5` 上成立**:在 1/8 及以下实测出现 **5/30 格倒挂**(自选臂 ≥ 臂 O),
且 30 格 INV-O 构造检查 0 FAIL ⇒ 倒挂不是仪器故障(F65.3)。⇒ 表中臂 O 一律称「神谕选点臂」,不称"上界"。

### 2.3 ⚠ 一个必须让 CTO 看见的配置差:PRD 写 16 点,本表在 8 点上测

PRD §二.5 原文:「每通道独立 **16 点陷波**防啸叫,三种工作模式」。本表 `NN = 8`(合同/原型默认)。
等预算实测(总陷波带宽钉死 ≡ 1.00 倍频程,用 `NN` 换 `bw_oct`,F69):

| 配置 | 神谕选点臂 中位 | **自选臂 中位** | 槽利用率(挂陷/NN) |
|---|---|---|---|
| NN=8 (1/8) | 4.00 | **3.25** | **0.81** |
| NN=10 (1/10) | 4.50 | 2.75 | 0.80 |
| NN=16 (1/16) | **5.00** | **2.75** | **0.34** |
| NN=24 (1/24) | 4.75 | **1.75** | **0.25** |

> **⇒ 加槽把神谕选点臂抬起来了(8→16 有可判改善,16→24 买不到任何东西 = 饱和点在 16);
> 而自选臂【完全没跟上,而且更差】—— 槽利用率从 0.81 掉到 0.25,NHS 根本填不满多出来的槽。**
> **⇒ ∴ PRD 的「16 点」在**神谕臂**上确有收益,在**自选臂**上本线**未测到**收益。**

⚠ 限定:r79 的 NN=16 配的是 `bw_oct=1/16`(等预算折算),而 PRD 未规定带宽 ⇒ **两者不是同一配置**,
不得直接读作"PRD 配置下的性能"。

---

## §3 全部限定(⛔ 引用本报告任一数字时必须同时带上)

```
① [L2/宿主仿真] —— numpy 闭环台架,⛔ 非板上、⛔ 非真实房间
② plant = **噪声 RIR**(`h(t)=n(t)·e^{−t/τ}`),它在**构造上**具有 Rayleigh 幅度、dB 域 σ≈5.57 dB
   ⇒ 台架复现"峰-均比 ≈10 dB"只证明它实现了该模型,**⛔ 不证明真实房间如此**
③ 激励 = **白高斯噪声**,⛔ 全程没有语音/音乐素材 ⇒ 掩蔽、可听性、音质代价**一律未测**
④ **环路 8 kHz 以上被带限**;NHS 检测带 = 120–7800 Hz。带外啸叫会在旁链混叠成**位置完全错误的幻峰**
   (实测 14531.9 Hz → 1468 Hz),日志上看起来"NHS 正常工作" —— 产品级发现 P-1,已在架构侧挂账
⑤ 六条种子(T60 ∈ {0.2, 0.5} × seed ∈ {0,1,2})⇒ **n=6,不是统计样本**;⛔ 逐条报,不得只给聚合
⑥ ⚠ **仪器底的推导式已被更正(verification m-2)**:`0.354 = STEP/2×√2` **算术对**,但该式设
   **两臂各有 ±半步独立误差**,而 `m0` 侧量化误差**恒为 0**(m0 ≡ 解析 anchor)⇒ 只有陷波臂在量化
   ⇒ **真实分辨力 = 整格 0.50 dB(单边)**。✅ 对头条表**不承重**(六格值与缺口列全是 0.5 整数倍)
   ⚠ 而 **`STEP = 0.5` 本身无来源** ⇒ 我们全部"可分辨/不可分辨"判据的地板,是一个没人刻意选过的数(台账 S-1)
⑦ 两臂同锚同栅格 ⇒ 臂间差恒为 0.5 的整数倍 ⇒ **|差|=0.50 只是"一格"**
⑨ ⚠ **ΔMSG 的不确定度 = ±0.50 dB(一格),方向未定**;而唯一方向性证据指向**高估**
   (检测器漏掉小超量失稳,实测 2/6 格 dO 高于解析真值)⇒ ⛔ 不得读作下界(§1.1)
⑩ ⚠ 本报告的 ΔMSG 全部是**全带**量;⛔ **带内列未给**(与"两列都给"的纪律相抵,§1.2 m-4)
⑧ 全部数字**未经独立 critic 评审**
```

---

## §4 ⭐⭐ 天花板的来源(CTO 做路线决定的依据)

**结论先写**:ΔMSG 不是"实现得好就能涨"的量。它是**顶部几个峰之间的序统计量**:

```
ΔMSG ≈ top1 − top(NN+1)      ← 完美陷掉前 NN 个峰后,第 NN+1 个峰接管
⛔ 不是 峰-均比(max − mean)
实测判别(六条种子,NN=8):|假说「峰-均比」误差| 中位 **6.88 dB**
                          |假说「序统计量」误差| 中位 **1.57 dB**
```
出处 F71。⇒ **Schroeder 的「≈10 dB」确实存在、也确实是峰-均比(一手原文 p.1719 已核),
但那是把【全部 223–475 个临界点】压平才够得着的量;用 8 个陷波能拿到的是【第 1 峰与第 9 峰之差】。**

### 逐条堵死四个方向

| 方向 | 实测 | 判 |
|---|---|---|
| **加陷波数**(⚠ **等预算**:总带宽钉死 ≡1.00 oct,用 `NN` 换 `bw_oct`) | 神谕臂 `NN` 8→16 有可判改善,**16→24 中位 +0.00 = 饱和**;而自选臂**填不满**(利用率 0.81→0.25) | 序统计量增长本就极慢 ⇒ **饱和不是实现缺陷,是这个量的数学形状**。⚠ 台架实测 `top1−top(NN+1)`:NN=8 ⇒ 1.92–3.15;16 ⇒ 2.51–4.18;24 ⇒ 3.21–5.26 |
| **开更窄的陷波** | 旁链 bin 宽 **15.625 Hz**;`bw_oct ≤ 1/8` 起陷波比一个 bin 还窄(1/24 @120 Hz = **3.47 Hz = 0.22 bin**);Quinn 选点误差 p95 **1.4 Hz** ⇒ 1/24 在 120 Hz 处实得深度损失 **−7.53 dB(42%)** | **低频物理墙**,与算法无关,改 `_allocate`、改排序键都修不掉 |
| **压得更深** | `recheck_free` 使六条种子的陷波**全部达到 `max_depth`**(基线下有两条卡在 −15.00 / −16.50)—— **而 ΔMSG 一格未涨:0/6 变好、1/6 变差 −0.50 dB(= 一格,弱)、4/6 不可判、1/6 修法不可达** | **公式里没有深度这一项**(r87 实证,F79) |
| **压得更深(旁证,⚠ 不同量)** | r21:深度设定 −3 → −18(多 15 dB),**陷波点残余电平**总落差只有 **−0.4 / +0.9 dB**,末三步不再下降 | ⚠ **方向一致,但不是同一个量**:r21 测的是**残余电平**不是 ΔMSG,且其 `depth` 是**输入参数**、并刻意 `T_low=999` 关掉自适应 ⇒ **只作旁证,⛔ 不得当作 ΔMSG 的实测** |

> **⚠ 这两句必须挨在一起读:「六条种子的陷波全部达到 max_depth」是真的;「它换不来任何可判的 ΔMSG」也是真的。**
> **⇒ 代理量达标 ≠ 交付量改善。**

### ⇒ 剩下的唯一杠杆:**选点**

本报告 §2 的「选点缺口」列 = 神谕选点臂 − 自选臂,**逐种子 0.00 / 4.00 / 3.50 / 0.50 / 4.50 / 2.00,中位 2.75 dB**。

> **⛔⛔ 而这个 `2.75 dB` 是本报告里【承重最狠】的数(它就是"剩下的唯一杠杆"那句),
> 因此它的限定必须与它同段出现,⛔ 不得只写在 §1**:
> ```
> 缺口 = m_O − m_Na = **两个各自独立测出的数之差** ⇒ **⛔ 不得假定检测器偏差相消**(D6-ai 主体)
> ```
> **⇒ ⭐ 两端现已都测(r89,2026-08-05):检测器偏差 = 实测临界增益 − 解析真值**
>
> | T60/sd | 臂 O 偏差 | Na 偏差 | **缺口偏差 = O − Na** | 缺口值 | 偏差占缺口 |
> |---|---|---|---|---|---|
> | 0.2/0 | −0.026 | −0.096 | **+0.070** | 0.00 | — |
> | 0.2/1 | −0.033 | −0.393 | **+0.360** | 4.00 | 9.0% |
> | 0.2/2 | −0.231 | −0.486 | **+0.255** | 3.50 | 7.3% |
> | 0.5/0 | −0.198 | **+0.197** | **−0.395** | 0.50 | **79.1%** |
> | 0.5/1 | **+0.169** | −0.222 | **+0.391** | 4.50 | 8.7% |
> | 0.5/2 | **+0.019** | **+0.007** | **+0.012** | 2.00 | 0.6% |
>
> ```
> ① **方向【不可判】** —— 缺口偏差 **变号**(−0.395 … +0.391)⇒ ⛔ 不得给 2.75 加方向性限定
> ② **而幅度【有界】** —— |缺口偏差| ≤ **0.395 dB**,**0/6 格达整格 0.50**;中位 |偏差| = 0.307 dB
> ③ ⭐ **∴ 承重结论稳健**:缺口中位 **2.75 dB** vs 偏差中位 **0.307 dB(≈11%)**
>    ⇒ **检测器偏差解释不掉"选点是剩下的唯一杠杆"这个结论**
> ④ ⛔ **而小缺口那两格【不稳健】**:`0.5/0` 缺口 0.50、偏差 0.395 ⇒ **79%**
>    ⇒ 该格的缺口几乎全在偏差量级之内 ⇒ **不可判**(此前只写"恰好一格,弱",现在有定量理由)
> ```
> **⚠ 限定(三条,⛔ 不得省)**:
> ```
> (a) Na 端测的是**冻结最终配置**下的检测器偏差,**不是活臂在时变过程中的偏差**
>     —— 活臂时变 ⇒ **没有解析真值** ⇒ ⛔ 本线不给它编近似值(PREREG_r89 §1 跑前写死)
> (b) 臂 O 端六格 ✅ **我已独立复算**(2026-08-05,MSGMeter 重算解析 dO):**最大绝对差 0.0004 dB**
> ⇒ 与 verification V-F 一致 ⇒ 该列**不再是「引用」,是「复核过的引用」**
> (c) n=6;两端偏差**没有理由相等**(臂 O 是 LTI 神谕谱,Na 是自适应选点后的谱)—— 实测也确实不等
> ```
> **⇒ ∴ `2.75` 读作**中心值**;⛔ 不读作下界或上界;而它**经得起** ±0.40 dB 量级的检测器偏差。**

```
✅ 空间存在:4/6 条种子上缺口 ≥ 2.00 dB
⛔ 但只在【部分种子】上:0.2/sd0 缺口 = **0.00**(**落在分辨力 0.50 dB(单边,§1.2 m-2)之下 ⇒ 不可判**
   —— ⛔ 既不能判为相等,也不能判为仍有差距),0.5/sd0 = 0.50(**恰好一格,弱**)
⛔ 且这张表是在 `bw_oct = 1/5` 上测的,而 1/5 **超文献带宽预算 1.3–2×**(1.60 oct vs 0.8–1.2)
⛔ 而在合规带宽(1/8)上,神谕选点臂**自己**退化(6/6 种子下降 0.50–2.50 dB),
   且出现 5/30 格倒挂 ⇒ **"把选点做到神谕水平"这个目标本身在窄带宽上定义不清**
```

**⇒ 给 CTO 的净判断**:靠继续修 NHS(选点/分配/判据)拿到的,乐观上限是**中位 2.75 dB 的一部分**,
而**四条判据层修法已实测全部无效**(`growth_and_gate` 无从评估 / `R_RISE` 下调 δ≡0 / `prefer_unnotched` 证伪不提交 /
`recheck_free` **0/6 变好、1/6 变差 −0.50 dB(= 一格,弱)、4/6 不可判、1/6 修法不可达**)。
⇒ **若目标是 4–5 dB,本线的证据不支持"再修实现就能到"。**

---

## §5 ⭐⭐ **低频过渡段 `f_low … f_c(V, T60)`**:五条互相独立的限制指向同一段

> **⛔ 这一节的频带边界【不是测出来的】,是两个借用输入(`V`、`T60`)的函数 —— 标题不得写成一个固定频带。**
> **⚠ 本节 2026-08-04 按 critic MAJOR-3 重写(原写法把它写成固定的「120–280 Hz」)。**

### 5.0 这一段到底是哪一段:`f_c ∝ 1/√V`,跨度 3.5×

**Schroeder 1964 p.1723 逐字原文**(§V 末方括号段,已开 PDF 目视核对):
> *"The lowest frequency for which the statistical theory is valid,⁴ f = 4000 (T₆₀/V)^½ is about 600 cps
> in the case of the booth. Since the instability occurred at a frequency below this limit, i.e., in the
> frequency range of isolated normal modes, somewhat higher gains must be expected because the frequency
> response is more irregular in this frequency range."*
> — 其上标 ⁴ 的出处 = `M. R. Schroeder, Acustica 4, Beih. 2, 594–600 (1954)`

**⭐ 原文自带一个校验例,它单独就能定死常数与单位**:
```
Fig. 7 图注(同页):"soundproof booth (volume **14 m³**, reverberation time T₆₀ = **0.34 sec**)"
正文说该 booth 的极限 "is about **600 cps**"
  k=4000 ⇒ 4000·√(0.34/14) = **623 Hz** ≈ "about 600" ✅
  k=2000 ⇒ **312 Hz** ⛔ 与原文自己的话矛盾
⇒ **在这篇论文里,常数只能是 4000,且 V 只能是 m³。**
```

**⚠⚠ 而库内并存另一个常数,⛔ 不得读作"谁错了"**:
```
架构侧 `W1A:1642` / 冻结合同 `IF-v1.8:366` 用 `f_c = 2000·√(T60/V)` [L3/教科书公式]
⇒ 两个常数在文献里**都存在**,差别是**模态重叠判据取多少**(严 vs 宽)
⇒ ⛔ 本报告【不裁定】哪个对:我方只能证「1964 这篇自己用 4000」,
   证不了「2000 那一版没有出处」(我方库内**没有** 2000 那一版的原文)
⇒ ∴ 本节两个口径**并列给出**,并按 k 分别报作用域
```
### 5.0b ⭐⭐⭐ **把这一节从"说不清的低频问题"变成"解析已知的过渡区"**

**模态重叠 `M(f) = (4πV f²/c³)·(2.2/T60)` —— 解析、连续、零采样噪声。**
参照:`M<1` 孤立模态 ｜ `M=2.74` = k2000 判据 ｜ `M=10.96` = k4000 判据。

| 房间 | V (m³) | T60 | **M @120 Hz** | **M @280 Hz** | f(M=1) |
|---|---|---|---|---|---|
| booth(Schroeder 原文那个) | 14 | 0.2 | 0.69 | 3.77 | 144 Hz |
| booth | 14 | 0.5 | 0.28 | 1.51 | 228 Hz |
| 会议室 | 60 | 0.5 | **1.18** | 6.45 | 110 Hz |
| 会议室 | 100 | 0.5 | **1.97** | 10.74 | 85 Hz |
| 会议室 | 300 | 0.5 | **5.92** | 32.23 | 49 Hz |

**⇒ 反解:要让【整条 120–280 Hz】都满足 `M<1`(= 全段孤立模态),房间须多小?**
```
T60=0.2 ⇒ V < **3.7 m³**    T60=0.5 ⇒ V < **9.3 m³**    T60=1.0 ⇒ V < **18.6 m³**
⇒ ⇒ **那是壁橱,不是房间 —— 没有任何现实会议室满足。**
```

> **⇒ ∴ 那一段在【任何现实房间里】都是【过渡区】,从来不是"几个驻波"。**
> **⇒ ⇒ 「它是模态区还是统计区」这个问法本身是错的 —— 它两者都不是。**
> **⇒ ⇒ ⇒ 而 `M(f)` 解析连续 ⇒ 把分界画在 `M=2.74` 还是 `M=10.96`,**不改变场本身**
> **⇒ ∴ Schroeder 常数之争【没有物理内容】。**

⚠ **这一条同时收窄了 §5.2 的路线建议**:该建议要对付的若是"几个驻波",前提不成立;
它真正对口的是 `M<1` 的那一段,而那一段(31–110 Hz)整段在检测带之下(§5.2)。

**⭐⭐ 而这场"之争"其实不是文献问题(critic 算出,我方复核成立)**:
```
把 f_c = k√(T60/V) 代回模态重叠 M = (4πVf²/c³)·(2.2/T60):
   M = 4π·k²·2.2 / c³   ⇒ ⭐ **只依赖 k,与 V、T60 【都无关】**
   k=2000 ⇒ M = **2.74 ≈ 3**       k=4000 ⇒ M = **10.96 ≈ 11**
⇒ ⇒ **两个常数不矛盾 —— 它们是【同一条连续曲线上的两个阈值】**
⇒ ⇒ ⇒ 要裁的不是"谁的原文更权威",是**【我们的设计需要 M 取多少】**
⇒ ⇒ ⇒ ⇒ 而这个问题**不需要任何一篇论文** ⇒ 技术选择,归架构侧 + C9(未答清单 E-6)
```

**V 敏感度(会议室合理范围 60–300 m³)—— 这就是为什么标题不能写成一个固定频带:**

| `f_c` (Hz) | V=60 | V=100 | V=300 |
|---|---|---|---|
| **k=4000**,T60=0.2 | 231 | 179 | 103 |
| **k=4000**,T60=0.5 | **365** | 283 | 163 |
| k=2000,T60=0.2 | 115 | 89 | 52 |
| k=2000,T60=0.5 | 183 | 141 | 82 |

```
⇒ 同一 k=4000 下,f_c 从 **103 Hz 到 365 Hz,跨度 3.5×**
⇒ 本节讨论的段 = 「**NHS 检测带下沿 120 Hz** 到 **f_c(V,T60)**」
   · k=4000 且 V 偏小 ⇒ 该段可宽达 120–365 Hz
   · k=2000 且 V 偏大 ⇒ 该段可能**为空**(f_c < 120 ⇒ 检测带全在统计区)
⇒ ⛔ 「120–280 Hz」这个旧标题**已作废**,它只是 (k=4000, V=100) 这一格
```

| # | 限制 | 数 | 类型 | 出处 / L 标 |
|---|---|---|---|---|
| ① | **Schroeder 统计理论的适用下限**`f = k·(T60/V)^{1/2}` | 见 §5.0 的 V 敏感度表;**k=4000 时 V=100 ⇒ 179 / 283 Hz** | 理论适用域 | **Schroeder 1964 p.1723 一手原文逐字**(常数经原文自校验坐实);代入为 [L3/公式代入],`V` **借用架构侧算例** |
| ② | **窄陷波挂偏的物理墙** | 1/24 @120 Hz:陷波 3 dB 带宽 3.47 Hz = **0.22 个分析 bin**;Quinn p95 误差 1.4 Hz ⇒ 深度损失 **−7.53 dB(42%)**;1/16 −5.00;1/8 −1.92;1/5 −0.86。300 Hz 处 1/24 仍损 −2.56;**1 kHz 处各档 <0.35 ⇒ 高频不受影响** | 物理(做不到) | F72,[L2/桌面数值] |
| ③ | **架构侧 C7 精度契约的约束点在低频端** | `BW/4 = 1 bin` 的对应点 = **1351 Hz**,越往低越紧 | 契约(做不到) | 架构侧 W1A v0.33 |
| ④ | **Hilbert/SSB 频移的镜像抑制在低频劣化** | 我方唯一实测 = 镜像 **−64 dB @1 kHz**(513 taps);**低频端劣化量本线未测** | 物理(做不到) | r80 守卫①;低频端归架构侧 |
| ⑤ | **`Hilb1025` 在延迟预算下可行组合 = 0** | 同一声学区内输出必须延迟对齐 ⇒ 全机延迟 = 区内 `max`;LR8@80 分频 21.04 ms 已占满 12 ms 预算 | **资源(买不起)** | 架构侧 W1A v0.58 |

> **⇒ 前四条说「做不到」,第五条说「买不起」。⇒ 而它们的机制彼此独立。**

### 5.1 ⛔ 而我们自己的台架在这一段的有效性也存疑(必须先说这一条)

```
台架 plant = **噪声 RIR**,其文档明写:选它正是因为检测带"几乎全在【统计区】"
⇒ 而 120 Hz … f_c 这一段在【统计区之外】(限制 ①)
⇒ ⇒ **落在该段内的低频数字,都悬在一个在该段不自称有效的模型上**
⇒ 这不是"我们测得不够准",是"我们这把尺子在这一段没声称过能用"
```
**⚠ 而这条限制的【宽度】随常数走(critic MAJOR-3 的直接后果,必须同段写明)**:
```
k=4000, V=100 ⇒ 该段 = 120–179(T60=0.2)/ 120–283(T60=0.5)⇒ 覆盖较宽
k=2000, V=100 ⇒ f_c = 89 / 141 ⇒ T60=0.2 时该段**为空**;T60=0.5 时只剩 120–141 Hz
⇒ ⛔ 原写法「本线【所有】低频数字都悬着」**已收窄**:它只对落在 [120, f_c] 内的数成立,
   而 f_c 取决于那个未定案的常数
```

### 5.2 ⭐ 建议(**待验推论,⛔ 不是结论**)——⚠ **其第一个前提已被库内判定否掉,本节 08-04 重写**

**⛔ 先写更正(critic MAJOR-3 后果二)**:
```
原写法:「低频模态【少】、尖、稳 ⇒ 那正是装机时一次性固定陷波最擅长的形态」
而架构侧 `W1A:1642`(写于 **08-02**,早于本收口件两天)明文:
   「『房间模态是离散少数』这个直觉在本频带【不成立】—— 固定性成立,**少数性不成立**」
且该判定**已进冻结合同** `IF-v1.8:366` ⇒ 与它冲突须走 **C9**,⛔ 不是我方可自行改写的
⇒ ⇒ **我原把这条列为「未测」是错的:它不是未测,是【测过而我没看见】。**
```

**⭐ 我方复核(用架构侧同一个 Weyl 式 `dN/df = 4πVf²/c³`,c=343 m/s;⛔ 无新测量)**:

| V (m³) | 120–280 Hz 内模态数 | 密度 @120Hz | 重叠 M @120Hz(T60=0.5 / 0.2) |
|---|---|---|---|
| 60 | **126** | 0.27 /Hz | 1.2 / 3.0 |
| 100 | **210** | 0.45 /Hz | 2.0 / 4.9 |
| 300 | **630** | 1.35 /Hz | 5.9 / 14.8 |

（自校验:同式算 100–8000 Hz @V=100 得 **5.3×10⁶** 个模态、@1 kHz 密度 **31.1/Hz**,
与 `CLAUDE.md` / 架构侧已发布数一致 ⇒ 我方实现与其同口径。）

```
⇒ **「少」在这一段不成立** —— 126–630 个模态,重叠 M 在 1.2–14.8
⇒ 架构侧那条判定**被我方独立复核支持**,⛔ 我不反驳
⇒ 三个形容词逐个处置:
   「少」⛔ **否掉**(本表)
   「尖」⚠ **条件性支持** —— Schroeder 原文对 f_c **以下**说 "the frequency response is more
        irregular in this frequency range";但该句只在 f_c 以下成立 ⇒ 随常数之争而定
   「稳」✅ **保留** —— 架构侧原话"固定性成立"(模态频率由房间几何决定)
```

**⇒ ⭐ 建议因此改写(而不是撤销)—— 它换了一个频段**:
```
真正"模态少"的那一段,可以算出来:令重叠 M = 密度 × (2.2/T60) ≈ 1
  ⇒ `f(M=1) = √( c³·T60 / (8.8π·V) )`
⚠⚠ **必须取【全网格】,⛔ 不得两端各取一个 V**(critic m-4 —— 我原写的「54–110」正是这个错:
   下沿 54 用 V=100、上沿 110 用 V=60;而 §5.0 刚刚才说过 V 是 60–300 的**区间**,
   **两小节之后我自己没用上**):
  V=300, T60=0.2 ⇒ **31 Hz**(下沿)     V=60, T60=0.5 ⇒ **110 Hz**(上沿)
⇒ ⇒ **孤立模态区 = 31 – 110 Hz(全网格)**,整段落在 NHS 检测带下沿 120 Hz 之【下】
⇒ ∴ 「装机时一次性固定陷波 / 驻波修正」(PRD §五.2)的**真正对口频段是 <120 Hz**,
   即我们已知的 **NHS 盲区**(F35 实测该盲区会成为封顶:f_trig 52.2 / 22.3 Hz)
⇒ 而 `120 … f_c` 是**另一个问题**:统计上已经稠密、却又在统计理论适用域的边缘 ——
   **⛔ 两段不能用同一个方案覆盖,这正是原建议把它们混在一起的错**
```
**⛔⛔ 而这个"干净故事"有一条适用边界,必须同段写明(critic m-4 的后半)**:
```
上沿 110.3 Hz(V=60, T60=0.5)距检测带下沿 120 Hz **只剩 9.7 Hz**
⇒ 解 `f(M=1) = 120` 得 **V ≈ 51 m³**(约 4×4×3.2 m)
⇒ ⇒ **V ≲ 51 m³ 的小房间里,孤立模态区的上沿越过 120 Hz ⇒ 它与检测带【重叠】**
⇒ ⇒ ⇒ **「两段分离、各归各的方案」这个干净结论,在小会议室里【失效】**
⚠ 而 51 m³ 不是极端假设:4×4×3.2 m 是常见的小会议室/讨论间
⇒ ∴ 该建议的适用域须写成「**V ≳ 51 m³**」,⛔ 不得当作对所有会议室成立
```

**⚠ 这条(改写后的)要成立,还缺的**:
```
① <120 Hz 段的"少/稳"在**真实会议室**里成立吗 —— 我方无 RIR 实测(缺口见 ③B-1)
② 固定陷波在**房间/摆位变化后**还准吗(驻波随人数、门窗、家具移动而变)—— **未测**
③ 与 PRD §五.2 现有功能是"复用"还是"新增一条链" —— **D1 信号链未定稿,未设计**
④ **常数之争未定案** ⇒ `f_c` 未定 ⇒ 「120 … f_c」这一段的宽度未定(⛔ 归 lead/架构侧走 C9)
```
**⇒ 故:作为【路线建议】提交,⛔ 不得写进任何规格、不得对外承诺。**

### 5.3 ⛔⛔ 收尾:**这一段不是"待验" —— 是【本台架原理上验不了】**

> **这两者对路线决定的意义完全相反:前者像是"我们还没做",后者是"我们做不了"。**

**先说一个看起来最省事、而【不成立】的办法**:
```
最自然的想法:绕开文献常数之争,直接在台架上数一数那一段里有多少个临界点就行了
⛔ 不成立 —— 我们的噪声 RIR 台架**按构造就是一个统计模型**
   (`clrig.make_F` 文档明写:Rayleigh 幅度、dB 域 σ≈5.57 dB、峰间距≈4/T60,
    且选它的**理由**就是"检测带几乎全在统计区")
⇒ 在它上面数,只会**复现它自己的假设**
⇒ ⇒ 它数不出"那一段是【几个驻波】还是【上千个统计极大值】" —— 那正是要问的问题
⇒ 这是**循环论证**,不是"精度不够"
```

**⇒ 四种手段的作用域,必须分清(⛔ 别把后面的当成前面的加强版)**:

| 手段 | 能答什么 | 不能答什么 |
|---|---|---|
| **Weyl 式解析**(§5.2 那张表) | 给定 `V`,**模态数**是多少 —— 这是**物理结果,不依赖台架** ✅ | 答不了真实房间的 `V`/形状/吸声分布;也答不了"统计极大值"与"可辨模态"哪个才是陷波要对付的对象 |
| **本台架数临界点** | ⛔ **什么都答不了(在这一段)** —— 循环 | — |
| **换一个非统计的合成 plant**(critic 提的第四种) | 原理上能答「**若**这一段是可辨模态,NHS 行为**会不会**不同」—— 它答不了"真实房间长什么样" | ⛔ **本项目已试,失败**:r88 闸门未过(判别量无分辨力 + 阳性对照自身横跨 M=1);r88b 闸门过了而**结果是伪影**(`f_cross` 接缝造成 +16.66 dB 谱密度倾斜,模态臂 9/9 格 f_trig 挤在接缝下方)⇒ **FINDINGS F81,已按 lead 硬边界不修不追** |
| **实测一个真实房间的 RIR** | ✅ **本项目现有手段中唯一能定这一段性质的** | 需要素材采集工单(见 ③B-1) |

```
⇒ ∴ §5.2 那条路线建议的性质是:
   **它的承重前提【本项目现有手段原理上无法验证】,须实测真实房间才能定。**
⇒ ⛔ 不得写成"待验证"(那读起来像排期问题);
   要写成"**验证手段本身缺失**"(这是资源/工单问题,须 CTO 决定买不买)
⇒ ⚠ 而这一条同样适用于常数之争:即使 1996 那版原文到手、常数定案,
   **"那一段到底是什么"仍然要靠真实房间** —— 常数只定边界在哪,不定边界内是什么
```

| 修法 | 结果 | 出处 |
|---|---|---|
| `growth_and_gate`(判据组合改 AND) | **无从评估** —— 该 OR 旁路在本台架从未被走过(`N_bypass = 0/1802`),成因是**台架激励类别**不是死代码 | F42 |
| `R_RISE` 18 → 3(死门下调) | 触发从 0 涨到 165 次,而 **ΔMSG 48/48 行逐格完全不变**;一条种子反而变差(挂陷 3→1) | F46 |
| `prefer_unnotched`(排序键加"是否已挂陷") | **实测证伪,判不提交** | F60 |
| `recheck_free`(复检不占名额) | 深度全部到顶,**ΔMSG 0/6 变好、1/6 变差 −0.50 dB(= 一格,弱)、4/6 不可判、1/6 修法不可达** | **F79** |

> **⇒ 四条判据/分配层修法,零收益。⇒ 而 §4 解析地解释了为什么:它们改的都不是限制 ΔMSG 的那个量。**

---

## §7 已知的两条真实设计缺陷(**已定位,但修它们不改善 ΔMSG**)

```
① `_allocate` 是**开环下令**:加深 = 每个 howl 一步 −3 dB **无速率限制**,而执行以 60 dB/s 爬
   (一步需 50 ms),分析槽 16 ms ⇒ **下令速率 3.1× 于执行速率**;下令时上一步平均还差 4.08 dB 未执行
   ⇒ 诊断性反事实(加最小生效守卫):`DEPTH_EXHAUSTED` 6/6 全消失(因果链坐实),
     **但挂陷数 3 条变差、1 条变好 ⇒ 该"自然修法"净负** ⇒ 「少做一点」不等于「温和一点」
② **供给恒为 1**:`nhs.py:891` 硬截断 ⇒ 每个分析槽只有一个 howl 名额,一次复检就吃掉它
   ⇒ r86/r87 修的正是它 ⇒ **深度确实被补足,ΔMSG 不动**(§4)
```
**⇒ 这两条是真实的、可复现的设计缺陷,应当记入设计件;⛔ 但不得作为"还能再拿几 dB"的依据。**

---

```
⛔ 本文件全部 [L2/宿主仿真],未经独立 critic 评审,未 commit。
⛔ 不得对外承诺、不得进规格、不得作为其他工件的依据(治理红线 1)。
```
