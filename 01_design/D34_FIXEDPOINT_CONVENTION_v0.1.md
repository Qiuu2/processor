# D3/D4 前置件 · 定点格式与量纲约定 v0.1

```
⛔ 门禁状态:未过门(2026-08-04)
   本件未经独立 critic 评审。禁止 release / 冻结 / 被其他工件引用为依据 / 对外承诺。
   归档性 commit 须带 [未过门] 前缀(团队纪律 Commit discipline)。
作者   : channel-dsp(第 1 实例)
交付物 : D3(输入通道链)/ D4(输出通道链)的前置约定;所有模块的接口按本件对齐
配套件 : 01_design/fixedpoint/{chdsp_fixed.h, chdsp_fixed.c, chdsp_tables.h}
         01_design/fixedpoint/{PREREG_FP_r1.txt, PREREG_FP_r2_addendum.txt, PREREG_FP_r3_addendum.txt}
         01_design/fixedpoint/{results_fp_r1.txt, results_fp_r2.txt, results_fp_r3.txt}
         01_design/fixedpoint/{scan_design_bounds.py, results_design_bounds.txt}
         01_design/fixedpoint/results_r3_detail/(三个坏版本 + 阴性对照的完整输出)
         01_design/D34_CONSTANT_LEDGER_v0.1.md(自定常数台账)
```

---

## 0. 一页纸结论

| 项 | 结论 | 依据 |
|---|---|---|
| ① 样本格式 | I/O 边界 **Q0.31**(1.0 = 0 dBFS,峰值参考);链内 **Q4.27**(同一 0 dBFS 参考,+24.08 dB 余量) | [L3/解析] + [L2/宿主实测 CHK-6] |
| ② 系数格式 | **Q4.27**(范围 ±16)。a 的界是数学界(稳定三角 <2);b 的界是**解析界** `max\|b\| ≤ 2·10^(G_max/20)`,**只依赖 G_max,与 Q/S/频率无关** ⇒ 硬包络 `G_max < 18.0618 dB`(§3.2.0) | [L3/数学] + [L3/解析],数值交叉核 [L2/宿主扫描] |
| ③ 溢出 | **饱和**,禁回绕。每个窄化点饱和 + 粘滞位 + 计数,消费者三条已写死 | [L2/宿主实测 CHK-1] |
| ④ 舍入 | **就近舍入(RTN)**,禁截断 | [L2/宿主实测 CHK-2b,坏版本可杀] |
| ⑤ 累加器 | 类型范围上界 **64.81 bit ⇒ 须 ≥66 bit**;参数范围实测占用 **58.005 bit**。SHARC+ MRF = **80 bit** ⇒ 免费满足 | [L2/厂家] + [L2/宿主实测 CHK-3/CHK-11] |
| ⑥ 链内余量 | 标称 −20 dBFS 起算共 **44.08 dB**;满量程输入起算 **24.08 dB**。超出 ⇒ 饱和 + 上报 | [L3/解析] |
| ⑦ dB↔线性 | 128+1 项表 + 线性内插;**≤0.01 dB 成立于 dB ≥ −109.8125**,全域单调非降 | [L2/宿主实测 CHK-7b,双轨逐位一致] |
| ⭐ 结构 | **DF1**,禁 DF2/DF2T;**二阶误差反馈(EF)必选** | [L2/宿主实测 CHK-4,坏版本可杀] |
| ⭐ HPF/LPF | **结构约束量化**(只量化 b0,b1=∓2b0,b2=b0) | [L2/宿主实测 CHK-5f] |

> **⚠ 全部结论均为 [L2/宿主] 或 [L3/解析],⛔ 无一条是 L1。目标板未跑。**

---

## 1. 记法与参考约定(先钉死,后面所有数才有意义)

### 1.1 Q 记法
写 `Qm.n` = **1 符号位 + m 整数位 + n 小数位**,总位宽 = 1+m+n。
- `Q0.31` = 32 bit,范围 [−1, 1−2⁻³¹],LSB = 2⁻³¹
- `Q4.27` = 32 bit,范围 [−16, 16−2⁻²⁷],LSB = 2⁻²⁷

**异名声明**:常见写法「Q1.31 / Q5.27」(把符号计入整数位)与上表**同物异名**,本项目不用。
单数字写法 `Qn` 一律读作 `Q0.n` ⇒ 库内旧文的「Q31」= 本文 Q0.31,「Q15」= Q0.15。
本节继承 W1-A §3.1-1 的记法统一声明(⚠ W1-A 未过门,此处**只继承记法约定,不继承其数值结论**;
本件的系数格式是独立重推的,结论也与 W1-A 的 Q1.30 不同 —— 见 §3.3)。

### 1.2 dBFS 参考约定 —— **峰值参考**
`dBFS(x) = 20·log₁₀(|x| / 1.0)`,即 **0 dBFS ⇔ 幅值 1.0**。

> **⚠ 这是本项目已栽过的同族二义面(DEC-0010:峰值/RMS)。**
> 对**正弦**而言,「峰值参考」与「满幅正弦 RMS = 0 dBFS」两种约定给出**相同读数**(可证:
> 20log₁₀((A/√2)/(1/√2)) = 20log₁₀(A));对**噪声/节目素材**两者相差 **3.01 dB**。
> ⇒ 本文件所有 dBFS 数一律峰值参考。引用本文数字时**必须连这句一起引**。

### 1.3 电平锚点(⚠ 依赖模拟前端,前端定稿后必须回核)

| 量 | 值 | 来源 |
|---|---|---|
| 标称输入电平 | **+4 dBu ⇔ −20 dBFS**,余量 20 dB | CTO 2026-08-04 拍板 |
| ⇒ 数字满量程对应模拟电平 | **0 dBFS ⇔ +24 dBu = 12.28 V rms** | [L3/上行推算] |
| ADAU1979 满量程差分输入 | **4.5 V rms(typ,min 4.18 / max 4.82)= +15.28 dBu** | [L2/厂家 datasheet Table 1] |
| ⇒ **模拟前端须提供的净衰减** | **−8.72 dB**(15.28 − 24) | [L3/解析] |
| ADAU1962A 满量程差分输出 | **2.00 V rms = +8.24 dBu** | [L2/厂家 datasheet] |
| ⇒ DAC 后须提供的净增益 | **+15.76 dB** | [L3/解析] |

> **⭐ 这是 CTO 那条限定的具体答案,不是重复那条限定**:
> 「+4 dBu ⇔ −20 dBFS」**在 ADAU1979 上不是自动成立的** —— 该 ADC 的满量程只有 +15.28 dBu。
> ⇒ 若前端为单位增益直入,实际是「+4 dBu ⇔ **−11.28 dBFS**」,余量只剩 11.28 dB,
> 则 §6 的余量预算两个数(44.08 / 24.08)须改为 35.36 / 24.08。
> ⇒ **模拟前端选型/增益结构定稿后,本节必须回核**(已列入 §10 待办)。
>
> **⚠ 另记一条给 lead 路由(不在我职责内,但它是我噪声预算的锚)**:
> ADAU1979 动态范围 **min 103 dB / typ 109 dB(A 计权)**[L2/厂家],
> 而 PRD §一.1 要求 **动态范围 > 106 dB**。
> ⇒ **至少最坏档不满足;typ 档是否满足取决于 PRD 的计权口径,见 W1-A(⚠ 未过门)。**
> 〔整改 2026-08-04 · critic m-8:原写「最坏档器件不满足对外规格」——**比手上证据乐观**。
>  同日 W1-A v0.57 的结论更强:PRD 未声明计权口径 ⇒ **不可判**;而最可能的读法下
>  整机 typ 107.8 dB(A) ⇒ 不计权 104.8–105.8 ⇒ **typ 档也不过**。
>  ⛔ 不得给出比证据更乐观的定性。⚠ 本件只作记录,该冲突的合并归 lead(critic P-4)。〕
> 该判断属器件选型(architect / platform-fw),此处只如实记录。

---

## 2. ① 样本格式与满刻度

### 2.1 两个域,一个参考

```
codec ──► [Q0.31] ──chdsp_io_to_smp──► [Q4.27 链内] ──chdsp_smp_to_io──► [Q0.31] ──► codec
          1.0=0dBFS                     1.0=0dBFS(整数位多 4 bit)          饱和点
```
**两个域用同一个 0 dBFS 参考**,差别只在余量位数。⇒ 「这个节点的 0 dBFS 是多少」全链只有一个答案。

| 节点 | 格式 | 0 dBFS 对应整数 | LSB 的 dBFS 值 |
|---|---|---|---|
| TDM 输入槽 / 输出槽 | Q0.31 | 2³¹ | −186.6 dBFS |
| 链内全部节点(HPF/PEQ/门/压限/分频/延时/限幅) | Q4.27 | 2²⁷ | −162.6 dBFS |
| 累加器 | 见 §5 | — | −325.2 dBFS |

### 2.2 为什么链内右移 4 bit 不丢信息
codec 为 **24-bit**(ADAU1979/1962A)⇒ 输入样本在 Q0.31 里低 8 bit 恒为 0。
右移 4 bit 后仍在 ADC LSB(2⁻²³)之下留 **4 bit**(2⁻²⁷ = 2⁻²³/16)。

**实测**:20 万个 24-bit 左对齐样本,`smp_to_io(io_to_smp(x)) == x` **逐位相同,0 处不等**
[L2/宿主实测 CHK-6]。
**阴性对照(证明该检查不是恒真)**:低 8 bit 非 0 的输入(超 24-bit)确实有损 ⇒ CHK-6n PASS。

---

## 3. ② 系数格式 —— 与样本不同,而且理由分两类

### 3.1 a 系数的界是**数学界**
任何稳定二阶节的极点在单位圆内 ⇒ **稳定三角**:`|a₂| < 1` 且 `|a₁| < 1 + a₂ < 2`。
⇒ `a₁ ∈ (−2, 2)`、`a₂ ∈ (−1, 1)`,**构造上不可超越**,不依赖任何参数网格。
数值旁证:10⁶ 次随机稳定极点抽样,max|a₁| = 1.999709、max|a₂| = 0.999998 [L2/宿主]。

### 3.2 b 系数的界 —— **v0.1 用经验扫描界,已按架构侧意见改为【解析界】**

> **⚠ 变更留痕(2026-08-04,架构侧意见经 lead 转,我采纳)**:
> 本节初版给的 `11.2148` 是**经验扫描界**,其完备性**依赖我扫了哪些量程** ——
> 若 D2 的实际量程比我扫的宽,界就失效。架构侧要求换成由参数量程直接推出的**解析界**。
> **该意见成立。下面 §3.2.0 是新的权威表述;原扫描表保留作交叉核。**

#### 3.2.0 ⭐ 解析界(权威):**max\|b\| ≤ 2 · 10^(G_max/20)**,只依赖 G_max

RBJ 归一化式,记 `A = 10^(G/40)`,`α = sin ω₀/(2Q) ≥ 0`,`c = cos ω₀ ∈ [−1,1]`:

| 族 | 推导 | 解析界 | 依赖什么 |
|---|---|---|---|
| 峰型 PEQ | 令 β=α/A:`b0/a0=(1+βA²)/(1+β)`、`b2/a0=(1−βA²)/(1+β)`、`b1/a0=−2c/(1+β)` | **max(2, A²)** | **只 G**,与 Q、频率**无关** |
| 架式 | `\|b1\|/a0 = 2A\|(A−1)±(A+1)c\|/a0`,在 c=±1(⇒α=0)取到分子 4A²、分母 2 | **2A²** | **只 G**,与 S、频率**无关** |
| HPF/LPF | `\|b1\| = (1±c)/(1+α) ≤ 2/(1+α)` | **2** | 与 Q、频率**无关** |
| a 系数 | 稳定三角 | \|a1\|<2, \|a2\|<1 | 构造上不可超越 |

**⇒ 全族 `max\|b\| ≤ 2·10^(G_max/20)`;⇒ Q4.27(\|c\|<16)的解析硬包络 = `G_max < 20·log₁₀8 = 18.0618 dB`。**

**数值交叉核(EXP-7,r7)**:扫描 f∈[20,20k]×Q∈[0.02,50]×S∈[0.3,2]

| G_max | 解析界 2A² | 峰型扫描 | 架式扫描 | HPF/LPF 扫描 | 紧度(架式/界) |
|---|---|---|---|---|---|
| 6 dB | 3.9905 | 2.0000 | 3.9845 | 1.9999 | 0.9985 |
| 12 dB | 7.9621 | 3.7607 | 7.9494 | 1.9999 | 0.9984 |
| **15 dB** | **11.2468** | 5.2228 | **11.2292** | 1.9999 | **0.9984** |
| 18 dB | 15.8866 | 7.2397 | 15.8641 | 1.9999 | 0.9986 |

**⇒ 0 处越界,紧度 ≥0.998 ⇒ 解析界既正确又紧,可以替代扫描。**
⇒ **台账 C2/C3 的性质因此改变**:C2(Q 量程)从 🔴 直接降 🟢(界与 Q 无关);
  C3(增益量程)仍 🔴,但它现在是**解析式里的唯一自由量**,而不是"扫描没覆盖到"的风险。

#### 3.2.1(历史)原经验扫描表 —— 保留作交叉核,⛔ 不再作为界的依据
> ⚠ 团队纪律 D6-ah:「上界」是需要证明的断言,不是命名选择。以下如实标为**扫描界**。

fs = 48 kHz,参数范围见 §9 台账。全族归一化系数扫描结果:

| 族 | 参数范围 | max\|b\| | 取到最大值的配置 |
|---|---|---|---|
| PEQ 峰型 | f∈[20,20k], Q∈[0.3,20], G∈[−15,+15] | 2.9082 | f=11.9 kHz, Q=0.3, G=+15 dB |
| 低架 | f∈[20,20k], S∈[0.3,1.0], G∈[−15,+15] | 6.2187 | f=20 kHz, S=1.0, G=+15 dB |
| **高架** | f∈[20,20k], S∈[0.3,1.0], G∈[−15,+15] | **11.2148** | **f=20 Hz, S=1.0, G=+15 dB** |
| HPF | f∈[20,20k], Q∈[0.3,20] | 1.9999 | f=20 Hz, Q=20 |
| LPF | f∈[20,20k], Q∈[0.3,20] | 1.8430 | f=20 kHz, Q=20 |

**⇒ 扫描最大值 11.2148 @ {高架, f₀=20 Hz, S=1.0, G=+15 dB, fs=48 kHz}** [L2/宿主扫描]
**⇒ 与 §3.2.0 的解析界 11.2468 相符(比值 0.9984)⇒ 两轨自洽。**

> **⭐ 这一格正是 D-4 的形态**:W1-A 只看陷波(只有 a₁ 要装)⇒ 得出 **Q1.30(±2)**;
> 而 D3/D4 的 **PEQ 峰型就已经 2.91 > 2**,架式滤波器到 11.21。
> **⇒ Q1.30 对 D3/D4 不够用。单族正确 ≠ 全族正确。**

#### 3.2.2 ⚠ 这个界对哪个参数敏感 —— **答案不是我一开始以为的那个**

我最初把 `S ≤ 1.0` 当成保住系数范围的关键限制。**实扫之后不成立**:

| 扫描 | max\|b\|(高架 @20 Hz) | 结论 |
|---|---|---|
| S 从 **1.0 → 2.0**(G 固定 +15 dB) | 11.2148 → **11.2292**(+0.0144) | **S 几乎不影响** |
| G 从 **+15 → +18 dB**(S 固定 1.0) | 11.2148 → **15.8373** | **增益是唯一的驱动量** |

**⇒ 扫描给出的包络:架式增益 ≤ +18.089 dB;⚠ 以 §3.2.0 的【解析包络 18.0618 dB】为准(更严)**(该点 max|b| = 16.000,
配置 = {高架, f₀=20 Hz, S=1.0, fs=48 kHz})[L2/宿主二分求解]。

> **⇒ 留痕(为什么值得单写)**:若我把「S ≤ 1.0」写成保护条件,就会**把保护归给一个几乎不起作用的常数**
> —— 而真正的约束(增益 `G_max < 18.0618 dB`,§3.2.0 解析包络)没人守。这与团队纪律 **D6-r「锁错字串的 lint 比没有 lint 更坏」**
> 是同一形态。**⇒ `S ≤ 1.0` 的理由是【音质】(RBJ Cookbook:S > 1 使架式响应产生峰起),
> 不是【系数范围】。两个理由不得互相冒充。**
>
> ⇒ 若 D2 参数字典把增益放宽到 ±18 dB 以上,**Q4.27 立即不够** —— 由 §3.4 的接口硬失败拦截。

### 3.3 裁决:**统一 Q4.27**(±16)
- 覆盖 §3.2.0 解析界在 C3 假设量程(G_max = 15 dB)下的值 **2·10^(15/20) = 11.2468**,余量 **1.42×**;
  ⇒ 余量随 G_max 走,**在 G_max = 18.0618 dB 处归零**(≠ 一个固定的 1.43×)
  〔整改留痕 2026-08-04:原写「覆盖 11.2148,余量 1.43×」—— 11.2148 是**扫描界**,
   按 critic MAJOR-4 换成同一工作点的解析界 11.2468;余量随之 1.43→1.42×,**不改裁决**〕
- a₁ 精度:半 LSB = 2⁻²⁸ ⇒ 陷波中心频误差 δf = δa₁/(2r·sinω₀)·fs/2π

| 候选格式 | 半 LSB | δf @100 Hz(r=.999346) | δf @20 Hz(r=.99868) | 对 BW/4 = 3.75 Hz 判据 |
|---|---|---|---|---|
| Q1.14(16-bit) | 3.052e−5 | **8.911 Hz** | 44.58 Hz | ✗ 不过 |
| Q1.30 | 4.657e−10 | 1.360e−4 Hz | 6.803e−4 Hz | ✓ 余量 2.8e4 × |
| **Q4.27(本裁决)** | 3.725e−9 | **1.088e−3 Hz** | 5.442e−3 Hz | ✓ **余量 3448 ×** |

> **⚠ 交叉核留痕**:上表 Q1.14@100 Hz 的 **8.911 Hz** 与 W1-A / critic-w1 / lead 三方
> 独立收敛的数**逐位一致**;Q1.30 的 1.360e−4 Hz 亦与其 0.00014 Hz 一致。
> ⇒ 本人第四轨独立复算相符,**该公式可信**。(⚠ W1-A 未过门,此处只作交叉核不作依据)
> **⇒ 从 Q1.30 降到 Q4.27 换来 3 bit 系数精度的代价,在 AFC 判据下仍留 3448 倍余量。**

### 3.4 ⭐ 界失效时靠**接口**拦,不靠文档提醒(D-1)
`chdsp_coef_from_f64()` 对 |x| ≥ 16 **返回非 0(硬失败)**,调用方必须处理。
**实测**:11.2148 → 0(成功);16.0 → −1;−17.5 → −1 [L2/宿主实测 CHK-8]。
⇒ 参数范围一旦放宽到使某个 b 超 16,**系数计算当场失败**,不会静默产出错误滤波器。

---

## 4. ③ 溢出策略:饱和,禁回绕

### 4.1 为什么这条是灾难级
回绕 = int64→int32 窄化时**正峰翻成大负值** ⇒ 满量程尖峰 ⇒ 爆音 + 后级功放/高音单元风险。
本项目库内已有同型前车:ITC `tree_filterbank` 的 PF-4 缺陷(Σ|h|=1.7309 > 1,
对抗激励下 acc>>15 达 1.73× FS,原无饱和 ⇒ 符号翻转爆音)[L1/ITC 同芯片,PROVENANCE 在案]。

### 4.2 规定
- **每一个** int64/int128 → int32 的窄化点必须饱和到 [INT32_MIN, INT32_MAX]
- 目标侧硬件支持:`MRF = SAT MRF`,溢出可由 `STKY.MOS` 粘滞位读出
  **[L4/待 CCES 实测]**〔整改 2026-08-04 · critic MAJOR-3〕
  > ⚠ 厂家原文(Core PR, Table 3-5)的六个饱和值都是**格式边界**
  > (分数 → MR1F/MR0F 满位;整数 → MR0F 满位),**不是**「右移 27 之后再钳到 int32」。
  > ⇒ 与本件需要的窄化饱和**不是直接对应关系**,不得当成免费。
  > ⚠ 且本行原写「两补码**分数**」,而 §5.5 明写按**整数模式** ⇒ **本件内部两处表述互相矛盾**,
  >   已并入 §10-X4 的待定工作点。
  [L2/厂家 SHARC+ Core PR §Multiply Register Instruction Types / Arithmetic Status]
- 每次饱和 ⇒ `sat_count++` 且 `sat_sticky = 1`

**实测**[L2/宿主实测 CHK-1/1n/1z]:
- 好版本:`smp_to_io(0x7FFFFFF0)` → `INT32_MAX`,粘滞位置起 ✓;负向 → `INT32_MIN` ✓
- 坏版本(强制回绕):同输入 → **负值(符号翻转)** ⇒ 该缺陷可被观测
- **反向(护栏会不会挡掉对的)**:未溢出输入**不置**粘滞位 ✓ (CHK-1z)

### 4.3 ⭐ 报警必须接处置(团队纪律 D3)
> 「计数器让人觉得问题被管起来了,实际只是被看见了。」

`chdsp_sat_t` 的**三个消费者已写死在头文件 §7**,D3/D4 落地时逐条接线,**缺任一即视为本机制未闭环**:

| # | 消费者 | 动作 |
|---|---|---|
| C-A | 诊断遥测 | 每统计窗随电平表上报上位机 → CLIP 指示 |
| C-B | Debug 构建 | **链内(非输出端)**饱和直接 assert —— 链内饱和 = 余量策略被突破 = 设计缺陷,不是运行工况 |
| C-C | 输出保护 | 输出端饱和计数进入 D4 音箱保护限幅器输入(持续饱和 ⇒ 降增益) |

> **⚠ 接口只供事实,不供判断**(团队纪律 Interface contract #7):
> 本层只给 `{sat_count, sat_sticky}` 原值,**不给「是否该报警」的阈值判断** —— 阈值属接收方判据层。

---

## 5. ④⑤ 舍入策略、累加器宽度、在哪一步重新量化

### 5.1 累加器:两个不同的量,不许混
| # | 量 | 值 | 性质 |
|---|---|---|---|
| (a) | **类型范围上界** | \|X\|≤2³¹ ∧ \|C\|≤2³¹ ⇒ 7 项 ⇒ **64.807 bit ⇒ 须 ≥66 bit** | 构造上不可超越 [L2/实测 CHK-3] |
| (b) | **参数范围实测占用** | 最坏合法配置(低架 20 Hz/S=1/+15 dB + 满量程方波)⇒ **58.005 bit** | 经验扫描 [L2/实测 CHK-11] |

> **⭐ 留痕:我第一版写的是「int64 不够 —— 这不是保守,是算出来的」。**
> **r2 实测 58.005 < 63 ⇒ 该说法在真实参数范围内【被证伪】(预注册证伪条件命中)。**
> **已按预注册要求改正被测物,而不是改判据。** 现行正确表述:
> - int64 在 (b) 下够用、在 (a) 下不够;
> - 本项目按 (a) 要求 ≥66 bit,理由**不是**「真实工况会溢出」,而是
>   **(b) 是经验扫描,随参数范围放宽而失效,而 SHARC+ 的 MRF 本来就是 80 bit ⇒ 按 (a) 要求不花任何代价**。

**目标侧**:SHARC+ 定点 MRF = **80 bit(64 bit 结果 + 16 guard bits)**
[L2/厂家:Core PR "a multiplier with an 80-bit fixed-point accumulator";
HW Ref §38 "A 32-bit fixed-point operation generates 80-bit results (64-bit result + 16 guard bits)"]。
**宿主参考实现**:`__int128`。

> ### ⚠⚠ 前提更正:关于「SHARC+ 的 40 bit 扩展精度」
> 派单里写「SHARC+ 有 40 bit 扩展精度 ⇒ 用不用它会改变整个噪声分析」。**在定点口径下这条不成立**:
> - **40-bit 是【扩展精度浮点】格式**(比 IEEE 单精度多 8 bit 尾数)[L2/厂家 Core PR §Dual Processing Elements]
> - **定点数据在 40-bit 寄存器里左对齐占高 32 bit,低 8 bit 读时忽略、写时清零**
>   [L2/厂家 Core PR §Data Registers] ⇒ **定点路径拿不到那 8 bit**
> - 定点的额外精度来自 **80-bit MRF**,不来自寄存器宽度
> ⇒ 本文件按 80-bit MRF 做噪声分析。**该问的问题不是「用不用 40 bit」,而是「重量化点放哪」(§5.3)。**
> ⇒ **同时闭掉 W1-A 的一条 [L4/待核]**:W1-A §3.1-2 写「核心侧指令级确认待 sw_reference 精读 [L4/待核]」
>   —— 已由上述两条厂家原文确认。

### 5.2 舍入:就近舍入(RTN),禁截断
两补码丢弃低位 = 向 −∞ 取整 ⇒ **每次量化引入 −0.5 LSB 的确定性直流偏置**,
该偏置在 DF1 递归里被本节的 |1/A(1)| 放大(20 Hz Butterworth HPF:|1/A(1)| ≈ 1.0e5)。
硬件支持:`MRF = RND MRF` —— **[L4/待 CCES 实测]**,⛔ 不得计入任何算力结论
〔整改 2026-08-04 · critic MAJOR-3〕
> **厂家原文逐字**(`knowledge_base/adsp21569/bsp/sw_reference/SHARC+ Core Programming Reference` §Round MRx Instruction):
> > "The RND operation (MRF = RND MRF) **applies only to fractional results, integer results are not effected**.
> >  This operation performs a round to nearest of the 80-bit MRF value **at bit 32** …"
>
> **⇒ 两条独立的对不上**:
> ① **模式**:本件 §5.5 明写按**整数模式**,而 `RND` **在整数模式下不生效**
>    ⇒「舍入是免费修饰符」这句**在本件自选的模式下不成立**。
> ② **舍入点**:`RND` 舍在 **bit 32**,而本件需要的重量化点是 **bit 27**
>    (`CHDSP_ACC_TO_SMP_SHIFT`);即使切到分数模式,舍入点仍不是 27。
> ⇒ 目标侧要拿到本件规定的 RTN,须**显式做 80-bit 移位 + 舍入 + 饱和序列**,**那是要花周期的**。
> ⇒ 与 §10-X1「本件零算力结论」对齐:本行不再是算力依据。
[L2/厂家 Core PR §Round MRx Instruction]。

**实测**[L2/宿主实测 CHK-2b]:
- 好版本(RTN + EF):`mean(y_定点 − y_参照)` = **4.99e−12 满刻度 = 0.001 LSB**
- 坏版本(截断 + 无 EF):|mean| ≥ 1e−5 ⇒ 被杀死

> **⚠ 一条自曝**:我最初的 CHK-2 测的是「输出信号的样本均值」,判据 ≤1e−7。
> r2 用 8 个种子复测,**种子间标准差 s = 1.226e−6** ⇒ **判据落在测量噪声内,该检查没有分辨力**
> (LESSONS C-3:分辨力之下的不等号既不能证真也不能证伪)。
> ⇒ 换被测量为「**算术误差**的均值」(CHK-2b)后才有分辨力。
> ⇒ **原 CHK-2 已退役但未删除**,判据与实测原样留在 results 里(E-2:加标注不删数)。

### 5.3 结构:**DF1**,禁 DF2/DF2T —— 理由是量的
DF2/DF2T 的内部节点 `w` 的传函是 `1/A(z)`,其峰值增益决定所需整数位:

| 算例 | max\|1/A\| | dB | DF2 需额外整数位 |
|---|---|---|---|
| PEQ 20 Hz Q=20 G=+15 dB | 5.711e6 | **135.13** | **23 bit** |
| PEQ 20 Hz Q=20 G=0 dB | 2.789e6 | 128.91 | 22 bit |
| HPF 20 Hz Q=0.7071 | 1.462e5 | 103.30 | 18 bit |
| PEQ 1 kHz Q=20 G=0 dB | 1.178e3 | 61.42 | 11 bit |

**⇒ 32-bit 字长装不下 ⇒ DF2 在本参数范围内不可用。** [L2/宿主]
DF1 的状态是 x 与 y 本身(均受信号电平约束),**每节只有一个量化器,在节输出处**。

### 5.4 ⭐ 二阶误差反馈(EF)—— **必选,不是可选优化**
DF1 的输出量化误差 E 经 `1/A(z)` 到达输出 ⇒ 噪声增益 `NG = Σ|h_{1/A}|²`。
把 `C(z) = A(z)` 的误差反馈加回累加器 ⇒ 噪声传函 `C/A ≡ 1` ⇒ 噪声白化。

**实测(定点仿真,Q4.27,8 个算例)**[L2/宿主实测 CHK-4]:
白噪基准 `q²/12 = −173.35 dBFS`(q = 2⁻²⁷)

| 算例 | NG (dB) | EF 开:实测 | 偏离基准 | EF 关:实测 | EF 收益 |
|---|---|---|---|---|---|
| PEQ 20 Hz Q=20 G=+15 dB | 91.22 | **−173.35** | −0.00 | **−84.44** | 88.9 dB |
| PEQ 20 Hz Q=20 G=−15 dB | 83.71 | −173.35 | −0.01 | −92.28 | 81.1 dB |
| PEQ 20 Hz Q=8 G=+15 dB | 87.23 | −173.36 | −0.01 | −88.63 | 84.7 dB |
| PEQ 100 Hz Q=20 G=+15 dB | 70.24 | −173.33 | +0.02 | −101.93 | 71.4 dB |
| PEQ 1 kHz Q=20 G=+15 dB | 40.29 | −173.35 | −0.00 | −133.16 | 40.2 dB |
| HPF 20 Hz Q=0.7071 | 72.96 | −173.36 | −0.01 | −99.88 | 73.5 dB |
| HPF 80 Hz Q=0.7071 | 54.95 | −173.36 | −0.01 | −118.50 | 54.9 dB |
| 低架 20 Hz S=1 G=+15 dB | 78.57 | −173.37 | −0.02 | −94.28 | 79.1 dB |

> **出处**:「EF 开」列 = `results_fp_r3.txt` §1;「EF 关」列 = `results_r3_detail/out_CHDSP_BROKEN_NOEF.txt`
> (同一份 `check_fixed.c`,仅 `-DCHDSP_BROKEN_NOEF=1`)。两列同 deps 行,可复现。
> 解析预测见 `results_design_bounds.txt` §D(无 EF 预测 −82.14 … −118.40,与实测差 ≤2.3 dB)。
>
> **⇒ EF 开时噪声底与 NG(跨度 40–91 dB)完全无关,最大偏离基准 0.02 dB。**
> **⇒ EF 关时最坏 −84.44 dBFS ——【连 PRD 的 >106 dB 动态范围都不过】。**
> **⇒ 因此 EF 不是优化项,是及格线。**

**代价**:2 乘 + 2 移位 + 2 状态字 / 节 / 样本。
〔整改 2026-08-04 · critic MAJOR-3 ③:原句尾带「约 +40% 的 biquad 内循环 op 数」——
 **那是一条没有 L 标的算力结论**,与 §10-X1「本件零算力结论,全部待 W1-C」直接冲突。
 ⇒ **二选一,不得并存 ⇒ 本件选择删除它**(⛔ 不是补个 L 标留着):
 op 数与周期数不是一回事,而本件没有任何定点口径的周期锚点。算力归 D13/W1-C。〕
**第二轨复核**:`ref_fixed.py`(python 独立重写,不 import C)四个算例给 −173.34…−173.37,与 C 轨一致。

### 5.5 重量化点与移位量
```
acc_raw = Σ Xᵢ·Cᵢ              (整数精确,X:Q4.27,C:Q4.27 ⇒ 真值 = acc_raw / 2⁵⁴)
y_raw   = RTN(acc_raw / 2²⁷)   (⇒ 回到 Q4.27),然后饱和到 int32
```
**移位量恒等于 `CHDSP_COEF_FRACBITS`,由格式推出,⛔ 调用方不得自选。**
头文件不提供任何暴露移位量的接口(见 §8 的负向编译测试 N5)。

> **⚠ 目标侧待确认 [L4/待 W1-C 或 CCES 实测]**:SHARC+ 定点乘法的**分数模式**会自动左移 1 位
> 去掉冗余符号位;**整数模式**不会。本文件按**整数模式**语义写(移位 27)。
> 若目标实现用分数模式,移位量改 26 —— **该差异只在 `chdsp_acc_to_smp_r()` 一处**,
> 且由 CHK-9(逐位一致)兜底。⛔ 未在目标板确认前,不得当作已定。

---

## 6. ⑥ 链内余量策略

### 6.1 余量账
```
链内格式 Q4.27 ⇒ 整数位 4 bit ⇒ 余量 = 4 × 6.0206 = 24.0824 dB
```
| 起点 | 到链内饱和的余量 |
|---|---|
| 标称输入 −20 dBFS | **44.08 dB**(20 dB 信号余量 + 24.08 dB 格式余量) |
| 满量程输入 0 dBFS | **24.08 dB** |

⚠ 上两个数依赖 §1.3 的电平锚点。**若模拟前端为单位增益直入(+4 dBu ⇔ −11.28 dBFS),
第一行改为 35.36 dB。**

### 6.2 规则
1. **链内允许超过 0 dBFS**(这是 Q4.27 存在的理由):PEQ +12 dB 作用在 −20 dBFS 上得 −8 dBFS,
   作用在 −6 dBFS 上得 **+6 dBFS —— 在 Q0.31 下会削,在 Q4.27 下不会**。
2. **链内饱和 = 设计缺陷,不是运行工况** ⇒ Debug 构建下 assert(消费者 C-B)。
3. **全链唯一合法的削波点 = `chdsp_smp_to_io()`**(输出端 Q4.27→Q0.31)。
   在它之前必须由**输出限幅器**把电平压住(D4 职责)。
4. 用户可把 8 段 PEQ 全部 +15 dB 叠在同一频率 ⇒ 链内提升可超 24.08 dB。
   本层**不阻止**,只保证:饱和而非回绕、被计数、被上报。**处置属 D3/D4 的增益结构设计。**

### 6.3 余量的代价 —— 以及为什么它现在是免费的
链内每让出 1 bit 余量,量化噪声底抬高 6.02 dB。Q0.31 → Q4.27 抬高 **24.08 dB**。
**但在 EF 之后,噪声底是 −173.35 dBFS/节**,而设计目标是 ≤ −120 dBFS ⇒ 余量 53 dB。
⇒ **EF 把「余量」与「噪声」这两个原本对立的目标解耦了。** 若无 EF,4 bit 余量会把
最坏算例推到 −84.44 dBFS 量级,完全不可接受。

### 6.4 D-4:整条链的噪声底(⛔ 不许只验单级)

> ### ⛔⛔ 适用范围声明(**先读这一段,否则下表会被读成它不成立的东西**)
> 下表的模型 `噪声底 = −173.35 + 10·log₁₀(N)` 是**把 N 个量化噪声源直接功率相加**,
> 它**只在【各节增益 = 1】的前提下成立**。
> 第 k 节的量化噪声要**经过第 k+1…N 节的传函**才到链末,而链内每一级的增益是自由参数
> —— 而 **§6.2-4 明文允许**用户「把 8 段 PEQ 全部 +15 dB 叠在同一频率」。
> **⇒ 「各节增益 = 1」是本表工作点向量的一部分,与「28 个量化器」同等重要。**

#### 6.4.1 各节增益 = 1(模型前提内)

| 链 | 量化器数 | 噪声底 = −173.35 + 10·log₁₀(N) |
|---|---|---|
| 单节 | 1 | −173.35 dBFS |
| 8 段 PEQ | 8 | **−164.32 dBFS** |
| D3 输入链(HPF+8×PEQ+门+压限+延时+限幅 ≈ 12) | 12 | −162.56 dBFS |
| D4 输出链(10×PEQ+LR8 4 节+限幅+延时 ≈ 16) | 16 | −161.31 dBFS |
| D3 + D4 全链 | 28 | **−158.88 dBFS** |

| 对标 | 值 | 余量(**仅在各节增益 = 1 下**) |
|---|---|---|
| PRD 动态范围 > 106 dB ⇒ 底噪 ≤ −106 dBFS | −106 | 52.9 dB |
| 本人设计目标(算术噪声比转换器噪声低 ≥10 dB)⇒ ≤ −120 dBFS | −120 | 38.9 dB |
| ADAU1979 动态范围 typ 109 dB | −109 | 49.9 dB |

#### 6.4.2 ⛔ 按**最坏合法增益配置**(级联复算,含级间增益)

每节噪声 = q²/12,经其后各节 |H|² 在 0…π 上取均值后求和
[L3/解析 + L2/宿主复算 `d34_chain/check_noise_chain_r5.py`,`results_noise_chain_r5.txt`]:

| 配置 | 实算链末 | 与 −164.32 差 |
|---|---|---|
| ① 8 节全单位增益(= 6.4.1 的前提) | −164.32 dBFS | **±0.00** ✓ 模型在此前提下**精确** |
| ② §9 CHK-5 用的那 8 段(+6,−8,+4,−10,+12,−6,+9,−15) | −164.49 dBFS | −0.17 |
| ③ 8 段各 +6 dB 分散(现实的一档) | −160.12 dBFS | +4.20 |
| ④ **§6.2-4 明文允许**:8 段全 +15 dB 同频(1 kHz,Q=1.4) | **−91.64 dBFS** | **+72.68** |
| ④′ 同配置下的 **D3 输入链**(HPF 的噪声再经 8×+15 dB) | **−76.97 dBFS** | **+85.59** |

| ④ 对标 | 结果 |
|---|---|
| PRD ≤ −106 dBFS | ⛔ **突破 14.36 dB** |
| 本人设计目标 ≤ −120 dBFS | ⛔ **突破 28.36 dB** |

**⇒ §6.4.1 表里的「余量 52.9 / 38.9 dB」在配置 ④ 下不存在。**

> **⚠ 两件事必须同时说清,免得被读成比实际更严重 / 更轻**:
> - 配置 ④ 里信号本身也被抬 +120 dB,链内 24.08 dB 余量会先饱和 ⇒ 它是个**退化工作点**。
>   但**噪声是链内产生、被后级放大的** ⇒ 把输入调小并不会降低它
>   ⇒ **链末可用动态范围确实真实地少了 72.68 dB**,这不是量的类别错。
> - 配置 ②③ 表明:**现实预设下,结论方向(算术噪声不是瓶颈)仍然成立。**
>
> **⇒ ∴ 本次改的是【那个数的适用范围】,⛔ 不是格式裁决**(Q4.27 / EF / DF1 全部不动)。
> **⇒ 配置 ④ 若判为不可接受,处置属 D3/D4 的【增益结构设计】**
> (把大增益放到链尾、或限制累计增益)—— ⛔ 不是改本件的格式。本件不替 D3/D4 裁。

> ⚠ 另一维(**原脚注只讨论了这一维**):上表假设各节量化噪声互不相关(标准假设);
> 相关时最坏 10·log₁₀(N) → 20·log₁₀(N),即 8 节最坏 −155.3 dBFS ⇒ **该假设值 9 dB**。
>
> **⛔ 整改留痕(2026-08-04 · channel-dsp 实例 #2 · critic MAJOR-2)**:
> 本节原先**只有** 6.4.1 那张表 + 上面这条相关性脚注,而结论句写「算术噪声不是本设计的瓶颈」。
> **⇒ 问题不是算术错**(6.4.1 的算术 critic 逐行核过,全对),**是模型少了一维**:
> 脚注去讨论了**值 9 dB** 的相关性假设,**漏掉了值 73 dB 的级间增益**。
> **⇒ 而本节标题正是 D-4「⛔ 不许只验单级」** —— D-4 被用在了系数/传函轴上(§9.3),
> **没被用在噪声轴上**。
> **⇒ 这是 LESSONS B-4-③ 的原样复发:一整套检验全过,却没有一条检查决定性的那一维,
>    因为没人问过「这套模型覆盖了哪些维度」。**
> **⇒ 护栏建好之后,下一步是问「这个护栏防住了哪些维度」,而不是问「它跑绿了吗」。**

---

## 7. ⑦ dB ↔ 线性换算与查表精度

### 7.1 约定
- dB 量类型 `chdsp_db_q23_8_t` = **Q23.8**,步进 **1/256 dB ≈ 0.0039 dB**
  ⇒ 若参数字典用 0.1 dB 步进,量化误差 ≤0.00195 dB,且相邻档相距 25.6 LSB(不会并档)
- `g = 10^(dB/20) = 2^(dB·K)`,`K = log₂(10)/20 = 0.16609640474436810`
- 表:`2^(i/128)`,i = 0..128,**Q2.29**,+ 线性内插。表内存 **516 B**(另 516 B 给 log₂ 反查)
- 定义域:钳到 `[−144 dB, +24 dB]`(**肯定式:先钳后算** ⇒ 异常/未来新增取值默认落安全侧,团队纪律 D-2)
- `dB ≤ −144` ⇒ **精确返回 0**(静音,不是「很小的数」)

### 7.2 实测精度 [L2/宿主实测 CHK-7/CHK-7b,43009 点全扫]
| 项 | 值 |
|---|---|
| **max\|误差\| ≤0.01 dB 成立的真实 dB 下界** | **−109.8125 dB**(最后一个仍合规的 1/256 dB 格,\|误差\| = 0.006097) |
| ⚠ 首个**越界**格(⛔ 不是下界) | −109.8164 dB,\|误差\| = 0.010003 |
| max\|误差\| 全域 [−144, +24] | 0.5256 dB(在 −144 附近) |
| 单调性 | **43009 点无一处下降** ✓ |
| 0 dB | 精确 = 1.0(raw = 2²⁷) ✓ |
| +24 dB | raw = 2127213458 < INT32_MAX ✓(线性 15.848975) |
| dB→线性→dB 回环 | max 0.0039 dB(仅电平表用途) |

**误差归因** [CHK-7b]:
- 「移位/常数」路径固有误差 = **1e−9 dB** ⇒ 表长不是瓶颈
- 在下界 −109.8125 dB 处:gain = 3.2336e−6 ⇒ raw = 434 ⇒ **半 LSB 相当于 0.010001 dB**

> **⚠ 整改留痕(2026-08-04 · channel-dsp 实例 #2 · critic m-2)**:本节原写下界 = **−109.816 dB**,
> 那是**首个越界格**而不是**最后一个合规格** —— 即报出来的"下界"本身 \|误差\| = 0.010003 > 0.01,
> **不满足它自己声称的判据**。成因:`check_fixed.c` 原在 `run_max > 0.01` **之后**才记 `bound_q8`。
> 已改为判据仍成立时才记。两个读数(0.010003 / 0.006097)与 critic 独立复核**逐位相符**。
> ⇒ 这与本件"不移动标杆"的自律同档:**标杆本身也要放在对的格上。**
- ⇒ **瓶颈是输出格式 Q4.27 的 LSB,在本格式下不可约**;加长表无用

> **⚠ 留痕(不移动标杆)**:我预注册的判据窗口是 `[−110, +24] 内 ≤0.01 dB`,实测在 **−109.938 dB
> 处为 0.010178 dB ⇒ 按预注册判据 FAIL**。预注册的**证伪条件(>0.02 dB)未触发** ⇒ 设计未被推翻,
> 但**我的判据窗口比真实包络严了 0.18 dB**。
> ⇒ 处置:**⛔ 不把窗口改成 −109.5 然后宣布通过**;原判据原样留在 results 里,
> 规格改按上表的**实测包络**书写,并写明机理。

### 7.3 实现一致性核 + 铁律七的独立轨(⛔ 两件事,分两行报)

> **⛔ 整改留痕(2026-08-04 · critic MAJOR-6)**:本节原标题是「双轨交叉核(铁律七)」,
> 把两件**性质不同**的事合并成了一句。核实:`ref_fixed.py` 与 `chdsp_fixed.c` 是**逐步同构**的
> —— 同样的 `K_Q40`、同样的 `>>16/>>32` 序列、同样的 `idx/lam` 拆分、同样的 `+half` 舍入、
> 同样的 `n−2` 移位;`df1_ef` 亦是逐行转写。
> **⇒ 「43009 点逐位相同」能证的是【转写忠实】(可抓表项转录错、移位顺序错),
>   ⛔ 不能证【算法对】** —— K 取错、舍入语义两边同错、移位口径两边同错,都会一致通过。
> **⇒ 同式一致 = 没复算。**
>
> **⚠ 但实质没有塌:真正独立的那一轨存在且两边都做了。** 见下 (b)。
`ref_fixed.py` 是 python 整数算术的**独立重写**(不 import C)。
**结果:43009 点 dB 表两轨【逐位相同,0 处不等】**;
**阳性对照**:强制改动其中一个值后比对器**报出差异** ⇒ 上一行的「相同」有意义 [L2/宿主]。

> ⚠ 第二轨的 `T1a` 报 FAIL,**那是同一条已退役的判据**(≤0.01 dB / 窗口 [−110,+24]):
> python 轨独立复算给出 **0.010178 dB**,与 C 轨**逐位相同的那个数**。
> ⇒ 这不是新缺陷,是**两轨对同一个数的独立确认**;判据退役的理由见 §7.2 的留痕。
>
> **⛔ 整改留痕(2026-08-04 · channel-dsp 实例 #2)—— 上面这段话当时是对的,但它掩盖了一件事:**
> **C 轨把 CHK-7 走了 `RETIRED()`(打印但不计入判定),而 py 轨的 `T1a` 走的是 `chk()`
> ⇒ 它计入判定 ⇒ `ref_fixed.py` 实际以 `exit 1` 收尾**,归档件 `results_fp_r3.txt` 里
> 白纸黑字写着「第二轨结果: 未通过 T1a」「第二轨退出码 = 1」。
> **而 `run_r3.sh` 只【打印】REF 不【消费】它 ⇒ 整轮仍以 exit 0 收尾。**
> ⇒ **∴ r3 那一轮是【带着一个 FAIL 通过的】,而没有任何人、任何机制会因此被拦住。**
> ⇒ 这正是 critic MAJOR-1 ② 指出的那个缺陷**已经在生效**的实例
>   —— critic 写「本轮三个变异恰好都被杀死,所以没出事」,**其实出了事,只是不在变异那一路**。
> **处置**:py 轨补 `chk_retired()`(与 C 轨 `RETIRED()` 同语义),`T1a` 退役留痕;
> 另立 **`T1b`**(按实测包络 −109.8125 dB 判定)+ **`T1b+`**(阳性对照:相邻一格即不等,
> 证明 T1b 的分辨力是 1 格而不是"任意值都过")。`run_r3.sh` 现已消费全部退出码。

### 7.4 本节显式砍掉的
- **增益平滑 / 斜坡 / 防爆音**:属 D3/D4(参数跳变的平滑是模块契约的一部分),本件只定换算。
  ⛔ 不静默省略,已列入 §10。

---

## 8. ⭐ 量纲怎么被强制(D-1:接口不允许 > 文档提醒)

### 8.1 机制
- 五个量各自是**独立 struct**(`CHDSP_STRICT_TYPES=1`)⇒ 互相赋值/传参/相加**编译不过**
- **不提供任何暴露移位量的接口** ⇒ 调用方写不出 `(int32_t)(acc >> 27)`
- 定标转换只有两条合法通道:`chdsp_io_to_smp()` / `chdsp_smp_to_io()`

### 8.2 机械证明 [L2/宿主实测 CHK-10]
| 片段 | 内容 | 预期 | 实测 |
|---|---|---|---|
| N1 | io(Q0.31) 当 smp(Q4.27) 用 | 编译失败 | ✓ 失败 |
| N2 | coef 当 smp 用(**Q 格式相同、量纲不同**) | 编译失败 | ✓ 失败 |
| N3 | 跨定标裸相加 io + smp | 编译失败 | ✓ 失败 |
| N4 | **dB 当线性增益用** | 编译失败 | ✓ 失败 |
| N5 | 调用方自己写 `(acc >> 27)` | 编译失败 | ✓ 失败 |
| P1 | 同类型赋值 | 编译**通过** | ✓ 通过 |
| P2 | 经转换函数 | 编译**通过** | ✓ 通过 |
| P3 | `chdsp_io_raw()` 取值(打 TDM 帧) | 编译**通过** | ✓ 通过 |

> **⚠ P1–P3 是团队纪律 D6-y 的第二个方向**:护栏不仅要问「会不会放过错的」,
> 还要问「**会不会挡掉对的**」。只做 N1–N5 就是单向验证。

### 8.3 目标 build 的逃生口
`CHDSP_STRICT_TYPES=0` 退化为裸 int32(防 ABI 开销)。
**使用前提**:CHK-9 已证两种构建**逐位一致**(20000 样本,SAME),
且**阳性对照**(强制改第 5000 行为错值 ⇒ DIFF)证明比对器有分辨力 [L2/宿主实测]。
⇒ **CI 必须用 `=1` 构建**(类型检查在那里发生),`=0` 仅供目标出货 build。

---

## 9. ⭐ HPF/LPF 的结构约束量化(D-4 的第二个战果)

### 9.1 缘起
r1 在 fc = 80 Hz 观察到自由量化的高通 DC 增益恰好为**精确 0**。
**「这是巧合还是保证?」r1 没有回答。** ⇒ r2 预注册 CHK-5f 去扫。

### 9.2 实测(fc 从 20 Hz 到 20 kHz 扫 500 点)[L2/宿主实测 CHK-5f]
| 量化方式 | 高通 DC 零点被破坏 | 低通 Nyquist 零点被破坏 | 最坏泄漏 |
|---|---|---|---|
| **自由量化**(b0,b1,b2 各自取整) | **251 / 500** | 261 / 500 | **单节 DC −59.26 dB @ 20 Hz** |
| **结构约束量化**(只量化 b0,b1=∓2b0,b2=b0) | **0 / 500** | 0 / 500 | 精确 0(构造保证) |

> **⇒ r1 的精确 0 确实是巧合。自由量化在半数 fc 上破坏零点,最坏单节 DC 泄漏 −59.26 dB
> —— 对超低音分频这是硬缺陷。**
> **⇒ 规则**:HPF/LPF 一律走 `chdsp_coef_hplp_from_f64()`(接口保证),⛔ 不得各自量化 b。
> **⚠ 适用范围**:仅限 `b1 = ∓2b0 ∧ b2 = b0` 的族(Butterworth / LR 的 HPF、LPF)。
> **PEQ / 架式不属该族,不得套用。**

### 9.3 级联响应(D-4 的正面数)[L2/宿主实测 CHK-5]
| 项 | 值 | 规格 |
|---|---|---|
| 8 节非平坦 PEQ 级联,量化 vs 理想 max\|Δ\| **(仅系数量化项)** | **0.003859 dB @ 20 Hz** | PRD ±0.3 dB ⇒ 余量 78× |
| LR8 高通(4 节)DC 增益 —— ⚠ **自由量化 @fc=80 Hz** | 精确 0 | ⛔ **不构成结构约束的证据** |
| LR8 低通(4 节)Nyquist 增益 —— ⚠ 同上 | −698 dB | 同上 |
| ⭐ **LR8 四节级联 · 结构约束量化 · 扫 400 个 fc** | **DC / Nyquist 非 0 的 fc 数 = 0 / 400** | [L2/宿主实测 **CHK-5g**] |
| ⭐ 阳性对照:同一批 fc 走**自由量化** | DC 非 0 = **189 / 400** | [L2/宿主实测 **CHK-5g+**] |

> **⛔ 整改留痕(2026-08-04 · critic MAJOR-5)**:上表第 2/3 行原标「精确 0(**结构约束下**)」。
> **那个标注是错的** —— 产出它的 `CHK-5d` 走的是 `to_fixed_bq()` = **自由量化**,
> 且 fc 取**单点 80 Hz**,而 §9.1 自己写着「r1 在 fc = 80 Hz 观察到的精确 0 **确实是巧合**」。
> **⇒ 表里那个"精确 0"就是那个巧合本身,不是结构约束的战果。**
> **⇒ 后果(具体、不夸大)**:「本件强制的结构约束量化,在 LR8 **四节级联**下表现如何」
> 这件事**实际未测**,而这张表让人读成已测(§9.2 的 500 点扫描只做**单节**)。
> ⇒ 已补 **CHK-5g**:走 `chdsp_coef_hplp_from_f64()`,**扫 400 个 fc**,级联 DC/Nyquist 恒精确 0。
> ⇒ ⭐ 并按 **D6-c** 记一条:**单点 fc 的级联检查在构造上就有一半概率给出"好消息"**
>   —— §9.2 已证 251/500 的 fc 会破坏零点,而 CHK-5d 恰好抽中了不破坏的那一半。
> **⇒ 判别法:凡"在某个工作点上通过"的检查,先问【这个工作点是怎么选的】。**

> ⚠ **m-10**:本表首行只含**系数量化项**,⛔ 不含信号路径项(饱和、极限环、EF 残差)。
> 信号路径项见 §5.4 与 §10-X3(X3 自认未验)。⇒ 「余量 78×」**只对系数量化这一项成立**。
| 第二轨复核(python 独立) | **0.003859 dB @ 20 Hz(逐位同)** | — |

**⛔ 一条被我自己证伪的断言,留痕不删**:
探索阶段我写下「0 dB 的 PEQ 节对信号是直通、对量化噪声是 87 dB 的放大器 ⇒ 必须旁路」。
**实跑证伪**:G = 0 时 b ≡ a(逐位),累加器低位恒 0 ⇒ **量化器根本不产生误差,输出与输入逐位相同**。
⇒ **该断言作废。旁路 0 dB 节的理由是【省算力】,不是【省噪声】。**

---

## 10. 显式砍掉 / 待办(⛔ 不静默省略)

| # | 项 | 状态 |
|---|---|---|
| X1 | **一切算力/周期数** | 本件零算力结论。全部待 W1-C / 目标板,标 [L4/待验证] |
| X2 | SHARC+ 定点乘法**分数模式 vs 整数模式**的隐含 ×2 | [L4/待 CCES 实测],影响仅 §5.5 一处移位量 |
| X3 | **极限环 / 怠机音**长时零输入扫描 | 未做。EF 在设计上消除主因,但**未验** ⇒ 列入 D14 |
| X4 | **denormal 防护** | 定点无 denormal 问题,本件不涉;若旁链走 float 则属 adaptive-dsp |
| X5 | **FIRA 定点模式**的实际吞吐与形态 | 见 §11,只记结构性事实,不估数 |
| X6 | **增益平滑 / 系数插值 / 防爆音** | 属 D3/D4,本件只定 dB↔线性换算 |
| X7 | **模拟前端增益结构回核**(§1.3) | **阻塞项**:前端选型定稿后必须回核,否则 §6 余量账两个数要改 |
| X8 | **参数范围与 D2 参数字典对齐** | §3.2 的 Q/S/增益范围是**我方假设值**,须 architect 确认(见台账) |
| X9 | 8 段 PEQ 的「动态」语义(PRD §二.4「动态参量 EQ」) | 未定,属 D3 任务二 |

---

## 11. FIRA 定点模式的结构性事实(D4 输出链 FIR 的前置)

**只记厂家原文,⛔ 不估数**(全部 [L2/厂家 HW Ref §38 / EE-408]):

1. **FIRA 支持 32-bit 定点**(`FIR_CTL1.FXD=1`),两补码可选(`TC`),
   舍入模式可选(`RND`:0 = 就近,1 = 截断)。
2. **定点模式下,每个输出样本回写的是完整 80-bit 结果,按 3 × 32-bit 突发**
   (LSW / MSW / 16-bit overflow)⇒ **`WINDOWSIZE = WINDOWSIZE × 3`,且须由核心侧
   「decimate 输出缓冲到目标样本」**。原文:*"A final routine must decimate the output
   buffer to the desired samples."*
3. **定点模式不支持 multi-iteration ⇒ 最大 TAP 长度 = 1024。**
   (与 DEC-0006 风险声明②已记录的「FIRA 定点 ≤1024 taps」一致)
4. **有符号分数格式时输出须 ×2**(硬件不右移去冗余符号位)。
5. **IIRA 没有 FXD 位** —— 其 `FORTYBIT` 选的是 32-bit vs 40-bit **IEEE 浮点**
   ⇒ **IIRA 是浮点专用**,在定点链里使用须做格式转换。
   (与 DEC-0014「IIRA 仅作卸载」不冲突,但**卸载要付格式转换的代价**,此前未记。)

> ### ⚠ 【待核】IIRA 的硬件系数格式(2026-08-04,lead 提出,我未查到)
> **状态**:lead 自查一次未查到;我在 HW Ref §38 的 IIRA 段只确认了 `FORTYBIT`(32/40-bit
> IEEE 浮点二选一)与 `RND`(舍入模式),**未找到系数字长/定标的明文**。
> **纪律(lead 指示,我采纳)**:biquad 已裁走**核心**、IIRA 仅作卸载 ⇒ **目前不影响本约定**;
> 但**若日后有任何一段卸载到 IIRA,其系数格式由硬件定死,⛔ 不得默认可以自由选 Q4.27**。
> **⇒ 本条标 [L4/待核],使用 IIRA 前必须先查明并回填。**

> **⇒ 对 D4 的含义**:0.25 cyc/tap 是 FIRA 的**计算**侧;定点模式另有 **3× 输出 DMA + 核心侧
> decimate** 的代价,短滤波器上可能吃掉优势。**⛔ 本件不给数** —— 属 W1-C / D13。

---

## 12. ⚠ 三条须由 lead 路由的前提问题

### 12.1 EE-408 的「2.5 cyc/biquad」厂家锚点,其工作点向量缺一维:**数据格式 = 浮点**
**直接观测**(读 EE-408 随附代码包原文,非二手转述):
`EE408V02/ADSP_2156x_IIR_Core_Performance/src/IIR_Core_Throughput_21569.c` 载明——
```c
float input[MAX_WINDOW_SIZE];  float output[MAX_WINDOW_SIZE];
float state[2*MAX_BIQUADS];    float pm coeffs[5*MAX_BIQUADS];
biquad(&input[0], &output[0], &coeffs[0], &state[0], window, biquads);   /* CCES <filter.h> */
```
FIR 侧同(`ADSP_2156x_FIR_Core_Performance` 亦为 `float` + `fir()`)。
EE-408 正文亦自陈:*"Core FIR/IIR cycles are measured with the help of the optimized library
functions available with CCES."*

> **⇒ 「SHARC+ 核心 IIR = 2.5 cyc/biquad」是【浮点】数。定点值未测。**
> ⇒ 这是团队纪律 **D6** 的形态(「改这个参数,这个数会变吗?会变就必须在向量里」):
> 定点 biquad 比浮点多出**移位 + 饱和 + 就近舍入**,再加本件强制的 **EF(2 乘 2 移位)**。
> ⇒ **建议给该锚点加限定串**:`2.5 cyc/biquad @ 浮点·CCES biquad()·L1·[L2/厂家]`,
> 并入 D6-x 限定串表。⛔ 我不改台账,报 lead 裁定。
> ⚠ **我没有测过定点 vs 浮点的周期差,不给方向性判断** —— 那是 W1-C 的题。

### 12.2 PRD 与 DEC-0006 在数制上的字面冲突,未见 PRD 偏离留痕
PRD §一.1 原文:「48kHz采样,**24bit/32bit浮点DSP运算**」。
DEC-0006 锁定点(「浮点硬件在手,项目锁定点口径——MCPS 效率考量非硬件限制」)。
⇒ 这是一条 **PRD 偏离**,但库内未见类似 DEC-0007 第 3 项那样的「PRD 偏离留痕 + 对外规格同步」条目。
⇒ 报 lead:若对外规格书沿用 PRD 字面「32bit 浮点运算」,与实现不符。**我不自行处置。**

### 12.3 IF-v1.4 回执里一处 dBFS 换算的算术看着不自洽(不在我职责内,只报不改)
回执写:「−128.19 dBFS(峰值参考);若换『满幅正弦 RMS = 0 dBFS』约定则为 −119.2 dBFS」。
两种约定对同一个 **RMS 量**的差按定义恒为 **20·log₁₀(√2) = 3.01 dB**,给不出 9.0 dB。
⇒ 或是标签写错、或是数写错。**该数属 adaptive-dsp/architect 的 AFC 检测底噪链**,
⇒ 报 lead 路由,⛔ 我不动它。

---

## 13. 自验与假绿纪律(证据在 `01_design/fixedpoint/results_fp_r*.txt`)

| 项 | 结果 |
|---|---|
| 好版本(r3) | **PASS = 25,FAIL = 0,RETIRED = 3**(退役项原样留痕,不计入判定) |
| **杀伤矩阵**(同一份**好判据**跑在坏模块上) | WRAP → 杀 CHK-1 / CHK-1n;TRUNC → 杀 CHK-4 / CHK-2b;NOEF → 杀 CHK-4 / CHK-2b。**3/3 变异全部被杀死** |
| **阴性对照** | 好模块 + 好判据:**零 FAIL** ⇒ 上面的杀死确由变异引起,不是检查本身红的 |
| 负向编译(CHK-10) | N1–N5 **全部编译失败**(应失败);P1–P3 **全部编译通过**(应通过) |
| 逐位一致(CHK-9) | STRICT=1 vs 0:20000 样本 **SAME**;**阳性对照**:强制错值 ⇒ **DIFF** |
| 第二轨(铁律七) | dB 表 **43009 点逐位相同**(+阳性对照);噪声底、级联响应两轨一致。其 `T1a` FAIL = 同一条已退役判据的独立复现(0.010178),非新缺陷 |
| 自验件真依赖被测物 | `check_fixed.c` **#include 并调用** `chdsp_fixed.h` 的函数;定点算术只经 `chdsp_*` 发生,⛔ 无公式转写 |

**⚠ 为什么 r1/r2 的杀伤证据不算数(自曝)**:r1/r2 的断言随 `BROKEN` 宏翻转,
坏版本里 CHK-1 断言的是「符号翻转确实发生」并 PASS —— **那不是「检查杀死了变异」,是检查换了题目**。
r3 新增 `CHDSP_CHECK_FORCE_GOOD_ASSERT=1` 才产出真正的杀伤矩阵。

**⚠ 一处已知的小缺口(如实记,不掩饰)**:`PREREG_FP_r2_addendum.txt` / `PREREG_FP_r3_addendum.txt`
两件**文件头没有写「未过门」四个字**(只有 r1 写了)。**⛔ 我没有事后补写** —— 理由:
①补写会改变它们的 sha256,而 `results_fp_r3.txt` 的 deps 行**已记录了当时的哈希**,
那是「那次跑真的用了哪个版本」的**事实记录**,不是待更新的引用(团队纪律 D6-aj);
②预注册件的全部价值来自「它写下时就是那样」。⇒ 二者的门禁状态**继承自本文件与 r1**。

**预注册纪律**:三份 PREREG 落盘于各自跑批之前,**一字未改**;
r1/r2 的结果文件原样保留(事实记录,不是待更新的引用 —— 团队纪律 D6-aj)。
**两次证伪条件命中(CHK-11b、CHK-7 判据窗口),处置均为【改正被测物 / 另做独立观测】,
⛔ 未改任何判据、未移动任何标杆。**
