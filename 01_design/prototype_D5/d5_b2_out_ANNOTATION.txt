未经 critic 评审 —— d5-b2 判读件。⛔ 未 commit。[L2/宿主仿真]。预注册 = PREREG_d5_b2.txt

═══════════════════════════════════════════════════════════════════
§0 ⛔ 先自曝一处**我自己的构型缺陷** —— H1 的结果是【构造出来的】,不是【测出来的】
═══════════════════════════════════════════════════════════════════
```
我在 §1 写了「两场景均把两通道 RMS 对齐 ⇒ 电平判据在构造上拿不到便宜」
⇒ ⇒ 而那**同时**使 `D_lvl ≡ 0` 在两个场景里都成立 ⇒ 区间 [0,0] vs [0,0]
⇒ ⇒ ⇒ ∴ 「电平判据无分辨力」这条**不是本轮测出来的,是我构造出来的**
```
**⇒ ⭐ 而那个结论本身仍然成立 —— 但它的依据是【解析】不是【实测】**:
两场景的差别**只在通道间关系**上,而电平是**逐通道量** ⇒ 逐通道量原理上看不见通道间关系。
**⇒ ⛔ 故 H1 应记为 [L3/解析],⛔ 不得记为 [L2/实测]。⇒ 我把"预期方向举证门槛更高"那条用在了别处,却没用在这里。**

═══════════════════════════════════════════════════════════════════
§1 主结果:**便宜的判据不够,贵的够**
═══════════════════════════════════════════════════════════════════
```
量        S1(两个人)        S2(一人两麦)       可分?
D_env    [0.659, 0.716]     [0.692, 0.745]     ⛔ **重叠** ⇒ 包络互相关**不够**
D_xcorr  [0.0085, 0.0120]   [0.0551, 0.0765]   ✅ **可分**(区间相距约 **5×**)
有利侧的闸(两个方向都报):
  D_env  : S2 判同源 92%  ｜ ⛔ **S1 被误判为同源 33%**(真的两个人被合并)
  D_xcorr: S2 判同源 100% ｜ ⛔ S1 被误判为同源 **0%**
```
**⇒ 命中预注册 H2 的中间支:**能做,但要贵的** ⇒ **`D5_AUTOMIXER_r1_criteria.md §2` 的界须重估**。**

═══════════════════════════════════════════════════════════════════
§2 ⛔⛔ 而这是「算力结构性有界」的**第二个威胁**,且这一个是**实测**的
═══════════════════════════════════════════════════════════════════
```
§2 证的界:`a·N + b·N·M + c`,全部为**逐通道**与**固定比较**
⇒ 而 `D_xcorr` 是**逐通道对**的量:N 路 ⇒ **N(N−1)/2 对**(N=12 ⇒ **66 对**)
  且每对需 **lag 搜索**(FFT 级)⇒ **结构上是 O(N²·L log L)**,⛔ 不是 O(N·M) 比较
⇒ ⇒ **它与我证的那个界【不是同一个量级,也不是同一种结构】**
⚠ 而 ⛔ 我**不**在此断言"界被推翻" —— 还有两条未查的出路:
   ① 互相关**不必每帧算**(它是慢变量)⇒ 摊薄后的每帧成本未估
   ② 可能只需**相邻/同组**麦两两算,⛔ 不必全配对 ⇒ 取决于 D6 路由与物理布放
⇒ ∴ 现状 = **界的前提多了一条待查项**,⛔ 而不是"界已失效"
```

═══════════════════════════════════════════════════════════════════
§3 ⭐⭐ 而最要紧的一条:**D_env 的阴性结果可能【完全是白噪声源造成的】**
═══════════════════════════════════════════════════════════════════
```
本轮源 = **白噪声**(素材缺口仍挂 CTO)
⇒ 白噪声的**包络**本身近乎无结构 ⇒ 两条不同白噪声经混响后的包络**本来就相似**
  (实测:S1 的 D_env 高达 0.659–0.716 —— 两个**完全无关**的源,包络相关性却这么高)
⇒ ⇒ 而**语音的包络高度结构化**(音节、停顿、起止)
   · 一人两麦 ⇒ 两路包络**几乎同一条曲线** ⇒ D_env 预期**接近 1**
   · 两个人   ⇒ 两路包络**各说各的** ⇒ D_env 预期**显著低**
⇒ ⇒ ⇒ ⭐ **∴ D_env 在语音上很可能【远比在白噪声上好用】,而本轮测不到这一点**
```
**⇒ ⭐⭐ 这把「素材缺口」从一条泛泛的待办,变成了一个【具体决定上的具体阻塞】**:
> **「automixer 的串音判据能不能用便宜的包络做」——**
> **这个问题【只能用语音素材回答】,而它直接决定 §2 那个界要不要重估。**
⇒ ⛔ 而按 D6-ab:方向可疑先查不写结论 ⇒ **本轮不判"包络不够用"**,只判「**在白噪声上不够用**」。

═══════════════════════════════════════════════════════════════════
§4 限定
═══════════════════════════════════════════════════════════════════
```
n = 12 试次/场景 ⇒ **12/∞**;单一 T60=0.4;两通道(⛔ 未测 N>2 的配对爆炸)
⛔ 白噪声源 ⇒ **不得外推到语音**(而 §3 正是这条限定咬人的地方)
⛔ 本轮未取任何门限 —— H3 里那个中点仅为报比例而临时取(预注册 §4 写死)
全部 [L2/宿主仿真];H1 一条为 [L3/解析](见 §0)
