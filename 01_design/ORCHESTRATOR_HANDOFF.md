# orchestrator 交接件 —— ⭐ 而它此前**不存在**

> **门禁状态:⛔ 未过 critic。本件是流程件,不是结论件。**
> **建于 2026-08-05 晚。⚠ 而"它此前不存在"这件事本身值得记:**
> **八个角色里,每个 teammate 都有交接件,而【唯一没有的是负责路由的那个】。**
> **⇒ ∴ 今天做出的每一条裁定,此前只活在 commit message 与已发出的消息里 ——**
> **⇒ ⇒ 而那正是本项目一整天在打的形状:【一个东西存在,而没有人能找到它】。**

---

## §0 ⭐ 接手先读这三样(⛔ 顺序不能换)

```
1. 00_governance/RETRACTED_STRINGS.txt  ← ⭐ 目录在文件头。今天全部纪律在这里
                                          ⛔ 而它【不是门】:只抓已登记的串
2. 01_design/W1_HANDOFF.md §0           ← 现役实例名单(唯一权威表)+ 四条分线导航
3. 各角色自己的交接件                     ← VERIFICATION_HANDOFF / CRITIC_HANDOFF_current
                                          / CHANNEL_DSP_HANDOFF / adaptive-dsp memory
```

---

## §1 ⛔⛔ 挂在 CTO 那里的(⚠ 而它们不会自己回来)

| # | 事项 | 状态 |
|---|---|---|
| 1 | **⭐ 那个权衡:累计增益** —— 饱和轴要增益靠后 / 噪声轴要靠前,而**链序只解决一半** | 已报,待裁 |
| 2 | **⛔ 我们到底有哪块板** —— ADI SOM+载板 vs 第三方 EVB,两份证据矛盾 | ⛔ **唯一会真正卡死的一条**;在此之前所有上板动作压着 |
| 3 | PRD §一.1 ">106 dB" 的修订措辞 | 已给模板,待定 |
| 4 | 误报率验收目标 —— **这个数目前不存在** | 三个选项已给 |
| 5 | **⭐ 让已收线的 `verification` 退出以腾并发位** | ⚠ 它挡着**两条已备好的上报**(见 §2) |

## §2 ⛔ 卡在并发位(≤4)上的两条 —— **内容已备好,只差一个位置**

```
① 「旁链 FFT 实际走 float32 吗」—— 一句话确认
   ⇒ 挡着 W1-C 臂 C 的整个设计(答"否" ⇒ A3 §2 整节翻)
② 「T60 分层」—— 而内容已被 critic 收窄两次,现在的正确版本是:
   ✅ 可提:r91 直接测 m —— 三个具名种子在 T=12 全部未收敛,一个 48 s 仍不收敛
           ⇒ 那三格上每个派生量都待查
   ✅ 可提:r93 t_det —— 0.20↔0.35 两层区间不重叠 ⇒ T60 依赖被直接测到(样本 9/∞)
   ⛔ 不得提:「单调关系已建立,可预测 T60=0.8 更糟」(已撤回)
   ⭐ 而给架构侧的成本口径:0.35↔0.50 分不开的病因是【层内散布 ≥ 层间差】
      ⇒ 要分开【只能加种子】,⛔ 不是加档位、也不是换量具
```

---

## §3 ⭐ 今天做出的裁定(⛔ 只列会影响下一步的;全文见 commit)

```
N-7(累加器轴的"最坏")   ⇒ 【具名待验】,不排本轮 —— 缺一次量程扫描才能从候选变判定
m-4(critic 的解析带内列)⇒ ⛔ 不进头条表 —— 性质不同(解析 vs 实测),而限定比数跑得慢
EXP-5c 的处置             ⇒ ⛔ 若判某 Q 值不该可达,改【参数字典】,不改判据
critic 队列                ⇒ ⭐ 由 lead 砍并排序,⛔ 不给无序清单(今天为此犯过一次)
w1c-addenda 那一摞         ⇒ critic 已判"暂不需要独立门",授权仍有效
分工假设(内部 vs 关系)   ⇒ ⚠ [L3/待检验] —— 「命名归 critic」那半只被检验过一次
                             且已有一个反例(而反例的机制是【缺触发器,不是缺意愿】)
```

## §4 ⚠ lead 今天的错误模式(⭐ 接手者请直接继承这几条动作,别重新学一遍)

```
① 转述而非给路径 —— ⛔ 8 次
   ⇒ 动作:凡给背景一律附【文件路径 + 章节】,并明写"这是转述,请核后再写进你的件"
   ⚠ 而【路径不够】:第 8 次是"给了路径,而转述的是路径里没有的东西"
     ⇒ 判别法:「把这段转给第三方,他读我给的路径能读到吗?」读不到 ⇒ 它还不在盘上
② 说在做之前 —— ⛔ 4 次("已上报 / 在盘上 / 已放行"说在动作发生前)
   ⇒ 动作:只说【已经发生的】;还没发的写"还在我这里,原因是 X"
③ 派单带分量预设 —— 1 次,而它把接收方第一版拉到了那个分量上
   ⇒ ⭐ 机制(verification 给的):【锚定不制造错误,它移除了本会抓住错误的那份怀疑】
   ⇒ 动作:派"去查 X 有多严重"时,⛔ 不预告任何分量档位的后果
④ 捏造一句无证据的话 —— 1 次,已进台账并被全库反扫清掉
⑤ 单位掉了(74 是份数被写成 74%)—— 1 次
⑥ tag 不含它被命名的件 —— 1 次;而那条自检纪律【第一版也写错了】(问 diff 而非树)
   ⇒ 动作:两问 —— ①`git cat-file -e <tag>:<path>` ②与 `git hash-object <path>` 相同
```

## §5 ⭐ 而这套东西今天真正学到的一条,写在最后

```
一整天,同一个机制在【九个载体】上出现:
  入口文件 · 交接件 · 台账议程 · 角色 memory · 软处开口 · 评审锁 · 导航指针
  · verdict 的修法本身 · 一个数被报出去之后
⇒ 而每一次的解法都不是"加一条规定"—— 规定(铁律五)早就有了
⇒ ⇒ ⭐ 缺的是【让规则和动作在同一时刻出现】的那个东西

而今天落地的每一个触发器,都自带"我挡不住什么"的自述:
  常设反扫表:只抓已登记的串        · 软处表:它只是候选
  基线机制:登记为空时它是恒等式    · 记号取值域:护栏可失败已证
⇒ ⭐ 那是它们不会退化成摆设的原因 —— 一个不说自己边界的检查,迟早被当成"已经查过了"
```
