# W1-A ↔ W1-B 正式契约段交叉核(Interface contract discipline #6)

```
reviewer: critic @ claude-opus-5 / 2026-08-01
被审件 A: 01_design/submissions/W1A_v0.6_dfba3c5cbdcc.md      (sha12 dfba3c5cbdcc)
被审件 B: 01_design/submissions/W1B_v1.3_804d80e8169d.md      (sha12 804d80e8169d)
权威源  : 01_design/submissions/IF-v1.4_ee35800c0be9.md        (sha12 ee35800c0be9,冻结)
回执    : IF-v1.4-receipt_6f8fc1312d13.md + addendum1_431c2c3e1ed2.md
```

**为什么现在做**:F-16 竞态(A/B 在运行点、检测 tap、精度判据三点完全相反而双方都以为已对齐)是 lead 例行核查抓到的,不是自验抓到的。这是本项目**第一次由同一个 critic 同时持有 A、B 与合同**,故按纪律 #6 逐条对表。

**方法**:①对 `IF-v1.4` C1–C13 每一款,取两侧的落点节逐句比对语义;②对两侧共享的**参数、口径、公式、命名**做机械比对;③对纪律 #2(禁转述规范值)双向核查;④对 lead 点名的 **B-F1 盲区表述三处同步**做坐实。

**结论:两侧在 13 款中的 9 款语义同源;**发现 **4 条 MAJOR 级不一致 + 6 条 minor 级不一致**,另坐实 **1 条纪律双向失守**。**没有出现 F-16 那种"完全相反且双方都以为已对齐"的形态**——本轮所有不一致都是**单侧已修、另一侧未跟**或**双侧同时未察觉的字典分叉**,性质轻于 F-16,但 X-1 的后果(验证用例少测一整类场景)不轻。

---

# 1. C1–C13 逐款对表

| 款 | A 侧落点 | B 侧落点 | 判定 |
|---|---|---|---|
| C1 帧结构/旁链调度 | §1.3(每 2ms 槽 / 500 FFT/s / 每通道 16ms / 最低优先级可抢占、允许跳帧) | §1.2/§2.2(T_hop 全文绑定此款)、§1.2-D(跳槽三态) | **同源 ✓** |
| C2 算力包络 | §2.1 行4(按包络 20,000 cyc/槽 计入 → 13,333 cyc/帧) | §8.2.1(实报 3.5-4.1k op → 7.0-8.2k cyc,余量 2.4×) | **同源 ✓**(A 按契约上界计 = 正确姿态)。附 X-m3 |
| C3 内存包络 | §2.2(为 B 预留 ≤16 KB;AFC 总包络 ≤112 KB) | §8.2.2(字节打包档 14.06KB / **WORD32 档 20.16KB 越界**) | **一致性风险 X-2** |
| C4 旁链供给 | §1.2 四联数表 + §1.4 看门狗 | §1.2 全文引 | **同源 ✓**;附 X-m1(A 内部 1.07s vs 8 次/s 不自洽) |
| C5 PNPR 邻域 | §7 目录列("—",无关联节) | §1.2 **只引用不定义**,全文零数值复制(我 grep 核过) | **同源 ✓**(B-F2 的结构修法双向成立) |
| C6 执行器接口 | §3.1/§4 C-4/C-6 + §5 字典(3dB/50ms、≥max(0.5Hz,0.25bin)) | §4.2/§5.1(同值) | **同源 ✓**(双方均转述,见 §3) |
| C7 精度语义 | §3.1-1/§3.4/§8 V-3(p95≤BW/4=3.75Hz,需求推导) | §1.2(引 C7,CHECK B 为旁证) | **同源 ✓**;两侧都标了"CHECK B 是最友好条件、V-3 须劣化素材" |
| C8 判据语义 | §8 V-7 | §2.2(dB/s 定义)、§2.6(IPMP 非唯一主判据) | **同源 ✓** |
| C9 变更流程 | §7 取代链 | §8.3(10 条协商项 + P8) | **同源 ✓**;B 新提 P8 须 A 侧响应 |
| C10 链序约束 | §4 C-7 + 推定链位 + PRD §二 差集表(模块粒度,差集=∅) | §3.2 臂2 前提 + **§8.3-7 子模块定案(tap 在 AEC 线性滤波后 / NLP 前)** | **不一致 X-4** |
| C11 GR 遥测 | §4.1 三条覆盖路径 + ④盲区公式 + §8 V-12 | §3.2 臂3(**加 dom 谓词 + 因果时序合取门**)、§1.2-C、§5.2、§7.2-B7、**§3.2 新盲区公式** | **不一致 X-1(最重)+ X-m5** |
| C12 T_panic 二分 | §4.1 输入/输出侧二分 + §6 panic 行 | §1.2/§3.3(同口径,措辞模板一致) | **文本同源 ✓,但被 X-3 的字典默认值架空** |
| C13 反应时间同参 | §6 主带行 + 高频行 | §2.3 逐档表 + §3.6 看门狗表 | **不一致 X-2'(见 X-2 表)** |
| 链序 C-1..C-4(非合同) | §4 | §0 图 / §3.3 / §4.4 | **同源 ✓**(C-3 = 陷波器组入口,A 已采纳 B 的反推并留了"v0.1 我错了"的痕) |

---

# 2. 不一致清单(带两侧位置)

## MAJOR 级

### X-1 · B-F1 残余盲区公式:**四个位置三种状态**(lead 点名项,坐实)

| 位置 | 现文 | 状态 |
|---|---|---|
| **合同 `IF-v1.4` C11-④**(:88) | 「无轨迹历史 ∧ 遥测降级 **∨ tap < T_low_gr**」 | **旧** |
| **W1-A §4.1-④**(:186) | 同上,标注「已知残余盲区(**定稿表述**)」 | **旧** |
| **W1-B §3.2**(:439-447) | 四析取项:`tap<T_low_gr ∨ (诞生即平台 ∧ 无可继承因果锚 ∧ PHPR 否决触发) ∨ ((a′)臂间隙 ∧ GR 降级) ∨ (掩蔽下族内最大为假)` | **新(正确)** |
| **W1-B §3.5-#11 残余栏**(:511) | 「case (b) 盲区 = "无轨迹历史 ∧ 遥测降级 ∨ tap < T_low_gr"(表述按 C11-④ 定稿)」 | **旧(B 侧自身未传播)** |

**实质差别**:旧公式把"诞生即平台"的盲区**条件化在"遥测降级"上**;W1-B 用 CHECK G 机械求值证明因果时序是三臂**共用的合取门**,故该盲区**与 GR 是否降级无关** ⇒ **旧公式比真实盲区窄**。

**后果(为什么这是最紧迫的一条)**:**W1-A §8 V-12 的验收判据正照旧公式写**(:282「残余盲区(无历史∧遥测降级 ∨ tap<T_low_gr)显式 log 不许静默」)⇒ **V-12 会把"GR 正常 + 真·诞生即平台"当作应覆盖场景**,而它实际不可覆盖。两种结局都糟:要么该用例不被构造(**B-F1 的核心场景漏测**),要么被构造后判为缺陷而去"修"一个物理上不可修的东西。

**处置**:已由 W1-B 按 C9 提 **点单 P8**(addendum-1),须 lead 转架构侧出 **IF-v1.5**,**同一次动作同步四处**:C11-④ / W1-A §4.1-④ / W1-A §8 V-12 判据 / W1-B §3.5-#11。**只改任何一处都不算闭。**

### X-2 · C3 / 112KB 两级内存包络在同一个未验证平台假设下同时失效,而两侧对该假设的处理不对称

| 侧 | 事实 |
|---|---|
| **W1-B §8.2.2** | 用 `mem_sizeof.c` 编译器实算,**两列都报**:字节打包档 14.06KB(87.9%,在 C3 内)/ **WORD32 档 20.16KB 越界**;明文「WORD32 档不得删除,作为**未排除的下行场景**保留 [L4/待 SHARC 工具链实测]」;拒绝"已收口"断言 |
| **W1-A §2.2 / §3.3** | **整个内存预算建立在"16-bit 按 2B"上**(抽取环形 32KB + 看门狗环形 16KB = 包络的一半),§3.3 明文以此论证"内存减半";**无 L 标、无 V 项、无敞口声明** |

**咬合后果(两侧各自都没算的第三种行为)**:若 WORD32 档成立,①W1-B 的 C3(16KB)越界 → 须走 C9;②W1-A 的 48KB 缓冲翻倍到 96KB;③W1-A 的 **AFC 总包络 112KB**(= 93.8 + 16)变为 ≈ 141.8 + 20.5 ≈ **162KB**,**当场破**。平台 L2 有余量(1024KB),故不是可行性问题,但**两级包络会同时作废,且没有任何一份文档预告了这一点**。
**修法方向**:把"存储宽度假设"提为**一个共同的 V 项**(与 W1-C 微基准同批,装 SHARC 工具链一次解决),两侧同步挂敞口声明;C3 与 112KB 的重议若发生,应**一次性走 C9**,不要分两次。

### X-2' · C13 反应时间:两侧对同一物理量给出两个数,且 A 侧是乐观的一侧

| 路径 | W1-A §6(:237-238) | W1-B(§2.3 :280-289 / §3.6 :525-531) | 差 |
|---|---|---|---|
| 主带主路 | **242ms**,**无档位** | **242ms@W_long=8 / 274ms@W_long=10**,明文"对外引用必须带档位" | 大房间预设下 A 低报 **32ms** |
| 高频 PANIC | 典型 **0.6–1.1s**(用**扫描相位期望 0.53s**) | **≈1.24s**(用**扫描周期上界 1.07s** + 满窗 21ms + 斜坡 150ms) | **口径不同源**;A 下界比 B 低 ~2× |
| 高频 PERSIST | 最坏 1.3–2.5s(3 连拍复扫路径) | **≈3.38s**(3×扫描周期 + 窗填充 + 斜坡) | 量纲同、口径不同 |

**C13 箍② 原文要求"对外账一律取满窗上界",末行自立「任何总反应数字若未含满窗 Δfill 且未标 L1 实测,即违反本条款」。** W1-B 已按此把 v1.1 的 1.1-1.2s 改为 1.24s 并留痕"按 C13 口径属偏乐观";**W1-A 未改**。
**且二者指向同一个 CTO 议程项**:W1-A §6 高频行「残余风险交 CTO」vs W1-B §9-④「进 CTO 第三关议程第 3 项时**用 1.24s,不用 1.1s**」⇒ **CTO 会在同一议程项上收到两个数** —— 这正是 C13 存在的理由。
**修法方向**:高频行按上界重算并与 B 对齐;主带行加档位。若保留"典型"栏,须与"对外账"栏并列并注明哪一栏进 D14。

### X-3 · 参数字典(D2 单一事实源)与算法侧设计件 **六处默认值分叉**;`panic_thr` 默认 OFF 架空 C12 的输入侧担保

| 字典项 | W1-A §5(:198-222) | W1-B v1.3 | 合同 | 判定 |
|---|---|---|---|---|
| **`AFC.panic_thr`(T_panic)** | 默认 **OFF** | **−6dBFS**;§1.2/§3.3/§2.1 的输入侧条件担保全建立在其启用之上 | C12 讨论以 −6dBFS 为默认并给出提前量论证 | **分叉,后果最重** |
| `AFC.fb_thr`(T_low) | 默认 **−50dBFS** | 初值 **−45dBFS**;`T_low_gr = T_low−20dB` 的全部算例(tap −56dBFS / 9dB 余量 / 失效临界 G>59dB)按 −45 推 | C11 注记引「默认 −45dBFS [L4/W1-B 草稿]」 | **分叉,合同站 B 侧** |
| `AFC.max_depth` | 默认 **−24dB**,量程 −50~−3 | 默认 **−18dB**,量程 **−3..−40**;预设 −18/−18/−12/−12 | — | **默认 + 量程双分叉** |
| `AFC.det_hpf`(F_det_lo) | 默认 **100Hz** | 初值 **120Hz**;预设 120/120/80/80 | — | 分叉 |
| `AFC.bw_mode` | 默认 **1/10 oct** | 语音档 **1/5**、音乐档 1/10 | — | 分叉 + **A 侧内部自相矛盾**(默认预设=语音大房间) |
| `AFC.lift_after` | 默认 **600s** | 语音大房间档 **120s** | — | 同上 |
| `AFC.exh_recover` | 默认 **60s** | hold 10s → recover **+1dB/s**(≈6s) | — | 语义 + 值分叉 |

**`panic_thr` 为什么最重**:W1-A §4.1 与 C12 共同建立的结论是「PANIC 路对**输入侧(ADC 满幅)钉住构成担保**」,W1-B 全文按此收窄并写进 §9-⑬。**但字典默认 OFF ⇒ 出厂配置下该担保不存在** —— 担保的载体被默认关闭。三份文档都没把"担保成立"与"参数默认启用"连起来。§5 该行的 `[m7:默认值标定期重议]` 留痕是针对高频 F4 残余风险的,**不是针对 C12 担保链的**。
**修法方向**:字典与 B 侧初值/预设**同源**(只能有一个定义处,与 C5 权威方向反转同一通则);`panic_thr` 默认状态单独裁 —— **若维持 OFF,两份文档 + 合同中所有"输入侧担保"表述必须加"仅在 panic 启用时"**。

### X-4 · C10 差集表粒度:B 已在职权内定案 AEC 子模块 tap 位,A 侧未跟

- **W1-A §4 推定链位 + 差集表**(:177):`.7 AEC` 判为「tap 上游——非增益动态,系自适应线性滤波」,**模块粒度**,差集=∅。
- **W1-B §8.3-7**(:964):该判定**只对 AEC 的线性滤波部分成立**;工程惯例的 AEC 含**残余回声抑制(NLP,谱域)+ 舒适噪声** = 频段选择性、时间常数远小于门限的衰减型环节 = **正是 C10 泛化条款自己禁止的形态**。**本方定案(职权内)**:tap 取在 AEC 线性滤波器输出之后、NLP 之前。
- **后果**:该定案**改变了 C10 担保推论与 C12 输入侧担保链的前提**(tap 上游是否还有频段选择性衰减)。若 D3 的 AEC 定稿把 NLP 置于 tap 前,C10/C12 担保须按 C9 重开。**A 侧差集表未加"模块内子级已核"列,亦未记该约束行。**
- **方法论留痕(B 侧给出,我认为成立且应入 team_config D1)**:「**差集 = ∅ 的强度受限于差集所用的粒度**」;COMP 漏项 → 动态 PEQ 漏项 → T_low 第四漏点 → **AEC 子级**,四连同型。**逐条核清单只能防住清单内的遗漏,防不住清单粒度之下的遗漏。**

## minor 级

| # | 项 | A 侧 | B 侧 | 判定 |
|---|---|---|---|---|
| **X-m1** | 看门狗扫描周期 | §1.4:「每通道 **~1.07s**」与「**8 次/s** → 634 cyc/帧」并存(8/s ⟺ 每通道 **1.00s**) | §3.6:用 **1.07s** 算 T_react=1.24s | A **内部**不自洽 7%;1.07s 是 C4 规范值,应以其为准并把摊销改为 594 cyc/帧(或改扫描率) |
| **X-m2** | PAPR 度量约定 | §2.1 单命中强判据「PAPR 强阈值(高于主带阈值 **+X dB**)」;§5 `AFC.det.*` 占位待回填 | §1.2 已统一 **20·log10(常规 dB)**,并警示 PX4 :514 为半 dB 不可直搬;§8.3-11 已请 lead 转 | **相对偏移量 +X 同样单位敏感**;字典回填前必须挂同一约定,否则同族二义第三例在两份文档之间复活 |
| **X-m3** | W1-B 算力自报值 | §8 V-11 引「≈2.5-3k op → 21-25k cyc」 | §8.2.1 经 m-7 勘正为 **3.5-4.1k op → 30-35k cyc** | A 侧引用陈旧;量级结论不变 |
| **X-m4** | 控制面信号命名 | §4 C-4:`NHS_GAIN_OVERRIDE(PANIC\|EXHAUSTED)`;§5 `AFC.status` 又写"含 NHS_PANIC_ACTIVE" | §3.3/§4.4:`NHS_PANIC_ACTIVE` / `NHS_DUCK_ACTIVE` 两个独立旗标;§8.4-1 又提「NHS_GAIN_OVERRIDE 互斥旗标」 | **一个控制面信号三种写法**,D9 指令集定稿前必须收 |
| **X-m5** | 臂3 的判定条件 | §4.1:「持续 GR + **TRACKED 高 PAPR 峰** = pinned-howl 证据」 | §3.2:臂3 = GR 作用域式 **∧ `dom`(本通道已跟踪轨中 **PNPR** 最高)**,且整体受 `族内最大 ∧ 因果时序` 合取门约束,**仅 PERSIST 路** | A 的表述**比 B 的实际规则宽得多**(缺 dom、缺合取门、缺路径限定)。A 侧读者会高估臂3 的覆盖 |
| **X-m6** | 计数语义 | 我 grep 过 W1-A:**无按槽计数的表述** ⇒ 当前无活冲突 | §2.2 三分(存在性 / 速率 / **墙钟时长**) | ✓ 无冲突,但**第三类(墙钟时长)两侧都没写**;B 已提点单 13(旁链读音频帧计数器的权限)须 A 侧确认 |

---

# 3. 纪律 #2(禁转述规范值)双向核查 —— **双方都转述,现在一真一假**

| 侧 | 自述 | 实际 | 判定 |
|---|---|---|---|
| **W1-B v1.3** | 头部(:9-12)**带例外清单的准确自述**:逐项列出转述了 `T_low_gr = T_low−20dB`(C11)、执行器斜坡上限(C6)、看门狗扫描周期(C4)、候选峰上限(C4)、C13 示例数、检测带上限;说明为何保留、代偿是什么 | 与自述相符 | **准确 ✓**(r2 的 m-6 已闭合) |
| **W1-A v0.6** | §7(:247)「本文档**不转述规范值**」;附录(:296)「契约引用全部走版本号,**规范值零转述**(合同纪律 #2)」 | 至少 **8 处**转述:`20,000 cyc/槽`(C2,:81)、`≤16 KB`(C3,:109)、`BW/4=3.75Hz`(C7,:129)、`T_low−20dB` 与 `tap≈−56dBFS`(C11,:186)、`3dB/50ms`(C6,:198/210/216)、`≥max(0.5Hz, 0.25bin)`(C6,:215)、`64+128+50=242ms`(C13,:237) | **自述失实** |

**我逐条回合同原文核过:两侧转述的值全部正确、无漂移。** 故这是"**自述与工件不符**"而非"数据错" —— 但纪律 #2 的立法理由正是"带版本号也挡不住单文档内混入异版本数字",**自述失实会让下一轮 critic 少查一遍**(这句话是 W1-B 自己写的)。
**建议**:W1-A 按 W1-B 的样式改为带例外清单的准确自述。**并建议把"纪律 #2 在实践中不可能零转述"这一事实提给 lead** —— 与其要求一个做不到的绝对,不如把纪律改成"**转述须紧邻标注条款号 + 明列例外清单 + 冲突时合同胜**",即 W1-B 已经在做的形态。

---

# 4. 一致性良好的部分(明确点出,避免只报坏消息)

1. **C5 的权威方向反转双向成立**:合同自足化(含"每侧"),W1-B 只引用不定义 —— 我 grep 全文 `187` / `0.26` / `1/3 oct` / `1/6 oct` / `主瓣` **零命中**;W1-A 对该款标"—"(无关联节)。**B-F2 这类 2× 分叉的根因已被机制消除,不是靠自觉。**
2. **检测 tap 位置(链序 C-3)**:两侧完全一致(陷波器组入口 = AEC 之后、陷波之前),且 W1-A 留了"v0.1 我定出口 tap,系本人错误,被 W1-B 反推纠正"的痕。**F-16 三个分叉点之一已彻底收敛。**
3. **运行点(B3)**:两侧一致,且两侧都标"**草稿态裁决**(F5:支撑证据含未过门件,冻结级决策不得引用)"、都指向 V-10 ROC 定案、都声明不预判胜者。**F-16 的第二个分叉点亦收敛,且处理方式正确。**
4. **精度判据(BW/4)**:两侧一致,且都记了"由需求正向推导"而非"从 BW/10 放宽",都挂了"CHECK B 是最友好条件、V-3 须劣化素材"。**F-16 第三个分叉点收敛。**
5. **C2 包络的用法**:W1-A 按契约上界(20k)计入而非按 W1-B 当期自报值 —— 接口预算的正确姿态,使对方估算波动不动摇本表。
6. **V-12 ↔ B7 互引**:两侧都写了"轨迹历史路与 GR 路须**各自独立**命中用例"、"双 broken 必 FAIL"、"判定量取输出信号非内部旗标"。**假绿纪律在跨文档验证钩子上执行到位**(判据本身须按 X-1 更正)。

---

# 5. 须一次性走 C9 的项(建议打包成 IF-v1.5 一版)

| 优先级 | 项 | 触及 | 来源 |
|---|---|---|---|
| **1** | **C11-④ 盲区公式改四析取项**(同步 W1-A §4.1-④ + V-12 判据 + W1-B §3.5-#11) | X-1 | addendum-1 **P8** |
| **2** | **C4 写明 top-N 排序度量** | 未成轨钉住峰的获取侧覆盖 | 回执 P1(**D3/D4 派单前必须定**) |
| **3** | **C12 增条件 (iii) AFE 削波锚点** | C12 输入侧"担保"二字的条件配齐 | 回执 P3 |
| 4 | **C13 示例数标档位**(242@W_long=8 / 274@W_long=10) | X-2' | 回执 P4 |
| 5 | **C11-② 窄带门 PAPR→PNPR 裁定** | W1-B §3.3 的偏离项;**lead 已裁"v1.5 定版前 W1-B 不得 PASS"** | 回执 P6 + addendum-1 |
| 6 | **C10 差集表加"模块内子级已核"列 / 补 AEC 子级约束行** | X-4 | W1-B §8.3-7 |
| 7 | C11-② 括注改为取舍陈述;C4 `−128dBFS` 加"(峰值参考约定)";C11 增 `bus_is_local_reinforcement[8]` | 文档正确性 / 二义预防 / 消过包含 | 回执 P2/P5 |
| 8 | **新增**:旁链读音频帧计数器的权限确认;不可读则 C4 增 `slot_time_ms` 事实字段 | W1-B m-3 三分的实现前提 | W1-B §8.3-13 |
| 9 | **新增(本件提出)**:C3 与 W1-A 112KB 两级包络在 WORD32 档同时失效 ⇒ 存储宽度假设立为共同 V 项,重议一次性做 | X-2 | 本件 |
| 10 | **新增(本件提出)**:参数字典默认值的**单一定义处**裁定;`AFC.panic_thr` 出厂默认状态(产品决策) | X-3 | 本件 |
