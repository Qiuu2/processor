# critic verdict · D3/D4 **r4 稿**(`review/d3d4-r4`)—— EXP-10b 二裁 + MAJOR-3② 补做 + 轮次机制

```
⛔ 门禁状态:被审件【未过门】;本件是 critic 产出
评审人:critic-w1c-2(第 2 实例)   日期:2026-08-05
锁   :tag `review/d3d4-r4` = `0922b64`,独立 worktree(⛔ 未碰作者工作树)
范围 :`c58e48c → 0922b64`(4 提交 / 15 文件)+ lead 点名的两条判断
```

## verdict

```
PASS-with-conditions ── 0 BLOCKER / 1 MAJOR / 2 MINOR
⚠ 而我 r3 verdict 的 BLOCKER-1 / MAJOR-1 / MAJOR-2 **仍开着** —— 见 §四,
  ⛔ 那【不计入本轮判定】,理由写在那里(时间差 14 分钟,且作者已如实登记为未做)。
```

---

# 一、⭐ lead 点名要我判的:EXP-10b 那个加强 —— **成立,而且比你转述的紧一格**

## 1.1 我按原文独立核了(`PREREG_D34_r9_addendum.txt`,tag 内)

```
:36-38  被测量分【三个评价频带】报: (a) 20 Hz–20 kHz /(b) 100 Hz–8 kHz /**(c) 200 Hz–8 kHz**
:43-45  「EXP-10b **若 (b)/(c) 通过**,重做拒绝率代理:**在 (b) 口径下**从 REF 还能加几段。
          预期 ≥4 段。证伪条件 <2 段 ⇒ **即便换频带口径**,日常调音仍会频繁撞拒绝。」
实测(results_d34_r16.txt:310-313):100→0 段 · 125→3 · 150→8 · **200→8**
```
**⇒ ✅ 加强成立:(c) = 200 Hz–8 kHz 确实是预注册自己枚举的候选,而 (c) 那一档 = 8 段。**

## 1.2 ⇒ 而我复核时发现它比作者写的还紧一格:**反例不在同一页,在同一句的前件里**

```
EXP-10b 那三行同时含【三个不同的作用域】:
  前件(触发条件)  「若 **(b)/(c)** 通过」        ⇒ 作用域 = 两个频带
  被测量(实际测的)「在 **(b)** 口径下」          ⇒ 作用域 = 一个频带
  后件(证伪结论)  「**即便换频带口径**」        ⇒ 作用域 = 任何频带
⇒ ⇒ ∴ ⛔ 不必说"反例在同一页纸上" —— **(c) 就写在这条断言自己的前件里**,
   与后件相隔【两行】。⇒ 它在写下那一刻就已经知道存在第二个频带,而只测了第一个。
```

## 1.3 ⚠ 而我要把这个加强【拆成两个时刻】—— 合在一起会说过头

```
时刻甲(预注册落盘时):(c) 已被列为候选,但 (c) 上的 EXP-10b 值【尚未测】
   ⇒ 此时的缺陷 = **过度泛化**(在一元素样本上做全集断言),⛔ 不是"无视反例"
时刻乙(设计件引用时):四档表已在盘(100/125/150/200),它引了 100 那一行,没读另外三行
   ⇒ 此时的缺陷才是【数据在手边而没读完】
⇒ ⛔ 两者严重度不同,而合起来说会读成"当时手里就有推翻它的数据" —— 甲不是那样。
⇒ ⭐ 作者写的「反例当时就在同一条检查的输出里」对**乙**成立,对**甲**不成立。建议按两条记。
```

## 1.4 ✅ 而它那条「⛔ 也不执行该分支」我判**对**,理由要写明

```
证伪条件(0 段 < 2)确实被触发 ⇒ 按预注册,应当执行后件。
⛔ 而后件本身已被同一条检查的更宽数据证伪 ⇒ 执行它会落盘一个假结论。
⇒ ∴ 正确处置 = **记录"分支被触发 + 后件已被证伪 ⇒ 预注册缺陷 = 过度泛化"**,⛔ 不执行。
⭐ 这是我见过的对"预注册与事实冲突"最干净的一种处理:
   ⛔ 既不假装分支没触发(那是洗),⛔ 也不照执行(那是把预注册当圣经)。
```

## 1.5 ⇒ 而那条合并内核与判别法,我判**成立且比两条单独的强**

```
它给的内核:【一个在集合上量化的断言,只在集合的一个元素上被检验】
⇒ r11 那次:理由的范围 = 三条检查,只在两条上成立 ⇒ 被用在全部三条
⇒ 这一次  :结论的范围 = 任何频带,只在一个频带上验过 ⇒ 被宣布成立
⇒ ✅ 方向相反(个别对→全体对 / 个别坏→全体坏)不影响它是同一个错 —— 这一点它说对了
✅ 判别法(「看到量化词,当场问:这个范围里我实际检验过几个元素」)我判**可执行且可当场做**
⚠ 而我补一句它没写的:**量化词也可能藏在【前件】里**(本例 (b)/(c) 就在前件)
  ⇒ ∴ 问的时候要问【整条断言的三个作用域】,⛔ 不只问结论句里的那个量化词。
```

---

# 二、⛔ MAJOR-1 · 反扫「最坏合法配置」时撞到**第二个指称** —— 而它是同一个病

**⇒ 这条是 BLOCKER-1 的反扫**顺出来的**,而反扫本身还没做 —— 我只是提前扫了一遍。**

## 2.1 事实:同一个工件,三处措辞,只有 §0 那处是错的

| 层 | 位置 | 原文 | 判 |
|---|---|---|---|
| 脚本 | `results_r16_batchreason.txt:64` | 「**存在** ±15 dB 量程内的**临界配置**…(**最早出现在** ④)」 | ✅ **对**(存在性 + 最早) |
| §7 表 | `D3D4:224-232` | ④ +30.00 / **⑤ 3 段 ⇒ +45.00**;正文写「**临界配置**确实存在(④)」 | ✅ **对** |
| **§0 承重表** | **`D3D4:70`** | 「**最坏合法配置**(满刻度 + **2 段** +15 dB 同频 @40 Hz…)下 +30.00 dBFS」 | ⛔ **错** |

```
⇒ §0 把一个【存在性 / 阈值】断言升格成了【极值】断言。
⇒ 而推翻它的数据在【同一份文件的 §7】:⑤ 3 段 = +45.00 > +30.00,相隔 154 行。
⇒ ⇒ ⭐ 与 BLOCKER-1 **同一个词、同一个病、第二个指称**,且这次连脚本自己都用对了词。
```

## 2.2 ⇒ 而它有一个我算不出但必须问的后果:**推荐的那个顺序在真最坏点上可能也越界**

```
§7 表自己的线性(它自己的行给的,⛔ 不是我编的):
  PEQ 在前:③ +15.00 → ④ +30.00 → ⑤ +45.00      ⇒ +15.00 dB/段
  分频在前:③ −19.14 → ④ −8.28  → ⑤ +6.72       ⇒ ④→⑤ 也是 +15.00 dB/段
⇒ 而参数字典允许 **8 段**。按它自己这条线性外推到 8 段同频:
     分频在前 ≈ +6.72 + 5×15 = **+51.7 dBFS**  ⇒ ⛔ **越 Q4.27 余量 24.08 dB**
⇒ ⇒ 若成立,则 §0 那一行的**结论**(分频前置即可)在真最坏点上**不足以解决问题**。
```
```
⛔⛔ 而我把它写成【问题】不是【判定】,理由要写清:
   我**没有实算** —— 我没有它那套最大节点电平的实现,上面是**用它自己表里的两个差值外推**。
   ⇒ [L3/由其自表外推,未实算] ⇒ ⛔ 不得当结论引用。
   ⇒ 而它是可以在几分钟内实算的:`check_r16.py` 把段数从 3 扫到 8 即可。
   ⇒ ∴ 我的判定只有一条:**§0 那个词错**;而 ⑥⑦⑧ 段的数【必须补出来才能知道结论稳不稳】。
```

## 2.3 修法(与 BLOCKER-1 修法② 是同一个动作,⛔ 别排两轮)

```
① `D3D4:70` 的「最坏合法配置」⇒ 改「**临界配置(最早越界者)**」—— 用脚本自己已经写对的那个词
② `check_r16.py` 的配置表从 3 段扫到 **8 段**(字典上限),把 ⑥⑦⑧ 补进 §7 表
   ⇒ 若分频在前也越界 ⇒ §0 该行的结论句须改写(⛔ 不是改数)
⭐ ⇒ 而这与 BLOCKER-1 修法②(EXP-5c 在字典范围上取 max)是**同一条**:
   【凡说"最坏",就在参数字典的范围上取 max;⛔ 不钉一个点然后叫它最坏】
```

---

# 三、MINOR

```
m-1 ⚠ 轮次自动导出(我 02impl-r11 MAJOR-1 的修法)——✅ 机制对,而留了一个**更窄的洞**
    `run_all.sh:16-24` 由 `max(review/02impl-rN) + 1` 导出 ⇒ 现取到 r12 ✅;
    取不到 tag 退回 `rx` 并写明"⛔ 不假装知道轮次" ✅;`tee "$OUT"` ⇒ **截断写**。
    ⇒ ⛔ 而若两个工作轮之间 lead 没打 tag,第二轮会**静默覆盖** `results_impl_r12.txt`。
    ⇒ 旧洞是"永远 r1",新洞是"两轮同名" —— **窄了很多,但同一个后果**(结果件名与轮次不符)。
    修法(一行):`$OUT` 已存在 ⇒ 拒绝覆盖并报错(或加 `-2` 后缀),⛔ 让碰撞出声而不是消音。
    ✅ 而同一文件里 `tee` 退出码那个坑(:119-133,用 PIPESTATUS 取回)已修对 —— 那正是我今天栽过的。

m-2 ⚠ 反扫表新条目写「反扫口径:**命中约 20 处**」。我实扫(⛔ 不加任何 --include,
    排除 .git/knowledge_base)= **59 处 / 38 个文件**。
    ⛔ 而我**不判它数错** —— 差异几乎全部来自【口径】:「最坏合法」这个串至少有**两个指称**
    (噪声底那条 = 被撤的;D4 分频那条 = MAJOR-1 说的另一个错用),
    还有 `01_design/fixedpoint/` 一整族无关上下文。
    ⇒ ∴ 问题不是数字,是**登记条目没写清楚扫的是哪一个指称**。
    修法:命中数须与【被撤的那个指称】绑定,例如「噪声底语境 N 处 / 其余 M 处为他义,逐条归因」。
    ⇒ 与本表自己 §「机械信号负责检出,打开来看负责归因」同一条,只是这次漏在了**计数**上。
```

---

# 四、⚠ 我 r3 verdict 三条的状态 —— **仍开着,而这不计入本轮判定**

```
时间线:我 r3 verdict 落盘 **04:31**;`607bf29`(收 BLOCKER-1)**04:35**;r4 tag **04:45**
⇒ 14 分钟。⛔ 我不会把"没来得及做"记成缺陷。
```
| 我的 finding | r4 状态 | 判 |
|---|---|---|
| **BLOCKER-1** 「最坏合法配置」不成立 | ✅ **已登进 `RETRACTED_STRINGS.txt`**,且分得极准(撤的是**词**不是**数**、承重轴是 Q、29↔52 指向同一决定);✅ lead 已当面更正 CTO。⛔ 而 8 处实例**未标注**,作者自己写明「归因逐条做,归作者」 | ✅ **登记正确,反扫未做且已声明未做** ⇒ 合法中间态 |
| **MAJOR-1** `:79/:338` 「0.02–50 是安全的」缺轴限定 | ⛔ 两行**一字未动** | 开着 |
| **MAJOR-2** BASELINE 住在带轮次号的文件里 | ⛔ `run_killmatrix_r16.py:26` 原样;⛔ 无「EXP-5c 必须存在」元检查 | 开着 |

**⇒ ⭐ 而 BLOCKER-1 的登记方式我要单独表扬一句,因为它做对了一件容易做错的事**:
它没有借"critic 说数错了"把 `−76.97` 一起撤掉。撤回条目第一行就是
「⛔ **撤回的是这个【词】,⛔ 不是那个【数】**」—— **撤回的作用域被写进了撤回本身。**

---

# 五、✅ 我核过并判为**闭合**的

```
MAJOR-3 修法② ✅ 闭,且我**手算复核了那两个数**(⛔ 不需要跑脚本):
   makeup +20 dB 在 ③HPF 之后 / ⑥PEQ 之前(k=1):上游 1 个量化器 ×100,其余 8 个 ×1
     ⇒ −173.35 + 10log₁₀(100 + 8) = **−153.02** ✓ 逐位相符
   落在 PEQ 之后(k=9):9 个全 ×100 ⇒ −173.35 + 10log₁₀(900) = **−143.81** ✓ 逐位相符
   两者之差 10log₁₀(900/108) = **9.21 dB** ✓
   ⇒ ⭐ 而它顺带**第二次独立确认了我的 erratum1**:增益越靠链尾,被它放大的上游量化器越多
     ⇒ 尾部增益在噪声轴上更差。⇒ 这次是用一个**新的、与噪声底无关的**配置得到的同一结论。
MAJOR-4 修法逐条打勾表 ✅ —— 四条各自落点具名,④ 写明"两侧都有"并列出 C 侧三个 CHK 编号
EXP-9c 由乙类改归甲类 ✅ 我核了 `PREREG_D34_r9_addendum [F-3]` 确有该前置分支
   ⇒ ⭐ 而它更正我的方式是对的:**指出我把出处记成了 r8,而正确的 r9 反而让退役更有据**
   ⇒ 这是今天第三次作者用盘面证据修正我(前两次 B-3 计数 / B-4 归族),三次我都接受
「我错在哪」原文保留 ✅ 甲类件撤在原处、原文不删,执行正确
```

---

# 六、⛔ 本轮未审

```
⛔ `02_impl/results_impl_r12.txt`(443 行新增)—— 未逐项核,只验了轮次名与机制
⛔ `RETIREMENT_LOG.txt` +178 行 —— 未开
⛔ `d34_bessel_delay/` 的 r14 改动 —— 未开
⛔ MAJOR-2 那张最大节点电平表的逐格独立重算(r3 起就登记未做,本轮仍未做)
   ⚠ 而 §二 2.2 那条外推恰恰需要它 ⇒ 登记为【我未做而它承重】
```

# 七、放行条件

```
1. MAJOR-1 ①(§0:70 换成脚本自己已写对的「临界配置(最早越界者)」)—— 必做
2. MAJOR-1 ②(段数扫到 8)—— 与 r3 BLOCKER-1 修法② 合并一次做
3. m-2(反扫登记写明扫的是哪个指称)—— 必做,否则那条登记会自带一个假的"已扫过"
⛔ m-1 不构成放行阻碍
⚠ r3 的 BLOCKER-1 反扫 / MAJOR-1 / MAJOR-2 仍是 r3 verdict 的放行条件,本件不重复列
```
