# critic verdict · 02_impl r8(r1/r2/r4 六条的收口)

```
⛔ 门禁状态:本件是评审产出。⛔ 未改任何文件,未 commit。
reviewer : critic @ claude-opus-5[1m] / 2026-08-05
版本锁   : **`review/02impl-r8` = `5bfc72b`**(annotated tag `ce4260d656281bd2`)
           独立 worktree,⛔ 未碰作者工作树
前件     : r1(`8402198`)· r2(`d8fced2`)· r4(`322ef2f`)三份 verdict
```

# 判定:**我在 r1/r2/r4 开的 1 BLOCKER + 5 MAJOR —— 六条【全部关闭】,逐条实证**

> **⇒ 而本件的重点不在那六条,在 §三:`channel-dsp-2` 提的那句话打到我,而【它是对的一半,
> 而错的那一半也在我身上】。**

## 六条逐条(全部我亲自核过,不采信声明)

| # | finding | 关闭点 | 我的实证 |
|---|---|---|---|
| **BLOCKER-1** `expect_red` 分不清"响了"与"跑不起来" | r4 | D6-d:拿走被测脚本 ⇒ G10/G11 **FAIL 且理由正确**,exit 1 ✅ |
| **MAJOR-1** `ref_modules.py` 同式转写却称"第二轨" | **r8** | docstring 已改「**实现一致性核** + **铁律七的独立解析轨** —— ⛔ 两件事,分开报」,并写明「那个定级是错的」 ✅ |
| **MAJOR-2** docstring 承诺三项只实现两项;T2 无阳性对照 | **r8** | 现有 `T1/T1+/**T2+**/T3/T3a/T3b/T4/T4a/T4+` ⇒ **补了缺失的限幅器轨,补了 T2 的阳性对照** ✅ |
| **MAJOR-3** 自证覆盖面与杀伤矩阵无对表 | r4 | 删一条登记 ⇒ 真实 exit 1、「杀伤率作废」✅ |
| **MAJOR-4** 六条零分辨力旧检查未标注 | **r8** | **逐字照我的修法做的**(见下)✅ |
| **MAJOR-5** 无杀手 14 条 | **r8** | **真实欠账 14 → 0** ✅(见下) |

### MAJOR-4:它把"无分辨力"这个事实写进了**检查自己的名字与打印串**
```c
OKC("CHK-B4(斜坡机制部分已被 CHK-B4b 取代·保留为回归项)", …
    "⛔ 但其中「无输出跳变」一半对 BQ_NORAMP 变异【零分辨力】:DF1 状态连续本来就不跳")
OKC("CHK-D1(方向性部分已被 CHK-D1b 取代·保留为回归项)", … "⛔ 但它对 DET_ONEDIR 变异【零分辨力】")
OKC("CHK-Y1b(已被 CHK-Y1c 取代·保留为回归项)",        … "⛔ 本条对 GATE_NEGATIVE 变异【零分辨力】")
```
**⇒ 保留而非删除(E-2 ✓),而"它守不住什么"跟着它自己走 ⇒ 读者摘走任何一行都带着限定。**

### MAJOR-5:我重算了「无杀手集合」,**14 → 4,而剩下的 4 条正是我自己划的 (a) 档**
```
r1: 29 条检查 / 15 有杀手 / **14 无杀手**
r2: 40 条      / 19       / **21 无杀手**(新增检查先于新增变异,比例变差)
r8: 40 条      / 36       / **4 无杀手** = CHK-C3a · CHK-N5a · CHK-Y1c0 · CHK-Y2b0
⇒ 而这 4 条全部是【前提自检】—— 正是我 r1 verdict 里划的 (a) 档「本来就不该被杀」
⇒ ∴ 真实欠账 = 0
```
**⇒ 变异 16 → 35,自证 16 → 68,而元检查仍 21/21、总闸门 PASS。**

---

# 二、我欠的那次复现:**m-1 的成因归属,我撤回**

r4 verdict 我开过一条 m-1,说 `check_gates_fire.sh` 用固定 `/tmp/gates_*` 路径、并发会互相破坏,
**并把 G10 那两行 `sed`/`grep` "没有那个文件" 噪声归因于它**。

## ⇒ 处置:**撤回该成因归属**,理由不依赖复现结果

```
我 r4 verdict 里自己就写了:「⚠ 一处我没做到的:我【没能复现出】那两行噪声」
⇒ 也就是说,我当时是【在没有隔离成因的情况下】给出了一个成因
⇒ ⇒ 而 channel-dsp-2 拒绝把它宣称为成因,理由是证据不足 —— **它是对的,证据确实在我这边而我没有**
```
**⇒ ∴ 我撤回的不是"并发会破坏"这个观察**(固定 `/tmp` 路径是代码事实,两实例并发确实会互踩,
而我确实在自己身上触发过一次);**撤回的是【把 G10 那两行噪声归给它】那一步 —— 那一步我从未验过。**

## r8 上的复现:**跑到了,复现不出来**

```
无并发、全新拷贝、单独跑 check_gates_fire.sh(r8 版)
G1 … G8 全部 [PASS]
[PASS] G10  …⇒ 闸门确实变红(命中 /\[FAIL\]|杀伤率作废/)      ← r4 时它是 FAIL(DISQ 误判)
[PASS] G11  …⇒ 闸门确实变红(命中 /未声明的连带变化|\[FAIL\]/)
⇒ 全程「没有那个文件 / No such file」命中 = **0**
```
**⇒ ∴ 三条结论**:
1. **我 r4 报的那两行噪声,在 r8 上复现不出来** ⇒ `channel-dsp-2` 说"两种跑法都 exit 0"**得到我方独立佐证**;
2. **我 r4 的 MAJOR-1(DISQ 先于 want 判、扫整段 ⇒ 把真响了的闸门误判)已闭** ——
   G10 在 r4 是 FAIL、在 r8 是 PASS 且**命中它自己的失败特征**,说明判定顺序的修法生效;
3. **而 m-1 的成因归属仍然撤回** —— 理由不变:**我当时就没有隔离过成因**,
   ⛔ 复现不出来只是让这条撤回更明确,不是撤回的理由。

**⚠ 而我要说清一处我做不到的**:复现不出 ≠ 证明当时不存在。
r4 那次是在**当时那棵树**上观察到的,而 r8 已改动多处。
**⇒ 所以正确的记法是「未能复现,成因未知,归属已撤回」,⛔ 不是「当时那两行是我看错了」。**

## ⚠ 而我那行 `want` 校验,`channel-dsp-2` 判它有洞 —— **它对**

我给的是 `printf '' | grep -qE "$want"`,理由是"拒绝匹配空串的 want"。
**⇒ 而它指出:该式抓得住 `""` 与 `".*"`,抓不住 `"."` —— 而 `"."` 正是我自己举的那个例子。**
**⇒ ⇒ 我的修法拦不住我自己举的反例。** 它改用语义判据(失败特征串不得出现在通过那一跑的输出里),
**并且一上线就抓出它自己 4 条弱 want** —— 与它 r4 那次"第一次跑就抓住作者自己"同型,可信度同理。

---

# 三、⭐ 答 lead:`channel-dsp-2` 那句话打到我,而**它对的那一半和错的那一半都在我身上**

它的话:
> 「CHK-D3 **根本没调被测物** —— 用 `exp()/ldexp()` 在测试里把公式重算一遍,与 `chdsp_*` 一个函数都不沾。
> 而它与 critic MAJOR-4 点名的 CHK-C1 形状完全相同,却没被那一轮抓到。
> ⇒ **逐条人工审查会漏,而『每条都必须有杀手』这个机械要求不会漏。**」

## ⇒ ⛔ 先纠一处事实:**机械要求没有漏。CHK-D3 当时就在我的名单里。**

`critic_02impl_r1_verdict_20260804.md`:
```
:253  无杀手集合 …  CHK-D3  CHK-Y1a  CHK-Y1b …          ← 它在里面
:262  (c) 真的没有变异 | CHK-B1 CHK-B1b CHK-B3 CHK-C4 **CHK-D3** CHK-Y1a
:271  补变异优先级:CHK-B1b > CHK-B3 > CHK-C4 > **CHK-D3(dB 换算精度退化)**   ← 我把它排在最后
```

## ⇒ ✅ 而它对的那一半:**漏的是【归因】,而那一步是我做的,我做错了**

```
「无杀手」这个信号有【两种】成因,而它们产生【同一个信号】:
   ① 检查是对的,只是还没人给它写变异        ⇒ 修法:写个变异
   ② 检查根本不碰被测物,所以【永远】杀不死   ⇒ 修法:重写检查
⇒ 我把 CHK-D3 归进 ①,还给它写了个描述「dB 换算精度退化」——
   ⇒ **那个描述本身就暴露了我的错**:我以为它在测 dB 换算,而它在测试里自己算公式
⇒ ⇒ 而归错档 ⇒ 排到最后一位 ⇒ 于是它一直没被动
```
**⇒ ∴ 正确的表述不是「人工审查会漏所以要机械要求」,而是:**
> **「每条都必须有杀手」负责【检出】,它没漏;
> 「打开每一条看它到底调了什么」负责【归因】,这一步只能人做 —— 而我没做,我按名字和断言分的档。**

## ⇒ 而 lead 问「这对 critic 这个角色的定位有什么话要说」—— 我的答:

```
① 我的价值不在【产生那个机械信号】—— 脚本比我做得好,而那个要求本来就是我提的
② 在【归因】那一步。而这一次我在那一步错了
③ ⭐ 而我错的方式,正是我今天打了一整天的那个形状:**我信了标签,没有打开东西。**
   CHK-D3 的名字(「功率底 vs release」)与断言看起来都合理 ⇒ 我假定它的 body 与之相符
   ⇒ 这是我自己蒸馏件里的【机制 A(名单型)】施加在我身上:
     清单上有这一项、我逐项处理了、而我没有核它的【内容】
④ ⇒ ∴ 我给自己加一条,并建议它对任何做分档的人成立:
   **凡对机械信号做【分档】,判为"低优先级"的那一档必须【全部开箱】** ——
   因为分档的作用就是决定谁不被看,而那正是错误的藏身处。
   ⇒ 抽验高优先级那档是浪费(它们本来就会被看);要抽的恰恰是被判为不重要的那一档。
```
**⇒ ⚠ 而我不接受"所以人工审查可以省掉"这个推论**:
本轮 r8 里另外两条「检查比它声称的弱」(`:1017` 的重复元素配对缺陷)**没有一条是靠"无杀手"信号发现的** ——
它们是**写变异的时候看代码看出来的**。**⇒ 机械信号看不见"检查比它声称的弱但仍能被杀死"这一类。**
**⇒ ∴ 两者不是替代关系:机械要求负责【没有杀手的】,人工负责【有杀手但杀的不是那件事的】。**

---

# 四、本轮未做

```
⛔ r8 的 40 条检查逐条通读(我核的是六条 finding 的关闭点 + 无杀手集合重算)
⛔ 35 个变异的逐个"只注入声称缺陷"复核(r4 已验断言② 机制成立,本轮未逐个复跑)
⛔ 另外三个冻结点:review/w1c-reaudit-r2 · review/w1c-addendum-a1 · review/w1p-closeout-r5
⇒ 其中 **w1c-addendum-a1(T3 是浮点内核,verification 自曝承重墙塌一半)** 我建议排最前
  —— 它与 DEC-0023「没有任何定点锚点」是同一条线,而那条线现在有一张已发出的工单
```

---

# 五、给 `channel-dsp-2` 的一句话

> **你那句话我判【对的一半在你,错的一半在我】。**
> 机械要求没有漏 —— CHK-D3 当时就在我的无杀手名单里,还带着我给它写的描述。
> **⇒ 而那个描述(「dB 换算精度退化」)本身就是证据:我以为它在测 dB 换算,
> 而它在测试里自己算公式。⇒ 我按【名字和断言】分的档,没打开 body。**
>
> **⇒ 所以你真正证明的不是"人工会漏",是【机械信号必须逐条开箱,尤其是被判为低优先级的那一档】。**
> 而那正好是我该做而没做的那一步。
>
> **⇒ 另:你指出我那行 `want` 校验抓不住 `"."` —— 那是我自己举的例子,而我的修法拦不住它。你对。**
