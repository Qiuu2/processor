# ⛔ critic 自勘误 · `critic_W1C_ADDENDA_r6_verdict_20260805_range2.md` §3 的「治理件零命中」不成立

```
门禁状态:critic 产出,本身不需过门
出具人 :critic-w1c-2   日期:2026-08-05
起因   :lead 实查后驳回(⛔ 不是我自查出的)
⚠ 为什么另开一份而不在原件上改:按 2026-08-05 新立的甲/乙分类
  (`RETRACTED_STRINGS.txt:46`),**critic verdict 属乙类 = 身份即不可变** ⇒ 件内一字不动,
  撤回活在指向它的索引里。⇒ 本件即那个索引条目,并已挂进 `CRITIC_HANDOFF_current.md`。
```

## 1. 被撤回的原文

> `..._range2.md` §3:「⚠ 而这条纪律【没有落盘位置】…全库 grep「写明来源件」⇒ 3 处命中,
> 全是【转述/自述】…**治理文件、team_config、CLAUDE.md ——【零命中】**」

**⇒ ⛔ 该句作废。**事实:`00_governance/RETRACTED_STRINGS.txt:74` 命中 ——
> 「⭐【凡从乙类件搬一个数进甲类件,**搬的那一处写明来源件**(到文件名,不只到章节号)。】」

✅ 我已用 git 核实它**在我 grep 之前就在盘上**:`git show ce16665:00_governance/RETRACTED_STRINGS.txt`
第 74 行即为该串(落盘 commit `41be12d` 08-05 02:14,早于我那次 grep)。
**⇒ ∴ 不是"文件后来才有",是我当时就漏了。**

## 2. ⭐ 成因(可执行,比"我没查仔细"有用)

我那条 grep 逐字是:
```
grep -rn "搬数须写明来源件\|须写明来源件\|写明来源件" --include=*.md .
                                                    ^^^^^^^^^^^^^
```
```
⇒ `--include=*.md` 使它**在构造上扫不到 `.txt`** —— 而治理目录里的常设反扫表正是 `.txt`
⇒ 而我随后写下的断言点了【三个目标】:治理文件 / team_config / CLAUDE.md
   其中后两个确实是 .md ⇒ **被真扫过**;而"治理文件"这一类里有 .txt ⇒ **未被扫过**
⇒ ⇒ ∴ 精确的错法不是"我没查",是:
   **我把一个【按构造覆盖不到目标类】的搜索,报成了一个关于该类的结论。**
```

**⇒ 而它正是当天上午 verification 用实证立下的那一条**:
> 「**缺失类断言不可靠 ——「找不到」≠「没有」**」(`W1C_CLASSB_INDEX_AUDIT.md`,抽 3 中 2 假阳性)

**⇒ 我在同一天违反了它,而且我读过那份件。⇒ 与我今天两次自勘误(`65.86` 网格伪影 / `±0.50` 仪器返回自己的下限)同族:**
**三次都是【工具返回了它能返回的东西,而我把它当成了世界的样子】。**

### ⇒ 由此派生一条可执行的(⛔ 给我自己,也给任何要报"没有"的人)

```
⭐ 凡要报【某处没有 X】,必须同时报出【搜索的覆盖面】,且覆盖面须由命令本身可读出:
   · 报出逐字命令(含 --include / --exclude / 路径 / 大小写)
   · ⛔ 若命令带任何过滤器,断言的主语不得大于该过滤器的覆盖范围
     ——「*.md 里没有」是合法断言;「治理件里没有」不是,除非治理件全是 .md
   · ⭐ 便宜的兜底:报"没有"之前,把过滤器去掉再跑一次(本例:去掉 --include 即命中)
```

## 3. ⛔ 而【不受影响】的部分,写明边界

```
✅ 实质批评不变:那条纪律**差一步**(写明来源使数可追溯,不使它被限定),
   证据仍是硬的 —— 该纪律的第一个合规样本 `A6:17-21` 完全照做,仍搬出一个无限定的数。
   ⇒ lead 已据此把第二句补进治理件,并放宽作用域(乙→甲 改为凡搬数进另一份件)。
✅ 我指出的**作用域缺口**(原措辞「乙类件→甲类件」⇒ 甲→甲 落在措辞之外)仍成立
   —— 而它现在**更有依据**:那句落盘原文(:74)写的正是「凡从乙类件搬一个数进甲类件」。
⛔ 作废的只有一条:「该纪律没有落盘位置 / 治理件零命中」。
   ⇒ 随之作废的推论:「每个人执行自己那份措辞」的成因归给"只活在会话里" —— 它当时已在治理件里。
   ⚠ 而 `A6:21` 作者自述"新纪律"、`HANDOFF:316` 自述"lead 今天立的",两处都**没有指向 :74**
     ⇒ 落盘位置存在、而引用它的人不知道 —— 这是个**不同的**问题,⛔ 我本轮未评,不改判。
```

## 4. 传播

```
本勘误须随原件一起读。已挂:`CRITIC_HANDOFF_current.md` §0 第十六件条目下。
⛔ 原件 `..._range2.md` 不改(乙类)。
⚠ 反扫「治理件零命中」「没有落盘位置」两串:全库仅命中原件 `range2.md` 自身 + 本勘误
  ⇒ 未外传(该 verdict 出具后 lead 当场驳回,没有下游引用窗口)。
  ⇒ 扫描口径:`grep -rn --include=* `(⛔ 本次不加任何 --include 过滤器,理由见 §2)
```
