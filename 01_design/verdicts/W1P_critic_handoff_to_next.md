# W1-P 独立 critic 交接件 —— 给下一任实例

> **写件人**:`critic-w1p`(claude-opus-5),2026-08-03。W1-P 首审执笔,verdict = **FAIL**。
> **verdict 正本**:`01_design/verdicts/W1P_critic_verdict_2026-08-03.md`。**本件不复述 findings** —— 本件写的是**你该怎么用它**。
> **被审对象**:`01_design/prototype_W1P/` 的 W1-P 闭环台架线(r51–r60)+ `W1_HANDOFF.md §7` + 其下游引用。

---

## 0. ⛔ 先读这条:**你不能是我,而且原因不是"我参与过判定"**

`.claude/team_config.md` 的 Critic instance rotation **允许**同一实例做"初审 → 修正稿复审"。**本轮不适用**,理由是 rotation 条款④(**自审禁令**):

> 我在复核中自己跑了三组测量(`critic_check1–4.py`、`critic_mutants/`),其中 **NHS 自选 ΔMSG = 1.62 dB @12s / 3.51 dB @30s** 已按 lead 令**并入 `W1_HANDOFF.md:358` 与 `:426`**。
> **⇒ 复审那份文件 = 复审我自己产出的数。**

**⇒ 你若按"评审链可复用"直接接手,是流程偏差。这一条比"我有既往判断要维护"硬得多,请先确认自己是全新实例。**

---

## 1. 我核实过什么 —— 可直接继承,但请抽验

**继承方式建议**:不要全部重跑(那要小时级)。**每类抽 1 条重跑**,对上了就整类继承;对不上就整类作废并告诉 lead。

| # | 结论 | 复核命令 / 方法 | 抽验成本 |
|---|---|---|---|
| 1 | 送审 14 件 sha256[0:12] 与 §7.5 清单相符 | `sha256sum <file> \| cut -c1-12` | 秒 |
| 2 | `r53_msgmeter.py` **清单未给 sha**,实为 `03ef4cbdda9f`(评审起点值) | 同上 | 秒 |
| 3 | **B-1 构造链**:`r57_bandlimit.py:51-76` picks 来自 `clrig.critical_points()`;`a.P.T_low = 999.`;`nhs.py:402` 门恒不可过;`nhs.py:94 lift_after_s=60 > T_OBS=6` | `grep -n "T_low\|pick_excl\|critical_points" r5{1,3,7,9}*.py` | 分钟 |
| 4 | `T_low_gr` **= −65.0 不是 979**(`Params.__init__` 构造时定死,后改 `T_low` 不重算)⇒ 已覆盖 bin 的**维持路径仍通** | `python3 -c "...NHS(); a.P.T_low=999.; print(a.P.T_low_gr)"` | 秒 |
| 5 | `cosource_check.py:60` **只检 `r45_flat.py` / `r38_paired.py`**,承重件一件未覆盖 | 直接读该行 + `python3 cosource_check.py` | 秒 |
| 6 | **H7 从未实现**:`PREREG_r52.txt:35-38` 预注册,`r52_closedloop.py:3` 声称已落盘,全库 `k=1` 臂 **0 命中**,输出无该段也无"未做"声明 | `grep -rn "k=1\|pick_excl(he, *1)" *.py` | 秒 |
| 7 | `r53` D 组 5 条变异**全部作用于 `MSGMeter` 的输入对象**,杀死判据 `abs(Δ) > 1e-6`;两条"KILLED"的 Δ 是 **+0.012 / −0.040 dB** | 读 `r53_msgmeter.py:154-191` | 分钟 |
| 8 | `__pycache__` **无陈旧字节码**(三件 pyc 均新于各自源码 mtime)⇒ 本项目的 pyc 前科**这次不成立** | `ls -la __pycache__/` 对比源码 mtime | 秒 |
| 9 | `f_cut=24k` 的**带内**神谕六条均值 = **5.548**,与 `f_cut=8k` 闭环实测均值 **5.667** 只差 **0.12 dB** | `r57_bandlimit_out.txt` §1 的 24000 行 `ΔMSG带内` 列手加 | 分钟 |
| 10 | **V0 逐位复现 `r57_bandlimit_out.txt §2`**(三条种子 m0/mk/Δ/\|差\| 全对)⇒ 台架可复现,我的变异台架忠实 | `critic_mutants/broken_out.txt` 对 `r57_bandlimit_out.txt §2` | 分钟 |
| 11 | 变异结果:**V1**(陷波算了不施加)**3/3 杀** ｜ **V2**(系数按 44100 设计)**2/3** ｜ **V3**(`is_howling` 上门 6→20 dB)**0/3** | `critic_mutants/run_broken.py`(拷贝目录,原件未动) | 20 分钟 |
| 12 | **m0 ≡ 栅格锚点**:`anchor = r0['in']['msg_db']`,f_cut=8k 时 `MSG_带内 ≡ MSG_全带` ⇒ m0 在 **6/6** 上精确等于 anchor,且在 V3 下**一格未动** | `critic_check4.py` | 分钟 |
| 13 | **P.341 §5.1.3.2 原文** = "the minimum loss **from the interface input (receiving) to the interface output (sending)**";§1 Scope 载明其适用设备音频带宽 "**approximately 100 Hz to 8000 Hz**…G.722" | `pdftotext -layout research/sources/standards/ITU-T_P.341_2011_*.pdf` | 分钟 |
| 14 | **P.341 事后合理化**:`PREREG_r57_r58.txt`(08-02 20:00)/ 数据(20:22)/ `W1_HANDOFF §7` 首落盘(21:11)**P.341 命中皆 0**;论证 mtime **08-03 11:22**;`f_cut=8000` 真源 = 旁链 Nyquist(`nhs.py:33-38` FS_SC=16000/DEC=3) | `grep -rn "P\.341" 01_design/prototype_W1P/`(应为 0)+ `ls --time-style` | 分钟 |
| 15 | 整改期五件变更 = **纯 docstring**:去 docstring 后 **AST 5/5 相同**,且基线 commit 内哈希与我评审起点锁的 **5/5 相符** | `scratchpad/ast_eq.py`(方法见 verdict 补件 3) | 分钟 |

**我的复核脚本(只读被审件,零改动)**:`/tmp/claude-1000/-home-it1234-processor/530be877-5ec0-4df7-ae7b-ed9cade0a0b7/scratchpad/` 下 `critic_check1.py`(等代价对照)/ `critic_check2.py`、`critic_check3.py`(NHS 自选)/ `critic_check4.py`(栅格量化)/ `critic_mutants/run_broken.py` + `broken_out.txt` / `ast_eq.py` / `p341.txt`。
> ⚠ **scratchpad 是会话级的,你大概率拿不到。** 上表每一行都写了**不依赖它**的复核方法,请走那条。

---

## 2. ⛔ 我**没能判**的 —— 不得继承我的沉默

**这一节是本件最重要的部分。** 我在 verdict 里对每条都标了"无法判定",但**沉默会在传递中被读成"没问题"**。下面写死**为什么判不了 + 需要什么才能判**。

### 2.1 B-1 修完后 NHS 的真实收益
- **判不了的原因**:我只做了**单一工作点**(`G = MSG_全带 + 3 dB`,T_OBS 12s/30s),**没有做起振扫描**。而被审方的协议是每个 G 起一个全新 NHS 实例、只给 6 s(`r57_bandlimit.py:85`)。两者不可直接比较。
- **需要什么**:自由槽位(`T_low` 默认 −45、槽全空)的**起振扫描**,六条种子,`f_cut=8k`,`T_OBS` 按 F36 随 `f_trig` 放大。
- **⚠ 我最担心的一条**:我那两个数(**1.62 / 3.51**)**已被并进 `W1_HANDOFF.md:358/:426`**。它们在文档里看起来像"已测"。**它们不是。** 凡见到这两个数,**必须同段出现"单工作点 / 未做起振扫描 / [L2/critic 复核跑]"**,否则判违规 —— 这是我给自己的数立的限定,请你替我执行。

### 2.2 陷波相对平坦衰减的**听感代价**
- **判不了的原因**:我算的是**带内功率均值**(平坦谱假设),那不是音质代价。SD 轴未仪表化(被审方 §7.2-4 自认)。
- **需要什么**:SD 式(111)仪表化(`w_ERB` 缺 ANSI S3.5 表,须显式记为偏离),或任何一条**成对**的音质度量。
- **在此之前**:5.67 dB **没有配对代价,不构成"净收益"**,而唯一能算的那条口径下它 6/6 输给平坦衰减器。**"输"这个结论我也没打透**——见 §4.2。

### 2.3 `f_cut=8k` 在真机是否成立
- **判不了的原因**:需要真实电声链高频滚降曲线,我们没有 [L1/L2] 数据。这不是我的能力边界,是**全项目的数据边界**。
- **需要什么**:V-34(真实房间 RIR 实测),或架构侧提的更便宜的路 —— **扬声器/麦克风厂家频响曲线 + 指向性图** `[L2/厂家规格]`。
- **⚠ 别把"未知"读成"居中"**:`f_cut=24k ⇒ 0.00–1.50` 与 `8k ⇒ 3.50–7.50`,**区间宽 6 dB > 整个交付指标**。架构侧那句"不是数字有点不确定,是数字还没有确定"是对的,请原样保留。

### 2.4 `§7.2` 第 5、6 条(4σ 过度分散真因 / `T` 敏感性第 6 条)
- **判不了的原因**:继承自前轮,**本轮无新证据,我未复核**。
- **需要什么**:若它们不影响任何结论(前轮 `LESSONS_W1P` B-1 的判据),**登记不追**即可;若下一轮要引用依赖它们的结论,则必须先关掉。

### 2.5 M-8:6.0 dB 降级的消费处是否还活着
- **判不了的原因**:`W1A:53`(v0.17)裁"C-8f/C-8g 对 AEC 整条不再适用",所以 `W1A:796`(「⇒ C-8f 线闭合,μ 可取 0.40,CTO 不必拍任何取舍」)**可能早已被作废**。**我没有把这条链追到底,不下断言。**
- **需要什么**:逐行判 `W1A:785 / :796 / :804` 是否在 v0.17 的作废范围内。**两种结果都要动作**:已作废 ⇒ 那三处必须加作废标记(现在没有);仍现行 ⇒ `μ=0.40` 与"CTO 不必拍取舍"站在一个 `[L4/我定的数]` 上,须重新论证或退回 CTO。

### 2.6 V2 的那 1/3 漏检为什么漏
- **判不了的原因**:`B3`(系数按 44100 设计,中心频偏 ~8.8%)在 `0.2/seed0` 上给出与未破坏**完全相同**的读数(|差| 0.03),我没有查清机制。
- **需要什么**:看那条种子挂陷**后**的主导临界点是否远离所有陷波裙边。**这决定 Hi 判据对系数级误差的真实灵敏度**,而 B-2 的降级正是建立在"V1 能杀"上。

---

## 3. ⚠ 我自己撤回过两条 —— 连教训一起继承

### 3.1 「这套实验**结构上测不到**实现损失」—— 过强,已撤回
变异 V1 **3/3 全杀**证明 Hi 能抓"陷波算了但没进环路"这一类。**⇒ B-2 由 BLOCKER 降 MAJOR。**
**教训**:我在拿到变异数据**之前**就写了"结构上测不到"。**那是一句可以先验证再写的话,我写反了顺序。** 凡"这个测试测不到 X"的断言,先做一次能杀的变异再说 —— **和我要求别人的假绿纪律是同一条**。

### 3.2 「verdict 前 commit ×6 = 违反纪律」—— **整条撤回**
`decisions_log.md` **DEC-0013 ①(2026-08-01,CTO 拍板)** 已把该条款正式改为:**允许归档性 commit**(条件:①文件头写明门禁状态 ②`[未过门]` 前缀)。六次 commit 全部带前缀 ⇒ **合规**。

> **⛔ 我错在:引了 `CLAUDE.md §1-1` 的旧文,没查台账。而这正是我给别人记的 C7 型错误(撤回/变更未传播时,读旧文的人会误判)。这次我踩了。**
>
> **⇒ 给你的硬规矩:读到任何"某某违反了纪律 X"的 finding(包括我写的),先 `grep -n "X" decisions_log.md` 确认 X 是否已被修订。台账优先于入口文件。**

**撤回后剩下的两条仍成立**(踩的是 DEC-0013 明文"强度一字未减"的那半条):**P-2** DEC-0013 自己的有效性判据(「不再出现文档声称状态 ≠ 仓库实际状态」)**已被触发且是同型第三次**;**P-3** `5.67` 在 verdict 前已进下游件与对 CTO 呈报句 = 禁止的"下游引用/作为其他工件依据"。**P-2 该不该回退是 CTO 的门,我没裁也不该裁 —— 我只裁"该事实必须原样上 CTO,不得以已修文案结案"。**

---

## 4. 我建议你优先打的方向

### 4.1 ⭐ **第一件事:打护栏本身,不是打结论**

本轮整改立了一批新机制:**护栏 A(工作点向量完整性)/ 护栏 B(动作计数 = 0 ⇒ 该数不是该模块的性能)/ 第四维(事前事后)/ 第五维(须指预注册行号 + 语义类别一致)/ D6-t(限定进量的名字)/ D6-s(评审期冻结)/ D6-w(写入后重读)**。

> **我验证的只是"这些文字写进了文件"。我没有验证过其中任何一条【跑过并且能对 broken 输入 FAIL】。**
> **⇒ 按本项目自己的假绿纪律:一条没对 broken 输入失败过的护栏,不算护栏,它只是一段注释。**

**判据(每条都要)**:造一个该护栏**应该**拦住的构造,喂给它,它必须报警。特别是:
- **护栏 B** —— 我评价它是本轮最好的一条修法(运行时不变量,不需要有人事先想到"神谕选点"这个词)。**正因为我给了高评价,请你比对别的更严地打它。** 关键问:它取的计数是**状态机转移**还是**轮询/帧数**?(架构侧自己注明取前者 —— 请核实实现真的如此。)取错就恒不为 0,变成恒绿。
- **D6-t(改名)** —— 它是**唯一能穿透"文档→消息→CTO"那一跳的机制**(见 §5)。但它只在**限定写进【产生处】的名字**时才有效;若只在**呈报处**改名,那一跳早就掉了。**请核实改名发生在哪一层。**

### 4.2 结论稳固、**不必重打**的(抽验即可,见 §1)

B-1 的**构造事实**(代码事实,除非代码改)｜V0 可复现｜AST 等价与 sha 基线｜**P.341 = 错的环路**(一手原文 + 项目自己 08-01 的撤回,已论证三次,不必第四次)｜H7 缺失｜`cosource_check` 覆盖范围｜`T_low_gr = −65`｜pyc 无陈旧。

### 4.3 ⛔ 我**打过但没打透**的 —— 最容易被你当成已闭

| 项 | 我做到哪 | 为什么容易误判为已闭 |
|---|---|---|
| **NHS 自选收益 1.62 / 3.51** | 单工作点,非起振扫描 | **已进 handoff 正文**,读起来像已测 —— 见 §2.1 |
| **M-1 等代价对照(6/6 输)** | 只有**功率均值**口径,平坦谱假设 | 结论方向很强,容易被当成"陷波不如平坦衰减"的定论。**它只说明"目前没有任何指标显示陷波更优",不说明"陷波确实更差"** |
| **B-2 降级后的边界** | V2 只 2/3,漏检机制未查 | 降级动作看起来是"结案" —— 实为**边界未定** |
| **M-8 消费处** | 未追到底 | 见 §2.5,**两种结果都要动作** |
| **B-4 判为实质闭合** | 残留一处:`agents/system-architect/memory.md` 顶部**缺全局撤回指针**(`:121`/`:148` 行内无标记,而 W1A 已有该指针) | "闭合"二字最容易被读成"全清" |

### 4.4 我建议的打击顺序
1. **护栏可失败性**(§4.1)—— 因为其余一切都要靠它们守;
2. **r61 / bw_oct / 等代价对照的结果**(我没看过,见 §6);
3. **B-1 的补测是否真做了**(自由槽位起振扫描),这是唯一能解开承重句的动作;
4. **M-8 消费处**;
5. 其余按 verdict 的放行条件表。

---

## 5. ⭐ 「这个数从产生到进 CTO 耳朵,经过几跳?每一跳靠什么载体存活?」—— 整改后的当前答案

```
跳 1  代码(产生处)  →  载体 = 量的名字        机制 = D6-t(不得有裸 ΔMSG,四成分)   ⚠ 已立法,落地层级未核
跳 2  代码 → _out.txt →  载体 = deps 行 + 表头   机制 = 既有 deps 纪律              ✓ 在跑
跳 3  _out → 结论件    →  载体 = 限定横幅         机制 = 护栏 A/B                    ⚠ 已立法,未对 broken 输入跑过
跳 4  结论件 → 合同/架构件 → 载体 = 段落 lint     机制 = 三选三 + 第五维(指行号)     ⚠ 已立法,未验证被执行过
跳 5  文档 → lead 的消息 → CTO → 载体 = ???       机制 = 【无】                       ⛔
跳 6  本会话 → 下一实例 → 载体 = memory / handoff  机制 = 本文件 + 花名册纪律          △ 一次性,不是常设
```

**⇒ 仍然没有机制的是跳 5,而它是唯一有对外后果的一跳。**
本轮的实证:`5.67` 就是**通过一条消息**到达 CTO 的,派单原话是「陷波(NHS)在其作用域内的**实测**收益」—— **在从文档到消息的那一跳上,限定掉了两个**(神谕选点、L 级)。跳 1–4 的 lint 一个字都管不到那条路径。

**⇒ 唯一可能扛住跳 5 的是 D6-t(名字)** —— 因为**段落会被摘要掉、标题会过时(本轮实证:`W1A:2840` 标题声明的正是正文撤回的)、lint 管不到消息流,而没有人会在转述时改掉一个量的名字。**

**⇒ 我建议你把这条链本身当作一个被检查项**,而不是把它当成结论:
- **每一跳的机制必须能【单独失败】**(§4.1);
- **跳 6 尤其脆**:本文件是一次性的,不是常设机制。**若你发现自己在重新发现我已经写在这里的东西,那说明跳 6 的机制不够,请把这件事本身报给 lead。**
- **不要再往下列第七跳/第七维** —— 手工枚举维度正是这套护栏反复失效的形态本身。该问的是结构问题,不是补清单。

---

## 6. ⛔ 我**没有**做的事(你会拿到这些数,它们不在我的覆盖范围内)

1. **`r61`(`bw_oct` 1/5→1/8 + 等代价平坦衰减 baseline)** —— `PREREG_r61.txt` 08-03 11:40 落盘、`r61_run.log` 08-03 12:20,**在我评审期间才开跑,我一个数都没看过**。它直接对应我的 **M-1 与弱点 3**,所以**很可能被当作"critic 的 M-1 已闭"** —— **不是,我没审过它。**
2. **第四臂 `N-ablate`** —— 我没看过。
3. **`dmsg_two_arm.py`** —— 08-03 11:46 新建,我没看过。
4. **`bw_oct` 扫描的任何结果** —— 同上。
5. **W1-A / W1-B 本体、IF-v1.8 合同全文** —— 我只审了它们**引用 W1-P 结论的那几处**(P-3)与 **P.341 相关的那一类**(B-4/P-5)。**这两份件的其余部分我没有评审,不得视为已过门。**
6. **`W1A` 版本记录 v0.41–v0.49 缺失**(索引层静默写失败,D6-w)—— 我核实过**它不影响我的任何一条 finding**(我没有一条建立在"W1A 里没有 X"这种缺席证据上),但**我没有评估它对别人结论的影响**。
7. **`nhs.py` 本体的算法正确性** —— 完全没审。我审的是"那个 dB 数说的是不是它被说成的东西"。`nhs.py` 的门禁状态见 `decisions_log.md:334`(**未过任何门**)。

---

## 7. 一句话交接

> **W1-P 这一轮的整改,全部发生在【依据层】—— 而且做得很好:P.341 撤回按正确特征串重扫、自补第 9 处、把"两条门值我一个都没造"整条反转成"两条我都造了"(主动放弃自己最有力的答辩)。**
> **但承重那个数本身 —— 3.50–7.50 dB —— 一个字都还没动。**
> **⇒ 你的第一个问题不应该是"他们改好了吗",而是"那个数现在测的,是不是它被说成的东西"。**
> **⇒ B-1 与 B-3 只能靠【补测】或【改名 + 撤销达标对表】修好,没有第三条路。整改稿若只动了措辞,是未修。**
