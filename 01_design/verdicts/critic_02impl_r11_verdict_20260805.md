# critic verdict · 02_impl r11(Bessel 群延迟 / 定点 τ / 轮次沿革)

```
⛔ 门禁状态:本件是评审产出。⛔ 未改任何文件,未 commit。
reviewer : critic @ claude-opus-5[1m] / 2026-08-05
版本锁   : **`review/02impl-r11` = `628efdd`**(annotated tag `6158312c0461bde2`)
```

# 判定:**PASS-with-conditions**(0 BLOCKER / 1 MAJOR / 2 MINOR)

> **⇒ 承重的那张表我【逐格独立复算,六格全中】。⇒ 而 MAJOR 是我进场前就预告要查的那一条。**

---

# 一、⭐ 我独立复算了那张已进 CTO 汇报的表 —— **六格全中**

我用**自己的**群延迟实现(解析导数,不调 `scipy.signal.group_delay`)重算 20 Hz–20 kHz 内的 max τ:

| fc | 型 | 文档 | **我复算** | |
|---|---|---|---|---|
| 80 Hz | Bessel8 | 6.326 ms | **6.326** | ✅ |
| 80 Hz | LR8 | 15.555 ms | **15.555** | ✅ |
| 80 Hz | BW8 | 18.508 ms | **18.508** | ✅ |
| 120 Hz | Bessel8 | 4.217 ms | **4.217** | ✅ |
| 120 Hz | LR8 | 10.370 ms | **10.370** | ✅ |
| 120 Hz | BW8 | 12.338 ms | **12.339** | ✅(舍入) |

**⇒ 而 `LR8@80 = 15.555` 与我在 D3/D4 verdict 里独立算的 `15.5553` 也相符** ——
**⇒ ∴ 这条测量链现在有【三方】独立结果一致:它的 r14、它引的任务二归档件、以及我两次不同场合的复算。**

## ⚠ 而我要如实记两次**我自己的**在途错误(都在开单前自己抓住)

```
① 我先按【高通支】复算 ⇒ Bessel8 三种归一化都对不上(14.295 / 31.820 / 27.615 vs 文档 6.326)
   ⇒ 我一度准备开「Bessel 数复现不出」
   ⇒ 而真相:那张表是【低通支】,Bessel 用 −3 dB@fc 归一化 ⇒ 换过来六格全中
   ⇒ ⛔ 我错在【从自己上一件工作的语境里带了一个假设进来】(D3/D4 我算的是高通支)
② 我用 grep 找各 checkpoint 的总闸门,命中了一堆「总闸门: FAIL」
   ⇒ 我一度准备开「多个已提交检查点是红的」
   ⇒ 而那些命中来自 **G9 自己的 want 正则描述串**(`命中 /总闸门: FAIL/`),不是真实状态
   ⇒ ⛔ 判别法:**当一个工具会打印它自己的判据时,对判据字符串做 grep 必然自命中**
```
**⇒ 两次都没进 verdict,但我记下来,因为第 ① 条是【我今天第四次"信了自己的上下文"】。**

---

# 二、⛔ MAJOR-1 · **没有轮次沿革,而结果件叫 `results_impl_r1.txt` —— 它现在是第 11 轮**

我进场前向 lead 预告过要当场查这一条(条件:r11 须自带【r9/r10 的 FAIL 去向 + 引入后又被移除的东西】)。

## 盘面事实
```
02_impl/ 工作树里与轮次有关的文件 = **只有一个** results_impl_r1.txt(38 KB)
`run_all.sh` 的 OUT 写死为该文件名 ⇒ **每一轮覆盖上一轮**
CHANNEL_DSP_HANDOFF.md 全文 grep `r9` / `r10` / `r11` ⇒ **零命中**
⇒ ∴ 从交付树上【看不到】r9/r10 存在过,更看不到它们是绿是红
```

## ⚠ 而与 D3/D4 BLOCKER-1 的差别,我要说清(所以判 MAJOR 不判 BLOCKER)
```
D3/D4:各轮结果【都在盘上】(results_d34_r2…r13),而清单指错了轮   ⇒ 数找不到 ⇒ BLOCKER
02_impl:各轮结果【不在盘上】,只在 git 里(该文件 34 次提交)      ⇒ 数可恢复 ⇒ MAJOR
```
**⇒ 我核过 git 确实保住了历史,并逐版查了总闸门与 FAIL 行:**
```
34 个版本;其中 `843f87d` 是【真红的】(总闸门 FAIL,2 条 [FAIL]),`3fb257d` 有 1 条 [FAIL]
⇒ 而按本项目 commit 纪律,带 [未过门] 前缀提交红的跑批**是允许的** ⇒ ⛔ 这不是违规
⇒ 问题不在"提交了红的",在【从交付树上看不出曾经红过】
```

## ⇒ 而最尖的一点是**命名**,不是缺失
```
文件名  results_impl_r1.txt        ← 它是第 11 轮
⇒ 一个按名字取用的人,会以为自己拿到的是 r1
⇒ ⇒ 这与 D3/D4 那条(清单写"r7 为现行"而现行是 r12)是同一类,只是这次错在【文件名】上
⇒ 而 r11 的内容里【也没有任何一行写着"本文件是第几轮"】
```

**修法(三条,前两条便宜)**
- ① 结果件抬头加一行「**本轮 = rN,跑批时间 …**」(⇒ 一行,立刻消除命名误导);
- ② `HANDOFF` 加一节【轮次沿革】:各轮的总闸门状态 + 出现过的 FAIL 及其去向 +
  **引入后又被移除的东西**(⛔ 后者是 git diff 看不出意图的那一类);
- ③ 或改为每轮独立留档(`results_impl_rN.txt`),与 D3/D4 侧一致 —— **⚠ 而这条是【建议】,我没核成本**。

---

# 三、MINOR

| # | 位置 | 问题 |
|---|---|---|
| **m-1** | ㉓ 那张表 + `results_bessel_delay_r14.txt` | **归一化口径未在表旁声明,而它承重**。我实测同一 Bessel8@80 在三种归一化下是 **6.326 / 1.990 / 12.220 ms —— 跨度 6×**。⇒ 表里三型并列比较,而只有从源码 `bessel_poles_norm()` 才能看出用的是 −3 dB@fc。⚠ **另**:LR 天生 **−6 dB@fc**、BW/Bessel 是 **−3 dB@fc** ⇒ 三者在 fc 处的衰减本就不同,**同一 fc 下比 τ 是行业惯例,但该注明** |
| **m-2** | ㉓ 表下那句 | 「(平坦度与绝对延迟在本例几乎相等 —— **低通支** τ 单调,最小值趋 0)」——⇒ **这是全文唯一透露该表是低通支的地方,而它在括号里**。⚠ 我就是因此先按高通支算了一遍(见 §一)。⇒ 建议把「低通支」提进**表头** |

---

# 四、INFO(我核过、值得记的三条)

**I-1 · ⭐ `results_bessel_delay_r14.txt` 抬头那条重生成留痕,是本轮最好的一处纪律执行**
> 「⚠ 重生成留痕:r15 自查发现本脚本 `bilinear_sections` 漏了【每节增益归一化】。
> ⇒ **τ 与常数增益无关 ⇒ 本件全部 τ 数值逐位不变**(已回归核对);
> ⇒ **但被测版本变了,deps 必须跟着变** ⇒ 重跑重存。」

**⇒ 它发现了一个【不改变任何数字】的缺陷,仍然重跑重存 —— 理由是"被测版本变了"。**
**⇒ 这是 D6-aj 在一个零影响改动上的自愿执行。⇒ 大多数人会说"数没变就不用重跑"。**

**I-2 · ⭐ ㉔ 它把 lead 的转述【核了并且顶了回去】**
> lead 转述「CTO 已批准 V-9」。按「转述须核」核了:`W1A_v0.6 §3.2` 是**申请**;
> `decisions_log:76` 仍列在「待 CTO」⇒ **盘上没有"已批准"的痕迹**
> ⇒ ⛔ 在 DEC 落盘前我不写"已批准"进任何件。

**⇒ 这正是今天团队反复立的那条规矩,被执行在【转述者是 lead】的方向上。**
**⇒ 而它同时说明了为什么它不阻塞:「无论批不批,申请书本身就写着音频通路仍全定点」。**

**I-3 · ㉓ 的两面同出,定性正确**
```
正面:Bessel8@80 = 6.326 ms vs LR8 15.555 ⇒ 把那一档从"单模块就超掉 12 ms 预算"变成可能
⛔ 反面:r8 已测 Bessel 的 LP+HP 求和不平坦(4 阶最好 1.686 dB,**8 阶最差 25.4 dB**)
⇒ ∴ 正确的问题是「这一支要不要被求和还原」,而不是「要不要 Bessel」
⇒ 它明写「由产品 / architect 定,本件只把两面摆齐」
```
**⇒ 这个收口是对的**:它没有把一个卖点数推成一个建议,而是把决策条件交出去。
**⚠ 而我未复算 25.4 dB 那个数**(它来自 r8,不在本轮版本锁内)⇒ 见 §五。

**I-4 · ⑭ 它修掉了我 r4 的 m-1**(元检查 `/tmp` 路径写死 ⇒ 递归/并发互踩)。
⚠ 而我当时**撤回了成因归属、保留了观察** —— 它修的正是那个观察。⇒ 处置一致。

---

# 五、本轮未审(⛔ 不得读作已审)

```
⛔ r8 的 Bessel 求和平坦度(1.686 / 25.4 dB)—— 不在本轮版本锁内,我未复算
⛔ ㉕ 定点 τ 的 48 个工作点(我只读了结论 max|Δτ| = 0.00064 ms,未重跑)
⛔ ⑯–㉒ 各条(本轮只查 ㉓㉔㉕ + 轮次沿革)
⛔ 37 个变异 / 71 条自证的逐条复核(总数已看,逐条未看)
```

---

# 六、放行条件

```
必闭:MAJOR-1 ①(结果件抬头写明轮次)+ ②(HANDOFF 加轮次沿革节)
同轮:m-1 归一化口径进表;m-2「低通支」提进表头
建议(⚠ 我未核成本,按【建议】读):MAJOR-1 ③ 每轮独立留档
```

---

# 七、给作者的一句话

> **那张已经进 CTO 汇报的表,我用自己的实现逐格重算,六格全中** ——
> 而 `LR8@80 = 15.555` 与我在 D3/D4 时算的 `15.5553` 也对得上,
> **⇒ 现在它有三方独立结果一致。**
>
> **⇒ 而我要告诉你一件对你有利的事:我在复算时先按【高通支】算,三种归一化全对不上,
> 一度准备开"复现不出"。⇒ 错的是我 —— 我把上一件工作的语境带了进来。**
> **⇒ 而我之所以没开错单,只因为我多试了一种读法。⇒ 所以 m-2 不是措辞问题:
> 「低通支」现在藏在一个括号里,而它是那张表能不能被复现的关键。**
>
> **⇒ 至于 `results_impl_r1.txt` ——它是第 11 轮。⇒ 而 git 里那 34 个版本救得回历史,
> 救不回一个按文件名取用的人。**
