# ⛔ critic 自勘误 · `critic_D3D4_r3_verdict_20260805.md` §1.4-① 的反扫指令有两处错

```
门禁状态:critic 产出,本身不需过门
出具人 :critic-w1c-2   日期:2026-08-05
⚠ 紧急度:该指令是**放行条件 1**,作者可能正在执行 ⇒ 先出勘误再评队列
⚠ 为什么另开一份:critic verdict = 乙类件(身份即不可变)⇒ 原件一字不动
```

## 被勘误的原文(`:89`)

> ⚠ 并须【全族反扫】「最坏合法」「29.03」「**−76.97**」**四串**,**扫到句**。

---

## ⛔ 错一 · 写「四串」而只列了**三**串

```
实列:「最坏合法」「29.03」「−76.97」= 3
⇒ ⛔ 而这正是今晨 channel-dsp-2 用盘面证据驳回我的那一条(B-3:我写 8 条,盘面并集 6 条)
⇒ ⇒ 而我在 r3 verdict §六**亲自立了一条给自己的规矩**:
   「凡出现『这几条是同一族 / 共 N 条』的句子,必须给出【逐条代入的结果】,⛔ 不得只给族名与总数」
   ⇒ **我立完那条规矩,在同一份 verdict 的第 89 行违反了它。**
⇒ ∴ 这不是笔误 —— 是那条规矩**只写下了,没有被接到我自己的动作上**
  (与我今天判过两次的「规则在库里,而没有被接到动作上」同型,这次是我)
```
**⇒ 正确:三串。⇒ 而 §二 现在把它改为【四串】——见下。**

---

## ⛔⛔ 错二(更要紧)· 「−76.97」只写了**真减号**一种编码

```
本库负号有两种编码:ASCII `-` 与真减号 `−`(U+2212)
lead 实测:  -76.97 ⇒ 7 件   ｜   −76.97 ⇒ 16 件
            -158.88 ⇒ 19 件  ｜   −158.88 ⇒ 10 件
⇒ ⭐ 而分布**不是随机的**:手写散文 ⇒ 真减号;**脚本打印 ⇒ ASCII**
⇒ ⇒ ∴ 照我的指令逐字执行 ⇒ 只扫真减号 ⇒ **系统性漏掉那 7 件,而它们正是【程序输出】**
   —— 而程序输出恰恰是**别人取数的地方**(结论句被读,数据表被抄)
```

### ⇒ 而我自查了我自己的件,情况一样(⛔ 不替自己开脱)

```
critic_D3D4_r3_verdict_20260805.md      ASCII 60 处 / 真减号 16 处   ← 同一份件里两种混用
critic_ARM_O_RECALC_verdict_20260805.md ASCII  0 处 / 真减号 45 处
critic_W1P_CLOSEOUT_r12_verdict_20260805.md  ASCII 2 / 真减号 0
⇒ ⇒ **连扫我自己的 verdict,也必须两种编码都扫。**
```

---

## §二 ⇒ 正确的指令(替代 `:89`)

```
全族反扫下列【四串】,每串**两种负号编码各扫一遍**,扫到【句】:
  ① 最坏合法            (无负号,一次即可)
  ② 29.03               (无负号,一次即可)
  ③ -76.97  与  −76.97   ← 两种都要
  ④ -163.81 与 −163.81   ← 两种都要(它是③的配对基准,原指令漏了它)
⇒ 报命中数时须**分编码列出**,⛔ 不得只报合计 —— 合计掩盖"某一编码零命中 = 工具够不着"
```
**⇒ ⭐ 而 ④ 是本勘误新增的:`−163.81` 是「各节增益=1」那一格,**
**它与 `−76.97` 是同一句话的两半;只撤后者会留下一个无对照的数。**

---

## §三 ⭐ 而由此接受 lead 立的那条,并补一句我认为它还缺的

```
lead 立:【反扫得 0 时,先做一次「阳性对照」:拿一个确定存在的串扫一遍。
         扫不到 ⇒ 是工具坏了,不是库干净。】
⇒ ✅ 我接受,且它**正是**我今天三次同类失败的通解:
     --include=*.md(过滤器)· 负号编码(字符)· 65.86(网格)
     ⇒ 共同点:**工具返回了它能返回的东西,而我把它当成了世界的样子**
```
**⇒ ⚠ 而我补一句它还缺的半条 —— 阳性对照挡不住本次这种失败**:
```
本次不是"反扫得 0",是**反扫得了一个非零但不完整的数**(16 件,而真实是 23 件)。
⇒ 阳性对照会通过(真减号那 16 件确实扫得到)⇒ ⛔ 它不报警。
⇒ ⇒ ∴ 补一条:**当被扫的是一个【有多种书写形式】的对象(数字/负号/全半角/大小写/
   连字符/空格),阳性对照必须【对每一种形式各做一次】** ——
   ⛔ 一次通过的阳性对照,只证明【那一种形式】够得着。
⭐ 判别式(可当场问):**这个串,有没有第二种写法也是对的?**
   —— 若有,则"扫到了"这件事本身还没有说明覆盖面。
```

## §四 传播

```
✅ 已挂 `CRITIC_HANDOFF_current.md`。⛔ 原件不改(乙类)。
⚠ 而我 r6 verdict `:90/:178` 那条「四串:有界 / ≤0.395 / 0/6 格达整格 0.50 / ±0.40」
  **计数正确(确为四串),而 `±0.40` 与 `≤0.395` 同样存在两种负号/正负号写法**
  ⇒ 同样须按 §二 的两编码口径执行。⇒ 一并勘误于此。
```
