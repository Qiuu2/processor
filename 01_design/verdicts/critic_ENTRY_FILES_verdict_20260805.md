# critic verdict · 入口文件与交接件(**已离开团队的那些数**)

```
⛔ 门禁状态:本件是评审产出。⛔ 我未改被审的两个文件(lead 保留改入口文件的动作)。⛔ 未 commit。
reviewer : critic @ claude-opus-5[1m] / 2026-08-05
被审对象 : CLAUDE.md §0「当前状态」(第 6–19 行)
           01_design/W1_HANDOFF.md §0 + 其后各断点节
版本锁   : CLAUDE.md sha16 = 见 §六;W1_HANDOFF.md sha16 = 见 §六
选题依据 : lead 引我 W1A verdict MAJOR-2 的原话「入口文件是最强形式的下游引用」
           ⇒ 该理由适用于该文件里的【每一条】,不只我当时打的那一条
```

# 判定:**FAIL**(0 BLOCKER / 3 MAJOR / 1 MINOR)

---

# ⛔⛔ 一、先答那件在跑的:**你派错了。而错的那句就在 CLAUDE.md 里,和纠正它的那句相隔一行**

## MAJOR-1 · `CLAUDE.md:9` 与 `:10–17` **直接矛盾**,而 `:9` 没有任何撤回标记

**`:9`(阶段那一条,末段)**:
> **T1 已获厂家锚点:ADI EE-408 载明 SHARC+ 核心执行 IIR = 2.5 cyc/biquad = 0.5 cyc/MAC [L2/厂家],
> **风险方向较原判反转**(DEC-0014)……计入值 T1=2.0 不动。

**`:16`(紧接着的下一条,DEC-0023 块)**:
> **⇒ ⇒ W1-C 不再是"验证厂家锚点在我方代码形态下是否成立",而是【取得第一个定点锚点】。**

**⇒ 而你派给 verification 的单子写的是**:「已有厂家锚点 ⇒ 降为验证它在我方代码形态下是否成立」
**⇒ ⇒ 那是 `:16` 明文说【不再是】的那一句,逐字。**

### ⇒ ∴ **是,你派错了。而这不是你的疏忽 —— 是文件让你读错的。**

```
纠正是以【新增一条】的方式加上去的(:10–17),而【被纠正的那句】(:9)一个字没动
⇒ 自上而下读,先撞见 :9
⇒ :9 说得像既成事实(「已获」「载明」「风险方向较原判反转」),还挂着 DEC-0014 的编号
⇒ ⇒ 而 :9 自己是【会标撤回的】—— 它里面就有一句「旧写「敞口 ×4.3 朝更差」**已撤回**」
     ⇒ 作者知道怎么在这一行里标撤回;T1 锚点那一句只是没被标
```
**⇒ 这正是我今天反复打的那个形状,而这一次它的下游是一张【已经发出去的工单】。**

### ⚠ 而它还有一个二阶后果,目前无人接

`:9` 写「**计入值 T1=2.0 不动**」。而 T1=2.0 的**成文依据**(架构侧 `W1A:83`)是:
> 「2.0 相对**厂家值**留 **4× 余量**」——其中"厂家值"= 那个 0.5 cyc/MAC。

**⇒ 若 0.5 是浮点数、在定点口径下不适用(DEC-0023),那么「4× 余量」是【对一个不适用的数留的余量】。**
**⇒ ⇒ T1=2.0 这个数可能仍然合适,但它现在【没有成文依据】,而 `:9` 写的是"不动"。**
⛔ 我**不主张**改 T1 的值(那是架构侧+CTO 的事);我主张的是:**"不动"必须附上"而其原依据已随 DEC-0023 失效"。**

### 修法(三条,第①条今天就要做,因为工单在跑)
- ① **立刻给 verification 发更正**:W1-C 的任务是【取得第一个定点锚点】,不是【验证厂家锚点】。
  ⇒ 两者的产出物不同:前者要 CCES/板上跑出一个定点数,后者只需比对。**派单口径决定它交什么。**
  ⚠ 而 `W1_HANDOFF:114` 已载明 **CCES 装的是 ARM-only、无 SHARC 工具链** ⇒ **「取得第一个定点锚点」当前可能【跑不了】**
  ⇒ 这一条必须同时告诉 verification,否则它会去撞一堵已知的墙。
- ② `CLAUDE.md:9` 的 T1 那一句就地加撤回标记(⛔ 不删,E-2),指向 DEC-0023。
- ③ `:9` 的「计入值 T1=2.0 不动」补一句「⚠ 其原依据(4× 于厂家 0.5)已随 DEC-0023 失效,该值待重立依据」。

---

# 二、MAJOR-2 · CLAUDE.md 把「现役名单」的权威指向 `W1_HANDOFF §0`,而那张表**已过期约 5 个实例、且根本没有 `channel-dsp` 这一行**

**`CLAUDE.md:8`**:
> **现役名单只以 `W1_HANDOFF.md` §0 表为准**,不得凭 memory 或旧消息推断。

**而 `W1_HANDOFF §0` 的表(`:29–35`,标注 2026-08-01)**:

| 表里写的 | 今天(08-05)的实际 |
|---|---|
| `adaptive-dsp-3` = **现役算法侧** | ⛔ 已退役;现役是 **`adaptive-dsp-7`**(中间还退役过 `-6`) |
| `system-architect-2` = **现役架构侧** | ⚠ 你说它**休眠**中 |
| 「现役 critic」= 无名字 | 现役是 **critic 第 2 实例**(前任 `critic-w1c` 08-04 已不可达) |
| **(无此行)** | **`channel-dsp`** —— 该角色存在、有过 2 个实例、正在做 C 实现 |

**⇒ 而同一份文件里【至少并存三张名单】**:
```
§0 的表(:29–35)      2026-08-01   ← CLAUDE.md 指向的就是这一张
:140 的表             system-architect 第 2 实例
:647–649 的表         2026-08-04:adaptive-dsp-6 已退役 / adaptive-dsp-7 现役 / channel-dsp(第 1 实例)现役
⇒ 而连最新那张也已过期:你在派我上岗时说 channel-dsp 第 1 实例今天已死,现役是 channel-dsp-2
```

## ⇒ 为什么这是 MAJOR 而不是"文件有点旧"

1. **失效模式是已发生过的事故**:向已退役实例名发消息 ⇒ 复活 ⇒ 裂脑(**DEC-0011,四实例**)。
2. **CLAUDE.md 把它写成【唯一权威】并明令"不得凭 memory 或旧消息推断"**
   ⇒ 一个照做的新实例,会拿到一张三天前的名单,**并且被明确禁止用别的方式核对**。
3. **⭐ 而这份文件【预言了自己的失效】然后照做了。** `W1_HANDOFF:42` 原文:
   > 「⚠ 本表由 lead 维护,任何实例变更后必须当场更新。……**handoff 自称"以本节为准",过时的它就是 F-18 的复发入口**。」

   **⇒ 它写下了这句话,而现在它就是那个入口。**
   ⇒ 这是「文件同时规定了缺陷和抓这个缺陷的纪律」的最强形态 —— 而且是**自我指涉**的那一种。

**修法**:①`CLAUDE.md:8` 的指针改指向**当前唯一那张**(建议把名单从 handoff 里**抽出成独立文件**,
因为 handoff 已自认有重复编号、旧节与新节并存);②`W1_HANDOFF §0` 的旧表就地加「**已过期,见 <新位置>**」
(⛔ 不删,E-2);③补 `channel-dsp` 与 `critic` 两行。
**⇒ ⚠ 而根因不是"忘了更新",是【同一份文件里三张名单没有一张声明自己是不是当前的】** ——
这与我 W1P 那条 MAJOR(缺口列沿用旧理由)同型:**新的加上去了,旧的没标。**

---

# 三、MAJOR-3 · 门禁状态:**CLAUDE.md §0 里唯一带门禁标记的,是我当初打过的那一条**

lead 让我按"若绝大多数没写门禁状态就报 MAJOR"来判。**逐条查完,结论正是如此:**

| CLAUDE.md §0 条目 | 门禁状态 | L 标 |
|---|---|---|
| `:9` DSP 平台定型 / DEC-0006 | 无 | — |
| `:9` 算力记账口径 DEC-0009 | **「待 CTO 追认」** ✓(部分) | — |
| `:9` **T1 厂家锚点** | ⛔ **无**(且内容已被 DEC-0023 推翻,见 MAJOR-1) | [L2/厂家] ✓ |
| `:9` 计入值 T1=2.0 | ⛔ 无 | ⛔ 无 |
| `:10–17` DEC-0023 块 | ✅ **【未过 critic 门】** —— **而它有这个标记,是因为我当初把它判成 MAJOR** | — |
| `:18` IIRA 比核心慢 **0.42×** | ⛔ 无 | ⛔ **无** |
| `:18` FIRA **0.25 cyc/tap** | ⛔ 无 | ⛔ **无** |
| `:18` L1 = **640 KB** | ⛔ 无 | 「`.ldf` 载明」(≈L2)✓ |
| `:18` C3=16KB 是配额不是硬件墙(DEC-0014) | ⛔ 无 | — |

**⇒ 8 条实质断言里,只有 1 条带门禁标记 —— 而那一条是被 critic 打过才加上的。**
**⇒ ⇒ 也就是说:目前的门禁标记不是制度产出,是【评审的副产品】。没被评审到的就没有。**

## ⇒ 而这为什么按 MAJOR 报(用 lead 给的理由,我确认它成立)

**读者无法区分「已过门」与「没人审过」,而默认读法是前者。**
⇒ 尤其 `:18` 那两个数(0.42× / 0.25 cyc/tap)**没有 L 标**,却直接支撑一条架构裁定
(「biquad 链走核心,输出链 FIR 走 FIRA」)—— 而那条裁定会决定 D3/D4 与 D13 的实现形态。
⇒ 按 **C1**(进 log/规格/预算/承诺的数字必须标 L 级),入口文件是最强的那一类下游。

**修法**:§0 每条实质断言后缀一个统一标记,三选一:`[已过门 <verdict>]` / `[未过门]` / `[未经评审]`。
**⇒ ⚠ 而我建议默认值是【未经评审】而不是留空** —— 留空的默认读法是"没问题"。

---

# 四、MINOR + 三条遗留的收口

## m-1(新)· `W1_HANDOFF` 自认的重复编号,与 CLAUDE.md 的指针冲突

`CLAUDE.md:7` 让人「先读 `W1_HANDOFF.md` §0」;而 `W1_HANDOFF` 开头自己写:
> 「⚠⚠ 本文件存在【重复编号】:`## 6.` 与 `## 7.` 各出现两次……**定位一律用【标题文字 + 日期】,不要用编号**」
> 「恢复工作请直接读文末的 `## 8.` 断点」

**⇒ 入口文件让人按【编号】跳(§0),而被指向的文件说【不要用编号】。**
⚠ §0 恰好不在重复编号之列,所以当前不会跳错;**但指针形式与被指向文件的自述纪律相矛盾。**
**修法**:`CLAUDE.md:7` 改为按标题文字指路,或直接指向 `## 8.` 断点。

## ⇒ 我本轮自己标的三条遗留,在 `review/w1p-closeout-r4` 里的现状(全部查过)

| 条 | 现状 |
|---|---|
| **addendum-2 m-4**(54–110 Hz 两端不同 V) | ✅ **闭,且闭得完整**:改为「孤立模态区 = **31 – 110 Hz(全网格)**」,并补了「**V ≲ 51 m³ 的小房间里上沿越过 120 Hz ⇒ 与检测带重叠**」——**我要的两部分都在**,且原样引了「⛔ 不得两端各取一个 V」 |
| **主件 m-2**(②§0「6 个」) | ⚠ **半闭,而且闭的是【另一件事】**:它把「6 个」改成「**6 行 = 7 个数**」,修的是**行/数之别**(那个精度问题我没提)。**⇒ 而我 m-2 的原意是【排除轴正交】**:`g_duck`/`Quinn`/`NFFT` 三项 🔴 按"是不是我们定的常数"排除,而头条句的轴是"会不会翻结论"——**那三条排除注记在 r4 里一字未动,头条仍只报一个轴**。⇒ **⛔ 未闭** |
| **addendum-2 §5.3 三手段表补第四行** | ⛔ **未闭**:表仍写「三种手段」,且「实测真实房间 RIR」仍标 **「✅ 唯一能定这一段性质的手段」**;我建议的第四行(**合成非统计 plant** —— 镜像源/几何声学/模态叠加,数小时 vs 一个采集工单)未加 |

**⇒ ⭐ 而主件 m-2 那一条值得单记一笔**:它被**以我的编号**闭掉了,而闭的是相邻的另一个问题。
**⇒ 「按编号勾掉」与「按内容闭掉」是两件事** —— 这是我那份蒸馏件【机制 A(名单型)】在 finding 台账上的形态:
**清单上打了勾,而勾的不是那一项。**

---

# 五、⚠ 我本轮**未做**的(⛔ 不得读作已审)

```
⛔ W1_HANDOFF 除 §0 与名单外的全部内容(该文件 59 KB,含 §1–§8 多个断点节)
⛔ decisions_log.md 全部 DEC 条目(本轮只对 DEC-0021/0022/0023 与 CLAUDE.md 的一致性做了点查)
⛔ 各角色 agents/<role>/memory.md(它们也是"离开过团队的数"的载体,而且是【会被新实例整个读进去】的那一类)
⇒ ⭐ 建议下一轮就审 memory.md 那一族:它与入口文件同属"新实例开工即读",而它【连门禁概念都没有】
```

---

# 六、版本锁与放行条件

```
CLAUDE.md               sha256[0:16] = 5ebf3a3678d8bd68   (mtime 2026-08-04 15:54)
01_design/W1_HANDOFF.md sha256[0:16] = f5043475f07aca0a   (mtime 2026-08-04 23:37)
⇒ 二者在我评审窗口内未变动
```

**必闭(其中第 1 条今天)**
1. **MAJOR-1 ①**:给 verification 发口径更正 + 同时告知 CCES 缺 SHARC 工具链这堵已知的墙。
2. MAJOR-1 ②③:`:9` 的 T1 句加撤回标记;「T1=2.0 不动」补依据失效说明。
3. MAJOR-2:名单权威指针改指当前那张;旧表就地标过期;补 `channel-dsp` / `critic` 两行。
4. MAJOR-3:§0 每条实质断言加统一门禁标记,**默认值取【未经评审】**;`0.42×` / `0.25 cyc/tap` 补 L 标。

**同轮**
5. m-1 指针形式;主件 m-2 真闭(排除轴);addendum-2 §5.3 第四行。

---

# 七、给 lead 的一句话

> **你问「如果我派错了,现在说」——你派错了。而我要说清楚的是:那不是你读得不仔细。**
> **纠正是以【新增一条】加进去的,而被纠正的那句一个字没动,还挂着 DEC 编号、写得像既成事实。**
> **⇒ 你是自上而下读的,先撞见的就是它。**
>
> **⇒ 而今天这一整轮我打的每一条,归到最后都是同一个动作没做:【改结论时没有回去标旧的那句】。**
> 收口件的缺口列是这样、W1A 的 T1 是这样、handoff 的名单是这样、
> **而我自己的两次勘误(大增益放链尾 / 65.86)也是这样。**
>
> **⇒ 所以我这一轮不建议再立新纪律。⇒ 建议只做一件可机械查的事:**
> **凡改动一条已被引用过的结论,当场对【旧值/旧措辞】做一次反扫,并把命中处逐一标注 ——
> 而这正是铁律五(撤回必须全库传播)已经写着的,只是它一直没有触发器。**
