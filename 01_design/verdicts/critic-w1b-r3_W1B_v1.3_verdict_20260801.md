# W1-B v1.3 复审 verdict(第 3 轮)

```
reviewer: critic @ claude-opus-5 / 2026-08-01
被审件: 01_design/submissions/W1B_v1.3_804d80e8169d.md (sha12 804d80e8169d)
```

**实例**:`critic-w1b-r3`(= r2 同实例续用,rotation 规①:同工单评审链保 findings 逐条闭环)。
**配套只读件**:`IF-v1.4_ee35800c0be9.md`(冻结)、`IF-v1.4-receipt_6f8fc1312d13.md`、`IF-v1.4-receipt-addendum1_431c2c3e1ed2.md`、`W1A_v0.6_dfba3c5cbdcc.md`、`selfcheck_W1B/{check_w1b.py v6, check_w1b_output.txt, mem_sizeof.c, mem_sizeof_output.txt}`。

**锁版与自验复现(我方现场执行,非采信声明)**

| 项 | 结果 |
|---|---|
| 被审件 sha256[0:16] | `804d80e8169d2b8d` ✓ 与声称一致 |
| 盘面工作副本 vs 快照 | `diff` 空输出,**逐字节相同** |
| `python3 check_w1b.py` 重跑 vs 存档输出 | **逐字节相同**(`diff` 空) |
| `gcc -O0 mem_sizeof.c && ./mem_sizeof` 重跑 vs 存档输出 | **逐字节相同**(`diff` 空) |
| §10.2 三组 grep 在快照上重跑 | `IF-v1.0×1 / v1.0a×3 / v1.1×1 / **v1.4×56**`、合并 `grep -c`=**5**、`在议`=**10** —— **逐位相符**;残留 5 处逐行归属判定复核正确 |

---

# 结论:**FAILED**(0 BLOCKER / **1 MAJOR** / 7 minor / 5 INFO)

> **判定必须说清楚,因为这是第 3 次 FAILED(触发 ESCALATE + rotation 规③)。**
>
> **五条 MAJOR 的实质全部闭合,而且闭得比我要求的更彻底。** 我逐条独立验过:MAJOR-1 的裁定方向正确且**代价直说**(case (b1)/(b5) 被判为真盲区,不再宣称"有臂3 即可达"),CHECK G 是本项目第一个**专门用来量自己**的检查、真依赖规则本体、机械暴露 6/9 格不符;MAJOR-2 承认了 v1.2 的因果叙述是错的,并把三件机制的分工写清、把 T_shadow 按我的移交定级为可统计量;MAJOR-3 **认下且比我估的更差**(19.00KB vs 我手算 17.1KB),新增 `mem_sizeof.c` 编译器实算第二轨、两列都报不挑有利列、明文拒绝"已收口"断言;MAJOR-4 按裁定改回合同字面并出勘误件;MAJOR-5 三道锁 + B12 回归锁。10 条 minor 亦逐条有落点。**这是一次高质量的修订。**
>
> **FAILED 的唯一理由是:本轮的修法只改了每条 finding 的首要位置,没有做全文传播。** 我机械比对出 **8 处旧定案与新定案在同一份文件内并存**,其中 **3 处是规范性陈述**——`§1.2-C` 仍规定 `relaxed_entry` 窄带门取 **PNPR**(lead interim 裁定与 addendum-1 的"执行口径"明文要求按合同字面 PAPR、且**禁止下游引用** PNPR);`§5.3` 仍规定影子表继承 **裸 `t_veto_start`**(这**正是 B11 broken 版专门要抓的缺陷形态**,文件同时规定了缺陷和抓它的测试);`§3.5-#11` 仍写**已被本文自己证伪的旧盲区公式**。另有 CHECK F 的**两套数字**(10.9/15.1 与 12.4/15.6)在同一文件内并存 4 处。
>
> **这不是"再挑一轮"**:D3 纪律(全量替换类自验两向都报)存在的理由就是这个,而本轮 D3 只跑了**合同版本号**,没跑**内容替换**;作者自己在 §10.2 悟出"自验数字必须在最后一次改稿之后重跑",但没有把同一条推广到内容传播。**修法是 sed 级的,零设计风险**——我建议 lead 在向 CTO 报 ESCALATE 时**明确区分"第 3 次 FAILED"的性质**:前两次是逻辑洞,这次是传播缺陷。

---

## 0. 三道关执行状态

| 关 | 状态 |
|---|---|
| ① agent 自验 | **已执行且完全可复现**(两份脚本 + 三组 grep,我均重跑,逐字节/逐位相符) |
| ② 独立 critic | 本件,裁定 **FAILED** |
| ③ CTO 常识审 | 未到;**另注**:lead interim 裁定「v1.5 定版前 W1-B 不得 PASS」独立于本 verdict 成立 |

## 1. C1–C10 逐项

| 门 | 判定 | 证据 |
|---|---|---|
| C1 L 标齐 | **PASS(附 m-5)** | m-10 大部闭合(`4 轨/槽`、`各降 3dB 持续 5s`、`hold 10s / +1dB/s` 均已补标);**`T_exempt` 仍无标**(:351) |
| C2 无"实测"越级 | **PASS** | 7 命中逐条核:均为"本项目零实测"声明、"待 L1 实测"语境、"L1 实测后方可按实报"。**零违规**。⚠ 我按 lead 的提醒对每处命中看了上下文,未用裸串判定 |
| C3 L4 撑不可逆 | **PASS** | 无不可逆动作;初值全 [L4/待标定] |
| C4 L3 撑强约束挂待验 | **PASS** | §8.2 全 [L3/手算] + F3 敏感性 + 明文"不得当坐实" |
| C5 出处可溯 + 双轨 | **PASS(由 FAIL 转正)** | 系数轨 CHECK A/A2 异源双轨;**内存新增 `mem_sizeof.c` 编译器实算第二轨,r2 的 C5 FAIL 已闭合** |
| C6 关键资源量 | **PASS(条件)** | 无 LOCKED 项;C3 包络在字节打包档内(87.9%),WORD32 档越界已如实保留为未排除下行场景并指向 C9 |
| C7 撤回全库传播 | **FAIL → 计入 MAJOR** | PAPR 度量约定、盲区公式、窄带门口径、CHECK F 数值四项**均只做了声明与首要位置修改,未做全文反扫**——铁律五三步(声明/全库反扫/逐处加标或删)缺第二三步 |
| C8 外部输入入库 | **PASS** | PX4 BSD-3 已入库带 NOTICE |
| C9 选型收益闸 | **PASS(优秀)** | 明文拒绝把"余量 ~2.4×"当坐实;内存拒绝"已收口";WORD32 档不删 |
| C10 硬件不可逆动作 | **N/A** | 无物理动作 |

C7 的 FAIL 按治理是 BLOCKER 级红线;但其对象是**本文件内部的传播**而非"已撤回的数字仍在全库流通"(旧值均有正确版本在同文件内并存且新版更醒目),**危害等级不到 BLOCKER**,故我按实质定为 MAJOR 并在此显式记录这一降级判断及理由,供 CTO 复核。

---

# 2. MAJOR

## MAJOR-1(唯一)· 本轮修法未做全文传播:8 处旧定案与新定案在同一文件内并存,其中 3 处为规范性陈述

**为什么这是 MAJOR 而不是若干 minor**:三处规范性残留分别位于**判据定义节(§1.2-C)**、**分配策略节(§5.3)**、**残余风险清单(§3.5-#11)**——实现者、测试设计者、CTO 风险审各自的入口。且其中 §1.2-C 违反的是 **lead 今日的 interim 裁定 + addendum-1 的"执行口径"条**(正式件),§5.3 规定的正是 **B11 broken 版专门要抓的缺陷**。逐条:

| # | 位置 | 现文(旧定案) | 与之矛盾的新定案 | 性质 |
|---|---|---|---|---|
| **1** | **§1.2-C(:163-164)** | 「只许走最严 PERSIST 变体 —— 窄带门取 **PNPR ≥ T_pnpr_high**(v1.2 勘正…已按点单 P6 提请 v1.5 改)」 | §1.3(:197)/§3.3(:486)/§5.5(:755) + addendum-1「执行口径 1/2」:**执行 = `PAPR ≥ T_papr_high`;PNPR 降级为待 v1.5 偏离项、禁下游引用** | **规范性 · 违反 lead interim 裁定** |
| **2** | **§5.3(:662)** | 「被淘汰的 relaxed_entry 轨把 {f, 删除时刻, **rapid_onset/t_veto_start**, PERSIST 可观测信用} 留在影子表…(保鲜 **T_exempt**)」 | §3.2 修正1(:383-387)+ §5.5 `StandbyShadow`(:697-702):继承 **`causal_ok` 布尔而非裸时间戳**,信用为 `(obs_credit,hit_credit)` 二元组,保鲜 **T_shadow** | **规范性 · 即 B11 要抓的缺陷本身** |
| **3** | **§3.5-#11 残余栏(:511)** | 「case (b) 盲区 = "无轨迹历史 ∧ 遥测降级 ∨ tap < T_low_gr"(表述按 C11-④ 定稿)」;对策栏 ④ 仍写「PERSIST 驻留改滑窗命中率 + 影子表继承信用(**跨过讲话段**)」 | §3.2(:439-447)四析取项公式(明文"取代 v1.2 表述");§3.3(:459)明文「**改驻留语义完全不触及删轨路径**…这个因果是错的」 | **规范性(风险清单)· 比真实盲区窄 + 保留了已自认错误的因果** |
| 4 | §1.2(:130) | 「故凡"钉住 + 掩蔽"路径的窄带门**一律以 PNPR 承重**」 | 同 #1 | 记述,但读作规则 |
| 5 | §10.3 D1 表(:1087) | 「掩蔽路径改以 **PNPR(局部)承重**」 | 同 #1 | 记述 |
| 6 | §10.3 D1 表(:1090-1091) | 「PERSIST 严格连续 → 改滑窗命中率 + 影子继承」/「老化 6 槽 → **按可观测槽计** + 影子继承」 | §3.3(:459)+ §10.4(:1113):讲话期是"**未命中**"不是"未观测","按可观测槽计"对本场景**无效**;真机制 = 老化门 GR 放宽 + T_shadow | 记述 · **D1 表与 D2 表自相矛盾** |
| 7 | §7.3 标定清单 ⑦⑧(:884) | ⑦「persist_credit 继承窗」(标量口径,已被 m-1 唯一模型取代)、⑧「relaxed_entry 路径的 **T_pnpr_high**」 | §3.3 唯一数据模型 `(obs_credit,hit_credit)`;#1 | 记述 · 标定清单会照它派单 |
| **8** | **CHECK F 数值混版** | §1.2(:130)/§2.5(:309)/§3.5-#12(:512)/§10.3(:1088) 用 **PAPR +10.9 / PNPR +15.1dB** | 脚本 v6 实跑输出与 §3.3(:488)、addendum-1 均为 **PAPR +12.4 / PNPR +15.6dB**(F-1 亦由 38.9/38.9 变为 **37.2/38.2**) | **数据 · 同一检查两套数字并存**——项目已知致命型(F-16 潜伏形态) |

**附带**:§3.5-#12(:512)仍写「**CHECK F-1 证明**宽带限幅钉住稳态下啸叫是 tap 主导分量」——m-5 的修法(脚本已改为带伴随语音、判定行已改为"**本检查是该论证的旁证,不是『证明』**")**没有传到文档**。

**为什么发生(根因,供蒸馏)**:D3 纪律只被用在**合同版本号**这一种替换上(§10.2 三组 grep 全部围绕 `IF-vX.Y` 与"在议")。本轮真正的替换是**内容替换**(窄带门口径、盲区公式、继承集、CHECK F 数值、因果叙述),**没有任何一条做了"反向 grep 旧值 → 逐处判定"**。作者在 §10.2 已经悟出"自验数字必须在最后一次改稿之后重跑",但没有把它推广成"**每条 finding 的修法都要做一次旧值反扫**"。

**建议修法方向(不写实现)**:对本轮每一条 finding 建立一个"旧值特征串",逐条 `grep -n` 反扫全文并逐处判定(改/留痕/删),把命令与命中数写进 §10.2——即把 D3 从"版本号替换"推广到"任意定案替换"。**建议把这条提为 team_config D3 的增补**。

---

# 3. minor

| # | 位置 | 事实与判断 |
|---|---|---|
| **m-1** | §2.2 三分(:251)vs §3.3 唯一模型(:478-481) | §2.2 明文把 **`P_persist` 列入"墙钟时长"类**、「必须按墙钟计,不能按槽计」;§3.3 的唯一数据模型却按 **可观测槽** 计数并在 `W_p = P_persist / T_hop` **槽**处折半,§5.5(:754)注释亦写"分母=可观测槽"。⇒ 跳槽降档下 PERSIST 窗在墙钟上被拉长 = **m-3 自己识别的失效**,修法没到 §3.3。命中率的**分母**用可观测槽是对的,**窗长**用槽是错的——两者需分开 |
| **m-2** | §5.5 `imsd_fit`(:772/:777)vs `Track`(:685) | 结构已改为 `u8 seq_delta[W_MAX]`(**相邻已交付槽的空号增量**),而 `imsd_fit` 仍写 `t->seq_hist[P->W_used-1] - t->seq_hist[0]` 并把 `seq_hist` 传给 `ls_fit_x`。字段不存在,且语义由绝对序号变为增量后 `span` 必须改为**前缀和**。MAJOR-3 收窄①的连带改动未落到算法体 |
| **m-3** | §5.5(:711)vs §8.2.2 收窄③(:940)/`mem_sizeof.c`(:64) | §5.5 仍是 `PresetParams P`(**按值**),而内存第二轨按**共享指针**建模。按 §5.5 实算 ≈**14.8KB**(仍在 C3 内,结论不变),但**规范件与其内存证据不同源** |
| **m-4** | `mem_sizeof.c`(:72-73) | `word32_track_v13()` 与 `word32_track_v12()` **函数体完全相同**,末组按 9 个标量计;而 v1.3 的 `Track` 末组是 **10 个**(增 `miss_run`/`unobs_run`,`flags` 替 2 个 bool)。⇒ WORD32 档 v1.3 少算 `4B×NT(12)×8ch = 384B`,应为 ≈**20.53KB** 而非 20.16KB(**"仍越界"的结论不变**,方向=低估)。第二轨自身的算术须与结构同源 |
| **m-5** | §3.2(:351)、§3.3(:468)、§5.3(:662) | 保鲜期出现**两个参数名**:臂2 sticky 用 `T_exempt`、影子表用 `T_shadow`(60s),而 §5.3 又把影子写成 `T_exempt`。**二者关系全文未定义**(是否同一量?若不同,臂2 签名与继承签名的有效期不一致会产生第三种行为)。且 `T_exempt` **仍无 L 标**(m-10 未全闭) |
| **m-6** | §3.2 修正1(:387) | 「**继承 causal_ok 与 t_onset 必须同时继承或同时不继承**」,但 `StandbyShadow`(:697-702)**无 `t_onset` 字段**。若继承布尔即足够,该句多余且会误导实现;若确需 t_onset,结构缺字段。二选一并说清 |
| **m-7** | §1.2-B(:114) | 仲裁保证「每条轨最迟 `⌈NT/4⌉=3` 槽内必被观测」只对**容量轴**成因成立;`unobs_run` 的另一成因(**整槽未交付**,C1 跳槽)不受该轮转约束,其唯一上界是 `U_max=32`。文中把两者写在一句里,读起来像"未观测恒 ≤3 槽"。两条成因的上界不同,应分开表述(不影响 MAJOR-5 三道锁的有效性) |

---

# 4. INFO

① **自验可复现性连续第二轮达标,且这轮更硬**:两份脚本(`check_w1b.py` v6 / `mem_sizeof.c`)我均在独立目录重跑,输出与存档**逐字节相同**;§10.2 三组 grep 在**送审快照**上重跑逐位相符。项目里"自验声明可复现"从来不是默认状态,这是第二次达标。

② **CHECK G 不是走过场,我读了源码**:合取式 `族内最大 ∧ 因果时序 ∧ (臂1∨臂2∨臂3∧dom)` 被**编码并逐合取项求值**,场景输入为显式赋值表;改动合取结构会改变输出 ⇒ **真依赖被测规则**。文档 §9-⑯ 主动声明"规则层求值,不含物理量、不证明发生概率",定级准确。**这是本项目第一个专门为"量自己"而写的检查,建议蒸馏进 team_config D1**。

③ **`mem_sizeof.c` 是合格的第二轨**:逐字段照抄 §5.5(我对照过),编译器布局与手算是两条独立路径且能抓手算必漏的对齐填充;**两列(字节打包 / WORD32)都报、不挑对自己有利的那列**;明文拒绝"内存已收口"断言、把 SHARC 工具链 `sizeof` 列为唯一收口条件。这是 C5/C9 的正面样板。

④ **T_shadow 的诚实边界要盯住**:文中已自认典型单人发言轮次「10s–**数分钟**」,而初值 `T_shadow = 60s`。⇒ **初值在相当比例的真实发言轮次下不足**,(c1) 的覆盖边界比字面看起来窄。这是已声明边界不是缺陷,但在 W2 语料统计出结果前,**任何文档不得把 (c1) 写成"已覆盖"**(§3.3/§9-⑦b/§7.2-B10 目前措辞是对的,保持)。

⑤ **跨文档暴露面不在本 verdict 内**:盲区公式三处不同步、参数字典六处默认值分叉、反应时间口径分叉等,见 `critic_AB_cross_consistency_20260801.md`。其中 **P8(C11-④ 公式走 C9)是本工单最紧迫的跨文档项**,因为 **V-12 用例会照旧公式设计,会少测一整类场景**。

---

# 5. r2 findings 逐条闭环判定

| # | r2 finding | 判定 | 独立验证依据 |
|---|---|---|---|
| **MAJOR-1** | 因果时序合取门使三臂豁免在 case (b)/重生轨恒不可达;可达性表宣称与规则本体矛盾;D1 漏扫豁免式合取项 | **实质闭合(传播未完)** | 裁定为全局硬门且**理由是物理的**(臂3 不能替代:被音乐驱动的限幅器给出相同 GR)✓;可达性表改按合取门填 ✓;(b1)/(b5) 判为**真盲区**、(c2) 条件不可达 ✓;修正1 继承 `causal_ok` ✓;修正2 给 `dom` 可判定定义(本通道已跟踪轨中 PNPR 最高)✓;修正3 族内最大失效入册 ✓;盲区公式重写为四析取项 ✓;B11 回归锁 ✓;§10.3 D1 表补齐**豁免式三个合取项**(我核过:①族内最大 ②因果时序 ③dom 全部在表内)✓;CHECK G 6/9 不符我重跑复现 ✓。**残留 = MAJOR-1 表 #3/#4/#5** |
| **MAJOR-2** | (c1) 跨讲话段覆盖不成立(老化门未改 + T_exempt 10s) | **实质闭合(传播未完)** | 明文承认"v1.2 给的因果是错的"并**引用了作者自己写过的那句话** ✓;三件机制分工重写(老化门管短间断/影子表**真正承重**/滑窗管部分可见)✓;老化门 GR 条件放宽 `U_hold=24 槽≈384ms`(我复算:仍 ≪ 1-3s 讲话,故**确实只能管短间断**,分工表述正确)✓;`T_shadow` 定级为可统计量 + 入 §7.3 首位 + W2 必产出项 ✓;B10 改为**分两组用例**(≤T_shadow / >T_shadow)✓;§10.4 新增咬合行 ✓。**残留 = MAJOR-1 表 #3/#6** |
| **MAJOR-3** | 内存 ≈14KB 自相矛盾且逼近/越 C3 | **闭合,且比我要求的更彻底** | 认下并复述了我的推导(45B×96=4.22KB=26.5 个百分点)✓;新增编译器实算第二轨 ✓;**v1.2 = 19.00KB(118.8%)越界**(比我手算的 17.1KB 更差,我按零填充、编译器含对齐)✓;v1.3 四项收窄 → 14.06KB(87.9%)✓;**WORD32 档 20.16KB 保留为未排除下行场景**并指向 C9 ✓;拒绝"已收口"断言 ✓;把 SHARC 工具链列为两个承重预算的共同收口条件 ✓。**残留 = m-3/m-4(均不动摇结论)** |
| **MAJOR-4** | 对已冻结 C11-② 做替换式偏离且已写进设计,回执却"确认" | **流程闭合,文内未闭** | 改回合同字面(§1.3/§3.3/§5.5 三处)✓;PNPR 降级为"待 v1.5 偏离项、禁下游引用"✓;**出 addendum-1 一版一文件、不原地改主件**、把"确认机制与数值"收窄为"确认机制、对取值保留"、冻结效力不变 ✓ —— 处置正确且干净。**但 §1.2-C 与 §1.2:130 仍规定 PNPR ⇒ 文内执行口径二义,MAJOR-1 表 #1/#4/#5/#7** |
| **MAJOR-5** | 僵尸轨永久占位(未观测不老化 × 直读预算 × 硬规则④) | **闭合** | 三道锁齐:轮转仲裁(最久未观测优先,⌈NT/4⌉=3 槽,我复算 ✓)、`unobs_run ≤ U_max=32` 强制直读、硬规则④改为**有条件** ✓;B12 回归锁 ✓;§10.4 新增咬合行 ✓;残余(U_max/预算/NT 组合的最坏占位时长)入 §7.3-⑩ ✓。**残留 = m-7(表述,不影响机制)** |
| m-1 | PERSIST 三套数据模型 + 回执"@2s" | **闭合(附新 m-1)** | 唯一模型(有界计数+周期折半,`(obs_credit,hit_credit)` 二元组)✓;"@2s" 由 addendum-1 更正为"窗长恒 = 所在预设的 P_persist" ✓。新问题见 minor m-1(墙钟 vs 槽) |
| m-2 | 伪代码空号门比规范松 2× | **闭合** | `2*(span+1) > 3*W_used` —— 我验算 ⟺ `span+1 > 1.5·W_used` ⟺ 空号 > W_used/2 ✓ 与 §2.2 规范一致 |
| m-3 | 计数语义应三分 | **闭合(附新 m-1)** | §2.2 增第三类"墙钟时长" ✓;§5.5 引入 `now_ms` 且注明不与 `slot_seq` 混用 ✓;新提点单 13(读音频帧计数器的权限须架构侧确认)✓ —— 这条点单是对的,不越权 |
| m-4 | CHECK A2 报 ≤1e-12dB 而实测 5.31e-12 | **闭合** | §2.5/§4.1 均改为"最大 5.31e-12dB"并留痕"v1.2 报得比实测紧 5 倍";我重跑输出确认 5.31e-12 为最大值 ✓ |
| m-5 | CHECK F-1 空场景不可能失败,却被称"证明" | **部分闭合** | **脚本已改**(F-1 加入伴随语音、判定行明文"是旁证不是『证明』",新数 37.2/38.2)✓;**文档未同步**:§3.5-#12 仍写"CHECK F-1 **证明**",且旧数 10.9/15.1 残留 4 处 ⇒ MAJOR-1 表 #8 + 附带 |
| m-6 | 转述纪律自述失实 | **闭合(优秀)** | 改为**带例外清单的准确自述**,逐项列出转述了哪些合同规范值、为何保留、代偿是什么,并留痕"自述失实会让下一轮 critic 少查一遍" ✓ |
| m-7 | maintenance 邻域均值 op 低估 ~6× | **闭合** | 按最坏重算 ~900op(f≈7kHz 每侧上百 bin)✓;算力表 → 3.5-4.1k op / 7.0-8.2k cyc / 余量 2.4× ✓;F3 情景 30-35k ✓(我复算 3.5k×8.5=29.75k、4.1k×8.5=34.85k);给出降档旋钮 ✓ |
| m-8 | Track 缺连续未命中计数 + 影子表容量 8<NT | **闭合** | 增 `miss_run`/`unobs_run` ✓;影子表扩到 `NT` 并定义淘汰序 ✓ |
| m-9 | 看门狗 PERSIST-粗 3.3s 向下取整 | **闭合** | 改 3.38s,我复算 3×1070+21.3+150=3381ms ✓ |
| m-10 | 若干自定初值缺 L 标 | **大部闭合** | `4 轨/槽`/`−3dB 5s`/`hold 10s`/`+1dB/s` 已补;**`T_exempt` 仍无标**(m-5) |
| INFO ①-⑤ | — | **维持/闭合** | ③ 的跨文档项已由本轮 P8/点单 13 推进;④ 的 W1-A 侧问题见 A/B 交叉核件 |

**统计**:5 MAJOR → **5 条实质全闭**(3 条带传播残留);10 minor → 9 闭 / 1 部分闭。**新增 1 MAJOR(传播)+ 7 minor**。

---

# 6. 无法判定 / 须他方裁定

**须实测或素材(我判不了)**
1. `T_shadow` 真值——W2 会议语料"单人发言轮次时长分布";在此之前 (c1) 的覆盖边界不可量化。
2. `U_hold` / `U_max` / 直读预算 / `NT` 组合的最坏占位时长(§7.3-⑩,C 原型合成忙房间)。
3. PERSIST 折半近似与真滑窗的偏差(§7.3-⑪)。
4. 影子继承全链、因果时序 ≥2 槽门对元音起音的拦截力——零仿真,且影子继承现在承担 (c1) 的唯一桥接职责。
5. `mem_sizeof` 是 **x86-64 宿主代理**;WORD32 档是否成立须 SHARC 工具链 `sizeof`。**装 SHARC 工具链同时卡住算力口径(W1-C)与内存包络(C3)两个承重预算** —— 我认同作者把它并入 CTO 第三关议程第 9 项加权理由。

**须架构侧 / lead(我不直接联系)**
6. **P8:C11-④ 盲区公式走 C9 更正(最紧迫)** —— 见 A/B 交叉核件,V-12 用例正照旧公式设计。
7. 点单 13:旁链读取音频帧计数器的权限;不可读则须 C4 增 `slot_time_ms` 事实字段。
8. P6(C11-② 窄带门)、P1(C4 top-N 排序度量,D3/D4 派单前必须定)、P3(C12 AFE 削波锚点)、P4(C13 档位)、P5(`bus_is_local_reinforcement[8]`)。
9. §8.3-7 的 AEC 子模块定案须架构侧在 v1.5 认账(W1-A 差集表仍是模块粒度)。

**须 CTO**
10. 看门狗带 T_react ≈1.24s / PERSIST-粗 ≈3.38s 残余风险(F4 联动,议程第 3 项)。
11. §3.5-#1/#4 高残余风险交产品/运营兜底;sub-低频盲域。
12. **本次 FAILED 触发 ESCALATE(同一交付物 3 次 FAILED)+ rotation 规③(下一轮须全新 critic 实例)**。我把性质差异写在结论段,请连同本 verdict 一并呈报:**前两次是逻辑洞,本次是传播缺陷,修法为 sed 级、零设计风险**。
13. 旁链 float32 例外(V-9)仍待批,影响两侧预算口径。
