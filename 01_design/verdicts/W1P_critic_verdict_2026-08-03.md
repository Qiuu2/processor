# W1-P 独立 critic verdict — 2026-08-03

**VERDICT = FAIL**(3 BLOCKER / 6 MAJOR / 6 minor)
reviewer: critic 全新实例(未参与被审工作),claude-opus-5
被审件:§7.5 清单,sha256 现场复算 **14/14 相符**(`r53_msgmeter.py` 清单漏 sha,实为 `03ef4cbdda9f`)

**C 门**:C1 FAIL / C2 FAIL / C9 FAIL / C5 部分 FAIL / C4·C7 PASS / 其余 n/a
⇒ 按治理 §4,C1/C2/C9 任一 FAIL = 整体 BLOCKER

---

## 一句话

> **那 3.50–7.50 dB 不是 NHS 的收益,是"神谕替 NHS 选好点、并把 NHS 的检测器关掉"之后,8 个陷波器的收益。**
> **它是 NHS 的【上界】,被当作【实测】报出,并已用于对 CTO 的 4–5 dB 目标对表。**
> **⇒ 与 LESSONS_W1P B-3 那个"拿上界比实测"是同一事故,方向反过来再犯一次。**

## B-1 承重数的产生臂里 NHS 未参与(BLOCKER)

```
r57_bandlimit.py:51-61   picks 来自 clrig.critical_points(he) —— 解析临界点,非 NHS 检测输出
r57_bandlimit.py:64-76   8 槽直接写 HOLD/max_depth/set_coef,随后 a.P.T_low = 999.
nhs.py:386,402           gate = P.T_low;lv < gate 即 continue ⇒ 新分配恒不发生
nhs.py:94                lift_after_s=60 > T_OBS=6 ⇒ 预挂槽整轮不释放
复用于                   r51:57-68 / r53:45-55 / r59:14
```
**⇒ 实际被测 = "8 个 RBJ 陷波器由解析式放在最优点上"。`nhs.py` 执行到的只有 `set_coef` 与滤波两行。**
**⇒ 该项不在工作点向量、不在 FINDINGS:1153 限定清单 —— 而两处清单读起来都完整。**

**critic 补做的缺失对照**(槽全空 / `T_low` 默认 / `G = MSG_全带+3dB` = 架构侧自定阳性对照点):

| T60/sd | 神谕预挂 | NHS 自选@12s | @30s | 30s挂陷数 | 起振 |
|---|---|---|---|---|---|
| 0.2/0 | 6.03 | 2.98 | 4.97 | 6 | no |
| 0.2/1 | 6.03 | 0.11 | 3.65 | 8 | **YES** |
| 0.2/2 | 7.73 | 4.78 | 4.78 | 3 | no |
| 0.5/0 | 3.20 | 0.44 | 0.44 | 1 | **YES** |
| 0.5/1 | 6.33 | 0.07 | 5.48 | 8 | **YES** |
| 0.5/2 | 4.48 | 1.31 | 1.74 | 5 | **YES** |
| **均值** | **5.63** | **1.62** | **3.51** | | **4/6 起振** |

⚠ [L2/单一工作点,未做起振扫描] — 证的是"两者不是一个量",不是"NHS 就是 3.51"。

## B-2 「0% 归实现损失」结构上测不到(BLOCKER)
神谕的 `N(ω)` 取自**闭环正在跑的同一组 `b,a`**(`msg_meter.py:74-85` 用 `s.b,s.a`;`nhs.py:274` 同)
⇒ 设计阶段损失(系数量化/Q 偏差/频点偏差)被神谕一起吸收 ⇒ `|差|≤0.30` 近乎恒等,
证的是"时域仿真与频域计算一致"(台架管路正确),**不是 0% 实现损失**。
⚠ 且扫描栅格锚在解析值上(`:193-197`)⇒ `d` 构造上恒为 0.5 整数倍 ⇒ 0.30 落在量化噪声里。

## B-3 对外第一句把「台架带限被控对象」换成了「评价频段限定」(BLOCKER)
两者不等价。**按后者字面执行得到的是 `f_cut=24k` 那六行的带内神谕(均值 5.55),
与 8k 的 5.67 只差 0.12 dB,而同批种子闭环实测是 0.00–1.50。**
⇒ 读者按字面理解,落到的正是本轮花数轮证伪的"带内神谕"读法,而数值近乎相同、看不出来。
⇒ F32 在对外措辞层的复发。另:该行无 L 标(C1),且「NHS 实测」是 C2 红线词。

## MAJOR
- **M-1 ⭐ 等代价平坦衰减器 6/6 赢过陷波**(7.80–9.31 vs 5.67 dB,带内功率均值口径)
  ⇒ F33「duck 冒名顶替」的升级版:消融了 duck 锁定机制,却没问"该机制在等代价下打不打得过 duck"
  ⚠ critic 的公平声明:功率均值 ≠ 听感代价,窄陷波可闻代价远低于同功率宽带衰减 —— **但那句现在只能靠常识,不能靠本轮数据**
  ⇒ **lead 提级:必须在该数进对外口径之前闭合;`bw_oct` 1/5→1/8 后很可能翻转,故须先跑**
- **M-2** 均值跨过受控因子:`T60=0.2 → 6.50` / `T60=0.5 → 4.83`,差 1.67 dB(混响越重越差);
  「五条达标」取目标区间下沿(≥4dB 五条 / ≥5dB 四条)
- **M-3** 预注册 H7(k=8→k=1)静默丢弃,全库零命中、无"未做"声明
- **M-4** 本轮无任何 broken 版证伪;r53 的 5 条变异只守"表真读了槽状态",不守被测算法,
  杀死阈值 `>1e-6` ⇒ 符号错的表照样 5/5 全杀;`nhs.py:229` 的 B1..B14 开关框架本轮未用
  (pyc 已查,无陈旧字节码,本项非问题)
- **M-5** `cosource_check.py:60` 只覆盖 r45/r38,承重件 r51/r52/r55/r57/r59 一件未进
- **M-6** `0.5/seed0` 的 `f_trig = 52.0 Hz` 却用 `T_OBS=6.0` ⇒ 违反本轮自立的 F36;
  而它正是六条最小值、唯一未达 4 dB 的那条

## minor
m1 §7.1①b 只带通过的 Hk,未带 Hj 5/6 ｜ m2 P-1 证据 n=1 未标 ｜
m3 `0.2/seed1` picks 含 108.9 Hz < `f_det_lo=120` ⇒ 48 个陷波里 1 个 NHS 结构上挂不上(B-1 旁证)｜
m4 r53 无 sha ｜ m5 该数行无 L 标 ｜ m6 `BAND_DET=(100,8000)` vs 真实检测带 `120–7800` 并存

## critic 明确认可的(原样保留)
F34/r56「有正确解释仍去观测」**逐行核过是真的** ｜ Hg 未过没被藏且真去查了第二机制 ｜
r59 Hj/Hk 如实 FAIL、互斥裁决事前落盘 ｜ r60 三档全报含不利档 ｜
F33 duck 消融拆出 0.16+3.9 是本轮最有价值的自我证伪 ｜ `msg_meter` 恒返两列 = 纪律做成类型约束 ｜
架构侧双句呈报协议 + 机械 grep 判据质量高(B-3 是该构件**内部**一处替换错误,非否定该构件)

## 放行条件(FAIL→PASS 最小集,全做完换实例复审)
B-1(a 或 b)/ B-2 / B-3 / M-2 / M-3 / M-4 / M-6
M-1、M-5、m1–m6 可与 `bw_oct→1/8` 那轮合并,**但 M-1 必须在该数进对外口径之前闭合**
**⇒ B-1/B-2/B-3 闭合前,`5.67` 与 `3.50–7.50` 不得以任何形式进入对外口径讨论。**
已落盘的 IF-v1.8 C15-⑥ 与 W1A v0.46 §(5) 两处引用同属待改。

## 三道关
①agent 自验 = 已做且质量高 ｜ ②独立 critic = **FAILED** ｜ ③CTO 人审 = 未到
未见 commit(`prototype_W1P/` 未入库)⇒ 放行纪律未被违反
