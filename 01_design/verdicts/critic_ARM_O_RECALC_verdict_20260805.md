# critic verdict · 臂 O 六格【第三方独立复算】(m-2 的 owner,lead 2026-08-05 指派)

```
⛔ 门禁状态:本件是 critic 产出;它评的 `01_NHS_numbers.md` 仍为【未过门】
评审人:critic-w1c-2(第 2 实例)   日期:2026-08-05
标的 :`01_design/W1P_CLOSEOUT/01_NHS_numbers.md:98/253-260/272-283` 的「臂 O」六格
⚠ 范围:按 lead 批准 —— **只复算那一列,不重评整件**
```

## 0. 版本锁

```
tag  review/w1p-closeout-r6 = ce16665   ✅ 已核:上列被审件与 tag 内容【逐字节相同】(git diff 空)
01_NHS_numbers.md        41d2b9ab5a16fa1c
02_self_set_constants.md b2b407804a50b5b8
r76_cell_s20f0.json      e5c3b7bf67f5d244   ← 头条表自述的出处
我的脚本 /tmp/.../scratchpad/critic_armO_recalc.py(⛔ 不 import 被审件任何模块)
```

## 1. verdict

**解析那一列 = ✅ 复现,六格全中。⇒ 该列可由「作者已复算」升为「双方已核」。**
**而复算过程中在【实测那一半】撞到一处 —— MAJOR-1,不在原委托范围内,按 lead「有出入顶格报」报。**

```
PASS-with-conditions ── 0 BLOCKER / 1 MAJOR / 2 MINOR
```

---

## 2. ✅ 解析 dO 六格:复现

我按**规格**自己重写了全链(plant → 带限 → h_eff → 神谕选点 → RBJ 陷波 → 全带相位过零 MSG),
⛔ 未 import `clrig` / `msg_meter` / `nhs` / `r61` / `r57` 任何一个模块。

| T60/sd | MSG 无陷波 | MSG 挂 8 陷 | 我算 dO | 被核值 | 差 |
|---|---|---|---|---|---|
| 0.2/0 | −11.8484 | −5.8226 | 6.026 | 6.026 | −0.0002 |
| 0.2/1 | −12.9325 | −6.8998 | 6.033 | 6.033 | −0.0003 |
| 0.2/2 | −13.0786 | −5.3475 | 7.731 | 7.731 | +0.0001 |
| 0.5/0 | −12.4907 | −9.2924 | 3.198 | 3.198 | +0.0004 |
| 0.5/1 | −13.7509 | −7.4197 | 6.331 | 6.331 | +0.0002 |
| 0.5/2 | −12.5187 | −8.0380 | 4.481 | 4.481 | −0.0003 |

**⇒ 最大 |差| = 0.0004 dB。**

### ⭐ 而顺带坐实了三件此前只有单方数据的事(全部是本次复算的副产品)

```
① 神谕 8 选点 六格【逐点相同】—— 与 r76_cell_s20f0.json 记录的 picks 一字不差
   ⇒ 意味着不只是终值相符:**上游的临界点求解 + 贪心排他选点也复现了**
② m0 anchor 六格【逐位相同】:−11.8484/−12.9325/−13.0786/−12.4907/−13.7509/−12.5187
   ⇒ 即 §1.1① 那条「m0 ≡ 解析 anchor」的**解析那一侧**现在也有第三方数
③ §1.2 m-4 的「0.5/0 带内 4.691 vs 全带 3.198,差 1.49」⇒ ✅ 复现(我算 4.691 / 3.198 / +1.493)
```

### ⚠ 而这个「复现」有三条边界,⛔ 不得省(其中第二条我用数据量化了)

```
(a) ⭐ 「最大 |差| 0.0004」**测不出比 0.0005 更细的东西** —— 被核值只公布到 3 位小数
    ⇒ 0.0004 是**取整残差**,⛔ 不是"两个实现相差 0.0004"
    ⇒ 正确读法:「与公布精度一致」。作者那句 0.0004 同理,建议改写。
(b) ⭐⭐ **同规格 ≠ 独立规格**。我重写了实现,⛔ 没有换规格 —— 我用的仍是 nfft=2^18。
    ⇒ 我做了网格依赖检查(2^17/2^18/2^19/2^20)量化这条边界:
       0.5/1  2^18=6.331 → 2^20=6.328   (动 0.0033)
       0.5/2  2^18=4.481 → 2^20=4.483   (动 0.0018)
       0.5/0  2^18=3.198 → 2^20=3.200   (动 0.0012)
    ⇒ ⇒ **两个实现彼此相符的程度(≤0.0005),比它们各自相符于收敛值的程度(≤0.0033)高约 8 倍。**
       —— 这正是共享规格的签名:两边共用同一个 2^18,于是共享同一份网格误差。
    ⇒ ∴ 本次交叉核证的是【算术与管线接线】,⛔ 不是【规格选择】。⭐ 这条必须随结论一起报。
(c) 承重性:上述 ≤0.0033 dB 对该表任何判据都不承重
    —— 最近的一次擦边是 0.5/0 的 |缺口偏差| 0.395 距整格 0.50 尚有 0.105,是网格效应的 30 倍。
```

---

## 3. ⛔ MAJOR-1 · 那个 `±0.40 dB` 的界,唯独产生它的那一格是【随观测时长跳整格】的

**位置**:`01_NHS_numbers.md:253-260`(偏差表)+ `:264` ②+ `:269` ④ + `:285` 的「经得起 ±0.40 dB」

### 事实(全部来自头条表自述的出处 `r76_cell_s20f0.json`,我只是把它按 rung 拆开)

```
该文件每格跑了【两个观测时长】T_OBS ∈ {6, 12} s。六格里五格两档同值,而:
  0.5/0  臂 O 实测 dO:  T=6 → **3.5**      T=12 → **3.0**      ← 差整整一格 0.5 dB
         (m_O: −8.9907 → −9.4907;而 m0 两档相同 = −12.4907)
  其余五格             T=6 与 T=12 完全相同
⇒ 头条表取的是 T=12 的 3.00 ✅(选得对,理由见下)
⇒ ⚠ 而同一格的【附列】dN 动得更多:4.5 → 3.0 = **三格**
⇒ ✅ 不是噪声:s10f0 / s20f0 / s30f0 / s40f0 / s60f0 **五个源电平档全部复现同一跳变**
```

**⇒ 而 T=12 那一档选得对,这点我核了,理由是它自洽而 T=6 不自洽**:
```
解析临界 = −9.2924,STEP = 0.5
  T=12:m_O=−9.4907 ⇒ 区间 [−9.49, −8.99) ∋ −9.2924  ✅ = 完美检测器 + 向下取整
  T=6 :m_O=−8.9907 ⇒ 区间 [−8.99, −8.49) ∌ −9.2924  ⛔ 检测器在 6 s 内**漏掉了 0.30 dB 的超量失稳**
⇒ ∴ 「臂 O 偏差」这一列量的其实是【检测器在 T_OBS 内漏掉多少】 —— 它按定义就是 T_OBS 的函数。
```

### ⇒ 而问题不是"没写 T_OBS",是【本件自己已把 T_OBS 判为会翻结论的红项,而这个界没接上】

```
`02_self_set_constants.md:58` S-2 逐字:
  「`T_OBS` = 12 s ⛔ **无来源** 🔴 **会翻,且已实证**:
    F36 实测 22.3 Hz 的临界点 6 s 下根本不是赢家、**24 s 才收敛**;
    F74.2b 频移臂在 12 s 窗内**仍在增长**;r87 实测 **m0 臂 6/6 在长**」
`:59` S-3 更狠:「r64 实测 T60=0.5 三条在 **48 s 仍未收敛**」
⇒ ⇒ 即:**同一份收口件的另一份文件,已经把 T_OBS 登记为"会翻结论"、且点名 T60=0.5 层**
⇒ 而 :264 ② 的「|缺口偏差| ≤ 0.395 dB,0/6 格达整格 0.50」与 :285 的「经得起 ±0.40 dB」
   **一个字都没提 T_OBS** —— 它们挂的限定是「冻结最终配置代理;活臂时变偏差未测」,那是**臂态**轴,不是**窗长**轴。
```

### ⇒ 若不修,下游会怎么错(方向是不利的那一侧)

```
S-2 实证的方向是【窗越长,还在长】⇒ 检测器会抓到更早的失稳 ⇒ m_O 更低 ⇒ 臂 O 偏差更负。
0.5/0 若在 T=24 再跌一格(与 6→12 同幅):
     臂 O 偏差 3.0−3.198 = −0.198  ⇒  2.5−3.198 = **−0.698**
     |缺口偏差| |−0.198−0.197| = 0.395  ⇒  |−0.698−0.197| = **0.895**
⇒ ⇒ ② 的「0/6 格达整格 0.50」**破**;⑤「偏差中位 0.307 ≈ 缺口的 11%」的量级也随之变。
⇒ 而 :285「2.75 经得起 ±0.40 dB 的检测器偏差」正是本件自述**最像结论、最容易被整句摘**的那句
   —— 给 CTO 的净判断(「选点是剩下的唯一杠杆」)就架在它上面。
```

**⛔⛔ 而我必须写明我没有证明它会破**:我**没跑** T=24。
我主张的是两条**已坐实**的事:①那个界唯一的产生者是六格中**唯一**随窗长跳整格的那格;
②本项目自己已把该轴登记为 🔴 会翻。⇒ **一个建立在已知会翻的轴上的界,不带该轴的坐标,就不是一个界。**

### 修法(两条,第一条一行,第二条一格实验)

```
① 立刻(措辞):把 T_OBS 焊进那两句 —— 按本件 :288 自己立的标准「限定焊在句内,不放脚注」:
   「|缺口偏差| ≤ 0.395 dB **@T_OBS=12 s**;⚠ 该轴经 `02_self_set_constants.md` S-2 判为
     🔴 会翻且未收敛(F36:24 s 才收敛 / r64:T60=0.5 层 48 s 仍未收敛),
     而六格中 `0.5/0` 在 6→12 s 已实测跳整格 ⇒ **本界未在 T_OBS 上收口**」
   ⚠ 并须做一次【全族反扫】—— 「±0.40」「0.395」「0/6 格达整格 0.50」三串,
     扫到**句**(RETRACTED_STRINGS 新立的粒度口径)。⛔ 只改这两处不算修法。
② 结论级(一格实验,便宜):`0.5/0` 臂 O 单格加跑 T_OBS = 24 s。
   ⚠ 边界由被审件自己给出:`r76_cell.py:139` 的 assert 要求 T_OBS < `lift_after_s` = 60 s
     ⇒ 24 合法,48 亦合法。⇒ 一格一臂,不必重跑全表。
   ⇒ 结果两条路都有价值:不动 ⇒ 该界真收口了;再跌 ⇒ ② 当场作废,而那才是 CTO 该看到的数。
⛔ owner 归 lead 路由(verification 或 adaptive-dsp-7);⛔ 我不跑 —— 那是新实验不是复算,超出批准范围。
```

---

## 4. MINOR

```
m-1 「最大绝对差 0.0004 dB」(:273 / :281)**在构造上不可能大于 0.0005** —— 被比对值只有 3 位小数。
    ⇒ 它是取整残差,不是实现差。⚠ 而它读起来像后者,且被用作"高度一致"的证据。
    修法:改为「与公布的 3 位小数**逐格一致**(分辨力 ±0.0005,⛔ 不可细分)」。
    ⇒ 我自己这份也同样受限,已写在 §2(a)。

m-2 `msg_meter.py:22-25` 模块头:「2^18 是实测收敛点…2^18 起变化 ≤0.001 dB」。
    ⚠ 该收敛表只跑了**一格**(T60=0.5/seed2/8陷波,自述"最难一档");
    而我在**另一格**(0.5/1)测得 2^18→2^20 动 **0.0033 dB** = 该上界的 3.3 倍。
    ⇒ ⛔ 不承重(见 §2(c)),但「≤0.001」是从 n=1 格推出的**全局措辞**,应写成
       「在 0.5/seed2 上逐档变化 ≤0.001;其余格未测」。
    ⇒ 与 LESSONS B-4(极值必须带它的取值范围)同族。
```

---

## 5. ⛔ 本轮【没做】的,写明

```
· 实测那一列的其余五格 —— 未独立重测(我复算的是解析侧;实测侧我只做了 rung 拆解)
· Na 臂那一列的偏差(−0.096/−0.393/−0.486/+0.197/−0.222/+0.007)—— ⛔ 未复算,不在委托内
· 整件的其余部分 —— 按批准范围刻意未动
· T_OBS=24 s 那一格 —— ⛔ 未跑(见 MAJOR-1 修法②,归 lead 路由)
```

## 6. ⇒ 给 m-2 那条登记项的处置建议

```
`01_NHS_numbers.md:275` 现写:「作者已复算、独立评审未复算 ⇒ ⛔ 不得读作双方均已核;
                              第三方复算 owner 待 lead 指派」
⇒ 建议改为:「**解析列**已由 critic-w1c-2 独立复算(2026-08-05,自写实现不 import 被审模块,
   六格与公布的 3 位小数逐格一致;⚠ 同规格非同实现 ⇒ 核的是算术与接线,不是规格选择)。
   ⚠ 而**实测列**在 `0.5/0` 上随 T_OBS 跳整格 ⇒ 见 critic MAJOR-1,该格尚未收口。」
⛔ 不要写成「臂 O 那一列双方已核」—— 那会把我核过的一半(解析)与我没核的一半(实测)一起放行。
```
