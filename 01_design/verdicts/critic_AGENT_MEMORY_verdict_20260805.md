# critic verdict · `agents/<role>/memory.md` × 8(新实例开工即整个读进去的那一族)

```
⛔ 门禁状态:本件是评审产出。⛔ 我未改任何被审文件。⛔ 未 commit。
reviewer : critic @ claude-opus-5[1m] / 2026-08-05
判据     : 沿用我在入口文件 verdict 里立的三问,⛔ 未换新判据(lead 指定)
⚠ 独立性 : `agents/critic/memory.md` 由**前任实例** `critic-w1b-r3`(2026-08-01)所写,
           我本会话未向它写入过一个字 ⇒ 不构成自审。**但我是同一角色**,
           ⇒ §四的那一条建议由**另一实例**复核,我只报事实、不给它定级。
```

## 版本锁(2026-08-05 00:13)

| 文件 | sha256[0:16] | 行数 | 最后修改 |
|---|---|---|---|
| `agents/adaptive-dsp/memory.md` | `e42389fb4390f97c` | 696 | 08-04 14:08 |
| `agents/system-architect/memory.md` | `59175ca7d07fc83c` | 517 | 08-03 12:23 |
| `agents/channel-dsp/memory.md` | `12e8cdae2c20fa51` | 213 | 08-04 15:21 |
| `agents/critic/memory.md` | `564fd16d7aad23d7` | 31 | 08-01 14:20 |
| `host-software` / `orchestrator` / `platform-fw` / `verification` | 各 4 行 | 4 | 07-30 |

---

# 判定:**PASS-with-conditions**(0 BLOCKER / 1 MAJOR / 3 MINOR)

> **⇒ 而最该先说的是:lead 的预期【被证伪了】,而反常的方向是好的。**
> lead 预期「memory 只增不减 ⇒ 撤回的结论原样躺着 ⇒ 命中率比入口文件更高」。
> **⇒ 实测:命中率【显著更低】,而且 `system-architect/memory.md` 的撤回传播是我在本项目见过最好的一处。**
> **⇒ §三给出我认为的成因,它是一条可用的机制。**

---

# 一、先交 lead 指定的那次反扫:**捏造那句没有进入任何一份 memory**

**扫描串**:`仿真器` / `连不上` / `模拟器` / `emulator` / `simulator` / `连接不上` / `接不上`
**范围**:`agents/*/memory.md` 全部 8 份

```
命中 = 1 处,而它【不是】那句捏造:
  agents/adaptive-dsp/memory.md:102  「⑤微基准→W1-C(CCES 模拟器=L2,EZKIT=L1 先过 C10)」
  ⇒ 这是把 CCES 模拟器列为 L2 路径的正常记述,与「连不上」无关
⇒ ∴ **八份 memory 零污染。**
```

## ⇒ 而它实际落在哪、以及【已经被 verification 自己抓住并更正了】

| 位置 | 状态 |
|---|---|
| `D14_ACCEPTANCE_TABLE_v0.1.md:181-182` | ✅ **已自纠**:「初版我写『无,且仿真器连不上』—— 那句**转述自派单消息,我未核** ⇒ 盘面无任何证据」 |
| `D14_BRINGUP_TRIAGE_v0.1.md §1.3` | ✅ **专节处置**:「盘面没有任何证据支持这句」,并列出两条真实阻塞 |
| `D14_BRINGUP_TRIAGE_v0.1.md:212` | ✅ 还留了正确的余量:「若 CTO 在会话里说过而未落盘…」 |

**⇒ 我核了它给的两条真实阻塞,与 `W1_HANDOFF:122` 第 9 项原文逐字相符**:
```
阻塞 A  EZKIT 实板是否存在(第 5 项)
阻塞 B  CCES 装的是 ARM-only:全树无 cc21k/easm21k、无 SHARC 目录、无 21569 支持包、未见 license
        ⇒ W1-C 的 CCES 模拟器分支(L2)当前不可跑
两者【相互独立】—— 原文明写 ✓
```
**⇒ 而 `D14_BRINGUP_TRIAGE` 多推了一步,我认为这一步是对的**:
> 「『连不上』是一个**比这两条都靠后**的状态:要先有板(A)、先有工具链(B),才谈得上连」

**⇒ ∴ 捏造与事实的差别不只是真假,是【修法完全不同】**:
「连不上」指向"调试连接";真实阻塞指向"装编译器 + 买板"。**⇒ 若那句没被抓住,工单会派去调一个还不存在的连接。**

## ⚠ 一条给反扫本身的提醒(MINOR,见 m-3)

`knowledge_base/adsp21569/platform_lessons/bringup/ezkit_bringup_checklist.md:22,133` 里
**合法地**写着「JTAG 仿真器抢不过,**连不上** DSP」——那是厂家文档记载的**真实 boot 模式陷阱**。
**⇒ 日后任何人再反扫这句捏造,都会命中这两行合法内容。⇒ 反扫串须排除 `knowledge_base/`,否则会得到"仍有残留"的错觉。**

---

# 二、MAJOR-1 · `adaptive-dsp/memory.md:102` —— **一行里压着五个已过期的值,全部无标记**

**这一行的性质**(它自己写明了):
> 「**v0.2 对我方的裁决(出处 = architect 消息 2026-07-31)**」

**⇒ 即:它是一条【消息转述】,而不是本人验过的事实。⇒ 这正是 lead 说的形状 A。**

| # | 该行写的 | 今天的实际 | 标记? |
|---|---|---|---|
| ① | **数制:音频路 `Q2.30`** | 现行 `D34_FIXEDPOINT_CONVENTION §①②`:I/O 边界 **Q0.31**、链内 **Q4.27**、系数 **Q4.27**。**Q2.30 在现行约定里根本不存在** | ⛔ 无 |
| ② | 「Q15 系数 100Hz 处 **35.7 Hz** 误差已量化否决」 | 三轨终裁改为 **8.91 Hz**(`critic/memory.md:20`:「作者 35.7 = Q2.13 满 LSB **虚高 2–4×**」)。⚠ **裁决(否决 16-bit)仍成立且被证稳健** —— 错的只是那个数 | ⛔ 无 |
| ③ | 「**上界 7.3** 竞品反推」 | W1A v0.7 **撤销了「上界担保」这个冠名**;现为「竞品结构类比参考值(存在性上界,**异芯片异实现**)」 | ⛔ 无 |
| ④ | 「合计 **≈11.5%** 闭合」 | **DEC-0023**:「`AFC 计入 155,872 cyc/帧 = 11.69%` 建立在 EE-408 之上 ⇒ **随之待重取**」 | ⛔ 无 |
| ⑤ | 「微基准→W1-C(**CCES 模拟器 = L2**)」 | `W1_HANDOFF` 第 9 项:该分支**当前不可跑**(无 SHARC 工具链) | ⛔ 无 |

## ⇒ 为什么判 MAJOR

1. **载体最强**:算法侧新实例开工即整份读入,而 §0「当前状态」块并不覆盖 :102 所在的追加区。
2. **① 最险**:`Q2.30` 与 `Q4.27` 是**同类记法、不同值**,而定点格式决定系数量化、余量与噪声底。
   一个读到 `Q2.30` 的新实例不会觉得可疑 —— **它长得就像一个正常的格式声明**。
3. **② 是"结论对、数错"的教科书形态**:否决 16-bit 的裁决活着,而支撑它的那个数虚高 2–4×。
   ⇒ 若日后有人回头问"当初为什么否决",拿到的会是那个错数。
4. **⇒ 而五条的共同点是它们【都不是这份 memory 的作者自己算的】** —— 全部转自一条 07-31 的消息。

**修法**(⛔ 不删,E-2):`:102` 整行前置一句
`⚠ 本条为 2026-07-31 转述,其中 Q2.30 / 35.7 Hz / 上界 7.3 / 11.5% / CCES 可跑性【五项均已过期】,逐项见 <指针>`;
或按其自身「追加区只增不改」的纪律,**在追加区末尾新增一条勘误并在 §0 当前状态块加指针**。

---

# 三、⭐ lead 的预期被证伪 —— 而我认为成因是一条可用的机制

**lead 预期**:memory 只增不减 ⇒ 撤回的结论原样躺着 ⇒ **命中率比入口文件更高**。
**实测(我用 9 个已知被撤回的结论做反扫)**:

| 被撤回的结论 | memory 里的处置 |
|---|---|
| `P.341 §5.1.3.2` 错环路 | ✅ **最好的一处**:`system-architect/memory.md:8` **文件顶部全局撤回指针**(「适用于本文件【全部】版本记录与正文」)+ `:124`/`:175-176`/`:496` 逐处标注 + **记录了自己第一次撤回失败并复活过一次** |
| `2.5 cyc/biquad` 是浮点 | ✅ `channel-dsp:58`「其基准代码是 `float` 数组 ⇒ 是浮点数,定点值未测」;`:174` 同 |
| `11.2148` 经验界 | ✅ `channel-dsp:50` 作为**流程教训的事实记述**出现,不作现行界 |
| `35.7 Hz` | ✅ `critic:20` / `system-architect:66` 均**作为"错数"记录** |
| `18.089` | ⚠ **一处半闭**:`channel-dsp:103` 正确写「18.0618(严于扫描的 18.089)」,而 **`:40` 仍以 18.089 作"硬包络"** ⇒ **m-1** |
| `Q2.30` / `上界 7.3` / `11.5%` / `CCES 可跑` | ⛔ 全部未标 ⇒ **MAJOR-1** |

## ⇒ 成因(我认为这是本轮最值得带走的一条)

```
处置得最好的两份(system-architect / channel-dsp),恰恰是【被 critic 在"撤回没传播"这件事上
反复打过】的两份:
   system-architect —— P-5 那一串(撤回未生效 / 借频段复活 / 修错了特征串)
   channel-dsp     —— 我 D3/D4 的 MAJOR-4(11.2148 五处并存)与它自己的 18.089 六处反扫
⇒ 而 MAJOR-1 那五条的共同点是:它们【不是本人算的,是转述来的】

⇒ ⇒ ∴ 机制:**人会在【自己写的】东西上标撤回,不会在【别人告诉他的】东西上标。**
   因为"标撤回"这个动作挂在【我知道这是我的结论】这个自我认知上,
   而转述来的东西在心理上是【背景事实】,不是【我的结论】
```
**⇒ 这与我在 02impl-r4 verdict 里归纳的【机制 F(接收侧的名单缺口)】是同一根:**
```
机制 F:我验的是被转给我的那些,而没问是不是全的        —— 接收时【漏了】
本 条 :我标的是自己写的那些,转述来的从不标            —— 接收后【不维护】
⇒ 两条合起来:**转述进来的内容,既不被核全,也不被维护。**
⇒ 而 lead 已经做对了修法的一半(这两封给的是 tag + 路径)
   ⇒ 另一半是:**转述进 memory 的内容,必须带【出处 + 日期 + 复核状态】三件**
```

---

# 四、MINOR

| # | 位置 | 问题 |
|---|---|---|
| **m-1** | `channel-dsp/memory.md:40` | 「真正的驱动量是增益(**硬包络 +18.089 dB**)」—— 而同文件 `:103` 已写「解析硬包络 **18.0618**(严于扫描的 18.089)」。⚠ 更要紧:我 D3/D4 复核时确认 **18.089 是二分求出的【越界点】**(该值处实扫 max\|b\| = 16.0002 已越界),**不是安全上限** ⇒ `:40` 把越界点写成了"硬包络"。**同一文件内改了一处没改另一处** |
| **m-2** | `agents/critic/memory.md` 全文 | **停在 2026-08-01**(前任 `critic-w1b-r3` 所写)。§ 顶部「当前状态」写「**现役 critic 实例 = 本会话实例…对 W1-B 用名 `critic-w1b-r3`**」——⛔ 该实例已不在;中间还有 `critic-w1c`(08-04 不可达);现役是我(第 2 实例)。「待办」三条与「需 CTO 裁」六条均为 08-01 状态。**⇒ 与我在入口文件 verdict 里打的 MAJOR-2 同型(名单/待办停在旧日期)**,只是载体是 memory。⚠ **定级我不给**(见抬头独立性声明),交另一实例 |
| **m-3** | 反扫串本身 | `knowledge_base/.../ezkit_bringup_checklist.md:22,133` 合法记载「JTAG 仿真器抢不过,连不上 DSP」(厂家文档的真实 boot 陷阱)⇒ **日后反扫那句捏造会命中它** ⇒ 反扫串须排除 `knowledge_base/`,否则会误判为"仍有残留" |

## ⇒ 判据 ②(门禁状态)—— 按 lead 指定,一句话判掉

**八份 memory 无一份有任何门禁标记,该文件族亦无门禁概念。**
⚠ 而与入口文件不同,**我不认为这里该逐条加门禁标记** ——
memory 的体裁本就是"我当时的判断",逐条加会淹没内容。
**⇒ 我建议只加一处:每份 memory 顶部的「当前状态」块声明
「本文件为角色工作记忆,⛔ 非过门件;引用其中任何数前须回原始出处核」。⇒ 一句话,不是逐条。**

---

# 五、⚠ 本轮未做

```
⛔ 四份 4 行的存根(host-software / orchestrator / platform-fw / verification)—— 无实质内容,未审
⛔ adaptive-dsp(696 行)与 system-architect(517 行)的【全文】—— 我做的是【定向反扫 + :102 深查】,
   ⛔ 不是逐条通读 ⇒ 未命中我那 9 个反扫串的过期结论可能仍存在
⇒ ⭐ 而这正是本族的结构性难点:memory 只增不减 ⇒ 它会持续变长 ⇒ 逐条通读的成本单调上升
   ⇒ 建议的可持续做法不是"定期通读",是**每次撤回时把该撤回串加进一张【常设反扫表】**
     (= D6-x 的形态,但用途相反:D6-x 管"必须出现",这张管"不该再出现")
```

---

# 六、放行条件

**必闭**
1. **MAJOR-1**:`adaptive-dsp/memory.md:102` 五项过期值加前置警示(⛔ 不删原文)。

**同轮**
2. m-1:`channel-dsp:40` 的 18.089 加标注(指向 `:103` 与"它是越界点不是上限")。
3. m-2:`critic/memory.md` 顶部当前状态块更新(⚠ 由另一实例定级与执行)。
4. m-3:反扫串排除 `knowledge_base/`。
5. 判据②:八份 memory 顶部各加一句"非过门件"声明。

**建议(非条件)**
6. 建一张**常设反扫表**(不该再出现的串),每次撤回时当场追加 —— 见 §五。

---

# 七、给 lead 的一句话

> **你的预期被证伪了,而反常的方向是好的:memory 这一族的撤回传播【比入口文件好】,
> 而 `system-architect/memory.md` 顶部那条全局撤回指针,是我在本项目见过最好的一处 ——
> 它甚至记录了自己第一次撤回失败、并在下方复活过一次。**
>
> **⇒ 而唯一那条 MAJOR 的五个过期值有一个共同点:它们都不是那份 memory 的作者自己算的,是转述来的。**
> **⇒ ∴ 机制是:人会在【自己写的】东西上标撤回,不会在【别人告诉他的】东西上标 ——
> 因为"标撤回"挂在"我知道这是我的结论"这个自我认知上,而转述来的东西在心理上是背景事实。**
>
> **⇒ 而你那句捏造,八份 memory 零污染;它落在 D14 两处,而 verification 自己抓住并更正了,
> 连"若 CTO 说过而未落盘"的余量都留了。⇒ 那件事上你不用再担心传播,只需确认工单口径。**
