# critic verdict · 算法侧 r87 收口件(FINDINGS F76–F80)

```
⛔ 门禁状态:本件是评审产出,不是被审件。被审对象仍为【未过门】。
reviewer : critic @ claude-opus-5[1m] / 2026-08-04
实例声明 : critic 第 2 实例(全新)。我未参与 r76–r87 任何一轮的生成或评审。
被审对象 : 01_design/prototype_W1P/FINDINGS.md 的 F76 / F77 / F78 / F79 / F80
           PREREG_r87b.txt · PREREG_r87.txt · r87_cell_t{02,05}s{0,1,2}.json · r87_STATUS.txt
版本锁   : FINDINGS.md 14:03 落盘;PREREG_r87b.txt 13:29;六个 cell json 均 13:3x—13:4x
           ⇒ 本目录在我的评审窗口内**未变动** ✓(与我今日另两件不同)
```

# 判定:**PASS-with-conditions**(0 BLOCKER / 1 MAJOR / 2 MINOR)

> **lead 问的那一条,我的回答:【区分成立】,而且我是从原始 JSON 独立核的,不是读它的报告。**
> **⇒ 负结论的强度不但对,而且执行者【低估了自己】** —— 这个区分是朝**对自己不利**的方向做的
> (它把一条本可以支持"修法无效"的数据点主动划掉了)。
> **⇒ 而我找到的那一条 MAJOR,是别人没怀疑过的地方:预注册里那条「不得伤害」的护栏,符号是反的。**

## 我实际跑过什么(不是只读)

| # | 动作 | 结果 |
|---|---|---|
| 1 | 从六个 `r87_cell_*.json` 原始数据独立重算逐格 δ 与 F3 计数 | **与 F79.1/F79.3 逐格相符** |
| 2 | 对 `t05s1` 做逐字段 diff(A_base vs A_rf) | **差异字段集 = 空集** ⇒「没打到」坐实 |
| 3 | 核 δ 的代数恒等性(五臂是否共用同一个 m0 数值) | **六格全部一致** ⇒ D6-ai 反面边界的引用成立 |
| 4 | 核 ΔMSG 的符号方向(m 越大是否 dmsg 越大) | 成立 ⇒ **δ>0 = 改善** ⇒ 预注册符号反了 ⇒ **MAJOR-1** |

---

# 一、先答 lead 的那一问:**区分成立**

## 结论:「打了没用(5/6)」与「没打到(1/6)」的区分 **成立**,三条独立证据

### 证据 ①(我做的,不在交付件里):逐字段 diff

我从 `r87_cell_t05s1.json` 取 `A_base` 与 `A_rf` 两行,**逐字段比对**(排除 `secs` 墙钟):

```
t05s1(T60=0.5, sd=1)  两臂差异字段集 = []   ← 空集
```
对照其余五格:
```
t02s0  差异 ['m','dmsg','f_trig','lp_rms','grow','tpeak_ratio','fr',...]
t02s1  差异 ['lp_rms','grow','depths','dmed','gmin','n4','n5','A3',...]
t02s2  差异 ['n5','F2','F3','F4']
t05s0  差异 ['n5','F2','F3','F4']
t05s2  差异 ['lp_rms','grow','tpeak_ratio','fr','depths','dmed',...]
```
**⇒ 只有 `t05s1` 是逐字段完全相同。⇒ 该格的 δ=0.00 确为恒等式,不是测量结果。**

### 证据 ②:机理闭合 —— `F3_dropped ≡ 0`

我从原始 JSON 复算 `F3`(被 `out[:1]` 挡掉的候选数),与 F79.3 的表**逐格相符**:

| 格 | F3 基线→修法 | 判读 |
|---|---|---|
| t02s0 | 239 → 98 | 打了 |
| t02s1 | 5 → 2 | 打了 |
| t02s2 | 3 → 2 | 打了(微) |
| t05s0 | 22 → 4 | 打了 |
| **t05s1** | **0 → 0** | **名额从未绑定** |
| t05s2 | 22 → 2 | 打了 |

`recheck_free` 改的是**名额绑定时的计费方式**。`F3_dropped ≡ 0` ⇒ 名额从未绑定 ⇒
被修改的那条路径**从未被执行** ⇒ 输出必然逐位相同。**机理与观测闭合。**

### 证据 ③(交付件自带,而且是独立一跑):r86a 的阳性对照

`F80.1②` 强制 `recheck_free=True` 做阳性对照,**17/30 出现差异**,而
「块 B 的 5/6 与 r87 闸门**逐条一致**,未出现差异的正是已知不可达的 `0.5/1`」。
**⇒ 另一次独立起跑、另一套比对器,独立复现了同一条格的不可达性。**

## 那么负结论的强度要不要改?——**要改,但方向与 lead 担心的相反:它现在被【低估】了**

```
执行者报的:  0/6 变好,1/6 变差
剔掉不可判后:有效样本 = 5 条(t05s1 不携带信息)
             ⇒ 0/5 变好,1/5 变差,4/5 不可判
```
**⇒ 「0 变好」这个数一格没动;变的是分母,而分母变小意味着【每一条都是有信息的】。**

**⭐ 而真正决定强度的是另一件事,执行者做对了而没有邀功**:
按 **D6-ar②** 的判据 —— 「它在【损害发生的那些样本】上是否起作用」——
本轮损害发生在 `0.2/1`(深度 −15.00)与 `0.5/2`(深度 −16.50)两条。**修法在这两条上确实起作用了**
(深度补到 −18.00,分别补 +3.00 / +1.50),**而 ΔMSG 两条都是 δ=0.00**。

> **⇒ 这是负结论最强的形式**:不是"机制没生效所以没看到收益",
> 而是"**机制恰恰在最该见效的那两条上生效了,收益仍然是零**"。
> **⇒ ∴ 「修法不提交」这个处置,证据是够的。**

## ⚠ 但结论的**作用域**必须写死一句(现在没写)

δ 被量化到 **0.5 dB 的整数倍**(两臂同锚同栅格,STEP=0.5)。
**⇒ 本轮能排除的是「收益 ≳0.5 dB」,⛔ 不能排除「收益 <0.5 dB」。**

执行者在 §5 预注册里写死了「⛔『不可判』不得读作『没变化』」✓,
F79.2 也用了「换不来任何**可判**的 ΔMSG」这个限定 ✓ —— **措辞没有越界。**
**而缺的是把它接到决策上**:本线目标是 **4–5 dB**(LESSONS A-1),现行 ΔMSG 是 2.00–6.00
⇒ **一个 <0.5 dB 的收益本来就不值得提交** ⇒ **分辨力对这个决策是够的。**
⇒ **建议在 F79 收口句里补这一句**,否则「无收益」会在转述中变成
「该机制与 ΔMSG 无关」——**后者本轮证不了**。

---

# 二、MAJOR(别人没怀疑过的那一条)

## ⛔ MAJOR-1 · `PREREG_r87b.txt:119` 的「不得伤害」护栏**符号是反的**:它在成功时响,在受害时哑

**位置**:`PREREG_r87b.txt:119`

```
证伪条件(写死):②任一条 δ > +0.354(变差)⇒ Hg3 触发 ⇒ 判有害
```

**而同一份预注册自己定义**:
```
:43   主结论(δ = 修后 − 基线)
:127  δ = ΔMSG_rf − ΔMSG_base
:2§   ΔMSG = m_臂 − m0,m = **最后一个不起振的 G**
```

**我从原始 JSON 核了方向**(`t02s0`):
```
arm        m         m0        dmsg
m0      −11.848   −11.848     0.00
A_base   −5.848   −11.848     6.00     ← m 越大(越晚起振)⇒ dmsg 越大
A_rf     −6.348   −11.848     5.50
```
**⇒ ΔMSG 越大 = 起振前能加的增益越多 = 越好。⇒ δ > 0 是【改善】,不是【变差】。**

### 后果:这条护栏**两个方向都错**(D6-y 的双向判据)

```
真有害:  δ = −0.50(实测的 t02s0)  ⇒ 不满足「δ > +0.354」⇒ **护栏不响**
真改善:  δ = +0.50 或 +1.00        ⇒ 满足「δ > +0.354」⇒ **护栏响,并宣布「判有害」**
```
> **⇒ 一个会把成功判成失败、把失败放过去的护栏。**
> **⇒ D6-y 原文:「护栏必须朝【两个方向】检查:它会不会放过错的?它会不会挡掉对的?」——本条两问都答错。**

### 而实际执行与字面不符,且**没有留痕**

`F79.2` 写:
> 「⇒ Hg3(不得伤害)**按预注册触发**:`0.2/0` δ=−0.50 ⇒ 判有害」

**按字面,δ=−0.50 不满足 `δ > +0.354`,Hg3 不会触发。**
执行者按**意图**执行了(并且是朝**对自己不利**的方向 —— 宣布自己的修法有害),
**动作是对的;缺的是那句「此处我与预注册字面不符」。**

**⇒ 按 E-2(预注册的价值全部来自"它写下时就是那样")与 LESSONS C-4:
⛔ 不得改预注册原件;✅ 必须在 FINDINGS 里显式留一条「预注册 §2-② 符号笔误,
本轮按意图执行,原件不改」。**

### 同一份预注册内部还不自洽

`§5` 的判读三分表用的是 **|δ|**:
```
δ=0.00 ⇒ 不可判;|δ|=0.50 ⇒ 一格(弱);|δ|≥1.00 ⇒ 多格
```
而 `§2-②` 用的是**有符号的 δ**。**同一个量在同一份文件里两处约定不同。**

### 若不修,下游会怎么错

1. **这条护栏在本轮之后仍然是反的。** 下一个修法若真的产出 δ=+1.00(多格改善),
   按字面它会被 Hg3 判为"有害" ⇒ **一个能把唯一的成功扼杀掉的护栏。**
   而本线已经跑了 87 轮、收益一直是 0 —— **第一个真正的改善出现时,恰恰是最不该被误杀的那一刻。**
2. 本轮之所以没出事,**唯一原因是执行者没有按字面执行**。
   ⇒ 这正是 D6-ao 的形态:规则写下了,但让人停下来的不是规则,是人。

### 修法
- ① **⛔ 不改 `PREREG_r87b.txt` 原件**;
- ② 在 `FINDINGS.md` F79 里补一行留痕(见上);
- ③ 下一份预注册的 Hg3 改写为 **`δ < −0.354 ⇒ 判有害`**,或统一改用 `|δ|` + 方向说明;
- ④ **⭐ 并按 D6-y 给证伪条件立一条机械自查**:每条证伪条件写下时,代入
  「真有害的那个值」与「真成功的那个值」各算一次,**两次答案必须相反**。
  本条代入后两次答案是「不响 / 响」—— 正好反了,**当场就能看出来**。

### 复核方式
把 `δ = −0.50` 与 `δ = +0.50` 分别代入 `PREREG_r87b.txt:119` 的条件式即可,不需要重跑。

---

# 三、MINOR

| # | 位置 | 问题 | 修法 |
|---|---|---|---|
| **m-1** | `PREREG_r87b.txt:118` 证伪条件① | 「若 `0.2/sd1` 与 `0.5/sd2` 两条 δ 均 = 0.00 **而其余四条 δ ≠ 0**」⇒ 该条件要求一个**很特定的图样**。实测:两条焦点确为 0.00 ✓,而其余四条里**三条也是 0.00** ⇒ 条件不成立 ⇒ **Hg1 既未被证实、也未被证伪**。⚠ 执行者写「Hg1 **不支持**」而没写「证伪」—— **措辞准确 ✓**;问题在预注册:**「全零」是先验上最可能的结果,而它落在证伪条件的盲区里** | 下一份预注册的证伪条件须覆盖「全零」这一支(如:「若六条 δ 全 = 0.00 ⇒ Hg1 不支持,且须同时报本轮的最小可判效应量」) |
| **m-2** | F79 标题 / F79.1 判读列 | 标题写「**1/6 变差**」、判读列写「**更差(一格)**」。§5 自己声明「|δ|=0.50 是一格,**它高于 0.354 只是构造使然,不代表 0.5 dB 的真分辨力**」⇒ **单条一格是弱证据**。执行者在 F79.2 已写「⛔ 强度 = 一格,不得放大」✓,但**标题层没带这个限定**,而标题层正是会被转述的那一层(lead 的派单转述即为「0/6 变好、1/6 变差」,限定已掉光) | 标题与摘要层同样带「(一格,弱)」。⇒ 这正是 **D6-t**:限定必须进量的名字,因为只有名字扛得住转述 |

---

# 四、INFO(我核过、成立的 —— 以及这一轮做得好的地方)

**I-1 · δ 的代数恒等性论证成立,我从原始数据核了。**
F79.5 写「两臂共用**同一次测量的同一个 m0**,`δ = (m_修 − m0) − (m_基 − m0) = m_修 − m_基` 是**代数恒等式**,
不是 D6-ai 那种『共享噪声在差里抵消』的假定」。
我核了六格全部五条臂的 `m0` 字段:**六格内五臂数值完全一致**(如 t02s0 全为 −11.848431)。
**⇒ 引用的是 D6-ai 的【反面边界】而不是主体,引对了。**
⇒ 这条正是 `adaptive-dsp-7` 自己在 D6-ai 下面补出来的那条边界 —— **它把自己立的边界用在了自己身上,用对了。**

**I-2 · D6-ar 执行到位,而且执行的是它的【第②条】(更难的那条)。**
不只逐种子报(①),而且**把焦点放在损害发生的那两条上**(②)。
昨夜 D6-ar 的事故正是"聚合把 −16%/−13% 稀释成 −3%";本轮没有复发。

**I-3 · 「代理量达标 ≠ 交付量改善」是一次干净实证。**
F78.3 报「六条种子全部到达 max_depth」(代理量),F79.2 报「ΔMSG 一格未动」(交付量)——
**同一个修法、同一批种子、两个量、相反的结论**,而且 F78.5 **在跑 ΔMSG 之前就写下了**
「深度是代理量……⛔ 因此不得说该修法提升了 ΔMSG」。**⇒ 先声明限制,再去测,而不是测完再解释。**

**I-4 · F79.4 附列的 F33 处置分两档,分对了。**
duck 不消融列出现 4 格"改善",执行者**不记为修法收益**,并把两档强度分开:
① `0.5/2` 有直接证据(duck 深度 −4.00→−6.00 ⇒ 冒名坐实,记为 **F33 第四次**);
② 其余三条两臂 `g_duck最深` 都撞 −6.00 底 ⇒ **`gmin` 在此无分辨力** ⇒ 只说「**不能排除**」,不说「已坐实」。
**⇒ 「没有分辨力」与「测出了一个值」被明确分开 —— 这正是 F77.4 那条教训的复用。**

**I-5 · F79.6 那条盘面事实的自曝,是本件最该被表扬的一处。**
执行者在**自己的收口件里**写下:`r86_sep.py:28` 印着「逐位等价 12/12 已证」,
**而目录里不存在任何等价性证据件** ⇒ 「该等价性目前 = **一句转述**,不是盘面证据」,
并点明「**它已经被下一轮派单当作既成事实引用了一次**」。
**⇒ 这条自曝直接导致了 F80(等价链补环),而 F80 又查出缺的不是一环是三环。**
⇒ **一次自曝换来三跳的等价性证据。**

**I-6 · F77 的停止规则被真的执行了。**
lead 自设「r82 闸门这一轮不过 ⇒ 登记不追」,实测闸门 A 1/4、闸门 B 0/4 ⇒ **执行搁置**,
且 F77.2 把恢复条件与**价目**(N≈20 ⇒ 约 7×)一并量化留下。
**⇒ 而 F77.3 的「新判据更便宜」被证伪的量化根据(散布 0.49–0.91 dB vs 现行 0.354 dB,∝1/√N)
是我特别想点出的一条:「原理上对、量级上不成立」——结构说得通而没人算量级。**

**I-7 · 我核过、没有问题的其余项**:F79.6 的闸门确为 `sys.exit(1)` 型(D6-ap 合规);
G2 阳性对照 5/6 与我从 JSON 复算的"有差异格数"一致;
F79.7 的 `f_trig` 六条两臂一致(716–7580 Hz,全部 > f_det_lo=120)⇒ 无低频盲区格;
F79.5 的窗长四项方向正确(m0 是**减数** ⇒ m0 偏高 ⇒ ΔMSG 被**低估** ⇒ 读作「≥」)。

---

# 五、C1–C10 逐项

| 门 | 判定 | 证据 |
|---|---|---|
| **C1** L 标齐 | **PASS** | F76–F80 每条抬头均带 `[L2/宿主仿真]` + 「⛔ 未经 critic 评审」;F79 抬头列全工作点向量(src_rms/T_OBS/bw_oct/T_low/NN/n_cand/STEP/带限/仪器底) |
| **C2** 无"实测"越级 | **PASS** | 全篇「[L2/宿主仿真]」,并在 §2 明写「全部 [L2/宿主仿真],**非板上、非实测**」 |
| **C3** 无 L4 撑不可逆 | **PASS** | 本件结论是**否定性**的(不提交修法),不构成任何不可逆动作 |
| **C4** L3 撑强约束挂待验 | **PASS** | 无 L3 撑强约束项 |
| **C5** 出处可溯 + 关键数双轨 | **PASS** | 每条 F 抬头列件名;我按件名逐个找到并从**原始 JSON** 复算成功。⇒ 与我今日审的另两件形成对比:**这一份的可复核性是完整的** |
| **C6** 关键资源量 LOCKED 有 L1 | **N/A** | 不 LOCK 任何资源量;F77 明确封存两列不进架构讨论 |
| **C7** 撤回全库反扫 | **PASS** | F77.4 的 `2.00–6.00` → `≥ 2.00–6.00` 口径改写已落到 F76.4/F77.4 两处并声明范围;F77.1 的封存范围逐条列出(含"哪些句子不许说") |
| **C8** 外部输入 24h 入库 | **N/A** | 本轮无外部输入 |
| **C9** 选型收益闸 | **PASS** | 无算力/加速收益进入任何依据 |
| **C10** 硬件不可逆动作闸 | **N/A** | 无物理动作 |

**⇒ 十门无 FAIL。MAJOR-1 属预注册纪律问题,不落在 C1–C10 任一门上 ⇒ 整体不打回。**

---

# 六、放行条件(闭合后即可 PASS;不需要重跑)

1. **MAJOR-1**:FINDINGS F79 补一行留痕「PREREG_r87b §2-② 符号笔误,本轮按意图执行,⛔ 原件不改」;
   下一份预注册的 Hg3 改为 `δ < −0.354`,并加 D6-y 的双向代入自查。
2. **m-1**:下一份预注册的证伪条件覆盖「全零」这一支。
3. **m-2**:F79 标题与摘要层带上「(一格,弱)」。
4. **作用域补一句**:F79 收口句写明「本轮能排除的是收益 ≳0.5 dB;⛔ 不能排除 <0.5 dB;
   而本线目标 4–5 dB ⇒ 该分辨力对『不提交』这个决策是够的」。

**⇒ 以上四条都是【写一句话】级别,⛔ 不需要重跑任何实验。**
**⇒ 「修法不提交」这个处置本身,我不动:证据够,而且是负结论里最强的那种形式。**

---

# 七、给作者的一句话

> **这一轮是我今天审的三件里,唯一一件我能从原始数据把每一个报出来的数复算回去的。**
> 六个 cell 的 JSON 都在、字段齐、逐格对得上 —— 另两件的承重数我要么找不到出处文件,
> 要么出处文件不在它自己的交付清单里。
>
> **⭐ 而你做对的最难的一件,是那个区分本身。**
> 「打了没用」和「没打到」在 ΔMSG 上长得**完全一样**(都是 δ=0.00),
> 只有漏斗计数能把它们分开 —— 而你**主动去看了计数,并且把结果朝对自己不利的方向用**:
> 你划掉了一条本可以支持你自己结论的数据点。
>
> **⇒ 所以我这一轮打的不是你的结论,是你【为保护这个结论而写下的那道护栏】——**
> **它的符号是反的:真有害时它不响,真改善时它响并宣布"有害"。**
> **本轮没出事,唯一原因是你没有按它的字面执行。**
> **⇒ 而本线跑了 87 轮、收益一直是 0 —— 第一个真正的改善出现时,恰恰是最不该被误杀的那一刻。**
