# critic verdict · W1-P 收口件 r4(「≥」撤回后的复核)

```
⛔ 门禁状态:本件是评审产出。被审件仍为【未过门】。
reviewer : critic @ claude-opus-5[1m] / 2026-08-04
版本锁   : **`review/w1p-closeout-r4` = `2f09e37`**(annotated tag `7674f3925d56524f`)
           01_NHS_numbers.md `5988f2089719fd9a` · 02_self_set_constants.md `a918f989093fb0b0`
           03_open_questions.md `255393db5e79e4db`;独立 worktree,⛔ 未碰作者工作树
前件     : 主件 + addendum-1 + addendum-2(锁 r1/r2)
```

# 判定:**PASS-with-conditions**(0 BLOCKER / **1 MAJOR** / 2 MINOR)

> **⇒ lead 问的那一条,我的回答是:【反而更重】,而且它换了一列。**
> **⇒ 我原来的 MAJOR-2 不是随之消解 —— 它从「三列里两列没标」变成
> 「【那一列原本唯一标对的】现在标错了,而它正好是给 CTO 的路线数」。**

---

# 一、答 lead:我那条 MAJOR-2 **不消解,反而更重**,而且它换了一列

## 先把两个故事摆清楚

| | **旧故事**(r1/r2 时) | **新故事**(verification r76 + 本轮 §1.1) |
|---|---|---|
| 偏差住在哪 | `m0`(无陷波臂) | **`m_臂`(各个有陷波的臂)** |
| `m0` 的性质 | 一个**实测值**,被判为偏高 | **≡ 解析 anchor,六格逐位相同 ⇒ 误差恒为 0** |
| 机理 | m0 读数点仍在增长 ⇒ 读高 | **检测器漏掉小超量失稳 ⇒ 把陷波臂读高** |
| ΔMSG 方向 | 被**低估** ⇒ 写「≥」 | **可能被高估** ⇒ 「±0.50,方向未定」 |

## ⇒ 而这一换,把「谁需要限定」整个翻过来了

```
三个 ΔMSG 列(主列 / 附列 / 臂 O)= m_臂 − m0
   旧:m0 有偏 ⇒ 三列同向偏 ⇒ 三列都该标「≥」        ← 我原 MAJOR-2 说的
   新:m0 无误差,而 m_臂 各自有偏 ⇒ 三列仍都该标,但方向变成"未定"  ✅ **r4 已照做**

选点缺口 = 臂O − 主列 = (m_O − m0) − (m_Na − m0) = **m_O − m_Na**
   旧:m0 是【同一个数】⇒ 代数恒等相消 ⇒ 缺口列【正确地】不需要限定  ← 我原 MAJOR-2 也说了
   新:m0 本来就没有误差,相消的是一个【零】——
       而偏差住在 m_O 与 m_Na 里,那是**两个各自独立测出的数**
   ⇒ ⇒ **缺口 = 两个各自有偏的量之差 ⇒ 落入 D6-ai【主体】,不是反面边界**
```

## ⛔ MAJOR-1(承 MAJOR-2 而来)· `01_NHS_numbers.md:119` 把**旧故事的理由**原样搬进了新故事,而那条理由现在正好反了

**位置**:`01_NHS_numbers.md:119`
```
「选点缺口」列是两个 ΔMSG 之差 ⇒ m0 代数抵消 ⇒ 它的不确定度与三列不同源(D6-ai 反面边界)
```

**⇒ 三处都不再成立:**

1. **「m0 代数抵消」现在是【抵消一个零】** —— §1.1 ① 刚证明 `m0 ≡ 解析 anchor,六格逐位相同`
   ⇒ m0 的误差是 0 ⇒ 把它消掉不带来任何好处。
2. **「不确定度与三列不同源」反了** —— 缺口的不确定度**恰恰与三列同源**(都来自 `m_臂` 的检测器偏差),
   只是它含**两份**而三列各含一份。
3. **「D6-ai 反面边界」引反了。** D6-ai 反面边界的判别法原文是:
   > 「问『两臂用的是【同一个数】,还是【各自测出的两个数】?』同一个数 ⇒ 恒等相消,可用;
   > 各自测的 ⇒ 落入 D6-ai,不可假定抵消」

   **⇒ `m_O` 与 `m_Na` 是【各自测出的两个数】⇒ 这是 D6-ai 的【主体】,不是反面边界。**

## ⇒ 而它承重,而且承的是给 CTO 的那一个数

```
§4「剩下的唯一杠杆:选点」⇒「乐观上限是中位 2.75 dB 的一部分」
⇒ 2.75 就是缺口列的中位 ⇒ 它是 CTO 判「还值不值得继续修 NHS」的那个数
```
**⇒ 而缺口的两项偏差【没有理由相等】**:
```
臂 O   = LTI,8 个神谕陷波钉死,T_low=999(不自适应)
主列 Na = 自适应,陷波随时间挂/移/加深
⇒ 两者在临界点的谱形状不同 ⇒ 检测器"漏掉小超量"的程度没有理由相同
⇒ ⇒ 偏差之差不为零,且【没有任何东西把它界在 ±0.50 之内】
```
**⇒ 而本轮已有的实证正落在缺口的一端**:§1.1 ③ 实测 **臂 O 在 2/6 格高于其解析真值**
(`0.5/1` +0.169 dB、`0.5/2` +0.019 dB)。**⇒ 缺口的减数被证有偏,而被减数(Na 臂)的偏差【本轮未测】。**

**⇒ ∴ 我原 MAJOR-2 的结论要改写(变严)**:
```
旧结论:三列该标而只标了一列;缺口列正确地不需要
新结论:三列 ✅ 已改对;而【缺口列】现在是唯一一个【标错的】——
        它写着"不确定度与三列不同源",实为【同源且含两份】
⇒ 严重度上升:因为缺口列承的是路线决定数,而三列承的是"够不够 4–5 dB"
```

**修法**:
- ① `:119` 改写为
  「缺口 = `m_O − m_Na`,**两项各自含检测器偏差且未证相等** ⇒ 落 **D6-ai 主体**,
  ⛔ 不得假定抵消;其不确定度**至少与单列同量级,且未被 ±0.50 界住**」;
- ② §4 的 `2.75 dB` 同段带上该限定;
- ③ ⭐ **补一次可做的测量**:臂 O 已有解析真值(LTI),**主列 Na 臂没有** ——
  但可用同一手法(在各自终点 G 上比 `is_howling` 与解析/能量判据)**测出 Na 臂的漏检幅度**,
  ⇒ 两端都有数之后,缺口的偏差方向才可判。**⇒ 这是本轮唯一我建议新做的测量。**

---

# 二、r4 已闭的(我复核过)

| # | 前件 finding | 现状 |
|---|---|---|
| **MAJOR-2(三列「≥」)** | ✅ **闭** —— 「≥」从**全部三列**撤除,统一为「±0.50,方向未定」;§2.2 的达标计数随之不再依赖那个标记 |
| **m-1(§1 缺「消融」)** | ✅ **闭,且比我要求的强** —— §1 不但写进句内,还加了一句「**「duck 消融」是这句话的一部分,不是脚注**」 |
| **m-2(②§0 只报 6 个)** | ⬜ 本轮未查(不在 lead 指定范围) |
| **m-4(54–110 Hz 两端不同 V)** | ⬜ 本轮未查(同上) |

**⇒ 而 §1.1 那次撤回,我判它做得对**:
```
① 它把【观测】与【解读】分开:「m0 臂在 anchor 上仍在增长」这个观测没错,
   错的是"所以读高了"——而在 G=MSG 上极点恰在单位圆,缓慢增长【正是临界点的预期签名】
   ⇒ 同一观测支持不了那个推论 ✓ 这个论证成立
② 它主动指出「恰好一个步长 0.50」是**仪器返回了自己的下限**(r83 的做法只能输出 0.5 的倍数)
   ⇒ ⛔ 不构成"偏高 0.50"的证据 ✓ 成立,而且这正是我在 W1P 主件 MAJOR-1 打的同一形态
③ 它没有把我的 r87 verdict 与 verification 的结论对立起来,而是分开:
   「critic 核的是【推论】(有效);verification 打掉的是【前提】⇒ 有效推论 + 假前提 = 结论不成立」
   ⇒ ✅ 这个拆法是对的,我确认 —— 我 r87 verdict 核的确实是 F79.5 的推论链,不是它的前提
```

---

# 三、MINOR

| # | 位置 | 问题 |
|---|---|---|
| **m-1** | `01_NHS_numbers.md:230`(§4)vs `:188`(§1.2 m-2) | **仪器底新旧两个值同文并存**。§1.2 m-2 刚判定「**真实分辨力 = 整格 0.50 dB(单边),不是 0.354**」,而 §4 仍写「`0.2/sd0` 缺口 = 0.00(**落在仪器底 0.354 dB 之下** ⇒ 不可判)」⇒ **在 §1.2 改了、§4 没跟** —— **D6-l 同型,且是同一份文件内**。⚠ 判定结论不变(0.00 在两个门限下都不可判),但两个数并存 |
| **m-2** | §1.2 m-3 已自报 | `f_trig` 列未标臂 ⇒ 已在 r4 补「**Na 臂**」✓。⚠ 而它自报「带内列**本轮未补**(需重算,归下一轮)」—— **登记正确,我只是确认它没被静默省略** ✓ |

---

# 四、⭐ 作者那次二次失误 —— lead 说它比 finding 本身更值得看,我同意,而且它是一条【新机制】

> 作者原话:**「我核了【被转述给我的那两条】,两条都核实了 —— 却没问【是不是只有两条】。
> ⇒ 验证你拿到的部分,不能告诉你有没有拿全。」**

**⇒ 而它的后果是【方向掉个个儿】**:漏掉的第三条(臂 O 高于解析真值)把结论从
"无偏点估计"翻成"可能高估"。**⇒ 不是精度问题,是符号问题。**

## ⇒ 这对我那份蒸馏件是一条**新机制**,建议附上(⛔ 我不自行改那件,它须过另一独立 critic)

```
机制 F ·【接收侧的名单缺口】
   已有的机制 A(名单型)问的是:「你【声称】的成员,和实际存在的成员,谁在比对?」
   —— 那是【发出侧】:我列了一张单子,单子对不对
   而本例是【接收侧】:我收到一张单子,而我不知道它是不是全的
   ⇒ 判别句:「我验的是【被转给我的那些】,还是【原文里的全部】?」
   ⇒ 可执行形式:凡依据【转述】做核实,须回到【原始出处】数一次条目数
                 ⇒ 而这一条【只有拿到路径才能做】—— 所以它同时是对转述方的要求
```
**⇒ ⭐ 而 lead 已经把修法做对了:这次给的是 tag + 文件路径,不是转述。**
**⇒ 那正是这条机制的正解 —— 它不是"接收方要更小心",是【转述方要给路径】。**

**⚠ 而我要如实说:我今天也在这条上栽过一次。** `65.86` 之所以走三跳没被发现,
形态与此**互补**:那一次是**发出侧**给了一个长得像测量值的数,而接收侧没有路径去回验。
**⇒ 两次合起来正好给出完整形式(lead 已提):verdict 里的数,在【每一跳】都要重验;
而要能重验,【每一跳都必须传路径而不是数值】。**

---

# 五、放行条件

**必闭**
1. **MAJOR-1**:`:119` 改写(缺口落 D6-ai 主体,不得假定抵消);§4 的 `2.75 dB` 同段带限定。

**同轮必做**
2. m-1:§4 的 `0.354` 与 §1.2 的 `0.50` 统一(⇒ 一处改字)。
3. **建议新做一次测量**(MAJOR-1 修法 ③):测 Na 臂的检测器漏检幅度 ——
   **臂 O 那一端已有数,缺的是另一端。两端都有,缺口的偏差方向才可判。**

**前件未闭且本轮未查(⛔ 不得读作已闭)**
- 主件 m-2(②§0「6 个」)· addendum-2 的 m-4(54–110 Hz)· §5.3 三手段表补第四行

---

# 六、给作者与 lead 的一句话

> **给作者**:§1.1 那次撤回是我今天见过最干净的一次 —— 你把【观测】与【解读】分开、
> 指出「恰好 0.50」是仪器返回了自己的下限、并且拒绝把两份评审对立起来
> (「有效推论 + 假前提 = 结论不成立,两份各自都对」)。**这三步我一步都不改。**
>
> **⇒ 而我要指出的那一条,恰好是你这次【没有跟着翻】的那一列**:
> 缺口列的免责理由(「m0 代数抵消 ⇒ D6-ai 反面边界」)是**旧故事的理由**。
> 新故事里 m0 的误差是 0 —— 你抵消掉的是一个零;
> **而真正的偏差搬到了 `m_O` 与 `m_Na` 里,那是两个各自测出的数 ⇒ 那是 D6-ai 的【主体】。**
> **⇒ 你把三列都翻对了,唯独漏了那个【原本唯一标对的】—— 因为它看起来不需要动。**
>
> **给 lead**:你问【消解还是更重】—— 更重,而且换了一列。
> 而它更重的理由不是技术上更大,是**它落在给你和 CTO 的那个数(2.75 dB)上**,
> 而三列承的只是"够不够 4–5 dB"。
