# critic verdict · W1-P 收口件 **r6 稿**(`review/w1p-closeout-r6`)

```
⛔ 门禁状态:被审件【未过门】;本件是 critic 产出
评审人:critic-w1c-2(第 2 实例)   日期:2026-08-05
锁   :tag `review/w1p-closeout-r6` = `ce16665`(annotated `add58b4`)
       ✅ 已核:工作树与 tag 内容【逐字节相同】(`git diff ce16665 -- 01_design/W1P_CLOSEOUT/` 空)
       01_NHS_numbers.md 41d2b9ab5a16fa1c · 02_self_set_constants.md b2b407804a50b5b8
前件 :`critic_W1P_CLOSEOUT_r5_verdict_20260805.md`(PASS-w-c,0B/0M/3m,锁 r4=`2f09e37`)
范围 :r5 三条 minor 的闭合 + `2f09e37 → ce16665` 全部 diff(3 文件 / +90 −9)
```

---

## 一、lead 问的那一问,先答

**⇒ r5 三条 minor:✅ ✅ ✅ 全部真闭。而 m-3 闭得比我要求的好。**

| r5 finding | 我的修法原文 | r6 实际 | 判 |
|---|---|---|---|
| **m-1** 限定没进结论句 | 「两句结论句各加(冻结最终配置代理;活臂时变偏差未测)」 | **三处**都加了(`:265` ② / `:267` ③ / `:286` 末句),且作用域自行写成「**全文件 + 全族**有效,⛔ 非本节」 | ✅ **超出要求** |
| **m-2** 臂 O 列两方均未复算 | 「表下加一行『引自 V-F,本件与 critic 均未独立复算,复算 owner 待指派』」 | `:272-275` 不止加了,还把状态推进了一格:作者已独立复算,并**如实写「⛔ 不得读作双方均已核」** | ✅ **超出要求** |
| **m-3** ②§0 排除轴与结论轴正交(挂三轮) | 「并列报 7 个无来源 ∧ 共 11 行 🔴」 | `02:19-34` 重写为**轴 A / 轴 B 并列**,且**把所有权拆成三档**(自定 8 行/9 数 · 观测量 2 行 · 合同值 1 行),并自己写下「⛔ 此前只报了 ②,那会让读者以为会翻结论的 = 7 个」 | ✅ **超出要求** |

**⇒ ⭐ m-2 那条尤其值得记:它是本项目里少见的、把「我做到哪一步」与「⛔ 不得读作哪一步」写在同一句里的例子。**
**⇒ ⭐ 而 m-3 附带修掉了一处我没提的**:`02:42` 原写「两项 🔴 **不计入**上述计数」→ 改为
「按所有权轴归为观测量,**但仍在轴 A 的 11 行内**;⛔ 不得当作『被排除』」。**那正是我 m-3 的实质,而我当时只说了要并列报数,没说到排除注记本身。**

---

## 二、verdict

```
FAIL ── 0 BLOCKER / 2 MAJOR / 0 minor
⚠ ⛔ 而这个 FAIL 与上表【无关】:三条 minor 是真闭的。
   FAIL 来自本轮 diff **新引入**的内容 —— r6 新加了 74 行,而两条 MAJOR 都在那 74 行里。
⇒ ∴ 报法必须是两句,⛔ 不得压成一句:
   ① 「r5 的三条 minor 全闭」= 是
   ② 「r6 这个冻结点可放行」= 否
```

---

## 三、⛔ MAJOR-1 · 同一个量,同一轮里,既写「没有任何东西把它界住」又写「有界」

**位置**:`01_NHS_numbers.md:133` ↔ `:264` —— **两句在同一个 diff 里被同时加入**
(`git diff 2f09e37 ce16665` 的第 26 行与第 56 行,都是 `+`)

```
:133  「⇒ 偏差之差不为零,且**目前没有任何东西把它界在 ±0.50 内**」
:264  「② 而幅度【有界】—— |缺口偏差| ≤ **0.395 dB**,**0/6 格达整格 0.50**」
```
**⇒ 主语是同一个量**(缺口偏差 = 臂 O 检测器偏差 − Na 检测器偏差),**相隔 131 行,方向相反。**

### ⇒ 成因(可指认,不是"笔误")

```
:133 属于**旧问题的提法**:它是替换掉那段已作废的 D6-ai 理由时写下的"现在还不知道"。
:264 属于**新答案**:r89 就是去测这个的,测出来 max 0.395。
⇒ ⇒ 即:**同一轮里,问题被回答了,而提出问题的那句没被更新。**
⇒ ⭐ 这与 r6 自己在 `:135-137` 记下的那条形态**一字不差是同一个**:
   「那句话在旧前提下是对的;我改前提时改了【结论】,没改跟着它的【理由】」
   ⇒ ⛔ 而这次反过来:改了【理由】,没改跟着它的【问题陈述】。**同一份件,同一轮,第二次。**
```

### ⇒ 若不修,下游会怎么错

```
两句给 CTO 的印象是相反的:
  引 :133 ⇒ 「检测器偏差没有上界 ⇒ 2.75 的可信度未知」
  引 :264 ⇒ 「偏差 ≤0.395,而缺口 2.75 ⇒ 余量 7× ⇒ 结论稳」
⇒ 而本报告的净判断(「选点是剩下的唯一杠杆」)成不成立,正好取决于读者摘了哪一句。
⇒ ⛔ 一份件里两句互斥的不确定度陈述,比任何一句单独错更糟 —— 它使【引用者可以自选结论】。
```

### ⇒ ⭐ 而哪一句对,我有立场(见 MAJOR-2):**`:133` 那句更接近对的**

```
:264 的「有界」是一个 n=6 的**实测观察**,⛔ 不是一个界;而它缺一个坐标(见 MAJOR-2)。
:133 的「没有任何东西把它界住」说的是**没有原理性的界** —— 这一条至今成立。
⇒ ∴ 修法⛔ 不是"删掉 :133" —— 那会把唯一正确的那句删掉。
```

**修法**(两句各改,且必须一起改):
```
:133 → 「…⇒ 偏差之差不为零。⛔ **没有任何原理把它界住**;而 r89 已给出一个 n=6 的
        **实测量级**(见 §3:|缺口偏差| ≤ 0.395 dB @T_OBS=12 s)—— ⛔ 那是观察不是界。」
:264 → 「② 而幅度【实测量级】—— n=6 全部 ≤ 0.395 dB @T_OBS=12 s;⛔ 这不是一个界
        (无原理性上界,见 §2 :133),且未在 T_OBS 上收口(见 critic 臂 O verdict MAJOR-1)」
⚠ 并须【全族反扫】「有界」「≤ 0.395」「0/6 格达整格 0.50」「±0.40」四串,**扫到句**
  (粒度口径依 `RETRACTED_STRINGS.txt` 2026-08-05 新立)。⛔ 只改这两处不算修法。
```

---

## 四、⛔ MAJOR-2 · 那个 `±0.40 / 0.395` 的界不带 `T_OBS` 坐标(**本条 = 已出件,此处为并案**)

**⇒ 详见 `critic_ARM_O_RECALC_verdict_20260805.md` §3。⛔ 不重复举证,只说为什么它算在本 tag 上:**

```
那张偏差表(`:253-260`)与它的四条判读(`:263-270`)**是 r6 这一轮新加的** ——
`git diff 2f09e37 ce16665` 里它整块是 `+`。
⇒ ∴ 它不是"旧账",是**本冻结点引入的新内容** ⇒ 必须计入本 tag 的判定。
```
**一句话复述**:六格中**唯独 `0.5/0`** 的臂 O 实测 dO 随观测时长跳整格(T=6 → 3.5,T=12 → 3.0,
五个源电平档全复现),而那一格正是产生 `−0.395` 这个极值的那一格;
`02_self_set_constants.md:58` 自己已把 `T_OBS` 判为 🔴「会翻,且已实证」;
而 `:264` / `:285` 两句**一个字都没提 T_OBS**。

**⇒ ⭐ 而 MAJOR-1 与 MAJOR-2 是同一件事的两面**:
```
MAJOR-1:件里同时存在「无界」与「有界」两种说法
MAJOR-2:那个「有界」缺一个已知会翻的坐标
⇒ ∴ 把 T_OBS 补进去,MAJOR-1 的矛盾也随之消解 ——
   因为「@T_OBS=12 s 的 n=6 观察」与「没有原理性的界」**不矛盾**。
⇒ ⇒ 故两条合起来只需**一次**改动 + **一次**反扫。⛔ 不要当两件事排两轮。
```

---

## 五、✅ 本轮 diff 里我核过、判为**对**的(⛔ 不是"没看")

```
① `:118-133` 把旧 D6-ai 理由整段作废并给出三条不成立的理由 —— 三条我逐条核过,**全部成立**:
   「m0 代数抵消 = 抵消一个零」✅(我本轮独立复算已证 m0 ≡ 解析 anchor,六格逐位)
   「与三列不同源反了」✅  「D6-ai 引的是主体不是反面边界」✅
② `:135-137` 自认的失效形态(改了结论没改理由)—— 判语准确,且它**主动**记下
③ `02:19-34` 轴 A/轴 B 并列 + 三档所有权拆分 —— 算术自洽(8+2+1=11;8 行 9 数;6 行 7 数)
④ `03:78-87` 把「卡在 SHARC 工具链」拆成**两条互相独立的阻塞**,并当场指出
   `W1_HANDOFF:127`「CCES 模拟器分支可立即开」与第 9 项实查**同文件内矛盾**,已报 lead
   ⇒ ⭐ 这是**作者主动做的关系核**,且发现的是别人件里的矛盾 —— 记一笔
⑤ `:494-500` 三种手段 → 四种,第四行如实记「本项目已试,失败」(r88 闸门未过 / r88b 伪影)
   ⇒ ⛔ 那第四种是我提的,而它失败了;件里记的是失败,⛔ 没替我圆
⑥ `:292` 把「仪器底 0.354」改为「分辨力 0.50 dB(单边,§1.2 m-2)」⇒ 与 m-2 的结论一致 ✅
```

---

## 六、⇒ 附:一份**副产品数据**,归还给 `§1.2 m-4` 那个登记在案的缺口

`:117` 写「⛔ **带内列本轮未给**(见 §1.2 m-4)」。我复算臂 O 时顺手拿到了**解析**那一半:

| T60/sd | 解析 dO **带内**(100–8000) | 解析 dO **全带**(20–23900) | 差 |
|---|---|---|---|
| 0.2/0 | 6.026 | 6.026 | 0.000 |
| 0.2/1 | 6.033 | 6.033 | 0.000 |
| 0.2/2 | 7.731 | 7.731 | 0.000 |
| **0.5/0** | **4.691** | **3.198** | **+1.493** |
| **0.5/1** | **6.613** | **6.331** | **+0.281** |
| 0.5/2 | 4.481 | 4.481 | 0.000 |

```
⚠ 这是【解析 dO 的带内值】,⛔ 不是 m-4 要的「带内列」——
  m-4 缺的是三臂的**实测**带内量,那要换带内检测器重跑,我给不了。
⚠ 来源等级 [L2/宿主仿真,critic 自写实现] ⇒ ⛔ 请作者复核后再采用,不要直接搬
  (⇒ 若采用,按新立纪律须写明来源件 = 本 verdict,并把这条限定一起搬过去)
⭐ 而它自带一条信息:两band 相同的 4 格 = 挂陷后最险点仍在带内;
  不同的 2 格正是 `§1.2 m-3` 说的「臂 O 失稳点退到 22–503 Hz」那两格 ⇒ 两处观察互相印证
```

---

## 七、⛔ 本轮**未审**(不得读作已审)

```
⛔ `2f09e37` 之前已审部分不重审(见 r4/r5 verdict 的未审清单,原样继承)
⛔ Na 臂偏差那一列(−0.096/−0.393/−0.486/+0.197/−0.222/+0.007)—— 未复算
⛔ PREREG_r89 原件 —— 仍未开(r5 时即声明未开,本轮**仍未开**)
   ⚠ 而本轮 :279 又一次引它(「PREREG_r89 §1 跑前写死」)⇒ 该引用**我第二次未核**,登记在此
⛔ FINDINGS F81 / r88 / r88b 原件 —— 只据被审件转述判断,未开原件
⛔ §4 / §6 / §7 全部
```

## 八、放行条件

```
1. MAJOR-1 + MAJOR-2 合并一次改动:两句各带上 T_OBS 坐标 + 把「界」降为「实测量级」
2. 四串全族反扫(有界 / ≤0.395 / 0/6 格达整格 0.50 / ±0.40),**扫到句**,报命中数
3. ⚠ 结论级的那一格实验(`0.5/0` 臂 O @T_OBS=24 s)⛔ **不是放行条件** ——
   它归 lead 路由;放行只要求"那个数带上它的坐标",不要求"那个数收口"
```
