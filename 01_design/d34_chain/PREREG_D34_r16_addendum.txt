================================================================================
PREREG_D34_r16_addendum  ——  D3/D4 设计件 MAJOR-2/3/4/5 + 7 条 MINOR 整改
                              以及【批量理由审计】(lead 2026-08-05 派)
================================================================================
作者    : channel-dsp(第 2 实例)
落盘时间: 2026-08-05(⛔ 在 r16 任何跑批之前)
门禁状态: 未过门
⛔ r1–r13 的预注册原件与结果文件一字未改(E-2 / D6-aj)。

--------------------------------------------------------------------------------
§0  本轮的两件事,以及它们为什么必须先写下来
--------------------------------------------------------------------------------
(一) critic `critic_D3D4_CHAIN_v0.1_verdict_20260804.md` 的 MAJOR-2/3/4/5 + m-1…m-7。
(二) lead 派的【批量理由审计】:把我自己在 EXP-10b 上证出来的那条判别法,
     回头用在开给我的这份 verdict 上 ——
       「一句理由盖了多条 finding 时,逐条问:这条理由对它成立吗?」
     ⇒ ⛔ 不问,就会用【多数的正确性替少数背书】。

⭐ 而 (二) 有一个我必须先声明的利益冲突:
   我在上一轮**已经按 erratum2 的批量判断撤回了设计件里的 87.72 dB**。
   ⇒ 若本轮测出它不是网格伪影,我就得**撤回我自己的撤回**。
   ⇒ ∴ 判据与证伪条件必须写在跑之前(就是这份件),⛔ 不许跑完再定。

--------------------------------------------------------------------------------
§1  批量理由审计 —— 四处「一句理由盖多条」
--------------------------------------------------------------------------------
[B-1] erratum2:「87.72 dB(被审件 EXP-3p)与我 verdict 里的 56.32 dB 同族,
      **都是网格伪影**」。
      ⇒ critic 对 65.86 做了网格密度扫描(实测),而对 87.72 与 56.32 **是按同族推断的**。
      做法    : 用被审件自己的 EXP-3p 算例(LR4 @2000 Hz,理想系数,LP−HP),
                只改 np.linspace 点数 {20001, 50001, 200001, 500001, 2000001},记 max|偏离|。
      预期    : 单调增大、不收敛 ⇒ 同族成立 ⇒ 我上一轮的撤回站得住。
      证伪条件: 若五个网格密度上该值落在同一个 ±1 dB 带内 ⇒ 它有极限值
                ⇒ **它不是网格伪影** ⇒ erratum2 的批量判断对这一条不成立
                ⇒ ⛔ 我必须撤回我在设计件 §9(C) 里做的那次撤回,并报 lead + critic。
      ⚠ 附带独立判据(⛔ 不依赖网格):在理想系数下直接算 fc 处的 |LP−HP|。
                若它是精确 0(到 float64 底),则"零点存在"成立,深度必为 −∞。

[B-2] BLOCKER-1:「§0 承重表里带 ⭐ 的三行,**全部**只存在于未被列出的那批文件里」。
      ⇒ 一句理由盖了三条(f_lo* = 105.2 / 8.02× / 拒绝率 0 段)。
      做法    : 对三条各自的 EXP 标识,在 results_d34_r2…r7 里逐个 grep。
      预期    : 三条在 r7 及更早**全部零命中**。
      证伪条件: 任一条在 r7 里能找到 ⇒ 该理由对它不成立 ⇒ BLOCKER-1 的作用域须收窄,
                而我上一轮的整改留痕里那句「全部只产自 r10–r12」须改。

[B-3] BLOCKER-1 修法②:「单列一段说明 r8–r10 出现过的 **8 条** FAIL 各自的去向」。
      做法    : 从 results_d34_r8/r9/r10 逐轮取 [FAIL] 行的标识,求并集。
      预期    : 并集 = 8 条。
      证伪条件: 并集 ≠ 8 ⇒ critic 的计数有误 ⇒ 我按**盘面实有**写,并在整改留痕里
                如实标明与 verdict 的差,⛔ 不凑数、⛔ 不静默改。

[B-4] 放行条件 8:「m-1 / m-2 / m-3 是**同一族**的口径标注,合并一次做」。
      做法    : 逐条问「它是不是口径标注问题」。
      预期    : m-1(逐频相加 vs 各最大值)与 m-2(评价频带上沿未标)是;
                m-3(3.744 vs 3.742)的成因是**网格分辨率**,不是口径。
      证伪条件: 若 m-3 的两个值能用口径差解释(即换口径能复现 0.002 ms 的差)
                ⇒ 我的拆分错,三条确属同族,按 critic 的合并做法办。

--------------------------------------------------------------------------------
§2  MAJOR-2  「31.14 dB 是差值不是余量」——独立重算最大节点电平
--------------------------------------------------------------------------------
  做法    : 独立实现 RBJ peaking + LR 高通(⛔ 不 import d34_analysis),
            对下列配置算【逐节最大节点电平】,与 Q4.27 的链内余量 24.0824 dB 摆一起:
              ① EXP-1 自己的工作点(−20 dBFS,LR4 HP @120,1 段 +12 dB @40 Hz)
              ② 同 ① 但满刻度输入(0 dBFS)
              ③ 满刻度 + 1 段 +15 dB @40 Hz
              ④ 满刻度 + 2 段 +15 dB @40 Hz(同频叠加)
              ⑤ 满刻度 + 3 段 +15 dB @40 Hz
  预期    : ① 两种顺序都不越 24.0824 dB;④ 起「PEQ 在前」越界而「分频在前」不越。
  证伪条件: 若在 §3⑥ 自己给的 ±15 dB 量程与 §4③ 的合法档位内
            **找不到任何**使「PEQ 在前」越界的配置
            ⇒ 「分频必须在 PEQ 之前」这条结论的**定点电平理由整条不成立**
            ⇒ ⛔ 不许换个数继续说同一句话,须把 §2③ 的结论句一并重写或删除。
  ⚠ 判据类别声明(LESSONS B-3):本表报的是【电平 vs 余量】,是余量类;
    31.14 dB 是【两臂之差】,是差值类。⛔ 两者不得互相替代,也不得相除。

--------------------------------------------------------------------------------
§3  MAJOR-3  噪声底模型缺级间增益 ——【把它做成会响的闸门,不是一句注意事项】
--------------------------------------------------------------------------------
  背景  : §6 与 EXP-5 现在的处置是「打了限定 + 在注释里指向 check_noise_chain_r5.py」。
          ⇒ 而最坏合法配置突破 PRD 这件事,**目前只是一行输出,不是一个闸门**
            (⭐ 我自己的纪律:闸门必须是【中止条件】)。
  做法  : EXP-5 增加两条真检查:
            EXP-5b  含级间增益的模型,在【各节增益 = 1】时必须逐位回到直接相加的 −158.88
                    ⇒ 这是"模型没写错"的护栏,可被变异杀死。
            EXP-5c  §6.2-4 明文允许的最坏合法配置(8 段全 +15 dB 同频)下的链末噪声底
                    ≤ −106 dBFS(PRD)。
  预期  : EXP-5b PASS;**EXP-5c FAIL**(实算约 −91.6 dBFS)。
  ⛔⛔ 预先写死的处置(⛔ 这一条是本件最要紧的一句):
        EXP-5c 若 FAIL,**它就以 FAIL 的身份留在结果里**。
        ⛔ 不退役、⛔ 不改判据、⛔ 不加"待路由"就当它不存在 ——
        增益结构的处置权在 architect/D2/CTO 手里,而**那不改变本轮它是 FAIL 这个事实**。
        ⇒ 这正是我在 EXP-10b 上刚裁过的那件事的反向:上次是【该响的没响】,
          这次先把它写成【必须响】,免得下一个实例用"已路由"把它洗掉。
  证伪条件: 若 EXP-5c 实测 ≤ −106(即最坏合法配置也不突破)
            ⇒ MAJOR-3 的严重性判错 ⇒ §6 那一行的 ⛔ 与 §0 第 67 行的"突破 PRD"须撤回。
  ⚠ 两轨:本模型与 `check_noise_chain_r5.py` 是**两个独立实现**(后者不 import 前者),
    两者在配置 ①④ 上的数须相符;不符则两边都不得报出。

--------------------------------------------------------------------------------
§4  MAJOR-4  「阶数 mod 4」的单位 —— 设计件侧的机械断言
--------------------------------------------------------------------------------
  ⚠ C 侧已有该断言(`check_modules.c` CHK-X6/X7 一族,r8 起),
    ⇒ 本轮补的是【设计件这一侧】,因为两侧现在不自洽(见整改留痕)。
  做法    : 在 d34_analysis.py 增 EXP-3d:对 xo_slope ∈ {12,24,36,48},
            按 N = xo_slope ÷ 6 求极性,再**用求和偏离实测反证**该极性是对的那一个
            (⛔ 不是自比:极性由规则给,平坦度由传函算,两者独立)。
  预期    : 12 ⇒ 反相、24 ⇒ 同相、36 ⇒ 反相、48 ⇒ 同相;
            且每档"规则给的极性"求和偏离 ≤ 0.05 dB、"另一个极性"必须出现零点。
  证伪条件: 任一档规则给的极性反而不平坦 ⇒ 规则错(而不是表述错),须整条重议。

--------------------------------------------------------------------------------
§5  MAJOR-5 / m-1…m-7  —— 纯记录对齐,⛔ 不重跑、⛔ 不改数
--------------------------------------------------------------------------------
  MAJOR-5 : §9 补齐 r8–r10 全部 FAIL 的去向(条数以 B-3 的盘面并集为准)。
  m-1/m-2 : 口径标注(逐频相加 vs 各最大值;评价频带上沿 8 kHz)。
  m-3     : 3.744 vs 3.742 —— 按 B-4 的结论定性后统一到一个网格并注明。
  m-4     : §6 第 4 行「0.003859 dB」补出处(产自前置件 results_fp_r3.txt)。
  m-5     : §0 第 50 行补工作点(⚠ 该行的依据本轮已被 MAJOR-2 换掉
            ⇒ m-5 的要求**转移到新依据上**:新依据同样必须自带工作点)。
  m-6     : 新增 `d34_chain/ERRATA_r1.txt`(⛔ 不改 PREREG_r2_addendum 原件)。
  m-7     : 47.9844/48 = 0.999675 ms ⇒ 文档写 0.99967 是**截断**。
            ⚠ 而 `PREREG_D34_r1.txt:90` 当时写的就是 **0.99968**(正确)
            ⇒ 这是一条【从预注册到设计件反而变坏】的漂移,须全库反扫。
================================================================================
