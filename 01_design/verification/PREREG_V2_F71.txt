未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 16:2x(verification 第 1 实例)
轮次 = V2   标的 = `F71`(FINDINGS.md:2626-2680)⇒ W1P 收口件 §4 天花板论证的**全部依据**
缘由 = 独立 critic:「r76 与 F71 是本件最承重的两处,⛔ 本 verdict 不为其原始数据背书。」

⛔ 本文件在复算任何 F71 数值【之前】写下。§1–§5 落盘后一字不改(LESSONS E-2)。
⇒ 前一轮 V1(r76)已出件并报 lead;本轮独立。

═══════════════════════════════════════════════════════════════════
0. 我在写本件之前已经看到了什么(D6-q 留痕,必须诚实)
═══════════════════════════════════════════════════════════════════
✅ F71 全文(= 被复核对象),含它报的三张数表
✅ **溯源动作**:为找 F71 的原始件,我 grep 了全目录并打开了 `r79_cell_N08_T0.{2,5}_s012.json`
   的 `dO` 字段 ⇒ 已确认 F71「臂O-静态@N08 实测」列 = **r79 的 dO**(5.5/3.5/5.0/2.0/4.5/3.5)
   ⇒ 这一列我**已经看过**,故本轮不把它算作"盲复算";它只作为**对表基准**。
⛔ **尚未计算/未看**:峰-均比、临界点数、top1−top9 —— 这三样是本轮真正要盲复算的量。
⭐ **本轮开工前先执行 adaptive-dsp memory 第④条**(设计实验前先查结论库):
   已 grep `件:` 行 ⇒ F69/F70/F72/F73/F74/F75/F76 **都有**配套件行,
   **F71 没有** ⇒ 这本身是本轮第一条待报项(见 V2-A)。

═══════════════════════════════════════════════════════════════════
1. 被复核的主张(F71 原文 + 收口件 §4 转述)
═══════════════════════════════════════════════════════════════════
Q-1 峰-均比(max − mean,**120–7800 Hz**)六条 = 9.70 / 10.92 / 10.59 / 10.14 / 11.58 / 10.18
    中位 10.39;dB 域 σ 六条 5.33–5.71,均值 5.58
Q-2 临界点数 = 234 / 233 / 223(T60=0.2) / 465 / 475 / 454(T60=0.5)
Q-3 top1 − top9 = 2.11 / 3.15 / 2.49 / 1.97 / 2.94 / 1.92
Q-4 臂O-静态@N08 实测 = 5.50 / 3.50 / 5.00 / 2.00 / 4.50 / 3.50   ← 已确认 = r79 dO
Q-5 |假说A(峰-均比)误差| **中位 6.88 dB**;|假说B(序统计量)误差| **中位 1.57 dB**
Q-6 台架实测 `top1−top(NN+1)`:NN=8 ⇒ 1.92–3.15;16 ⇒ 2.51–4.18;24 ⇒ 3.21–5.26
Q-7 结论:ΔMSG ≈ `top1 − top(NN+1)`(序统计量),**不是**峰-均比;
    ⇒ 饱和不是实现缺陷,是这个量的数学形状

═══════════════════════════════════════════════════════════════════
2. 检查项与证伪条件 —— 每条附双向代入自查
═══════════════════════════════════════════════════════════════════

V2-A **配套件可溯性**。F71 的 FINDINGS 条目有无 `件:` 行?其数是否能定位到某个落盘产物?
     证伪:承重结论(收口件 §4 全部依据)**无任何可溯原始件** ⇒ 报为可复核性缺陷
           (= critic 在 D3/D4 开的 BLOCKER-1 同型)。
     双向:若有 `件:` 行且文件在盘 ⇒ 不触发 ✓;若无 ⇒ 触发 ✓

V2-B **Q-1/Q-2 复算**(峰-均比与临界点数)。用 `clrig.critical_points` 在 **120–7800 Hz** 上
     取临界点幅度 `mdb`,算 `max − mean` 与 `len`。
     ⚠ 输入 `h` 有四种可能口径,F71 没写是哪种 ⇒ **四种都算,报哪一种复现**:
       (a) `h0` 原始 RIR   (b) `band_limit(h0,8000)`   (c) `h_eff(h0)`   (d) `h_eff(band_limit(h0,8000))`
     证伪:**四种口径无一能复现** Q-1/Q-2(逐格 |差|>0.05 dB 或点数不等)
           ⇒ F71 的数无法从现有件复算 ⇒ 报为不可复现。
     双向:若某一口径六格全符 ⇒ 不触发 ✓;若四种都差 >0.05 ⇒ 触发 ✓

V2-C ⭐⭐ **Q-3 的构造:`top` 到底怎么取**(lead 点名要核的那条)。
     候选构造(全算,报哪一种复现 2.11/3.15/2.49/1.97/2.94/1.92):
       (i)  **裸序统计量**:`mdb` 降序排序,top1 − top9(**不去重**)
       (ii) **去重(排除窗)**:`pick_excl(he, bw, 9)` 取第 1 与第 9 个 pick 的幅度差
            —— 这是**物理上正确**的量:陷掉 8 个峰后接管的是【不在任何陷波带内】的最高点
       (iii) 同 (ii) 但 `bw_oct = 1/8`(= r79 N08 的等预算带宽)
     证伪①:若复现的是 (i) ⇒ **该量不是「第 NN+1 个峰」而是「第 NN+1 个临界点」**,
             两者在 223–475 个临界点、8 个陷波带的场景下**不是同一个东西** ⇒ 构造与命名不符。
     证伪②:若 (i) 与 (ii) 的差 > 1.0 dB(中位)⇒ 该选择**承重**,必须在件里写明。
     双向:若 (i)≈(ii) ⇒ 不触发 ✓;若 (i)=2.1 而 (ii)=5.5 ⇒ 差 3.4 > 1.0 ⇒ 触发 ✓

V2-D ⭐ **判别的工作点是否与它服务的表同口径**(LESSONS B-3 量的类别 / B-4 取值范围)。
     收口件 §4 把 1.57 / 6.88 写在 §2 头条表(`bw_oct = 1/5`)之下;
     而 Q-4 的实测列来自 **r79 NN=8**,其 `bw_oct` = ?(本轮核实)。
     证伪:若两者 `bw_oct` 不同,而 §4 未声明 ⇒ **判别用的工作点 ≠ 它所服务的表的工作点**,
           须报出,并**用 §2 自己的臂O 列(bw=1/5)重做一次判别**,两组数并列。
     双向:若 r79 N08 也是 1/5 ⇒ 不触发 ✓;若是 1/8 ⇒ 触发 ✓

V2-E ⭐⭐ **频带口径:接管的那个峰是否落在 120–7800 之外**。
     V1 已实测:臂 O 在 (0.5,0)/(0.5,1) 上 `f_trig` = **52 / 22 Hz**,
     且解析 MSG 的全带值与带内值在这两条上相差 1.49 / 0.28 dB
     ⇒ **陷掉前 8 峰后接管的临界点可能在 120 Hz 以下** ⇒ 而 Q-3 的序统计量只在 120–7800 内取。
     动作:逐格算「8 个神谕陷波后,全带上真正接管的临界点频率与幅度」,看它是否在 120–7800 内。
     证伪:若 ≥1/6 条种子上接管点落在 120–7800 **之外** ⇒ **序统计量的取值范围漏掉了实际接管者**
           ⇒ 假说 B 的构造在该条上不成立(不是"误差大",是"算的不是那个点")。
     双向:若六条接管点全在带内 ⇒ 不触发 ✓;若有一条在 52 Hz ⇒ 触发 ✓

V2-F **Q-5 复算**。用复算出的 Q-1/Q-3 与 Q-4,重算两个误差中位。
     证伪:与 6.88 / 1.57 不符(|差| > 0.01)⇒ 报出。
     双向:若逐格相同 ⇒ 中位相同 ⇒ 不触发 ✓;若某格输入错 ⇒ 中位变 ⇒ 触发 ✓

V2-G ⭐ **第二条独立路径:解析真值**(铁律七)。臂 O 是 LTI ⇒
     「完美陷掉前 NN 个峰后的 ΔMSG」有**解析真值** = `msg(神谕NN陷波) − msg(无陷波)`(V1 已建此路径)。
     ⇒ 用它作第三个对照量,与假说 A / 假说 B 一并比。
     证伪:若解析真值与假说 B 的差(中位)> 假说 B 与实测的差(中位)
           ⇒ **假说 B 拟合的不是"完美陷波的极限",而是别的东西** ⇒ Q-7 的因果叙述须修。
     双向:若解析真值 ≈ 假说 B ⇒ 不触发 ✓;若解析 6.0 而假说 B 2.1 ⇒ 差 3.9 ⇒ 触发 ✓

V2-H **Q-6 的三档数**(NN=8/16/24 ⇒ 1.92–3.15 / 2.51–4.18 / 3.21–5.26)。
     用 V2-C 复现出的同一构造复算三档。
     证伪:任一档区间与 Q-6 不符 ⇒ 报出。
     双向:区间相同 ⇒ 不触发 ✓;若 NN=24 实为 4.5–7.0 ⇒ 触发 ✓

═══════════════════════════════════════════════════════════════════
3. 我预期会发生什么(写死,跑完不改)
═══════════════════════════════════════════════════════════════════
- V2-A:**预期触发**(已核 F71 无 `件:` 行)。
- V2-B:预期能在四种口径中的**某一种**上复现;⛔ 不预测是哪一种。
- V2-C:⛔ **不预测**。这是本轮的要害。
- V2-D:⛔ 不预测。
- V2-E:**预期触发** —— V1 实测臂 O 的 f_trig 有 22 / 52 Hz 两条。
  ⚠ 但 f_trig 是**时域起振频率**,与**临界点接管**不是同一个量 ⇒ 须实际算,不得据 V1 直接下结论。
- V2-F:预期与 6.88 / 1.57 相符(它只是对已报三列做算术)。
- V2-G / V2-H:⛔ 不预测。

═══════════════════════════════════════════════════════════════════
4. 纪律
═══════════════════════════════════════════════════════════════════
⛔ 复核脚本不 import F71/收口件的判读层;用到 clrig/msg_meter/r61 处显式声明其为被测件。
⛔ 每个数挂 L 标;⛔ 不编;⛔ 不 commit / 不 spawn / 不联系其他 teammate(只报 lead)。
⛔ 结论只能在看到数之后写。
⛔ 我是复核者:对不上如实报,不替任何人圆。
