未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 15:4x(verification 第 1 实例)
轮次 = V1(verification 复核第 1 轮)
标的 = `r76` 的原始数据 ⇒ W1P 收口件 `01_design/W1P_CLOSEOUT/01_NHS_numbers.md` §2 头条表
缘由 = 独立 critic 在 `critic_W1P_CLOSEOUT_verdict_20260804.md` 声明:
       「本件引用约 20 轮实验,我只审过 r87;r76(头条表原始出处)与 F71(§4 天花板论证)
         ⛔ 本 verdict 不为这两处的原始数据背书。」
⇒ 本轮 = **回到原始 json / out 文件**独立复算,并核那些数【是怎么得出的】。

⛔ 本文件在看任何 r76 数值【之前】写下。§1–§6 落盘后一字不改(LESSONS E-2)。
⛔ 我是复核者:对不上如实报,不替任何人圆。全部 [L2/宿主仿真] 复核,⛔ 无 L1。

═══════════════════════════════════════════════════════════════════
0. 我已读过什么 / 我还没看什么(D6-q 式留痕)
═══════════════════════════════════════════════════════════════════
已读(全是**代码与文档**,不含 r76 的任何数值输出):
  CLAUDE.md / GOVERNANCE / LESSONS_W1P / CRITIC_HANDOFF_current / adaptive-dsp memory 顶部四条
  W1P_CLOSEOUT/01_NHS_numbers.md(⇒ 收口件报的数已知,它们是本轮的**被复核对象**)
  PREREG_r76.txt / r76_cell.py / msg_meter.py / howl_detect.py / clrig.py(make_F/h_eff/
  critical_points/Loop) / r61_bwoct_baseline.py(pick_excl/mk_oracle) / nhs.py(duck 相关行)
⛔ 尚未打开:`r76_cell_*.json` / `r76_cell_*_out.txt` / `r76_srclevel_full_out.txt` /
            `r76_fixedG*` / FINDINGS.md 的 F71/F76/F77/F79 段

═══════════════════════════════════════════════════════════════════
1. 被复核的具体主张(收口件原文,逐条抄录 = 我的"预期复算得到什么")
═══════════════════════════════════════════════════════════════════
出处声明:收口件 §2 写「出处:`r76_cell_s20f0`(主列 = `dA`、附列 = `dN`、臂 O = `dO`)」
工作点:src_rms = −20.0 dBFS / T_OBS = 12 s / bw_oct = 1/5 / NN = 8 / T_low = −45 / STEP = 0.5

P-1 逐格值(T60/seed ⇒ 主列 dA / 附列 dN / 臂O dO / 缺口 dO−dA / f_trig):
    0.2/0  ≥6.00 / 4.50 / 6.00 / 0.00 / 2559 Hz
    0.2/1  ≥2.00 / 6.50 / 6.00 / 4.00 / 1034 Hz
    0.2/2  ≥4.00 / 8.00 / 7.50 / 3.50 / 3111 Hz
    0.5/0  ≥2.50 / 3.00 / 3.00 / 0.50 / 2258 Hz
    0.5/1  ≥2.00 / 3.00 / 6.50 / 4.50 / 7580 Hz
    0.5/2  ≥2.50 / 1.50 / 4.50 / 2.00 /  716 Hz
P-2 中位:主列 ≥2.50 / 附列 3.75 / 臂O 6.00 / 缺口 2.75
P-3 计数:主列 ≥4 dB = 2/6、≥5 dB = 1/6;臂O ≥4 = 5/6、≥5 = 4/6
P-4 分层中位:主列 T60=0.2 层 4.00、0.5 层 2.50;臂O 0.2 层 6.00、0.5 层 4.50
P-5 分层计数(lead 派单转述):≥4 dB 在 0.2 层 2/3、0.5 层 0/3
P-6 仪器底 = 0.354 dB = `STEP/2×√2`
P-7 §2 脚注「主列由 r87 独立重跑,6/6 四项全符(m0 / ΔMSG / 挂陷数 / 终点 RMS 逐项相同)」
P-8 「≥」的理由:m0 臂在其读数点上仍在增长 ⇒ m0 被高估 ⇒ 差值被低估;
    量级「恰好一个扫描步长 0.50 dB(4/6 条可判,2/6 条本方法无分辨力)」

═══════════════════════════════════════════════════════════════════
2. 检查项与【证伪条件】—— 每条附双向代入自查
   (LESSONS C-4 / critic 2026-08-04 立:真有问题的值与真没问题的值各代一次,答案必须相反)
═══════════════════════════════════════════════════════════════════

V-A **逐格对表**。从 `r76_cell_s20f0.json` 取 T=12 的 6 行,**用原始 m 字段自己相减**
     (dA' = mA − m0,dN' = mN − m0,dO' = mO − m0),与 P-1 逐格比。
     证伪:任一格 |复算 − 收口件| > 0.001 dB ⇒ **FAIL,头条表与原始数据不符**。
     双向代入:若某格真为 5.00 而报 6.00 ⇒ |差|=1.00 > 0.001 ⇒ 触发 ✓
               若逐格相同             ⇒ |差|=0.00 ≤ 0.001 ⇒ 不触发 ✓  (答案相反 ✓)

V-B **统计量复算**。用 V-A 复算出的 6 个主列值重算 P-2/P-3/P-4/P-5(中位用
     `numpy.median`,偶数样本 = 中间两值平均)。
     证伪:任一统计量与收口件不符 ⇒ FAIL。
     双向代入:六值 [2,2,2.5,2.5,4,6] ⇒ 中位 2.50 ⇒ 与 P-2 符 ⇒ 不触发 ✓
               若真值为 [2,2,2.5,2.5,4,3] ⇒ 中位 2.50 但 ≥4 计数 1/6 ≠ 2/6 ⇒ 触发 ✓

V-C **双路一致(json ↔ out.txt)**。out.txt 是 print 出来的表,json 是同一次跑另写的;
     两者由**不同的格式化路径**产生 ⇒ 可互核。逐格比 m0/dN/dA/dO/挂陷/INV。
     证伪:任一格两路不一致 ⇒ FAIL(⇒ 至少一路不可信,不知哪一路)。
     双向:两路相同 ⇒ 不触发 ✓;人为改一个数 ⇒ 触发 ✓

V-D **量程与状态**。逐格核 `st_m0/st_N/st_Na/st_O` 四个 status 是否全 `ok`;
     核 m0 是否落在其扫描量程 [anchor−3, anchor+3.5] 的边界上;核 INV_N / INV_O 判定。
     证伪:任一格 status ≠ `ok`(即 `no_howl` / `howl_at_lo` / `skipped`)而该格仍被
           收口件计入统计,⇒ FAIL(该格的 ΔMSG 是量程截断值或缺失值,不得进头条表)。
     双向:status 全 `ok` ⇒ 不触发 ✓;若某臂 `no_howl`(m=nan)而表里有数 ⇒ 触发 ✓

V-E ⭐ **尺子自检:m0 与解析 anchor 的差**。`anchor` = `MSGMeter(h_eff).msg()['full']['msg_db']`
     = **无陷波时的解析稳定边界**;`m0` = 同一条件下**实测扫描**出的最后一个不起振 G。
     两者测同一个量、走完全不同的路径 ⇒ 逐格报 `m0 − anchor`(铁律七 第二独立工具)。
     ⚠ 本条**不预测方向**(有限 T_OBS 会让实测边界偏高,而带口径 in/full 会让它偏低)。
     证伪:|m0 − anchor| 中位 > 2.00 dB ⇒ **两条路径量级不符 ⇒ 台架尺子与解析模型对不上,
           r76 全表存疑**,须报出。
     双向:若 m0−anchor 逐格 ≈ +0.5 ⇒ 中位 0.5 ≤ 2.00 ⇒ 不触发 ✓
           若逐格 ≈ +4.0(实测边界比解析高 4 dB)⇒ 中位 4.0 > 2.00 ⇒ 触发 ✓

V-F ⭐⭐ **臂 O 的第二条独立路径**。臂 O 的陷波是**构造钉死的**(picks 8 点、depth −18、
     bw_oct 1/5、全程 HOLD、`T_low=999` 不新分配)⇒ 它的 ΔMSG **可以解析预测**:
       dO_analytic = msg(slots=神谕8陷波)['full'] − msg(slots=())['full']
     与实测 dO 逐格比。⇒ 这是对头条表**上界一列**的真正交叉核。
     证伪:|dO_analytic − dO_实测| 的**中位** > 1.00 dB ⇒ 两路径不一致 ⇒ 臂O 列存疑。
     双向:若逐格差 ≤0.5(一格)⇒ 中位 ≤0.5 ⇒ 不触发 ✓
           若臂O 实测被高估 3 dB ⇒ 中位 3.0 > 1.00 ⇒ 触发 ✓
     ⚠ 前置:若 nhs.py 的陷波系数在 r76 跑完后被改过(见 V-I),本条只能作**现状**核,
       须显式声明"用的是现在的系数,不是当时的" —— ⛔ 不得静默当作当时的复现。

V-G ⭐ **「≥」限定的对称性**(P-8)。收口件用「m0 臂在读数点上仍在增长」论证 ΔMSG 被低估。
     ⇒ 但**同一个有限观测窗偏置对陷波臂同样成立**。若陷波臂在其读数点上也在增长,
       偏置会部分抵消,「≥」的强度就不是收口件写的那样。
     动作:在同一 (T60,sd) 上,取 m0 与 mA 两个终点 G,各跑一次 12 s 闭环,
           量【末秒 RMS − 首秒 RMS】(与 F76.3/F77.4 同口径),两臂并排报。
     证伪:若陷波臂(Na)的增长量在 ≥4/6 条种子上 ≥ m0 臂的一半,
           ⇒ **「m0 被高估 ⇒ 差值被低估」这条推理缺了一半**,须报出(⛔ 不是说 ΔMSG 错,
             是说「≥」的论证不完整)。
     双向:若 Na 增长 ≈0 dB 而 m0 增长 ≈+2 dB ⇒ 0 < 1.0 ⇒ 不触发 ✓
           若两臂都 ≈+2 dB ⇒ 2 ≥ 1.0,6/6 条 ⇒ 触发 ✓

V-H **仪器底 0.354 的推导**(P-6)。①算术:STEP/2×√2 = ? ②它的**适用性**:
     两臂同锚同栅格 ⇒ 差值恒为 0.5 的整数倍 ⇒ 0.354 落在 0.00 与 0.50 之间。
     证伪:若存在任一被判"可判/不可判"的格,其判定在把门限从 0.354 换成
           (0, 0.5) 区间内**任意**其它值时会改变 ⇒ 说明 0.354 这个具体数值承重;
           若不存在 ⇒ 该数不承重,但**它的推导式仍需成立**。
     双向:门限取 0.1 与取 0.4,若所有格判定相同 ⇒ 不承重 ✓(不触发)
           若某格差值 = 0.25(非 0.5 整数倍)⇒ 门限一换判定就变 ⇒ 触发 ✓

V-I ⭐ **被测件的代码状态**(复用旧轮次件第①条:口径是否一致)。
     核 `nhs.py` / `clrig.py` / `msg_meter.py` / `howl_detect.py` / `r61_bwoct_baseline.py`
     的**当前哈希与 mtime** vs PREREG_r76 §头钉的哈希、vs r76 输出文件的 mtime。
     证伪:任一被测件的 mtime **晚于** r76 输出文件的 mtime ⇒ **现在的代码不是当时的代码**
           ⇒ ⛔ 任何"重跑复现"都不构成对 r76 的复现,须显式声明。
     双向:若全部被测件 mtime 早于 r76 输出 ⇒ 不触发 ✓
           若 nhs.py mtime 晚于 r76 输出 ⇒ 触发 ✓

V-J **duck 消融的实现**(主列的定义)。核 `a.duck_gain = lambda: 1.0` 是否真的切断
     音频作用;核 `g_duck_db` 是否还有**其它**读者(若某个判决分支读它,则消融不干净)。
     证伪:存在任一**非音频增益**的代码路径读 `g_duck_db` 并据以分支 ⇒ 消融不干净,
           主列不是"只有陷波"的臂。
     双向:只有 `duck_gain()` 读它 ⇒ 不触发 ✓;若 `_maybe_slot` 里有 `if self.g_duck_db<X` ⇒ 触发 ✓

V-K **终点 G 的判定**。核 `scan` 的 `last` 语义(= 最后一个不起振的 G)、
     `is_howling` 的三个常数(6/3 dB 双门 + 末 25% 保持)、以及**四臂是否用同一判据**。
     证伪:四臂的判据/量程/参考电平不完全同构 ⇒ 臂间差含判据差,不纯是陷波效应。
     双向:四臂同 `ref`、同 `is_howling`、同 STEP ⇒ 不触发 ✓
           若某臂用了不同的 ref 或不同的 hi ⇒ 触发 ✓(⚠ 已知 m0 臂量程 hi 不同,V-D 专门查)

V-L **m0 的取法**。核 m0 臂 = `proc=None`(严格线性、无 NHS),其扫描量程
     [anchor−3, anchor+4] 与其它臂 [anchor−1, anchor+20] 不同 ⇒ 核这个差异是否可能
     影响 m0(V-D 已查量程边界)、以及两臂栅格是否同相位(anchor−3 与 anchor−1 相差 2.0
     = STEP 的整数倍 ⇒ 应同栅格)。
     证伪:两臂栅格不同相位 ⇒ 差值会含一个固定的半格偏置。
     双向:(anchor−1)−(anchor−3) = 2.0 = 4×STEP ⇒ 同栅格 ⇒ 不触发 ✓
           若一臂从 anchor−0.75 起 ⇒ 触发 ✓

═══════════════════════════════════════════════════════════════════
3. 我预期会发生什么(写死,跑完不改)
═══════════════════════════════════════════════════════════════════
- V-A/V-B/V-C:**预期全符**。理由 = r87 已独立重跑过主列(P-7),且 critic 复算过 §2
  的**统计量**(verdict:「§2 头条表全部统计量我独立重算逐格相符」)。
  ⇒ ∴ 本轮若在 V-A/V-B 上发现不符,那才是**大事**;而更可能出问题的是 V-D…V-L。
- V-E:预期 m0 > anchor(有限 T_OBS ⇒ 实测边界偏乐观),量级 0.5–2 dB。
- V-F:⛔ **不预测**。这是本轮唯一一条我事先不知道答案的定量核。
- V-G:⛔ **不预测方向**。
- V-I:**预期触发**(nhs.py 的 mtime 目测晚于 r76)⇒ 那么本轮所有"重跑"都必须带这个声明。

═══════════════════════════════════════════════════════════════════
4. 纪律
═══════════════════════════════════════════════════════════════════
⛔ 复核脚本**不 import r76 的分析层**(不 import r76_merge / r76_cell 的判读函数);
   只读 json/txt 原始字段 + 自己重算。用到 nhs/clrig/msg_meter 处必须显式声明
   「这是被测件本身,不是独立工具」并说明为何仍构成第二条路径。
⛔ 每个数挂 L 标;不确定标 [L4/待验证];⛔ 不编。
⛔ 不 commit;不 spawn;不联系其他 teammate(只报 lead)。
⛔ 结论只能在看到数之后写。
