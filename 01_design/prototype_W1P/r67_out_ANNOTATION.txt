未经 critic 评审 —— r67 的【判读件】(一版一文件,不原地改 _out)。[L2/宿主仿真]
第 5 实例,2026-08-03。数据源:r67_rrise_sweep_out.txt(96 行)+ r67_out_ANNOTATION_bottleneck.txt

═══════════════════════════════════════════════════════════════════════
零、先自曝第三个探针缺陷:`⛔m0≠r64` 是**假警报**,He4 对照【恒 FAIL】
═══════════════════════════════════════════════════════════════════════
我把 r64 的**打印值**(2 位小数)硬编码进 `R64_M0`,却用 `1e-9` 容差比。
实测 `anchor(0.5/sd1) = −13.750896`,表里写 `−13.75` ⇒ 差 **8.96e-4 ≫ 1e-9**
⇒ **该对照在任何情况下都会 FAIL** = LESSONS C-3「必然失败的断言不是对照」。
**⇒ 正确读法:逐行比对 2 位小数,m0 在 48/48 行上与 r64 完全一致 ⇒ He4 实际【通过】。**
⇒ 本 session 第三个探针缺陷(前两个:1e9 哨兵污染 / `id()` 作键),已并入 F45 与 D6-ac。

═══════════════════════════════════════════════════════════════════════
一、阶段 A(可达性)—— R_RISE 是死门,**双向证据齐了**
═══════════════════════════════════════════════════════════════════════
```
R_RISE = 18.0(现值)  rapid_onset 触发 = **0**     ⇒ 门控:不进阶段 B
R_RISE = 10.0         触发 = 18   |  R_RISE = 6.0  触发 = 18  |  R_RISE = 3.0  触发 = **165**
```
⇒ **He1 成立**:绑定约束确实是 `R_RISE`,**不是** `N_RISE` 窗口(那是我的证伪分支,未触发)。
⇒ 比 `r66c` 强一档:那里只有阴性(现值下从不触发),这里有了**干预确实生效**的阳性对照(C5)。
⚠ 10.0 与 6.0 触发数相同(18)⇒ 6–10 dB 之间无新事件 ⇒ **不是连续可调**,是一小簇固定事件。

═══════════════════════════════════════════════════════════════════════
二、⭐⭐ 阶段 B(ΔMSG)—— **48/48 行,四个档位逐格【完全相同】**
═══════════════════════════════════════════════════════════════════════
`ΔMSG_实测@带限8k_NHS自选_有duck_R{18|10|6|3}_[L2]` 与 `_duck消融_` 两列、
`top1_hit` / `hit@BW/2` / `cov` / 挂陷数 / 不变量 —— **在 R_RISE = 18/10/6/3 四档上一格未变。**
⇒ **δ(T) ≡ 0.00,六条种子 × 两档 T_OBS,无一例外。**

**⚠ 先按 D6-ab 自查"方向合不合理"**:参数明明改变了行为(阶段 A 触发 0→165),
结果却逐格相同 —— 会不会是 R_RISE 在阶段 B 没被应用?
**⇒ 排除**:阶段 A 与阶段 B **调用同一个 `mk()`**,而阶段 A 明确显示了差异 ⇒ 参数确实生效。

═══════════════════════════════════════════════════════════════════════
三、瓶颈定位(直接观测,固定 G = anchor+3;`r67_out_ANNOTATION_bottleneck.txt`)
═══════════════════════════════════════════════════════════════════════
```
T60/sd  R_RISE  入选式成立   N5_howl(过PHPR否决)  N4_born  n_carried  挂陷
0.2/0    18→3     31 → 43        21 → 23            5→5      5→5      5→5
0.2/2    18→3      6 → 12        23 → **12**        4→**2**  3→**1**  3→**1**
0.5/2    18→3     19 → 49        41 → 43            4→4      4→4      1→1
```
> **⇒ `入选式成立` 涨了最多 +158%,而 `N5_howl` 几乎不动(21→23 / 41→43),
>   `N4_born` / `n_carried` / 挂陷数 **全部不变或【变差】**。**
> **⇒ 瓶颈不在判据组合,在【GROWTH 入选 → 真分类 → 入槽】这一段的下游。**
> **⇒ 而 `0.2/sd2` 上 R_RISE 下调把挂陷数从 3 打到 1、分类数从 23 打到 12
>   —— 这是 He2 的【强形式】:触发变多,检出【变差】。**
  (机理未查清:多出来的低质量轨在 NT=12 的轨表里挤掉了真轨,还是别的?**本轮不猜,列下一轮。**)

═══════════════════════════════════════════════════════════════════════
四、预注册逐条结算(含对我不利的)
═══════════════════════════════════════════════════════════════════════
He1 下调 R_RISE 会触发:**成立**(0 → 18 → 18 → 165)。
He2 触发变多 ≠ 检出变好:**成立,且是强形式** —— ΔMSG 全档不变,一条种子上分类/挂陷反而下降。
He3 若 ΔMSG 升须在选点轴看到机制:**空条件**(无档上升)⇒ 未被检验,⛔ 不记为通过。
He4 m0 复现:**通过**(48/48 行 2 位小数一致);⚠ 脚本内的判定是假警报,见 §零。

═══════════════════════════════════════════════════════════════════════
五、结论(按 PREREG_r66 Hs3 的预写分类)
═══════════════════════════════════════════════════════════════════════
> **落在 Hs3(a):选点【没变】(`top1_hit`/`hit`/`cov` 四档逐格相同)
>  ⇒ 该判据缺陷【不是 ΔMSG 低的成因】。**
**⇒ `R_RISE = 18` 确实是死门(F42/F43/本轮三重坐实),但【修它不改善 ΔMSG】。**
**⇒ 两件事都要说,缺一即误导:**
  ① 它**是**一个真实的、已记录三天未执行的缺陷(欠账,该修);
  ② 它**不是**「NHS 只有 1.0–2.5 dB」的成因(本轮实测)。
**⛔ 按 PREREG 明令,禁止把本结果解释成「改得还不够/再调调阈值」——
  四档已扫到 3.0(PAPR 域 p95=2.64 之上),再低就是让噪声过门。**

**⇒ 下一轮方向(证据支持,非猜测)**:瓶颈在 **GROWTH 入选 → 分类 → 入槽** 这一段
(PHPR 否决 / 轨表容量 NT=12 / `_allocate` 仲裁),**不在候选门与判据组合**。
⚠ 这也意味着 lead 排的 ②(_papr_jaes)与 ③(_pnpr_jaes)**同属"候选门与判据层"**,
**很可能同样落在 Hs3(a)** —— 建议在开跑前先用本轮同样的**两阶段可达性门控**筛一遍。

⛔ 未经 critic 评审。
