未经 critic 评审 —— r66c 的【判读件】(一版一文件,不原地改 _out)。[L2/宿主仿真]
写件人:adaptive-dsp 第 5 实例,2026-08-03。数据源:r66c_papr_role_out.txt

═══════════════════════════════════════════════════════════════════════
⛔ 零、先自曝一个【我自己造的伪影】—— `noPAPR过门 / R倍` 两列**不可用**
═══════════════════════════════════════════════════════════════════════
`NoPAPR._papr` 恒返 `1e9` 来"摘掉门"。**但 `nhs.py:418` 会把这个值写进
`obs[k]['papr']` ⇒ `:488/:537` 进 `tr.papr_hist`** ⇒ 下游三处被污染:
`_imsd`(在 papr_hist 上做 LS 斜率)、PERSIST 门(`papr_hist[-1] >= T_papr_high` 恒真)、
臂2 快升签名。⇒ **该臂的闭环动力学已被我的探针改变,不是"只摘了门"。**
> **⇒ 表里 `R = 0.32`(摘掉一个门反而过门更少)正是这个伪影,⛔ 不得引用。**
> **⇒ 按 D6-j:读起来不合理的结果,先按操作事故处理,不先解释成物理现象。** 本条即是。
**⇒ 要做真正的 (乙) 臂,须改成"只在门的比较处跳过 PAPR 项,仍把真值写进 `obs`"。本轮未做。**
**⇒ 下面的结论**全部只用**未被污染的 base 臂静态交叉表**(在 base 自己的谱上统计),
  它不依赖那个坏臂。

═══════════════════════════════════════════════════════════════════════
一、答 lead 的 甲/乙/丙 —— 建议 **(丙),但把顺序倒过来:③ 先于 ②**
═══════════════════════════════════════════════════════════════════════
候选门交叉表(6 条种子 × 3 个增益,**门前候选求值 104281 次**,base 臂):
```
两者都过(现行过门)      4002
仅 PNPR 过(= 移出 PAPR 会【新增】的)   34460
仅 PAPR 过(= PNPR 单独否掉的)             58
都不过                                   65761
```
**(1) 移出 PAPR ⇒ 过门数 4002 → 38462 = ×9.61**(静态口径)。轨表只有 `NT = 12`
⇒ **(乙) 单独做不安全**:9.6 倍候选灌进 12 格轨表,正是 W1-B MAJOR-5「僵尸轨占满」的形态。
⇒ **lead 的顾虑成立,而且有了数:9.61×。**

**(2) ⭐ 但交叉表掉出一个更重的东西:`仅 PNPR 否` 只有 58,`仅 PAPR 否` 有 34460 ——
    不对称 594 倍。**
> **⇒ 在【已有 PAPR】的条件下,我方的 PNPR 几乎不否任何东西。**
> **⇒ 我们的候选门实际上是【PAPR 单门】,PNPR 近乎惰性。**

**(3) 而这正好接上 ③**:我方 PNPR 之所以近乎惰性,与 ③ 指出的缺陷同源 ——
它是**对一个宽带(下限 187 Hz,5 kHz 处 ±1300 Hz)求均值的单一比值**,
比原文的**逐邻元(±2,±3,±4 bin = ±62.5 Hz)AND** 宽 3–20 倍 ⇒ **门松得多**。
> **⇒ 结论链:③ 修好 PNPR ⇒ PNPR 重新承担否决 ⇒ 此时移出 PAPR 才有意义。**
> **⇒ 故建议顺序改为:③ → ②(甲)→ (乙) 作为 ③ 之后的条件臂。**
> **⇒ 若照原顺序先做 ②,我们会在一个【PNPR 近乎惰性】的链上调 PAPR 的门,
>   等于在单门系统里调那唯一的门 —— 结果会好看,但它掩盖了 PNPR 失效这件事。**

⚠ **本台架的适用边界(必须同时说)**:激励是**平稳白噪**,**没有语音掩蔽**。
而我方已记录 PAPR 的已知失效面正是掩蔽(全带统计被带外语音抬高 ⇒ 弱峰 PAPR 塌陷,
CHECK F 实测 +10.9 dB < 门 15)。**⇒ 本台架会【系统性低估】PAPR 的危害。**
⇒ 交叉表说的是"PAPR 在无掩蔽白噪下做了 89.6% 的否决工作",**不是**"PAPR 是个好判据"。

═══════════════════════════════════════════════════════════════════════
二、答 lead 的新问:`rapid_onset` 是 **A(信号没有)**,不是 B(门够不到)
═══════════════════════════════════════════════════════════════════════
D1 逐合取项扫(1692 次可参与判定的 (轨,槽) 求值):
```
合取项(i) 跃升 max(h[j]−h[i]), j−i≤2 :最大 10.44 / p95 2.64 / 中位 0.19 dB   门 = 18.0  ⇒ 达门 **0/1692**
合取项(ii) 平台 std(h[j:])            :最小 0.00 / 中位 0.36 dB              门 = ≤2.0  ⇒ 达门 **1435/1692**
两项同时成立                                                                              **0**
```
> **⇒ 绑定约束是【跃升项】,不是平台项。**
> **⛔⛔ 这【推翻了我自己在 `r66b_out_ANNOTATION.txt` 与 FINDINGS F42.3 写下的解释】** ——
> 我当时写「本台架环内无限幅器 ⇒ 不封顶 ⇒ **更无平台**」。**实测:平台项 1435/1692 轻松达标。**
> **⇒ 我那半句是错的,已在 F42.3 就地标注撤回并指向本件(D6-u:撤回要落到被撤回的位置)。**
> **⇒ 教训(feedback 类):我当时手上那个解释【听起来完全合理,而且我刚用它说服了 lead】。
>   它对了一半、错了一半,而错的那一半只有靠这次观测才看得见。**

**判为 A 的理由(不是"没达标就是 A")**:最大跃升 **10.44 dB** 对门 18 dB —— **同量级,差 1.7 倍**,
不是差一个数量级。⇒ 该门**在原理上够得到**,只是本台架的**指数渐进起振**产生不了那么陡的跃升。
⇒ **`rapid_onset` 不是死代码,是【本台架激励类别打不到的分支】** ⇒ 保留、默认关的处置正确。
⇒ 若日后加**增益阶跃**或**环内限幅器**,该分支预期会被触发(阶跃可轻易造出 >18 dB/2 槽)。

═══════════════════════════════════════════════════════════════════════
三、本件自身的已知缺陷(不藏)
═══════════════════════════════════════════════════════════════════════
1. `满/阻塞` 列把 `table_full` 与 `n_blocked` 相加,而 `table_full` 在噪声谱下**恒等于槽数**
   (375/跑,因为局部极大数恒 >16)⇒ **该列无信息量,是我列错了**。
   真正该看的 `n_blocked` 未单列。⇒ 下轮改。
2. (乙) 的闭环臂无效(§零)。
3. 全部结论限于:**白噪激励、无掩蔽、`f_cut=8k`、`T_OBS=6s`、三个增益点**。

⛔ 未经 critic 评审。
