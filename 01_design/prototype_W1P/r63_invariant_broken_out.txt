r63 · 运行时不变量的 broken 版(critic 点名项)
deps: nhs.py@706b658842d84316 clrig.py@8ad47ce8d260dd18   [L2/宿主仿真]
问题:把不变量的计数器换成『错的那种』,它会不会【错误地放行】臂 O?

计数器              臂O(神谕)     臂N(自选) | 用它做不变量的后果
------------------------------------------------------------------------------
N2_lvl                0        348 | ✅ 正确判臂O为『非NHS自选』
             nhs.py:404 候选过电平门后自增 = 真·条件计数
slots               250        250 | ⛔ 错误放行臂O ⇒ 恒绿
             nhs.py:365 每分析槽无条件自增
frame_i            3000       3000 | ⛔ 错误放行臂O ⇒ 恒绿
             nhs.py:264 每帧无条件自增
n_notch               8          5 | ⛔ 错误放行臂O ⇒ 恒绿
             已分配槽数(臂O被预挂8个)

⇒ 现用计数器 N2_lvl:臂O=0(须为0) 臂N=348(须>0) ⇒ PASS(双向都对)
⇒ broken 版杀死率 3/3 —— 三种『错的计数器』中有 3 种会让不变量恒绿

⭐ 结论:承重的是 **N2_lvl**(条件计数),不是 n_notch。
   若不变量只写 `n_notch > 0`,臂 O 会以 n_notch=8 通过 ⇒ 与 B-1 同形。
   ⇒ critic 的担心成立:**计数器选错,护栏就是一段注释。**
