未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 12:5x(adaptive-dsp **第 7 实例**)
轮次 = r87 · **`recheck_free` 的 ΔMSG 验证扫描**   输出 = r87_dmsg_out.txt(D6-j)
⛔ 本文件在**任何一次主扫描起跑之前**落盘;§4/§5 跑完后**不得修改**。

═══════════════════════════════════════════════════════════════════
0. 与第 6 实例 `PREREG_r87.txt` 的关系 + 偏离声明
═══════════════════════════════════════════════════════════════════
```
`PREREG_r87.txt`(2026-08-04 12:24,第 6 实例落盘)是同一次跑的草案,**从未起跑**
⇒ 按 lead 裁定 B「一版一文件,旧版不得原地改」⇒ **不覆盖它**,本件另立
⇒ 其 §0(有界例外)/§1(设计)/§2(Hg1-Hg4)**原样承继**,下列三处为我的偏离,逐条声明:
```
**偏离①(加一列,非改口径)**:除【duck 消融】主列外,**另跑【duck 不消融】附列**。
  理由 = F33 在本线已发生三次(r54/r64/F33),而其中一次正落在【达标判定】上;
  只报消融列 ⇒ 无法回答"产品形态下改善了没有";只报有duck列 ⇒ 兜底可冒名顶替。
  ⇒ **主列 = 消融**(锁定被测机制 = 陷波),附列并报 `g_duck最深`,使冒名当场可见。
  ⚠ 附列 **⛔ 不得单独引用**,须与 `g_duck最深` 同出。
**偏离②(加闸门)**:PREREG_r87 无中止条件。本件加 §3 两道闸门(**exit(1) 型**)。
**偏离③(加诊断)**:加 §7 的 r76 复现对照 —— **诊断,不是闸门**,理由见 §7。
**未偏离**:臂定义/固定参数/扫描区间/逐种子报/0.354 底/⛔不 commit ── 全部照旧。

═══════════════════════════════════════════════════════════════════
1. ⛔ 复用旧轮次件之前的三核(memory 顶部常规,逐条落纸)
═══════════════════════════════════════════════════════════════════
被复用件:`r76_cell.py` 的 `scan()` 与臂构造 / `r81_windowcheck.py` 的 `envelope_stats()`
         / `r61_bwoct_baseline.py` 的 `pick_excl/GR/FRAME` / `r86_sep.py` 的臂参数

**核① 口径是否一致**
```
(a) `clrig.make_F`:r76 用旧名 `delay_ms=8.`,r86/r81 用新名 `prop_delay_ms=8.`
    ⇒ 读码核实(`clrig.py:22`):旧名仍接受、赋给同一形参、仅打 stderr 提示
    ⇒ **同一 plant**;本轮统一用新名 `prop_delay_ms=8.`
    ⚠ 而改名的理由本身是本项目同族病:8.0 ms 声传播延迟 vs 8.0 ms 块处理延迟
      **数值相同、必须相加不是相等** ⇒ 任何数值校验都分不开 ⇒ 只有名字能分开
(b) `nhs.py` 当前 sha256[0:16] = **b77a0524669386af**,而 `VERSIONS.txt` 的等价链
    末端是 `c2eb9bef77bc06ea`(bw_oct_match 那一跳)⇒ **`recheck_free` 这一跳不在链上**
(c) ⛔⛔ **「逐位等价 12/12 + 阳性对照 8/12 已证」这句话,盘面上只存在于
    `r86_sep.py:28` 与 `r86_sep_out.txt:2` 的一行打印里 —— 没有 `r86a_bitexact.py`,
    没有 `_out.txt`,全目录 grep `recheck_free` 只命中 6 个文件,无任何等价性证据件。**
    ⇒ 按 `VERSIONS.txt` 使用规则 ③(新增开关必须配 `rXXa_bitexact` 含阳性对照),
      **该等价性目前 = 一句转述,不是盘面证据**。
    ⇒ **对本轮的影响**:本轮两臂在**同一次跑、同一份 nhs.py** 内成对测量
      ⇒ 主结论(δ = 修后 − 基线)**不依赖**该等价性 ⇒ 不阻塞;
      而"跨轮引用 r76 基线"**依赖**它 ⇒ 故 §7 把它降为诊断,并**不据 r76 取基线值**。
```
**核② 那个量是【自变量】还是【因变量】**
```
`max_depth = −18.0`(`nhs.py:109`)是 **Params 的输入参数**(封顶值)
`r86` 表里的 `−18.00` 是 **算法走到的输出读数**(恰好顶到封顶)
⇒ 两者数值相同、量纲相同、名字近似,**在因果链上是两端**
⇒ ⇒ 本轮**不设深度参数**、不建神谕臂(`mk_oracle` 里 `depth_db` 才是输入参数)
   ⇒ 本轮报的深度一律是**读数**,且⛔**深度到顶不得当作 ΔMSG 改善的证据**(lead 明令)
```
**核③ 它当时有没有为了别的目的关掉/冻结什么**
```
`r86_sep.py:16` `a.duck_gain=lambda:1.0`  ⇒ **duck 被消融**(不是默认行为)⇒ 本轮主列照旧,附列解除并单列
`r61.mk_oracle` `T_low=999`               ⇒ 神谕臂的冻结,**本轮不用该臂**,不受影响
`r76.mk_self` `T_low=−45` 构造后设        ⇒ `T_low_gr` 在 `__init__` 已算死 −65,不重算(已知性质,照旧)
其余开关必须全在默认:`prefer_unnotched=False`(已判不提交,F60)、`growth_and_gate=False`、
                     `bw_oct_match=None`、`inherit_credit=False` ⇒ **写成 §3 闸门 G1 的断言**
⚠ `r76_cell.scan()` 与 `r61.scan()` **同名、返回值顺序不同**(前者 (m,ft,st,status),后者 (m,st,ft))
  ⇒ 本轮**只用 r76 形态**,并在本件写死,防止按名字直觉取错位
```

═══════════════════════════════════════════════════════════════════
2. 设计(承继 PREREG_r87 §1)
═══════════════════════════════════════════════════════════════════
```
臂(每种子 5 条):
  m0        无 proc(标度不变性对照,ΔMSG 的减数)
  A_base    recheck_free=False · duck 消融   ← **主列基线**
  A_rf      recheck_free=True  · duck 消融   ← **主列修法**
  D_base    recheck_free=False · duck 不消融 ← 附列(偏离①)
  D_rf      recheck_free=True  · duck 不消融 ← 附列(偏离①)
固定:src_rms=**−20.0 dBFS(标称)** · T_OBS=**12.0 s** · prefer_unnotched=**False**
     bw_oct=1/5 · T_low=−45 · NN=8 槽全空 · n_cand=16 · f_cut=8000 · STEP=0.5 · fs=48k · frame=64
     cal_offset_db=0.0 · nfft=2^18 · GR={out_lim_active:False, gr_db:0.0}
种子:(T60,sd) ∈ {0.2,0.5} × {0,1,2},与 r86/r76 同六条
扫:m0 = anchor−3 … anchor+4 ; 四条算法臂 = anchor−1 … anchor+20(与 r76 逐字同)
报:ΔMSG = m_臂 − m0,**逐种子**;m = 最后一个不起振的 G
```
⚠ **频带口径(承重,凡报 ΔMSG 必写)**:环路 plant 经 `band_limit(h,8000)` ⇒ **8 kHz 以上被带限**;
  起振判据 `HD.is_howling` 作用于求和节点全带信号。全部 **[L2/宿主仿真]**,非板上、非实测。

═══════════════════════════════════════════════════════════════════
3. ⭐ 闸门(**中止条件**,不是输出行)
═══════════════════════════════════════════════════════════════════
自查句(D6-ap):「这个检查失败时,会阻止什么?」⇒ **答:阻止主扫描启动(`sys.exit(1)`)。**
```
G1 · 配置断言(起跑前,零成本)
    P.recheck_free 存在且默认 False;prefer_unnotched/growth_and_gate 均 False;
    bw_oct_match is None;inherit_credit False;NN==8;n_cand==16;max_depth==−18.0;
    bw_oct==1/5;T_low==−45;**T_OBS(12) < lift_after_s(60)**;STEP==0.5;src==−20.0
    ⇒ 任一不成立 ⇒ exit(1),**六个 cell 一个都不起**
G2 · 修法可达性(阳性对照,起跑前,≈1 min)
    在 r76 已落盘的基线终点 G 上,逐种子跑 rf=0 / rf=1 各一次,比对
      (深度向量 ∨ 挂陷数 ∨ F2_kept/F3_dropped/F4_drop_notched ∨ A3_deepen_real)
    ⇒ **6 条里 0 条出现差异 ⇒ 开关在本工作点不可达 ⇒ exit(1),主扫描不启动**
    理由(r66a 的教训):**「逐位相同」与「那条分支根本没被走到」在数据上完全同形** ——
      没有阳性对照,δ≈0 会被读成"修法无效",而真相可能是"没打到"。
    ⚠ 门设得**极松**(≥1 条即过),正是为了不犯 A-1 那类错:
      **护栏会朝【丢掉对的数据】的方向失效,而那个方向长得像"严谨"。**
```

═══════════════════════════════════════════════════════════════════
4. ⛔ 预注册假设(跑完不得修改)
═══════════════════════════════════════════════════════════════════
Hg1 · **收益应集中在【基线下深度未到顶】的那两条种子上**(承继 PREREG_r87)
```
r86 实测基线深度中位:0.2/sd1 = −15.00、0.5/sd2 = −16.50(其余四条已到 −18.00)
⇒ 若机制是"让陷波压到该压的深度",ΔMSG 改善应**集中在这两条**;其余四条应无可判改善
```
Hg2 · ⛔ **若收益反而出现在其余四条上** ⇒ 不是"补足深度"的机制 ⇒ **另有其物**,须如实指出
Hg3 · ⛔ **不得伤害**:任一种子 ΔMSG 变差 > 0.354 dB ⇒ 判该修法有害,如实报
Hg4 · ⛔ **不预测总体幅度** —— 深度补足 3.00 / 1.50 dB 换来多少 ΔMSG,无先验
Hg5 · **附列(有duck)**:⛔ 不预测方向。若附列改善而主列不改善,且 `g_duck最深` 同时变化
      ⇒ **按 F33 判为兜底冒名顶替嫌疑**,⛔ 不得记为修法收益
**证伪条件(写死)**:①若 0.2/sd1 与 0.5/sd2 两条 δ 均 = 0.00 而其余四条 δ ≠ 0 ⇒ Hg1 证伪;
                    ②任一条 δ > +0.354(变差)⇒ Hg3 触发 ⇒ 判有害。

═══════════════════════════════════════════════════════════════════
5. ⭐ 判读规则(**跑前**写死量化事实,B-2 的教训)
═══════════════════════════════════════════════════════════════════
```
仪器底 = **0.354 dB** = STEP/2×√2(⚠ STEP=0.5 本身无来源,FINDINGS:2091)
⚠⚠ 而**两臂同锚同栅格** ⇒ 每个 m 都是 anchor + 0.5 的整数倍
   ⇒ **δ = ΔMSG_rf − ΔMSG_base 恒为 0.5 的整数倍**(m0 在差里抵消)
   ⇒ ⇒ **|δ|=0.50 是"一格",它高于 0.354 只是构造使然,不代表 0.5 dB 的真分辨力**
   ⇒ 判读三分:δ=0.00 ⇒ **不可判**;|δ|=0.50 ⇒ **一格(弱)**;|δ|≥1.00 ⇒ **多格**
⛔ **「不可判」不得读作「没变化」**(lead 明令,F77.4 同型)
⛔ 逐种子报,**不得只报聚合**(D6-ar:昨夜聚合 −3% 掩盖了逐条 −16%/−13%)
```

═══════════════════════════════════════════════════════════════════
6. 窗长有效性(必报四项之④)
═══════════════════════════════════════════════════════════════════
```
判据(r81 已验形态,复用其 `envelope_stats`):在**各臂自己的终点 G** 上
  ① 末秒 − 首秒 RMS(dB):**在衰(<0)= 稳定点;在长(>0)⇒ 该格只能作上界**
  ② 到峰时刻 / 窗长 ≥ 0.7 ⇒ 窗不足 ⇒ 只能作上界
参照(r80b 已验):干净的样子 = 陷波臂 −5.87 dB(在衰)
⇒ 判为上界的格,在结果表里**逐格标出**,⛔ 不得与干净格混报
```

═══════════════════════════════════════════════════════════════════
7. 诊断(**不是闸门**)· r76 跨轮复现对照
═══════════════════════════════════════════════════════════════════
```
比对:本轮 A_base 的 (ΔMSG, 挂陷数, 终点 lp_rms) vs r76_cell_s20f0.json 的 (dA, n_notch_Na, lp_Na)
      —— 同一工作点(src=−20/fix=0/tlow=−45/T=12),差别只有 nhs.py 版本
⇒ 相符 ⇒ §1 核①(c) 那条**未落盘的等价性声明**在 ΔMSG 层面得到一次事后支持
⇒ 不符 ⇒ **大声标出**,并按"跨轮不可比"处理
⛔ **为什么不做成闸门**:两臂在同一次跑内成对测量,基线不符**不影响 δ 的有效性**
   ⇒ 做成闸门会把**有效的成对数据整片剔掉** = A-1 那个方向的护栏失效(它长得像严谨)
```

═══════════════════════════════════════════════════════════════════
8. 纪律
═══════════════════════════════════════════════════════════════════
```
⛔ 不 commit(归档由 lead 做)   ⛔ 不 spawn 子 agent   ⛔ 不联系其他 teammate
⛔ 出数即收口:不管结果好坏,**不加任何一轮、不提任何修法、不开任何新调查**
   若 ΔMSG 未改善 ⇒ 如实记「深度达标而 ΔMSG 未改善」—— 那本身就是一条结论
每个数挂 L 标:全部 **[L2/宿主仿真]**,未过 critic;不确定标 [L4/待验证],⛔ 不编
setsid+nohup 起跑,⛔ 不用 timeout 包(前一实例被自己的 timeout 杀掉,退出码 143)
```
