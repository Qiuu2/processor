未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-05 0x:xx(adaptive-dsp 第 7 实例)
轮次 = r90 · **`0.5/0` 单格窗长加跑**(critic `critic_ARM_O_RECALC_verdict_20260805.md` MAJOR-1)
输出 = r90_tobs_out.txt   ⛔ §2/§3 跑前落盘,跑完不得改。

═══ 0. 为什么跑(⛔ 边界极窄:一格,两档)
```
`±0.40 dB` 这个界,**唯一的产生者**是 `0.5/0` 那格(|缺口偏差| 0.395 = 六格最大)
而 critic 查明:**该格是六格中唯一随 T_OBS 跳整格的** —— 臂 O 实测 dO:T=6 → 3.5,T=12 → 3.0
  (五个源电平档 s10/s20/s30/s40/s60 全部复现同一跳变 ⇒ 不是噪声)
而「臂 O 偏差」按定义 = **检测器在 T_OBS 内漏掉多少** ⇒ 它**就是 T_OBS 的函数**
⇒ 而我方 `02_self_set_constants.md:58` 早已把 `T_OBS` 登记为 **🔴 会翻,且已实证**
⇒ ⇒ critic 的结论句:**「一个建立在已知会翻的轴上的界,不带该轴的坐标,就不是一个界。」**
⛔ 而 critic 明写它**没跑 T=24**,不主张那个界一定会破 ⇒ 本轮就是去跑那一格。
```
合法性:`r76_cell.py:139` 的 assert 要求 `T_OBS < lift_after_s = 60` ⇒ **24 / 48 均合法**。

═══ 1. 设计
```
格:T60=0.5 / seed=0 / src=−20 dBFS(标称)/ bw_oct=1/5 / f_cut=8k / STEP=0.5 / duck 消融
臂:m0(proc=None)· 臂 O(神谕 8 选点,depth=−18,T_low=999)· **冻结 Na**(r89 的最终配置)
档:T_OBS ∈ {6, 12, 24, 48}  —— 6/12 作**复现对照**(须重现 3.5 / 3.0),24/48 是新档
⚠ 三臂同档跑 ⇒ 缺口偏差在每一档内自洽(⛔ 不把 T=24 的臂 O 与 T=12 的 Na 相减)
```

═══ 2. ⭐⭐ 判据(**跑前写死**,⛔ 跑完不得改)
```
解析真值(第三方已复现两次):m_O_解析 = **−9.2924**,dO_解析 = **3.198**
⇒ 「完美检测器 + 栅格向下取整」的**自洽区间** = [m_O_解析 − STEP, m_O_解析) = **[−9.7924, −9.2924)**

⭐ 而必须先写明一件事,否则会把两种失效读成一种:
   **T=12 的 m_O = −9.4907 【已经落在自洽区间内】** ⇒ 它已经收敛到正确的那一格
   ⇒ ∴ 若 T=24 **再跌一整格**(−9.9907),那**不是**"窗更长抓到更早的失稳",
     而是**检测器在解析临界增益【之下】就报了失稳** = **另一种失效(过检)**
   ⇒ ⇒ 故三分判读:
      (i) m_O(T) ∈ [−9.7924, −9.2924)  ⇒ **已收敛**,偏差 ∈ (−0.5, 0]
      (ii) m_O(T) ≥ −9.2924            ⇒ **漏检**(T 不足)—— T=6 正是此类
      (iii) m_O(T) < −9.7924           ⇒ **过检**(检测器在临界之下就响)⇒ ⛔ 是新问题,须单列
```
**H1(主问)**:`m_O(24)` 与 `m_O(48)` 是否仍 == `m_O(12)` = −9.4907?
**H2(承重)**:任一档 **|缺口偏差| ≥ 0.50** ⇒ **②「0/6 格达整格 0.50」当场作废** ⇒ 顶格报。
**H3**:⛔ **不预测方向**。⚠ 而我须声明先验偏向:按 S-2 的实证方向(窗越长还在长),
       "会再跌"是**对我方不利**的那侧;而按 §2 的自洽区间分析,**我预期不跌**。
       ⇒ ⭐ **两者冲突 ⇒ 举证门槛按【不利方向】设**:若实测"不跌",我须**同时**给出自洽区间证据,
         ⛔ 不得只报"两档相同"就宣布收敛。
**H4**:6/12 两档须**复现** 3.5 / 3.0;不复现 ⇒ 台架或口径漂 ⇒ **整件作废**,⛔ 不出结论。

═══ 3. 纪律
```
⛔ 逐档报,⛔ 不报聚合;每格标是否在仪器分辨力(整格 0.50,单边)之上
⛔ 不 commit;全部 [L2/宿主仿真];setsid+nohup,⛔ 不用 timeout 包
⇒ 出数即报 ①,⛔ 不等措辞那一步
```
