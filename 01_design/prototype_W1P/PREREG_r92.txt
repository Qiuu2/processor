未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-05(adaptive-dsp 第 7 实例)
轮次 = r92 · **T60 = 0.35 单格**(critic r8 提的便宜实验;lead 批)  输出 = r92_t035_out.txt
⛔ §2 跑前落盘,跑完不得改。

═══ 0. 这一格买什么
```
现有两点:T60=0.2 三格 ⇒ m 在 **T_OBS=12 即收敛**
         T60=0.5 三格 ⇒ **12 均未收敛**(24 收 / 48 收 / 48 仍不收)
机制假说(critic 提):建立时间由环路衰减尺度定,τ = T60/6.908
         ⇒ **所需 T_OBS 应随 T60 单调增**
⇒ ⭐ 加跑中间点 0.35:**落在中间 ⇒ 得到一个能【预测】的版本**(它同时预言 T60=0.8 更糟)
                    **不落在中间 ⇒ 这个推广当场作废**
⛔ 而本轮**不**去证"台架性质 vs ΔMSG 性质" —— 那条链子 critic 已判未被论证,本轮不碰
```

═══ 1. 设计
```
格:T60 = 0.35 / seed = 0 / src = −20 dBFS / bw_oct=1/5 / f_cut=8k / STEP=0.5 / duck 消融
臂:m0(proc=None)+ **臂 O**(神谕 8 选点,depth=−18,T_low=999)
   ⚠ ⛔ **不跑冻结 Na** —— r89 没有该 T60 的配置,而本问只需 **m 本身的收敛性**,
     臂 O 是 LTI ⇒ 有解析真值 ⇒ 足够;⛔ 不为凑一列而临时造一个 Na 配置
档:T_OBS ∈ {12, 24, 48}
解析真值本轮自算(MSGMeter),⛔ 不转抄
```

═══ 2. ⭐ 判据(跑前写死)
```
自洽区间 = [解析真值 − STEP, 解析真值);三分:漏检(未收敛)/ 已收敛 / 过检
`T_conv` := 最小的、使臂 O 落入自洽区间的档

H1 ✅ **支持(且可预测)**:`T_conv(0.35) = 24`
     ⇒ 严格落在 0.2 的 12 与 0.5 的 24–∞ 之间 ⇒ 单调关系**被此格检验且通过**
H2 ⚠ **弱支持**:`T_conv(0.35) = 12`
     ⇒ 与 T60=0.2 同档 ⇒ **不增,但也不减** ⇒ 单调性**未被此格检验**(阶跃与单调在此不可分)
     ⇒ ⛔ 须如实标"本格不构成对单调性的检验",⛔ 不得记为支持
H3 ⛔ **证伪**:`T_conv(0.35) = 48` 或 **48 s 内不收敛**
     ⇒ 与 T60=0.5 同或更差 ⇒ **单调性在 [0.2, 0.5] 区间不成立 ⇒ 该推广当场作废**
⚠ ⛔ **n = 1 个种子** ⇒ 无论结果如何,**不得外推到该 T60 的其它种子**
   (⇒ 而 T60=0.5 层三个种子的 T_conv 就是 24 / 48 / ∞ —— **同层内散布已达 2× 以上**)
```

═══ 3. 纪律
```
⛔ 不 commit;[L2/宿主仿真];setsid+nohup,⛔ 不用 timeout;出数即报
⛔ 不据本格改任何设计;⛔ 不重开"台架性质"那条链
