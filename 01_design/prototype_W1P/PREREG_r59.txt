PREREG_TS = 2026-08-02 (adaptive-dsp 第 4 实例) —— 承 PREREG_r57_r58
轮次 = r59   输出 = r59_ftrig8k_out.txt   (D6-j:路径唯一)
被测哈希:nhs.py 706b658842d84316 / clrig.py 8ad47ce8d260dd18 / howl_detect.py fd63e901f2d8be33

缘起:lead 点名要的一列 **r57 没报** —— r57 §1 报的是【解析】峰频(且是挂陷**前**的),
     lead 要的是 **f_cut=8k 时闭环【实测】起振主导频率**,并明言
     「这一列是"带外主导"这个归因的**直接验证,不是推论**」。**这是我的漏项,补测。**

判读(先写死):
  Hj · f_cut=8k 时,闭环起振主导频率**落回带内**。
       判据:六条种子 f_trig ∈ [100, 8000] Hz 全中 ⇒ Hj 立。
       对照基准:f_cut=24k 时同一列实测为 15182 / 18895 / 14531 / 17244 Hz(r52,全部带外)。
       证伪:任一条仍 >8 kHz ⇒ 带限没起作用或另有折叠通路 ⇒ 归因须重审。
  Hk · f_trig 应等于**挂陷【后】**的解析全带最高临界点(而非挂陷前的)。
       判据:|f_trig − f_crit(挂陷后,全带)| ≤ 20 Hz 六条全中。
       ⚠ 这一条比 Hj 强:Hj 只说"回到带内",Hk 说"回到**我们算得出的那个具体频点**"。
       证伪:落在别处 ⇒ 预测式漏了机制(与 §6.1 原判读第二分支同构)。
  Hl · 低频侧(F35)的直接体现:`0.5/seed0`(挂陷后解析峰 52.2 Hz)与 `0.5/seed1`(22.3 Hz)
       的 f_trig 应落在 **<120 Hz**。若落在带内高处 ⇒ F35 归因错。
       ⚠ 注意:52.2/22.3 Hz **低于 Hj 的 100 Hz 下沿** ⇒ **Hj 与 Hl 对这两条互斥**。
         预先声明:以 **Hk 为准**(它是更强的、能分辨二者的判据);Hj 的"带内"对这两条
         按"落在 f_cut 以下且等于解析后峰"判,**不得事后改口径凑 Hj**。

臂:陷波 8 个 + **duck 已消融**(锁定被测机制,lead 通则 F33)。
[L2/宿主仿真]
