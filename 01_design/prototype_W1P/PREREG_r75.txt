未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-03 20:xx(adaptive-dsp 第 5 实例)
轮次 = r75  输出 = r75_srclevel_fix_out.txt  日志 = r75_run.log  (D6-j 路径唯一)
被测:nhs.py@a6b467df127e741f(**已加 prefer_unnotched,默认 False**;旧 31decc8e8d07e085)
      clrig.py@8ad47ce8d260dd18 howl_detect.py@fd63e901f2d8be33 msg_meter.py@a0c16fd22b29f083
      r57_bandlimit.py@74036010b514080d

═══════════════════════════════════════════════════════════════════
0. ⛔ 前轮作废留痕
═══════════════════════════════════════════════════════════════════
`r74`(源电平 ∈ {−70,−60,−50,−40,−30})**已中止,未产出 out 文件**,
日志留存 `r74_run_ABORTED_range_superseded.log`,**不得当数据引用**。
中止理由:架构侧裁定**标称输入 = −20 dBFS**(`+4 dBu` 端子 + 20 dB headroom,`[L4/行业惯例]`,
待 CTO 追认)⇒ **r74 的五档全部在标称以下,主档缺失。**

═══════════════════════════════════════════════════════════════════
1. ⛔ 算力截断(显式 log,不静默省略 —— lead 要求)
═══════════════════════════════════════════════════════════════════
lead 给的完整矩阵 = 源电平 5 档 × 修法 2 × T_low 2 = 20 组 × 6 种子。
**本轮只跑 lead 指定的优先两端**:
```
✅ 跑    源电平 ∈ {−20(标称,主档), −60(与 B-1 对齐)} × 修法 {关, 开} = 4 组 × 6 种子
⛔ 砍    源电平 −40 / −30 / −10   —— 算力,待本轮出数后按需补
⛔ 砍    T_low = −50 对照         —— 架构侧已裁定 T_low 定为 **−45**,分叉已闭
                                    ⇒ −50 降为"若 CTO 改标称输入才需重启"的条件项
⛔ 砍    T_OBS 的 F36 阶梯          —— 钉死 {6, 12} s 两档,与 r64 逐档对齐
         ⇒ 代价:T60=0.5 层未收敛(r64 已证 48s 仍不收敛)⇒ **该层 δ 不得单独成句,
           须与"该层在 48s 仍不收敛"同段出现**
```

═══════════════════════════════════════════════════════════════════
2. 工作点向量(**新增两维,lead 点名**)
═══════════════════════════════════════════════════════════════════
`{ **src_rms_dbfs**(★新;此前从未进过任何一轮 —— 架构侧原话「正是护栏 A 该拦而没拦到的一维」),
   **cal_offset_db = 0.0**(★新;`nhs.py:236`,默认 0 从未被用过;它是"端子 dBu → 内部 dBFS"
   的映射位,已进 D14 bring-up 清单),
   prefer_unnotched(修法开关), T_low = −45(架构侧已定,与标称输入是**绑定对**),
   fs=48k, frame=64, f_cut=8k, STEP=0.5, bw_oct=1/5, depth=−18dB, 8 槽全空,
   T_OBS ∈ {6,12}, nfft=2^18, seed, T60 }`

═══════════════════════════════════════════════════════════════════
3. ⭐⭐ 预注册假设 —— 含 lead 点名要写死的那一条
═══════════════════════════════════════════════════════════════════
Hq1 · **⛔「源上移 ⇒ 自选臂 ΔMSG 上升」是一个【需要被证伪的假设】,不是背景。**
      架构侧预判该方向,**而它对我们有利** ⇒ 按第一号失效模式(证据门槛不得随结论方向浮动),
      **举证门槛更高**:必须实测,⛔ 不得外推,⛔ 不得据此先行放宽 B-1。
      **⇒ 若实测不升甚至下降,那是【更重要】的结果,须原样报出。**
Hq2 · 过门率随源电平上升(r74 补测已在固定 G 上见到 0.82%→8.88%)。
      ⚠ **某档过门率接近 0 或接近 100% ⇒ 该档 ΔMSG 不可比,单列不进比较**(lead 要求 ⑤)。
Hq3 · 修法(`prefer_unnotched=True`)使【不同频点的挂陷数】上升、`top1_hit` 不降。
      证伪:挂陷数不升或 `top1_hit` 下降 ⇒ 修法在本工作点无效或有害,如实报。
Hq4 · **⛔ 若修法效果在不同源电平上【变号】—— 那是最重要的发现,⛔ 不许平均掉。**
Hq5 · 逐位等价:`prefer_unnotched=False` 时与旧 `nhs.py` 行为**逐位相同**
      (第二键恒 False ⇒ 常量分量不影响排序)。**由 r75a 实跑证明,不靠读表达式。**
      证伪 ⇒ 立即回滚 nhs.py 并报 lead。

═══════════════════════════════════════════════════════════════════
4. 必报项(lead 五条)+ 纪律
═══════════════════════════════════════════════════════════════════
①ΔMSG(**兜底消融列必有**)②过门率 vs 源电平 ③§4 四量,**`top1_hit` 单列**
④运行时不变量双向断言(INV-N 三分 / INV-O 构造精确)⑤不可比档单列
⛔ 结论行看到数之后再写;⛔ 修法反事实仍标「**非提交修法**」;
⛔ **B-1 的 1.00–2.50 dB 须当场标注「在 `src_rms = −60 dBFS` 上测得,而标称为 −20 dBFS」**;
⛔ 不报跨层均值;命名用全限定名;不 commit。全部 [L2/宿主仿真]。
