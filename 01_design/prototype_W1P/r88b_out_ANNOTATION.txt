未经 critic 评审 —— r88b 判读件。⛔⛔ 本轮 **ΔMSG 全列作废**(伪影,成因已实测查明)。
预注册 = PREREG_r88b.txt。件:r88b_gate_out.txt / r88b_cell_*_out.txt / r88b_plant_out.txt

═══════════════════════════════════════════════════════════════════
§0 ⛔⛔ 结论先写:**本轮的 ΔMSG 对比无效,而无效的原因是我造的 plant**
═══════════════════════════════════════════════════════════════════
⇒ 凡引用本轮 ΔMSG 的数,一律用改名后的标识:
   `DMSG_r88b_ARTIFACT_勿用`(D6-t 负向用法:被判伪影的数**当场改名**,别指望段落里的标注)

**⇒ 闸门(解析先验)是过的;失效的是【被测装置本身】,而它是被自己的输出暴露的。**

═══════════════════════════════════════════════════════════════════
§1 证据链(三步,全部实测)
═══════════════════════════════════════════════════════════════════
① **起振频率全部挤在 `f_cross = 600 Hz` 下缘**:
```
模态臂 9 格的 f_trig:579 / 471 / 486 / 558 / 595 / 584 / 490 / 487 / 488 Hz  ⇒ 全在 471–595
统计臂 6 格的 f_trig:2559 / 1034 / 3111 / 2258 / 7580 / 716 Hz              ⇒ 散在全带
挂陷频点同样成簇:470 / 486 / 488 / 495 / 530 / 560 / 581 / 595 …
⇒ 9/9 落进接缝下方 125 Hz 的窗内 ⇒ ⛔ 不可能是巧合
```
② **成因实测(功率谱密度对拍)**:
```
P_stat:120–600 Hz 平均 PSD 5.07 dB ｜ 600–7800 Hz 4.75 dB ⇒ 低/高 = **+0.32 dB**(平)
P_conf:                  15.62 dB ｜             −1.04 dB ⇒ 低/高 = **+16.66 dB**(⛔ 巨大倾斜)
最高临界点:P_stat 在 2259 Hz(12.49 dB);P_conf 在 **558 Hz**(20.57 dB)
```
③ **机理**:低段(模态合成,0–600 Hz)与高段(噪声尾,600 Hz 以上)**各自归一化后相加**
   ⇒ 相同能量摊在**窄带 vs 宽带**上 ⇒ 低段谱密度高出 **16.7 dB**
   ⇒ 全部最高临界点被搬到接缝下方 ⇒ NHS 只在那里挂陷 ⇒ ΔMSG 差异全部由这条倾斜驱动

═══════════════════════════════════════════════════════════════════
§2 ⭐ 而这个伪影是【我为防止伪影而加的那条措施】造成的
═══════════════════════════════════════════════════════════════════
`PREREG_r88.txt §1-③` 我的原话:
> 「低/高两段**各自归一化后相加** ⇒ 否则 `f_cross` 会变成一个**隐藏增益旋钮**」

**⇒ 它恰恰把 `f_cross` 变成了一个隐藏增益旋钮,而且是 16.7 dB 的那种。**
⇒ 归入 F45 族(哨兵值 / 键 / 参考值)第四例,**共同形状**:
  **为观测而引入的东西,在被测系统里并不中立。**
⇒ ⚠ 而本例的新形态:**那个"东西"是一条护栏本身** —— 护栏不中立,比探针不中立更难发现,
  因为它带着"我已经防过这一点了"的心理背书。

═══════════════════════════════════════════════════════════════════
§3 ⛔ 因此不能说什么(逐条)
═══════════════════════════════════════════════════════════════════
```
⛔ 不得说「NHS 在模态场里表现更差」—— 实测差异出自 16.7 dB 倾斜,不是场的性质
⛔ 不得说「B-1 不成立 / 常数之争会影响决定」—— 该判读建立在作废的数上
⛔ 不得说「阳性对照 3/3 通过 ⇒ 对比有分辨力」—— 它看见的是倾斜,不是模态结构
✅ 仍成立:闸门的**解析部分**(M(f) 全段 <1 / >1)—— 它不依赖仿真
✅ 仍成立:§4 那条**解析结论**(与本轮仿真无关)
```

═══════════════════════════════════════════════════════════════════
§4 ✅ 本轮唯一站得住的结论(**解析,零仿真**)
═══════════════════════════════════════════════════════════════════
```
M(f) = (4πVf²/c³)·(2.2/T60),解析、连续、零采样噪声
要让整条 120–280 Hz 满足 M<1(全段孤立模态),须 V < 3.7 m³(T60=0.2)/ 9.3(0.5)/ 18.6(1.0)
⇒ **没有任何现实房间满足** ⇒ 该段在任何现实会议室里都是**过渡区**,从来不是"几个驻波"
⇒ 「那一段是模态区还是统计区」这个问法本身错了 —— 它两者都不是
⇒ 而分界画在 M=2.74(k2000)还是 M=10.96(k4000)**不改变场本身**
⇒ ⇒ **Schroeder 常数之争没有物理内容**(与未答清单 E-6 的改写一致)
```

═══════════════════════════════════════════════════════════════════
§5 ⇒ 处置(按 lead 写死的硬边界)
═══════════════════════════════════════════════════════════════════
```
lead 的边界:一轮;若本轮不成 ⇒ 走选项 ③,⛔ 不许再换第三把尺子,本轮之后不再加轮
⇒ ∴ **Q2(NHS 在模态场 vs 统计场的行为)在合成 plant 上【本项目不再追】**
⇒ 并入「须实测真实房间」(收口件 ③B-3)
⇒ ⛔ 我不提修法(把接缝做平、或改用镜像源全带合成)—— 那是第三把尺子
```
⛔ 未经 critic 评审。全部 [L2/宿主仿真];§4 为 [L3/解析]。⛔ 未 commit。
