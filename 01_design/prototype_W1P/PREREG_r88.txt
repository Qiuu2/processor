未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 15:5x(adaptive-dsp 第 7 实例)
轮次 = r88 · **非统计 plant 对照**(critic 找出的第四种手段)  输出 = r88_plant_out.txt(D6-j)
⛔ §3/§4/§5 在主对比起跑前落盘,跑完不得修改。

═══════════════════════════════════════════════════════════════════
0. 这一格要回答什么(⛔ 不是"更像真实房间")
═══════════════════════════════════════════════════════════════════
```
背景:现有台架 `clrig.make_F` **按构造即统计模型**(Rayleigh 幅度 / σ_dB≈5.57 / 峰间距≈4/T60)
     ⇒ 在它上面数低频临界点只会复现它自己的假设 ⇒ **循环**(收口件 §5.3)
critic 指出的第四种手段:换一个**非统计的合成 plant**
     ⇒ 它答不了"真实会议室长什么样"
     ⇒ ⭐ 但它能答:**【若这一段是可辨模态而非统计场,NHS 的行为会不会不同】**
⇒ ⭐⭐ 判据写成 B-1 形式(lead 指定):
   **若两种 plant 下 NHS 行为无可判差异 ⇒ Schroeder 常数之争【不改变任何决定】⇒ 登记不追。**
   ⇒ 那是最好的结局,而且它是可能的。
⛔ 本轮**不**声称非统计 plant 更真实;⛔ 不据它改任何设计。
```

═══════════════════════════════════════════════════════════════════
1. 被测物:`modal_rig.make_F_modal`(新件)
═══════════════════════════════════════════════════════════════════
```
刚性矩形房间本征频率(解析,非随机):f = (c/2)·√((nx/Lx)²+(ny/Ly)²+(nz/Lz)²)
振型 Ψ = Π cos(nπx/L);幅度 a_k = Ψ_k(源)·Ψ_k(收) ⇒ **由几何与位置决定,不是随机抽的**
h_low(t) = Σ_k a_k·e^{−t/τ}·cos(2πf_k t),τ = T60/6.908(与 clrig 同式)
接口与 `clrig.make_F` 逐字同形:返回 (h, D),已含直达延迟、已归一化
```
**⚠ 三条已声明的取舍(⛔ 不藏)**:
```
① **混合**:全带模态合成不可行(V=60 到 8 kHz 约 3.2×10⁶ 个模态)
   ⇒ 只在 `f_cross = 600 Hz` 以下做模态合成,以上沿用高通后的噪声尾
   ⇒ **低频段(争议所在)非统计,高频段两 plant 同构** —— 变量被隔离在争议频段,这是有意的
② **seed 的含义两边不同**:统计 plant 的 seed 换整条 RIR;模态 plant 的 seed 只换**源/收位置**
   (几何固定)⇒ **模态 plant 的种子间变异性在构造上更小** ⇒ ⛔ 不得把两边的散布直接对比
③ 低/高两段**各自归一化后相加** ⇒ 否则 `f_cross` 会变成一个隐藏增益旋钮
```

═══════════════════════════════════════════════════════════════════
2. ⭐ 器械能力预检(**已跑,如实全列,⛔ 无挑选**)
═══════════════════════════════════════════════════════════════════
T60=0.5、seed=0、频带 120–280 Hz、`nfft=2^18`、8 kHz 带限后取 `h_eff`:
```
房间                V(m³)  f_c(k=4000)  σ_dB    临界点密度(/Hz)   Weyl 模态数
booth(原文那个)     14        755      **10.13**    0.031            29
小间                 30        513       9.42       0.050            64
会议室               60        365       6.22       0.069           126
会议室              100        283       7.96       0.087           210
会议室              300        163       6.55       0.069           629
统计 plant(clrig)    —         —       **5.71**    0.050             —
Rayleigh 理论                            5.57
```
**⇒ 读法(跑前写死,免得事后挑)**:
```
① 器械**确实能**造出非统计场:booth σ_dB = 10.13 ≫ 5.57,密度 0.031 ≪ 0.050 ⇒ 判别量有量程
② 而**会议室尺寸上两者已经很接近**(σ 6.2–8.0 vs 5.71)⇒ 这本身就是一条结果的雏形
   ⚠ 但 n=1 seed、且 100 m³ 反高于 60 m³ ⇒ **非单调,散布未知** ⇒ ⛔ 现在不下任何结论
```

═══════════════════════════════════════════════════════════════════
3. ⭐ 闸门(**中止条件**,`sys.exit(1)`;⛔ 不是输出行)
═══════════════════════════════════════════════════════════════════
自查句(D6-ap):「这个检查失败时,会阻止什么?」⇒ **阻止主对比启动。**
```
G1 · 器械能力(限定①要求的空测)
    **booth(14 m³)模态 plant** 在 120–280 Hz 必须与统计 plant **可判地不同**:
      |σ_dB(booth) − σ_dB(统计)| > 1.0 dB  ∧  临界点密度之比 ∉ [0.8, 1.25]
    ⇒ 六条种子中 **≥4 条**须同时满足 ⇒ 否则 **"我造了一个非统计 plant" 这个声称不成立** ⇒ exit(1)
G2 · 配置断言:两 plant 同 T60 / 同 prop_delay / 同 8 kHz 带限 / 同归一化 / NHS 全开关默认
    ⇒ 任一不成立 ⇒ exit(1)
```
**⛔⛔ 而【会议室尺寸上两 plant 无差异】⛔ 不是闸门,是结果** ——
把它写成闸门就会把**最有价值的那个结局**(B-1:争议不影响决定)当成失败剔掉。
> **这正是 A-1 那类"长得像严谨"的护栏失效方向:它会挡掉对的数据。**

═══════════════════════════════════════════════════════════════════
4. ⛔ 预注册假设(跑完不得改)
═══════════════════════════════════════════════════════════════════
```
臂:P_stat  = clrig.make_F(噪声 RIR,现行台架)
    P_conf = modal_rig 会议室 100 m³(5.8×4.6×3.75)  ← 主对比
    P_booth= modal_rig booth 14 m³(2.6×2.4×2.25)   ← **阳性对照**(限定④)
固定:src=−20 dBFS(标称)· T_OBS=12 s · bw_oct=1/5 · NN=8 · T_low=−45 · duck 消融 · 修法全关
每臂:m0(无 proc)+ NHS 自选 ⇒ ΔMSG = m_NHS − m0,**逐种子**
```
**Hp1 · 主问**:`P_conf` vs `P_stat` 的 ΔMSG 逐种子差 δ,**⛔ 不预测方向**。
    判读:若 **0/6 可判**(|δ| < 仪器底 0.354)⇒ **「常数之争不改变决定」成立 ⇒ 登记不追**。
**Hp2 · ⭐ 阳性对照(限定④)**:`P_booth` vs `P_stat` **必须**出现可判差异。
    ⛔ **若 P_booth 也无差异 ⇒ 本对比无分辨力 ⇒ Hp1 的"无差异"不得读作阴性结论**
    ⇒ 那种情况下整件按"器械无分辨力"如实记,⛔ 不出 B-1 结论。
    **理由:选 booth 作阳性对照,是因为【那里本就该有差异】(f_c=755 ≫ 280,深在模态区)**
    —— 比人为放大差异更接近被测机制。
**Hp3 · 低频行为(争议所在)**:逐 plant 报**挂陷频点落在 <300 Hz 的个数**与命中率。
    ⛔ 不预测方向;若两 plant 的低频挂陷行为不同而 ΔMSG 相同 ⇒ 须如实并报(两件事)。
**Hp4 · ⛔ 不得伤及**:若某 plant 下 NHS 的挂陷数塌到 0 ⇒ 该格标"退化",单列不混入统计。

**⛔ 判读规则(跑前写死)**:两臂同锚同栅格 ⇒ δ 恒为 0.5 整数倍 ⇒ **|δ|=0.50 只是一格**;
δ=0.00 ⇒ **不可判**,⛔ 不得读作"没变化";逐种子报,⛔ 不得只报聚合(D6-ar)。

═══════════════════════════════════════════════════════════════════
5. 纪律
═══════════════════════════════════════════════════════════════════
```
⛔ 不 commit  ⛔ 不 spawn  ⛔ 不联系其他 teammate(报 lead)
每个数挂 L 标:全部 [L2/宿主仿真],未过 critic
setsid+nohup 起跑,⛔ 不用 timeout 包
⛔ 出数即报;⛔ 不据本轮改任何设计(本轮只判"争议影不影响决定")
```
