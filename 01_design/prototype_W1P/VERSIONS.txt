# W1-P 原型件版本台账(DEC-0016:nhs.py 是 NHS 算法行为的唯一权威源)
# 规矩:改动 = 新版本号 + 本台账行 + decisions_log 台账行 + W1-B 引用版本同步
# ⚠ 全部 [L2/宿主仿真];本件尚未过任何门(未经独立 critic 评审)

vP1.0 | 2026-08-01 | sha256[0:16] = 7907fec001d081b0 | adaptive-dsp-3
  首个版本化快照。内容 = 五轮原型的终态:
   - 核心链(候选提取/六判据/三路分类/陷波状态机/分配-加深-回收)
   - F4 修法(T_low_gr 放宽的非自指出口 _notch_covers,nhs.py:458)
   - F9 修法(影子继承不再以 GR 为前提)
   - F10 修法(inherit_credit 默认关:死亡时刻信用会毒化重生轨)
   - B9 护栏参数化(gap_guard_ratio,定标结论见 results_r5.txt:25-31)
   - 计数器仪表(ctr:table_full/unobs/readback_ok/shadow_new/shadow_inherit/umax_hit/gapguard)
  配套件:env.py(image-source RIR + 合成素材 + 单通道闭环台架)
          multi.py(多通道 + 共享母线 + 8段动态PEQ,空闲严格单位增益)
          experiments.py / run_all.py / r4_*.py / r5*.py
  证据:results.txt / results_r4.txt / results_r5.txt
  待办:①补独立 critic 评审(含本件)②定点移植须对本件 bit-exact

═══════════════════════════════════════════════════════════════════════════
# ⭐ nhs.py 行为等价链(2026-08-04,adaptive-dsp 第 6 实例建立)
#   ⛔ 未经 critic 评审。用途 = 让归档件里的 `deps: nhs.py@…` 行**可判读**。
═══════════════════════════════════════════════════════════════════════════

## ⚠ 为什么建链,而不是把归档件里的 deps 行批量改成新哈希

**归档 `_out.txt` 里的 `deps: nhs.py@X` 是【那次跑真的用了哪个版本】的事实记录。**
**把它改写成当前哈希 = 篡改记录**,而且改完之后**再也无法判断那次跑到底用的哪版**。
⇒ 与 lead 2026-08-04 对三处 commit message 的处置**同一原则**(E-2:**加标注不删数**)。
⇒ **故:记录一律不动;改为建立【等价链】,让每个旧哈希都能查到"它与当前版行为是否等价"。**

## 链(每一跳都有【实跑逐位对照 + 阳性对照】,⛔ 不靠"读起来一样")

```
706b658842d84316            基线(r64 所用)
   ↓ 加 growth_and_gate(默认 False)
31decc8e8d07e085            等价证据:r66a_bitexact —— 逐位相同
   ↓ 加 prefer_unnotched(默认 False)
a6b467df127e741f            等价证据:r75a_bitexact —— **24/24 逐位相同**;
                                      阳性对照(强制开)**9/24 出现差异**
   ↓ 加 bw_oct_match(默认 None ⇒ 回落 bw_oct)
c2eb9bef77bc06ea            等价证据:r78a_bitexact —— **24/24 逐位相同**;
                                      阳性对照(强制 1/12)**6/24 出现差异**
   ↓ 加 r84 漏斗遥测(F1–F5 计数)      ⚠ 中间版,**未单测**(见下"端到端"注)
57ced119c783ecae
   ↓ 加 r85 preempt_log + A1/A2/A3 计数 ⚠ 中间版,**未单测**
47a57d212b17d7f4
   ↓ 加 recheck_free(默认 False)
b77a0524669386af            等价证据:**r86a_bitexact**(2026-08-04,第 7 实例,lead 批)
   ← 当前版(2026-08-04)             —— **30/30 逐位相同**(块A 24/24 @T_OBS=3s、块B 6/6 @T_OBS=12s);
                                      阳性对照(强制 recheck_free=True)**17/30 出现差异**
                                      对照臂 = **git 归档原文** c2eb9bef(跑前现场复算哈希,跑完即删副本)
```
**⚠ 这一条边是【端到端】的:r86a 对照的是 `c2eb9bef ↔ b77a0524`,一次覆盖三跳。**
**   ⇒ 端点等价**已证**;而中间两版 `57ced119` / `47a57d21` **未各自单测**
**   ⇒ 引用这两个中间哈希的归档数时,按规则 ② 处理(标"版本未对齐"或补跑),⛔ 不得据本条推断。**
**⚠ 判据口径**:行为等价 = `y` / `loop` / `slots` 三项逐位 + `ctr` **共有键**逐值;
**   新增的纯计数键(F1_cls_out/F2_kept/F3_dropped/F4_drop_notched/F5_kept_notched/**
**   A1_in/A2_deepen_branch/A3_deepen_real/A3_cmd_db)单列,⛔ 不计入差异也不藏。**
**⇒ 链上任意两版,在【全部开关取默认值】时行为逐位相同。**
**⇒ 因此:引用旧哈希的归档结果【不失效】(既有归档失效集 = 空),**
**   而它们记录的版本号【保持原样】—— 两件事不冲突。**

## ⚠ 使用规则(引用归档数时照此判读)

```
① deps 哈希 ∈ 上链  ⇒ 该结果与当前版**行为等价** ⇒ 可引用,⛔ 无需重跑
② deps 哈希 ∉ 上链  ⇒ **未建立等价** ⇒ ⛔ 引用前须先补一次逐位对照,或标注为"版本未对齐"
③ 凡新增开关,**默认必须是"关"**,且必须配一次 `rXXa_bitexact`(含**阳性对照**)
   ⇒ 阳性对照缺失 ⇒ "逐位相同"不构成证据(F62:器械必须能失败)
```

## ⛔ 本目录内 deps 哈希现状(扫描日期 2026-08-04,见 r79 报告)

链外哈希仍有若干(多为 vP1.0 前后的早期件)⇒ 按规则 ② 处理,**不批量改写**。

