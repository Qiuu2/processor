未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 03:0x(adaptive-dsp **第 6 实例**)
轮次 = r80 · **合成实验**(陷波 × 频移)  输出 = r80_cell_<tag>_out.txt + r80_compose_out.txt
日志 = r80_cell_<tag>.log   (D6-j:路径唯一,合并件不重跑任何仿真)
新件 = `fshift.py`(单边带频移器,Hilbert 法)

═══════════════════════════════════════════════════════════════════
0. 本轮要答的那一件(架构级)
═══════════════════════════════════════════════════════════════════
```
F73 已确立(一手原文):频移的 Δl 与我方 ΔMSG **同类**,且我方房间理论值 8.96 / 9.81 dB
F73.4 我的推断:**二者争同一份余量(峰-均比)⇒ 次可加,⛔ 不是 3–5 + 5 = 8–10**
⇒ 本轮直接量:**次可加量 = (ΔMSG_陷波 + ΔMSG_频移) − ΔMSG_合成**
⇒ 这是【实测合成】,不是相加 —— 而 lead 的原算法正是相加
```
**⚠ lead 补的那条待判方向(分层报,⛔ 不平均)**:
```
Δl 随 W·T60 **上升**(8.96 @T60=0.2 → 9.81 @T60=0.5)
陷波的序统计量随混响**下降**(峰更多 ⇒ top1−top9 更小)
⇒ **频移在【陷波最失败的那一层】反而更强**,而 T60=0.5 正是我们 0/6 达标的那层
⇒ 若成立 ⇒ 主次可能倒过来(频移为主、陷波补低频与窄带谐振)
```

═══════════════════════════════════════════════════════════════════
1. ⛔⛔ 跑前已完成的两条守卫(lead 要求,**未过则本轮作废**)
═══════════════════════════════════════════════════════════════════
**守卫①(直接观测,LESSONS B-2):频谱确实被搬移了 Δf 吗**
```
纯音注入 f0=1 kHz,逐块流式,丢暂态后测主峰:
  Δf = 2/3/5/8/12/20/200 Hz ⇒ 实测搬移 2.00/3.00/5.00/8.00/12.00/20.00/200.00,**误差全 0.00**
  镜像(下边带 f0−Δf)抑制 **−64 dB**
  Δf = 0 ⇒ 主峰不动(恒等)✅
⇒ **守卫① 通过。⇒ 不是"看起来接上了",是直接量到了搬移量。**
```
**守卫②(阳性对照):Δf 取荒谬值 200 Hz ⇒ ΔMSG 必须明显变化**
```
⇒ 已列入主扫描作为一档(**非结论档,只作阳性对照**)
⇒ **若 200 Hz 档的 ΔMSG 与 Δf=0 无可判差异 ⇒ 器械没接进环路 ⇒ 本轮作废**
```

**⚠⚠ 而守卫① 顺带查出一条【必须写进限定】的缺陷:低频端镜像抑制差**
```
Hilbert FIR 长度 vs 镜像抑制(Δf=5 Hz)          群延迟
  257 taps: 120Hz **−12.7 dB** / 200Hz −23.4      2.67 ms   ⇒ ⛔ 不可用
  513 taps: 120Hz **−30.5 dB** / 200Hz −55.3      5.33 ms   ⇒ ✅ 本轮采用
 1025 taps: 120Hz −62.9        / 200Hz −61.3     10.67 ms   ⇒ 延迟代价过大
⇒ **本轮取 513 taps**;⚠ **120–200 Hz 段镜像仅 −30…−55 dB ⇒ 该段的频移不是干净单边带**
⇒ ⛔ 低频段结论须打折,与 F72(窄陷波做不到)/ F73.5(统计理论下限 179–283 Hz)**同一频段**
```

═══════════════════════════════════════════════════════════════════
2. ⚠⚠ 群延迟处置 —— 并对我自己用一次 D6-ai
═══════════════════════════════════════════════════════════════════
```
频移器带 5.33 ms 群延迟 ⇒ 若只有频移臂挂它,则"频移臂 vs 陷波臂"= **一次动两个变量**
⇒ 处置:**四臂【全部】挂同一条 FreqShifter 路径**,非频移臂 `df = 0`(恒等但同延迟)
   ⇒ 含 m0 臂 ⇒ 四臂**同一个被控对象**
```
**⚠ 但我⛔ 不声称"延迟在差里抵消"** —— 那正是 lead 的 D6-ai 被推翻的那个推理形式
(「共享一个常数 ⇒ 它在差里抵消」只有当两臂对它的**依赖相同**时才成立)。
```
⇒ 正确表述:四臂**同一被控对象** ⇒ 轮内比较干净;
   但环路延迟 8.0 → **13.33 ms** ⇒ **被控对象已与 r76/r78/r79 不同**
   ⇒ ⛔ r80 的 ΔMSG 绝对值**不得与前几轮并列**;⛔ 也不得假设两臂对延迟依赖相同
⇒ **免费对照(零成本)**:本轮"只陷波"臂(8×1/5,df=0)vs r78 的 C1(无频移器)
   **同配置、只差那条 5.33 ms 路径** ⇒ 差值即【延迟改变被控对象】的单独效应
```

═══════════════════════════════════════════════════════════════════
3. 矩阵
═══════════════════════════════════════════════════════════════════
```
固定  src=−20 dBFS(标称) · 修法关 · T_OBS=12s · T_low=−45 · f_cut=8k · 6 种子
      陷波配置 = **现状 8×1/5**(⛔ 本轮不动它:一次只问一件事)
      Hilbert 513 taps · n_cand=16(默认;F70 已证 16→48 零效应)

臂    m0        无 NHS,shifter df=0
      陷波      NHS,shifter df=0
      频移      无 NHS,shifter df=Δf
      合成-后   NHS → shifter(**陷波在前**;= Schroeder Fig.2 的位置:处理链→移频→功放)
      合成-前   shifter → NHS(**移频在前**;此时陷波看到的是【已搬移】的谱)
      ⇒ lead 点名"两种都便宜就都跑" ⇒ **都跑**

Δf ∈ {2, 3, 5, 8, 12, 20} Hz  + **200 Hz(阳性对照,⛔ 非结论档)**
   ⚠ 原文"最优" Δf = 峰谷平均间距 ≈ 4/T60 ⇒ T60=0.2 ⇒ 20 Hz;T60=0.5 ⇒ 8 Hz
   ⇒ 扫描区间**同时罩住两层的理论最优**

⛔ 砍  其余源电平 / 修法开臂 / T_OBS=6s / bw_oct 与 NN 的任何变化
```

═══════════════════════════════════════════════════════════════════
4. ⭐ 预注册假设(每条带证伪条件)
═══════════════════════════════════════════════════════════════════
**Hd1(本轮头条,⛔ 方向对我方不利,如实报)** · **次可加**:
`(ΔMSG_陷波 + ΔMSG_频移) − ΔMSG_合成 > 0`,且**显著大于仪器底 0.354 dB**。
证伪:次可加量 ≤ 仪器底 ⇒ **二者近似可加 ⇒ 我 F73.4 的机制推断错**,当场记 F 条,⛔ 不替自己圆。
⚠ 若出现**超可加**(合成 > 两者之和)⇒ 更重要,须原样报出并查成因。

**Hd2(lead 的互补性待判)** · 分层:`ΔMSG_频移` 在 **T60=0.5 层**应 ≥ **T60=0.2 层**
(依 F73:Δl 随 W·T60 上升 8.96 → 9.81)。
证伪:0.5 层不高于 0.2 层 ⇒ **lead 的互补性推断在本台架不成立**,如实报。
⚠ **⛔ 分层报,不跨 T60 平均。**

**Hd3(Δf 曲线的形状 —— lead 点名要看的那件)** · `ΔMSG_频移` 对 Δf 的曲线**有多平**。
```
✅ 若 Δf=5 Hz 已拿到 20 Hz 的大部分 ⇒ 可用性问题消失(工程实践的 3–8 Hz 够用)
⛔ 若必须 20 Hz 才有效 ⇒ 频移在语音上有大麻烦(同移 Hz ⇒ 谐波结构被破坏)
⚠ **⛔ 本轮源为噪声 ⇒ 不得在本轮碰"可听性"**,只报 ΔMSG–Δf 曲线;可听性须语音素材(缺口仍在)
```
**Hd4** · **顺序效应**:`合成-后` vs `合成-前` 的差。**⛔ 不预测方向**,只记录。
      机制备注(⛔ 非断言):陷波在前 ⇒ 被陷的分量随后被搬走,回到环路时已不在陷波处;
      移频在前 ⇒ 陷波看到的是已搬移的谱。⇒ 二者对"陷波是否命中"应有不同后果。
**Hd5** · **阳性对照**:Δf=200 Hz 档的 ΔMSG 必须与 Δf=0 有**可判差异**。
      证伪 ⇒ 频移器没接进环路 ⇒ **本轮作废**(守卫②)。

═══════════════════════════════════════════════════════════════════
5. 必报 + 纪律
═══════════════════════════════════════════════════════════════════
```
① ΔMSG 四列(陷波/频移/合成-后/合成-前),**分 T60 层**,⛔ 不平均
② **次可加量**逐格 + 仪器底标注(本轮头条)
③ ΔMSG–Δf 曲线(Hd3 的"平不平")
④ 阳性对照 200 Hz 档单列
⑤ 免费对照:本轮"只陷波" vs r78-C1 ⇒ 5.33 ms 延迟的单独效应
⑥ 低频限定:120–200 Hz 镜像仅 −30…−55 dB ⇒ 该段打折
```
⛔ 呈报 = 同条件内比较,仪器底 0.354 dB。⛔ 全程不用「上界/天花板/极限」指代任何参照臂。
⛔ **不得在本轮做 AEC 兼容性判断**(lead 已路由架构侧)。⛔ 不得碰可听性(噪声源)。
⛔ 结论行只能在看到数之后写。⛔ 不 commit。全部 [L2/宿主仿真],未经 critic 评审。
