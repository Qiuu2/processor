未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 08:0x(adaptive-dsp 第 6 实例)
轮次 = r85 · **量深度分布,不量计数**   输出 = r85_depth_out.txt(D6-j)

═══════════════════════════════════════════════════════════════════
0. lead 的假设(本轮唯一被检验的那条)
═══════════════════════════════════════════════════════════════════
```
r84 量到三件:preempt 1→535 ｜ n_notch 8→8 ｜ 执行/命令 1.22(执行速率正常)
⇒ lead 假设:**槽被反复抢占 ⇒ 陷波来不及【加深】**
   陷波是逐步加深的(depth0=−3,每步 −3,到 −18 要六步)
   抢占一次 ⇒ 深度归零重来 ⇒ 535 次抢占 ⇒ 大部分陷波停在很浅处
   ⇒ 终态"挂了 8 个"没变,但那 8 个**每个都更浅** ⇒ ΔMSG 6.00 → 2.50
```
**⇒ 判据:量【深度分布】,不量计数。**
```
修法臂显著更浅   ⇒ 假设成立 ⇒ 病根是**抖动**,不是**选点**
两臂深度分布相同 ⇒ 假设错,另有其物 —— ⛔ 当场记,不替 lead 圆
```
**⇒ 若成立,问题性质就换了:不是"名额给谁",是"给了之后能不能待够久" ——
   而这两个问题的修法完全不同。**

═══════════════════════════════════════════════════════════════════
1. ⛔ 两条限定(lead 点名,写死)
═══════════════════════════════════════════════════════════════════
```
① **只有 1/6 条种子出现抖动** ⇒ 无论结果如何,⛔ **不得当趋势**
   ⇒ 目标是【搞清机制】,不是【得到量级】
   ⇒ lead 今晚已两次栽在"把单条种子读成趋势"上,⛔ 不重犯
② 若假设成立,还须答一句:**为什么只有这一条种子?**
   ⇒ 答不出 ⇒ 该机制只在这条种子上成立,⛔ 不得推广
```

═══════════════════════════════════════════════════════════════════
2. 测什么
═══════════════════════════════════════════════════════════════════
在**同一个 G**(r76 该格终点 G)上跑两臂,各记:
  ① 终态每个非 FREE 槽的**实际 depth**(不是 target,不是 max_depth)⇒ 分布 + 中位 + 总和
  ② 每次抢占时**被抢走那个槽已压到多深**(`preempt_log`:f_old / depth_old / target_old)
  ③ 每槽被抢占次数
遥测已加进 nhs.py(**纯计数**),**逐位等价已证:新旧版 12/12 逐位相同**。

═══════════════════════════════════════════════════════════════════
3. 预注册假设
═══════════════════════════════════════════════════════════════════
He1 · 修法臂终态深度**显著更浅**(中位深度差 > 仪器底 0.354 dB)。
      证伪:两臂深度分布无可判差异 ⇒ **lead 的抖动假设不成立**,当场记 F 条。
He2 · 抖动种子(0.2/sd0)上,`preempt_log` 的 `depth_old` 应**集中在浅处**(接近 depth0=−3)
      ⇒ 说明槽在还没压深就被抢走。
      证伪:`depth_old` 多在 −12…−18 ⇒ 抢占发生在**已压深之后** ⇒ 损失机制不同。
He3 · ⛔ **不预测**其余五条种子会怎样 —— 它们 preempt=0,本就没有抖动可言。
      ⇒ 它们的作用是**对照**:若它们两臂深度也不同 ⇒ 深度差与抢占无关 ⇒ He1 的归因失效。

⛔ 本轮仍**不提修法**(lead 令未解除)。⛔ 结论行看到数之后写。⛔ 不 commit。
