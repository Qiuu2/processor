未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-05(adaptive-dsp 第 7 实例)
轮次 = r93 · **换被测量:连续的检出时刻 `t_det`**(critic r12 MAJOR-1 建议;lead 批)
输出 = r93_tdet_out.txt   ⛔ §2 跑前落盘,跑完不得改。

═══ 0. ⛔ 先写清上一轮判据坏在哪(⛔ 不重蹈)
```
r92 的判据「T_conv 严格落在 12 与 24 之间」——
  档位网格 T_OBS ∈ {12,24,48} ⇒ T_conv 只能取 {12,24,48,∞}
  ⇒ ⇒ **「严格落在 12 与 24 之间」在这个网格上不是一个可表示的结果**
  ⇒ 非证伪出口只剩 =12(我自己已判"不构成检验")与 =24(**等于 0.5 层的最小值**)
  ⇒ ⇒ ⭐ **H1 与 H2 穷尽了全部非证伪出口,而两者都证不了严格单调**
⇒ **形态:【量具的分度值,大于它要分辨的差】** ⇒ 判据**在写下的那一刻就不可能兑现**
```
**⇒ ∴ 本轮第一件事是换量具,⛔ 不是加密档位**(加密每格 500–1600 s 而仍受网格限制)。

═══ 1. 设计:被测量改为**连续量**
```
`t_det` := 在**固定长窗**内,逐帧 RMS **首次**越过 `ref + th_on` 的时刻(秒)
  分辨力 = 一帧 = 64/48000 = **1.33 ms** ⇒ 相对于 12–48 s 的差,**实质连续**
  ⇒ ⭐ 它直接就是「还差多少时间」的**单调代理**,且**不落在 {12,24,48} 格子上**
工作点:G = **解析临界增益 + 0.5 dB**(一格之上 ⇒ 确实失稳,⛔ 不在临界点上量)
       窗长固定 **48 s**(仅为容纳最慢的一格,⛔ 不作为判据档位)
       src=−20 dBFS · bw_oct=1/5 · duck 消融 · 臂 O(神谕 8 陷,T_low=999,LTI ⇒ 有解析真值)
格:T60 ∈ {0.20, 0.35, 0.50} × sd ∈ {0,1,2} = **9 格**(⇒ 同时给出**层内散布**)
```
**⇒ ⭐ 可表示性自查(上一轮就是栽在这)**:`t_det` 取值于连续区间 ⇒
**「0.35 层严格落在 0.2 层与 0.5 层之间」是一个【可表示】的结果**,而且可以不重叠地判。⇒ 判据成立的前提已满足。

═══ 2. ⭐ 判据(跑前写死)
```
按层取中位 t_det:med(0.20) · med(0.35) · med(0.50);并同时报**每层三格的极差**
H1 ✅ **单调被检验且通过**:med(0.20) < med(0.35) < med(0.50)
     ∧ **0.35 层与另两层的取值区间【不重叠】**(⇒ 否则"居中"读不出来)
H2 ⚠ **单调成立而 0.35 未与两层分开**(中位有序,但区间重叠)
     ⇒ 如实记「顺序成立,而**本设计分辨不出 0.35 是否自成一层**」,⛔ 不记为 H1
H3 ⛔ **证伪**:med 非单调(任一层序反转)⇒ **该推广作废**
⚠ 而 `t_det` 若某格在 48 s 内**从不触发** ⇒ 记 `未收敛`(marks.UNCONVERGED)
   ⇒ ⛔ **不得记 nan、不得跳过** ⇒ 该层的中位/极差按 `marks` 聚合护栏 **中止并报 FAIL**
   (⇒ 这正是 critic r12 MAJOR-2 的修法,本轮**第一次实用**)
```
**⛔ 而无论结果如何**:样本 = **9 格 × 1 plant 族 × 1 条轴** ⇒ **9/∞**,⛔ 不是全称;
量化词按被测元素命名,⛔ 不写"在 T60=x 上"。

═══ 3. 纪律
```
⛔ 不 commit;[L2/宿主仿真];⛔ 不据本轮改任何设计
⛔ 不重开「台架性质 vs ΔMSG 性质」那条链(critic r8 已判该链未被论证)
输出**必须**用 `marks.py` 的四记号,⛔ 不得出现 nan
