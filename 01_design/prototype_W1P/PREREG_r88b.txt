未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-04 16:1x(adaptive-dsp 第 7 实例)
轮次 = r88b · **非统计 plant 对照(换判别量后重做)**  输出 = r88b_plant_out.txt(D6-j)
⛔ 一版一文件:**不覆盖 `PREREG_r88.txt`**(那一轮闸门未过、主对比未起,记录原样保留)。
⛔ §3/§4 在主对比起跑前落盘,跑完不得修改。**lead 批:一轮,硬边界。**

═══════════════════════════════════════════════════════════════════
0. r88 为什么不过,以及这一版换掉了什么
═══════════════════════════════════════════════════════════════════
```
r88 闸门 = 用【实测统计量】(σ_dB ∧ 临界点密度)判"我造的 plant 是不是非统计的" ⇒ **2/6,未过**
两条已查明的成因:
 ① ⛔ **判别量坏**:σ_dB 的门 1.0 dB < 统计 plant 自己跨六种子的散布 **1.42 dB**
    ⇒ 该判据在任何 plant 上都判不出东西(与 is_howling / 斜率判据同型:**被证无分辨力**)
 ② ⛔⛔ **阳性对照本身选错了**(这一条是我设计错,不是运气差):
    booth 14 m³ 在 120–280 Hz 的模态重叠 M 跨 **0.69 → 3.77**(T60=0.2)/ **0.28 → 1.51**(T60=0.5)
    ⇒ **它自己就横跨 M=1** ⇒ 它不是一个"模态场",是一个过渡场 ⇒ 拿它当阳性对照,本就不该必然命中
    ⇒ 而这解析地预测了实测的 T60 分裂:T60=0.2 → 0/3 有差异;T60=0.5 → 3/3 有差异 ✅
⇒ ∴ 本版换掉的是 **判别量 + 阳性对照的选法**,⛔ 不是把门放宽(lead 写死的判据)
```

═══════════════════════════════════════════════════════════════════
1. ⭐ 新判别量:**解析模态重叠 `M(f)`** —— 分辨力【先验可证】
═══════════════════════════════════════════════════════════════════
```
M(f) = (4πV f²/c³)·(2.2/T60)      ← 解析、连续、**零采样噪声**
判据锚点:M<1 = 孤立模态 ｜ M=2.74 = k2000 的 Schroeder 判据 ｜ M=10.96 = k4000 的
⇒ ⭐ 它满足 lead 的硬条件:**跑之前就能算出每个 plant 落在哪一侧**,不依赖任何一次仿真
```
**⇒ 阳性对照 plant 因此改为【按 M 选出来的】,而不是"随手取一个小房间":**
```
P_mod9 :L = 2.2 × 2.1 × 1.95 m ⇒ **V = 9.01 m³**,T60 = **0.5**
        解析 M(120…280) = **0.18 … 0.97,全段 < 1** ⇒ **构造上就是模态场**(已复算,全频点验证 True)
P_conf :L = 5.8 × 4.6 × 3.75 ⇒ V = 100 m³,T60 = 0.5 ⇒ M = 1.97 … 10.74(**过渡→重叠**)
P_stat :clrig 噪声 RIR(现行台架,构造上即统计场)
```
**⚠⚠ 已声明的硬限定(⛔ 不藏)**:
```
T60=0.2 **没有**可行的阳性对照 —— 要全段 M<1 需 V < **3.7 m³**(壁橱,非房间)
⇒ ∴ 阳性对照**只在 T60=0.5 的三条种子上做**
⇒ ⇒ **T60=0.2 那三条的"无差异"因此【无阳性对照背书】,⛔ 不得读作阴性结论**(单列)
```

═══════════════════════════════════════════════════════════════════
2. ⭐⭐ 而有一条解析结论**已经不需要仿真**(先写下来,免得被本轮结果盖过)
═══════════════════════════════════════════════════════════════════
```
反解「整条 120–280 Hz 都满足 M<1」所需的房间:
   T60=0.2 ⇒ V < 3.7 m³   T60=0.5 ⇒ V < 9.3 m³   T60=1.0 ⇒ V < 18.6 m³
⇒ **没有任何现实房间满足** ⇒ 那一段在【任何现实会议室里】都是**过渡区**,从来不是"几个驻波"
⇒ ∴ 「那一段是模态区还是统计区」这个问法本身是错的 —— 它两者都不是
⇒ 而 M(f) 解析已知且连续 ⇒ 把分界画在 M=2.74 还是 10.96 **不改变场本身**
⇒ ⇒ **常数之争没有物理内容**(与 E-6 改写一致:要定的是我们设计需要的 M)
```
**⇒ 本轮仿真要答的因此只剩 Q2:【NHS 在模态场与统计场里的行为是否不同】。**

═══════════════════════════════════════════════════════════════════
3. ⭐ 闸门(**中止条件**,exit(1))
═══════════════════════════════════════════════════════════════════
自查句:「失败时会阻止什么?」⇒ **阻止主对比启动。**
```
G1 · 解析先验(**零仿真、零噪声**):
     P_mod9 须 **全频点 M(120…280) < 1**  ∧  P_conf 须 **M(120) > 1**
     ⇒ 不成立 ⇒ 阳性对照不是模态场 / 主对比臂不是过渡场 ⇒ exit(1)
G2 · 配置断言(同 r88:开关全默认、T_OBS < lift_after_s、两 plant 同 T60/延迟/带限/归一化)
⛔⛔ **不再用实测统计量当闸门** —— σ_dB 已被证无分辨力(§0-①);
     它仍**照常测、照常报**,但降级为**诊断**,⛔ 不阻止任何事
⛔⛔ 而【会议室尺寸上两 plant 无差异】**仍不是闸门,是结果**(把它设成闸门会剔掉最有价值的结局)
```

═══════════════════════════════════════════════════════════════════
4. ⛔ 预注册假设(跑完不得改)
═══════════════════════════════════════════════════════════════════
```
固定:src=−20 dBFS(标称)· T_OBS=12 s · bw_oct=1/5 · NN=8 · T_low=−45 · duck 消融 · 修法全关
每 plant:m0(无 proc)+ NHS 自选 ⇒ ΔMSG = m_NHS − m0,**逐种子**
```
**Hq1 · 主问(Q2)**:`P_conf` vs `P_stat` 的逐种子 δ。**⛔ 不预测方向。**
**Hq2 · ⭐ 阳性对照(仅 T60=0.5 三条)**:`P_mod9` vs `P_stat` **必须**出现可判差异。
```
⛔ 若 P_mod9 也无差异 ⇒ **本对比无分辨力** ⇒ Hq1 的"无差异"不得读作阴性结论 ⇒ 整件如实记为"器械无分辨力"
✅ 若 P_mod9 有差异 ∧ Hq1 在 T60=0.5 上 0/3 可判 ⇒ **「NHS 行为不随场的性质而变」在该档成立(B-1)**
⚠ T60=0.2 三条无阳性对照 ⇒ **单列,不并入 B-1 结论**
```
**Hq3 · 低频行为**:逐 plant 报挂陷频点 <300 Hz 的个数与挂陷总数;⛔ 与 ΔMSG 分开报。
**Hq4 · ⛔ 不得伤及**:某 plant 下挂陷塌到 0 ⇒ 该格标"退化",单列不混入统计。
**判读**:两臂同锚同栅格 ⇒ δ 恒为 0.5 整数倍 ⇒ |δ|=0.50 只是一格;δ=0.00 ⇒ **不可判**,⛔ 不得读作"没变化";
逐种子报,⛔ 不得只报聚合。

═══════════════════════════════════════════════════════════════════
5. 纪律(lead 写死的硬边界)
═══════════════════════════════════════════════════════════════════
```
⛔ **本轮之后不再加轮**,无论结局
⛔ 若本轮闸门又不过 ⇒ 走选项 ③:并进"须实测真实房间",零成本,⛔ 不许再换第三把尺子
⛔ 不 commit ⛔ 不 spawn ⛔ 不联系其他 teammate
全部 [L2/宿主仿真](M(f) 那部分为 [L3/解析]),未过 critic;setsid+nohup,⛔ 不用 timeout 包
```
