未经 critic 评审 —— r66d 的【判读件】。[L2/宿主仿真]。第 5 实例,2026-08-03。

════════════════════════════════════════════════════════════════════
⛔ 零、结论:**域错配问题【本件回答不了】** —— 我的探针有缺陷,自曝在前
════════════════════════════════════════════════════════════════════
表面结果:电平域 ≤2 槽升幅**最大 18.52 dB ≥ 门 18.0** ⇒ 按我跑前写死的 Hd1,判"域错配成立"。
**⛔ 但这个判定不成立,理由是我的采样方式错了:**

`nhs.py:242` `self.tracks = [Track() for _ in range(P.NT)]` ⇒ **轨对象是预分配、【反复复用】的**
(`:488` `tr.papr_hist = [o['papr']]` 就是一次就地重生)。
而我用 `lv_hist.setdefault(id(tr), []).append(tr.last_level)` 归集电平轨迹
⇒ **同一个 `id(tr)` 会把【多条先后不同的逻辑轨】的电平串成一条**
⇒ 前一条轨末尾 → 后一条轨开头 的落差会被当成"≤2 槽内的升幅"。
> **⇒ 那个唯一的 18.52(33 个样本里的 1 个,单条种子 0.2/sd2 @Δ=+3)极可能正是这样拼出来的。**
> **⇒ 我自己写的 Hd1 判据【太弱】:它只看最大值,n=33 里 1 个点就能触发**
>   —— 这正是我一直在别处执行的「n=1 不得外推」,这次犯在我自己的预注册上。

**⚠ PAPR 域的数【不受此影响】**:它读的是 `tr.papr_hist`,该列表在每次重生时被就地重置
⇒ 每个样本都是**轨内**轨迹。⇒ `r66c` / F43.3 的 PAPR 域结论(0/1692,最大 10.44)**仍然成立**。

════════════════════════════════════════════════════════════════════
一、能站住的部分(两域都远达不到门)
════════════════════════════════════════════════════════════════════
```
PAPR 域 (n=1692,采样有效):最大 10.44 / p95 2.64 / 中位 0.19 dB   门 18.0 ⇒ 达门 0/1692
电平域 (n=33,**采样有缺陷**):最大 18.52 / p95 5.59 / 中位 1.85 dB  门 18.0 ⇒ 达门 1/33
两域最大值之比 1.77×;中位之比 ~9.7×
```
> **⇒ 无论用哪个域,`R_RISE = 18 dB` 都定在了实测分布的【尾巴之外】。**
> **⇒ 这一条不依赖域的问题,是本件唯一可以承重的结论。**
> ⛔ **而"域错配是不是真因"= 未决**。要答它须重做:按**轨的生命周期**分段采集电平
> (用轨的诞生序号而非 `id()`),并把 n 提到与 PAPR 域同量级。

════════════════════════════════════════════════════════════════════
二、给下一轮的两条(避免同型)
════════════════════════════════════════════════════════════════════
1. **凡按对象归集时间序列,不得用 `id()` 作键** —— 对象池会复用地址。须用**逻辑实体的诞生序号**。
   (与本项目已记的「跳槽 × 计数」同族:两个各自正确的机制,在咬合处产生第三种行为。)
2. **判据不得只看极值** —— 极值对**单个伪影**零抵抗力。
   我跑前写 Hd1 时用了 `max(LV) >= R_RISE`,而 n=33。**下次此类判据须同时给 n 与分位数,
   并要求命中率而非命中存在性。**

⛔ 未经 critic 评审。
