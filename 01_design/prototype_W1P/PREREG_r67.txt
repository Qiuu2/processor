未经 critic 评审 —— 跑前预注册。PREREG_TS = 2026-08-03 19:xx(adaptive-dsp 第 5 实例)
轮次 = r67  输出 = r67_rrise_sweep_out.txt  日志 = r67_run.log  (D6-j 路径唯一)
被测:nhs.py@31decc8e8d07e085 clrig.py@8ad47ce8d260dd18 howl_detect.py@fd63e901f2d8be33
      msg_meter.py@a0c16fd22b29f083 r57_bandlimit.py@74036010b514080d

对象 = lead 裁定第 1 项:**`R_RISE` 扫描**(候选主因)。
`R_RISE ∈ {18.0(现值), 10.0, 6.0, 3.0}`,其余三个门不动(N_RISE=2/S_PLAT=2.0/MIN_PLAT=3)。

═════════════════════════════════════════════════════════════════
⭐ 两阶段设计(先测可达性,再谈修法 —— lead 点名要保持的次序)
═════════════════════════════════════════════════════════════════
**阶段 A(便宜,固定 G,不做增益扫描)**:每档只数三个量 ——
  `rapid_onset` 触发次数 / GROWTH 入选数 / 挂陷数。
  **门控**:若某档 `rapid_onset` 触发数仍为 0 ⇒ **该档不进阶段 B**(测 ΔMSG 无意义)。
  若**全部四档**都为 0 ⇒ 本轮到此为止,如实报「下调 R_RISE 至 3.0 仍不触发」。
**阶段 B(贵)**:只对**阶段 A 有触发**的档跑 ΔMSG 阶梯 + §4 选点轴 + 误报侧。

**⚠ 算力截断(lead 要求显式 log,不许静默省略)**:
阶段 B 的阶梯**截断为 `{6, 12} s`(全部六条种子)**,不跑 24/48。
  理由:①主统计是**同档配对差** `δ(T)`,与 r64 在**相同两档**上对齐即可;
        ②r64 已证 T60=0.5 三条在 48s 仍不收敛 ⇒ 加长不会带来收敛,只加成本;
        ③本轮问的是**方向**(下调 R_RISE 有没有用),不是绝对值。
  **⇒ 代价:T60=0.5 层的 δ 只在未收敛档上成立,须逐条标注。⛔ 不得据此报绝对值。**

═════════════════════════════════════════════════════════════════
臂与对齐(与 r64 逐项一致,否则不可相减)
═════════════════════════════════════════════════════════════════
六条种子 / f_cut=8k / bw_oct=1/5 / depth=−18dB / **8 槽全空** / T_low 默认 −45 /
STEP=0.5 / frame=64 / nfft=2^18 / 源 = 1e-3·N(0,1) 按 seed。
  臂 m0  proc=None(**与 R_RISE 无关**)⇒ 每 (种子,档位) 只算一次并**对 r64 做复现核对**
  臂 N   NHS 自选 + duck 不消融
  臂 Na  NHS 自选 + duck 消融      ← **兜底消融列,硬性项**
  臂 O   **本轮不跑**:`T_low=999` ⇒ 无候选过门 ⇒ 无轨 ⇒ 无分类 ⇒ **R_RISE 对它恒无影响**
         (r63/r65 已实测 N2_lvl=0)。上界列引用 r64,**并标明来源轮次**。

**运行时不变量(双向)**:臂 N/Na `N2_lvl>0 ∧ 挂陷>0` ⇒ OK;零动作且 |ΔMSG|≤STEP ⇒ ZERO_ACT
(**合法的不利结果,照常计入**);零动作却有收益 ⇒ FAIL(B-1 形状,该列作废)。

═════════════════════════════════════════════════════════════════
§4 选点轴(零额外算力)+ ⑤ 误报侧
═════════════════════════════════════════════════════════════════
  **top1_hit**  最高神谕临界点是否落在某个 NHS 陷波的 BW/2 内 ← **单独提出来报**(lead 要求)
  hit@BW/2      NHS 所挂陷波中落在任一 pick 的 BW/2 内的比例(= **准确率**)
  cover@BW/2    神谕 top-8 picks 被覆盖的比例(= 召回率)
  |Δf|_min      每个 NHS 陷波到最近 pick 的距离(Hz)
**⑤ 误报侧(主)= `1 − hit@BW/2`** —— 挂在**非临界点**上的陷波 = 纯音质代价、零收益。
**⑤ 误报侧(次)= `c8_ext` / `c8_howl`**(C8-② 事后甄别)
  **⚠ 已知偏差,必须同段声明**:F40.4 实测该探针在深度失稳环路上 **30/30 全判「外部源」**
  ⇒ **它作为误报代理本身可疑** ⇒ **两个都报,⛔ 不拿 c8 单独下结论。**

═════════════════════════════════════════════════════════════════
预注册假设与证伪(⛔ 结论行不得预写进脚本)
═════════════════════════════════════════════════════════════════
He1 · 下调 `R_RISE` 会让 `rapid_onset` 开始触发。
      预测:`R_RISE=3.0` 档触发数 >0(实测 PAPR 域 p95=2.64,门 3.0 恰在 p95 之上)。
      证伪:**四档全 0** ⇒ 绑定约束不是 `R_RISE`,而是**另一个合取项**
            ⇒ 须回头查 `N_RISE=2` 的窗口约束(r66c 已测平台项 1435/1692 通过,故嫌疑在 N_RISE)。
He2 · 触发变多 **≠** 检出变好。
      预测:随 `R_RISE` 下降,`GROWTH 入选数` 上升而 `top1_hit` **不必然**上升。
      判读:**必须同时报 ③ΔMSG 与 ⑤误报侧**;若 ΔMSG 不升而误报升 ⇒ 该档**有害**,如实报。
      ⛔ **禁止**只报触发数上升就称"修好了"。
He3 · 若某档 ΔMSG 上升,须能在选点轴上看到对应的机制(`top1_hit` 或 `cover` 上升)。
      证伪:ΔMSG 升而选点轴全不动 ⇒ **收益不是来自选点** ⇒ 先查 duck(消融列)再下结论。
He4 · 复现对照:臂 m0 与 r64 对应格**逐位相同**(m0 与 R_RISE 无关)。
      证伪 ⇒ 台架漂移,整轮作废,先按操作事故处理(D6-j)。

命名(D6-t):`ΔMSG_实测@带限8k_NHS自选_有duck_R{值}_[L2/宿主仿真]`;⛔ 无裸 ΔMSG。
⛔ 不报跨层均值(M-2)。第一行「未经 critic 评审」。不 commit。全部 [L2/宿主仿真]。
