# D3 输入通道链 / D4 输出通道链 —— 设计 v0.1

```
⛔ 门禁状态:未过门(2026-08-04)
   未经独立 critic 评审。禁止 release / 冻结 / 被其他工件引用为依据 / 对外承诺。
   归档性 commit 须带 [未过门] 前缀。
作者   : channel-dsp(第 1 实例)
前置件 : D34_FIXEDPOINT_CONVENTION_v0.1.md(定点约定,同样未过门)
配套件 : 01_design/d34_chain/{PREREG_D34_r1.txt, _r2_addendum.txt, _r3_addendum.txt}
         01_design/d34_chain/{d34_analysis.py, results_d34_r2.txt, results_d34_r3.txt,
                              results_d34_r2_killmatrix.txt}
         01_design/D3D4_CONSTANT_LEDGER_v0.1.md(自定常数台账)
```

> **⚠ 一处操作失误,如实记(收尾机械核盘抓到)**:**r1 没有落盘** —— 我只把它跑到了控制台,
> 因此**不存在 `results_d34_r1.txt`**(本件初稿的配套件清单曾写有该文件,已改正)。
> r1 产出的两条(EXP-2b 门增益两侧均 0.00 dB;EXP-4b 峰值落在 20.0 Hz)
> **逐字保存在 `PREREG_D34_r2_addendum.txt` §1**(该件写于 r2 跑之前),
> 且在 r2/r3 的退役项输出里仍可复现。
> ⛔ **我不补造一个 r1 文件** —— 那会是在发明记录。
> ⇒ 这是团队纪律 D6-j 的同族:输出路径不仅要唯一,**还要真的存在**;
>   而「文档声称的文件 vs 仓库实际」正是本项目机械可查的那一类偏差。

> **⚠ 参数字典(D2)归 `system-architect`,是单一事实源。**
> 本件所有参数表是**提案**,须与 D2 对齐;凡与 D2 冲突,以 D2 为准并报 lead 路由。
> 本件不新建一套字典。

---

## 0. 一页纸

**D3 输入链(我方确定性部分)**
```
① 极性反转 → ② 前置增益(数字 trim) → ③ HPF
   → 〔AEC 插入点 ✳〕 → 〔ANC 插入点 ✳〕
→ ④ 噪声门/扩展器 → ⑤ 压缩器 → 〔AGC 插入点 ✳〕
→ ⑥ PEQ ×8 → 〔AFC 陷波器组 ✳〕
→ ⑦ 延时 → ⑧ 保护限幅 → (去矩阵)
```
**D4 输出链**
```
(矩阵求和) → ① 输出增益 → ② 极性反转 → ③ 分频(HPF+LPF) → ④ PEQ ×10
→ ⑤ 线性相位 FIR → ⑥ 延时 → ⑦ 输出限幅 → ⑧ 音箱保护限幅(RMS+峰值) → ⑨ 斜坡静音 → DAC
```
**✳ = 耦合点,归 `adaptive-dsp` / `system-architect`。⛔ 本件只给我这一侧的约束与理由,不替它们定。**

| 承重结论 | 值 | 依据 |
|---|---|---|
| D4 中**分频必须在 PEQ 之前** | 阻带激励下省 **31.14 dB** 链内电平 | [L2/宿主实测 EXP-1a] |
| 两者总传函严格相等 | 差 **2.8e−14 dB** ⇒ 顺序**不能**用传函选 | [L2/宿主实测 EXP-1c] |
| **LR 分频的求和极性由阶数决定** | 阶数 mod 4 = 0 ⇒ 同相;= 2 ⇒ **须反相** | [L2/宿主实测 EXP-3] |
| Q4.27 下 LR 求和平坦度 | 最坏 **0.0036 dB**(规格 ±0.3 dB) | [L2/宿主实测 EXP-3b] |
| **LR8 分频器低频群延迟** | fc=80 Hz 时 **15.555 ms**,已超 12 ms 全链预算 | [L2/宿主实测 EXP-4,两轨] |
| 转换器往返延迟 | ADC 22.9844/fS + DAC 25/fS = **0.99967 ms** | [L2/厂家 datasheet] |
| FIR 抽头受延迟预算硬约束 | L=64 时 **1024 tap 必超预算**;512 tap 余 3.011 ms | [L3/解析 + L2/厂家锚点] |
| D3+D4 全链量化噪声底 | **−158.88 dBFS**(28 个量化器) | [L3/解析,基于任务一 [L2/实测] 单节值] |
| PEQ 增益范围硬上限 | **±15 dB**(Q4.27 包络 +18.09 dB) | 前置件 §3.2.1 |
| PEQ 的 Q 可放到竞品口径 | Q ∈ [0.02, 50] 下 max\|b\| = **5.2229** < 16 ✓ | [L2/宿主实测 EXP-6] |

---

## 1. D3 输入链:链序推导(逐处理由)

> **⛔ 不按 PRD §二 的枚举顺序推(假设 A9)。** PRD §二 是功能清单,不是链序。
> 对标基线:竞品 DSP Controller V1.3 实测链序 = `EXP → COMP → AGC → EQ → AFC`
> [L4/竞品明文配置]。**本件与它同向,但有两处刻意不同,逐条给理由。**

### ① 极性反转 —— 最前
**理由(硬)**:极性只在**跨通道求和**时才有意义(单通道内幅度检测对极性不敏感)。
链上最早的求和是 AEC 参考路径与 automixer ⇒ 极性必须在它们之前确定。
放最前是"确定得最早"的位置,且不引入任何代价(乘 ±1,可并入 ② 的增益)。

### ② 前置增益(数字 trim)—— 紧随
**理由(硬)**:下游**所有阈值型参数都是绝对电平量** —— 门阈值、压缩阈值、
AFC 的 `fb_thr` / `panic_thr`、限幅阈值。若增益在它们之后,调一次 trim 就把
全部阈值的实际作用点移走了,现场调试会陷入"改一个动全部"。
⇒ **增益必须在第一个用绝对电平做判据的模块之前。**

### ③ HPF —— 动态处理之前,且**必须是独立模块**
**理由(实测)**:低频隆隆声(手持噪声、空调、桌面震动、近讲效应)会驱动动态处理器的
侧链检测器,造成误开门 / 误压缩。

实测(EXP-2a,合成素材 = 1 kHz 猝发串 −30 dBFS + 45 Hz 隆隆 −20 dBFS):
| 链序 | 静默段侧链检测器读数 |
|---|---|
| HPF 在动态**之前** | **−33.42 dBFS** |
| HPF 在动态**之后** | **−23.01 dBFS** |
| **分离度** | **10.41 dB** |

**⚠ 适用范围(EXP-2d 实测,⛔ 不得省略)**:分离度强烈依赖隆隆声频率、HPF 阶数与 fc。

| 隆隆声 | 2 阶 fc=60 | 2 阶 fc=80 | 2 阶 fc=120 | 4 阶 fc=60 | 4 阶 fc=80 | 4 阶 fc=120 |
|---|---|---|---|---|---|---|
| 20 Hz | 19.12 | 24.05 | 30.90 | 37.27 | 44.18 | **48.32** |
| 30 Hz | 12.30 | 17.11 | 24.05 | 24.05 | 33.66 | 44.18 |
| 45 Hz | 6.19 | 10.41 | 17.11 | 10.41 | 20.01 | 33.66 |
| 60 Hz | **3.01** | 6.19 | 12.30 | 3.01 | 10.41 | 24.05 |
(单位 dB,分离度 = HPF 在后 − HPF 在前)

⇒ **60 Hz 隆隆 + 2 阶 60 Hz HPF 只有 3.01 dB** —— 这条理由**不是无条件成立的**。
⇒ 默认值必须让 fc 明显高于目标隆隆声频段,或用 4 阶。见 §3 参数表。

**⭐ 而且 HPF 必须是【独立模块】,不能只做成 PEQ 的一个 band type**
(竞品把高低通做成 EQ 的类型之一 [L4/竞品手册])。
**理由**:本设计把 PEQ 放在动态处理**之后**(见 ⑥)。若 HPF 只是 PEQ 的一段,
它就落在动态之后 ⇒ **完全失去保护侧链的作用**,而界面上看不出任何区别。
⇒ 这是本件相对竞品的一处刻意不同,**代价 = 多一个模块(1 个 biquad)**。

### 〔AEC 插入点 ✳〕—— 我方给出的**约束**,不是位置
**约束(硬,交 adaptive-dsp / 架构侧)**:
> **AEC 之前只允许【线性时不变、且系数不随时间改变】的模块。**

理由:AEC 的自适应滤波依赖"参考信号 → 麦克风信号"的线性关系。
门 / 压限 / AGC / AFC 都是**非线性或时变**的 ⇒ 放在 AEC 之前会破坏该假设,
使 AEC 无法收敛或残余回声升高。
- ③ HPF 是 LTI 且系数静态 ⇒ **可以**在 AEC 之前,且有益(去掉低频能量利于收敛)
- ② 增益若**运行时可变**(推子拖动),严格说也是时变的 ⇒ 建议增益变化时给 AEC 一个
  `gain_changed` 事件(控制面信号),由 AEC 侧决定是否冻结/重收敛。**该信号的定义归架构侧。**

⛔ **AEC 的具体插入位置由 adaptive-dsp 与架构侧定,本件不定。**

### 〔ANC / ANS 插入点 ✳〕
非线性谱域处理 ⇒ 同样须在 AEC **之后**(否则残余回声被非线性化,AEC 更难收敛)。
⛔ 位置由对方定。

### ④ 噪声门 / 扩展器 —— 在压缩器之前
**理由**:压缩会**抬高低电平段的相对电平**(动态范围被压小,本底噪声被抬起)。
若门在压缩之后,门限的实际作用点会随压缩量漂移 ⇒ 用户每改一次压缩比都要重调门限。
⇒ 门在前,它面对的是未被压缩的原始动态,门限含义稳定。
(与竞品同向:`EXP → COMP`)

### ⑤ 压缩器
### 〔AGC 插入点 ✳〕
惯例在压缩器之后:压缩管**快峰值**,AGC 管**慢电平**;两者时间常数相差 1–2 个数量级,
串起来互不打架。竞品亦为 `COMP → AGC` [L4/竞品明文配置]。⛔ 位置由对方定。

### ⑥ PEQ ×8 —— 在动态处理**之后**
**理由(两条)**:
1. 会议系统的 PEQ 主要是**房间修正与反馈前的音色整形**,是"最终音色"。
   若放在压缩器之前,PEQ 的提升会触发压缩、又被压回去 ⇒ 用户拧 EQ 却听不到相应变化。
2. 下游的 AFC 需要看到**接近最终**的信号才能正确定位啸叫频点;
   若 PEQ 在 AFC 之后,AFC 抑制的频点与实际进入扩声环路的频点不一致。

**代价(必须显式接住)**:PEQ 的提升**不再受压缩器保护** ⇒ 可能把电平推到削波。
⇒ 由 ⑧ 保护限幅兜底,**这就是 ⑧ 存在的理由**。
(与竞品同向:`... → EQ → AFC`)

### 〔AFC 陷波器组 ✳〕
靠链末,接近最终信号。⚠ 其**检测 tap** 已由 W1-A 定为"反转回陷波器组入口"
—— 该件未过门,⛔ 本件不引其结论,仅记录存在耦合。⛔ 位置由对方定。

### ⑦ 延时
**约束**:必须在 **AEC 之后** —— 输入延时会改变麦克风路径的时间对齐,
放在 AEC 之前等于给 AEC 增加一段未知的固定时延,浪费其尾长预算。
延时是纯搬运,不改变信号内容 ⇒ 在 AEC 之后的任意位置皆可,放 ⑦ 是为了紧邻链末便于理解。

### ⑧ 保护限幅 —— 最末
**理由(硬)**:矩阵是**多输入求和**。若各路输入不受约束,求和后必然溢出。
⇒ 保护限幅保证进入矩阵的每一路都不超过约定电平,**这是矩阵求和不溢出的前提**。
同时它是 ⑥ 的兜底(见上)。
**它与 ⑤ 压缩器职责不同**:压缩器是音乐性动态控制(阈值低、比率适中、拐点软);
保护限幅是**砖墙**(阈值贴近满刻度、比率 ∞、attack 极快),唯一目的是防下游溢出。

---

## 2. D4 输出链:链序推导

### ① 输出增益 → ② 极性反转
同 D3 的理由(阈值锚定 / 求和前确定极性)。

### ③ 分频(HPF + LPF)—— **在 PEQ 之前**
**⚠ 先说一件我自己搞错过的事(留痕)**:我最初用「链内峰值增益」来论证这个顺序,
**实测两种顺序的链内峰值完全相同(12.95 dB vs 12.95 dB,差 0.00 dB)⇒ 论证被证伪**。
原因:那是"max over 全频"的宽带测度,而两种顺序的**最后一个节点就是输出**,
输出相同 ⇒ 最大值必然相同。**⛔ 我没有就地改写论证,而是重新注册了一个被测量。**

**重新注册后的实测(EXP-1,窄带激励)**:
配置 = PEQ 在 40 Hz 提升 +12 dB;分频 = LR4 高通 fc = 120 Hz(⇒ 40 Hz 落在阻带);激励 −20 dBFS。

| 顺序 | 逐节电平(dBFS) | 最大节点电平 |
|---|---|---|
| PEQ 在前 | −8.00 → −27.14 → −46.28 | **−8.00** |
| **分频在前** | −39.14 → −58.28 → −46.28 | **−39.14** |
| | | **差 = 31.14 dB** |

**通带对照(1 kHz)**:两者差 **+0.002 dB** ⇒ 该测量**只在阻带有分辨力**,与预注册一致。

**⇒ 结论**:两者**总传函严格相等**(差 2.8e−14 dB),顺序**不能**用传函选;
选它的唯一理由是 **定点链内电平**:分频在前使被滤掉频段内的大信号**先被衰减**,
不会先被 PEQ 提升 31 dB 再扔掉 —— 而中间那一步若饱和,是**非线性的、后面的分频器救不回来**。
(与竞品同向:`XOver → EQ` [L4/竞品明文配置])

### ④ PEQ ×10
### ⑤ 线性相位 FIR —— 在限幅之前
FIR 为 LTI,位置不改传函。放在 ⑦ 之前的理由:**限幅必须是最后一道**,
且 FIR 的振铃(前冲/后冲)必须落在限幅的约束之内,否则限幅形同虚设。

### ⑥ 延时 —— 在限幅之前
用于扬声器物理距离对齐。纯搬运。放限幅之前(竞品同 `Delay → Limiter`)。

### ⑦ 输出限幅 → ⑧ 音箱保护限幅
**⑦** 砖墙限幅,保证 DAC 不削波。
**⑧** PRD §三.5,**双通道**:长期功率(RMS,保护音圈热失效)+ 短期峰值(保护位移超程)。
两者时间常数相差 2–3 个数量级,不能合成一个检测器。

### ⑨ 斜坡静音 —— 最末
静音必须在链末,保证任何情况下(包括前级失控)都能切断。**斜坡**(非硬切)防爆音。

---

## 3. D3 参数表(提案,待与 D2 对齐)

> 每行的 L 标见末列。**凡 [L4/我定的数] 的,全部进 `D3D4_CONSTANT_LEDGER_v0.1.md`。**

### ①② 极性 / 前置增益
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `polarity` | {正相, 反相} | — | 正相 | — | [L2/PRD §二.1] |
| `trim_gain` | −60 … **+20** | 0.1 | 0.0 | dB | 上限受 `CHDSP_DB_MAX_Q8=+24 dB` 约束 [L2/前置件] |
| `mute` | {关, 开} | — | 关 | — | [L2/PRD §二.1] |

### ③ HPF(独立模块)
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `hpf_en` | {关, 开} | — | **开** | — | [L4/我定的数] |
| `hpf_fc` | 20 … 500 | 1 | **80** | Hz | [L4/我定的数,依据 §1③ 的 EXP-2d 表] |
| `hpf_type` | {BW12, BW24, LR12, LR24} | — | **BW12** | — | [L4/我定的数] |

> **⚠ 默认值的取舍已量化**:2 阶 / 80 Hz 对 45 Hz 隆隆只有 **10.41 dB** 分离度;
> 4 阶 / 120 Hz 可达 **33.66 dB**,但会切入男声基频(85–180 Hz)的下沿。
> ⇒ 默认取保守档,**并把 EXP-2d 整张表交给集成商**,由现场噪声环境决定是否上调。

### ④ 噪声门 / 扩展器
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `gate_en` | {关, 开} | — | 关 | — | [L4/我定的数] |
| `gate_thr` | −80 … 0 | 0.5 | **−45** | dBFS | [L4/我定的数] |
| `gate_ratio` | 1.0 … 20.0 | 0.1 | 4.0 | :1 | [L4/竞品:比率 1:20 即等效噪声门] |
| `gate_attack` | 0.1 … 100 | 0.1 | 1.0 | ms | [L4/我定的数] |
| `gate_hold` | 0 … 1000 | 1 | 50 | ms | [L4/我定的数] |
| `gate_release` | 10 … 3000 | 1 | 200 | ms | [L4/我定的数] |
| `gate_hysteresis` | 0 … 20 | 0.5 | 3.0 | dB | [L4/我定的数,防抖] |

> **⚠ `gate_thr` 与 `hpf_fc` 是耦合的**(EXP-2c 实测):HPF 前置带来的门行为改善,
> 只在门限落在「HPF 前后两个检测器读数之间」时才出现。
> 在本件的测试素材上该区间是 **[−27, −10] dBFS**;⛔ **该区间随素材电平变化,
> 不是普适常数** —— 它说明的是**耦合关系存在**,不是"门限该设 −27"。
> ⇒ **进 D2 时须把这条耦合写成注记**,不能让两个参数各自独立地被调。

### ⑤ 压缩器
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `comp_en` | {关, 开} | — | 关 | — | — |
| `comp_thr` | −60 … 0 | 0.5 | −20 | dBFS | [L4/我定的数,= 标称电平] |
| `comp_ratio` | 1.0 … 20.0 | 0.1 | 3.0 | :1 | [L4/竞品:比率 1~20] |
| `comp_knee` | 0 … 20 | 0.5 | 6.0 | dB | [L4/我定的数] |
| `comp_attack` | 0.1 … 200 | 0.1 | 10.0 | ms | [L4/我定的数] |
| `comp_release` | 10 … 3000 | 1 | 100 | ms | [L4/我定的数] |
| `comp_makeup` | 0 … +20 | 0.1 | 0.0 | dB | 上限同 `trim_gain` |
| `comp_detector` | {峰值, RMS} | — | RMS | — | [L4/我定的数] |

### ⑥ PEQ ×8(每段独立)
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `band_en[k]` | {关, 开} | — | 关 | — | — |
| `band_type[k]` | {PEQ, 低架, 高架, 低通, 高通} | — | PEQ | — | [L4/竞品:类型 PEQ/高低架/高低通] |
| `band_freq[k]` | 20 … 20000 | 1/24 oct | 1000 | Hz | [L2/PRD §一.1 频响范围] |
| `band_gain[k]` | **−15 … +15** | 0.1 | 0.0 | dB | **硬上限**,见下 |
| `band_q[k]` | **0.02 … 50** | — | 1.41 | — | [L4/竞品] + [L2/实测 EXP-6 已验证安全] |

> **⭐ `band_gain` 的 ±15 dB 是硬约束,不是习惯**:系数格式 Q4.27 的包络是
> **架式增益 ≤ +18.089 dB**(前置件 §3.2.1)。超过即 `chdsp_coef_from_f64()` 硬失败。
> ⇒ **建议 D2 把它锁死在 ±15 dB**,留 3 dB 余量。
> **⭐ `band_q` 放到竞品的 0.02–50 是安全的**:EXP-6 实测该范围内 PEQ 的
> max|b| = **5.2229**,解析上界 A² = 5.6234,均远小于 16。⇒ 台账 C2 由 🔴 降 🟡。

### ⑦ 延时
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `in_delay` | 0 … **100** | 1 样本 (20.8 µs) | 0 | ms | [L4/我定的数] |

### ⑧ 保护限幅
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `lim_thr` | −20 … 0 | 0.1 | **−1.0** | dBFS | [L4/我定的数] |
| `lim_release` | 10 … 1000 | 1 | 50 | ms | [L4/竞品:限幅仅阈值+释放] |
| `lim_lookahead` | {0, 1, 2} | — | **1** | ms | ⚠ **直接进延迟预算**,见 §5 |

---

## 4. D4 参数表(提案,待与 D2 对齐)

### ③ 分频
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `xo_hp_en` / `xo_lp_en` | {关, 开} | — | 关 | — | — |
| `xo_hp_fc` / `xo_lp_fc` | 20 … 20000 | 1 | 80 / 20000 | Hz | [L4/我定的数] |
| `xo_type` | {Butterworth, Linkwitz-Riley, Bessel} | — | Linkwitz-Riley | — | [L4/竞品] |
| `xo_slope` | {12, 24, 36, 48} | — | 24 | dB/oct | [L4/竞品给 6…48;**LR 仅偶数阶可用**] |
| `xo_polarity_auto` | {关, 开} | — | **开** | — | **见下,新增项** |

> **⭐⭐ 新增项 `xo_polarity_auto` 的理由(EXP-3 实测)**:
> LR 分频器的 LP 与 HP 求和是否平坦,**取决于阶数**:
> | LR 阶数 | 12 (LR2) | 24 (LR4) | 36 (LR6) | 48 (LR8) |
> |---|---|---|---|---|
> | 阶数 mod 4 | 2 | 0 | 2 | 0 |
> | **求和极性** | **反相** | 同相 | **反相** | 同相 |
> | 正确极性下求和偏离(理想) | 2.0e−11 dB | 3.7e−11 dB | 6.3e−11 dB | 4.0e−11 dB |
> | 正确极性下求和偏离(Q4.27) | 0.0011 dB | 0.0023 dB | 0.0036 dB | 0.0023 dB |
> **阳性对照**:把 LR4 的极性人为写反 ⇒ 求和出现 **87.72 dB 深谷** [L2/实测 EXP-3p]。
> **⇒ 若界面允许用户自由选斜率而不自动处理极性,用户在 12/36 dB/oct 上会得到
> 分频点的深谷,而他不会知道原因。** ⇒ 建议默认自动,并在界面显示当前极性。
> ⚠ 竞品手册未提及该项 [L4/竞品手册],**这是本件相对竞品的一处增量**。

> **⚠ `xo_slope` 只列 12/24/36/48**:LR 的定义是「Butterworth 的平方」⇒ **只有偶数阶存在**。
> 竞品列了 6/18/30/42 [L4/竞品手册],那几档**在 LR 类型下没有定义**(只能是 BW/Bessel)。
> ⇒ **界面须按类型联动斜率选项**,否则会生成一个数学上不存在的滤波器。

### ④ PEQ ×10 —— 参数同 D3 §3⑥(10 段)
### ⑤ 线性相位 FIR
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `fir_en` | {关, 开} | — | **关** | — | [L4/我定的数] |
| `fir_taps` | {128, 256, 512} | — | 256 | tap | **受延迟预算硬约束**,见 §5 |

> **⚠ 1024 tap 不在候选内**:见 §5,L=64 时它单独就把固定延迟推到 14.32 ms > 12 ms。
> **⚠ FIR 的低频分辨力**:N 抽头的频率分辨力 ≈ fs/N;经验上可控最低频 ≈ 2·fs/N。
> | N | 128 | 256 | 512 | 1024 |
> |---|---|---|---|---|
> | 可控最低频(≈2fs/N) | 750 Hz | 375 Hz | 187 Hz | 94 Hz |
> ⇒ **256 tap 的 FIR 管不到 375 Hz 以下** —— 低频修正仍须靠 IIR。
> ⇒ 产品话术上「FIR 线性相位」不等于「全频段线性相位」,**对外描述须注意**(报 lead)。

### ⑥ 延时
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `out_delay` | 0 … **200** | 1 样本 | 0 | ms | [L4/我定的数,竞品为 1–1200 ms] |
| `out_delay_unit` | {ms, m, ft} | — | ms | — | [L4/竞品] |
| `sound_speed` | 330 … 350 | 0.1 | **343.0** | m/s | @20 °C [L3/教科书] |

> **⭐ 为什么我把上限定在 200 ms 而不是竞品的 1200 ms —— 内存账**
> (每通道缓冲 = 时长 × 48 样本/ms × 4 B,Q4.27):
> | 最大延时 | 单通道 | ×8 通道 | 落在哪一级存储 |
> |---|---|---|---|
> | 100 ms | 19.2 kB | 153.6 kB | L1(640 kB)可容,占 24% |
> | **200 ms** | **38.4 kB** | **307.2 kB** | L1 占 48%,或移 L2 |
> | 500 ms | 96 kB | 768 kB | **超 L1**,须 L2(1024 kB) |
> | 1200 ms(竞品口径) | 230.4 kB | **1843.2 kB** | **超 L2**,须外挂 **L3/DDR3** |
> [L3/解析;存储器容量 L1=640 kB / L2=1024 kB / L3=外挂 DDR3 为 [L2/厂家 datasheet Table 1]]
>
> ⇒ **1200 ms × 8 通道必须走 DDR3**,意味着每样本的延时读写要过外部总线
> ⇒ 须块 DMA、且占 DDR 带宽 ⇒ **属架构/固件决策,不是我能定的**。
> ⇒ **报 lead:延时上限是一个「产品规格 × 存储架构」的耦合决策,请路由架构侧 + CTO。**
> 200 ms = 68.6 m,对会议室/报告厅足够;若要对标竞品的 1200 ms(412 m,延时塔场景),
> 须先定存储架构。

### ⑦⑧⑨ 限幅 / 音箱保护 / 静音
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `out_lim_thr` | −20 … 0 | 0.1 | −0.5 | dBFS | [L4/我定的数] |
| `out_lim_release` | 10 … 1000 | 1 | 50 | ms | [L4/竞品] |
| `spk_rms_thr` | −40 … 0 | 0.1 | −12 | dBFS | [L4/我定的数] |
| `spk_rms_tc` | 100 … 10000 | 10 | 2000 | ms | [L4/我定的数,音圈热时间常数量级] |
| `spk_peak_thr` | −20 … 0 | 0.1 | −3 | dBFS | [L4/我定的数] |
| `spk_peak_attack` | 0.1 … 10 | 0.1 | 0.5 | ms | [L4/我定的数] |
| `mute_ramp` | 1 … 200 | 1 | 20 | ms | [L4/我定的数,防爆音] |

> **⚠ 音箱保护的全部阈值/时间常数都是 [L4/我定的数]**:真实值取决于所接音箱的
> 功率、音圈热阻、Xmax —— **本项目不知道用户接什么音箱**。
> ⇒ 这些默认值只是**安全的保守占位**,须由集成商按音箱规格设定。
> ⇒ **建议 D2 把它们标为"必须现场设定"而不是"出厂默认可用"。**

---

## 5. ⭐ 延迟预算(PRD §一.4:全链路 < 12 ms)

### 5.1 固定项(不可配置)
| 项 | 值(样本 @48 kHz) | ms | 来源 |
|---|---|---|---|
| ADAU1979 ADC 群延迟 | 22.9844 | 0.479 | [L2/厂家 datasheet] |
| ADAU1962A DAC 传播延迟 | 25.0 | 0.521 | [L2/厂家 datasheet] |
| **转换器合计** | **47.9844** | **0.99967** | |

### 5.2 块 I/O + FIR(三联数:抽头 → 群延迟 → 预算占用)
固定合计 = 转换器 + 块 I/O(2L)+ FIR 群延迟 (N−1)/2

| 帧长 L | 块 I/O | FIR 128 | FIR 256 | FIR 512 | FIR 1024 |
|---|---|---|---|---|---|
| 32 | 1.333 ms | 3.656 ms(余 +8.344) | 4.989(+7.011) | 7.656(+4.344) | **12.989(−0.989)** |
| **64** | 2.667 ms | 4.989(+7.011) | 6.323(+5.677) | **8.989(+3.011)** | **14.323(−2.323)** |
| 128 | 5.333 ms | 7.656(+4.344) | 8.989(+3.011) | 11.656(+0.344) | **16.989(−4.989)** |

> **⇒ FIR 1024 tap 在任何帧长下都超预算。**
> **⇒ 推荐工作点:L = 64 + FIR ≤ 512 tap,余 3.011 ms 给 IIR / AEC / 矩阵 / 用户延时。**
> ⚠ 块 I/O 按 **2L**(乒乓双缓冲)计;若固件用单缓冲 + 及时处理可降到 ~L,
> **该口径归 platform-fw / 架构侧**,本件按保守的 2L 记。

### 5.3 ⭐⭐ IIR 群延迟 —— 这一项通常没人记,而它可能是最大的一项
分频器的群延迟随频率变化,**在 fc 附近最大**(高 Q 节)或**在带下沿最大**(LR2)。
两轨实测(逐节 group_delay 求和 vs 整体相位差分,最大差 **0.0007 样本**):

| 分频 | fc = 80 Hz | fc = 120 Hz | fc = 2000 Hz |
|---|---|---|---|
| LR2(12 dB/oct) | 3.744 ms | 2.581 ms | 0.158 ms |
| LR4(24 dB/oct) | 6.792 ms | 4.528 ms | 0.272 ms |
| **LR8(48 dB/oct)** | **15.555 ms** | **10.370 ms** | 0.627 ms |

> **⇒ LR8 @ 80 Hz 单这一个模块就是 15.555 ms,已超 12 ms 全链预算。**
>
> **⚠⚠ 但这句话必须带口径,否则会被读错(LESSONS B-3:先核量的类别)**:
> 群延迟是**频率的函数**。上表是 20 Hz–20 kHz 内的**最大值**,它出现在 fc 附近的窄带里;
> 同一个滤波器在 1 kHz 处的群延迟接近 0。
> ⇒ **「全链路处理延迟 < 12 ms」这句规格,必须先定义它测的是哪个量**:
> | 口径 | LR8 @80 Hz 是否满足 |
> |---|---|
> | (a) 宽带/中频段的处理延迟(转换器 + 块 + FIR) | ✓ 满足(8.989 ms) |
> | (b) 20 Hz–20 kHz 内**任意频点**的群延迟上界 | ✗ 不满足(15.555 ms) |
> ⇒ **这不是「规格做不到」,是「规格没定义清楚」。⇒ 报 lead 交 CTO 定口径(§7)。**
> ⇒ 若取 (b) 口径,则**低频段的高阶 LR 分频须限档**(如 fc < 150 Hz 时最高 LR4)。

### 5.4 其它可配置项
| 项 | 延迟 | 备注 |
|---|---|---|
| `lim_lookahead` | 0 / 1 / 2 ms | 默认 1 ms,**直接计入预算** |
| `in_delay` / `out_delay` | 用户设定 | 用户显式加的,不算"处理延迟",但**须在界面上与总延迟一起显示** |

---

## 6. D-4:级联结论(⛔ 不许只验单级)

| 被测 | 单级 | **级联** | 判据 | 结果 |
|---|---|---|---|---|
| LR 分频求和平坦度(Q4.27) | — | 最坏 **0.0036 dB**(LR6 @80 Hz) | PRD ±0.3 dB | ✓ 余量 83× |
| LR 高通 **DC 增益** | 精确 0(结构约束量化) | 精确 0 | ≤ −120 dB | ✓ |
| LR 低通 **Nyquist 增益** | 精确 0 | 精确 0 | ≤ −120 dB | ✓ |
| 8 段非平坦 PEQ 级联(量化 vs 理想) | — | **0.003859 dB** | ±0.3 dB | ✓ 余量 78× |
| **量化噪声底** | −173.35 dBFS | D3(9 量化器)**−163.81**;D4(19)**−160.56**;合计(28)**−158.88** | ≤ −106 dBFS | ✓ 余量 52.9 dB |
| **群延迟** | 见 §5.3 | LR8@80 Hz **15.555 ms** | 见 §5.3 口径问题 | ⚠ **须定口径** |

> ⚠ 噪声底一行**继承任务一的门禁状态**(其单节值 −173.35 dBFS 尚未过门)⇒ ⛔ 不得独立引用。
> ⚠ 增益 / 门 / 压限 / 延时**不引入新量化器**:增益并入相邻节的累加器;
> 门与压限只做一次增益相乘(1 次窄化,已计入其上游节);延时是纯搬运。

---

## 7. ⚠ 须由 lead 路由的四条

1. **「全链路处理延迟 < 12 ms」的口径未定义**(§5.3)。宽带延迟 vs 任意频点群延迟上界,
   两种读法对 LR8 低频分频给出相反结论。**这是 PRD 规格的定义问题,须 CTO 定。**
   若取"任意频点"口径,须对低频高阶分频限档。

2. **输出延时上限 200 ms vs 竞品 1200 ms 是存储架构决策**(§4⑥)。
   1200 ms × 8 ch = 1.84 MB **超 L2(1024 kB)**,须外挂 DDR3 + 块 DMA。
   ⇒ 请路由架构侧 + platform-fw + CTO。

3. **「FIR 线性相位」的对外话术**(§4⑤)。256 tap 管不到 375 Hz 以下;
   要覆盖到 100 Hz 需 ≥1024 tap,而那超延迟预算。
   ⇒ **对外规格不宜写成"全频段线性相位"**,请 CTO 确认话术。

4. **`xo_slope` 的 6/18/30/42 dB/oct 在 LR 类型下不存在**(§4③)。竞品界面列了这些档
   [L4/竞品手册],若我们照抄会生成数学上不存在的滤波器。建议界面按类型联动。

---

## 8. 显式砍掉 / 待办(⛔ 不静默省略)

| # | 项 | 状态 |
|---|---|---|
| Y1 | 全部 C 实现与 bit-exact | 本轮交设计 + 参数表,不含模块实现 |
| Y2 | 一切算力 / 周期数 | **零结论**,待 W1-C / D13 |
| Y3 | 动态处理器的真实素材验证 | 本轮只用合成素材做**链序**论证;压限效果验证属 D14 |
| Y4 | AEC / ANC / AFC / AGC / automixer 的位置 | 只给我这一侧的约束,⛔ 未替对方定 |
| Y5 | 参数默认值的听感标定 | 全部 [L4],进台账待标定 |
| Y6 | PRD §二.4 "动态参量 EQ" 的「动态」语义 | **PRD 未定义**,待 CTO / 上位机对标澄清 |
| Y7 | Bessel 分频的系数上界 | 未扫(本轮只扫了 BW / LR / PEQ / 架式)⇒ 启用 Bessel 前须补扫 |
| Y8 | `xo_slope` 36 dB/oct(LR6)的实现 | 分析已含,但 LR6 = (3 阶 BW)²,含一个一阶节,**实现细节未定** |
| Y9 | 前置件(定点约定)尚未过门 | 本件建立其上 ⇒ **本件的门禁状态不高于它** |

---

## 9. 自验与假绿纪律

| 项 | 结果 |
|---|---|
| r3 好版本 | **PASS = 17,FAIL = 0,RETIRED = 2**(退役项原样留痕) |
| **杀伤矩阵** | `polarity`(极性规则写反)→ 杀 EXP-3a / EXP-3b;`qcoef`(系数退化到 16-bit)→ 杀 EXP-2d / EXP-3b / EXP-3c / EXP-4a / EXP-4d。**2/2 变异全部被杀死** |
| **阴性对照** | 好版本 FAIL 数 = **0** ⇒ 杀死确由变异引起 |
| **阳性对照** | EXP-3p:极性写反 ⇒ 求和出现 **87.72 dB 深谷** ⇒ EXP-3a/b 不是恒真 |
| **两轨交叉核** | 群延迟:逐节求和 vs 整体相位差分,最大差 **0.0007 样本**;EXP-6:数值扫描 vs 解析上界 A² 自洽 |
| **第三轨** | EXP-4d:闭式解析 τ=2/(ωc(1+(ω/ωc)²)) vs 数值,最大相对差 **0.57%** |

**⚠ 三次证伪条件命中,处置全部是【改被测物 / 另做独立观测】,⛔ 未改任何判据**:
1. **EXP-1(顺序论证)** —— 宽带峰值测度下两种顺序完全相同 ⇒ 论证被证伪。
   ⇒ 在 r2 预注册里**重新注册被测量**(窄带逐节电平),并**预先写死**:若新测量也无差异,
   则该论证整条删除。结果:31.14 dB,论证成立。
2. **EXP-2b(门行为)** —— 我拍的门限 −45 dBFS 落在两个检测器读数之外 ⇒ 两种链序都不动。
   ⇒ **不改门限充数**,改为扫门限(EXP-2c)+ 扫适用范围(EXP-2d)。
   ⇒ 产出反而更有用:得到一张**可交给集成商的适用范围表**。
3. **EXP-4b(群延迟峰值位置)** —— 判据「峰必在 fc 附近」对 LR2(Q=0.5)不适用。
   ⇒ 用**闭式解析第三轨**独立证实峰在带下沿是物理的(相对差 0.57%),然后**退役该判据**。

**⚠ 另记一条工具失效(r1 之前的探索阶段,已写进 PREREG_D34_r1 §0 X-1)**:
`scipy.signal.group_delay` 作用在**卷积后的 9 阶多项式**上,给出
「LR8 fc=120 Hz 群延迟 18.59 ms @ **1426.8 Hz**」——**对一个 120 Hz 高通,这在物理上不可能**。
⇒ 改为逐节求和 + 强制两轨。**若照原样报出,§5.3 整段都会是错的。**
