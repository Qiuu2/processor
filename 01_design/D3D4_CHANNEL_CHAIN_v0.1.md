# D3 输入通道链 / D4 输出通道链 —— 设计 v0.1

```
⛔ 门禁状态:未过门(2026-08-04)
   未经独立 critic 评审。禁止 release / 冻结 / 被其他工件引用为依据 / 对外承诺。
   归档性 commit 须带 [未过门] 前缀。
作者   : channel-dsp(第 1 实例)
前置件 : D34_FIXEDPOINT_CONVENTION_v0.1.md(定点约定,同样未过门)
配套件 : 01_design/d34_chain/PREREG_D34_{r1, r2_addendum, r3_addendum, r4_addendum, r5_addendum,
                                            r8_addendum, r9_addendum, r10_addendum}.txt
         01_design/d34_chain/d34_analysis.py
         01_design/d34_chain/results_d34_{r2,r3,r4,r5,r6,r7,r8,r9,r10,r11,r12,r13}.txt
                                                              ← **r13 为现行结果**
         01_design/d34_chain/results_d34_{r2,r6,r13}_killmatrix.txt
         01_design/d34_chain/RETIREMENT_LOG.txt   ← 五条 FAIL→RETIRED 改判的正式记录
         01_design/D3D4_CONSTANT_LEDGER_v0.1.md(自定常数台账)

⛔⛔ 整改留痕(2026-08-05 · critic BLOCKER-1):本清单原先只列到 r7,并写「r7 为现行结果」。
   而全文 §0 承重表里带 ⭐ 的三条(f_lo* = 105.2 Hz / Q 的影响是段数的 8.02× / 拒绝率 0 段)
   **只产自 r10–r12,在 r7 里根本找不到**;而 r8–r10 恰是本件唯一出现过 FAIL 的一段。
   ⇒ 后果不是"藏了结论"(§5.8/§5.9 白纸黑字写着坏消息),是**交付件的可复核性断了**:
     `f_lo* = 105.2` 已进 DEC-0021,而按「DEC → 设计件 → 配套件清单」追下去,最后一跳会断。
   ⇒ 已补齐清单;⛔ 未重跑(r8–r12 结果均在盘上,本轮是记录对齐,不是数值修正)。
```

> **⚠ 一处操作失误,如实记(收尾机械核盘抓到)**:**r1 没有落盘** —— 我只把它跑到了控制台,
> 因此**不存在 `results_d34_r1.txt`**(本件初稿的配套件清单曾写有该文件,已改正)。
> r1 产出的两条(EXP-2b 门增益两侧均 0.00 dB;EXP-4b 峰值落在 20.0 Hz)
> **逐字保存在 `PREREG_D34_r2_addendum.txt` §1**(该件写于 r2 跑之前),
> 且在 r2/r3 的退役项输出里仍可复现。
> ⛔ **我不补造一个 r1 文件** —— 那会是在发明记录。
> ⇒ 这是团队纪律 D6-j 的同族:输出路径不仅要唯一,**还要真的存在**;
>   而「文档声称的文件 vs 仓库实际」正是本项目机械可查的那一类偏差。

> **⚠ 参数字典(D2)归 `system-architect`,是单一事实源。**
> 本件所有参数表是**提案**,须与 D2 对齐;凡与 D2 冲突,以 D2 为准并报 lead 路由。
> 本件不新建一套字典。

---

## 0. 一页纸

**D3 输入链(我方确定性部分)**
```
① 极性反转 → ② 前置增益(数字 trim) → ③ HPF
   → 〔AEC 插入点 ✳〕 → 〔ANC 插入点 ✳〕
→ ④ 噪声门/扩展器 → ⑤ 压缩器 → 〔AGC 插入点 ✳〕
→ ⑥ PEQ ×8 → 〔AFC 陷波器组 ✳〕
→ ⑦ 延时 → ⑧ 保护限幅 → (去矩阵)
```
**D4 输出链**
```
(矩阵求和) → ① 输出增益 → ② 极性反转 → ③ 分频(HPF+LPF) → ④ PEQ ×10
→ ⑤ 线性相位 FIR → ⑥ 延时 → ⑦ 输出限幅 → ⑧ 音箱保护限幅(RMS+峰值) → ⑨ 斜坡静音 → DAC
```
**✳ = 耦合点,归 `adaptive-dsp` / `system-architect`。⛔ 本件只给我这一侧的约束与理由,不替它们定。**

| 承重结论 | 值 | 依据 |
|---|---|---|
| D4 中**分频必须在 PEQ 之前** | 阻带激励下省 **31.14 dB** 链内电平 | [L2/宿主实测 EXP-1a] |
| 两者总传函严格相等 | 差 **2.8e−14 dB** ⇒ 顺序**不能**用传函选 | [L2/宿主实测 EXP-1c] |
| **LR 分频的求和极性由阶数决定** | 阶数 mod 4 = 0 ⇒ 同相;= 2 ⇒ **须反相** | [L2/宿主实测 EXP-3] |
| Q4.27 下 LR 求和平坦度 | 最坏 **0.0036 dB**(规格 ±0.3 dB) | [L2/宿主实测 EXP-3b] |
| **LR8 分频器低频群延迟** | fc=80 Hz 时 **15.555 ms**,已超 12 ms 全链预算 | [L2/宿主实测 EXP-4,两轨] |
| 转换器往返延迟 | ADC 22.9844/fS + DAC 25/fS = **0.99967 ms** | [L2/厂家 datasheet] |
| FIR 抽头受延迟预算硬约束 | L=64 时 **1024 tap 必超预算**;512 tap 余 3.011 ms | [L3/解析 + L2/厂家锚点] |
| D3+D4 全链量化噪声底 | **−158.88 dBFS**(28 个量化器,**⛔ 仅在各节增益 = 1 下**)。最坏合法增益(8 段全 +15 dB 同频,§6.2-4 明文允许)下:8×PEQ 段 **−91.64**、D3 输入链 **−76.97** ⇒ ⛔ 突破 PRD 的 −106 | [L3/解析,基于任务一 [L2/实测] 单节值] + [L2/宿主复算 `d34_chain/check_noise_chain_r5.py`] |
| PEQ 增益范围硬上限 | **±15 dB**(Q4.27 解析包络 `G_max < 18.0618 dB`) | 前置件 **§3.2.0**(⚠ 原引 §3.2.1 = 已退役的历史扫描表) |
| PEQ 的 Q 可放到竞品口径 | Q ∈ [0.02, 50] 下 max\|b\| = **5.2229** < 16 ✓ | [L2/宿主实测 EXP-6] |
| **系数界已由经验扫描界改为解析界** | `max\|b\| ≤ 2·10^(G_max/20)`,**只依赖 G_max** | [L3/解析 + L2/实测 EXP-7 交叉核] |
| **延迟可简单相加** | 简单相加比逐频相加高估 **< 0.8 ms** ⇒ 配平表按相加做 | [L2/宿主实测 EXP-8b/8c] |
| **⭐ 决定延迟线的是规格的评价频带下沿** | REF 恰好 12 ms 的下沿 **f_lo\* = 105.2 Hz** | [L2/宿主实测 EXP-11] |
| **⭐ 群延迟由最低频那段的 Q 主导,不由段数** | Q 的影响是段数的 **8.02×** | [L2/宿主实测 EXP-10c] |
| **拒绝率**:下沿 100 Hz ⇒ 0 段余量(不可用);≥150 Hz ⇒ 风险低 | 见 §5.9 | [L2/宿主实测 EXP-10b] |

---

## 1. D3 输入链:链序推导(逐处理由)

> **⛔ 不按 PRD §二 的枚举顺序推(假设 A9)。** PRD §二 是功能清单,不是链序。
> 对标基线:竞品 DSP Controller V1.3 实测链序 = `EXP → COMP → AGC → EQ → AFC`
> [L4/竞品明文配置]。**本件与它同向,但有两处刻意不同,逐条给理由。**

### ① 极性反转 —— 最前
**理由(硬)**:极性只在**跨通道求和**时才有意义(单通道内幅度检测对极性不敏感)。
链上最早的求和是 AEC 参考路径与 automixer ⇒ 极性必须在它们之前确定。
放最前是"确定得最早"的位置,且不引入任何代价(乘 ±1,可并入 ② 的增益)。

### ② 前置增益(数字 trim)—— 紧随
**理由(硬)**:下游**所有阈值型参数都是绝对电平量** —— 门阈值、压缩阈值、
AFC 的 `fb_thr` / `panic_thr`、限幅阈值。若增益在它们之后,调一次 trim 就把
全部阈值的实际作用点移走了,现场调试会陷入"改一个动全部"。
⇒ **增益必须在第一个用绝对电平做判据的模块之前。**

### ③ HPF —— 动态处理之前,且**必须是独立模块**
**理由(实测)**:低频隆隆声(手持噪声、空调、桌面震动、近讲效应)会驱动动态处理器的
侧链检测器,造成误开门 / 误压缩。

实测(EXP-2a,合成素材 = 1 kHz 猝发串 −30 dBFS + 45 Hz 隆隆 −20 dBFS):
| 链序 | 静默段侧链检测器读数 |
|---|---|
| HPF 在动态**之前** | **−33.42 dBFS** |
| HPF 在动态**之后** | **−23.01 dBFS** |
| **分离度** | **10.41 dB** |

**⚠ 适用范围(EXP-2d 实测,⛔ 不得省略)**:分离度强烈依赖隆隆声频率、HPF 阶数与 fc。

| 隆隆声 | 2 阶 fc=60 | 2 阶 fc=80 | 2 阶 fc=120 | 4 阶 fc=60 | 4 阶 fc=80 | 4 阶 fc=120 |
|---|---|---|---|---|---|---|
| 20 Hz | 19.12 | 24.05 | 30.90 | 37.27 | 44.18 | **48.32** |
| 30 Hz | 12.30 | 17.11 | 24.05 | 24.05 | 33.66 | 44.18 |
| 45 Hz | 6.19 | 10.41 | 17.11 | 10.41 | 20.01 | 33.66 |
| 60 Hz | **3.01** | 6.19 | 12.30 | 3.01 | 10.41 | 24.05 |
(单位 dB,分离度 = HPF 在后 − HPF 在前)

⇒ **60 Hz 隆隆 + 2 阶 60 Hz HPF 只有 3.01 dB** —— 这条理由**不是无条件成立的**。
⇒ 默认值必须让 fc 明显高于目标隆隆声频段,或用 4 阶。见 §3 参数表。

**⭐ 而且 HPF 必须是【独立模块】,不能只做成 PEQ 的一个 band type**
(竞品把高低通做成 EQ 的类型之一 [L4/竞品手册])。
**理由**:本设计把 PEQ 放在动态处理**之后**(见 ⑥)。若 HPF 只是 PEQ 的一段,
它就落在动态之后 ⇒ **完全失去保护侧链的作用**,而界面上看不出任何区别。
⇒ 这是本件相对竞品的一处刻意不同,**代价 = 多一个模块(1 个 biquad)**。

### 〔AEC 插入点 ✳〕—— 我方给出的**约束**,不是位置
**约束(硬,交 adaptive-dsp / 架构侧)**:
> **AEC 之前只允许【线性时不变、且系数不随时间改变】的模块。**

理由:AEC 的自适应滤波依赖"参考信号 → 麦克风信号"的线性关系。
门 / 压限 / AGC / AFC 都是**非线性或时变**的 ⇒ 放在 AEC 之前会破坏该假设,
使 AEC 无法收敛或残余回声升高。
- ③ HPF 是 LTI 且系数静态 ⇒ **可以**在 AEC 之前,且有益(去掉低频能量利于收敛)
- ② 增益若**运行时可变**(推子拖动),严格说也是时变的 ⇒ 建议增益变化时给 AEC 一个
  `gain_changed` 事件(控制面信号),由 AEC 侧决定是否冻结/重收敛。**该信号的定义归架构侧。**

⛔ **AEC 的具体插入位置由 adaptive-dsp 与架构侧定,本件不定。**

### 〔ANC / ANS 插入点 ✳〕
非线性谱域处理 ⇒ 同样须在 AEC **之后**(否则残余回声被非线性化,AEC 更难收敛)。
⛔ 位置由对方定。

### ④ 噪声门 / 扩展器 —— 在压缩器之前
**理由**:压缩会**抬高低电平段的相对电平**(动态范围被压小,本底噪声被抬起)。
若门在压缩之后,门限的实际作用点会随压缩量漂移 ⇒ 用户每改一次压缩比都要重调门限。
⇒ 门在前,它面对的是未被压缩的原始动态,门限含义稳定。
(与竞品同向:`EXP → COMP`)

### ⑤ 压缩器
### 〔AGC 插入点 ✳〕
惯例在压缩器之后:压缩管**快峰值**,AGC 管**慢电平**;两者时间常数相差 1–2 个数量级,
串起来互不打架。竞品亦为 `COMP → AGC` [L4/竞品明文配置]。⛔ 位置由对方定。

### ⑥ PEQ ×8 —— 在动态处理**之后**
**理由(两条)**:
1. 会议系统的 PEQ 主要是**房间修正与反馈前的音色整形**,是"最终音色"。
   若放在压缩器之前,PEQ 的提升会触发压缩、又被压回去 ⇒ 用户拧 EQ 却听不到相应变化。
2. 下游的 AFC 需要看到**接近最终**的信号才能正确定位啸叫频点;
   若 PEQ 在 AFC 之后,AFC 抑制的频点与实际进入扩声环路的频点不一致。

**代价(必须显式接住)**:PEQ 的提升**不再受压缩器保护** ⇒ 可能把电平推到削波。
⇒ 由 ⑧ 保护限幅兜底,**这就是 ⑧ 存在的理由**。
(与竞品同向:`... → EQ → AFC`)

### 〔AFC 陷波器组 ✳〕
靠链末,接近最终信号。⚠ 其**检测 tap** 已由 W1-A 定为"反转回陷波器组入口"
—— 该件未过门,⛔ 本件不引其结论,仅记录存在耦合。⛔ 位置由对方定。

### ⑦ 延时
**约束**:必须在 **AEC 之后** —— 输入延时会改变麦克风路径的时间对齐,
放在 AEC 之前等于给 AEC 增加一段未知的固定时延,浪费其尾长预算。
延时是纯搬运,不改变信号内容 ⇒ 在 AEC 之后的任意位置皆可,放 ⑦ 是为了紧邻链末便于理解。

### ⑧ 保护限幅 —— 最末
**理由(硬)**:矩阵是**多输入求和**。若各路输入不受约束,求和后必然溢出。
⇒ 保护限幅保证进入矩阵的每一路都不超过约定电平,**这是矩阵求和不溢出的前提**。
同时它是 ⑥ 的兜底(见上)。
**它与 ⑤ 压缩器职责不同**:压缩器是音乐性动态控制(阈值低、比率适中、拐点软);
保护限幅是**砖墙**(阈值贴近满刻度、比率 ∞、attack 极快),唯一目的是防下游溢出。

---

## 2. D4 输出链:链序推导

### ① 输出增益 → ② 极性反转
同 D3 的理由(阈值锚定 / 求和前确定极性)。

### ③ 分频(HPF + LPF)—— **在 PEQ 之前**
**⚠ 先说一件我自己搞错过的事(留痕)**:我最初用「链内峰值增益」来论证这个顺序,
**实测两种顺序的链内峰值完全相同(12.95 dB vs 12.95 dB,差 0.00 dB)⇒ 论证被证伪**。
原因:那是"max over 全频"的宽带测度,而两种顺序的**最后一个节点就是输出**,
输出相同 ⇒ 最大值必然相同。**⛔ 我没有就地改写论证,而是重新注册了一个被测量。**

**重新注册后的实测(EXP-1,窄带激励)**:
配置 = PEQ 在 40 Hz 提升 +12 dB;分频 = LR4 高通 fc = 120 Hz(⇒ 40 Hz 落在阻带);激励 −20 dBFS。

| 顺序 | 逐节电平(dBFS) | 最大节点电平 |
|---|---|---|
| PEQ 在前 | −8.00 → −27.14 → −46.28 | **−8.00** |
| **分频在前** | −39.14 → −58.28 → −46.28 | **−39.14** |
| | | **差 = 31.14 dB** |

**通带对照(1 kHz)**:两者差 **+0.002 dB** ⇒ 该测量**只在阻带有分辨力**,与预注册一致。

**⇒ 结论**:两者**总传函严格相等**(差 2.8e−14 dB),顺序**不能**用传函选;
选它的唯一理由是 **定点链内电平**:分频在前使被滤掉频段内的大信号**先被衰减**,
不会先被 PEQ 提升 31 dB 再扔掉 —— 而中间那一步若饱和,是**非线性的、后面的分频器救不回来**。
(与竞品同向:`XOver → EQ` [L4/竞品明文配置])

### ④ PEQ ×10
### ⑤ 线性相位 FIR —— 在限幅之前
FIR 为 LTI,位置不改传函。放在 ⑦ 之前的理由:**限幅必须是最后一道**,
且 FIR 的振铃(前冲/后冲)必须落在限幅的约束之内,否则限幅形同虚设。

### ⑥ 延时 —— 在限幅之前
用于扬声器物理距离对齐。纯搬运。放限幅之前(竞品同 `Delay → Limiter`)。

### ⑦ 输出限幅 → ⑧ 音箱保护限幅
**⑦** 砖墙限幅,保证 DAC 不削波。
**⑧** PRD §三.5,**双通道**:长期功率(RMS,保护音圈热失效)+ 短期峰值(保护位移超程)。
两者时间常数相差 2–3 个数量级,不能合成一个检测器。

### ⑨ 斜坡静音 —— 最末
静音必须在链末,保证任何情况下(包括前级失控)都能切断。**斜坡**(非硬切)防爆音。

---

## 3. D3 参数表(提案,待与 D2 对齐)

> 每行的 L 标见末列。**凡 [L4/我定的数] 的,全部进 `D3D4_CONSTANT_LEDGER_v0.1.md`。**

### ①② 极性 / 前置增益
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `polarity` | {正相, 反相} | — | 正相 | — | [L2/PRD §二.1] |
| `trim_gain` | −60 … **+20** | 0.1 | 0.0 | dB | 上限受 `CHDSP_DB_MAX_Q8=+24 dB` 约束 [L2/前置件] |
| `mute` | {关, 开} | — | 关 | — | [L2/PRD §二.1] |

### ③ HPF(独立模块)
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `hpf_en` | {关, 开} | — | **开** | — | [L4/我定的数] |
| `hpf_fc` | 20 … 500 | 1 | **80** | Hz | [L4/我定的数,依据 §1③ 的 EXP-2d 表] |
| `hpf_type` | {BW12, BW24, LR12, LR24} | — | **BW12** | — | [L4/我定的数] |

> **⚠ 默认值的取舍已量化**:2 阶 / 80 Hz 对 45 Hz 隆隆只有 **10.41 dB** 分离度;
> 4 阶 / 120 Hz 可达 **33.66 dB**,但会切入男声基频(85–180 Hz)的下沿。
> ⇒ 默认取保守档,**并把 EXP-2d 整张表交给集成商**,由现场噪声环境决定是否上调。

### ④ 噪声门 / 扩展器
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `gate_en` | {关, 开} | — | 关 | — | [L4/我定的数] |
| `gate_thr` | −80 … 0 | 0.5 | **−45** | dBFS | [L4/我定的数] |
| `gate_ratio` | 1.0 … 20.0 | 0.1 | 4.0 | :1 | [L4/竞品:比率 1:20 即等效噪声门] |
| `gate_attack` | 0.1 … 100 | 0.1 | 1.0 | ms | [L4/我定的数] |
| `gate_hold` | 0 … 1000 | 1 | 50 | ms | [L4/我定的数] |
| `gate_release` | 10 … 3000 | 1 | 200 | ms | [L4/我定的数] |
| `gate_hysteresis` | 0 … 20 | 0.5 | 3.0 | dB | [L4/我定的数,防抖] |

> **⚠ `gate_thr` 与 `hpf_fc` 是耦合的**(EXP-2c 实测):HPF 前置带来的门行为改善,
> 只在门限落在「HPF 前后两个检测器读数之间」时才出现。
> 在本件的测试素材上该区间是 **[−27, −10] dBFS**;⛔ **该区间随素材电平变化,
> 不是普适常数** —— 它说明的是**耦合关系存在**,不是"门限该设 −27"。
> ⇒ **进 D2 时须把这条耦合写成注记**,不能让两个参数各自独立地被调。

### ⑤ 压缩器
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `comp_en` | {关, 开} | — | 关 | — | — |
| `comp_thr` | −60 … 0 | 0.5 | −20 | dBFS | [L4/我定的数,= 标称电平] |
| `comp_ratio` | 1.0 … 20.0 | 0.1 | 3.0 | :1 | [L4/竞品:比率 1~20] |
| `comp_knee` | 0 … 20 | 0.5 | 6.0 | dB | [L4/我定的数] |
| `comp_attack` | 0.1 … 200 | 0.1 | 10.0 | ms | [L4/我定的数] |
| `comp_release` | 10 … 3000 | 1 | 100 | ms | [L4/我定的数] |
| `comp_makeup` | 0 … +20 | 0.1 | 0.0 | dB | 上限同 `trim_gain` |
| `comp_detector` | {峰值, RMS} | — | RMS | — | [L4/我定的数] |

### ⑥ PEQ ×8(每段独立)
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `band_en[k]` | {关, 开} | — | 关 | — | — |
| `band_type[k]` | {PEQ, 低架, 高架, 低通, 高通} | — | PEQ | — | [L4/竞品:类型 PEQ/高低架/高低通] |
| `band_freq[k]` | 20 … 20000 | 1/24 oct | 1000 | Hz | [L2/PRD §一.1 频响范围] |
| `band_gain[k]` | **−15 … +15** | 0.1 | 0.0 | dB | **硬上限**,见下 |
| `band_q[k]` | **0.02 … 50** | — | 1.41 | — | [L4/竞品] + [L2/实测 EXP-6 已验证安全] |

> **⭐ `band_gain` 的 ±15 dB 是硬约束,不是习惯**:系数格式 Q4.27 的包络是
> **架式增益 `G_max < 18.0618 dB`**(= 20·log₁₀8,前置件 **§3.2.0 解析界**)。
> 达到即 `chdsp_coef_from_f64()` 硬失败。
> ⇒ **建议 D2 把它锁死在 ±15 dB**,留 **3.0618 dB** 余量。
>
> 〔整改留痕 2026-08-04 · channel-dsp 实例 #2:原写「≤ +18.089 dB(前置件 §3.2.1)」。
>  两处都要改:①`18.089` 是**扫描二分求出的越界点**(在该值处实扫 max|b| 已 = 16.0002 ⇒ 越界),
>  ⛔ 不是安全上限;②`§3.2.1` 是**已退役的历史扫描表**,权威表述在 §3.2.0。
>  解析包络 18.0618 对任意 f/Q/S 成立,扫描值只在 f∈[20,20k] 内成立。
>  详见 D34 台账 §F 的整改留痕表。〕
> **⭐ `band_q` 放到竞品的 0.02–50 是安全的**:EXP-6 实测该范围内 PEQ 的
> max|b| = **5.2229**,解析上界 A² = 5.6234,均远小于 16。⇒ 台账 C2 由 🔴 降 🟡。

### ⑦ 延时
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `in_delay` | 0 … **100** | 1 样本 (20.8 µs) | 0 | ms | [L4/我定的数] |

### ⑧ 保护限幅
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `lim_thr` | −20 … 0 | 0.1 | **−1.0** | dBFS | [L4/我定的数] |
| `lim_release` | 10 … 1000 | 1 | 50 | ms | [L4/竞品:限幅仅阈值+释放] |
| `lim_lookahead` | {0, 1, 2} | — | **1** | ms | ⚠ **直接进延迟预算**,见 §5 |

---

## 4. D4 参数表(提案,待与 D2 对齐)

### ③ 分频
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `xo_hp_en` / `xo_lp_en` | {关, 开} | — | 关 | — | — |
| `xo_hp_fc` / `xo_lp_fc` | 20 … 20000 | 1 | 80 / 20000 | Hz | [L4/我定的数] |
| `xo_type` | {Butterworth, Linkwitz-Riley, Bessel} | — | Linkwitz-Riley | — | [L4/竞品] |
| `xo_slope` | {12, 24, 36, 48} | — | 24 | dB/oct | [L4/竞品给 6…48;**LR 仅偶数阶可用**] |
| `xo_polarity_auto` | {关, 开} | — | **开** | — | **见下,新增项** |

> **⭐⭐ 新增项 `xo_polarity_auto` 的理由(EXP-3 实测)**:
> LR 分频器的 LP 与 HP 求和是否平坦,**取决于阶数**:
> | LR 阶数 | 12 (LR2) | 24 (LR4) | 36 (LR6) | 48 (LR8) |
> |---|---|---|---|---|
> | 阶数 mod 4 | 2 | 0 | 2 | 0 |
> | **求和极性** | **反相** | 同相 | **反相** | 同相 |
> | 正确极性下求和偏离(理想) | 2.0e−11 dB | 3.7e−11 dB | 6.3e−11 dB | 4.0e−11 dB |
> | 正确极性下求和偏离(Q4.27) | 0.0011 dB | 0.0023 dB | 0.0036 dB | 0.0023 dB |
> **阳性对照**:把 LR4 的极性人为写反 ⇒ 求和出现 **87.72 dB 深谷** [L2/实测 EXP-3p]。
> **⇒ 若界面允许用户自由选斜率而不自动处理极性,用户在 12/36 dB/oct 上会得到
> 分频点的深谷,而他不会知道原因。** ⇒ 建议默认自动,并在界面显示当前极性。
> ⚠ 竞品手册未提及该项 [L4/竞品手册],**这是本件相对竞品的一处增量**。

> **⚠ `xo_slope` 只列 12/24/36/48**:LR 的定义是「Butterworth 的平方」⇒ **只有偶数阶存在**。
> 竞品列了 6/18/30/42 [L4/竞品手册],那几档**在 LR 类型下没有定义**(只能是 BW/Bessel)。
> ⇒ **界面须按类型联动斜率选项**,否则会生成一个数学上不存在的滤波器。
> ✅ **实现已跟上(r8)**:`chdsp_bq_design_xover2()` 支持 BW/Bessel 的 1..8 阶(含奇数),
> LR 仍只接受偶数阶并返回 `CHDSP_BQ_ERR_ORDER`。[L2/宿主实测 CHK-X3/X5]

> ### ⛔⛔ 选 Bessel 时,LR 的极性规则**不适用**(r8 新发现,须进界面)
> `xo_polarity_auto` 那条「阶数 mod 4 == 2 ⇒ 反相」是**LR 专有**的:
> LR = BW² 是全通求和分频,所以选对极性后求和**精确平坦**(实测 0.0000 dB)。
> **Bessel 不是全通求和分频** —— 两个相位都试过,求和仍不平坦:
>
> | 类型 | 同相求和 max\|偏离\| | 反相求和 max\|偏离\| |
> |---|---|---|
> | LR2 / LR4 / LR6 / LR8 | 51.59 / **0.0000** / 42.05 / **0.0000** dB | **0.0000** / 45.57 / **0.0000** / 39.55 dB |
> | **BW 奇数阶(1/3/5)** | **0.0000 dB** | **0.0000 dB** |
> | **Bessel 2 / 4 / 6 / 8** | 8.36 / **2.79** / **2.18** / **3.00** dB | 2.68 / **1.69** / 5.98 / 25.38 dB |
>
> **⇒ 三条产品结论**:
> ① **Bessel 无论选哪个极性都不平坦**(最好的一格 = 4 阶反相 1.686 dB)⇒
>    界面**不得**对 Bessel 复用 `chdsp_xover_needs_polarity_flip()`(该函数对非 LR 已返回 0,行为正确)。
> ② **奇数阶 Butterworth 求和恒平坦**(0.0000 dB,与极性无关)—— 这是 BW 奇数阶的经典性质,
>    ⇒ 6/18/30 dB/oct 反而是"不需要极性规则"的那一档。
> ③ **Bessel 的卖点是群延迟平坦,不是求和平坦。** 是否向用户提供由产品定;
>    ⛔ 本件只负责把数摆出来,不替产品裁。⚠ 群延迟平坦度**本轮未测**(见 §8 N-1)。
> [L2/宿主实测 EXP-11,`d34_chain/xover_r8.py` + `results_xover_r8.txt`]

### ④ PEQ ×10 —— 参数同 D3 §3⑥(10 段)
### ⑤ 线性相位 FIR
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `fir_en` | {关, 开} | — | **关** | — | [L4/我定的数] |
| `fir_taps` | {128, 256, 512} | — | 256 | tap | **受延迟预算硬约束**,见 §5 |

> **⚠ 1024 tap 不在候选内**:见 §5,L=64 时它单独就把固定延迟推到 14.32 ms > 12 ms。
> **⚠ FIR 的低频分辨力**:N 抽头的频率分辨力 ≈ fs/N;经验上可控最低频 ≈ 2·fs/N。
> | N | 128 | 256 | 512 | 1024 |
> |---|---|---|---|---|
> | 可控最低频(≈2fs/N) | 750 Hz | 375 Hz | 187 Hz | 94 Hz |
> ⇒ **256 tap 的 FIR 管不到 375 Hz 以下** —— 低频修正仍须靠 IIR。
> ⇒ 产品话术上「FIR 线性相位」不等于「全频段线性相位」,**对外描述须注意**(报 lead)。

### ⑥ 延时
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `out_delay` | 0 … **200** | 1 样本 | 0 | ms | [L4/我定的数,竞品为 1–1200 ms] |
| `out_delay_unit` | {ms, m, ft} | — | ms | — | [L4/竞品] |
| `sound_speed` | 330 … 350 | 0.1 | **343.0** | m/s | @20 °C [L3/教科书] |

> **⭐ 为什么我把上限定在 200 ms 而不是竞品的 1200 ms —— 内存账**
> (每通道缓冲 = 时长 × 48 样本/ms × 4 B,Q4.27):
> | 最大延时 | 单通道 | ×8 通道 | 落在哪一级存储 |
> |---|---|---|---|
> | 100 ms | 19.2 kB | 153.6 kB | L1(640 kB)可容,占 24% |
> | **200 ms** | **38.4 kB** | **307.2 kB** | L1 占 48%,或移 L2 |
> | 500 ms | 96 kB | 768 kB | **超 L1**,须 L2(1024 kB) |
> | 1200 ms(竞品口径) | 230.4 kB | **1843.2 kB** | **超 L2**,须外挂 **L3/DDR3** |
> [L3/解析;存储器容量 L1=640 kB / L2=1024 kB / L3=外挂 DDR3 为 [L2/厂家 datasheet Table 1]]
>
> ⇒ **1200 ms × 8 通道必须走 DDR3**,意味着每样本的延时读写要过外部总线
> ⇒ 须块 DMA、且占 DDR 带宽 ⇒ **属架构/固件决策,不是我能定的**。
> ⇒ **报 lead:延时上限是一个「产品规格 × 存储架构」的耦合决策,请路由架构侧 + CTO。**
> 200 ms = 68.6 m,对会议室/报告厅足够;若要对标竞品的 1200 ms(412 m,延时塔场景),
> 须先定存储架构。

### ⑦⑧⑨ 限幅 / 音箱保护 / 静音
| 参数 | 范围 | 步进 | 默认 | 单位 | L 标 |
|---|---|---|---|---|---|
| `out_lim_thr` | −20 … 0 | 0.1 | −0.5 | dBFS | [L4/我定的数] |
| `out_lim_release` | 10 … 1000 | 1 | 50 | ms | [L4/竞品] |
| `spk_rms_thr` | −40 … 0 | 0.1 | −12 | dBFS | [L4/我定的数] |
| `spk_rms_tc` | 100 … 10000 | 10 | 2000 | ms | [L4/我定的数,音圈热时间常数量级] |
| `spk_peak_thr` | −20 … 0 | 0.1 | −3 | dBFS | [L4/我定的数] |
| `spk_peak_attack` | 0.1 … 10 | 0.1 | 0.5 | ms | [L4/我定的数] |
| `mute_ramp` | 1 … 200 | 1 | 20 | ms | [L4/我定的数,防爆音] |

> **⚠ 音箱保护的全部阈值/时间常数都是 [L4/我定的数]**:真实值取决于所接音箱的
> 功率、音圈热阻、Xmax —— **本项目不知道用户接什么音箱**。
> ⇒ 这些默认值只是**安全的保守占位**,须由集成商按音箱规格设定。
> ⇒ **建议 D2 把它们标为"必须现场设定"而不是"出厂默认可用"。**

---

## 5. ⭐ 延迟预算(PRD §一.4:全链路 < 12 ms)

### 5.1 固定项(不可配置)
| 项 | 值(样本 @48 kHz) | ms | 来源 |
|---|---|---|---|
| ADAU1979 ADC 群延迟 | 22.9844 | 0.479 | [L2/厂家 datasheet] |
| ADAU1962A DAC 传播延迟 | 25.0 | 0.521 | [L2/厂家 datasheet] |
| **转换器合计** | **47.9844** | **0.99967** | |

### 5.2 块 I/O + FIR(三联数:抽头 → 群延迟 → 预算占用)
固定合计 = 转换器 + 块 I/O(2L)+ FIR 群延迟 (N−1)/2

| 帧长 L | 块 I/O | FIR 128 | FIR 256 | FIR 512 | FIR 1024 |
|---|---|---|---|---|---|
| 32 | 1.333 ms | 3.656 ms(余 +8.344) | 4.989(+7.011) | 7.656(+4.344) | **12.989(−0.989)** |
| **64** | 2.667 ms | 4.989(+7.011) | 6.323(+5.677) | **8.989(+3.011)** | **14.323(−2.323)** |
| 128 | 5.333 ms | 7.656(+4.344) | 8.989(+3.011) | 11.656(+0.344) | **16.989(−4.989)** |

> **⇒ FIR 1024 tap 在任何帧长下都超预算。**
> **⇒ 推荐工作点:L = 64 + FIR ≤ 512 tap,余 3.011 ms 给 IIR / AEC / 矩阵 / 用户延时。**
> ⚠ 块 I/O 按 **2L**(乒乓双缓冲)计;若固件用单缓冲 + 及时处理可降到 ~L,
> **该口径归 platform-fw / 架构侧**,本件按保守的 2L 记。

### 5.3 ⭐⭐ IIR 群延迟 —— 这一项通常没人记,而它可能是最大的一项
分频器的群延迟随频率变化,**在 fc 附近最大**(高 Q 节)或**在带下沿最大**(LR2)。
两轨实测(逐节 group_delay 求和 vs 整体相位差分,最大差 **0.0007 样本**):

| 分频 | fc = 80 Hz | fc = 120 Hz | fc = 2000 Hz |
|---|---|---|---|
| LR2(12 dB/oct) | 3.744 ms | 2.581 ms | 0.158 ms |
| LR4(24 dB/oct) | 6.792 ms | 4.528 ms | 0.272 ms |
| **LR8(48 dB/oct)** | **15.555 ms** | **10.370 ms** | 0.627 ms |

> **⇒ LR8 @ 80 Hz 单这一个模块就是 15.555 ms,已超 12 ms 全链预算。**
>
> **⚠⚠ 但这句话必须带口径,否则会被读错(LESSONS B-3:先核量的类别)**:
> 群延迟是**频率的函数**。上表是 20 Hz–20 kHz 内的**最大值**,它出现在 fc 附近的窄带里;
> 同一个滤波器在 1 kHz 处的群延迟接近 0。
> ⇒ **「全链路处理延迟 < 12 ms」这句规格,必须先定义它测的是哪个量**:
> | 口径 | LR8 @80 Hz 是否满足 |
> |---|---|
> | (a) 宽带/中频段的处理延迟(转换器 + 块 + FIR) | ✓ 满足(8.989 ms) |
> | (b) 20 Hz–20 kHz 内**任意频点**的群延迟上界 | ✗ 不满足(15.555 ms) |
> ⇒ **这不是「规格做不到」,是「规格没定义清楚」。⇒ 报 lead 交 CTO 定口径(§7)。**
> ⇒ 若取 (b) 口径,则**低频段的高阶 LR 分频须限档**(如 fc < 150 Hz 时最高 LR4)。

### 5.4 ⭐⭐ 延迟逐项表(交架构侧配平)—— 我这一侧,每项标可否降

> lead 已把三条线并起来看得 ≈27 ms(预算 12 ms),并路由架构侧出**组合可行性表**。
> **本表是我这一侧的输入。⛔ 我不做那张组合表。**
> 全部为 [L2/宿主实测,两轨] 或 [L2/厂家锚点]。

**(A) 现状逐项(典型设置,20 Hz–20 kHz 内最大群延迟)**

| 模块 | 最大群延迟 | 峰值频率 | 可否降 |
|---|---|---|---|
| 转换器 ADC+DAC | **0.99967 ms** | 全频恒定 | ⛔ **不可降**(器件固有) |
| 块 I/O(L=64,2L 乒乓) | 2.667 ms | 全频恒定 | ✅ 见 (D) |
| D3 HPF BW12 @80 Hz | 3.396 ms | 51.5 Hz | ✅ 但与隆隆抑制权衡(§1③ 表) |
| D3 PEQ ×8(典型设置) | 3.771 ms | 61.7 Hz | ⚠ 取决于用户设置,非固定成本 |
| **D4 分频 LR8 @80 Hz** | **15.555 ms** | 71.1 Hz | ✅ **降幅最大,见 (B)** |
| D4 PEQ ×10(典型设置) | 3.647 ms | 61.7 Hz | ⚠ 同上 |
| D4 FIR 512 tap | 5.323 ms | 全频恒定 | ✅ 见 (C) |
| `lim_lookahead` | 1.000 ms | 全频恒定 | ✅ 降到 0,代价见下 |

**(B) 分频档位(相对 LR8 @80 Hz)—— 我这一侧最大的一块可降量**

| 档位 | 群延迟 | 省 | 代价 |
|---|---|---|---|
| LR8 @80 Hz(现状) | 15.555 ms | — | — |
| LR8 @120 Hz | 10.370 ms | **+5.185** | 分频点上移 |
| LR8 @200 Hz | 6.223 ms | **+9.333** | 分频点上移 |
| **LR4 @80 Hz** | 6.792 ms | **+8.763** | 斜率 48→24 dB/oct |
| LR4 @120 Hz | 4.528 ms | **+11.027** | 斜率↓ + 分频点↑ |
| LR4 @200 Hz | 2.717 ms | **+12.838** | 同上 |
| LR2 @80 Hz | 3.742 ms | **+11.813** | 斜率 48→12 dB/oct |
| LR2 @200 Hz | **1.576 ms** | **+13.980** | 最省,但分频保护最弱 |

**(C) FIR 档位(相对 512 tap)**

| 档位 | 群延迟 | 省 | 代价(可控最低频 ≈2fs/N) |
|---|---|---|---|
| 1024 tap | 10.656 ms | −5.333 | 94 Hz;**但已超 12 ms 预算,不在候选** |
| 512 tap(现状候选上限) | 5.323 ms | — | 187 Hz |
| 256 tap | 2.656 ms | **+2.667** | 375 Hz |
| 128 tap | 1.323 ms | **+4.000** | 750 Hz |
| 关闭 | 0 ms | **+5.323** | 无线性相位能力 |

**(D) 其它**

| 项 | 省 | 代价 |
|---|---|---|
| 块 I/O L=64 → L=32(仍乒乓) | +1.333 ms | 中断率翻倍 ⇒ 算力开销↑(**不是我的域**) |
| 块 I/O 乒乓 → 单缓冲(L=64) | +1.333 ms | ⚠ **口径归 platform-fw**,我按保守的 2L 记 |
| `lim_lookahead` 1 → 0 ms | +1.000 ms | 限幅改**反馈式**,过冲不受控 ⇒ 会削波 |
| 转换器 | 0 | ⛔ 不可降 |

### 5.5 ⚠ 一条我提了又撤回的更正 —— **给架构侧配平表的**

**我在 r4 提出**:群延迟是频率的函数,把各项**最大值相加**是把不同频率上的峰硬凑到一起
⇒ 会系统性高估 ⇒ 配平表应逐频相加。

**r5/r6 实测,证伪条件命中,按预注册分支【撤回该更正】**:

| 配置 | 各自最大值相加 (a) | 逐频相加最大 (b) | 高估 (a)−(b) |
|---|---|---|---|
| ① 分频 LR8 @80 Hz(峰聚集在 51–71 Hz) | 26.370 ms | 25.632 ms @63.0 Hz | **0.738 ms** |
| ② 分频 LR8 @2 kHz(峰分散到 1785 Hz) | 11.442 ms | 11.147 ms @61.6 Hz | **0.295 ms** |

> **⇒ 结论(一句可执行的话,交架构侧)**:
> **简单相加可用,保守量 < 0.8 ms。⛔ 不要指望峰错开带来抵消。配平表按简单相加做即可,不必逐频卷积。**
>
> **⇒ 为什么我原来的假设错了**:我以为"峰分散 ⇒ 高估变大"。实测**反而更小**(0.295 < 0.738)——
> 因为被移开的那一项(分频 @2 kHz)**本身贡献就小**(0.627 ms),移开它不产生大缺口。
> **⇒ 而低频块的群延迟峰天然都在低频,它们本来就对齐。**
> **⇒ 这意味着 lead 那个 ≈27 ms 的和【基本是对的,不是高估】。** 这比我原来的更正更值得知道。

### 5.7 ⭐⭐ 参考配置(REF) vs 最坏可配置(WORST)—— 急件产出

> **⚠ 先更正一条前提(以盘面为准)**:lead 来件称我上轮那三项是「各块最坏设置下的峰值」,
> 举例「HPF 3.396 ms 是把高通设在 51.5 Hz」「PEQ×8 是 8 段全压在 61.7 Hz 高 Q」。
> **两条与 `d34_analysis.py:446-452` 都不符**:HPF 是 `bw_hp(80.0, 0.7071)`(fc = 80 Hz = 出厂默认),
> PEQ×8 是 63/160/400/1k/2.5k/4k/8k/12.5k、Q ∈ [0.7, 2.0]、增益 ±3–6 dB(**已分散、中等 Q**)。
> **51.5 / 61.7 Hz 是群延迟曲线的【峰值出现频率】,不是【参数设置】。**
> ⇒ 但 lead 的诉求成立:我确实**没给「典型」的定义与理由**,也**没给最坏可配置**。下面补。

#### 5.7.1 REF 的定义与理由(我定,带出处)

| 项 | 设置 | 依据 |
|---|---|---|
| HPF | BW12 @ 80 Hz | = 出厂默认(§3③) |
| 输入 PEQ×8 | **启用 4 段**:45 Hz Q=8 −6 / 72 Hz Q=6 −5 / 250 Hz Q=1.4 −4 / 3.15 kHz Q=1.0 +3 | 见下 |
| 输出 PEQ×10 | **启用 5 段**(上列 + 8 kHz Q=0.7 −3) | 同 |
| 分频 | LR4 @ 80 Hz | = 出厂默认 24 dB/oct |
| FIR | 关 | = 出厂默认 |
| lookahead | 1 ms | = 出厂默认(架构侧已判否降 0) |

> **⭐ 为什么 REF 必须含两段低频窄 Q 陷波 —— 这条改变了"典型"的定义**:
> **PRD §五.2 明文「开机自动扫描房间频响,一键自动修正房间驻波」。**
> ⇒ **低频窄 Q 陷波不是"用户不会配出来的极端",是本产品的功能会【自动】配出来的。**
> ⇒ 房间轴向模式 `f = c/(2L)`,L = 5–10 m ⇒ 前几阶落在 34–103 Hz [L3/解析]
> ⇒ 45 / 72 Hz 是小会议室的典型模式频率。**不含它们的 REF 不代表真实部署。**

#### 5.7.2 两组数(全链求和口径,lead 已裁定)

| | REF | WORST |
|---|---|---|
| D3 HPF | 3.396 ms @51.5 Hz | 29.230 ms @20.1 Hz |
| D3 PEQ | **5.895 ms** @40.1 Hz(4 段) | ⛔ 不可数值确定 |
| D4 分频 | 6.792 ms @51.5 Hz | 58.459 ms @20.1 Hz |
| D4 PEQ | 5.904 ms @40.1 Hz(5 段) | ⛔ 不可数值确定 |
| 固定项(转换器+块+FIR+lookahead) | 4.666 ms | 10.989 ms |
| **全链(20 Hz–20 kHz 口径)** | **26.407 ms** | **量级 ≈ 14 秒** |

> **⚠ WORST 的群延迟数值【不得引用】**:其极点半径 r ≈ 1−5e−5,`|D(e^jω)|` 落到 float64 精度以下,
> **两条独立实现的轨在同一处一起失去精度**(差 61653 样本)⇒ 按预注册,数值不得报出。
> 只报**解析量级**:高 Q 二阶节峰值群延迟 ≈ `2Q/ω₀` [L3];Q=50 @20 Hz ⇒ 796 ms/节;
> 18 个峰型节 ⇒ **量级 ≈ 14 秒**。REF 侧两轨差 **0.1785 样本**,其数可报。
>
> **⇒ ⭐ 由此得一条给架构侧的实现约束:运行时校验的拦截线【不应基于群延迟数值】
> (极端配置下算不准),应基于【参数本身】(f₀ 与 Q 的组合上限)。**

#### 5.7.3 ⛔ 我上轮那条判断错了:段数不是主因,**Q 才是**

REF 只启用 4 段,群延迟 **5.895 ms**,反而**大于**上轮 8 段全开的 3.771 ms。
敏感度分离实测(20 Hz–20 kHz 口径):

| 变量 | 扫描 | 群延迟变化幅度 |
|---|---|---|
| 低频两段的 **Q**(1.4 → 10,段数固定 4) | 3.170 → 6.831 ms | **3.759 ms** |
| **段数**(1 → 8,Q 固定 1.4,频点分散) | 3.320 → 3.788 ms | **0.469 ms** |
| | | **比值 8.02×** |

> **⇒ 群延迟由【最低频那一段的 Q】决定,不由【启用了几段】决定。**
> **⇒ lead 来件里「PEQ 用 3–4 段 ⇒ 更省」的方向是反的**,少几段几乎不省;降低频 Q 才省。

### 5.8 ⭐⭐⭐ 决策数:REF 恰好等于 12 ms 的评价频带下沿 = **105.2 Hz**

| 评价频带下沿 | IIR 峰 | 全链 | 余量 | |
|---|---|---|---|---|
| 20 Hz | 21.741 ms | 26.407 ms | −14.407 | ✗ |
| 80 Hz | 12.279 ms | 16.945 ms | −4.945 | ✗ |
| 100 Hz | 8.168 ms | 12.835 ms | −0.835 | ✗ |
| **105.2 Hz** | — | **12.000 ms** | **0** | ← **f_lo\*** |
| 125 Hz | 5.131 ms | 9.798 ms | +2.202 | ✓ |
| 150 Hz | 3.452 ms | 8.118 ms | +3.882 | ✓ |
| 200 Hz | 0.992 ms | 5.658 ms | +6.342 | ✓ |

**⇒ 决定整条延迟线的不是链上任何一个模块,是【规格的评价频带下沿】。**
**⇒ 代价(EXP-11b)**:被排除的 20–105 Hz 内,REF 群延迟最大 **21.741 ms**。
   这是"我们同意不管的那一段有多糟"的数,**交 CTO 权衡**。⛔ 不是我能定的。

### 5.9 ⭐ 拒绝率:定量代理 + 定性判断(lead 问的那件)

**做法**:从 REF 出发逐段追加「45–250 Hz、Q=8、−6 dB」的房间陷波(= PRD §五.2 会自动生成的那类),
看加到第几段撞 12 ms。

| 评价频带下沿 | 还能追加几段 | 判断 |
|---|---|---|
| 100 Hz | **0 段** | ⛔ **不可用** —— 用户/自动 EQ 加任何一段就被拒 |
| 125 Hz | 3 段 | ⚠ 勉强,重度房间修正会撞 |
| **150 Hz** | **≥8 段**(我的追加清单 8 条耗尽仍未撞) | ✓ 拒绝风险低 |
| 200 Hz | ≥8 段(同上) | ✓ |

> **⇒ 定性回答**:**评价频带下沿若定在 100 Hz,拒绝率不可接受**(REF 本身零余量,
> 而 PRD §五.2 的自动房间 EQ 会自己去加陷波 ⇒ **产品的自有功能就会触发拒绝**)。
> **下沿 ≥150 Hz 时,正常调音撞拒绝的概率低**。
> ⚠ 「≥8 段」是我的追加清单长度上限,不是真实上限 —— **⛔ 不得读作"最多 8 段"**。
> ⚠ 这是**定量代理**,不是真实拒绝率:真实拒绝率需要用户操作分布,**本项目没有该数据**,我不编。

### 5.10 可降级档与两条已裁定项(应 lead 要求写明)

**① 线性相位 FIR = 明确的可降级档**(架构侧判定被挤掉的是 FIR 而非分频)

| 档 | 群延迟 | 相对 512 tap 省 | **放弃什么** |
|---|---|---|---|
| 512 tap | 5.323 ms | — | — |
| 256 tap | 2.656 ms | +2.667 ms | 可控最低频 187 → **375 Hz** |
| 128 tap | 1.323 ms | +4.000 ms | → **750 Hz** |
| **关闭** | 0 ms | +5.323 ms | **无线性相位能力**(相位校正质量,可降级) |

> 分频关系到**能不能驱动双分频音箱(硬功能)**;线性相位关系到**相位校正质量(可降级)**。
> ⇒ 降级顺序:**先降 FIR,后动分频**。

**② LR2(12 dB/oct)只能作可选低延迟档,⛔ 不得作默认**(架构侧核出两条代价,记录于此)
- **安装依赖的失效点**:极性规则「阶数 mod 4 = 2 ⇒ 须反相」——**LR2 与 LR6 正落在该档,LR4/LR8 不落**。
  按 DEC-0020「按任意器材设计」不能假设安装方接对极性 ⇒ 分频点会出现深谷(阳性对照实测 **87.72 dB**)。
- **冲程保护弱**:12 dB/oct 对小口径单元的低频冲程保护不足。
- ⇒ **默认维持 LR4(24 dB/oct);LR2 作可选低延迟档,界面须提示极性依赖。**

**③ `lim_lookahead` 1 ms 不动**(架构侧判否)
- 降到 0 ⇒ 限幅改反馈式 ⇒ 过冲不受控;而输出限幅器的职责是保护高音单元
  ⇒ **一个会过冲的保护器,在它唯一该起作用的那一刻失效。**
- ⇒ 那 1 ms 从可降清单中**移除**,§5.4(D) 的该行作废。

### 5.6 其它可配置项
| 项 | 延迟 | 备注 |
|---|---|---|
| `lim_lookahead` | 0 / 1 / 2 ms | 默认 1 ms,**直接计入预算** |
| `in_delay` / `out_delay` | 用户设定 | 用户显式加的,不算"处理延迟",但**须在界面上与总延迟一起显示** |

---

## 6. D-4:级联结论(⛔ 不许只验单级)

| 被测 | 单级 | **级联** | 判据 | 结果 |
|---|---|---|---|---|
| LR 分频求和平坦度(Q4.27) | — | 最坏 **0.0036 dB**(LR6 @80 Hz) | PRD ±0.3 dB | ✓ 余量 83× |
| LR 高通 **DC 增益** | 精确 0(结构约束量化) | 精确 0 | ≤ −120 dB | ✓ |
| LR 低通 **Nyquist 增益** | 精确 0 | 精确 0 | ≤ −120 dB | ✓ |
| 8 段非平坦 PEQ 级联(量化 vs 理想) | — | **0.003859 dB** | ±0.3 dB | ✓ 余量 78× |
| **量化噪声底**(⛔ **各节增益 = 1**) | −173.35 dBFS | D3(9 量化器)**−163.81**;D4(19)**−160.56**;合计(28)**−158.88** | ≤ −106 dBFS | ✓ 余量 52.9 dB |
| **量化噪声底**(⛔ **最坏合法增益**:8 段全 +15 dB 同频) | 同上 | 8×PEQ 段 **−91.64**;D3 输入链 **−76.97** | ≤ −106 dBFS | ⛔ **不过,差 14.36 dB** |
| **群延迟** | 见 §5.3 | LR8@80 Hz **15.555 ms** | 见 §5.3 口径问题 | ⚠ **须定口径** |

> ⚠ 噪声底一行**继承任务一的门禁状态**(其单节值 −173.35 dBFS 尚未过门)⇒ ⛔ 不得独立引用。
>
> **⛔ 整改留痕(2026-08-04 · channel-dsp 实例 #2 · critic MAJOR-2)**:原表只有「各节增益 = 1」那一行,
> 且直接标 ✓ 余量 52.9 dB。而 D34 §6.2-4 **明文允许**「8 段 PEQ 全部 +15 dB 叠在同一频率」——
> 该配置下噪声被后级放大 **72.68 dB**(D3 链 85.59 dB)⇒ **✓ 变 ⛔**。
> **⇒ 这一格是本件的,不是前置件的** —— 增益结构归 D3/D4。
> **⇒ 待处置(⛔ 本轮未做,须 lead 路由)**:①把大增益放到链尾,或 ②限制**累计**增益(不是单段增益),
> 或 ③在参数字典里对「同频多段同向大增益」加约束。三选一须与 D2 / architect 一起定。
> 复算件:`d34_chain/check_noise_chain_r5.py` + `results_noise_chain_r5.txt`。
> ⚠ 增益 / 门 / 压限 / 延时**不引入新量化器**:增益并入相邻节的累加器;
> 门与压限只做一次增益相乘(1 次窄化,已计入其上游节);延时是纯搬运。

---

## 7. ⚠ 须由 lead 路由的四条

1. **「全链路处理延迟 < 12 ms」的口径未定义**(§5.3)。宽带延迟 vs 任意频点群延迟上界,
   两种读法对 LR8 低频分频给出相反结论。**这是 PRD 规格的定义问题,须 CTO 定。**
   若取"任意频点"口径,须对低频高阶分频限档。

2. **输出延时上限 200 ms vs 竞品 1200 ms 是存储架构决策**(§4⑥)。
   1200 ms × 8 ch = 1.84 MB **超 L2(1024 kB)**,须外挂 DDR3 + 块 DMA。
   ⇒ 请路由架构侧 + platform-fw + CTO。

3. **「FIR 线性相位」的对外话术**(§4⑤)。256 tap 管不到 375 Hz 以下;
   要覆盖到 100 Hz 需 ≥1024 tap,而那超延迟预算。
   ⇒ **对外规格不宜写成"全频段线性相位"**,请 CTO 确认话术。

4. **`xo_slope` 的 6/18/30/42 dB/oct 在 LR 类型下不存在**(§4③)。竞品界面列了这些档
   [L4/竞品手册],若我们照抄会生成数学上不存在的滤波器。建议界面按类型联动。

---

## 8. 显式砍掉 / 待办(⛔ 不静默省略)

| # | 项 | 状态 |
|---|---|---|
| Y1 | 全部 C 实现与 bit-exact | 本轮交设计 + 参数表,不含模块实现 |
| Y2 | 一切算力 / 周期数 | **零结论**,待 W1-C / D13 |
| Y3 | 动态处理器的真实素材验证 | 本轮只用合成素材做**链序**论证;压限效果验证属 D14 |
| Y4 | AEC / ANC / AFC / AGC / automixer 的位置 | 只给我这一侧的约束,⛔ 未替对方定 |
| Y5 | 参数默认值的听感标定 | 全部 [L4],进台账待标定 |
| Y6 | PRD §二.4 "动态参量 EQ" 的「动态」语义 | **PRD 未定义**,待 CTO / 上位机对标澄清 |
| Y7 | Bessel 分频的系数上界 | ✅ **已闭(r8,2026-08-04)**:实扫 1..8 阶 × LP/HP × fc∈[20,20k] ⇒ **max\|b\| ≤ 2**,与 D34 §3.2.0 的 LPF/HPF 解析界一致(换 (f0,Q) 不换结构)⇒ Q4.27 装得下。已实现,见下 Y7-b |
| Y7-b | Bessel **实现路径** | ✅ **已实现,但走的不是逐节 RBJ**:归一化极点表 → 单次预畸 → 双线性。⛔ 逐节 RBJ 对 Bessel **不成立**(各节 ω0 不同 ⇒「逐节各自预畸」≠「整支预畸一次」),照搬会使 8 阶高通与第二轨差 **91.7 dB**(r8 证伪条件 F-2 命中后定位)。两轨(scipy)max\|Δ\| = **0.000000 dB** |
| Y7-c | ⭐ **Bessel 的 LP+HP 求和不平坦** | **新发现,须进参数表**:实测 4 阶最好一相 **1.686 dB**、8 阶最差 **25.4 dB** ⇒ LR 那条「阶数 mod 4 定极性」规则**对 Bessel 不适用**。Bessel 的卖点是群延迟平坦,不是求和平坦 ⇒ 界面选 Bessel 时**不得**套用 `xo_polarity_auto` 的 LR 规则 |
| Y8 | `xo_slope` 36 dB/oct(LR6)的实现 | ✅ **已闭**:LR6 = (3 阶 BW)² = Q=0.5 一节 + Q=1.0 两节,共 3 个双二阶(⛔ 不含一阶节 —— 一阶节平方后就是 Q=0.5 的双二阶)。原描述「含一个一阶节」**不准确**,已更正 |
| Y8-b | **奇数阶 BW/Bessel**(竞品 6/18/30/42 档) | ✅ **已实现(r8)**:`chdsp_bq_design_xover2()` 支持 1..8 阶。⚠ 原实现第一行就 `if (order%2) return ERR_ORDER` ⇒ **本件自己列为合法的档位,实现全部拒绝**。⛔ LR 奇数阶仍拒(LR=BW²,数学上不存在,这不是缺口) |
| Y8-c | ⚠ `butter_q` 曾只对偶数阶正确 | 原式用 **cos**:偶数阶给出**同一 Q 集合、顺序相反**(∴ 当时是对的),**奇数阶不等**(n=3 给 0.5774,正确 1.0)。r8 改 **sin**,并加 CHK-X4 逐位回归证明偶数阶未变 |
| Y9 | 前置件(定点约定)尚未过门 | 本件建立其上 ⇒ **本件的门禁状态不高于它** |

---

## 9. 自验与假绿纪律

| 项 | 结果 |
|---|---|
| **r13 好版本(现行)** | **PASS = 28,FAIL = 0,RETIRED = 10**(退役项原样留痕)⚠ 与 r12 逐项相同 —— 本轮改的是**变异**,不是被测物 |
| **杀伤矩阵**(**r13 重跑,归因已更正**) | `polarity`→EXP-3a/3b ｜ `qcoef`(**只退位宽**)→EXP-2d/3b/4d/9a ｜ `freeq`(**只关结构约束**)→EXP-3c ｜ `hpf_order`→EXP-2a/2c ｜ `xo_order`→EXP-1a。**5/5 变异全部被杀死** |
| ⚠ **而 EXP-4a 没有单缺陷杀手** | 它**只在 qcoef ∧ freeq 同时存在**时才被杀 ⇒ 证据力弱于单缺陷杀手,⛔ 如实记 |
| **阴性对照** | 好版本 FAIL 数 = **0** ⇒ 杀死确由变异引起 |
| **阳性对照** | EXP-3p:极性写反 ⇒ 求和出现深谷 ⇒ EXP-3a/b 不是恒真。⚠ **深度数已撤回**,见下 |
| **两轨交叉核** | 群延迟:逐节求和 vs 整体相位差分,最大差 **0.0007 样本**;EXP-6:数值扫描 vs 解析上界 A² 自洽 |
| **第三轨** | EXP-4d:闭式解析 τ=2/(ωc(1+(ω/ωc)²)) vs 数值,最大相对差 **0.57%** |

> ### ⛔⛔ 整改留痕(2026-08-05 · critic BLOCKER-1 ② / BLOCKER-2 / erratum2)
>
> **(A) 本表原报 r7(21/0/4),而现行是 r12(28/0/10)。** r8–r10 是本件**唯一出现过 FAIL 的一段**,
> 而它整段未被列入配套件清单 ⇒ 「FAIL=0」这个字在两份套件里**含义不同**,而读者无从分辨。
>
> **r8–r10 那 8 条 FAIL 的去向(⛔ 分两类,不得混为一谈;完整记录见 `d34_chain/RETIREMENT_LOG.txt`)**:
>
> | 类 | EXP | 处置 |
> |---|---|---|
> | **甲:有预先写下的分支,按分支执行** | EXP-9b | 命中 PREREG_r8 §3 的【窄 Q 主导】分支 ⇒ 改由 EXP-10c 承接 ✅ |
> | | EXP-9d | 命中 PREREG_r10 §1 分支;根因 = 两轨在极点贴单位圆处**一起**失去 float64 精度 ✅ |
> | **⛔ 乙:改判理由写在跑完之后,没有预注册** | EXP-9c / EXP-10a2 / EXP-10b | 理由「规格作用域未定义 ⇒ 不可判」**成立**,但它是**事后**写下的 |
>
> **⛔ 而 EXP-10b 必须单独点名**:`PREREG_r9 §2` 对它**预先写死**了证伪分支
> (「<2 段 ⇒ 即便换频带口径,日常调音仍会频繁撞拒绝」),实测 **0 段 ⇒ 分支被触发**,
> **而 r11 未执行它**,改用事后理由退役。
> ⇒ 两条理由都不假,但**证据力不同**:预注册分支写在看见结果之前,新理由不是。
> ⇒ ⛔ 本件不替它翻案,交 critic / CTO 判。
>
> **(B) 杀伤矩阵归因更正(BLOCKER-2,已在 r13 重跑)**:`qcoef` 变异**同时注入了两个缺陷**
> (16-bit 系数退化 ∧ 顺手关掉结构约束量化)⇒ 它杀掉的 5 条里,
> **`EXP-3c` 与 `EXP-4a` 实际是被"结构约束被拿掉"杀死的,不是被系数精度退化杀死的**。
> ⇒ 已在 `d34_analysis.py` 里拆成 `qcoef`(只退位宽)与 `freeq`(只关结构约束)两个独立变异。
> **⇒ 而 `hpf_order` / `xo_order` 原先只存在于 docstring 里(零实现)** —— 跑它们一个字节都不变,
> 结果头却照印变异名 ⇒ **会产出一份看起来像"该变异存活"的归档件**。而缺的这两个恰是
> §0 承重表 #1/#3 两条链序结论**唯一可能的变异**。⇒ 已实现:`xo_order` 杀 EXP-1a、`hpf_order` 杀 EXP-2a/2c。
> **⇒ 顺带闭掉 MAJOR-1**(它说 EXP-1a 没有任何变异杀得死)。
> ⚠ 而拆开后暴露一件 critic 未点明的事:**EXP-4a 只在两个缺陷【同时】存在时才被杀** ——
> 它没有单缺陷杀手,证据力弱于单缺陷杀手,⛔ 如实记。
>
> **(C) 阳性对照的深度数已撤回(erratum2)**:`87.72 dB` 与 critic verdict 里的
> `65.86/59.84/56.32/53.82` 同族,**都是网格伪影** —— 错误极性下求和在 fc 处有零点,
> 理想深度 = **−∞**,测到多深只取决于最近网格点离 fc 多近(20001 点→46.03,2000001 点→84.51,**单调不收敛**)。
> ⇒ **阳性对照的结论不变**(它要的是"有零点",不是"多深");⛔ 塌掉的只是那个深度数字。
> ⇒ 判别法:**把网格/窗长/采样率改一倍,这个数会变吗?会变而没写明 ⇒ 它是【器械】的性质,不是【被测物】的。**

**⚠ 五次证伪条件命中 / 判据未达,处置分两类**
〔整改 2026-08-05:原写「处置**全部**是【改被测物 / 另做独立观测 / 按预注册分支撤回】,
⛔ 未改任何判据、未移动任何标杆」—— **那句话对下面这五条成立,而对 r8–r10 那批不成立**
(其中三条是事后改判)。⇒ 原句作用域已收窄,详见上方 (A)。〕:
1. **EXP-1(顺序论证)** —— 宽带峰值测度下两种顺序完全相同 ⇒ 论证被证伪。
   ⇒ 在 r2 预注册里**重新注册被测量**(窄带逐节电平),并**预先写死**:若新测量也无差异,
   则该论证整条删除。结果:31.14 dB,论证成立。
2. **EXP-2b(门行为)** —— 我拍的门限 −45 dBFS 落在两个检测器读数之外 ⇒ 两种链序都不动。
   ⇒ **不改门限充数**,改为扫门限(EXP-2c)+ 扫适用范围(EXP-2d)。
   ⇒ 产出反而更有用:得到一张**可交给集成商的适用范围表**。
3. **EXP-4b(群延迟峰值位置)** —— 判据「峰必在 fc 附近」对 LR2(Q=0.5)不适用。
   ⇒ 用**闭式解析第三轨**独立证实峰在带下沿是物理的(相对差 0.57%),然后**退役该判据**。
4. **EXP-8a(延迟能否简单相加)** —— 我预测高估 ≥1 ms,实测 0.738 ms 未达判据。
   ⇒ 诊断:我选的模块峰值频率全在 51.5–71.1 Hz,本来就对齐 ⇒ 由 EXP-8b 换配置再观测。
5. **EXP-8b** —— 假设「峰分散 ⇒ 高估变大」,实测**反而更小**(0.295 < 0.738)⇒ **证伪条件命中**。
   ⇒ 按 `PREREG_D34_r5_addendum` §2 **预先写死的分支**执行:**撤回我在 r4 提的那条更正**。
   ⇒ **这一条最值得记:我提的是一条要求别人改做法的"更正",而它本身是错的。**
     提更正比提结论更需要证据 —— 因为它会改变别人的做法。

**⚠ 另记一条工具失效(r1 之前的探索阶段,已写进 PREREG_D34_r1 §0 X-1)**:
`scipy.signal.group_delay` 作用在**卷积后的 9 阶多项式**上,给出
「LR8 fc=120 Hz 群延迟 18.59 ms @ **1426.8 Hz**」——**对一个 120 Hz 高通,这在物理上不可能**。
⇒ 改为逐节求和 + 强制两轨。**若照原样报出,§5.3 整段都会是错的。**
