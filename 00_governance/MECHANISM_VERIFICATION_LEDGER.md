# 机制验证台账 —— 「一条没对 broken 输入失败过的护栏,不算护栏,是一段注释」

> **立台缘由**(critic 下岗前的自我怀疑,`W1P_critic_handoff_to_next.md §4.1`):
> **「本轮立了七条新机制。我验证的只是【这些文字写进了文件】。**
> **我没有验证过其中任何一条【跑过、并且能对 broken 输入 FAIL】。」**
>
> **⇒ 而 `r63` 当场证明它是对的**:同一条护栏,换一个计数器就恒绿 —— 而且 `n_notch` 那种写法
> 会把**臂 O 判得比臂 N 更像"NHS 在自选"(8 > 5)**,即**加强版的 B-1**。
>
> **⇒ 本台账的唯一作用:让「已立法」与「已验证」不再在读者眼里长得一样。**

## 状态定义(三档,不许有第四档)

| 档 | 含义 |
|---|---|
| ✅ **已验** | 有 broken 版记录:构造一个坏输入,该机制**确实 FAIL** |
| ⚠ **未验·可执行** | 是代码/脚本,**能**做 broken 版,但**还没做** |
| ⛔ **未验·仅文本** | 是一条**写在文档里的规矩**,**目前没有任何可执行物** ⇒ **它不是护栏** |

## 台账

| # | 机制 | 状态 | owner | broken 版该长什么样 |
|---|---|---|---|---|
| 1 | **护栏 B**(动作计数) | ✅ **已验 3/3** | adaptive-dsp | `r63_invariant_broken.py`:换 `slots`/`frame_i`/`n_notch` 三种错计数器,验证会错误放行臂 O |
| 2 | 护栏 A(工作点向量完整性) | ⚠ 未验·可执行 | **system-architect** | 喂一个**故意漏掉一维**的工作点向量,验证会 FAIL |
| 3 | 第四维(事前/事后限定) | ⚠ 未验·可执行 | **system-architect** | 造一段**只出现在结论、不在预注册**的限定,验证 `grep` 判据会标「事后」 |
| 4 | 第五维(命中≠一致,须指行号+语义类别) | ⚠ 未验·可执行 | **system-architect** | 造一处**命中字串但语义类别不同**的引用(如把"评价频段"指到"被控对象前提"那一行),验证会 FAIL |
| 5 | **D6-t**(限定进量的名字) | ⚠ 未验·可执行 | **adaptive-dsp** | 造一个裸 `dmsg` 变量,验证命名层常驻检查会 FAIL。**⚠ critic 已标「落地层级未核」:改名必须在【产生处】,只在【呈报处】改名等于没改** |
| 6 | **D6-x**(限定串表 lint) | ⛔ **未验·仅文本** | **lead** | 写一段含 `5.67` 而缺限定串的文字,验证 lint 会 FAIL。**⚠ 目前没有可执行的 lint,只有一张表 ⇒ 它现在【不是护栏】** |
| 7 | **D6-s**(评审期冻结快照) | ⛔ **未验·仅文本** | **lead** | **⚠ 目前是一条规矩,没有任何机制。** 可执行形式候选:评审开始时落 `SNAPSHOT.sha256`,任何 commit 前比对 |
| 8 | **D6-w**(写入须核【是否发生】) | ⚠ 未验·可执行 | **system-architect** | 构造一次**锚点不存在**的 `replace`,验证断言会抛;并验**反方向**(区间替换多删) |

## ⛔ 现状小结(不粉饰)

```
✅ 已验        1 / 8
⚠ 未验·可执行  5 / 8   ← 能做而没做
⛔ 未验·仅文本  2 / 8   ← 【它们现在不是护栏,是注释】,且两条都是 lead 的
```

> **⇒ 也就是说:本轮立的机制里,**目前只有八分之一**经受过它自己要求别人经受的那种检验。**
> **⇒ 而两条「仅文本」的都归 lead —— 立规矩的人最容易只立不落,因为立规矩本身就有产出感。**

## 纪律

1. **新立任何机制,当场入本台账**,默认 `⚠ 未验`;
2. **⛔ 「未验·仅文本」的机制,不得在任何 verdict / 交接件 / 对外表述中被称作"护栏"或"已防住"**,只能称"已立规,未落机制";
3. **放行门(critic verdict)复核时,本台账是必查项** —— 一份 verdict 若引用了未验机制作为"已闭"的依据,该引用无效。
