# 评审锁(D6-s)—— 列在这里的路径在 critic 评审窗口内【不得 commit】,由 pre-commit 强制
# ⚠⚠ 生命周期硬绑 verdict:**verdict 出具 = 窗口关闭 = 当场释放**。⛔ 不靠"记得释放"
#     (lead 犯过一次:锁了 D3D4 后 verdict 已出却忘了释放,又在该文件上派了活 ⇒ 钩子把自己派的活拦下)
# ⚠ 只锁【没有并发写者】的件;有并发写者的改用「critic 钉 tag 评审快照」
#
# ══════════════════════════════════════════════════════════════
# ⛔⛔ 2026-08-05 新增:释放【必须带通知】,否则等于没释放
# ══════════════════════════════════════════════════════════════
# 事故(已发生,本文件是当事件):
#   D3/D4 的锁在 08-04 verdict 出具时已释放,而**受它约束的 `channel-dsp-2` 从未被告知**。
#   ⇒ 它在此后【每一轮】的报告里都写「锁遵守:D3D4_CHANNEL_CHAIN_v0.1.md 与 d34_chain/ 一个字没动」,
#     而 lead 每一轮都读过去了。
#   ⇒ ⇒ 后果:那份设计件顶着 critic 的 **FAIL(2 BLOCKER / 5 MAJOR / 7 minor)** 躺了一整天,
#     而当天全部 C 实现都建立在它之上。
#
# ⭐ 机制(与本项目 2026-08-05 全库六次限定脱落同型,只是载体是【锁】):
#   **状态变了(锁释放了),而受它约束的人没被通知。**
#   ⇒ 释放这个动作发生在【本文件】里,而被约束者在【它自己的会话】里 —— 两者没有连接。
#   ⇒ ∴ 这不是"忘了通知",是【释放动作与通知动作之间没有触发器】。
#
# ⇒ ⇒ 纪律(三条,缺一不可):
#   ① ⭐ **上锁时必须同时写下【谁被这把锁约束】** —— 空着 = 不许上锁。
#      理由:不知道约束了谁,就不可能在释放时通知到人;而"到时候想得起来"是运气。
#   ② ⭐ **释放 = 改本文件 + 当场通知①里列的每一个名字。两个动作是一件事,⛔ 不是两件。**
#      ⇒ 只改文件不通知 = 锁在纸上开了,在人身上还锁着 = **本次事故**。
#   ③ ⚠ **被约束者若连续两轮报告里出现"锁遵守"而 lead 未回应 ⇒ 由被约束者主动问一次。**
#      ⇒ 这是给 ② 的兜底:①② 都是 lead 的动作,而 lead 会漏(已证)。
#
# ⚠ 而 ③ 是【兜底不是替代】:靠被约束者主动问,等于把成本推给受害方 ⇒ ①② 仍是主责。
#
# ══════════════════════════════════════════════════════════════
# 当前锁
# ══════════════════════════════════════════════════════════════
# 当前锁:无
#
# 沿革(⛔ 保留,不删 —— 释放记录本身是留痕):
#   D3/D4        verdict 已出(2026-08-04)⇒ 释放
#                ⚠ 而释放【未通知 channel-dsp-2】⇒ 见上文事故;2026-08-05 已补通知并派整改
#   02_impl      改用 tag 快照,不锁工作树
#   W1P_CLOSEOUT verdict + addendum 已出 ⇒ 释放
